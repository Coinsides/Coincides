> **From**: fable
> **To**: codex
> **Status**: ready(两层制;13.3 单 0=板 MVP 侦察,只读)
> **日期 (Date)**: 2026-09-08
> **性质**: 只读侦察单(⛔ 改任何产品代码)

# 13.3 · 单 0 · K-0 侦察(板/魂/书记官业务面)

## 〇 · 上游(先读,顺序)

1. 段 plan:`plans/v13-3-board-mvp-plan.md`;
2. 图二全文(板/魂/钢骨相,本段图纸):`analysis/2026-09-07-board-data-model-design.md`;
3. 图二 §九 的 13.3 拆单必做清单(本单主体)。

## 一 · 四题(文件:行号级证据)

1. **数据层对表**:迁移编号续位现状;`content_mounts`(035)机制对 board_members 的可复用面(mount 语义/几何列/身份键异同);**purposes(044)三遗物处置的消费者全名单**——routes/services/validators/`courseLifecyclePolicies` 等字面量登记面、client PurposeFrame 消费面(runtimeDataTypes/purposeService/仓储)逐一列点,标注"放宽 project_id/退役 note_id 语义/退役 is_note_default"各自会碰到谁;
2. **板 UI 挂点**:路由与页面骨架的挂法选项(App.tsx 路由族/现有 page 骨架可仿面);home 与侧栏的板入口位候选;双击进纸的跳转缝现物(openAgentWithContext 同族机制/现有 note 导航);板视口可复用件(旧 canvas 视口控制器族退役件中哪些可正当征用——⛔ 与退役禁令冲突的列明);
3. **书记官业务面挂点**:board/purpose 新路由挂 recordEvent 的中间件包装方案(与既有事务边界的关系;actor 从认证态取、channel=路由名的实现位;verb 枚举现有 board_created/mounted/unmounted/purpose_created 够用性核对);
4. **画笔与准备区搬迁**:板上自由笔迹的存储候选(canvas_objects 族复用 vs board 层新件);准备区画物/摆阵行→board_members 的搬迁动作数据路径。

## 二 · 报告与回执

报告 `docs/agent-ops/analysis/2026-09-08-v13-3-s0-recon.md`(标准状态头,四题分节,末节拆单建议:单 1/2/3 先后与最小改动面)。工单末尾 apply_patch 追加 ## Result。⛔ 改产品代码;⛔ 用户库;⛔ commit;⛔ 读 .env;⛔ 打印 key。

## Result

2026-09-08，Codex 侦察员：单 0 只读侦察完成，报告与本回执交 HQ；非 13.3 施工完成或放行。基线 HEAD=55b555d，分支 fable/v2-bn12-exoskeleton。

- 报告：[2026-09-08-v13-3-s0-recon.md](../analysis/2026-09-08-v13-3-s0-recon.md)。含标准状态头、四题文件/行级证据、生产 Purpose 消费者矩阵、notes 引用完整性候选、末节单 1 → 单 2 → 单 3 的最小改动面。
- 读序：段 plan 实际位于 handoffs/plans；纠正首次路径定位失败后，按 plan → 图二全文 → §九 重按顺序完成。CodeGraph CLI/MCP 不可用，rg 不在 PATH，回退限定源码检索；三项只读证据核查并行，未派产品施工。
- 关键现物：迁移最高 056、下一候选 057；events/recordEvent 已由 054 落地。044 真实 course_id 已可 NULL，project_id 是 DTO 别名；旧 GET 补默认魂与 PUT 维护名单仍活。旧视口有界；准备区为 tray placement，画物与四种 board member 的映射未定。
- 待拆单确认：旧魂解除 note 随葬与默认机制的承接、缓存编译/旧 UI writer 退出、板画物/范围身份和几何保全、CRUD/隐式 unmount 事件范围；报告仅举证与列候选，未改上游判决。
- 实跑：只在 new Database(':memory:') 中执行生产 054 与 recordEvent；4 个指定 verb、动作/事件一起提交与回滚、两个 append-only 触发器均通过，退出码 0。输出：S0_RECON_MEMORY_PASS database=:memory: migration=054 triggers=2 verbs=4 commit=PASS rollback=PASS append_only=PASS。
- 文档核查：报告完整路径引用文件与行号范围检查通过；git diff --check 通过。没有把引用范围检查冒充所有结论的动态验证。
- 未做：产品代码、任何用户库或磁盘 DB、迁移数据快照/.env/凭证读取、应用启动/浏览器/3001/5173、安全类测试、commit/push/PR。未跑 typecheck/build 或 verify:v2-bn8-runtime：本单仅侦察，无产品变更；不以本回执豁免后续施工验证门。
- 本次只写报告和本工单末尾 Result；开工已有的未跟踪文件未改动。工单原状态头与正文保留，按用户本次要求只追加回执。
- numstat：报告新增 +277/-0；本工单末尾追加 +14/-0；合计 +291/-0。新报告未暂存，使用 git diff --no-index 对空文件计数；工单使用 git diff --numstat 计数。
