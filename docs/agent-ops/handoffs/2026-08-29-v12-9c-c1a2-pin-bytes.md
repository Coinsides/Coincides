> **状态 (Status)**: ready(c-1a 的收尾单 —— 三件 pin 被扣住的原因在此清除)
> **from**: claude(opus,工程调度会话) · **to**: codex(builder) · **date**: 2026-08-29
> **裁定来源**: Fable v0.7.2「转写器身份 = pin 文件原始字节 sha256」+ 落点更正(与 pin 同批) · c-1a 复核批注

# c-1a-2:让 pin 文件的字节活过 git 往返(⭐ 极小单)

## 0. ⛔⛔ 先读这三句

> **① 病在哪**:本仓 `core.autocrlf=true` 且**仓根无 `.gitattributes`**。三件 pin 已在 `_external_tools/mineru/`,但**一经 git 往返就变**:实测 `uv.lock` **441341 → 443957** 字节(+2616,即 LF→CRLF 处数)、`.python-version` 5→6、`pyproject.toml` 184→193。
>
> **② 为什么这不是洁癖**:v0.7.2 立法「转写器身份 = pin 的**原始字节 sha256**,⛔ 无归一化」。⇒ 仓内副本的指纹**恒不等于**证据本体的指纹 ⇒ **同一个环境被判「未证同一」,每次 checkout 产一个假阴性**。**法会从生效第一天起就在报假警。**
>
> **③ ⛔ 不许改 pin 文件本身。** 它们此刻与现址逐字节相同(已复核)。本单只加 `.gitattributes`,**⛔ 一个字节都不许动那三件**。

## 1. 允许面(⛔ 只这些)

- `.gitattributes`(**仓根新建**)
- 本工单的 `## Result` 段

⛔ **禁区(逐条零 diff)**:`_external_tools/mineru/` 下的三件 pin(**⛔ 内容不许动**)、`server/**`、`client/**`、`docs/**`、`.gitignore`、本工单顶部状态行。

## 2. 判据

### K-1 加 `.gitattributes`,范围**最小**

- 只针对 pin 路径关掉文本转换,例如 `_external_tools/mineru/** -text`。
- ⛔ **不许**写成全仓 `* -text` 或任何波及其它路径的宽模式。

### ⭐ K-2 往返锁(**本单唯一的真 killer**)

- `git add` 三件后,用 `git checkout-index -f --prefix=<仓外临时目录>/` 把它们**签出**。
- **签出副本与现址原件 `D:/Coinsides/v12.9-selection/tools/mineru/` 逐件比 sha256 与字节数,必须全等。**
- ⭐ **必须附【前】的红**:先记下**未加 `.gitattributes` 时**同样往返得到的字节数(应为 443957 / 6 / 193),再记加了之后的(应为 441341 / 5 / 184)。
  ⚠️ **⛔ 只给"加了之后是绿的"不算数** —— 那证明不了 `.gitattributes` 是那个绿的原因(两义性 #4:红/绿在别处)。**前后对照才是证据。**

### K-3 反例:**⛔ 别把别的文件也关掉转换**

- 取一个**不在 pin 路径下**的既有仓内文本文件(如 `server/package.json`),`git check-attr text -- <该文件>`,**必须仍是未设置/自动**,⛔ 不得变成 `-text`。
- ⭐ 这是本单的反例半边:只证明"pin 保住了"不够,还要证明**没顺手改变其它文件的行为**。

### K-4 现址零触碰

- 全程 **⛔ 不写** `D:/Coinsides/v12.9-selection/**`。收尾复测该目录三件 + `.venv/pyvenv.cfg` 的哈希,**须与开工前相同**。

## 3. 回执

> **⚠️ 第一件事:无论结果如何,退出前必须把回执写进本工单的 `## Result` 段;若预算紧张,先写 Result 再做别的。**

须含:K-2 的**前后两组字节数/哈希**(⛔ 缺"前"的一组不算)、K-3 的 `check-attr` 输出、K-4 的四哈希、以及 `.gitattributes` 的全文。
⛔ 不许提交(commit 由调度方做),⛔ 不许翻本工单顶部状态行。

## Result

**结论:PASS。** 仅新增仓根 `.gitattributes`;三件 pin 工作区字节相对开工基线零变化。本段为本工单唯一文档改动;顶部 `ready` 状态行未翻,未 commit。

### K-1 最小范围

`.gitattributes` 全文:

```gitattributes
_external_tools/mineru/** -text
```

规则只命中 `_external_tools/mineru/**`,没有全仓模式。

### K-2 checkout-index 前后对照

两组均先对三件 pin 执行精确 `git add -- <三路径>`,再以 `git checkout-index -f --prefix=<仓外临时目录>/ -- <三路径>` 签出;比较对象为 `D:/Coinsides/v12.9-selection/tools/mineru/` 现址原件。临时目录在系统 TEMP,不在 selection 下。

**【前】仓根尚无 `.gitattributes`(红):**

| 文件 | 往返字节 | 往返 SHA-256 | 现址字节 | 现址 SHA-256 | 全等 |
|---|---:|---|---:|---|---|
| `uv.lock` | 443957 | `403b36885368fe36be6724b718e98116950487f4fe217408b0e032176f3e9c37` | 441341 | `67c4b42dcd269ffa3e9f05633ce98d6259812bc460f2dd2f8f58cb142e3ee231` | 否 |
| `.python-version` | 6 | `8dfda54ba2f66ab3a8ebab460f4338bbeea1fb1e0f85ce06fe3530aa3c64679b` | 5 | `7b55f8e67b5623c4bef3fa691288da9437d79d3aba156de48d481db32ac7d16d` | 否 |
| `pyproject.toml` | 193 | `b4078f2480fb45240b05545bc2b2b3bdec9801680b41bea057966e052f80b202` | 184 | `253b88b01b81b69b6c68b4fc45392600322a20286fcce180b90a8d7183b8ba57` | 否 |

**【后】加入并暂存上述 `.gitattributes` 后(绿):**

| 文件 | 往返字节 | 往返 SHA-256 | 现址字节 | 现址 SHA-256 | 全等 |
|---|---:|---|---:|---|---|
| `uv.lock` | 441341 | `67c4b42dcd269ffa3e9f05633ce98d6259812bc460f2dd2f8f58cb142e3ee231` | 441341 | `67c4b42dcd269ffa3e9f05633ce98d6259812bc460f2dd2f8f58cb142e3ee231` | 是 |
| `.python-version` | 5 | `7b55f8e67b5623c4bef3fa691288da9437d79d3aba156de48d481db32ac7d16d` | 5 | `7b55f8e67b5623c4bef3fa691288da9437d79d3aba156de48d481db32ac7d16d` | 是 |
| `pyproject.toml` | 184 | `253b88b01b81b69b6c68b4fc45392600322a20286fcce180b90a8d7183b8ba57` | 184 | `253b88b01b81b69b6c68b4fc45392600322a20286fcce180b90a8d7183b8ba57` | 是 |

### K-3 非 pin 路径反例

```text
> git check-attr text -- server/package.json
server/package.json: text: unspecified
```

对照命中 pin 路径时为:

```text
_external_tools/mineru/uv.lock: text: unset
```

因此既有仓内文本文件仍走未设置/自动行为,未被改成 `-text`。

### K-4 selection 现址零触碰

开工前与收尾复测的字节数、SHA-256 逐项相同:

| 现址文件 | 字节(前=后) | SHA-256(前=后) |
|---|---:|---|
| `.python-version` | 5 | `7b55f8e67b5623c4bef3fa691288da9437d79d3aba156de48d481db32ac7d16d` |
| `pyproject.toml` | 184 | `253b88b01b81b69b6c68b4fc45392600322a20286fcce180b90a8d7183b8ba57` |
| `uv.lock` | 441341 | `67c4b42dcd269ffa3e9f05633ce98d6259812bc460f2dd2f8f58cb142e3ee231` |
| `.venv/pyvenv.cfg` | 202 | `cb9ad065dd87e7ddb416cb8aeb7009424261bcce5743ad1da9de8b419d407a6e` |

仓内三件 pin 收尾亦仍为 `5 / 184 / 441341` 字节及同组三个现址 SHA-256,证明 `git add` 与两次仓外签出未改其工作区内容。

### 范围审计

- 本单暂存项只有 `.gitattributes` 与工单要求 `git add` 的三件 pin;没有 commit。
- 开工前已有的 `server/src/routes/projections.ts` 与 `docs/agent-ops/handoffs/2026-08-29-seventh-check-functionize.md` 工作区改动保持为既有状态,本单未触碰。
- `client/**`、`.gitignore` 均未触碰;本工单除本 `## Result` 外无其它改动,顶部状态行仍为原文 `ready(c-1a 的收尾单 —— 三件 pin 被扣住的原因在此清除)`。
