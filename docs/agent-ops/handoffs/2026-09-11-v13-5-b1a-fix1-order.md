> **状态 (Status)**: done（builder 工作树交付；待 HQ 复核放行）
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-11
> **单号**: 13.5 · B1a 修一 · api mock 出口补全
> **上游**: HQ 全库复跑:Board 全族套件级崩(`No "getToken" export is defined on the "@/services/api" mock`,authStore.ts:23 经 B1a 皮 hook 进入 Board 测试模块图);builder 二轮只跑定向 14 文件未跑全库

# B1a-fix1 · mock 出口普查补全

## 零 · 裁定

1. 皮 hook 复用 authStore=合法设计⛔改产品;对**全 client 测试**普查 `vi.mock("@/services/api"` 类 mock,凡因 B1a 新增模块图边而缺出口的(getToken 及其它被新消费的导出)逐一补全(用 importOriginal 部分 mock 或显式补导出,保持各测试原语义);
2. 修后 **client 全库(低并发可)必须全绿**(名录内负载超时独立复验绿除外);
3. **新常备条款(自本单起入所有工单模板)**:凡改产品代码的单,交付前必跑 client 全库,⛔以定向替代——定向网抓语义,全库网抓模块图。

## 一 · 验收

全库绿+受影响静态门绿;Result=补全位置清单/普查证据/测试数字。⛔动产品代码;⛔碰 .git。

## Result

2026-09-11 · codex(builder) · 工作树交付，未 commit。

### 补全位置与语义

以下 9 处统一采用 typed `importOriginal<typeof import('@/services/api')>()` 部分 mock：先展开真实模块出口，再覆盖原 `default` HTTP mock。由此保留 `getToken`、`setToken` 与 `API_BASE`；原请求处理、调用台账、夹具和断言均不改。

| 文件（相对 `client/src/`） | mock 起始行 |
|---|---:|
| `pages/Boards/BoardPage.bookmarks.test.tsx` | 11 |
| `pages/Boards/BoardPage.chalk.test.tsx` | 10 |
| `pages/Boards/BoardPage.item.test.tsx` | 10 |
| `pages/Boards/BoardPage.layers.test.tsx` | 9 |
| `pages/Boards/BoardPage.selection.test.tsx` | 9 |
| `pages/Boards/BoardPage.snapping.test.tsx` | 9 |
| `pages/Boards/BoardPage.tools.test.tsx` | 8 |
| `pages/Boards/boardTextRangeClipboard.test.tsx` | 17 |
| `pages/Notes/canvasEngine/hooks/useTrayRelocation.test.tsx` | 20 |

### 普查证据

- 射程：递归枚举全 `client` JS/TS 源文件（含测试、setup、浏览器辅助 mock；排除 `node_modules`、`dist`、`.vite`、`coverage`）。共 **525 个源文件、138 个测试文件、64 个含 mock 的文件、110 个 mock 调用、45 处 API mock**。`.codegraph/` 存在，但当前 MCP/CLI 不可用；改用 TypeScript AST 与去除类型导入后的值依赖图，并以全库实跑交叉验证。
- 同查 alias、相对路径、非字面量、`doMock`、手工 `__mocks__`：45 处均为字面量 `@/services/api`；其它上述形状各 **0**。7 个浏览器 `mockApi`/`inkMockApi` 辅助模块已有 `default/getToken/setToken/API_BASE`，本单无需修改。
- 修前闭合：**45 = 9 缺口 + 2 原已补齐 + 34 不达真实 named-export 消费者**。2 个已有 token mock 为 `BoardPage.unboxing.test.tsx`、`useNoteSkin.test.ts`。部分测试由本地 `authStore` / `BoardNoteModal` mock 截断新依赖边，保留其隔离语义。
- 新边是 `useNoteSkin → authStore → api.getToken/setToken`，`getToken()` 在 store 初始化时即执行。全 client 另一 named-export 消费者 `agentStore`（`getToken/API_BASE`）不在这 45 个测试的真实依赖图内；未见其它新增消费缺口。修后可达缺口 **0**。
- 完整 45 行位置、前后出口、分类及调用路径见 [mock-census.json](../../audits/2026-09-11-b1a-fix1-builder/mock-census.json)，可重导脚本 [mock-census.cjs](../../audits/2026-09-11-b1a-fix1-builder/mock-census.cjs)（仓库根执行 `node docs/audits/2026-09-11-b1a-fix1-builder/mock-census.cjs`）。静态图包含字面量动态导入，可能保守多算；执行结论以下述 Vitest 实跑为准。

### 验证数字

| 运行 | 结果 |
|---|---|
| 修前 client 全库，`--maxWorkers=4 --minWorkers=1` | **9 文件套件级失败 / 129 文件通过；1410 测试通过**，9 处均报缺 `getToken`；43.89s |
| 修后首轮总门，仅设 `VITEST_MAX_THREADS=4` | named-export 崩溃归零；默认 fork 池未被限流，**136 文件通过 / 2 失败，1483 测试通过 / 2 失败**：unboxing 1 例超时、Gallery Purpose retirement 1 例断言失败；未改这两处测试 |
| 修后低并发 client 全库（总门首步） | **138/138 文件、1485/1485 测试全绿，0 skipped；43.49s**，包括上轮两处失败；恢复执行原先被套件崩溃遮住的 **75** 个测试 |
| 完整 `npm run verify:v2-bn8-runtime` | **exit 0**；client 全库、工具面测试/静态门、canvas/shell/source/relation 契约门、60 组 model contract、client/server 构建、performance smoke、docs、diff 与 changed-file secret scan 全部通过 |

最终总门进程同时设置 `VITEST_MAX_THREADS=4`、`VITEST_MIN_THREADS=1`、`VITEST_MAX_FORKS=4`、`VITEST_MIN_FORKS=1`，覆盖默认 fork 池；未改 Vitest 配置或测试超时。最终是一次完整全库全绿，未使用名录豁免或定向复验代替全库。构建只保留现有大 chunk 提示，不阻塞验证。

原始运行输出与退出码/校验摘要入 [validation-results.json](../../audits/2026-09-11-b1a-fix1-builder/validation-results.json) 及同目录三份 `.txt` 日志。独立普查方对 9 文件在内存撤去新增 mock 展开后逐一核对开工 SHA256，**9/9 与原文件一致**；磁盘未做回滚。开工枚举的 `client/src`、`client/scripts`、`client/test`、`server/src`、`shared` 共 **970 文件**中，只有上述 9 个测试文件发生本单增量，产品和 B1a 未提交交付均保留。未改 agent 指令/权限配置，未写 `.git`，未 commit/push/PR/merge。
