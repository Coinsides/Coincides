> from: claude | to: codex | status: done | re: 8.11-closeout-commit | date: 2026-07-04

# 8.11 收口 commit：干净落地 burndown（Stages 1–5 + teardown）

> **前置**：8.11 object-family 的 highrisk + MED burndown 全部落地并经 Claude 对抗核查：Stage-1 高危（8.11.12）/ Stage-2 生命周期（8.11.13）/ Stage-3 表格（8.11.14）/ Stage-4 标注真相（8.11.15 + fix + fix-2）/ Stage-5 图片 GC（8.11.16）/ course 资产 teardown（8.11.17）。**逐行复核：0 个 open 🔴 红线**（`🔴.*☐` 零匹配）；剩余 open ≈ 19 MED + 63 LOW，全为 🅱 友好性/硬化，foundation 期正确 defer。
> **一件事**：把这批已落地的工作**干净地收口提交** —— 不是改代码，是**提交卫生 + 构建纪律 + 验证门**，让 8.11 这一版可信地封版。

---

## 核心纪律（先读 · 这条比清单任何一条都重要）

**必须干净构建后跑 CI，别让陈旧产物假绿。** register 跨 Stage-3 + Stage-4 反复撞到同一过程风险：`canvas-engine-model-contract` smoke 依赖 `.codex-tmp` 的增量 tsc，**stale 构建会假 fail 或（更危险）掩盖真 fail**。收口前**清 `.codex-tmp`（或确保 incremental 可靠）再全量跑**，否则所有绿测证据不可信。

---

## 清单（要做）

1. **确保 burndown 全部工作已提交**：当前工作树脏（canvasEngine 数十文件 + contentGroup + Source viewer 等）。核对 8.11.12–8.11.17 的代码 + 测试 + patch notes 都进了 commit，别把承重改动留在未提交状态。
2. **`git add` 未跟踪的承重测试文件（CR-8.11.16-03）**：`server/src/__tests__/v2CanvasPersistenceCutover.test.ts` 现为 `??`（`git ls-files --error-unmatch` 失败）却已挂进 `test:v2` —— **fresh checkout / CI 没这文件，GC + page_frame 红线覆盖不随代码走**。`git add` 它 + 任何其它未跟踪的承重测试；提交后 `git status` 应无未跟踪的测试/源文件。
3. **干净构建 + 全量验证门**（每条须绿）：
   - 清 `.codex-tmp` → `cd server; npm run test:v2`（应 173+）
   - `npm run smoke:canvas-engine-model-contract`（应 57 groups）
   - `npm run check:canvas-runtime-boundary`（应 159）
   - `node --import tsx --test src/__tests__/v2CanvasPersistenceCutover.test.ts`（应 44）
   - `npm run verify:v2-bn8-runtime`（client/server build + performance smoke + `git diff --check` + changed-file secret scan）
4. **回填 commit SHA**：提交后把收口 SHA 回填进 register 各 `## Codex Follow-up Result` 段 + 各 patch note，让"哪次 commit 落的哪颗坑"可追。

## 不做什么（本单边界）

- **不碰任何 open MED/LOW**（那 82 个 🅱 项 foundation 期 defer；导出族待 export 管线一并做）。
- **不改** `docs/agent-ops/**` 与 `docs/brainstorm/**`（Claude 的工作区）。
- **不新增功能**、不动未在 8.11.12–17 范围内的代码。
- 若发现某颗"已 ☑"的坑其实没真落地（代码与 register 不符），**别猜、别硬提交** —— 记 `needs: claude/henry`，我来复核。

## 验收标准

- 四套 suite + `verify:v2-bn8-runtime` **在清 `.codex-tmp` 后的干净构建上全绿**；
- `git status` 无未跟踪的承重测试/源文件；`git diff --check` 净；secret scan 净；
- register / patch notes 回填了收口 SHA。

## 完成后的文档动作

- 回写本 handoff `## Result`（status→done）：列出收口 commit SHA + 四套数字 + 干净构建证据（清了 `.codex-tmp`）。与预期不符**不猜**、记 `needs: claude`。
- **Claude 收口后核**：重点核 ① 干净构建证据（非 stale 假绿）② 承重测试已 tracked（`git ls-files` 命中）③ 8.11.12–17 范围完整无遗漏提交。核过即 8.11 正式封版，转前方（目的层 / Source 契约接缝）。

## Result

Codex closeout completed on 2026-07-05.

Main closeout commit:

- `454f7c9` — `feat: land v2 bn canvas engine through 8.11`

Clean-build evidence:

- Cleared `.codex-tmp/canvas-engine-contract`.
- Cleared `.codex-tmp/canvas-engine-performance`.
- `server/src/__tests__/v2CanvasPersistenceCutover.test.ts` is tracked in `454f7c9`.
- No untracked files remain under `client/` or `server/` after staging the closeout source/test set.

Validation:

- `cd server; npm run test:v2` passed: 176 / 176.
- `npm run smoke:canvas-engine-model-contract` passed: 57 groups.
- `npm run check:canvas-runtime-boundary` passed: 159 checks.
- `cd server; node --import tsx --test src/__tests__/v2CanvasPersistenceCutover.test.ts` passed: 47 / 47. This differs from the handoff's older "44" expectation because later burndown patches added coverage.
- `npm run verify:v2-bn8-runtime` passed, including runtime boundary, model contract, client build, server build, performance smoke, `git diff --check`, and changed-file secret scan.

Notes:

- The main closeout commit intentionally includes the staged 8.8-8.11 canvas engine source/test/release-doc set needed for a fresh checkout to reproduce the verified runtime.
- Claude/documentation-system work under untracked `docs/agent-ops/**`, root `AGENTS.md` / `CLAUDE.md`, and archived brainstorm moves were left out of the main closeout commit unless already part of the 8.11 burndown record.
- Evidence回填 is recorded after the main commit because a commit cannot contain its own final SHA.
