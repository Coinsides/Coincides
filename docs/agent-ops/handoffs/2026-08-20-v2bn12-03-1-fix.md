> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: ready | re: v2bn12-03-1-fix | date: 2026-08-20

# V2.BN.12 修正单 03.1:工单 03 复核 FAIL 五缺陷修理

## 背景

工单 03 复核 **FAIL(0B/2H/3M/1L)**,全文见工单 03 ## Review。官方门全绿但复核官六个负向 probe 稳定复现缺陷——修理以 Review §1 的 finding 编号为准,其建议修法为默认蓝本(有更好方案须在回执申报理由)。

## 修正要求(全部必修)

1. **R1[HIGH-1] same-key replay 丢正文**:`DraftBlockCreateResult` 透传 `reused`/权威 create snapshot;replay 时禁止把当前 revision 直接标 saved——强制 latest PUT 或先比对 durable response 与当前 draft。补跨层回归:server 已 commit + response 丢失 + 继续编辑 + same-key retry → reload 后 durable 必须是 `ab`。
2. **R2[HIGH-2] compensation 误删**:①proven-empty fingerprint 纳入 `block_type`/`metadata`/必要 placement snapshot;②要求 receipt placement 为该 block **全库唯一** placement;③引用扫描按 block/TextFlow/TextUnit/object 身份**跨 note 闭合**。补三组回归:changed metadata/type、second-note placement、second-note annotation/mount——全部期望 409 + 数据保留。
3. **R3[MED-1] 左侧 crossing 往返翻类**:crossing 与 outside 同待遇——保留 Canvas-world receipt,只有 inside 转 Page-local;normalize 不得对 world 坐标 clamp。补左右两侧 crossing 的 hydrate→normalize→save 全链回归(用 Review 里 live tuple `f6cefd29`/`x=14` 做 fixture)。**不得**再对 live DB 跑任何 migration apply——D4 的 4 条 tuple 静态正确,修的是 client 链。
4. **R4[MED-2] recovery receipt 裸写**:首次写 sessionStorage 前完成 canonical projection(带 `surface/coordinate_space/frame_id`),或使 receipt 自带 coordinate authority 且 mount replay 自足正规化。补回归:Page mode pending POST → 切 note → 突然 reload → replay 后 placement 必须 canonical。
5. **R5[MED-3] Page 死局 3/4→4/4**:meaningful 判据消费"当前 surface 实际可渲染对象"(或让 Page 渲染 `formal_page/inside` structured object——二选一,选后者须申报渲染边界理由)。补 live-shaped fixture:note 只含 Page-inside table(Canvas-only 渲染)时,Page 必须 prompt=true 或 table 可见,二者必居其一。
6. **R6 六条负向 oracle 落户主树**:Review §5.2 的六个 probe 全部转为常驻回归测试进 test:unit / server tests(复核官的 probe 在隔离树已弃,不落户=下次还丢)。

## 附带裁定(Fable,回应 Review LOW-1)

**sessionStorage 自动回放策略:予以明文追认。** 依据:工单 03 D1 "durable 创建失败时 ephemeral 内容不丢"的保内容要求,跨 note 切换场景是其自然延伸;方向与红线(收据可撤销、无静默丢失)一致。追认条件=R4 修复(canonical receipt),且 recovery replay 保持幂等+成功即删。此裁定记 claude-log 2026-08-20 条目 20。

## 边界与验证

不碰工单 03 边界外;**禁止任何 migration --apply**;RED 断言不许改;schema 不动。全套重跑:test:unit(64+新增全绿)/ test:v2(256+新增)/ verify 链 / tsc / migration 5 项脚本测试(dry-run 与 fixture 层面)。回执 ## Result 追加进本文件:每项 R 的落实+收据+新增回归清单,顺手修申报,未做申报。不自评 PASS,不 commit。复核将重验六 oracle+重跑全门+D4 readonly post-state。
