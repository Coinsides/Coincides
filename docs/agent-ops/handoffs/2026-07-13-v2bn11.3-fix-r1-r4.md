> from: claude | to: codex | status: done | re: v2bn11.3-fix-r1-r4 | date: 2026-07-13

# V2.BN.11.3 修正单：R1 净库索引缺失 + R2 successor-active + R3 文档更替 + R4 去重断言

> 沿 11.2 修正单先例发出（Henry 确立的"复核发现→立即修正"流程）。完整判定见 11.3 Review §8（条件 PASS）。**窗口:11.3 全部改动未 commit——047 此刻改两行,commit 后改=048 或启动 ensure。**

## R1 · [MED,必修] 净库丢失 `idx_content_group_members_item`

侧修 #1 把索引从 schema.sql 移除后,它只活在 047 `rebuildContentGroupMembers` 的 rebuild 分支里;净库上 schema.sql 已建终形表(含 item_id)→guard `tableHasColumn(item_id)` 早退→**索引永不创建**。升级库有、净库没有=形状漂移,违反 11.2 plan"避免新空库与升级库形状漂移"。

**改动**:
1. 047 `up()` 在 `rebuildContentGroupMembers(db)` 调用之后、**guard 外**,无条件执行 `CREATE INDEX IF NOT EXISTS idx_content_group_members_item ON content_group_members(user_id, item_id);`(升级路径 rebuild 已建则 no-op;净库路径由此补上)。
2. 启动回归测试(`v2Items.test.ts` L103 那条)加**净库**分支断言:fresh `initDb` 后 `PRAGMA index_list(content_group_members)` 含该索引——两条路径都锁死,不再只测升级半边。

## R2 · [MED-LOW,必修] successor 必须 active

父 plan §3.1/§4.3 明文要求,当前 `assertSuccessorChainAcyclic` 只查环与归属。现有 L180-186 的"退役进已退役卡"用例**因环而抛**——错误理由的通过。

**改动**:
1. `retireItem` 对**直接后继**加状态检查:successor 非 active → 400(如 "Item successor must be an active Item");链上更深节点无需查(链中段天然退役)。
2. 专项测试:A→B(B 退役,无环),再 C 退役进 B → 被拒;错误消息断言与环用例区分开。

## R3 · [LOW-MED,文档] 父 plan §3.2 加 dated 更替注

实现与 11.3 plan 不变量 2(每次正文写入生成/复用快照,读路径已依赖)与父 plan §3.2"Snapshot 只能由 Relation create/reaffirm 命令建立"矛盾。拓宽已被复核接受;请在父 plan §3.2 加一行 dated 注:自 11.3 起 Item 写路径生成/复用快照(INSERT OR IGNORE 按 content_hash 去重),"仅 Relation 命令"口径更替;"不开放快照独立编辑 API"不变;11.5 的双端快照复用流不受影响。

## R4 · [LOW,顺手] 相同内容重存 no-op 断言

在快照测试里补一步:以相同 plain_text 再 update 一次,断言 `item_snapshots` 行数不变、current_snapshot.id 不变——把 INSERT OR IGNORE 去重路径直接行使一次。

## 记档(不要求本单处理,知悉即可)

- 侧修 #3 使 `replaceGroup` 会 throw,四个既有 fire-and-forget 调用点(soft-delete/move-to-folder/add-candidates/drop-on-group)无 catch→保存失败时未处理 rejection(可见 UX 不变)。修法你定(调用点补 catch 或 legacy 路径返回 boolean 不 throw),可并入本单也可留 11.4。
- itemPoolQuerySchema/itemListQuerySchema 未 `.strict()`(其余 item schema 均 strict)——顺手可齐。
- Note 删除的 receipt survival 目前只经 course 级联传递测得。

## 验证与交付

改毕重跑:`cd server && npm run test:v2`、`npm run verify:v2-bn8-runtime`、`npm run check:v2-bn11-legacy-shutdown`。在本单追加 `## Result`(数字+改动清单),**不自签 PASS**——Claude 复验后落 checkpoint commit 并准予 11.4。

## Result（Codex，2026-07-13）

状态：R1-R4 已工程修正，等待 Claude 第三方复验；本回执不自签 PASS。

### 改动清单

1. **R1**：047 在 `rebuildContentGroupMembers` 之后、guard 外执行 `CREATE INDEX IF NOT EXISTS idx_content_group_members_item`。专项同时锁定 fresh DB 与 legacy rebuild 两条路径。
2. **R2**：直接 successor 非 active 时以 `400 Item successor must be an active Item` 拒绝；cycle 仍由独立错误分支拒绝，失败后 candidate 保持 active。
3. **R3**：父 Plan §3.2 加 2026-07-13 更替注，明确 Item 写路径生成 / 复用 Snapshot、无独立 Snapshot 编辑 API、Relation 命令后续只复用端点 Snapshot。
4. **R4**：相同 `plain_text` 重存后，Snapshot id 与行数均不变。

### RED / GREEN

- RED Item 专项：7 条中 5 通过、2 失败；失败精准对应 fresh DB 缺索引与 inactive successor 被放行。
- GREEN Item 专项：7/7 通过。
- Full server V2：224/224 通过。
- Canvas runtime boundary：159 checks 通过。
- Canvas engine model contract：58 groups 通过。
- Client build / server build / performance smoke：通过；performance seed 总计 12.04 ms。
- `npm run verify:v2-bn8-runtime`：通过。
- `npm run check:v2-bn11-legacy-shutdown`：通过。
- `git diff --check`：通过，仅既有 LF/CRLF 提示。
- changed-file secret scan：最终文档落地后 24 files 通过。

本补丁没有 UI 改动，因此未重复 V2.BN.11.3 已完成的浏览器旅程。完整工程回执：`docs/releases/V2.BN.11.3.1-Item-Lifecycle-Review-Fixes-Review.md`。
