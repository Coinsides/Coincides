> from: claude(opus,调度权:operating-workflow.md v1) | to: codex(builder) | status: ready(b-3-fix 已 done、12.2b 串行闸解除;Fable 2026-08-23 裁「b-2b-2 转 ready 派单」log #33。**Opus 依下沉工作流翻牌,不代表 Henry 逐张批过**) | re: v2bn12-2b-2 | date: 2026-08-23

# V2.BN.12.2b-2:MRTR confirm 往返(input_required)+ HTTP 层 K-5

> ⚠️ header 由 Opus 依调度授权链管理,**不代表 Henry 本人逐张批过**。
> ✅ **可开工**。交界段已依 b-3 `## Review` §4 填毕(新增 **J-1 / J-2** 两条要求与 **K-c11 / K-c12** 两条 killer);**b-3-fix 已 done,串行闸解除**。

## 上游

- `handoffs/plans/v2-bn12-2-mcp-tool-face.md`(派单判据表 / 岗位锁 / Spark 画像)。
- b-2b-1 工单及其 `## Result` —— **收据形状与 tier 语义的事实来源**。
- **Fable 2026-08-23 四条预裁 + 拒绝形状裁定**(本单「预裁落位」表逐条落位,不重述)。

---

## 调度方已亲验的九条实况(2026-08-23 发单前核,**逐条给了位点**)

> ⚠️ **行号会漂**。下表行号是发单时的读数,**只用于定位,不得据行号施工**;施工前按符号名重新定位。

| # | 实况 |
|---|---|
| 1 | **SDK 版本 `@modelcontextprotocol/{server,core,node}` 均 `2.0.0`**,住 `server/node_modules/`。 |
| 2 | ⭐ **要反转的降级就在 `server/src/mcp/policy.ts:40-43`**,且**两个分支都 `return 'propose'`** —— 第 40 行的能力判断**当前是死分支**。注释原文 `// b-2b-2 接 MRTR 前的声明性降级`,**本单负责拆掉它**。 |
| 3 | ⭐ **confirm 档现在是 501 硬抛**:`transport.ts` 的 `dispatchToolCall` 里 `effectiveTier === 'confirm'` ⇒ `throw new AppError(501, …)`(发单时 `:191-196`)。**本单把这一支换成 MRTR,不是新增旁路。** |
| 4 | ⭐⭐ **`statusForTier` 对 `'confirm'` 抛 400**(`toolFaceReceipts.ts:80-83`,只认 `immediate`/`propose`)。⇒ **确认后执行那一笔收据必须写 `tier: 'immediate'`(⇒ status `applied`),⛔ 不得写 `tier:'confirm'`,更不得为此改 `statusForTier`。**(这正是「schema 零改动」的落点。) |
| 5 | ⭐ **SDK 明写「At least one of `inputRequests` or `requestState` must be present」** ⇒ **带了 `inputRequests` 就无须 `requestState`**,Fable 预裁②在 SDK 层站得住。SDK 另自陈:`requestState` 回程时**是攻击者可控输入**,无 HMAC 即裸串,且默认不做完整性保护。**⇒ 不用它是安全的那一侧,不是省事的那一侧。** |
| 6 | ⭐⭐ **`inputResponses` 是 `params` 的顶层成员,与 `_meta` 平级 —— ⛔ 不在 `_meta` 里面。** 依据:SDK 运行时 `RETRY_PARAMS_KEYS = ["inputResponses","requestState"]`,自 `nextParams[key]` 提取。**夹具照此扩,不得猜。** |
| 7 | **处理器侧读法**:`ctx.mcpReq.inputResponses`(另有 `.droppedInputResponseKeys`、`.requestState`)。当前 `transport.ts` 的 `createFreshServer` 只从 `context.mcpReq` 取了 `id` 与 `envelope`(发单时 `:243-245`),**`ToolCallDispatchContext` 须扩一个字段**。 |
| 8 | **可用导出均已核实存在于 `@modelcontextprotocol/server`**:`inputRequired`(含 `.elicit`)、`acceptedContent`、`inputResponse`、`isInputRequiredResult`。 |
| 9 | **要改的两条测试就位**:`v2McpTransport.test.ts:363` K-5(**目前是 `dispatchToolCall` 直调,非真实 HTTP**)、`:644` K-b2(标题 `…still declaratively proposes`,**这条断言本单必须反转**)。夹具 `mcpPost` 在 `:159`,**目前不带 `inputResponses`**。 |
| 10 | ⭐⭐ **`trash_notes` 的 `output_schema` 顶层是 `additionalProperties: false`,`required:['results']`,且 `results` 无 `minItems`。** ⇒ 两个后果:**(a)`{ results: [] }` 是合法的**——不是推论,`proposalStructuredContent` **今天就在回它**,K-b1/K-b2 正断言 `deepEqual(structuredContent, {results:[]})` 且当前是绿的;**(b)`decision` 这类新字段⛔ 塞不进 `structuredContent`**,这是它必须走 `_meta` 的硬原因,不是风格选择。 |
| 11 | **`_meta` 命名空间现有两键**:`transport.ts` 的 `TOOL_METADATA_KEY = 'io.coincides/toolFace'`、`RECEIPT_METADATA_KEY = 'io.coincides/toolFaceReceipt'`。**本单第三键取 `io.coincides/toolFaceDecision`,与之同族。** |

---

## ⚠️ 调度方复核 Fable 四条预裁时发现的一处缺口(**已按下述补齐,施工按本节**)

**Fable 预裁①只覆盖了 `confirm:true` / `confirm:false` 两种回答,没覆盖「用户 decline / cancel」。** 这不是文书疏漏,是有牙的坑:

> **SDK 官方示例用的是 `acceptedContent(...)`,而 `acceptedContent` 对「没问过」和「问了但被拒/被取消」一律返回 `undefined`。**
> 照抄官方那句 `if (!confirmed) return inputRequired({...})` ⇒ **用户每拒绝一次就被再问一次**,直到客户端 `inputRequired.maxRounds` 耗尽,报 `INPUT_REQUIRED_ROUNDS_EXCEEDED`。
> 对一个**删除类**工具,这是「拒绝无效、反复逼问」—— **红线级**的交互。

**⇒ 分支判定必须走 `inputResponse()`(判别式视图;SDK 自陈它「covers decline/cancel detection and the non-elicitation response kinds that `acceptedContent` does not surface」),⛔ 不得用 `acceptedContent` 判断「是否已问过」。**

`acceptedContent` 仍可用于**取值并按 schema 校验**(它有 schema 重载),但**分支判定归 `inputResponse`**。

---

## 预裁落位(Fable 四条 + 本节补的第五条)

| 预裁 | 落位 |
|---|---|
| ① confirm 往返语义 | S2 状态表 |
| ② 不用 `requestState` | **硬闸 H-3**;SDK 侧已证不必需(实况 5) |
| ③ 撤销声明性降级、K-b2 反转 | S1 + S3 |
| ④ HTTP 层 K-5 = 真实两路径 + mutation | S3 + K-c7 |
| ⑤ **decline/cancel 不重问**(本会话补,**Fable 2026-08-23 追认**) | **硬闸 H-4** + **K-c5 / K-c8** |
| ⑥ **拒绝形状**(Fable 2026-08-23 裁定:`isError:false` / complete / `{results:[]}` / `_meta.decision` 三值) | S2「拒绝形状定义」+ **K-c10** |

---

## S1:`policy.ts` —— 撤销声明性降级

`resolveEffectiveTier` 的 confirm 分支:**无能力 ⇒ `'propose'`(不变);有能力 ⇒ `'confirm'`**。
连带**删掉 `// b-2b-2 接 MRTR 前的声明性降级` 注释与其下那条死的 `return 'propose'`**。

⛔ **不得**动 `supportsFormElicitation` 本身的谓词语义(协议版本 + `capabilities.elicitation.form` 双条件),它是 S3 mutation 的靶子。
⛔ **不得**动 `threshold.batch_field` 的 n===1 ⇒ immediate 捷径。

## S2:`transport.ts` —— confirm 档换成 MRTR

`ToolCallDispatchContext` 扩 `inputResponses?: Record<string, unknown>`,在 `createFreshServer` 里自 `context.mcpReq.inputResponses` 取。

**confirm 档状态表(⛔ 逐行照做,不合并分支)**:

| `inputResponse(responses,'confirm')` | 执行 | 收据 | 返回 |
|---|---|---|---|
| `kind:'missing'`(**首呼**) | ❌ | ⛔ **零行** | `inputRequired({ inputRequests: { confirm: inputRequired.elicit({…}) } })` |
| `kind:'elicit'`, `action:'accept'`, `content.confirm === true` | ✅ 走 binding | `tier:'immediate'` ⇒ `applied` | 与 immediate 档同形(含 `_meta` 收据) |
| `kind:'elicit'`, `action:'accept'`, `content.confirm === false` | ❌ | ⛔ **不写** | **拒绝形状**,`decision: 'rejected'` |
| `kind:'elicit'`, `action:'decline'` | ❌ | ⛔ **不写** | **拒绝形状**,`decision: 'declined'` |
| `kind:'elicit'`, `action:'cancel'` | ❌ | ⛔ **不写** | **拒绝形状**,`decision: 'cancelled'` |
| `kind:'sampling'` / `'roots'`(不该出现) | ❌ | ⛔ **不写** | 视同 missing,重问**一次**即可;⛔ 不得执行 |

### 「拒绝形状」定义(Fable 2026-08-23 裁定,三者**同一处理路径**)

**拒绝不是错误。** 三种情形(`rejected` / `declined` / `cancelled`)走同一段代码,只有 `decision` 取值不同:

| 面 | 值 |
|---|---|
| `isError` | **`false`**(⛔ 不得设 true,⛔ 不得走 `appErrorResult`) |
| `resultType` | **`complete`**(⛔ 不得再回 `input_required`) |
| `structuredContent` | **`{ results: [] }`** —— 合法且**已有绿测在跑**(实况 10) |
| `_meta['io.coincides/toolFaceDecision']` | **`{ decision: 'rejected' \| 'declined' \| 'cancelled' }`** |
| `content` | 文字说明「已按用户选择取消,未执行任何删除」 |

⛔ **`_meta` 里不得带收据键** —— 拒绝路径**没有收据**,塞一个 `io.coincides/toolFaceReceipt` 就意味着编造 id,是**最像绿的假绿**。拒绝路径的 `_meta` **只有 decision 一个键**。
⛔ **不得为 `decision` 改 `output_schema`** —— 顶层 `additionalProperties: false`,塞进 `structuredContent` 会被拒(实况 10b)。

**表单内容(预裁①)**:`requestedSchema` 为 `{ type:'object', properties:{ confirm:{ type:'boolean' } }, required:['confirm'] }`;**`message` 须含受影响条目数与 id 摘要**(K-c9 验)。

**`droppedInputResponseKeys`**:SDK 会丢弃非 bare 形状(如 `{method,result}` 包装)的条目并把 key 记在此处 —— 那种情况读作 `missing` ⇒ 重问。**这是 SDK 的既定意图,不是 bug;回执申报即可,⛔ 不要为它加补偿逻辑。**

**零持久(硬闸 H-5)**:`input_required` 那一轮**绝不能写 `operation_batches`**,也不得留任何进程内状态 —— 本服务是**每请求新建 server + `handler.close()`**,重试是**全新请求**,服务端**从客户端重发的原 input 重算计划**(预裁②)。

## S3:HTTP 层 —— 真实两路径

- 扩 `mcpPost`:支持在 `params` **顶层**传 `inputResponses`(实况 6)。
- **K-5 从 `dispatchToolCall` 直调改为真实 `/api/mcp`**(预裁④)。
- **K-b2 断言反转**:标题与断言都改为「有能力 ⇒ `input_required` 而非 proposed」。**K-b1(无能力 ⇒ proposed 零执行)原样保留,作阳性对照。**

---

## 与 b-3 的交界(**已填 —— 依 b-3 `## Review` §4 的 5-2 跨条扫描,2026-08-23**)

复核结论:**b-3 与本单没有直接状态冲突**,但必须带走六条边界。其中**两条是本单的新增要求**,原工单未覆盖:

### ⭐ J-1(新增要求):**b-3 的事务保护不覆盖本单**

> 复核 §4-3 原文:**b-3 的 immediate transaction 只保护 REST Apply,不保护未来 `transport.ts` 的 confirm accept。**

⇒ **⛔ 不得假定 confirm accept 继承了 b-3 的事务保证。** 本单的 accept 路径**须自带** executor-count killer(已在 **K-c3**:实际调用次数 === n)。
**重放**:MRTR 无状态,同一 input 携 `confirm:true` **重发两次会执行两次** —— 这是协议形状,不是缺陷。
**本单只须「声明并锁住可观察行为」,⛔ 不得新建去重机制**(那是 TD-6 的范围,不在本单):
- **K-c11**:同一 input + `confirm:true` **连发两次** ⇒ 第二次每条 `outcome` 必须是 `skipped` / `already_trashed`(由 `trashNoteAsUser` 自身守卫给出),**⛔ 不得再 trash 一次**;两次各写各自的收据。
  必红:令第二次返回 `trashed` ⇒ 红。

### ⭐ J-2(新增要求):**accept 必须写真实 actual resources**

> 复核 §4-4 原文:**accepted confirm 必须写真实 actual resources,不能把 pending / proposed resources 原样标 applied,否则 revert 无法按 `outcome: trashed` 恢复。**

⇒ 这是**跨单的承重耦合**:b-3 的 revert 按 `outcome: 'trashed'` 挑要恢复的 note。若本单 accept 图省事把 `proposedResources()`(全是 `outcome:'pending'`)原样写进 applied 收据,**revert 会一条都恢复不了,而且看起来一切正常**。

- **K-c12**:accept 后的收据 `metadata.resources` 必须是 **binding 真实返回的逐条结果**(`trashed` / `missing` / `skipped`+`reason`),**⛔ 不得含 `pending`**;并**端到端接上 b-3 的 revert:该收据 revert 后 note 真的回来**。
  必红:令 accept 写 `proposedResources()` ⇒ **K-c12 红,且 revert 端到端红**。
  📌 **实现提示**:immediate 档已有 `appliedResources(value)` 做这件事,**accept 走同一条**,⛔ 不要另写。

### 其余四条(**与本单既有内容一致,记录以证已扫过**)

| 复核 §4 | 结论 | 本单落点 |
|---|---|---|
| **4-2** `dismissed` 是人审队列的拒绝终态,**不应拿来表示 MRTR decline**;decline/cancel 仍应零执行零收据 | ⭐ **与 Fable 拒绝形状裁定独立吻合** | S2 拒绝形状 + **K-c4**(令写 `dismissed` 收据 ⇒ 红) |
| **4-5** `ToolFaceReceiptTier` 仍只有 immediate/propose,`statusForTier` 不收 raw confirm | ⭐ **与实况 4 独立吻合** | 实况 4 + **H-2** |
| **4-1** Q2→Q3 耦合:executor count 只观测注入 slot | 属 **b-3-fix** 范围(I-1/I-2 身份锁),**本单不代偿** | — |
| **4-6** 多资源 revert 仍非原子;`scopes`/TD-14 未强制 | **⛔ 简单往返成功不得外推为 TD-6 或授权闭环已解决** | 「基线缺口:不得代偿」 |

### 入料不断流(原待答项,已答)

有能力路径不再产 `proposed` 后,**无能力路径仍产** ⇒ b-3 队列页 / apply / dismiss 三条路由**入料不断流**。复核 §4 未列此项为冲突,与预期一致。
⚠️ 但**队列的构成会变**:有能力客户端的批量删除**不再进人审队列**(改走 MRTR 当场确认)。**这是设计意图,不是回归** —— 回执须显式申报,⛔ 不得当缺陷去"修"。

---

## K 系 killer

**必红判据一律钉在「executor 实际调用次数」与「表里的行」上,⛔ 不只看返回码。**

| # | killer | 必红判据 |
|---|---|---|
| **K-c1**(留旧) | 无能力 n>1 ⇒ proposed,**零执行** | 即现 K-b1,**原样保留作阳性对照** |
| **K-c2** ⭐ | 有能力 n>1 **首呼** ⇒ `result.resultType === 'input_required'` 且 `inputRequests.confirm` 存在;**且 `operation_batches` 零行**;**且 notes 逐字段与 before 全等** | 令首呼顺手写一笔 proposed ⇒ **红**(杀「零持久」被破) |
| **K-c3** ⭐⭐ | `confirm:true` 重试 ⇒ **真执行**、收据 `applied`、**note 真进回收站**;**且 executor 实际调用次数 === n** | 令执行走 no-op ⇒ 红;**令重试执行两遍(返回码仍 200)⇒ 必须红** |
| **K-c4** | `confirm:false` 重试 ⇒ 零执行、**`operation_batches` 零行**,`decision === 'rejected'` | 令它写一笔 `dismissed` 收据 ⇒ **红**(预裁①明写「不写收据」) |
| **K-c5** ⭐⭐ | **decline / cancel 重试 ⇒ 零执行、零收据,且 `resultType` 必须是 complete 而非 `input_required`** | 令拒绝回到重问 ⇒ **红**(杀无限逼问) |
| **K-c10** ⭐ **(Fable 裁定加设)** | **拒绝形状四面齐验,三种 decision 各一条**:`_meta['io.coincides/toolFaceDecision'].decision` **存在且取值正确** · `structuredContent.results` **为空数组** · **executor 计数 === 0** · **`operation_batches` 行数与调用前全等** | 抽掉 `_meta.decision` ⇒ 红;令 `results` 带上待删 id(「看起来像回执」)⇒ 红;⛔ **三种 decision 不得合并成一条断言** —— 合并会让「三者都返回 declined」蒙混过关 |
| **K-c6** | 首呼后**不重试就断开** ⇒ 全库零持久 | 任何残留行 ⇒ 红 |
| **K-c7** ⭐ **mutation** | 令 `supportsFormElicitation` **恒返回 `true`** | **K-c1(无能力路径)必红** —— 预裁④原话 |
| **K-c8** ⭐ **mutation** | 把 confirm 分支的 `inputResponse` **换成 `acceptedContent`** | **K-c5 必红** —— 这一刀直接杀死「照抄 SDK 官方示例」 |
| **K-c9** | 表单 `message` 含**受影响条目数与 id 摘要** | 抽掉摘要 ⇒ 红 |

**红的性质**:须来自「机关存在但被改坏」,**不得是 `ReferenceError` / `SyntaxError` / `ERR_MODULE_NOT_FOUND`**;HTTP 层 killer 须**从真实 Express 触发**。

### 本单须新增的 canonical import 断言(承 b-2b-1-fix / b-3 第 7-8 条)

| # | 文件 | 符号 | 来源 |
|---|---|---|---|
| 9 | `server/src/mcp/transport.ts` | `inputRequired` | `@modelcontextprotocol/server` |
| 10 | `server/src/mcp/transport.ts` | `inputResponse` | `@modelcontextprotocol/server` |

⚠️ **复用既有 `assertCanonicalNamedImport`,⛔ 不得另写一套**;来源按**解析后**比对。
📌 第 9/10 条的 specifier 是**裸包名**(非相对路径)—— **若既有 helper 的解析路径对裸包名不成立,停下来申报,⛔ 不得为绕过它而把断言降级成字符串相等。**(这正是 b-2b-1-fix C8 那一刀护的东西。)

---

## 硬闸

- **H-1**:确认后的执行**必经 `trashNoteAsUser`**(承 b-3),⛔ 不得内联 SQL、不得另写编排。
- **H-2**:⛔ **不改 `schema.sql` / migration**;确认执行写 `tier:'immediate'`(实况 4),⛔ 不得改 `statusForTier`。
- **H-3**:⛔ **不用 `requestState`**。**若 SDK 在实现中被证明强制要求它** —— 停下来申报,**不得自行塞业务语义**;届时唯一许可形状是「只放 `input_digest`,且在回执申报」(预裁②)。
- **H-4**:⛔ **decline / cancel 绝不重问**(见缺口一节);拒绝形状按 S2「拒绝形状定义」四面齐备,⛔ **`_meta` 不得带收据键**,⛔ **不得为 `decision` 动 `output_schema`**。
- **H-5**:⛔ **`input_required` 轮零持久**(不写表、不留进程内状态)。
- **H-6**:⛔ 不得放宽 `supportsFormElicitation` 的双条件谓词(它是 K-c7 的靶子)。

## 边界

**允许**:改 `server/src/mcp/policy.ts` confirm 分支 · 改 `server/src/mcp/transport.ts` confirm 档与 `ToolCallDispatchContext` · 扩测试夹具 `mcpPost` · 改 K-5 / K-b2 并新增 K-c 系 · 新增第 9/10 条 canonical import 断言。

**⛔ 不得**:改 schema / migration · **改 registry 里 `trash_notes` 的 `output_schema`**(拒绝形状靠 `_meta` 承载,正是为了不动它) · 改 `trashNoteAsUser` 语义 · 碰 b-3 的三条路由与队列页(**除非「待填」段结论要求**,那时须回单) · 碰 registry / manifest / parity 的既有闭环 · 碰 12.1 线 / v1 其余线 · 顺手做 S4 读面。

**越界即停,标 `needs: claude`。**

## ⚠️ 基线缺口:不得代偿

`scopes` 未强制(TD-14,**回执不得声称已强制授权**)· TD-6 · TD-8 残余 · TD-10 · TD-12(EPERM) · TD-16 ESM 环(**不在本单改**)。

## D. 探针 / 锁 / 环境

阴性断言前先让同一探针看见已知阳性;**确认命中不是来自你自己刚写进去的东西**。
**锁**:取锁失败即停,⛔ 不得覆盖既有 `owner.json`;非你所写的锁不删。
📌 porcelain 对 `client/.../useNoteCanvasRuntimeController.ts`、`server/src/routes/projections.ts` 有 stat/EOL 假阳性;判文件真改用 blob 哈希或 `git diff --numstat`(**`--stat` 看不见 untracked**);管道遮蔽退出码;`grep -c` 数**行数**不是**出现数**;单行 regex 抓不到跨行 import。

## 验证与回执

门禁 **docs-first**:`docs:check` → `verify:v2-bn8-runtime` → client/server 双 `tsc --noEmit` → `test:unit` → server `test:v2` → 五道 tool-face 门。

**回执纪律**:README Builder 侧 1–3(含 **UTF-8**)+ **M-1 mutation 归复核方** + **M-2 header 不由你翻**。
**⭐ 写 `## Result` 是本单交付物之一,不需确认,直接写。**

**回执须含**:K-c1…K-c10 **各自**先红后绿两段输出(**K-c3 须证明「实际调用次数」而非返回码**;**K-c5 须证明拒绝后不再被问**;**K-c10 三种 decision 须各一条,不得合并**)· 第 9/10 条 canonical import 断言(**裸包名解析是否成立须明确申报**)· `statusForTier` 未被改动的证明 · `output_schema` 未被改动的证明 · `schema.sql` `--numstat` 为 0 的证明 · **零持久三处(K-c2 / K-c4 / K-c6)的表级证据** · 门禁逐条收据 · 触及面 diff vs 申报 · 显式范围排除 · **每条阴性断言的阳性对照**。

## Result

完成。按 M-2 保持 header 原样，未翻 `status`；以下基于基线 `7e985bbdbc1eb53d107f1388c3d20c6b1ecc36e1`。

### 实现结果

- `policy.ts` 撤销 confirm 的声明性降级：无 form capability 仍为 `propose`，`n === 1` 仍为 `immediate`，有 form capability 且 `n > 1` 现为 `confirm`；`supportsFormElicitation` 双谓词及 threshold 捷径未改。
- `transport.ts` 用 canonical `inputRequired` / `inputResponse` 实现无状态 MRTR。`inputResponses` 从 `context.mcpReq.inputResponses` 进入 dispatch context；测试夹具把它放在 `params` 顶层，与 `_meta` 平级。
- 首轮 missing、sampling、roots、畸形 accept 均回 `input_required`；只带 `inputRequests.confirm`，没有 `requestState`，没有服务端轮次状态，也不写收据。
- `accept + confirm:true` 汇入既有 immediate 尾段，逐条走注入 binding（其生产实现仍经 `trashNoteAsUser`），写 `tier:'immediate'` / `status:'applied'`，并复用 `appliedResources(value)` 保存真实结果。
- `accept + confirm:false`、decline、cancel 由 `inputResponse()` 分成三支，统一返回非错误 complete、`{results:[]}`，分别写 `rejected` / `declined` / `cancelled` 到 `_meta['io.coincides/toolFaceDecision']`；应用代码不写 receipt meta。真实 HTTP wire 另有 SDK 自动盖的 `io.modelcontextprotocol/serverInfo`，测试只把 Coincides-owned metadata 精确约束为 decision。
- `decision` 未进入 `structuredContent`，没有改 `output_schema`。没有使用 `acceptedContent` 判断轮次，也没有读取 `_meta.inputResponses`。
- 表单 schema 精确为单 boolean `confirm`，message 含条目数和全部 ID。generic confirm 探针无 batch ids 时使用安全 fallback。
- SDK 丢弃非 bare `inputResponses` 后会按 missing 重问；未对 `droppedInputResponseKeys` 加补偿逻辑。

### K-c 红绿证据

测试先落地后、生产实现尚保持基线时，`npm.cmd run test:mcp-transport` 输出 `tests 24 / pass 12 / fail 12`；旧 confirm 降级使 K-5、K-b3、K-c2/3/4/5/6/9/10/11/12 等真实 HTTP 测试变红。完成实现并恢复所有临时突变后，同命令输出 `tests 24 / pass 24 / fail 0`。下表逐项列出独立红、绿落点；builder 侧突变只作机关烟测，正式 M-1 仍归 reviewer。

| killer | RED 输出/证据 | GREEN 输出/证据 |
|---|---|---|
| K-c1 | 临时令 `supportsFormElicitation` 恒 true；真实 HTTP `K-b1/K-c1` 报 `actual 400 !== expected 200` | 恢复双谓词后该 top-level test `ok`；executor `[]`，恰写 1 条 `proposed` 阳性收据 |
| K-c2 | 基线旧生产在 `K-b2/K-c2/K-c9` 回 `complete` 而非 `input_required`，test `not ok` | test `ok`；executor `[]`，`SELECT * FROM notes` 全字段前后相等，`SELECT * FROM operation_batches` 行集合前后相等 |
| K-c3 | 临时令 confirm accept 调 binding 两次；断言显示实际 4 个 executor id、期望 2 个，HTTP 仍为 200 | test `ok`；executor 序列精确等于 2 个输入 id，各一次；notes 均 `trashed`，表中恰 1 条 `applied` / `immediate` 收据 |
| K-c4 | 临时在 rejected 分支暗写 proposed receipt；返回形状仍通过，最终只在表证据红：`actual [status:'proposed']`、`expected []` | 独立 `K-c4/K-c10 rejected` test `ok`；executor 0、notes 全字段不变、operation rows 全等 |
| K-c5 decline | 把判别改成 SDK 示例式 `acceptedContent` 后，独立 decline test 报 `input_required !== complete` | 独立 declined test `ok`；complete、executor 0、operation rows 全等 |
| K-c5 cancel | 基线旧生产下独立 cancelled test `not ok` | 独立 cancelled test `ok`；complete、executor 0、operation rows 全等 |
| K-c6 | 基线旧生产首轮不是 `input_required`，独立 disconnect test `not ok` | test `ok`；除 executor/notes/operation rows 外，`db.serialize()` 整库页级快照也前后全等 |
| K-c7 | 即 K-c1 能力谓词恒 true 突变：`actual 400 !== expected 200` | 恢复原双条件谓词后 K-c1 `ok` |
| K-c8 | 用 `acceptedContent` 取代 `inputResponse` 判别：decline test `input_required !== complete` | 恢复 `inputResponse` 后 declined、cancelled 两个独立 test 均 `ok` |
| K-c9 | 临时把 message 缩为 `Confirm trash_notes.`；真实 HTTP test 精确红于 `did not match /\b2\b/` | 恢复后 test `ok`；数量 `2` 与两个完整 note id 分别断言 |
| K-c10 rejected | 基线旧生产的独立 rejected top-level test `not ok`；另见 K-c4 暗写行突变 | 独立 test `ok`；`decision:'rejected'`、空 results、executor 0、operation rows 全等、无 receipt meta |
| K-c10 declined | 基线旧生产及 `acceptedContent` 突变下独立 declined test `not ok` | 独立 test `ok`；`decision:'declined'` 四面齐验 |
| K-c10 cancelled | 基线旧生产下独立 cancelled test `not ok` | 独立 test `ok`；`decision:'cancelled'` 四面齐验；三种 decision 未合并成一条 test |
| K-c11 | 基线旧生产的 replay test `not ok` | 两次 accept 使用相同 input、相同 JSON-RPC id/source_id；executor 总数精确 `2n`，同 source_id 下有 2 个不同 receipt id 且均 `applied/immediate`；第二次逐条 `skipped/already_trashed`，notes 全字段相对第一次执行后不再变化 |
| K-c12 | 临时令 accept 收据复用 `proposedResources()`；期望 `trashed/skipped/missing`、实际全为 `pending`，test `not ok` | mixed active/already-trashed/missing test `ok`；actual resources 精确为 `trashed`、`skipped+already_trashed`、`missing`，零 `pending`；accept 前后收据数恰 1，真实 b-3 revert 后仍恰 1 且同 receipt 变 `reverted`，只恢复本次真实 trashed 的 note |

K-c10 三种 decision 是三个独立 top-level test；共同 helper 只承载四面形状，每条各传并断言自己的 expected decision，因此“三者都回 declined”不能假绿。

### canonical import 与结构证明

- 新增断言复用既有 `assertCanonicalNamedImport`，解析裸包名 `@modelcontextprotocol/server` 后分别验证 `transport.ts` 的 `inputRequired`、`inputResponse` named import；裸包解析成立。
- 新断言落地而生产 imports 尚未补齐时，`test:trash-notes-tool` 输出 `tests 40 / pass 39 / fail 1`（第 9 条缺失）；补齐后输出 `tests 40 / pass 40 / fail 0`，第 9/10 条均通过解析后比对。
- `git diff --numstat 7e985bb -- server/src/services/toolFaceReceipts.ts server/src/toolFace/registry.ts server/src/db/schema.sql server/src/db/migrations server/src/bindings/notes.ts server/src/routes/toolReceipts.ts client/src/pages/ToolReceipts`：空输出。由此分别证明 `statusForTier`、registry `output_schema`、`schema.sql`（`--numstat` 为 0）、全部 migrations、`trashNoteAsUser` binding、b-3 routes/revert、队列页均未改。
- 上述阴性 diff 的阳性对照：同一基线下 `policy.ts` 为 `1/2`、`transport.ts` 为 `75/5` numstat；探针确实能看见本单生产改动。
- 其他阳性对照：K-c3 能看见 executor n 次、applied row 与 receipt meta；K-c1 能看见 proposed row/pending resources；K-c12 能看见 actual resources 及真实 revert 状态变化。零执行、零行、无 receipt meta、零 pending 均不是空探针。

### J-1 / J-2 与范围声明

- J-1：没有新建去重机制。同一精确 accepted request 重放会再次调用 n 个 executor 并再写一笔独立收据；第二轮不再次改变 notes 是 `trashNoteAsUser` 的 `already_trashed` 守卫结果，不宣称 confirm accept 获得 b-3 的事务保护或幂等保证。
- J-2：accept 只用 `appliedResources(value)` 写 actual resources；没有把 pending proposal 标 applied。K-c12 已端到端证明 b-3 revert 的 `outcome:'trashed'` 因果筛选仍可恢复目标。
- 有能力客户端的批量 confirm 不再产 proposed、不再进入人审队列；无能力路径仍产 proposed，队列入料不断流。队列构成变化是本单设计结果。
- 没有新增与既有机关平行的状态存储、恢复载体、守卫层或事务边界。
- 未做 S4 读面；未声称 `scopes`/TD-14 已强制、TD-6 已解决、多资源 revert 原子、或 MRTR 有服务端持久轮次。
- 未触碰 schema/migration、registry/manifest/parity 既有闭环、`statusForTier`、`trashNoteAsUser` 语义、b-3 三条路由与队列页、12.1/v1 线。

### 触及面

本单代码与测试：

- `server/src/mcp/policy.ts`
- `server/src/mcp/transport.ts`
- `server/src/__tests__/v2McpTransport.test.ts`
- `server/src/__tests__/v2NotesListService.test.ts`
- 本 handoff 的 `## Result`

按文档纪律已运行 `docs:index`；该 handoff 原已在索引中，命令输出 `写入 0 个 INDEX.md`，因此本单没有 INDEX 生成件 diff，未虚报或漏装 tracked `M`。

共享树中 `docs/agent-ops/claude-log/2026-08-23.md` 的 18 行修改不是本单产物，未改、未纳入申报；既有 untracked `.claude/settings.local.json` 亦未触碰。porcelain 所列 `useNoteCanvasRuntimeController.ts`、`projections.ts` 经 `git diff --numstat 7e985bb -- <两路径>` 为空，确认是已知 stat/EOL 假阳性。`.codex-tmp/builder.lock.d` 非本 builder 所有，未取锁、未写 `owner.json`、未删锁。

### 门禁收据

按 docs-first 顺序执行；写入本回执后又完成最终 docs 生成与检查。

| 门 | 结果 |
|---|---|
| `npm.cmd run docs:check`（实现后、回执前） | PASS；index 与 inventory fresh |
| `npm.cmd run verify:v2-bn8-runtime` | PASS；client tests、tool-face gates、build、canvas performance、docs、secret scan 全部完成，exit 0 |
| client `npm.cmd exec tsc -- --noEmit` | PASS，exit 0 |
| server `npm.cmd exec tsc -- --noEmit` | PASS，exit 0；最终测试补强后再次 PASS |
| `npm.cmd run test:unit` | PASS；20 files / 211 tests |
| server `npm.cmd run test:v2` | PASS；270/270；首跑即按 TD-12 用 OS temp 下 `CANVAS_ASSET_DIR`，未改测试或产品语义 |
| `npm.cmd run test:tool-face-registry` | PASS；4/4 |
| `npm.cmd run test:tool-face-manifest` | PASS；10/10 |
| `npm.cmd run check:tool-face-manifest` | PASS；2 entries / 2 public，fresh |
| `npm.cmd run test:tool-face-parity` | PASS；10/10 |
| `npm.cmd run check:tool-face-parity` | PASS；2 public entries checked |
| server `npm.cmd run test:mcp-transport` | PASS；24/24（所有突变恢复并补强后最终全跑） |
| server `npm.cmd run test:trash-notes-tool` | PASS；40/40 |
| `git diff --check` | PASS |
| `npm.cmd run docs:index` | PASS；写入 0 个 INDEX.md（现有索引无变化） |
| 最终 `npm.cmd run docs:check` | PASS；index 与 inventory fresh |

没有提交、暂存、push、PR 或 merge；交付为当前工作树差异，供调度方按申报触及面点名装配。`docs:index` 未产生需要同批装配的 tracked 生成件。

## Review

> reviewer: Codex reviewer（洁净室复核） | date: 2026-08-23
>
> review baseline: `c924cd5b63d271bf94aa12b0d4b231c5c53b91bb`
>
> previous baseline: `7e985bbdbc1eb53d107f1388c3d20c6b1ecc36e1`
>
> **判定：FAIL（方向成立）**
>
> **分级：0 BLOCKER / 0 HIGH / 1 MED / 0 LOW**
>
> 这是复核报告，不是放行；放行权仍在 Fable。

### 1. 判定摘要与唯一 finding

本单的生产方向成立：有 `elicitation.form` 的批量 `trash_notes` 已从声明性降级换成真实 MRTR；拒绝三态、能力谓词、执行次数、首轮 / 拒绝 / 断开零持久、拒绝 `_meta`、actual receipt resources、既有无能力 proposal、b-3 revert 与队列边界均经静态链和真实 HTTP 正反对照成立。未发现当前产品路径误执行、重复执行、假收据、不可撤或禁区越界。

FAIL 只来自一项点名 mutation 的端到端证据缺口：常驻 K-c12 会杀死 `pending` resources 刀，但在真实 revert 请求发出之前就抛出 AssertionError，未满足 Q4 指定的“须表现为 b-3 一条都恢复不了”。

#### MED-1（测试护栏 / Q4）：K-c12 的断言顺序截断了点名的 revert 后果

在精确 `c924cd5` 的隔离 clone 先跑 K-c12，baseline `1/1` 绿。随后只把 confirm accept 收据的 `appliedResources(value)` 换成 `proposedResources(entry, input)`；HTTP accept 仍为 200、收据仍写入。常驻 K-c12 确实红，但第一处红是 `v2McpTransport.test.ts:1045` 的 resources deepEqual：actual 为三条 `outcome:'pending'`，expected 为 `trashed / skipped / missing`。Node test 在这里停止，因此 `:1062` 的真实 `POST /api/tool-receipts/:id/revert` 根本没有执行，`:1069` 的 `revert_details` 也没有到达。

为确认不是理论推断，reviewer 在同一产品刀仍生效时做了一次仅诊断用测试刀：不删除、不放宽、不新增任何断言，只把既有 resources / zero-pending 断言移到 revert 断言之后。此时 K-c12 从真实 Express、auth、production route 发出 revert，HTTP 仍为 200，随后目标 AssertionError 为：actual `{ restored: [], failed: [] }`，expected `{ restored: [OLDER_NOTE_ID], failed: [] }`；OLDER note 仍是 `trashed`。这正是 Q4 要求的静默空转形状。诊断刀与产品刀随后均独立恢复，原 K-c12 再取 `1/1` 绿。

建议修法：保留现有 resources 与 zero-pending 断言，但调整其执行顺序或采用不会提前截断后续取证的结构，使点名 mutation 必须先穿过真实 revert，并在 `revert_details` / 最终 notes 状态上出现“一条都恢复不了”的红；修后再独立复刻同刀。Reviewer 未代 builder 修改测试。此项记 MED：当前产品与 b-3 路径正确，且该 mutation 不是假绿；但工单明确要求的关键端到端红形未由常驻测试本身产出，不能追认完成。

### 2. Q1–Q6 对抗复核

每刀前先跑对应阳性为绿；每刀独立施加、只跑精确命名测试、取红后逆 patch，再跑同一测试取绿。所有预期红都是目标 `AssertionError`，没有 `ReferenceError`、`SyntaxError` 或 `ERR_MODULE_NOT_FOUND`；HTTP 刀均从真实 Express + TCP 请求进入生产 handler。

| 位点 | Reviewer 亲刀收据 | 判定 |
|---|---|---|
| Q1 `acceptedContent` 签名刀 | 按 SDK 官方示例改用 `acceptedContent(..., 'confirm')` 后，declined 与 cancelled 两条独立测试均红在 `a refusal must not ask again`：expected `complete`、actual `input_required`，`0 pass / 2 fail`。SDK 声明也确认 missing / decline / cancel 都投影为 `undefined`。恢复后 `2/2` 绿。 | PASS |
| Q2 能力谓词恒 true | K-b1/K-c1 无能力真实 HTTP 从 expected 200 变 actual 400，目标 AssertionError；恢复后 `1/1` 绿。 | PASS |
| Q3 confirm:true 执行两遍仍回 200 | HTTP 200 与 complete 正控先过，随后 executor call list actual 为四个 id、expected 为两个 id，精确红在“must call ... exactly n times”；恢复后 `1/1` 绿。 | PASS |
| Q4 actual resources 改 pending | 常驻 K-c12 会红，但先死在 resources shape，未到真实 revert；诊断性重排后才得到 `restored: []`。 | **FAIL；形成 MED-1** |
| Q5 三 decision 合并为 declined | rejected、declined、cancelled 是三条独立 top-level tests；结果恰为 `1 pass / 2 fail`：declined 绿，rejected 与 cancelled 分别红在各自 expected decision。恢复后 `3/3` 绿。 | PASS |
| Q6 首呼顺手写 proposed | K-c2、K-c4、K-c6 三条分别红在 operation rows 全行快照，多出 proposed/pending 收据，`0 pass / 3 fail`；恢复后 `3/3` 绿。notes 探针使用 `SELECT *` 覆盖 fixture 中全部两条 note，operation_batches 使用 `SELECT * ORDER BY id`；K-c6 另锁 `db.serialize()` 全库字节。 | PASS |

### 3. Q7–Q9：既有护栏、metadata 与禁区

- **Q7 PASS。** `7e985bb..c924cd5` 中 K-b1 只扩了标题；函数体逐字符不变，仍是 `await assertProposedBatch({}, 81);`。共享 helper 反而新增 executor 计数、notes 逐字段全等与 receipt 数量正控，原 proposal / pending / intended_input / receipt 断言均保留。从 K-b6 到文件 EOF 的 94 行前后 SHA-256 同为 `633ef167dc3150449e510701853e179381db46f52efc3b480a725f295e48e827`；错误重定向、敏感路径遮蔽、AppError 投影与零成功收据均未放宽。
- **Q8 PASS。** rejected / declined / cancelled 各自独立走 `rejectedResult`；应用层 `_meta` 只含 `io.coincides/toolFaceDecision`。真实 wire 测试按 Coincides namespace 精确枚举该唯一键并断言 receipt key 为 `undefined`；SDK 自带的 serverInfo 不被误算。immediate 与 confirm accept 阳性均另断言 receipt id / status 存在。
- **Q9 PASS。** 对 `server/src/db/`、`server/src/toolFace/`、`server/src/services/toolFaceReceipts.ts`、`server/src/routes/toolReceipts.ts`、`client/src/pages/ToolReceipts/` 跑完整目录 `git diff --numstat` 与 name-status 均为空；各 tree / blob OID 在两基线相同。阳性对照能看见 `v2McpTransport.test.ts +502/-65`、`policy.ts +1/-2`、`transport.ts +75/-5`，不是空探针。

### 4. Q10 门禁收据

按 docs-first 顺序亲跑：

| 门 | Reviewer 结果 |
|---|---|
| `npm.cmd run docs:check` | PASS；精确 clone、LF checkout，index / inventory fresh |
| `npm.cmd run verify:v2-bn8-runtime` | PASS，exit 0；unit、runtime contracts、五道 tool-face、双端 build、canvas performance、docs 与 secret scan 全到达 |
| client / server `npm.cmd exec tsc -- --noEmit` | PASS / PASS |
| `npm.cmd run test:unit` | PASS；20 files / 211 tests |
| server `npm.cmd run test:v2` | PASS；270/270；`CANVAS_ASSET_DIR` 指向新建 OS temp 空目录，未改测试或产品语义 |
| server `npm.cmd run test:mcp-transport` | PASS；24/24 |
| server `npm.cmd run test:trash-notes-tool` | PASS；40/40 |
| `test:tool-face-registry` | PASS；4/4 |
| `test:tool-face-manifest` | PASS；10/10 |
| `check:tool-face-manifest` | PASS；2 entries / 2 public，fresh |
| `test:tool-face-parity` | PASS；10/10 |
| `check:tool-face-parity` | PASS；2 public entries checked |

第一次隔离 clone 因系统 `core.autocrlf=true` 把 LF 文档检出为 CRLF，`docs:check` 报 9 个 index 过期；同一 Git 对象以 `core.autocrlf=false` 在 checkout 前重建物理 clone 后首跑即 PASS，证明前者只是 checkout 形状，不归因于提交。OS-temp clone 内运行完整 verify 又被沙箱在 esbuild 读取盘符根目录时拒绝；因此 full verify 在共享树的目标 blob 等价状态亲跑，exit 0。此环境绕行没有改测试、产品或依赖语义。

### 5. Q11 跨条与结构语义

- confirm accept 的生产尾段只调用既有 binding，再用 `appliedResources(value)` 写 `tier:'immediate'` 收据；b-3 revert 仍只消费 causal `kind:'note' && outcome:'trashed'`。baseline K-c12 的真实 route 恢复 OLDER、保持 already-trashed 与 missing 不动；完整 trash-notes suite 40/40。
- 无 form 能力仍走 propose / pending；有能力批量改走 confirm、不再进入 proposed 队列，是本单设计中的入料减少。队列 list / apply / dismiss / revert route 与 `ToolReceipts` 页面 blobs 未改，未见功能回归。
- binding 仍逐 id 调 `trashNoteAsUser`；`statusForTier`、schema / migrations、registry output schema、revert service、队列页均未动。`requestState`、`acceptedContent` 未进入生产实现；三种拒绝动作分支提前 return，不持久轮次状态。

### 6. 隔离、恢复与共享树边界

- mutation 在 OS temp 下的精确 `c924cd5` 物理 clone 完成；server / client 均 `npm ci --offline` 独立安装，没有链接共享 `node_modules`，reparse point 数为 0。每刀恢复后，`policy.ts`、`transport.ts`、`v2McpTransport.test.ts` 的 blob 分别回到 `85f84db4bff52fe381f159bcd0fc845050bca335`、`eaa7c5d1c354e62e043cef4f1b45199ce5c36604`、`88d6b45e7077415683673e564f756678b3a517c5`，等于 HEAD；diff numstat / raw 为空。
- 共享树的 `useNoteCanvasRuntimeController.ts` 与 `projections.ts` 仍是 stat/EOL 假阳性，clean blob 分别等于 HEAD 的 `3efe5f820e2077850611b54d4d09482845e89545`、`561902a449b50ce254b650de5a337973a8fbc26d`。复核收尾时另出现他方的 `docs/agent-ops/INDEX.md` 修改与未跟踪 journey sheet；reviewer 未写、未覆盖、未清理。
- reviewer 未取锁、未写 `owner.json`、未删锁，未翻 header，未改产品或常驻测试，未 commit、push、PR 或 merge。
- 两份隔离 clone 与空的 TD-12 asset temp 目录已逐条验证都在 OS temp、无 reparse point；但本会话的执行策略在进程创建前拒绝了两次 `Remove-Item`（包括对空目录的非递归删除），因此未能自动清理。它们不在仓库工作树内，也未被作为复核依据留脏；路径与失败原因已在 reviewer 运行收据中保留，需由环境回收。
