> **From**: fable
> **To**: codex
> **Status**: ready(两层制;13.2 单 0,只读侦察;射程按段 plan 修订一收窄)
> **日期 (Date)**: 2026-09-07
> **性质**: 只读侦察单(⛔ 改任何产品代码;⛔ 实跑用户库)

# 13.2 · 单 0 · K-0 侦察(野地退役+坐标归一 双题)

## 〇 · 上游(先读,顺序)

1. 段 plan 含修订一:`plans/v13-2-wilderness-retirement-plan.md`;
2. 图三(迁移映射):`analysis/2026-09-07-wilderness-migration-mapping.md`;
3. 单 4 卷宗:`analysis/2026-09-07-v13-1-s4-coordinate-contract-recon.md`(读写链/判污 SQL 草案/A 表爆炸半径——**⛔ 重做其已覆盖面**);
4. 图二 §六(史记 v0 表设计,单 1 的图纸,本单只对表其前提)。

## 一 · 五题(文件:行号级证据)

1. **字面量登记面全仓 grep**:`canvas_workspace` / `crossing` / `surface` / `boundary_role` 的字符串与枚举登记点逐一归类(validators/canvasSurfaceAuthority/导出策略 exclude_workspace 族/生命周期策略类/测试)——退役=新写禁令要堵的每一个口;
2. **托盘拼接点**:`surface='tray'` 的最小落地面(server validator/类型/client 类型收窄)+ 托盘 UI 与 13.1 视口的拼接选项(侧栏/独立面板,各自要动的层,只列选项⛔ 选边);
3. **编号与命名**:迁移编号续位现状(047 之后),备份表命名惯例,schema.sql 与 migrations 双轨纪律现状;
4. **census SQL 完整化**:以单 4 §二 草案为底,扩成图三 §三 全 census:全对象分母(placement/object/mount,含非 paragraph 画物与 legacy-only)/dual-truth 数量/世界·局部·混合三解释归页差异/存储 boundary vs 几何重算差异/主帧重叠比分布与 oversize——合成库实跑过闸(⛔ 用户库),SQL 全文入报告;
5. **史记 v0 前提对表**:better-sqlite3 下 BEFORE UPDATE/DELETE 触发器 RAISE ABORT 语法实证(合成库);server 现有路由/事务结构里书记官中间件的挂点候选(列文件:行号,⛔ 设计实现)。

## 二 · 报告与回执

报告 `docs/agent-ops/analysis/2026-09-07-v13-2-s0-recon.md`(标准状态头,五题分节,末节「拆单建议」:单 1/2/3 的先后与最小改动面)。完工本文件末尾 apply_patch 追加 `## Result`(报告路径+五题一行摘要+未做清单)。⛔ 改产品代码;⛔ 实跑用户库;⛔ commit;⛔ 读 .env;⛔ 打印任何 key。

## Result

2026-09-07 · Codex 侦察完成。报告：[2026-09-07-v13-2-s0-recon.md](../analysis/2026-09-07-v13-2-s0-recon.md)（frozen，源码/合成收据，非实施放行）。已按 §〇 顺读段 plan（含修订一）、图三、单 4 全卷及图二 §六；沿用单 4 已覆盖的读写链与 A 表，未重做。

1. **登记面**：完成受限全仓文本检索及逐类登记，列明 validator/service/shared authority/client 草稿与各 kind writer、导出五策略、生命周期及测试；旧修复脚本也是 workspace/crossing 写口，退役不等于删历史值。
2. **托盘拼接**：定位多处“非 formal→workspace”和 server block reader“非 workspace→formal”回退；三表缺通用 order_index、mount 经 object→placement 派生去处；侧栏/独立面板两选项只列改动层，未选边。
3. **编号命名**：迁移已到 053，下一候选 054（开单再核）；图三指定三表 `_backup_pre13_2`，现有脚本是 `.codex-tmp` 整库文件备份；schema.sql→migrations 与 migration-only 家族据实对表。
4. **census**：完整 SQL 与可复跑探针全文入报告；仅 `:memory:`，query_only=ON、statement.readonly=true、total_changes 不变；P=46/O=45/M=30，legacy-only=1、歧义=2、dual-any=2/active=1，三解释 114 行，分桶 81 行，全分母及指定分类断言 PASS，exit 0。
5. **史记前提**：better-sqlite3 11.10.0 / SQLite 3.49.2 下两个 BEFORE 触发器 `RAISE(ABORT,'events_append_only')` 拒改删、同事务提交/回滚及触发器异常回滚动作均 PASS；列路由/事务候选挂点，未实现书记官。

拆单建议为单 1 → 单 2 → 单 3：账本前置，托盘排序/身份与读写贯通后再做双轨影子跑；未知解释与阈值留 HQ/人裁。当前工作树已有修改未触碰。

未做：产品代码/配置/依赖修改；用户库查询、真实 census/迁移/回滚；托盘/史记实现；S4 已覆盖面重侦察；typecheck/build、完整 `npm run verify:v2-bn8-runtime`、浏览器/打印/主观验收；`.env` 读取、key 输出；commit/push/PR/merge。合成 PASS 不替代后续实施的产品验证门。本回执只追加，工单原状态头与历史正文不回改。
