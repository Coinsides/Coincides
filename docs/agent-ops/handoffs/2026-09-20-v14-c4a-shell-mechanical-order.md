> **状态 (Status)**: ready(HQ 按代理权翻牌;14.5 壳批拆两半——本单=机械半;Home/悬浮窗/命令台=UX 原型半,候 Henry 商量⛔本单)
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-20
> **单号**: V14 Agent 墙 · C4a 壳批机械半(工具条分区+文档标签条+UI 控制动词)
> **上游**: `plans/v14-agent-era-plan-draft.md` §14.5(壳批,拍;「UX 原型驱动」部分明确留 C4b)+ 种子箱「工作台标签条」全款(标签=身份+视口/blur 即存零未保存态/⛔与 Staging·板合并/Agent 在场徽章+通知徽标落点)+ Henry 2026-09-20 实机点名工具条心智分区(HQ 四群方案,无异议照拍)+ B 静墨去盒纪律(B3 先例)。**设计裁量已完成,照拍施工⛔重开设计。**

# C4a · 壳批机械半

**性质**:笔记页 chrome 的秩序化+「在场」器官+Agent 的 UI 控制通道。⛔Home 重造⛔悬浮窗⛔任务队列⛔托盘命令台(全部 C4b 候商量)。

## 一 · 工具条四群分区(G5,Henry 点名)

1. 笔记页底部工具条重排为四群(群间发丝线分隔,B 静墨纪律):
   - **纸的状态**:Preview / Layout / 外观 / 装订;
   - **手上的笔**:Selection / Pen / Eraser(现役三钮 icon 化保留);
   - **「+ 插入」单入口菜单**:表格/时间线/柱图/折线图/媒体图/引文块样式/提示框样式全部收编为菜单项(现役 Insert table 等平铺钮退场);斜杠命令零变;菜单项文案与斜杠命令同词;
   - **看的方式**:视图(View options)/ 导航窗格 / Overview / 阅读步进与缩放;
2. 各钮现役行为零变(纯重排+收编);tooltip 保留;键盘可达;
3. 窄屏折叠策略申报(群优先级:笔>插入>状态>看)。

## 二 · 文档标签条(种子箱全款)

1. **标签=在场器官**:开着的纸/板在文档区顶部挂标签(Chrome 式);标签=身份+视口(切回恢复滚动位/阅读步进);**blur 即存,零未保存态包袱**(现役自动保存语义,标签零新真相——申报存法:会话级 UI 态,⛔持久化表;刷新后以当前打开页重建);
2. 点标签切换;中键/×关闭(关闭≠删除,纸回 project 列表);拖拽排序;
3. **Agent 在场徽章**:Agent 本会话正在操作的板/纸,其标签亮天青徽章(读现役 batch/收据活动,零新机关);**通知徽标落点**留位(计数气泡插槽,消费者候 C4b);
4. ⛔与板上 Staging/Recent/Favorites 合并(在场≠货≠记忆);⛔Tauri 撕窗(候 Tauri 批)。

## 三 · UI 控制动词族(contextHint 反向通道)

1. 注册两个 Agent 动词:`ui_open_note`(打开笔记并挂标签)/`ui_focus_object`(聚焦到块/页/板成员——滚动定位+短暂高亮);**零真相写**(effectClassification 归类申报——UI 控制=新第三类?⛔:归 channel_write(向人发出的呈现动作)或按现役分类法申报最贴切归属,完整性闸零漏);
2. 收据照记(✓ ui_open_note);频控申报(单轮上限,防抖动);用户当前正在输入时 focus 不抢焦点(申报守则);
3. 指纹 `--update`+说明书 §五 同步;随单 1 个 scripted 场景(动词调用→收据对账→零域写)。

## 四 · 验收与禁区

1. 定向:四群重排快照+插入菜单全项与斜杠等价+标签生命周期(开/切/关/排序/视口恢复/blur 存)+Agent 徽章亮灭+两动词正反例与收据+焦点守则;agent 族回归+client 全库+server 全量(**全量补集含 v13WildernessExecute,文件预算 ≥600s**);既有回归零破(十二砖全部/收据条/contextHint);
2. **说明书义务**:`current-state/app-operating-manual.md` 补工具条分区/标签条/两动词条目;
3. 证据落 `docs/audits/2026-09-20-c4a-shell-builder/`(蒸馏件+分区前后截图),原始日志留 `.codex-tmp/c4a-shell/`;
4. **禁区(全部带射程)**:⛔一切 git 写操作;只读 git 明文允许(含验证门内部);git 检查+secrets 两组件留 HQ,其余按「非 git/secrets N 组件」申报;注册表/prompt 仅限本单两动词及义务面(⛔动既有动词⛔新写门);⛔Home/悬浮窗/任务队列/托盘(C4b);⛔Relation/判断域⛔笔记写权;⛔TextFlow 真相 schema;⛔坐标契约;⛔新依赖;⛔新真相表(标签=会话 UI 态);⛔真实模型调用;⛔用户库;⛔新设计安全对抗类用例(既有功能回归全库整跑明文允许零排除);合成凭据形值 ≤20 字符;新 `--sk-` token 后段 ≤18 字符。Result:分区前后截图+标签存法申报+两动词注册申报(分类归属论证+指纹)+场景断言表+逐件行号+测试数字+说明书申报+未做项。冲突停线举证。
