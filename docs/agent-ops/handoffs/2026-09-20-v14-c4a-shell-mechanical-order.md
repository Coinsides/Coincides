> **状态 (Status)**: done(二轮施工;一轮停线=斜杠条款互咬(第四型合同缺陷),补遗一裁「补齐七项斜杠+15 项零变」后续派;HQ 收口:client 227 文件 2319/2319 亲跑定案;builder 侧 shared/dist EPERM 环境红由 HQ 亲跑 tsc -b shared 转绿(server build exit 0,v2McpArtifact 2/2);双门绿;余 2=Python 基线;2026-09-20)
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-20
> **单号**: V14 Agent 墙 · C4a 壳批机械半(工具条分区+文档标签条+UI 控制动词)
> **上游**: `plans/v14-agent-era-plan-draft.md` §14.5(壳批,拍;「UX 原型驱动」部分明确留 C4b)+ 种子箱「工作台标签条」全款(标签=身份+视口/blur 即存零未保存态/⛔与 Staging·板合并/Agent 在场徽章+通知徽标落点)+ Henry 2026-09-20 实机点名工具条心智分区(HQ 四群方案,无异议照拍)+ B 静墨去盒纪律(B3 先例)。**设计裁量已完成,照拍施工⛔重开设计。**

# C4a · 壳批机械半

**性质**:笔记页 chrome 的秩序化+「在场」器官+Agent 的 UI 控制通道。⛔Home 重造⛔悬浮窗⛔任务队列⛔托盘命令台(全部 C4b 候商量)。

## 一 · 工具条四群分区(G5,Henry 点名)

1. 笔记页底部工具条重排为四群(群间发丝线分隔,B 静墨纪律):
   - **纸的状态**:Preview / Layout / 外观 / 装订;
   - **手上的笔**:Selection / Pen / Eraser(现役三钮 icon 化保留);
   - **「+ 插入」单入口菜单**:表格/时间线/柱图/折线图/媒体图/引文块样式/提示框样式全部收编为菜单项(现役 Insert table 等平铺钮退场);斜杠命令零变;菜单项文案与斜杠命令同词;
   - **看的方式**:视图(View options)/ 导航窗格 / Overview / 阅读步进与缩放;
2. 各钮现役行为零变(纯重排+收编);tooltip 保留;键盘可达;
3. 窄屏折叠策略申报(群优先级:笔>插入>状态>看)。

## 二 · 文档标签条(种子箱全款)

1. **标签=在场器官**:开着的纸/板在文档区顶部挂标签(Chrome 式);标签=身份+视口(切回恢复滚动位/阅读步进);**blur 即存,零未保存态包袱**(现役自动保存语义,标签零新真相——申报存法:会话级 UI 态,⛔持久化表;刷新后以当前打开页重建);
2. 点标签切换;中键/×关闭(关闭≠删除,纸回 project 列表);拖拽排序;
3. **Agent 在场徽章**:Agent 本会话正在操作的板/纸,其标签亮天青徽章(读现役 batch/收据活动,零新机关);**通知徽标落点**留位(计数气泡插槽,消费者候 C4b);
4. ⛔与板上 Staging/Recent/Favorites 合并(在场≠货≠记忆);⛔Tauri 撕窗(候 Tauri 批)。

## 三 · UI 控制动词族(contextHint 反向通道)

1. 注册两个 Agent 动词:`ui_open_note`(打开笔记并挂标签)/`ui_focus_object`(聚焦到块/页/板成员——滚动定位+短暂高亮);**零真相写**(effectClassification 归类申报——UI 控制=新第三类?⛔:归 channel_write(向人发出的呈现动作)或按现役分类法申报最贴切归属,完整性闸零漏);
2. 收据照记(✓ ui_open_note);频控申报(单轮上限,防抖动);用户当前正在输入时 focus 不抢焦点(申报守则);
3. 指纹 `--update`+说明书 §五 同步;随单 1 个 scripted 场景(动词调用→收据对账→零域写)。

## 四 · 验收与禁区

1. 定向:四群重排快照+插入菜单全项与斜杠等价+标签生命周期(开/切/关/排序/视口恢复/blur 存)+Agent 徽章亮灭+两动词正反例与收据+焦点守则;agent 族回归+client 全库+server 全量(**全量补集含 v13WildernessExecute,文件预算 ≥600s**);既有回归零破(十二砖全部/收据条/contextHint);
2. **说明书义务**:`current-state/app-operating-manual.md` 补工具条分区/标签条/两动词条目;
3. 证据落 `docs/audits/2026-09-20-c4a-shell-builder/`(蒸馏件+分区前后截图),原始日志留 `.codex-tmp/c4a-shell/`;
4. **禁区(全部带射程)**:⛔一切 git 写操作;只读 git 明文允许(含验证门内部);git 检查+secrets 两组件留 HQ,其余按「非 git/secrets N 组件」申报;注册表/prompt 仅限本单两动词及义务面(⛔动既有动词⛔新写门);⛔Home/悬浮窗/任务队列/托盘(C4b);⛔Relation/判断域⛔笔记写权;⛔TextFlow 真相 schema;⛔坐标契约;⛔新依赖;⛔新真相表(标签=会话 UI 态);⛔真实模型调用;⛔用户库;⛔新设计安全对抗类用例(既有功能回归全库整跑明文允许零排除);合成凭据形值 ≤20 字符;新 `--sk-` token 后段 ≤18 字符。Result:分区前后截图+标签存法申报+两动词注册申报(分类归属论证+指纹)+场景断言表+逐件行号+测试数字+说明书申报+未做项。冲突停线举证。

## Result

2026-09-20 · from: codex(builder) · **STOPPED：考古发现工单冲突，未施工、未验收。** 按用户与本单「冲突停线举证」要求回填后停止；保留原 ready 状态头，不标 done。

**阻断**：§一.1 的「斜杠命令零变」与 §四.1 的「插入菜单全项与斜杠等价」在现物上不能同时成立。`client/src/pages/Notes/noteSlashCommands.ts:37` 的完整注册表只有15项，没有表格、时间线、柱图、折线图、媒体图、B3引文块样式、B3提示框样式的等价命令；`:195` 只过滤该表，`canvasEngine/hooks/useSlashCommandController.ts:210` 只追加禁用原因，没有动态增员。已枚举 client/src 全部649个 .ts/.tsx 文件复核引用，七项差集为 **7/7**。

`Source Quote`（`noteSlashCommands.ts:116`）是 TextFlow `writingRole:'quote'` 转换；B3 样式（`canvasEngine/paragraphFurniture.ts:7`、`:30`）写 placement 的 `display_overrides_json.paragraph_furniture_v1`。两者不是同一行为，不可用同名或相近文案冒充等价。补七项斜杠会违反「零变」，不补则无法满足「全项等价」。继续前需补遗明确允许补齐斜杠，或调整该等价要求；builder 本轮不代选、不扩大施工。

蒸馏证据：[builder.md](../../audits/2026-09-20-c4a-shell-builder/builder.md)，含全钮盘点（19个含条件按钮）、B3去盒先例、contextHint/三分类/收据链、逐件行号及未做项。原始日志 `.codex-tmp/c4a-shell/exec.log`，独立源码证据 `.codex-tmp/c4a-shell/stop-evidence.log`（采集脚本同目录 `capture-stop-evidence.mjs`）。分类初判：不可硬塞 AGENT_ACTION_TOOLS；独立 UI 注册组按本单窄范围授权归 channel_write 可保持既有集合不变，**尚未实施，分类不是本次停线原因**。

**实际完成与未做**：产品代码改动0；功能测试0；非 git/secrets runtime 门0/23组件，未跑 client/server/agent 全量及 wilderness（≥600s 预算义务未解除）；无前后截图、标签实现、两动词注册、场景验证、指纹更新或说明书更新。仅写考古证据与本 Result；无 git 写、无 commit，未接触用户库或真实模型。工作树留 HQ，不能视为完工或放行。


## 补遗一(2026-09-20 · HQ 裁定:斜杠条款互咬消解)

停线成立,合同缺陷在 HQ(第四型:同单两义务互咬——「零变」保守条款与「全项等价」进取条款在现物上不可并立;现役斜杠表 15 项无七插入命令)。裁定即时生效:

1. **「斜杠命令零变」收窄为**:既有 15 项斜杠命令的行为与文案零变;
2. **明文允许补齐七项插入斜杠命令**(表格/时间线/柱图/折线图/媒体图/引文框样式/提示框样式)——菜单与斜杠=同词双入口,行为同源;
3. **命名区分照 builder 举证**:`Source Quote`(writingRole 转换)零动;B3 两样式命令文案用「引文框」「提示框」,⛔与 Source Quote 同名相近;
4. **两 UI 动词分类**:按 builder 初判照准——独立 UI 注册组归 channel_write,既有三封闭集语义不变,完整性闸零漏;
5. 其余条款原字不动。一轮停线 Result 保留为档;续派恢复施工,完工后另起「## Result(二轮)」。

## Result(二轮)

2026-09-20 · from: codex(builder) · **按补遗一施工完成；验证仍有环境阻塞，工作树交 HQ，未标全绿或放行。** 一轮 Result 原样保留。没有 git 写操作、没有 commit。

**交付**：四群工具条、七项菜单/斜杠同词同执行器、会话文档标签条、Agent 天青活动徽章及空通知插槽、两 UI 呈现动词、独立第13 scripted 场景、说明书 v2 与知识指纹。既有15项斜杠对象文本校验相同，Source Quote 零动；B3 两项命名「引文框」「提示框」。宽屏纸→笔→插入→看，≤1100px 折叠看、≤760px 再折叠纸，笔与插入常驻。

**标签存法**：仅 Zustand 内存，无新表、无 localStorage/sessionStorage。身份保存纸滚动/阅读档位/步进或板视口；刷新只以当前URL重建。去重开、点切、×/中键关、拖拽排序及键盘等价入口已接；关闭不删除，有邻页切邻页，最后纸回所属 Project、最后板回 Boards。离页沿纸原 blur/flush 屏障，板等待纸模态 requestClose、viewport提交与 board.flush。通知消费者候 C4b；不与 Staging/Recent/Favorites 合并。

**注册申报**：独立 `AGENT_UI_TOOLS` 两项归 `channel_write`（向人发出的呈现动作），原 door_write/read 闭集与语义不变，无第四类、无新域写门。注册35→37、provider definitions41→43、channel_write2→4；公开 MCP 仍14。单用户轮实际下发≤8，同动词同目标1000ms防抖；收据照记，调用/结果成对落史后才发 `ui_command`。输入框/contenteditable/IME/对话框或指针操作时延后，空闲150ms后仅执行最新待办；不 focus 编辑器。纸块/页滚动并短亮，板聚焦仅改会话相机，不调用持久视口写门。收据成功表示 issued，不冒充客户端已显示。

**说明书与指纹**：`current-state/app-operating-manual.md` §一、§五及旧表格/时间线/图表入口已同步，戳 `v2 / 2026-09-20`。显式 `--update` + check 通过，hash `85acce270a2224893e4c011ce4e46e1625f01d5ceba81f7f393cca7aa2a78b15`；manifest及两份文档INDEX已同步。

**证据**：[builder-round2.md](../../audits/2026-09-20-c4a-shell-builder/builder-round2.md) 为总账（逐件源码行号、标签与焦点守则、六张分区前后/窄屏截图、边界）；[toolbar.md](../../audits/2026-09-20-c4a-shell-builder/toolbar.md) 含旧15项保全与双入口；[ui-verbs.md](../../audits/2026-09-20-c4a-shell-builder/ui-verbs.md) 含分类论证、注册、指纹和场景断言表；[verification.md](../../audits/2026-09-20-c4a-shell-builder/verification.md) 含全量/十二砖/收据/contextHint逐件验证。截图为同种子真实组件的合成夹具，不冒充用户端到端验收。原始输出与所有首轮失败留 `.codex-tmp/c4a-shell/`。

| 验证 | 实际结果 |
|---|---|
| 非 git/secrets runtime 门 | **22/23 PASS**；原标准 server build 环境失败；git 检查+secrets 两组件按单留 HQ |
| client 全库 | **227 files / 2318 tests PASS**，标准 client build PASS；之后仅新增板保存等待测试，2文件8/8 PASS，最终 TypeScript noEmit PASS，不虚称2319全库同次通过 |
| 工具条与插入定向 | **8 files / 180 tests PASS**；纸/板/标签保存屏障定向4文件25/25，旧board相机/选择回归11/11 |
| UI 动词与 Agent 家族定向 | server **76/76**；客户端真实SSE **5/5** |
| 全部 scripted 场景 | **13场景122/122**，新增第13场景8/8；harness14/14与agent-eval typecheck PASS |
| server 全文件并集 | **108/108文件已调度**，含 wilderness；去重TAP记录**1079 PASS / 4环境FAIL（1083条）**，不是一次全绿执行；其中MineruWiring一条为模块加载失败，内部用例未执行 |
| v13WildernessExecute | **27/27 PASS**，96s；它与全量补集文件预算均为**600000ms**，零文件排除 |

**尚未解除**：原 shared 构建写 `shared/dist` 的新输出得到 EPERM，原 server build 因缺声明报 TS6305；因此 `v2McpArtifact` 两例失败。另 `v2SourceMineruWiring` 找不到 `python.exe`，`v2SourceRegionCells` 固定旧 venv 的 uv CPython 不能启动，直接 `--version` 也被拒绝访问。没有改权限、跳过测试或伪造解释器。授权临时目录的严格源检查与实际emit通过（436 JS + 436 declarations），仅作补充证据，不能替代标准build PASS。故保留工单 ready 头、不代HQ翻牌；HQ须在具备原构建输出及Python链的环境补跑这些失败项。

**未做/禁区**：Home/悬浮窗/任务队列/托盘/C4b、通知消费者、Tauri撕窗、Relation/判断域、笔记Agent写权、TextFlow schema、坐标契约、新依赖/新真相表、既有动词语义、agent操作指令/权限配置均未动；没有读用户库、调用真实模型、增加设计安全对抗类用例。本轮无新的合同语义冲突。git/secrets检查与主观验收留HQ，不能据本Result直接放行。
