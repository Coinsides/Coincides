> **From**: fable
> **To**: codex
> **Status**: ready(两层制;13.2 单 2=托盘+分蘖 v1,client+server 中大单)
> **日期 (Date)**: 2026-09-07
> **性质**: 施工单(允许多轮内部推进)

# 13.2 · 单 2 · 托盘 + 分蘖 v1

## 〇 · 上游(先读,顺序)

1. 段 plan(含修订一)单 2:`plans/v13-2-wilderness-retirement-plan.md`;
2. 单 0 侦察报告 §二(最小贯通面表+两 UI 选项——本单按下方裁定执行):`analysis/2026-09-07-v13-2-s0-recon.md`;
3. 图三 §一/§四(托盘数据形态与语义):`analysis/2026-09-07-wilderness-migration-mapping.md`。

## 一 · 口径(冻结;含 HQ 对单 0 缺口的三裁)

**裁定甲:托盘 UI=视口侧栏**(documentShell 同级列表,⛔ 浮层⛔ 独立路由);**裁定乙:排序载体=`canvas_placements.order_index INTEGER NULL` 新列**(⛔ 挪用 z_index;⛔ 旁表);**裁定丙:⛔ 本单接书记官**(业务面事件随 13.3 路由中间件成体系上线)。

1. **迁移新单**(编号开工复核,承 054 之后):canvas_placements 加 `order_index INTEGER NULL`;双轨按单 0 第 3 题所记家族现状处理(migration-only 表不自创双轨);
2. **surface 收 `'tray'`**:server validator 放行 tray 值;tray 行仍写 finite 几何(置 0),语义由 surface 判(⛔ 大改 validator 结构);`order_index` 进 normalizePlacementForWrite 白名单;**⛔ 本单停写 workspace/crossing——那是单 5 财产,canvas 模式在单 5 前照常活着**;
3. **client 分流**:`CanvasSurface` 加 'tray';raw normalizer/layout reader/payload/相关 mapper 的 tray 分支;engineModel 分流——tray 行⛔ 入纸 runtime、⛔ 经 blockLayouts 回纸(单 0 §二 警告),入托盘列表模型(行=placement,块/画物/mount 三类标签+空态);
4. **侧栏 UI**:开合组件+列表;开合触发 fit 重算的正确性(gear/居中稳定,存储几何零变化——13.1 单 1 的 K-档位判据延用);`NotePrintLayer` 排除 tray;
5. **动作三件**:
   - 入托盘:纸上选中块下架(surface→tray,order_index 追尾);
   - 出托盘上纸:拖放到纸面落点(复用现有 drop/放置路径赋 frame+坐标,⛔ 新造放置逻辑);
   - 分蘖 v1:托盘选中块→抽成新笔记(复用 notes.ts:255-280 现有创建+挂载事务边界;块身份不变、⛔ 复制内容;原 note 侧 placement 摘除)。三动作均为普通可撤销编辑语义,⛔ 永久删除任何东西;
6. **零变化面**:canvas 模式全行为、迁移存量(单 4 财产)、书记官、Agent 工具面、Preview/打印语义(除排除 tray)。

## 二 · 验证(段纪律)

1. server+client typecheck/build(申报输出摘要);
2. 单测:validator tray 放行与 order_index 白名单;client 分流(tray 行不入纸 runtime);分蘖事务(新 note 挂载成立+原 placement 摘除,同事务);
3. 一条端到端冒烟(可扩展 pageReadingSmoke fixture):纸上块→入托盘(纸上消失/托盘出现)→出托盘上纸(落点正确)→分蘖(新 note 成立)——DOM+数据双断言;侧栏开合下 gear/居中稳定、存储几何零变化。

## 三 · 回执与边界

完工 apply_patch 追加 `## Result`(numstat+验证输出摘要+未做清单);⛔ commit(工作树交 HQ);⛔ 读 .env;⛔ 打印任何 key;⛔ 查询用户数据。现物冲突(如分蘖与 mounts 生命周期策略打架)⇒ 停线举证,⛔ 自行改判。
