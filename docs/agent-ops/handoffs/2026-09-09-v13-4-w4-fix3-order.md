> **状态 (Status)**: ready
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④删 project 500 复测(真实报错栈在案,claude-log §26);修二 F6 的第二来源
> **单号**: 13.4 走查修三 · F10 生命周期闸的归档豁免

# 走查修三 · F10

**根因(Henry 真栈,⛔ 复现猜测)**:`assertCourseLifecyclePolicyCoverage`(courseLifecyclePolicies.ts:156,0831 机械闸)在删 project 时扫到**13.2 扳机日备份归档表**——`canvas_objects_backup_pre13_2`、`canvas_placements_backup_pre13_2`、`content_mounts_backup_pre13_2` 及其 `_rolled_back_4` 变体(共 6 张,均带 course_id)——未登记→coverage mismatch→unhandled→500。这些表只存在于 Henry 真库(扳机日执行器建档),隔离库全新迁移无此表,故修二两轮复现全扑空。

## 零 · HQ 已裁(⛔ 复议)

1. **闸的判定正确,⛔ 废闸**;缺的是"归档"词汇——coverage 断言增**归档豁免类**:`_backup_pre13_2` 家族(含 `_rolled_back_<N>` 变体,N 任意)按**模式**显式登记为归档(带文档注释:13.2 扳机日备份,来源=wilderness/coordinate 执行器);政策语义=**preserve:删除链绝不触碰,豁免覆盖要求**;
2. **⛔ 删/改归档表本身**(那是 Henry 的扳机日存档,动它=动用户历史);⛔ 触碰用户库(复现全走内存合成);
3. **闸不拔牙**:豁免仅限归档模式;真实未登记表仍必须报错——**阳性对照必测**(机械化记忆条:闸自己也要过阳性对照);
4. 断言失败(其余情形)升格为带明码的 AppError(⛔ unhandled 裸 500),错误文案报缺表清单。

## 一 · 验证(单内纪律)

- typecheck + build 全绿;定向回归(courses 删除面+lifecycle)不破;
- 冒烟四条:①内存库合成六张归档表(含 rolled_back_2/rolled_back_4 两种变体)+装数据→删 project **成功**,归档表行**逐字节不变**;②同库另建一张真实未登记表(带 course_id)→删除仍报错且错误为明码 AppError 列出缺表(阳性对照);③修二 F6 的 legacy 409 与全链降级回归不破;④正常/含引用 project 删除回归不破。

## Result 格式

`## Result`:numstat + 冒烟逐条 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。
