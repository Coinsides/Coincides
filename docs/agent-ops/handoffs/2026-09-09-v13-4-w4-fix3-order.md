> **状态 (Status)**: done(HQ 收口:F10 收货;STOP-1 裁陈旧红入 13.6 清册)
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④删 project 500 复测(真实报错栈在案,claude-log §26);修二 F6 的第二来源
> **单号**: 13.4 走查修三 · F10 生命周期闸的归档豁免

# 走查修三 · F10

**根因(Henry 真栈,⛔ 复现猜测)**:`assertCourseLifecyclePolicyCoverage`(courseLifecyclePolicies.ts:156,0831 机械闸)在删 project 时扫到**13.2 扳机日备份归档表**——`canvas_objects_backup_pre13_2`、`canvas_placements_backup_pre13_2`、`content_mounts_backup_pre13_2` 及其 `_rolled_back_4` 变体(共 6 张,均带 course_id)——未登记→coverage mismatch→unhandled→500。这些表只存在于 Henry 真库(扳机日执行器建档),隔离库全新迁移无此表,故修二两轮复现全扑空。

## 零 · HQ 已裁(⛔ 复议)

1. **闸的判定正确,⛔ 废闸**;缺的是"归档"词汇——coverage 断言增**归档豁免类**:`_backup_pre13_2` 家族(含 `_rolled_back_<N>` 变体,N 任意)按**模式**显式登记为归档(带文档注释:13.2 扳机日备份,来源=wilderness/coordinate 执行器);政策语义=**preserve:删除链绝不触碰,豁免覆盖要求**;
2. **⛔ 删/改归档表本身**(那是 Henry 的扳机日存档,动它=动用户历史);⛔ 触碰用户库(复现全走内存合成);
3. **闸不拔牙**:豁免仅限归档模式;真实未登记表仍必须报错——**阳性对照必测**(机械化记忆条:闸自己也要过阳性对照);
4. 断言失败(其余情形)升格为带明码的 AppError(⛔ unhandled 裸 500),错误文案报缺表清单。

## 一 · 验证(单内纪律)

- typecheck + build 全绿;定向回归(courses 删除面+lifecycle)不破;
- 冒烟四条:①内存库合成六张归档表(含 rolled_back_2/rolled_back_4 两种变体)+装数据→删 project **成功**,归档表行**逐字节不变**;②同库另建一张真实未登记表(带 course_id)→删除仍报错且错误为明码 AppError 列出缺表(阳性对照);③修二 F6 的 legacy 409 与全链降级回归不破;④正常/含引用 project 删除回归不破。

## Result 格式

`## Result`:numstat + 冒烟逐条 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。

## Result

2026-09-09 · Codex builder：F10 实现已落工作树，四条冒烟通过；完整定向回归有 25 项失败，按 STOP-1 停线，**不宣称本单全绿/放行，不翻 done**。

### 实现与 numstat

- `courseLifecyclePolicies.ts`：独立登记三基表的归档模式 `^(canvas_objects|canvas_placements|content_mounts)_backup_pre13_2(?:_rolled_back_[0-9]+)?$`，不把代次写死为 2/4；政策为 `preserve`，注释说明 13.2 扳机日 wilderness/coordinate 执行器来源与删除链禁止触碰的语义。仅豁免归档的 missing coverage，不进入活动表登记或删除/搬迁遍历。
- missing/stale/duplicate/onDeleteMismatch 四闸保留；失败改为 `AppError(409)`，`details.code=course_lifecycle_policy_coverage_mismatch`，文案和 details 均列缺表；沿用实际 errorHandler 输出明码错误信封。
- `v13ProjectDeletionReferences.test.ts`：新增一条非安全类 F10 功能回归，在同一内存库依次完成归档保留与未登记表阳性对照；原 F6 两条测试保持。

| 文件 | 新增 | 删除 |
|---|---:|---:|
| `server/src/services/courseLifecyclePolicies.ts` | 20 | 2 |
| `server/src/__tests__/v13ProjectDeletionReferences.test.ts` | 65 | 1 |
| `docs/agent-ops/handoffs/2026-09-09-v13-4-w4-fix3-order.md` | 43 | 0 |

以上为本单 `git diff --numstat`（合计 +128/-3）；本工单仅追加本节，验证运行器与原始日志存于 `.codex-tmp/f10/`，未覆盖修二证据。追加回执后 `docs:check` 已复跑通过，原工作区 diff 检查日志为 `diff-check-workspace.log`。

### 四条冒烟（同一次完整 server 回归内取证）

| # | 结果 | 证据 |
|---|---|---|
| ① 六归档 + 数据 → 删 project 成功，行逐字节不变 | PASS | 三张原名 + `canvas_objects…_rolled_back_2`、`canvas_placements…_rolled_back_4`、`content_mounts…_rolled_back_4`；内存 CTAS 复制真实表形状，装入合成文本/BLOB 行；删除前后读取全部列（raw + safeIntegers），比较 v8 序列化字节及 schema，project/note 确实删除。 |
| ② 同库真实未登记表阳性对照 | PASS | 同库创建 `unregistered_project_work(course_id)` 后再删 project，仍抛 409 AppError，明码与 `missing=[unregistered_project_work.course_id]` 精确断言；实际 errorHandler 的响应信封亦核对。project、业务行及六归档保持。 |
| ③ F6 legacy 409 与全链降级 | PASS | 既有 legacy soul/board 用例两种 action 均 409、无部分写入；既有 range/Item/board/soul/layer 用例保真相、引用降级为 lost/missing、保 excerpt。 |
| ④ 正常/含引用 project 删除 | PASS（所列生命周期路径） | SourceLifecycleClosure 的纯投影删除、跨 note 引用保护、整图搬 Home，以及 RelationLifecycleClosure 的跨 project 组织可读均通过；旧 canvas 图片删除用例另有阻塞，见 STOP-1，不能据此宣称整个 courses 删除面全绿。 |

原始证据：`.codex-tmp/f10/server-functional.log`（F10/F6 为 TAP 43–45，Item coverage 为 104，Project/Source 生命周期为 125、128–131）。

### 验证与未做

- `node .codex-tmp/f10/validate.mjs --server`：完整文件执行，无名称过滤，**140 tests：115 PASS / 25 FAIL / 0 skipped**。涵盖全部 v13Board 文件及 Item/RecordedAction/ProjectDeletionReferences/Purposes/NotesLifecycle/SourceLifecycleClosure/RelationLifecycleClosure/CanvasPersistenceCutover/ItemRelationFloor。
- `node .codex-tmp/f10/validate.mjs`：21 子项 PASS，包括 shared/client typecheck、client/server build（server build 含 tsc）、完整 client **83 files / 726 tests**、tool-face 各门、runtime 各机械门、model/performance、docs。原始结果 `.codex-tmp/f10/validation-gate.json`；净化子进程的 `git diff --check` 返回 129（Not a git repository），原记录保留；随后在原工作区独立执行 `git diff --check` 返回 0。
- 验证复用修二已核验运行器：净化子进程环境、内存默认 DB、隔离资产目录、既有禁读 dotenv preload；Vite/Vitest 使用 `envFile:false`。F10 复现只用 `createV13BoardsFixture()` 的内存库；既有套件自行生成内存/临时合成库。
- 未原样执行总 `verify:v2-bn8-runtime`：其尾项 `check:changed-file-secrets` 与本单“凭据扫描留 HQ”冲突；按修二方式实跑允许子门，**不修改总门，不声明总门 PASS**。凭据扫描未做，留 HQ。
- 未 commit/push/PR；未读 `.env`、未输出 key、未接触用户数据库、未运行真实迁移/回滚执行器、未删改真实归档。未设计/新增安全类测试，既有套件整跑无过滤；未开展用户真库验收。

### 停线

**STOP-1：定向整套不全绿，保留失败不改判。** 25 项失败均在既有 `v2CanvasPersistenceCutover.test.ts`，包含三条 Course 图片资产删除用例；失败发生于 fixture 的 canvas workspace 写入或相应 validator，报 `canvas_workspace_retired`（或对该错误的断言不符），不是 F10 coverage mismatch。现行闸位于 `server/src/services/canvasWritePolicy.ts:8`。原始堆栈与用例名保留在 `server-functional.log`，未过滤、删测试、修改退役闸或扩大本单施工范围；未对改前版本另行整跑，不将这些失败标记为已证明的基线 PASS。交 HQ 处理此阻塞及总门凭据扫描后再判。

独立只读复核：F10 正则边界、preserve 隔离、四类 coverage 检查、AppError 传播与内存字节比对未发现实质问题；该复核不替代上述实跑结果或 HQ 放行。

## HQ 收口(2026-09-09):F10 收货;STOP-1 裁为陈旧红入 13.6 清册

1. **F10 收货**:四冒烟绿、闸保牙(阳性对照)、明码 409——状态翻 done,HQ 代账;
2. **STOP-1 裁定:陈旧套件红,⛔ 挡本单**——25 失败全在 `v2CanvasPersistenceCutover.test.ts`(13.2 时代),失败机制=夹具向已退役 canvas 面写入撞 `canvas_workspace_retired` 闸(13.2 单 5 立法),与 F10 diff(courseLifecyclePolicies+测试)零交集;判为**退役立法当日即陈旧、今日首次被更宽定向网捞出**。依据=机制不相交的确定性推理;未跑改前基线,若后续证伪翻案;
3. **落位**:该文件登记入 13.6 canvas 死代码清册(删或现代化随册裁);**附记**:其中三条"Course 图片资产删除"用例覆盖的行为(project 删除时资产清理)仍是活语义(canvasAssets 引用计数面),13.6 裁决时需给它**page/board 世界的现代替身测试**,⛔ 只删不补。
