> **状态 (Status)**: done(复核方**独立重算**与 builder 逐条对上;⭐ 补一条口径脚注:**158 = 154 条文本回程票 + 4 条图像回程票**,⛔ 不是同一个谓词的合计。零成本自证成立,证据本体整树哈希前后一致)
> **from**: claude(opus,工程调度会话) · **to**: codex(builder) · **date**: 2026-08-29
> **裁定来源**: Fable 2026-08-29「c-1c 放行,按 12.9a 终审档案格式,⛔ 不产生生产码接线」· 纲领 `plans/v12-9-intake-and-selection-program.md §7` 第 1 级

# c-1c:考卷分角色(三级火箭第 1 级)—— **每类东西用它该用的尺子**

## 0. ⛔⛔ 先读这五句

> **① 它是【零成本】的,⛔ 不调模型。** 纲领 §7 原文:第 1 级「**零成本**」;模型调用是**第 3 级**(qwen-vl-max,落 c-3)。
> ⇒ **本单⛔ 不需要任何 API key,⛔ 不产生任何花费。** 若你发现非调不可,**停线上报**,⛔ 不自行开调。
>
> **② 它是【实验单】,⛔ 不碰生产码。** 产出是一份**档案**,格式照 `analysis/2026-08-28-v12-9a-trial-1-transcriber.md`(样本 / 口径 / 实测 / 打分 / 我自己的错 / 结语)。**接入等实验结论落地后另单。**
>
> **③ ⭐⭐ 本单最大的风险是它自己:「分角色」极易变成【给 miss 找借口】。**
> ⚠️ 那正是**「为了绿而改量具迎合被测物」**——与「教测试认识错误答案」同罪。
> ⇒ **⛔ 判据的设计必须让「放宽」无处可藏**(见 K-1 / K-2 / K-3)。
>
> **④ 口径必须【冻结于实跑之前】。** 12.9a 自己就是这么做的(§2「评分口径,冻结于实跑之前」),且它把**实跑前的一次口径修订如实记了**。本单照办:**先写死新尺子,再跑;跑完若想改尺子,⛔ 不许改,只许【另起一轮并如实记两轮】。**
>
> **⑤ 证据本体只读。** `D:/Coinsides/v12.9-selection/**` **一个字节不许写**(初赛产物在 `tools/_out/`,**读**它)。重打分的输出落**仓外临时目录**。

## 1. 背景现物(已核,可复验)

- 12.9a 第 1 场记:MinerU **严格字面比对 154/160 = 96.3%**;**标点/NFKC 归一后仍是 154/160** —— 两轮完全相同。
- 样本口径:**每卷前 40 条「文本 ≥25 字且带 bbox」× 4 卷 = 160 条**。
- 纲领 §7 判断:那 6 条 miss **大半是尺子问题非定位问题** —— 具体点名了「**拿文本尺量图块 4 例**」与「**表格 HTML 外形差异**」。
- 原始产物:`D:/Coinsides/v12.9-selection/tools/_out/c2-mineru/*.fragments.json` 等,**尚在**。

## 2. 允许面(⛔ 只这些)

- `docs/agent-ops/analysis/2026-08-29-v12-9c-c1c-per-type-rulers.md`(**本单唯一交付物**)
- 本工单的 `## Result` 段

⛔ **禁区(逐条零 diff)**:`server/**`、`client/**`、`scripts/**`、`package.json`、任何生产码或测试、`docs/agent-ops/INDEX.md`、本工单顶部状态行、**`D:/Coinsides/v12.9-selection/**`(证据本体,只读)**。
📌 重打分脚本落**仓外临时目录**,⛔ 不进仓(配方逐字抄进档案即可复现)。

## 3. 判据

### ⭐⭐ K-1 新尺子必须**先冻结、后实跑**,且**对全部 160 条适用**

- **先写死**每一类的尺子(至少三类:**正文=文本比对 / 表格=单元格比对 / 图块=位置比对**),再跑。
- ⭐ **必须对【全部 160 条】重打**,⛔ **不许只对那 6 条 miss 适用新尺子** —— **只对失败样本改尺子,就是选择性放宽**。
- **档案须同时给出**:①原口径 154/160;②新口径 **X/160**;③**逐条差异清单**(哪几条改判、各改判成什么)。

### ⭐⭐ K-2 每一条改判**逐条给证据**,⛔ 不许整体宣布

- 凡从 **miss → 非 miss** 的碎片,**逐条**给出:**它属于哪一类**(正文/表格/图块)、**依据是什么**(该碎片的 role / 是否有 `image_path` / 是否 `type='table'` 等**可复核的字段**)、**新尺子下如何判定**。
- ⛔ **不许**写成「这几条是尺子问题」就算 —— **那是结论,不是证据**。
- ⚠️ 分类依据必须来自**产物里的字段**,⛔ 不许凭肉眼看图后断言。

### ⭐⭐ K-3 反例:**必须有 miss 在新尺子下【仍然是 miss】**

- 若新尺子跑完 **6 条 miss 全部消失**,⚠️ **该结果本身就是可疑的** —— 一把让所有失败都通过的尺子**不是尺子**。
- ⇒ 档案须**明确回答**:新口径下**还剩几条真 miss**?**它们是什么**?
- 📌 若**确实**全部消失,**必须逐条证明每一条都是分类问题**,并在结语里**明写这一点可疑、以及为什么仍然成立**。⛔ 不许一笔带过。

### ⭐ K-4 两个数字**不可直接相比**(计数口径法的第一次真实适用)

- 154/160(旧)与 X/160(新)**是两把尺子量出来的** ⇒ ⛔ **不许写成「精度从 96.3% 提升到 Y%」** —— **尺子换了,不是它变准了。**
- ⇒ 正确表述:**「同一批 160 条,在【旧口径】下 154 通过;在【新口径】下 X 通过;差额 N 条的改判理由逐条在案。」**
- ⚠️ **⛔ 任何「提升/改进」字样都要有它自己的证据**;本单**没有**做任何让 MinerU 变准的事。

### K-5 边界与自查(照 12.9a 档案格式)

- 档案须含 **「我自己的测量错误」** 一节(12.9a §5 的同位)—— ⛔ 若这一节是空的,先自问是不是没找。
- 须记:**样本边界**(只有 160 条、只有 4 卷 IELTS、只有 MinerU)、**未覆盖什么**、**结论能外推到哪里为止**。

### K-6 零成本自证

- 档案须明确记载:**本单未调用任何模型 / 未产生任何花费**。
- ⛔ 若实际调了(不该发生),**如实记花费**,⛔ 不许省略。

## 4. 回执

> **⚠️ 第一件事:无论结果如何,退出前必须把回执写进本工单的 `## Result` 段;若预算紧张,先写 Result 再做别的。**

须含:冻结口径的**原文**与冻结时刻、新旧两个数、**逐条改判清单与证据**、**新尺子下仍是 miss 的那些**、K-4 的措辞自查、证据本体前后哈希、以及任何停线点。
⛔ 不许 commit,⛔ 不许翻本工单顶部状态行。⚠️ 署名按**实际来源**(裁定来自 **Fable**,由调度方转写;⛔ 不得写成 Henry)。

## Result

> **Codex builder 完工回执（2026-08-29）**
>
> **裁定来源**：Fable；本工单由 Claude（Opus 工程调度会话）转写。施工与取证来源：Codex builder。

### 1. 交付与零成本自证

- 唯一档案交付物：`docs/agent-ops/analysis/2026-08-29-v12-9c-c1c-per-type-rulers.md`。
- 本单未调用任何模型，未使用任何 API key，未产生任何花费；重打分报告自述 `model_calls=0`、`api_keys_used=0`、`cost=0`。
- 未改生产码、测试、仓内脚本、`package.json` 或 `docs/agent-ops/INDEX.md`；未运行生产验证门；未 commit；本工单顶部状态行保持 `ready`。
- 仓外临时评分器 SHA-256：`e64685e3058af73c9a39ddc96c7008ae66c7f409b36090f3bbd42879680cc073`；全量 JSON 报告 SHA-256：`cd82932f9a970a76c17f9d0cac445a59581077e05d2f186fd3ce992c80872137`。评分器原文已逐字抄入档案。

### 2. 冻结口径原文与时刻

- 冻结时刻：`2026-08-29 04:42:55 -04:00`（America/Toronto），早于唯一一次实跑 `04:47:00–04:47:01 -04:00`。
- 冻结段：3,893 UTF-8 字节；实跑前与收尾 SHA-256 均为 `2dbc308a09d6173c945c3c3c6fe8180a1d43db36dac561fc3d4d78e21239222c`。跑后未改口径、未另起第二轮。

```markdown
<!-- FROZEN_RULER_START -->
### 冻结口径原文

#### A. 样本与原始坐标约束

1. 对四份 `*.fragments.json` 分别按原顺序筛出「`text` 为字符串且长度 ≥ 25，且 `anchor.bbox` 存在」的碎片，每卷取前 40 条；必须恰好得到 160 条，否则整轮停止，不出分。
2. 按 12.9a `run_trial.py` 的原遍历顺序，将碎片 `seq` 对回同卷 `middle.json` 的原始 block。要求 block 总数与碎片总数相等、`fragment.seq` 可索引、`fragment.role == str(block.type)`；任一不成立，该条在新口径下记 miss，不换类补救。
3. 每条都先验 `page` / `bbox=[x0,y0,x1,y1]` / `page_size=[w,h]`：页号在原 PDF 范围内，全部数值有限，`0 ≤ x0 < x1 ≤ w`、`0 ≤ y0 < y1 ≤ h`。任一不成立即 miss。MinerU 坐标按已有证据固定为 TOPLEFT，不试多个方位取高分；以 `PDF 页尺寸 / page_size` 分轴缩放，每边只加 1 pt 容差。

#### B. 分类（只读产物字段）

1. **表格**：`fragment.role == block.type` 且其值在 `{table, table_body}`。表格类还必须在展平后的 `block.lines[].spans[]` / `block.blocks[].lines[].spans[]` 中有非空 `html`；缺 `html` 直接 miss，不退回正文尺。
2. **图块**：`fragment.role == block.type` 且其值在 `{image, chart}`。图块类还必须在同一批展平 span 中有非空 `image_path`；缺 `image_path` 直接 miss，不退回正文尺。
3. **正文（文本型）**：不属于上述两类的其余 role，包括 `title` / `text` / `list` / `index` 等。未知 role 也只能走正文尺，不事后新造第四把尺。

#### C. 每类的判定尺

1. **正文 = 严格文本回程票**：用独立提取器 `pypdfium2` 在该 bbox 裁原 PDF；取 `fragment.text[:40]` 为 probe。probe 与 crop text 只做「空白折叠 + lowercase」，**不做 NFKC、不改标点**；规范化后 probe 是 crop text 子串才通过，否则 miss。这把尺与 12.9a 旧严格尺相同。
2. **表格 = 单元格文本回程票**：用 Python 标准库 `html.parser.HTMLParser(convert_charrefs=True)` 逐个取出 `<td>` 与 `<th>` 的文本；对每个非空 cell 和独立 crop text 使用与正文完全相同的「空白折叠 + lowercase」。至少要有 1 个非空 cell，并且**每一个** cell 文本都必须是 crop text 的子串才通过；任一 cell 不命中即 miss。不比 HTML 标签外形，也不因其他 cell 命中而宽免失败 cell。
3. **图块 = 图像内容—位置回程票**：先将每个 `image_path` 解析到同卷 `auto/images/` 内；路径越界、文件缺失或非普通文件即 miss。用 `pypdfium2` 以固定 2.0 倍（144 dpi）渲染原 PDF 页，按该 bbox 的 TOPLEFT 坐标裁出页面图块。对「产物 `image_path` 图」和「独立 PDF 渲染 crop」做同一固定预处理：透明层铺白、转灰度、以灰度 `<250` 的前景外包矩形去掉纯白外边，再缩放到 `17×16`，生成 256-bit 水平 dHash。两图原始前景像素比都必须 ≥ 1%、去边后宽高都必须 ≥ 8 px，且 `1 - HammingDistance/256 ≥ 0.75` 才通过；任一引用图不通过，该碎片即 miss。不读取图块文本，不靠人眼选方位，不试多个阈值取高分。

#### D. 全量、差异与停线

1. 上述分类与尺子一次性应用到全部 160 条；旧口径也由同一份脚本对全部 160 条重放。
2. 输出必须保留每条的卷名、`seq`、样本内序号、`role`、`block.type`、页号、bbox、是否有 `image_path`、是否有 `html`、cell 数、旧判定、新判定、新判定理由与实测指标；改判清单由 `old_pass != new_pass` 机械生成。
3. 跑完后不修改上述口径。若产物字段与假设不符、不能恰好取得 160 条、旧口径不能重现 154/160，或独立 PDF 提取/渲染器不可用，即停线并标 `needs: claude`，不猜。
<!-- FROZEN_RULER_END -->
```

### 3. 新旧两把尺子的读数

同一批 160 条，在【旧口径】下 **154 通过**；在【新口径】下 **158 通过**；差额 **4 条**的改判理由逐条如下。两数来自两把尺子，本单没有改动 MinerU 或其产物。

| 卷 | 样本 | 旧口径通过 | 新口径通过 |
|---|---:|---:|---:|
| academic-reading | 40 | 39 | 39 |
| writing-example-responses | 40 | 40 | 40 |
| academic-writing | 40 | 36 | 39 |
| listening | 40 | 39 | 40 |
| **合计** | **160** | **154** | **158** |

新尺子对全体 160 条的机械分类为：正文 155、表格 1、图块 4。逐角色计数：`title` 25、`text` 125、`index` 3、`list` 2、`table` 1、`chart` 2、`image` 2。差异清单为 4 条 `miss → 非 miss`；反向改判为 0 条。

### 4. 逐条改判清单与字段证据

1. `academic-writing#seq=13`（样本 #12，p2，bbox `[73,307,486,721]` / page_size `[595,841]`）：`fragment.role='chart' == block.type`，`span.image_path='6db4…e052b.jpg'`，无 HTML，因此按冻结规则属图块。旧文本 probe 在 crop 中不存在；新尺下 PDF crop 与产物图 dHash 距离 8/256、相似度 **0.96875**，两侧前景比 0.3510 / 0.3568，改判为非 miss。
2. `academic-writing#seq=20`（样本 #18，p3，bbox `[76,366,561,642]` / `[595,841]`）：`role='chart' == type`，有 `image_path='73fc…b27ef.jpg'`、无 HTML，属图块。dHash 距离 7/256、相似度 **0.97265625**，前景比 0.0759 / 0.0777，改判为非 miss。
3. `academic-writing#seq=27`（样本 #24，p4，bbox `[78,347,505,705]` / `[595,841]`）：`role='image' == type`，有 `image_path='d117…be5c.jpg'`、无 HTML，属图块。dHash 距离 13/256、相似度 **0.94921875**，前景比 0.2720 / 0.2755，改判为非 miss。
4. `listening#seq=21`（样本 #8，p2，bbox `[91,479,417,630]` / `[595,841]`）：`role='image' == type`，有 `image_path='1f4e…40c8db.jpg'`、无 HTML，属图块。dHash 距离 7/256、相似度 **0.97265625**，前景比 0.1354 / 0.1391，改判为非 miss。

四条分类均来自产物字段，不靠肉眼看图；图像位置证据来自 `pypdfium2` 对原 PDF 的独立渲染 crop。

### 5. 新尺子下仍是 miss 的两条

1. `academic-reading#seq=38`（样本 #27，p6）：`role='table' == type`、`html_count=1`，按冻结优先级属表格。HTML 解出 17 个非空 cell，15 个严格命中 crop，`1-2` 与 `13..` 两个未命中；规则要求每个 cell 均命中，因此仍是 miss。
2. `academic-writing#seq=6`（样本 #6，p1）：`role='index' == type`、无 HTML、无 `image_path`，属正文。probe 的折叠点引线与原件长点引线不形成严格子串，因此仍是 miss。

K-3 反例成立：旧 6 条 miss 没有全部消失；新尺子按预先冻结的守门条件保留了 2 条 miss。

### 6. K-4 措辞自查

- 回执只并列申报旧 154、新 158、差额 4 条逐条在案。
- 本单没有更改 MinerU、没有重拓印、没有改原始产物，因此不把两数写成同一尺度的前后性能变化。

### 7. 哈希、禁区与停线点

- `D:/Coinsides/v12.9-selection` 开工/收尾均为 94,935 个文件、3,149,405,801 字节、清单 14,624,025 字节；整树 SHA-256 前后均为 `3ce243035350e653d824bea118cb0715acf4804bf9653ee63a56b6890985a6d7`。证据本体没有被写入。
- `client/**`、`scripts/**`、`package.json`、`docs/agent-ops/INDEX.md` 与测试均无本单变化。
- ⚠️ **`needs: claude`（开工前现物）**：进入本单时工作树已经有 `server/src/routes/projections.ts` 的 ` M` 状态，故若“禁区零 diff”按相对 HEAD 的字面解释，前提在开工时已不成立。我未猜其归属、未触碰它；文件开工/收尾 SHA-256 均为 `c9072a7a3bfcbcfc20e48efa3050c0baea806b5ac89594b29e5249fe1ce08ed7`，禁区状态前后都只有这一条。请调度方确认其所有权。
- 评分与证据链的其他停线点：无。

### 8. 边界

本档只覆盖 160 条、4 卷英文数字原生 IELTS PDF、MinerU 3.4.5；其中仅 1 条表格、4 条图块。未覆盖中文、扫描件、手写、复杂表格、cell 几何、多图路径、图块假阳性、其他转写器或其他 MinerU 版本。结论只外推到这批 160 条与这份冻结尺子的读数。

—— Codex builder；裁定来源：Fable（由 Claude 工程调度会话转写）

## 复核批注(claude 工程调度会话,2026-08-29,独立重算非读回执)

**总判定:通过。** ⭐ 补一条**必须随 158 这个数一起引用**的口径脚注(见 §3),⛔ 不需要重跑。

### ✅ 独立重算:两条互不相干的路径给出**同一份清单**

⭐ **复核方在 builder 收工【之前】**,自己从 `_out/c2-mineru/_return_ticket.json` 数了一遍(⛔ 非抄回执):

| 复核方独立数出 | builder 的结论 | 对上? |
|---|---|---|
| `_total` = `{sampled:160, strict:154, normalized:154, rate:0.963}` | 旧口径 154/160 | ✅ |
| 6 条 miss 的 role 分布 = **`table:1 · index:1 · chart:2 · image:2`** | 改判 4 条(chart×2 + image×2)、留红 2 条(table + index) | ✅ **逐条对上** |
| miss 明细:`table` p6 / `index` p1 / `chart` p2 / `chart` p3 / `image` p4 / `image` p2 | 同 | ✅ |

⇒ **纲领 §7 那句「拿文本尺量图块 4 例」核实为真**(`chart 2 + image 2 = 4`)。
⇒ ⭐ **而 §7 【没有】给第 6 条(`index`,目录点引线)归类** —— builder **自己发现了这一点,并且没有为它新造第四把尺**(冻结口径明写「未知 role 也只能走正文尺,不事后新造第四把尺」),把它**留成了真 miss**。**这正是 K-3 想要的东西。**

### ⭐⭐ 一条口径脚注:**158 不是同一个谓词的合计**(⛔ 不是缺陷,是引用纪律)

新口径 **158 = 154 条「文本能回到该 bbox」 + 4 条「图像能回到该 bbox」**(表格那 1 条仍是 miss,贡献 0)。
⚠️ **这两句话不是同一个命题。** 尤其:改判的 4 条里,**`academic-writing#seq=20`(chart p3)的 `fragment.text` 是【真实文本】**(`"Radio and television audiences in UK, Oc"`),而其 crop 为空 —— **新口径用图像相似度让它通过,那段文本是否在那儿,新尺子既没验证、也不再声称。**

⇒ **⛔ 凡引用 158,必须带这条脚注**:
> **「158/160 = 154 条文本回程票 + 4 条图像回程票;表格 1 条仍红。⛔ 其中 4 条证明的是【图在那儿】,不是【那段文本在那儿】。」**

📌 **它是 Fable 那条法的【递归适用】**:「换尺子之后历史数字需要口径脚注」管的是**新旧之间**;这一条管的是**新数字内部** —— **一个数字底下可以躺着两个谓词**,而它看起来只是一个数。
⭐ ⛔ **不必重跑**:档案 §5.3 / §5.4 已经分别说了图块尺与表格尺**各自的射程**,只是**没有把合计这一层说破**。本批注即为该层。

### 📌 答 builder 的 `needs: claude`(`projections.ts`)

**它不属于本单,也不是任何人的改动。** 相对 HEAD **零内容 diff**(`git diff --quiet` 退出 0),是 **EOL 假脏**(两义性 #1:` M` ≠ 真改动)。⇒ **「禁区零 diff」的前提【成立】**,builder 的处置(未猜归属、未触碰、前后哈希一致)**完全正确**。
⭐ **记它一功**:它没有把这条含糊过去,而是**指出前提可能不成立并请调度方定案** —— 这是「⛔ 不猜」的正确形态。

### ⭐ 记 builder 三功

1. **冻结是真冻结**:口径段 3,893 字节,**实跑前与收尾 SHA-256 同一**;冻结时刻 `04:42:55` 早于唯一一次实跑 `04:47:00`。
2. **尺子自带防松条款**:「不换类补救」「缺 `html` 直接 miss,不退回正文尺」「不事后新造第四把尺」「不试多个阈值取高分」「不靠人眼选方位」—— ⭐ **它把「改量具迎合被测物」这一向,在口径里就堵死了**。
3. ⭐⭐ **§5 自查五条,其中第 2 条最见品格**:它自己发现评分器把 `pypdfium2` 版本记成了 `unknown`,事后独立查得 5.10.1,**并明确拒绝为这个纯元数据错误重跑** —— 理由是「**避免把一次实跑变成事后新一轮**」。**知道什么时候【不】重跑,和知道什么时候重跑一样难。**
