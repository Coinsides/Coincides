> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: done | re: v2bn12-03-4-fix | date: 2026-08-20

# V2.BN.12 修正单 03.4:恢复收据 geometry↔role 一致性(单轴)

## 背景

03.3 复核 FAIL(0B/0H/**1M**/0L),全文见 03.3 ## Review。已确认:03.2 两枚原始 witness 复核官亲手重放转绿、R1 写失败保收据、R3 toast 次数、保护正控 47/47、全门、D4、Page producer 零语义变化。唯一残余:validator 未把收据几何交给 classifier 对账。

## 修正要求(单项)

**R1[MED-1] framed receipt 的 geometry 一致性校验**:按复核官核形原文执行——
- framed receipt 直接调用**共享 `classifyCanvasSurfaceAuthority`**,以收据的 coordinate space、`{x,width}`、`pageBoundary` 与 explicit surface 求唯一 decision;
- 要求 decision 的 `(surface, boundaryRole, frameId)` 与收据**申报 tuple exact-equal**,不等即 fail closed 留队(reason 沿用 owner_authority_unknown 或更精确的新 reason,申报选择理由);
- **禁止手写第二套 classifier**——必须复用工单 03 D2 的单点函数,这本身就是 D2「共用同一 classifier」教义对恢复路径的兑现。
- 常驻回归:复核官的四例(双方向 × raw v1/v2——自称 inside 实为 outside;自称 crossing 实为 inside)全部落户,断 blocked、原 bytes 保留、finalizer/POST/PUT=0;合法几何一致的 inside/crossing/outside 正控保持绿。

## 保护性约束

03.3 已验绿面全部不许回归(quota 三重 witness/null-frame 矩阵/toast 次数/正控 47 项);不碰 classifier 本体与 03 系列其他已定面;禁止 migration --apply;schema/RED 不动。

## 验证与回执

全套门重跑(unit 104+新增/server 262/verify 链/双 tsc/migration 5 项)。## Result 追加进本文件。不自评 PASS,不 commit。

## Result

> builder: codex | date: 2026-08-20 | disposition: submitted-for-review

### 1. 施工

1. **framed recovery receipt 复用 sole classifier 对账**
   - `hasValidCanonicalOwner` 保留既有 outer/authority coordinate、surface/role、frame 与 boundary 结构守卫；framed tuple 通过结构门后，直接调用共享 `classifyCanvasSurfaceAuthority`。
   - classifier 输入全部来自 receipt：outer `coordinate_space`、layout `{x,width}`、`surface_authority.pageBoundary` 与 explicit `surface`；未复制 box/right/boundary 几何判断。
   - classifier decision 的 `(surface,boundaryRole,frameId)` 分别与 receipt 申报 `(surface,boundary_role,frame_id)` 做 `===` exact-equal；任一不等即 validator 返回 false，沿现有 `owner_authority_unknown` fail closed 留队。选择沿用该 reason，是因为失败事实仍是 owner authority 无法由申报 tuple 与权威几何共同证明，无需扩 reason 轴。
2. **合法几何正控校正**
   - 既有 canonical inside fixture 原为 `x=128,width=540,boundary=[96,636]`，几何实际 crossing；只将测试 boundary 右界改为 `832`，使其成为真正 inside 正控。
   - canonical crossing 与 outside 正控保持原语义；共享 classifier 本体未修改。

### 2. 常驻 witness 与保护面

- 复核官四例按原几何全部落户：
  - declared `formal_page/inside/frame-a`，但 `x=300,width=10,boundary=[100,200]`，权威 decision 为 outside；
  - declared `canvas_workspace/crossing/frame-a`，但 `x=120,width=20,boundary=[100,200]`，权威 decision 为 inside；
  - 两个方向各覆盖 raw v1 与 raw v2。
- persistence 四例逐例断言：`replayable=[]`、blocked raw/reason/storageKey、原 storage bytes exact 保留、另一 storage key 不生成、finalizer/onRecovered 调用均为 0。
- adapter 四例逐例等待 `pageFrameCollection` hydration 引发 recovery effect rerun，再断 blocked toast 恰好一次、POST/PUT=0、原 bytes exact 保留。
- RED-first 收据：只落测试、尚未接生产 classifier 时，定向 2 files 为 **8 failed / 30 passed**；接线并完成 hydration 次态加强后为 **38/38**。
- 03.3 保护面未被替换：quota throw / silent no-op read-back / same-key replacement、raw v1/v2 null-frame matrix、blocked same-key/corrupt bytes、canonical v1→v2、合法 inside/crossing/outside、toast guard 等均继续进入 112 项 unit 总门。

### 3. 全门回执

- `npm.cmd run test:unit`：15 files，**112/112**（03.3 的 104 + 新增 8 个 test rows）。
- 隔离 `CANVAS_ASSET_DIR=<repo>/.codex-tmp/canvas-assets-v2bn12-03-4-builder` 的 `npm.cmd --prefix server run test:v2`：**262/262**。
- `npm.cmd --prefix server run test:v2-bn12-migration`：**5/5**；仅运行测试 fixture / 临时数据库，未执行 migration `--apply`。
- client 与 server `npm.cmd exec tsc -- --noEmit`：最终复跑均 exit 0。
- `npm.cmd run verify:v2-bn8-runtime`：exit 0；含 unit 112、runtime boundary 159 checks、relation freshness、model contract 60 groups、client/server build、performance 5 scenarios、`git diff --check` 与 changed-file secret scan 58 files。
- 本回执追加后再次执行 `git diff --check` 与 changed-file secret scan：exit 0，59 files。
- 非阻塞输出仅为既有 React Router future-flag、Vite dynamic-import/chunk-size 与 Git 换行提示；不影响上述门禁结果。

### 4. 范围与申报

- 03.4 生产改动仅 `draftBlockPersistence.ts` 的 recovery validator classifier 接线；测试改动仅对应 persistence 与 adapter 两文件。未碰 placement/classifier 的既定语义或其他 03 系列生产面。
- `shared/types/canvasSurfaceAuthority.ts` 本体未改；`server/src/db/schema.sql`、`server/src/db/migrate.ts`、`server/src/db/migrations/**` 本轮未改。
- 未执行 migration `--apply`；未 commit。本回执只陈述施工与门禁证据，提交复核方判定。

## Review

> reviewer: Codex（independent reviewer） | date: 2026-08-20 | disposition: PASS | reviewed worktree HEAD: `c7fc58ed488565fcba6fa07a7564fc345aef2803` | preserved clean 03.2 reviewer candidate: `.codex-tmp/review-03-2-mut-adapter`

### 1. 判定与 Findings

本轮 **PASS（0B/0H/0M/1L）**。03.3 Review §1 的双方向 × raw v1/v2 四例原始 `esbuild.buildSync({ write:false })` witness 已按原输入重放：四例全部 `replayable=[]`、以 `owner_authority_unknown` blocked 留队，原 bytes 不变，finalizer/create/placement/latest 均 0；真实 adapter 常驻路径另证 POST/PUT=0、toast 精确一次。共享 classifier 接线、fixture 纠错、03.3 全保护面、全门、D4 readonly 与五轮累计合取均成立。

#### LOW-1 — 原 `[96,636]` 右界 crossing fixture 未作为精确常驻行保留 `[测试护栏][fixture 纠错后覆盖]`

03.2 preserved candidate → 当前 test diff 显示，原 canonical Page fixture 的 `x=128,width=540,pageBoundary=[96,636]` 被直接改为 `[96,832]`；对 `canvasEngine/**/*.{test.ts,test.tsx}` 的完整扫描中，`right: 636` / `[96,636]` 命中为 **0**，所以用户点名的“原 `[96,636]` crossing 形态仍在矩阵”并未按精确 fixture 落户。

这不是当前 production failure：reviewer 亲调共享 classifier 得到旧 boundary=`canvas_workspace/crossing/owner-frame-a`、新 boundary=`formal_page/inside/owner-frame-a`；再把旧 tuple 正确申报成 `canvas_workspace/crossing`，raw v1/v2 production probe 均为 replayable、blocked=[]。现有 `surfaceAuthorityContract.test.ts` 也有 Page-local 与 Canvas-world 的右界 crossing，persistence 另有左界 canonical crossing。因此按 LOW 记常驻精确行缺口，不阻断本轮 PASS。建议把原 `[96,636]` 作为 recovery positive matrix 的独立 right-crossing row（raw v1/v2）补回，避免纠错 fixture 同时抹去原形收据。

### 2. 03.3 Review §1 四例原始 witness 原样重放

Reviewer 以 03.3 Review 记载的原坐标和申报 tuple 直接 bundle 当前 production；esbuild metafile 的 production inputs 只有 `draftBlockPersistence.ts` 与 `shared/types/canvasSurfaceAuthority.ts`，没有借用测试实现。

| 原例 | raw | 共享 classifier 权威 decision | 当前 queue / 写链 |
|---|---:|---|---|
| declared `formal_page/inside/frame-a`; `x=300,width=10,boundary=[100,200]` | v1 | `canvas_workspace/outside/null` | replayable 0；blocked 1；finalizer/create/placement/latest/recovered 全 0；v1 bytes exact；v2 null |
| 同上 | v2 | `canvas_workspace/outside/null` | replayable 0；blocked 1；五类 calls 全 0；v2 bytes exact；v1 null |
| declared `canvas_workspace/crossing/frame-a`; `x=120,width=20,boundary=[100,200]` | v1 | `formal_page/inside/frame-a` | replayable 0；blocked 1；五类 calls 全 0；v1 bytes exact；v2 null |
| 同上 | v2 | `formal_page/inside/frame-a` | replayable 0；blocked 1；五类 calls 全 0；v2 bytes exact；v1 null |

四例 `tupleExactEqual=false`，blocked reason 均为 `owner_authority_unknown`，storage key 分别保持在 v1/v2 原 key。与 03.3 的实际 `replayable=[key] + create→placement→latest` 相比，原证人已转绿。

HTTP 层没有从“finalizer 未调用”外推：reviewer 另亲跑 production hook 的 persistence + adapter 两文件 **38/38**。adapter 的同四例均等待 `pageFrameCollection` hydration 触发 effect rerun，再精确断 `addToast` 一次、`mocks.post=0`、`mocks.put=0`、原 storage bytes 不变；因此 POST/PUT=0 有实际 adapter 证人承载。

### 3. 接线与 classifier 零 diff 核形

1. `draftBlockPersistence.ts:2-5` 从 `../../../../../shared/types/canvasSurfaceAuthority` 直接 import `classifyCanvasSurfaceAuthority`；本文件没有第二份 box-right/boundary 几何算法。
2. framed tuple 先过 coordinate/frame/boundary 结构门，`:157-162` 把 receipt 的 `coordinate_space`、`{x,width}`、`pageBoundary`、explicit `surface` 交给共享函数；`:163-165` 分别以 `===` 核对 `decision.surface / boundaryRole / frameId` 与申报三元组。不是 truthy/部分比较。
3. preserved 03.2 reviewer candidate 最终 `git status` clean。对其中 classifier 与当前 classifier 做 EOL-normalized SHA-256，二者同为 `9f5aff0690b6e7e863b3c3818cd590e7ee50e35407a4319884b11f3fa480a959`；`git diff --no-index --ignore-space-at-eol` exit 0。classifier 本体零内容 diff。
4. 对 preserved 03.2 candidate → 当前的 `client/src + client/scripts + server/src + server/scripts + shared + 三份 package.json` 共 **504** 文件做 EOL-normalized union scan，差异精确为四文件：`draftBlockPersistence.ts/test.ts`、`useNoteCanvasDataAdapter.test.tsx`、`useRuntimeNaturalWritingController.ts`，与 03.3 Review 已核的累计清单一致；server/shared/package 无新增差异。03.4 没有顺手改 classifier/placement/server/migration。

### 4. Fixture 纠错与 03.3 保护面

#### 4.1 Fixture 两版本亲验

同一 production classifier 的直接输出：

```json
{"oldBoundary_96_636":{"surface":"canvas_workspace","boundaryRole":"crossing","frameId":"owner-frame-a"},"correctedBoundary_96_832":{"surface":"formal_page","boundaryRole":"inside","frameId":"owner-frame-a"}}
```

旧 fixture 的 box right=`668`，越过旧 right=`636`，确为 crossing；新 right=`832` 才完整包住 `[128,668]`。所以把 right 从 636 改为 832 是修正错误正控，不是把 production assertion 调松。精确旧 row 未常驻的问题已单列 LOW-1。

#### 4.2 保护矩阵

- **quota 三重**：throwing `setItem`；silent no-op/read-back mismatch；`remember` same-key replacement。三者均证明未确认 v2 bytes 前不清 v1；第一案还走 `load → failed replay → reload`，原 receipt 仍在。
- **null-frame / structural matrix**：raw v1/v2 `crossing + frame/boundary null` 均 blocked；surface/role 互换、outside 携 frame/boundary、outer/authority coordinate mismatch、boundary frame mismatch、falsy malformed field 均保留。
- **toast 次数**：ownerless 与四个 geometry contradiction adapter rows 都让 hydration 改变 callback identity并触发 effect rerun，`toHaveBeenCalledTimes(1)` 在 rerun 后仍成立。
- **正控**：canonical inside、left crossing、outside、canonical v1→v2、multi-frame、PageStack existing/append、blocked same-key、corrupt v2、blocked-v2 + canonical-v1 并存均仍进入 112 项 unit 门；没有用“全部 v1 blocked”砍升级。

### 5. Reviewer 亲跑全门

| 门 | 结果 |
|---|---|
| root `npm.cmd run test:unit` | 15 files，**112/112** |
| server，独立 `CANVAS_ASSET_DIR=.codex-tmp/canvas-assets-review-v2bn12-03-4-rerun`，`npm.cmd run test:v2` | **262/262** |
| root `npm.cmd run verify:v2-bn8-runtime` | exit 0；unit 112、runtime boundary 159、relation freshness、model contract 60、client/server build、performance 5、diff/secrets 全绿；secret scan 59 files |
| client `npm.cmd exec tsc -- --noEmit` | exit 0，无 diagnostics |
| server `npm.cmd exec tsc -- --noEmit` | exit 0，无 diagnostics |
| server `npm.cmd run test:v2-bn12-migration` | **5/5**；fixture/临时 DB，未 `--apply` |
| persistence + adapter 定向 | 2 files，**38/38** |

非阻塞输出仅为既有 React Router future flags、Vite dynamic-import/chunk-size 与 LF/CRLF warning。

### 6. D4 readonly byte-level post-state

Reviewer 没有让 SQLite 打开 live path：先对 DB/WAL/SHM 做 fingerprint，把 DB bytes 克隆到 Buffer，仅把克隆 header `[18,19]` 从 `[2,2]` 改为 `[1,1]`，再以 `{readonly:true}` + `PRAGMA query_only=ON` 查询；关闭后重新 fingerprint。三件 bytes/mtimeMs/SHA-256 前后 exact，`live.identical=true`：

- DB：`2539520` bytes / `2c47907fcaeedcfe9a47e1bff701437c1eea63978285d10e0b162cb3a6e9c519`；
- WAL：`0` bytes / `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`；
- SHM：`32768` bytes / `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb`。

四 tuple 未变：`1b83…`、`38f9…`=`x72/w760/formal_page/inside/primary-page-frame/canvas_world`；`c02b…`=`x414/w760/canvas_workspace/crossing/...`；`f6ce…`=`x14/w760/canvas_workspace/crossing/...`。21 ghosts 的六类 physical residue 均 0，birth operation batches=21，client create receipts=`[]`，FK=`[]`，quick/integrity=`ok`。

`git diff --exit-code 41e6d1b -- schema.sql migrate.ts migrations/** surfacePersistenceContract.test.ts` exit 0；未执行 migration apply。

### 7. §5-2 跨条、下一状态与五轮累计扫描

| 合取面 | 03→03.1→03.2→03.3→03.4 终态 |
|---|---|
| same-key response loss × adapter reused × controller revision | adapter seam 与 controller dirty sentinel 仍在 unit 门，VERIFIED，无正文回退 |
| compensation predicates × cross-note refs × cleanup identity | server 262 全绿，相关 server files 相对 preserved 03.2 candidate 零 diff，VERIFIED |
| crossing hydrate/normalize/save × D4 tuples | surface authority 16 项 + D4 四 tuple 均稳定，VERIFIED |
| meaningful gate × Page table × D4 ghost cleanup | helper 5 项与 live post-state一致，4/4 旧 dead-end 修复未回归 |
| visible primary × stale secondary × PageStack existing/append × immutable session | runtime 5 项 + PageStack 2 项全绿，VERIFIED |
| storage write failure × replay failure × reload | quota/no-op/same-key 三重均绿，v1 不先删，VERIFIED |
| structural tuple × geometry authority × finalizer/adapter | null-frame 与本轮四原 witness 全 blocked；finalizer/POST/PUT=0，VERIFIED |
| schema/migration/package/受保护 RED | 限定 diff + 504-file scan 无新增施工，VERIFIED |

下一状态也闭合：矛盾 receipt 不进入 replay 集，后续 hydration/effect rerun仍只提示一次并原 key 留队，不会调用 forget 或产生网络写；正确申报的原 `[96,636]` right-crossing raw v1/v2 均可 replay（v1 仍走正常升级），没有因 fail-closed 误伤合法 crossing。对全部 handoff header 做未截断扫描后，2026-08-20 的 V2.BN.12 集合中没有 03.4 之后的实现单；没有一个具体“下一单”会把当前结论变形，可验证的下一态限于上述 recovery mount/replay。

五轮累计没有发现 03.4 把 03/03.1/03.2/03.3 已确认修复互相打架。LOW-1 是精确 regression fixture 欠账，不是运行时矛盾。

### 8. 5-1 收据完备、范围与过程披露

1. shared tree 开始即含工单 03 全链累计未提交改动；reviewer 没有修改 product/schema/migration/生产代码/常驻测试。唯一最终交付写入是本 `## Review`。
2. 四例 witness 的 review-only 临时脚本只在 `.codex-tmp` 存活到输出取得，随后以 patch 删除；esbuild `write:false`，sessionStorage 为内存实现，未 mutation shared production。
3. 旧/新 boundary 的前三次 Windows `node -e` 变体因参数引号剥离失败，输出全部作废；同一 source 经 stdin 成功后才采信。首次 504-file PowerShell scan又因旧 .NET 缺 `Path.GetRelativePath`/静态 SHA API 作废，随后用 Node 完整重跑为 504/4。失败命令均无写入。
4. CodeGraph shell 不在 PATH 后改用同一已索引项目的 CodeGraph MCP；当前 untracked/fresh 源再由 on-disk verbatim source与全门承载。未把工具缺席推成代码缺席。
5. 未执行 migration `--apply`，未写/delete live row或 sidecar，未 commit/push/PR/merge。未另跑真实浏览器 E2E/hit-test；本修正单点名问题由 production storage/classifier/finalizer、真实 adapter fixture与全门直接回答。

### 9. 结论

本轮 **PASS（0B/0H/0M/1L）**。03.3 的唯一 MED 已由共享 classifier 本体接线关闭，四枚原 witness、HTTP 零写、fixture 几何、03.3 保护面、全门、D4 与五轮累计状态均取得 reviewer 自有收据。**复核侧意见：工单 03 全链具备 checkpoint 条件。** LOW-1 可作为非阻断测试债补齐；最终抽检、放行与 checkpoint 权仍在 Fable。
