> **状态 (Status)**: done(HQ 收口:显式恢复通道闭环,阳性对照闸保牙)
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-10
> **上游**: C-fix1 冒烟⑥停线举证(真实生产链:B /text-save rev3 提交 drifted,undo rev4 提交 active,库终态仍 drifted;boardTextRanges.ts:114-118 强制保留+atomicTextSave 复用;证据 docs/audits/2026-09-10-c-fix1-builder/06-board-range-undo-gap.json)
> **单号**: 13.5 · C 走查修二 · F17 范围撤销的显式恢复通道

# C 走查修二 · F17

**冲突本体**:客户端 B4 快照恢复契约(undo=范围状态精确回到 before)vs 服务端漂移证据保留规则(既有 drifted ⛔被普通写复活)——两法各有其正,现状=库终态与客户端认知分裂。

## 零 · 裁定(⛔复议)

1. **普通保存 fail-closed 不动**:非恢复意图的写入,既有 drifted 照旧强制保留(证据规则零松动);
2. **历史恢复=显式通道**:text-save 的 range 更新携显式恢复意图字段(命名裁量,如 `history_restore: true`);服务端**仅对该意图**在同一事务放行 status/offsets/pre_edit_offsets 的精确恢复;undo 与 redo 双向皆走此通道(redo 重新致漂=证据链完整,库史可循);
3. 客户端:B4 恢复路径为快照内的范围更新标记恢复意图;恢复失败=B4 失败语义(entry 留栈+明码);
4. **TextFlow-Contract(frozen)补修订注记**:追加 amendment 段记录本通道(⛔改冻结正文语义);
5. ⛔动漂移判定/rebase 语义/三态视觉;⛔新 schema。

## 一 · 验证(单内纪律)

- typecheck + build 全绿;client 全库+server 定向(text-save/boardTextRanges 文件)整跑⛔过滤;
- 冒烟五条:①C-fix1 同参真机链:跨块替换致 drifted→undo→**库终态 active、pre_edit_offsets 清**(修前断言现状红:库仍 drifted,即 06-board-range-undo-gap 现象);②redo→库重新 drifted,revision 链完整;③**阳性对照**:普通保存(无恢复意图)对既有 drifted 照旧保留(fail-closed 回归,闸不拔牙);④恢复失败注入→entry 留栈明码可重试;⑤B4-C4/C-fix1 全回归+全库。

## 二 · Result 格式

`## Result`:numstat + 五冒烟逐条 + 契约修订注记文本 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。

## Result

**执行者：Codex builder，2026-09-10（Toronto）。F17 修复完成，五冒烟 PASS；未暂存，待 HQ 复核／翻牌。** 普通保存保漂移规则保留；仅原子 text-save 的逐范围 `history_restore: true` 恢复快照。失败同向重试保留意图、冻结范围与 revision base，undo／redo 共用现有历史条目。无数据库 schema 变更，未动漂移判定、rebase、读时派生或三态视觉。本工单仅追加 Result，原状态头与裁定正文保持。

### numstat 与改动

代码／测试 **11 文件，+351 / -35**（`git diff --numstat`，未暂存）：

| 文件 | + | - |
|---|---:|---:|
| `client/src/pages/Notes/canvasEngine/atomicTextSaveRepository.ts` | 2 | 0 |
| `client/src/pages/Notes/canvasEngine/boardTextRangeEditSession.ts` | 2 | 0 |
| `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.ts` | 2 | 0 |
| `client/src/pages/Notes/canvasEngine/hooks/useTextFlowHistory.ts` | 27 | 11 |
| `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.test.tsx` | 120 | 5 |
| `client/src/pages/Notes/canvasEngine/hooks/useTextFlowHistory.document.test.tsx` | 30 | 2 |
| `server/src/services/atomicTextSave.ts` | 4 | 4 |
| `server/src/services/boardTextRanges.ts` | 19 | 4 |
| `server/src/validators/boardTextRanges.ts` | 18 | 9 |
| `server/src/__tests__/v13AtomicTextSave.test.ts` | 102 | 0 |
| `server/src/__tests__/v13BoardTextRanges.test.ts` | 25 | 0 |

另：冻结契约纯追加 **+10 / -0**；新建 `client/scripts/cFix2Smoke/` 三文件 **+211 / -0**（本地故障代理、断言／取证脚本、复跑说明）；本 Result 和 [证据目录](../../audits/2026-09-10-c-fix2-builder/README.md) 为交付收据。逐文件完整统计见 [delivery-numstat.txt](../../audits/2026-09-10-c-fix2-builder/delivery-numstat.txt)。原 C-fix1 八个源码／测试文件及原 fixture 与 HEAD 相同，未改开工前其他未跟踪文件。

### 五冒烟

1. **① PASS，先证修前红。** 复用 C-fix1 真实 controller／history／adapter／Express 路由＋新建 `:memory:` 库。相同 A/B 正文、B `tu-1`、`Ta`／0..2，先经三次普通 no-op 保存建立 B revision 3，再以 Chrome 原生 Home、Right×5、Shift+Down、`x` 跨块替换及 Ctrl+Z。修前 base3 提交 drifted、base4 提交 active／0..2／null，库读回仍 drifted／pre-edit 0..2，目标断言 **exit 1**，在改生产代码前完成。[修前请求／库状态](../../audits/2026-09-10-c-fix2-builder/01-before-red.json)、[红灯日志](../../audits/2026-09-10-c-fix2-builder/01-before-red.log)。修后同参同手势：base3 普通保存不带意图；base4 undo 携意图，**revision5 / active / 0..2 / pre_edit_offsets:null**。[修后证据](../../audits/2026-09-10-c-fix2-builder/01-after-undo.json)。
2. **② PASS。** Chrome 原生 Ctrl+Y，base5 携恢复意图，**revision6 / drifted / offsets:null / pre-edit 0..2**；3→4→5→6 顺序完整。[Redo 证据](../../audits/2026-09-10-c-fix2-builder/02-after-redo.json)。
3. **③ PASS，阳性对照闸保留。** 通过真实生产 API／合成内存库，普通保存把 B 正文恢复成与 `Ta` 匹配的 before 正文，同时提交 active／0..2／null **且无 history_restore**；base9→revision10 后仍 drifted／pre-edit 0..2。先断言正文切片确实等于 `Ta`，排除“正文不匹配自然派生 drifted”造成的伪阳性。[完整请求与结果](../../audits/2026-09-10-c-fix2-builder/03-positive-control.json)。此前 base8 的弱对照仅用当时编辑后正文，不作为闸证据，完整请求史仍保留。此条为 API 操作，未冒充 Chrome 手势。
4. **④ PASS。** 下一次原生 Ctrl+Z 中 A 已写成、B 被合成代理一次性 503 拒绝；B revision 保持6、仍 drifted。页面明确出现 **“A block edit is waiting for recovery”／Apply／Dismiss／Retry saving block**。再次 Ctrl+Z，**只补 B**，完整 JSON 请求与失败请求相等（base6／冻结范围／意图皆保留），成功为 revision7／active／null；继而 Ctrl+Y 仍可执行，revision8，证明原条目未被丢弃。[失败状态](../../audits/2026-09-10-c-fix2-builder/04-after-failure.json)、[失败请求](../../audits/2026-09-10-c-fix2-builder/04-after-failure-transport.json)、[重试对照](../../audits/2026-09-10-c-fix2-builder/04-after-retry-transport.json)。客户端两方向测试另验 blur retry 不卷入后来新范围、recovery receipt 保留意图、模板首次保存无意图。服务端原始 SQL 测试在正文及实际 annotation 更新之后触发 Board 写失败，验证整事务回滚及同请求重试。
5. **⑤ PASS，前序回归完整。** Client **116/116 文件、1249/1249 测试 PASS，65.53s**，使用 `npm.cmd run test:unit -- --maxWorkers=2`，不筛文件／用例、不 skip、不改 timeout。[全库日志](../../audits/2026-09-10-c-fix2-builder/client-full-unit.log)。覆盖 B4–C4 text/history、跨块选择／输入、导航、atomic adapter、inline、grapheme、把手／提取／迁移、Board、PaperInk／打印／总览，以及 C-fix1 的属性桥／把手／page-frame 回归；原 C-fix1 八文件及 fixture diff 为零。[保护检查](../../audits/2026-09-10-c-fix2-builder/delivery-checks.json)。Server 两份指定文件整跑 **29/29，0 skip**：[日志](../../audits/2026-09-10-c-fix2-builder/server-targeted.log)。本条是完整自动回归＋上述 F17 真机链，不声称把前序所有手势逐项再走一遍。

### 类型、构建与额外门

- Client `tsc --noEmit`、`npm.cmd run build`（`tsc -b && vite build`）PASS；server `npm.cmd run build`（含 `tsc`）PASS。[client](../../audits/2026-09-10-c-fix2-builder/client-build.log)、[server](../../audits/2026-09-10-c-fix2-builder/server-build.log)。首次 client build 抓到新增测试 `.at()` 不符合现有 lib，已改数组索引，再 typecheck／build／全库通过；未改 tsconfig。仅既有 bundle 大小／schema recursive-any 警告。
- 允许的 runtime 子命令 **16 PASS / 1 FAIL**；唯一失败仍为既有 `docs/agent-ops/INDEX.md` 过期，未重生成。短路后的 inventory、glossary K1–K3 独立 PASS。[逐项结果](../../audits/2026-09-10-c-fix2-builder/runtime-results.json)。最终 `git diff --check` PASS；独立只读复核无阻塞发现。

### 契约修订注记文本（仅末尾追加）

以下为 [TextFlow-Contract amendment](../../contracts/TextFlow-Contract.md#amendment--2026-09-10--v135-c-fix2--f17-explicit-range-history-restoration) 原文；冻结正文未改：

**Amendment — 2026-09-10 · V13.5 C-fix2 / F17 explicit range history restoration**

Authority: [C-fix2 order](2026-09-10-v13-5-c-fix2-order.md). This appended amendment reconciles B4 snapshot replay with the ordinary board-range drift-evidence rule; the frozen sections above are unchanged.

Each touched board-range patch in a history restore through `PUT /api/note-blocks/:id/text-save` carries `history_restore: true`. Undo and redo both use this explicit channel. Only a patch carrying that literal intent may restore its snapshot's status, start/end offsets and `pre_edit_offsets` exactly, including clearing the evidence with `null` on undo and restoring it on redo. The server applies this within the existing body/annotation/range/revision transaction, with the existing scope and revision checks. Independently deleted ranges stay absent.

Ordinary saves omit the intent. An existing drifted range remains drifted and retains its existing pre-edit evidence under the ordinary save rule. A standalone board-range update does not acquire a history-restore channel. Intent belongs to the save request and recovery snapshot, not to the persisted range model; this amendment adds no database migration and changes no drift detection, rebasing, read-time derivation, coordinates or three-state visual behavior.

Only the touched ranges frozen in the history entry receive restore intent. Initial edits and initial template conversions remain ordinary saves. A failed restore retains its entry, explicit failure/retry state, original revision base and same-direction restore intent; retry reuses the frozen ranges rather than adding unrelated later ranges. Confirmed undo/redo alone moves the entry between stacks. The B7 per-block transaction boundary and its cross-block and lost-response limitations remain in force.

### 未做与停线

- **未 stage／commit／push／PR／merge；未读 `.env`／key 值；未接触用户库；未跑安全类测试或凭据扫描。** Vite 使用空 envDir；真机 fixture 明确 `envFile:false`，数据仅新建内存库与合成资产目录。两个本轮 helper 进程使用完毕后停止。
- **完整 `npm run verify:v2-bn8-runtime` 未运行**，末项凭据扫描按本单留 HQ；允许的子门结果如上，不能称总门全绿。既有 docs INDEX 过期也留文档守门人处理，未改 current-state／权限文件。
- fixture 的范围读回沿用既有读时状态派生；原始 SQL 持久字段另由 server 定向测试断言。浏览器截图在会话内查看，未另存图片；保留请求／响应／revision／合成库视图及未过滤测试日志。
- **F17 技术停线项已消除；交付停在未暂存、待 HQ 复核态。** 总门未闭合（禁跑扫描＋既有文档索引红），最终放行仍留 HQ／Henry。本单不修改跨块非原子边界、丢响应重试语义，也未扩修失败恢复时既有 “Source locked” 文案。
