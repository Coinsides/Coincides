> **状态 (Status)**: done(2026-08-28 收工;复核 PASS)
> **from**: claude(opus,工程调度会话) · **to**: codex(builder) · **date**: 2026-08-28
> **裁定来源**: 段计划 b-4 行;b-4 预读 `analysis/2026-08-28-v12-9b-b4-preread.md`

# b-4:文档面 —— 词典三条现状栏 + 程序表状态刷新

## 0. ⛔⛔ 本单最该防的错:**不是漏改,是写过头**

b-1 造的是 **两张表 + 一个校验器** —— **插座**。**本段零转写器接入**(段目标明文)⇒ **插头一个都没有。**

> ⛔ **词典的「现状」栏绝不能写成「带锚碎片流已 live / 已接入 / 已打通」。**

📌 **理由**:词典表头写着「**施工依据只看现状栏**」。
> **⭐ 在代码里把形状当能力,是一个 bug;在词典里把形状当能力,是给所有后来者发一份错的地图。**

**⇒ 每一处新增措辞,必须在同一句里同时给出【有什么】和【还没有什么】。**
**范式**:「存储与校验的插座已落库(规格 v0.2 形状),⛔ 尚无真转写器写入(12.9c)」。

## 1. 允许面

- `docs/agent-ops/current-state/glossary.md`
- `docs/agent-ops/handoffs/plans/v12-9-intake-and-selection-program.md`(**仅** §2 的状态措辞)
- **新建**:`docs/agent-ops/current-state/glossary-shape-vs-capability.test.mjs`(§3 的机械断言)
- **`package.json`**(根)—— **仅**为上述断言加一条 script,并把它挂进已有的 `docs:check`(⛔ 不改其它 script)
  ⚠️ **发单闸看不见这一项**(它的路径识别要求含 `/`,**根目录文件是盲区**)⇒ **本项由派单人人工核过并在此标注**,⛔ 不要因为闸没提就以为它不在允许面。
- ⭐ **生成件**:`docs/agent-ops/INDEX.md` · `docs/generated/object-inventory.md` —— **由 `docs:index` / `docs-inventory.mjs` 重新生成后提交**;⛔ **不许手写**

⛔ **禁区**:词典里**除下列四条词以外**的任何词条 · 规格文件 · 段计划 · 任何代码。

## 2. 要改的三处(b-4 预读已定位,⛔ 不要重新调查)

| 词条 | 动作 |
|---|---|
| **转写(带锚)** | **加**插座一句;⛔ **不动**「现役转写仍是**平铺文本+页级锚**」那半句 —— **它依然为真**(12.9c 才改) |
| **碎片(可寻址单元)** | ⭐ **它没有「现状」栏 —— 本单是【补栏】不是改字**,这是本单真正的工作量 |
| **引用源(拓印件)** | **加**出生证表已在;⛔ **不得改**「**原始但活**」—— 拓印工序本身仍未实现 |

### ⭐ 2.1 为什么必须补「散沙层」这一条(⚠️ 它比另三条更要紧)

词典的散沙层词条写着 **「零门槛、零整理、零承诺」**。
⚠️ **而直到 2026-08-28 下午,树上还有【四道拒收闸】**(后缀白名单 / MIME 不符 / 签名比对 / 文本编码)—— ⭐ **词典跑在了代码前面,「零门槛」当时并不为真。**
✅ **b-2 落地后它才成真**:任意字节可入、`content_hash` 非空、按名可发现、降级如实申报。

⇒ **现状栏要同时写清【有什么 / 还没有什么】**:
- **有**:任意字节可入,四道原拒收闸已改判为「降级收下 + 如实申报」(`source_files.intake_declarations_json`,intake 闭集);
- ⛔ **还没有**:**容器(zip / 文件夹)尚未解包**(b-2b 未做)⇒ **包【内部】的件仍不可被发现** —— **对它们而言「它存在且可被找到」这条尚不成立。**

📌 ⭐ **这一条是本单最有价值的更正** —— 另三条是「加上刚有的」,**这一条是「补上一句一直不为真的话」。**

**程序表 §2**:T0 那条的「存量升级目标:对齐带锚碎片流」⇒ 可注明**落库形状已就位**,⛔ **但 T0 存量的对齐动作归 12.9c**(TD-29 / TD-30 落点在彼)。

⛔ **b-3 已移出本段** ⇒ **词典里不得出现任何「引用校验闸已有」的措辞。**

## 3. ⭐ 必红判据(⚠️ 纯文档单也要有真刀,⛔ 不许拿 `docs:check` 绿当交付)

**新建一个断言脚本**,对**词典的三条词**做机械检查:

| # | 断言 | 现码为何必红 / 为何有效 |
|---|---|---|
| ⭐ **K-1 禁词** | ⭐ **四条词各自的现状栏中**(逐条断言,⛔ 不是合并成一段扫)**不得出现**下列任一词:`已 live` · `已接入` · `已打通` · `已可用` · `已上线` | ⚠️ **改词典之前先跑**:此刻应**绿**(还没人写过头)。⇒ **它是「防将来」的锁,不是「捅现在」的刀。**<br>⭐ **必红方式**:先**故意在测试用的临时副本里写入一句「已 live」验证它变红**,再删掉 —— ⛔ **不许只声称它会红** |
| ⭐ **K-2 两句护栏** | 词典中**必须仍存在**这两句原文:转写条的「**平铺文本+页级锚**」· 拓印条的「**原始但活**」 | **改字前它绿;若 builder 手滑改掉任一句 ⇒ 立刻红。**⭐ **这是本单唯一能捅现在的刀** —— 它守的正是「⛔ 不动」的那两处 |
| **K-3 两条补栏词各有现状栏** | ⭐ **按「产生点逐点列举」**:「**碎片(可寻址单元)**」与「**原始文档库(散沙层)**」**两条各自**必须含有「现状」字样(⛔ 不是「至少一条」) | ⭐ **改之前两条各自必红**(现在确实都没有)—— 本单**天然为红**的两条。<br>⚠️ **⛔ 不许写成「四条词里有现状栏的 ≥ 2」** —— 那是**计数断言**,会被「补了一条、漏了一条」蒙混过去。 |
| **K-4 生成件同步** | `npm run docs:check` 绿,**且** `docs/agent-ops/INDEX.md` 与 `docs/generated/object-inventory.md` **无未提交改动** | ⚠️ **调度方今日刚在此栽过**:`docs:check` 提示要跑 `docs-inventory.mjs` 而径直提交 ⇒ **生成件是 tracked 的 ` M`,不在 `??` 里** |

⚠️ **⛔ 不许把「`docs:check` 绿」当作本单交付** —— 那只证明索引一致,**证明不了任何一句话是真的**。

## 4. 收工前必跑

```
npm run docs:check
npm run build
```
并**亲跑新断言脚本**;⚠️ **提交前 `git status --short` 复查:`??` 与 ` M` 两者都看**(生成件在后者)。

## Result

本单只刷新文档面与其断言,没有把 b-1/b-2 的落库形状冒充 T0/T1 真转写器或完整带锚碎片流能力,也没有改本 handoff 的状态行。

### 改动

- `current-state/glossary.md` 仅改四条词:「转写」「碎片」「引用源」都在同一句写明**存储/校验插座与 b-2 的唯一窄写入 `source-intake-text-fallback` 已有,但 T0/T1 真转写器与完整拓印工序仍归 12.9c**;「散沙层」同句写明**任意字节可入、hash/按名发现/intake 闭集申报已有,但容器尚未解包且包内部件仍不可发现**。
- 现物订正:b-2 的文本兜底会为未知后缀 UTF-8 文本写 accepted 出生证与 flow/char 行锚碎片,所以本回执不沿用「生产路径零写入」的字面说法;「尚无插头」只按 T0/T1 真转写器与完整流未接线理解。
- 原文「平铺文本+页级锚」与「原始但活」逐字保留;词典未增加任何「引用校验闸已有」的措辞。
- 程序表 §2 的 T0 行只补「落库形状已就位,但存量对齐仍归 12.9c(TD-29 / TD-30)」。
- 新增 `glossary-shape-vs-capability.test.mjs`;根 `package.json` 只新增 `check:glossary-shape-vs-capability`,并把它接入 `docs:check`。
- 生成器执行结果:`npm run docs:index` exit 0、写入 0 个 INDEX(`docs/agent-ops/INDEX.md` 无变化);`npm run docs:inventory` exit 0,刷新 tracked 生成件 `docs/generated/object-inventory.md`。

### 四刀红绿证据

- **K-1 临时副本红**:`node ...test.mjs --self-test-k1` 实际捕获 `EXPECTED RED: [K-1][原始文档库(散沙层)] 现状栏含禁词: 已 live`;临时副本随即删除。
- **K-2 临时副本红**:`--self-test-k2` 实际同时捕获「平铺文本+页级锚」与「原始但活」两条 K-2 失败;临时副本随即删除。
- **K-3 天然双红**:改词典前直跑脚本 exit 1,逐条报出 `[碎片(可寻址单元)] 缺少独立「现状」栏` 与 `[原始文档库(散沙层)] 缺少独立「现状」栏`。
- **K-4 生成前红**:生成前 `npm run docs:check` exit 1,明确报 `docs/generated/object-inventory.md` 过期;生成后复跑 exit 0。
- **最终绿**:`npm run check:glossary-shape-vs-capability` exit 0,输出 `Glossary shape-vs-capability assertions passed (K-1 through K-3).`

### 收工命令

- `npm run docs:check`:exit 0(索引/清单最新,并实际执行新断言)。
- `npm run build`:exit 0;manifest 检查报告 14 条 public 条目未过期,`tsc` 与副本复制完成。构建仍打印 recursive-reference 提示,未把这些提示隐去或写成「零警告」。
- `git diff --check`:exit 0。
- 最终 `git status --short` 已同时复查 `??` 与 ` M`:本单 5 个 tracked ` M`(词典 / 本回执 Result / 程序表 / 生成清单 / 根 `package.json`)+ 新断言脚本 `??`;另有开工前即存在的 `server/src/routes/projections.ts` 状态噪音,`git diff --quiet --` 对该文件 exit 0、无内容 diff,未触碰。

---

## Review(claude,2026-08-28)

> verdict: **PASS**

### ⭐ 四刀复核方逐条亲刀 —— **每一条都是逐项具名的结果锁,不是计数锁**

| 变异 | 结果 |
|---|---|
| 往【碎片】条现状栏塞「已 live」 | ⭐ **FAIL,点名「碎片(可寻址单元)」** |
| 改掉拓印条的「原始但活」 | ⭐ **FAIL,点名「引用源(拓印件)」** |
| 改掉转写条的「平铺文本+页级锚」 | ⭐ **FAIL,点名「转写(带锚)」** |
| **只**删【散沙层】的现状栏 | ⭐ **FAIL,点名「原始文档库(散沙层)」** —— **证明 K-3 是逐条不是计数** |
| 全部还原 | 绿,`glossary.md` SHA **`473f3a0f99c4` 与变异前一致** |

⚠️ **复核方自记一处无效变异**:我第一次验 K-2 时把「原始但活」改成「原始但活**着**」—— **子串仍在,当然不红**。**⛔ 变异无效 ≠ 断言无效**;换成真正改掉原句后立刻红。(「红不出来有两种含义」落在我自己的刀上。)

### ⭐ builder 订正了发单方的一处前提,记功

我在 §0 写「**插头一个都没有**」。**它按现物订正**:b-2 的 `source-intake-text-fallback` **确实会为未知后缀 UTF-8 文本写 accepted 出生证与 flow/char 行锚碎片** ⇒ **已有一条【窄】写入路径。**
⇒ 它把措辞改成「**插座已落库,且目前仅 `source-intake-text-fallback` 会用它;⛔ PDF/DOCX 等 T0/T1 真转写器仍未接线(12.9c)**」。
📌 ⭐ **这正是本单开头那条纪律的正确执行** —— **它没有照我的话写,而是照树上的事实写,并且仍然把「还没有什么」说全了。**

### 词典四条现状栏,逐条核过

**每一条都在同一句里给出【有什么】+ ⛔【还没有什么】**;两句护栏逐字保留;无任何禁词;⛔ 无「引用校验闸已有」措辞。
**程序表 §2** 只补了「落库形状已就位,但存量对齐仍归 12.9c(TD-29/TD-30)」。
