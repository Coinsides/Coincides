> **状态 (Status)**: done（builder 工作树交付；待 HQ 复核放行）
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-11
> **单号**: 13.5 · 波次 E4 · Overview 重做为 Word 式多页网格
> **上游**: 09-11 设计日会议记录 §三.5(Henry 拍)+ **Henry 09-11 晚追加反馈(截图在案)**:现 Overview"太丑、空间太小"——单页小卡挂在角落,大片视口空置;目标=Word 缩放视图的感觉

# E4 · Overview 多页网格

## 零 · 裁定原文

1. **真内容,非缩略卡**:每页=真实页面内容按比例缩放渲染(打印投影 fragment 管线现物,换排布),⛔截图/⛔简化占位;
2. **Word 感,空间用满**:多列网格铺满整个视口,页间距紧凑;列数随视口宽自适应(宽屏 3-4 列,窄屏 1-2 列);页多时纵向滚动,⛔一屏一页翻屏式(现"Previous/Next screen"翻屏废除);
3. **页脚页码**:每页下方页号(沿 Word 惯例小字居中);当前阅读页高亮描边;
4. **点击回读**:点任一页→关闭 Overview 并滚动到该页(现役语义保留);
5. 安静气质:去现版的标题区/说明文案大块头,顶部一行轻标题即可(或纯网格+Esc 退出),空间还给页面。

## 一 · 交付面

- Overview 层重排:网格布局+缩放渲染+自适应列数+滚动;
- 现"Page overview/Select a page…/Pages 1-1 of 1/Previous·Next screen"整套 chrome 清算:保留退出钮(右上 X 或 Esc),其余收敛;
- 页渲染复用 NoteReadOnlyPageContent/打印投影管线(E2 申报:该管线现不含标注章/高亮——维持现状,⛔本单扩);
- 性能:页数多时可视区外惰性渲染(申报策略),⛔一次性全量渲染大笔记卡死。

## 二 · 禁区

⛔动打印投影语义;⛔动页数据;⛔TextFlow 面;⛔安全类测试;⛔碰 .git;⛔改权限配置。收口跑受影响秒级静态门。

## 三 · 验收

- typecheck+build 绿;定向绿+新增网格布局/列数自适应/点击回读用例;
- 冒烟(真浏览器):①多页笔记 Overview=网格铺满视口,真内容可辨;②窄/宽视口列数自适应;③点击页回读定位正确;④单页笔记不再是角落小卡(居中合理占幅);⑤大页数惰性渲染不卡;
- 证据落 `docs/audits/2026-09-11-e4-builder/`(含前后对比截图)。

## 四 · 申报义务

Result 必含:交付清单+diff、布局/惰性策略说明、冒烟证据、测试数字。冲突停线⛔自作主张。

## Result

2026-09-11 · codex builder 工作树交付，未 commit。无施工文件冲突，主观放行留 HQ。

- **交付**：Overview 连续纵向网格，1–4 列随可用宽度自适应；原 220px 页宽上限、两行翻屏、说明区与底部 Previous/Next 全部移除。原 980px 工作区限制仅在 Overview 时放开；顶部只留一行 `Overview · 页数` 与 X。页脚居中页号、当前阅读页描边、整页点击回读及 Esc 退出齐备。
- **真内容/禁区**：每页仍是 `NoteReadOnlyPageContent` 的原打印 fragment 内容，在原 frame 宽高下整体缩放。长页不再截成 1.6 比例。打印投影语义、TextFlow、页数据、权限配置均未改；标注章/高亮/纸上标题保持现役投影边界。
- **布局/惰性**：最小列宽 280px、间距 24px、最多 4 列；单页按可用宽高完整缩放并居中。保留轻量页壳维持原生连续滚动，只挂载视口及前后各一屏的真实页面内容，远页卸载。初开定位当前阅读页；resize 保留可视页及页内位置。当前页由阅读 DOM 派生，不写 collection 选择态。
- **diff/清单**：[生产 diff](../../audits/2026-09-11-e4-builder/production.diff)，[8 文件 SHA-256 清单](../../audits/2026-09-11-e4-builder/source-manifest.json)。6 个生产文件涉及 Overview 组件/CSS、显示布局 helper、Overview controller、document 层传参与外壳 CSS；2 个测试文件覆盖网格/惰性及回读回归。
- **冒烟**：[完整证据](../../audits/2026-09-11-e4-builder/README.md)，含同夹具多页/单页前后截图、真实应用单页前后截图、宽窄屏、长页、240 页截图与原始 DOM 指标。1920/960/720/390px 实测 4/3/2/1 列且无横溢出；240 页初开真实内容 16 页，滚至中段 24 页，首页已卸载；夹具点击第 89 页回读正确，真实应用单页回读成功，最终新加载页面 console error 0。
- **测试**：受影响 6 文件 **78/78 PASS（3.00s）**；`tsc -b` exit 0；`npm run build` exit 0（Vite 3.45s）；runtime boundary 静态门 **167/167 PASS**。日志在证据目录。依本单仅跑受影响门，未运行安全类测试或总 runtime 门。独立复核发现的 resize 锚点漂移已修并纳入测试。
- **观测边界**：大页数使用不写数据库的生产渲染夹具；真实持久笔记本轮覆盖单页完整入口/回读。施工热更新曾触发 hook 队列错误，重载恢复；旧标签页离开时见通用保存失败提示，最终新加载页面复验无 error，未扩改保存管线。详见审计说明。
