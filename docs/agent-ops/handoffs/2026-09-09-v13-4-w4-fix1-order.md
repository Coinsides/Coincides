> **状态 (Status)**: done
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④第一批 findings(claude-log 09-09 §21 记账);13.4 已收官,本单为走查修整批一
> **单号**: 13.4 走查修一 · 块移动保存 bug + Staging 化妆 + 弹窗形态

# 走查修一

**使命**:修走查④第一批——一只真 bug 置顶,两批外观/形态,一项打磨核查。

## F1 · 【bug,置顶】移动 paragraph block 报 "Failed to save block layout"

- **现象**(Henry 实测截图在案):笔记内移动一个 paragraph 块,右下角弹红 toast "Failed to save block layout";疑似导致"只有开 Layout 模式才能拖块"的体感(Move 手柄路坏了只剩 Layout 一条道);
- **先复现后修**:至少覆盖四象限——{全页笔记, open-note 弹窗} × {块控制条 Move 手柄一次性拖, Layout 模式拖};复现场景与根因写进 Result;
- 嫌疑面(按近改动倒查):单 3 的 `useBlockPlacementInteractions` onEnd 织入/`interactionController` pointercancel 改动;单 5 的 blur-save 暂停;单 6 的 inFlightWriteRegistry 登记;单 10 的只读块保存屏障;
- 修复+回归:普通移动四象限全部落库无错误;拖入 staging 照常;既有 placement 测试面不破。

## F2 · Staging(笔记级准备区,含弹窗内)化妆

Henry 原话口径:"成熟用户心智,不用写字"。

1. **入口挪位**:"Staging (n)" 入口从纸面上方移到**页面右缘**(与右缘既有 Groups 按钮同族的竖排位置,相邻摆放);弹窗内同理;
2. **侧栏文字清空**:两句说明文(“Drag rows to reorder…”“Move selected block to staging”长句)全部删除;标题 "Staging" 保留;Close 文字钮换 **✕** 图标钮;选中块存在时的移入动作精简为小按钮(icon+“Stage”短词);
3. **空态=虚线框**:空时显示一个虚线边框的投放区域(可配极淡的下载/拖入 icon),⛔ 任何句子;拖拽悬停时虚线框高亮(既有 drop 逻辑不动,只换皮)。

## F3 · Open note 弹窗形态

1. **贴纸宽**:弹窗内容区宽度收敛到纸宽+必要边距(去掉传统笔记页的大留白)——弹窗的使命=不离开板看/改笔记、板和笔记对照,板要露得出来;弹窗高度沿现;
2. **窗口可拖移**:标题栏作抓手,按住可在视口内拖动弹窗;位置仅会话内记忆(⛔ 持久化);与装卸区让位联动共存(打开装卸区时若相撞,按既有让位逻辑先行);
3. ⛔ resize 手柄(Henry 未要,候下批)。

## F4 · 【核查】板上 item 卡(member 卡)大小不可调

- 13.3 起 member 卡应有右下角 resize 手柄(min 160x100);Henry 实测铸卡产物调不了——核现物:手柄对 kind=item 是否在/是否被 A2/单 8 改动碰掉/还是可见性太差;
- 坏了就修;在而难发现则增强手柄可见性(hover 显式);Result 写清是哪种。

## 裁量与停线

- 停线举证不改判;F1 若根因在更深层(保存链契约)→停线举证;
- ⛔ 动 Layout 模式存在论(那是 13.5 设计题,HQ 已记账);⛔ 动板级装卸区语义;⛔ 新 event verb。

## 验证(单内纪律)

- typecheck + build 全绿;定向回归(canvasEngine placement/tray/modal+Boards)不破;
- 冒烟六条:①四象限移动块全部无错落库;②Move 手柄不开 Layout 也能拖块进 staging;③"Staging (n)"入口在右缘,弹窗内一致;④staging 空态虚线框、无说明文、✕ 关闭;⑤弹窗贴纸宽且标题栏可拖移、装卸区让位共存;⑥板上 item 卡可调大小(或 Result 说明现物结论)。

## Result 格式

`## Result`:numstat + F1 复现与根因申报 + 六冒烟逐条 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。

## Result

2026-09-09 · Codex builder 回执。**F2/F3/F4 已施工并验证；F1 已复现，命中保存链契约停线，未修复、未放行。** 因 F1 未完成，工单保持 `ready`，交 HQ 裁决；本次不 commit，工作树交 HQ 代账。

### Numstat 与施工范围

代码及功能回归共 10 文件，`+259 / -25`；本 Result 文档另计 `+86 / -0`，合计 11 文件 `+345 / -25`。

| + | - | 文件 |
| ---: | ---: | --- |
| 12 | 4 | `client/src/pages/Boards/BoardNoteModal.module.css` |
| 77 | 0 | `client/src/pages/Boards/BoardNoteModal.test.tsx` |
| 74 | 3 | `client/src/pages/Boards/BoardNoteModal.tsx` |
| 50 | 1 | `client/src/pages/Boards/BoardPage.item.test.tsx` |
| 6 | 3 | `client/src/pages/Boards/BoardPage.tsx` |
| 7 | 1 | `client/src/pages/Boards/Boards.module.css` |
| 10 | 2 | `client/src/pages/Notes/NoteDetail.module.css` |
| 5 | 2 | `client/src/pages/Notes/canvasEngine/layers/NoteRuntimeDocumentLayer.tsx` |
| 2 | 2 | `client/src/pages/Notes/canvasEngine/layers/NoteTraySidebar.test.tsx` |
| 16 | 7 | `client/src/pages/Notes/canvasEngine/layers/NoteTraySidebar.tsx` |

- F2：笔记级 Staging 入口复用 Groups 右缘按钮样式并相邻竖排；删除侧栏说明句，保留标题；关闭改为带可访问名称的 ✕；选中块时显示 icon + `Stage`；空态改为无句子的虚线投放框及淡图标，pointer 悬停高亮。未改 drop、排序或板级装卸语义。
- F3：弹窗宽度收至纸宽 904px + 两侧各 24px，受可用视口约束；高度保持 90dvh。标题栏支持 pointer 拖移并限制在可用区域内，位置只存在模块内存，关闭重开保留；沿用装卸区 280px 让位，暂时夹取不覆盖用户记忆位置。未加 resize 或持久化。
- F4：现物是“手柄存在且逻辑能工作，但难发现”，不是 `kind=item` 被排除。把原 14px 空方块增至 24px，加入对角 resize 图形和提示，保留 hover/选中显示及既有 resize 保存链。
- 独立复核已只读检查最终 F2–F4 diff，未发现实质问题。

### F1 复现与根因申报

**环境与证据边界**：在 loopback `127.0.0.1:5198` 建隔离夹具，使用生产 `NoteCanvasRuntime`、`BoardNoteModal` 与生产 canvas-object 路由/服务，SQLite `:memory:` 只装合成 user/course/note/paragraph 数据，浏览器执行真实指针拖拽。其他宿主数据使用夹具 API。未启动生产后端、未访问用户数据库，也未读取 Henry 的原始块记录或截图。因此下述为同症状的可复现契约缺口，不声称已唯一识别 Henry 那条记录的来源。

对照样本：v2 笔记含真实 page frame，paragraph 已有有效 `frame_id` 与 `page_frame_local` 布局。反例样本：同样保留笔记、paragraph、逻辑 placement 与真实 page frame，**仅去掉 paragraph 的 canvas-object 布局**，使它走默认块布局路径。Layout 打开后使用现有块拖动入口，不更改 Layout 的交互定义。

| 场景 | 有完整块布局的对照：实际保存回读 | 仅缺块布局的最小反例 |
| --- | --- | --- |
| 全页 × Move 手柄，Layout 关 | PASS，`x=0,y=121,width=760,height=44`，无 toast | FAIL，`Failed to save block layout`，18:37:01 UTC |
| 全页 × Layout 开启后拖动 | PASS，`x=0,y=181,width=760,height=44`，无 toast | FAIL，同 toast，18:37:22 UTC |
| 弹窗 × Move 手柄，Layout 关 | PASS，`x=0,y=222,width=760,height=44`，无 toast | FAIL，同 toast，18:37:48 UTC |
| 弹窗 × Layout 开启后拖动 | PASS，`x=0,y=263,width=760,height=44`，无 toast | FAIL，同 toast，18:38:12 UTC |

四次反例控制台底层错误均为 `A resolved page frame is required to save this coordinate contract`。四象限运行前后读取同一内存库的完整 canvas persistence，JSON 正文一致，未落入新块布局；不仅是 toast 表象。另曾用完全无 frame 的合成样本复现同错，但以保留 frame、仅缺块布局的最小样本作为根因证据。

调用链（路径均在 `client/src/pages/Notes/canvasEngine/` 下）：

1. `placementService.ts:440–455` 的 `buildDefaultBlockLayouts` 给缺布局块生成 `coordinate_space: 'page_frame_local'`，没有 `frame_id`；`:385–389` 只会保留已有块布局的坐标身份。
2. `hooks/useNoteCanvasLayoutModel.ts:129–150` 使用上述默认值，再交给 `toStoredLayout`。
3. `placementContractService.ts:32–33` 的 v2 帧选择不替缺 `frame_id` 的非 `canvas_world` 布局猜帧；`:88–90` 无法解析帧时保留原布局；`:128–130` 的 `requireStoredLayout` 在保存时抛出上述异常。
4. `canvasObjectRepository.ts:48–55` 加载笔记帧上下文，但没有补块归属；`:136` 在 `api.put` 之前调用 `requireStoredLayout`，故失败在请求发出之前。
5. `hooks/useNoteCanvasDataAdapter.ts:1935–1937` 捕获异常并显示 Henry 所见 toast。

按工单指定近改嫌疑面倒查后的排除边界：

- 单 3（`b23bb8e`）：常规 onEnd 仍走 `persistChangedBlockLayouts`；staging 分支及 pointercancel 没有补/删块的 `frame_id`，不是此最小反例的直接制造点。
- 单 5（`03f5b1e`）：blur-save 暂停受 overviewOpen 控制，普通 paragraph 布局保存不由这处暂停。
- 单 6（`aef4cb9`）：registry 立即调用原操作、返回原 Promise，不改布局；这里登记并暴露原有失败，不生成错误坐标身份。
- 单 10（`c6c057d`）：只读保存屏障针对 `item_ref`；本反例是普通 paragraph，不命中屏障。
- `BoardNewNoteDialog.tsx:75–82` 的正常 ceremony 已先建/保存笔记 frame，不能据此把 ceremony 判为“漏建帧”。本反例也保留了真实 frame。

**停线理由**：修复需要明确“缺布局块的默认帧归属”及其初始化/兼容规则，触及 v2 保存契约，不是本单四处近改的局部回退。遵照本单“F1 若根因在更深层（保存链契约）→停线举证”，未猜帧、未降级坐标契约、未绕过保存屏障，也未仅隐藏 toast。F1 仍是真 bug，未改判。

### 六条冒烟

1. **四象限普通移动无错落库：FAIL / 停线。** 有完整布局的对照四项均通过；最小反例四项均报错且未保存，证据及根因如上，不能把本条记绿。
2. **Move 手柄不开 Layout 拖入 staging：PASS。** 全页与弹窗均在浏览器真实拖入，Staging 计数从 0 到 1；生产路由写入内存库后均读回 `surface=tray, boundary_role=outside, frame_id=null, x=0,y=0,width=0,height=0`。使用有完整布局的样本；既有 placement/tray 回归包含在前端整套中通过。
3. **右缘 Staging 入口、弹窗一致：PASS。** 浏览器检查全页与弹窗，按钮在 Groups 下方相邻竖排；拖移弹窗后入口随弹窗移动。
4. **空态虚线框、无侧栏说明句、✕ 关闭：PASS。** 全页与弹窗均目视检查空框及淡图标，无可见空态句子；选中块显示小型 `Stage`，✕ 可关闭。高亮实现经代码复核且真实拖入路径可达，未另截取拖拽中间帧作视觉断言。纸面所有块移走后的既有提示不属于本单侧栏，未改动。
5. **贴纸宽、标题栏拖移与装卸区让位：PASS（隔离浏览器 + 功能测试）。** 在 1920×855 视口，弹窗实测 952×769.5；标题栏拖移从 `(484,42.75)` 至 `(784,66.75)`，关闭重开保留。打开真实 modal 的 `stagingOpen` 让位后夹取到 `(672,66.75)`、右缘 1624；关闭恢复 `(784,66.75)`。夹具切换宿主属性，未冒称整块真实 Board 装卸 UI 的端到端测试。无窗口 resize 手柄，无位置存储写入。
6. **板上 item resize 现物结论：PASS（现物核查 + 组件/仓储回归）。** `kind=item` 未被排除；选取工具下未 pin 的 member 原本就有右下角手柄，缺点是 14px 无图形且仅 hover/选中可见。本次增强可发现性。测试使用真实 `BoardPage/useBoard/repository`、合成 transport：zoom=2 下从 260×156 拖至 320×196，再触及最小 160×100，卸载重挂后回读一致。此项没有访问用户板，也未宣称完成真实板 UI 浏览器拖动。

### 验证结果

所有下列测试均按既有 suite/script 整跑，无 test-name 过滤；新增/调整的是本单交互功能回归，没有设计或新增安全类测试。运行使用已有 `.codex-tmp/v13-no-env.cjs` preload、专用空 validation env 目录与 `DB_PATH=:memory:`。

- 前端 `npm run test:unit`：82 文件、713 tests 全过；包括 placement/tray/modal/Boards。
- 服务端 `npm run test:v13-boards`：25/25，0 skipped；`npm run test:v13-item-floor`：8/8，0 skipped。
- client/server `tsc --noEmit` 与各自 `npm run build` 均退出 0。已有非阻塞诊断：客户端 chunk >500 kB，服务端 manifest 递归引用降为 any 提示。
- `verify:v2-bn8-runtime` 其余非凭据扫描步骤按脚本原顺序执行且全过：tool-face registry（5）、manifest（10 + 14 public entries freshness）、parity（10 + check）、server shared runtime import、canvas runtime boundary（159）、group gallery（8）、groups rail、single editor、source experience（两契约）、legacy shutdown、relation freshness、canvas model（60 组）、performance（5 场景）、`docs:check`、`git diff --check`。
- **未运行聚合命令本身，也不标聚合 PASS**：其中 `check:changed-file-secrets` 按本单禁令留 HQ，不能以拆分执行冒称整门通过。追加 Result 后再次执行文档检查与 whitespace 检查。
- 本地证据保留在 `.codex-tmp/w4-fix1/`（ignored scratch，不计生产 numstat）：各命令 `.log` / `.exit.txt`、隔离夹具 `start.mjs/fixture.tsx/mockApi.ts`、`f1-unplaced-before.json` / `f1-unplaced-after.json`、全页与弹窗 `f2-*-staged-readback.json`。早期 `validation-summary-initial.md` 标记需对后续源码变动复跑；其后未再改生产/测试代码，只完成隔离复现与本回执。

### 未做与停线交接

- F1 保存链契约修复未做，六冒烟第 1 条未达标；请 HQ 先确定缺布局块的帧归属/初始化与兼容规则，再下后续施工单。Henry 原始数据上的唯一根因未核验，原始用户库完全未接触。
- 未改 Layout 模式存在论、板级装卸区语义、新 event verb；未给弹窗加 resize 或位置持久化。
- 未运行凭据扫描；未读 `.env`，未外传 key，未触碰用户数据库，未做安全类测试，未 commit/push/PR/merge。开工已有 untracked 文件未改动。
- 工作树含 F2–F4 可审查结果与上述 F1 停线证据；放行与代账归 HQ，不将本单整体标成完成。

## 补遗一(HQ 裁定,2026-09-09:F1 帧归属定案,续工令)

1. **停线采纳,病根定位认可**:缺陷=默认块布局(从未被摆放过的块)不带帧身份;`requireStoredLayout` 的拒收是 v2 契约**正当执法**——⛔ 放松校验⛔ 隐藏 toast⛔ 降级坐标契约;
2. **修复裁定**:`buildDefaultBlockLayouts`(及同路径)在构建默认布局时**解析并写入该块屏显上实际归属的帧身份**——复用渲染侧同一帧解析逻辑(屏显不变量精神:存的=显示的,⛔ 另造猜帧启发式);首次拖动/保存即以完整身份落库;
3. **真无帧笔记(数据异常)**:保留现有显式报错(诚实),⛔ 拖动手势顺手建帧;Result 记边界;
4. **回归四条**:①无布局块四象限拖动全部落库且回读=屏显位置(以你的最小反例转正向);②有布局块行为不变;③无帧样本仍显式报错;④坐标契约既有测试面(4a 双模对照/placement 族)不破;
5. 渲染侧帧解析若无法在 placement 层复用(架构缝)→停线举证;完成后新 Result 追加于本补遗后,F2-F4 与本修复同批交 HQ 代账。

## Result

2026-09-09 · Codex builder 续工回执。**命中补遗一第 5 条，F1 再次停线；未改判、未修复、未放行。** 现存正文渲染链对这个最小反例同样没有解析出帧身份，因此无法按“复用渲染侧同一帧解析逻辑”完成默认布局初始化。这里的架构缝是缺少该解析行为，并非模块无法 import。工单保持 `ready`，交 HQ 裁定该缝的处理方式。

### Numstat 与范围

- 本轮生产代码及正式测试改动：`+0 / -0`。开工已有 F2–F4 的 10 文件 `+259 / -25` 原样保留，未重做。
- 本轮文档 `+47 / -0`，仅在补遗一之后追加此 Result；此前 Result 与 HQ 补遗正文未改。合成坐标取证脚本及输出位于 ignored scratch `.codex-tmp/w4-fix1-f1-resume/trace.mts`、`trace.json`，不计生产 numstat。

### 架构缝证据

以下路径均相对 `client/src/pages/Notes/canvasEngine/`，行号为本轮未修改的源码：

1. `placementService.ts:440–455` 的默认布局是 `x=0,y=cursorY,coordinate_space=page_frame_local`，无 `frame_id`；`hooks/useNoteCanvasLayoutModel.ts:129–150` 将它依次交给 normalize 与 `toStoredLayout`。
2. **正文实际使用的帧解析器**是 `layers/NoteWritingSurfaceLayer.tsx:3830` 的 `selectPlacementFrame(layout, pageFrames, contract)`。`placementContractService.ts:32–33` 对 v2、缺 `frame_id`、非 `canvas_world` 的布局返回 `undefined`。这正是默认布局的形状；渲染侧没有另一个已解析身份可供 placement 接走。
3. `layers/BlockEditorLayer.tsx:181` 将上述结果交给 `resolveScreenRect`。`placementContractService.ts:54–55,67–70` 在无解析帧时保留原 y，正文因此仍可显示。**纸上可见不等于已解析归属帧。**
4. 同路径还有一处需后续处理：`placementService.ts:389` 的 `normalizeBlockLayout` 只取 `stored?.frame_id`，不会保留默认 fallback 的身份。即使只在 `buildDefaultBlockLayouts` 加身份，现有链也会丢掉它。

已核对相邻可复用候选，没有擅自替换：

- `screenLayoutToLocal`（`placementContractService.ts:192–208`）明确服务新的 pointer/drop 矩形，调用点为 `useCanvasSurfacePointerController.ts:68` 与 `NoteWritingSurfaceLayer.tsx:3272`。它要求落点 y 在 frame **content** 区内；原最小反例首块 `y=0`、frame `inset.top=96` 不命中，仍不带身份。
- `findPageFrameForLayout`（`hooks/useRuntimeNaturalWritingController.ts:48–66`）的 primary/selected fallback 是自然写入草稿的归属政策，不是缺布局既有块的正文渲染解析。未将它搬来猜帧。
- `resolveBlockAffiliationOutline`（`blockAffiliationOutlineService.ts:29–52`）只在拖动/缩放期间对 runtime placement 作 outer 几何判断，返回类别和颜色；底层标记为 `geometry_derived_no_ownership`。它不向正文输出坐标归属帧，而且缺身份时 runtime world 的 x 与 continuous-paper 正文的 x+pageOffsetX 并非同一输入。把这个描边判断升级为保存身份无法据现有代码证明符合 HQ 所要求的同一正文解析规则，未实施。

### 合成坐标取证

亲跑生产纯函数，未启动服务或浏览器、未访问任何数据库。输入沿用上一轮最小反例的几何：frame `(0,0,904,1279)`，inset `(top=96,right=72,bottom=96,left=72)`；无布局块高 44，contentWidth=760，pageOffsetX=72，contract=v2。

| 观察项 | 实际输出 |
| --- | --- |
| 默认构建 → normalize | `(0,0,760,44)`，`page_frame_local`，无 `frame_id` |
| 正文 `selectPlacementFrame` → `resolveScreenRect` | 无解析帧；屏显 `(72,0,760,44)` |
| 现有 `screenLayoutToLocal` | 原样返回，仍无 `frame_id` |
| 仅在 fallback 附带 frame ID 后 normalize | 身份仍被丢弃 |
| 单独观察“只附加 ID、不改坐标”的屏显 | `(72,96,760,44)`，纵向跳 96；这只是合成函数输入，没有修改实现 |
| `requireStoredLayout`，真实 frame 存在 / frames 为空 | 两者均显式抛出 `A resolved page frame is required to save this coordinate contract` |

取证命令在 `server/` 执行 `node --import tsx ../.codex-tmp/w4-fix1-f1-resume/trace.mts`，沿用既有 `.codex-tmp/v13-no-env.cjs` preload，退出 0。独立子 agent 只读复核了正文链及以上候选，结论同为触发补遗一第 5 条。

### 回归、未做与停线交接

- 补遗四条：①无布局块四象限转正向 **未达成**；②有布局块未修改，本轮未重跑，保留上一 Result 的对照证据；③无帧显式拒收经上述纯函数亲跑确认，未冒称浏览器 toast 回归；④4a 双模/placement 既有套件本轮未重跑，不申报新 PASS。
- 原六冒烟：第 1 条仍停线；第 2–6 条沿用上一 Result 的已完成证据，本轮未重新施工或复验 F2–F4。
- 因在实施前停线，本轮未运行 typecheck/build/前后端回归套件或 `verify:v2-bn8-runtime`，不标聚合门通过。文档追加后 `npm.cmd run docs:check`（含既有 glossary 检查整跑）与 `git diff --check` 均退出 0；凭据扫描仍留 HQ。
- 未放松 `requireStoredLayout`、未隐藏 toast、未降级契约、未顺手建帧、未新增归属启发式；未读 `.env`、未外传 key、未触碰用户数据库、未设计或新增安全类测试、未 commit/push/PR/merge。
- 待 HQ：针对“正文当前返回未解析帧”的现物补充可执行的归属/屏显规则或指定现存解析入口；在此之前不以首帧、选中帧、drop 或描边政策代替该裁定。F2–F4 成品继续保留在工作树，F1 不宣称完成。

## 补遗二(HQ 裁定,2026-09-09:归属规则=13.2 不变量方程,续工令)

1. **架构缝采纳**:正文渲染对缺布局块确无解析帧,补遗一"复用渲染侧入口"落空——改为**立归属规则**,且不是新启发式,是 13.2 修订四已立法的**不变量方程**在本场景的应用;
2. **可执行规则**:
   a. **帧选择**=块当前屏显矩形与各页帧矩形**重叠面积最大**者;零重叠(理论罕见)→该块所在 stack 的 primary frame;
   b. **坐标**=以当时真实呈现几何做 world→frame-local 变换(减帧原点与 inset,与 4a/迁移执行器同一数学);**负 local 合法**(13.2 修订四原文——你的最小反例正解即 frame1 + local(0,−96));
   c. **验收铁律**:落籍前后屏显**逐像素相等**(⛔ 96px 跳变;跳变=变换没做只补了 ID);
3. **实施位置**:保存路径归一(拖动结束/首次保存时以真实呈现几何解析落籍)——⛔ 动 `selectPlacementFrame` 的 v2 显示语义(显示路径不变,只在保存时落籍);配套修 `normalizeBlockLayout:389` 丢弃 fallback 身份的问题(你已点名);落籍后即普通有布局块,后续走既有链;
4. **回归**=补遗一四条 + 新增"落籍零跳变"断言(合成纯函数+浏览器双证,以你的 trace 样本转正:frame(0,0,904,1279)/inset 96,块 y=0 → 存 local(0,−96) → 屏显仍 (72,0));
5. 无帧笔记仍显式报错;完成后新 Result 追加,F1-F4 同批交 HQ 代账。

## Result

2026-09-09 · Codex builder 按补遗二续工回执。**F1 已修复并完成合成回归；F2–F4 原成品保留，同批交 HQ 代账与验收。** 工程状态改为 `done`，不代表 HQ 主观放行；未 commit。

### Numstat 与实施范围

- 本轮 F1 代码与功能测试共 **7 文件，+285 / -10**；其中生产 4 文件 `+56 / -5`、测试 3 文件 `+229 / -5`。
- 开工已有 F2–F4 的 10 文件仍为 **+259 / -25**，未重做、未修改。本批代码与正式测试合计 **17 文件，+544 / -35**；文档另计：本工单 `+67 / -1`，状态索引机械同步 `+1 / -1`，整批共 **19 文件，+612 / -37**。
- 本地合成夹具、纯函数 trace、浏览器观测与验证日志位于 `.codex-tmp/w4-fix1-f1-final/`（ignored scratch，不计生产 numstat）。

| + | - | 本轮文件（`client/src/pages/Notes/canvasEngine/` 下） |
| ---: | ---: | --- |
| 48 | 1 | `placementContractService.ts` |
| 2 | 2 | `canvasObjectRepository.ts` |
| 1 | 1 | `placementService.ts` |
| 5 | 1 | `hooks/useBlockPlacementInteractions.ts` |
| 119 | 1 | `placementContractService.test.ts` |
| 68 | 2 | `coordinateContractSession.test.ts` |
| 42 | 2 | `hooks/useBlockPlacementInteractions.test.tsx` |

### F1 根因与修复

1. 原最小反例仍成立：真实页帧存在，paragraph 仅缺 canvas-object 布局，默认布局没有 `frame_id`；原保存入口拒收。新 `normalizeBlockLayoutForSave` 仅在保存边界为 v2 缺身份的正文布局落籍，`saveBlockCanvasPlacementForNote` 统一调用它；`selectPlacementFrame`、hydration 与正文显示函数均未修改。
2. **同空间最大重叠**：用现有 `resolveScreenRect` 投影块及页帧外框，再比较重叠面积；不复用旧 center-first/content 归属。页帧外框输入为 local `(-inset.left,-inset.top,width,height)`，因此连续纸面外框为 `(pageOffsetX-inset.left,frame.y,width,height)`。零重叠取该默认块所属的笔记 primary stack 的 primary frame；不取 selected frame，旧无 stack collection 沿用其 primary frame。
3. **真实呈现几何逆变换**：选择帧后，先逆连续纸面的横向投影，把呈现矩形送回该帧 world 空间，再调用既有 `requireStoredLayout/toStoredLayout` 做 world→local。对本次无身份 local 正文，化简为 `x′=x, y′=y-(frame.y+inset.top)`，与 HQ 指向的 13.2 单 4b 补遗四按标签方程一致；负 local 保留。仅给 ID 而漏变换的 96px 跳变未发生。
4. `normalizeBlockLayout` 在没有 stored layout 时采用完整 fallback，身份、surface authority 与负 local 坐标一并保留；已有 stored layout 优先，未把不同来源坐标和身份拼接。
5. **浏览器补出的第二个根因**：Layout 模式在释放时会先收拢 crossing 块。无身份 local 被 `collectCrossingBlockOnRelease` 的 `resolveWorldRect` 当成裸 world 值；弹窗 60px 拖动反例从 preview `(0,60.8072)` 被夹到 `(72,96)`，继而报 `canvas_workspace_retired`，没有发出 PUT。已加局部 guard：v2 非 world 布局尚无可解析帧时，跳过这次无合法 world 输入的收拢，把真实释放矩形交保存入口落籍。已有身份/world 输入继续原收拢链，tray 分支不变。修后同一反例成功保存 `local(0,-35)`、回显 `y=61`。

### 补遗回归与落籍零跳变双证

环境仍为 loopback 隔离夹具，使用生产 `NoteCanvasRuntime`、`BoardNoteModal`、canvas-object 路由/服务与 SQLite `:memory:`，仅合成 note/block/frame；没有访问用户笔记或数据库。四象限使用独立缺布局样本；Layout 开启项使用现有块拖动入口，不改变 Layout 的交互定义。

| 缺布局块场景 | 保存回读 local `(x,y)` | 保存后及重载/重开正文 style `(left,top)` | 结果 |
| --- | --- | --- | --- |
| 全页 × Move，Layout 关 | `(0,-26)` | `(0,70)` | 无错落库，重载矩形一致 |
| 全页 × Layout 开 | `(0,-16)` | `(0,80)` | 无错落库，重载矩形一致 |
| 弹窗 × Move，Layout 关 | `(0,-5)` | `(0,91)` | 无错落库，关闭重开矩形一致 |
| 弹窗 × Layout 开 | `(0,-35)` | `(0,61)` | 原 crossing 反例转正，关闭重开矩形一致 |

四项均有真实 `frame_id=closing-chain-source-frame`、`coordinate_space=page_frame_local`、`surface=formal_page`、`boundary_role=inside`；HTTP 内存库回读宽 760、高 42。DOM 自然内容实际高 44，呈现前后不变。

- **纯函数精确尺 PASS**：使用 HQ 原 trace `frame(0,0,904,1279)`、inset 96/72、块 `(0,0,760,44)`、pageOffsetX=72，实际生产函数得到 `frame1 + local(0,-96)`；归一前后 `resolveScreenRect` 均严格等于 `(72,0,760,44)`，hydration 后仍相等。脚本与输出：`trace.mts`、`trace.json`。
- **浏览器静止首存精确尺 PASS**：通过原控制条 `Exclude from export` 触发同一 `persistBlockLayout` 保存入口，不移动块。全页和弹窗均读回 local `(0,-96)`；保存前、保存后、真正重载/关闭重开后的 DOM rect 四字段完全相等。全页 rect=`(548.0531005859375,228.46142578125,823.893798828125,47.6990966796875)`；弹窗 rect=`(580.0442504882812,279.86627197265625,749.9115600585938,43.41595458984375)`。不同宿主的父级缩放/偏移不参与身份改写。证据是实际 DOM 几何逐字段相等及目视核对，未冒称做过整张截图像素哈希。
- **已有布局行为对照 PASS**：全页 Move/Layout 回读 local y=100/140、屏显 y=196/236；弹窗已落籍块再次 Move/Layout 回读 local y=56/97、屏显 y=152/193。既有 4a 双模、placement、tray、modal、Boards 面均在整套中运行。
- **量化边界如实单列**：拖动最后一帧到 durable 保存仍经过原 `buildLayoutPayload` 的 `Math.round`。弹窗缩放下，无布局 Move 的 style y=91.2108→91，对应 DOM y 差约 -0.20044px；已落籍 Move 对照 y=152.211→152，同样约 -0.20041px；已有布局 Layout 对照也有约 +0.46252px。此处没有宣称所有拖动 pointerup 前后都严格零差，也没有修改既有整数保存规则。新增落籍变换的精确不变量由上述纯函数与两宿主静止首存直接证明；量化原始观测一并交 HQ，不能用“没有 96px”替代精确尺。
- **无帧边界 PASS**：合成无帧样本仍显示 `Failed to save block layout`，底层明确 `A resolved page frame is required to save this coordinate contract`；读取内存库 objects/placements 均为空，仅 GET、无保存 PUT，没有顺手建帧。显式悬空 frame ID 也在功能测试中保留拒收。
- 浏览器坐标摘录为 `browser-summary.json`；各项真实库回读为 `*-readback.json`。释放夹取修复前失败与修后转正均保留，没有抹掉失败轮。

### 六冒烟与 F2–F4 同批交接

1. **四象限无错落库：PASS**，见上表与量化边界申报。
2. **Move 手柄不开 Layout 拖入 Staging：PASS**，最终代码在全页与弹窗均重跑；计数 0→1，真实回读 `surface=tray,boundary_role=outside,frame_id=null,x=y=width=height=0`。
3. **Staging 右缘入口、弹窗一致：PASS**，本轮浏览器仍见与 Groups 相邻竖排。
4. **Staging 空态/短动作/关闭：PASS**，本轮两宿主目视复核虚线空框、淡图标、无侧栏说明句，选中块出现短 `Stage`、✕ 关闭；既有 drop 与相关整套回归通过。
5. **贴纸宽、标题栏拖移、装卸区让位：沿用前 Result 的已完成浏览器与功能证据**；本轮未重做 F3，modal 全套再次运行。
6. **板上 item resize：沿用前 Result 的现物结论及组件/仓储证据**；手柄原有而难发现的 F4 成品未改，Boards 全套再次运行，不新增真实用户板操作声明。

### 验证与未做

- 最终验证使用已有 `.codex-tmp/v13-no-env.cjs` preload、`DB_PATH=:memory:`、专用空 Vite validation envDir。既有套件整跑、无 test-name 过滤；新增 10 项均是本单坐标/交互功能回归，没有设计或新增安全类测试。
- 前端最终 `npm run test:unit`：**82 文件 / 723 tests，全过**。初轮新测试有两处 TS 类型桥接问题，已修且构建/类型检查重跑；Layout release guard 后再次整套验证，旧失败日志保留。
- 服务端 `test:v13-boards` **25/25**、`test:v13-item-floor` **8/8**、`test:v13-coordinate-contract` **16/16**，均 0 skipped；client/server typecheck 与 build 通过。
- `verify:v2-bn8-runtime` 的非凭据扫描步骤按原顺序执行，含 registry/manifest/parity、runtime boundary、gallery/rail/editor/source、legacy/freshness、canvas model/performance、两端 build、docs/whitespace；**没有执行聚合命令本身，不标聚合 PASS**，`check:changed-file-secrets` 按用户禁令留 HQ。最终日志分别见 `validation/`、`validation-guard-final/`；本 Result 追加及状态索引同步后的 docs/whitespace 结果另见 `docs-check-final.log`、`diff-check-final.log` 和对应 `.exit.txt`。首次追加后发现的索引过期日志亦保留。
- 非阻塞已有诊断：React Router future flags；Vite `taskStore` 静/动态混用及 chunk >500 kB；manifest recursive reference 降为 any。
- 无待裁架构缝、未改判或主观放行；未改显示语义、Layout 存在论、板级装卸区、新 event verb；未读 `.env`、未外传 key、未触碰用户库、未执行凭据扫描、未 commit/push/PR/merge。开工已有未跟踪文件保留。F1–F4 工作树与上述量化观测一起交 HQ 代账。
