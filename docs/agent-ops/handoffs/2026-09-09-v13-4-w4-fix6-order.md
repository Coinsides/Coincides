> **状态 (Status)**: ready
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④第五批(修五后直接拖块仍报 Failed to save block layout;纸顶灰线与页面错位);修五 lesion B 预案兑现
> **单号**: 13.4 走查修六 · F11-B 陈旧 frame_id 重落籍 + F12 页框边线对齐

# 走查修六 · F11-B + F12

## F11-B · 陈旧/死 frame_id 的保存期重落籍

**症状**:修五(写时自愈)落地后,Henry 真机直接拖块**仍**报 "Failed to save block layout";绕 Staging 一趟仍能逐块治愈。

**根因假设(HQ,builder 须先合成复现红,⛔碰真库)**:笔记**有**持久化页框集合,但块的存量布局(或据其生成的拖拽草稿)携带的 `frame_id` 指向**集合里已不存在的帧**(历史上集合被替换/重建)。`normalizeBlockLayoutForSave` 的早退分支(frame_id 为真)→ `requireStoredLayout` → frames 里找不到该 id → 抛 "A resolved page frame is required..."。修五只治"没集合",不治"错帧章"。旁证严丝合缝:tray-out 会给块盖**现行有效**的帧章,故绕道逐块治愈;渲染端 `selectPlacementFrame` 找不到帧时按 frameless 渲染,所以屏上一切正常、保存必炸。

**修法(HQ 已裁,⛔复议)**:

1. 保存边界(v2):`layout.frame_id` 不在现行 frames 中 → **剥掉死 id,走既有几何落籍路径**(最大重叠 → primary 兜底 → **新增:首帧最后兜底**——primary/stack 指针悬空时不再无帧可落);修正后的 frame_id 随本次保存写回=逐块永久治愈;
2. **零跳变铁律**:渲染端对死 id 本就按 frameless 处理,故落籍以当前屏幕矩形重算即天然零跳变——逐像素断言之;
3. ⛔ 对**有效** frame_id 的布局零行为变化(payload 逐字节不变);⛔ 松动 v2 契约(落籍后仍无帧=照抛;修五自愈在前,本修在后,两级串联);
4. primary/stack 悬空兜底加固随本条,⛔ 另开分支体系。

**冒烟五条**:①合成"集合在+块布局挂死 frame_id"笔记→修前拖块断言红(现状)→修后同路径绿,frame_id 重写为现行帧,零跳变逐像素;②健康笔记(有效 frame_id)保存 payload 逐字节不变;③frameless 块落籍回归(修一路径)不破;④修五半成品自愈回归不破(两级串联次序验证:先自愈铸集合,再重落籍);⑤tray 往返回归不破。

## F12 · 页框边线与页面错位

**症状(Henry 截图,全页 NoteDetail)**:纸顶部淡灰线(页框边界)与页面内容整体不对齐(起点横向偏移/观感歪斜)。

**侦察线索(HQ 静态,builder 确权)**:`NoteWritingSurfaceLayer` 的 `formalPageBoundary` 定位直接用 `pageFrame.x/y`(世界账);块屏幕位 = `layout.x + pageOffsetX`(偏移账)。两账对齐的前提=pageOffsetX 与帧内容原点同源;若 pageOffsetX 独立推导(如居中计算)而集合中 frame.x 来自另一来源(仪式端点/种子/历史),即错位。修五自愈铸帧=渲染同源不背锅;历史集合可能背。

**要求**:builder 合成"frame.x 与渲染偏移不一致"的集合先量化复现错位(修前红),再裁修法申报——**对齐规则只能有一本账**(边线与块共用同一坐标推导);⛔ 硬调像素;若勘定与 F11-B 同根(帧几何账 vs 偏移账),申报合并修。

**冒烟四条**:①错位集合修前红(可量化)→修后边线与块列逐像素对齐;②正常集合回归;③打印/统揽的页边渲染不破;④Fit width/缩放各档位对齐保持。

## 纪律

- typecheck + build 全绿;定向回归(canvasEngine+Notes+tray)不破;既有全库整跑⛔过滤;
- **⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触(Henry 真机取证=HQ 另收,⛔阻塞本单);⛔ 安全类测试(凭据扫描留 HQ);
- `## Result`:numstat + 两组冒烟逐条 + 未做 + 停线;复现推翻假设→停线举证⛔改判。

## Result

> **From**: codex(builder) · **日期**: 2026-09-09
> **结论**: **STOP / needs HQ**。保留未暂存实现与红断言；不翻 `done`，不宣称本单完成或总门全绿。

### 停线证据（优先读）

F11-B 的保存早退假设已复现成立，但「仅在保存边界重落籍即可保证整条拖拽零跳变」的覆盖范围被另一种存量标签推翻：**死 `frame_id` + `coordinate_space: canvas_world` + Layout 组织模式**。

- 内存合成 frame：`(x=0, y=0, width=904, height=1279)`，inset `left=72, top=96`；块 `frame_id='retired-frame'`，width=760、height=44，displayScale=0.987。
- `pointermove(0,60)` 后屏上草稿：`x=0, y=60.79027355623101`。
- `pointerup` 后实际变成：`x=72, y=96`。位移是 `(+72,+35.20972644376899)` 布局单位，**发生在保存重落籍之前**。
- 原因：`useBlockPlacementInteractions.ts` 的 `collectCrossingBlockOnRelease` 仅对 `coordinate_space !== 'canvas_world' && !frame` 早退；该样本继续走 `clampCrossingPlacementIntoPageFrameContent`。
- 现物：`useBlockPlacementInteractions.test.tsx` 保留同一拖拽冒烟的 `retired-frame / canvas_world` 参数与零跳变断言。整文件 **1 FAIL / 20 PASS**；最终 client 整库 **1 FAIL / 756 PASS（757 条、85 文件）**。没有 skip、todo 或按测试名过滤。
- **遵令停线**：未自行扩改该 hook 的生产收集规则，未把有跳变的结果改判为通过。请 HQ 裁定这条存量标签的释放收集规则后续工。

### 已落代码（未提交）

- F11-B：v2 保存时只将不存在的显式 frame ID 从落籍输入剥离；从现有屏幕矩形走最大重叠 → primary → 首帧兜底。修五铸集合依然在前；没有 frame 仍抛原契约错误。有效 ID 继续原 `requireStoredLayout` 路径。
- F12：全页 NoteDetail 的灰线实际为 `top-ruler`；`formalPageBoundary` 仅 Canvas 模式挂载。核心「世界账 vs 页面偏移账」假设成立。全页 guides 通过 `projectPageFrameToReadingSurface` 调用块使用的 `resolveScreenRect`，不硬调像素、不修改存量集合、布局或块投影。
- 两者独立：F12 用有效 ID 也能复现，未合并根因。
- Canvas 扩查另量得 guides / outer boundary 相对块列 `+56px`。其移动预览与世界命中仍用 frame.x；直接套全页修法会破坏拖动语义。因此只落实工单截图对应的全页修复，Canvas 的移动/命中协议留 HQ，**未宣称 Canvas 错位已修**。

### F11-B 五条冒烟

1. **集合在 + 死 ID：红 → 绿（page_frame_local）**。修前实际浏览器拖拽出现 `Failed to save block layout`，没有 block-placement PUT；修前两功能文件共 **5 FAIL / 39 PASS**，错误为 `A resolved page frame is required to save this coordinate contract`。修后同路径写 `primary-page-frame`，零 collection PUT、布局 PUT 成功、无该错误 toast。暂停布局回包后比较前后两个块及文字的 `getBoundingClientRect`，全部逐字段/JSON 完全相等；第一块矩形均为 `(543.0531005859375,339.4314270019531,823.893798828125,47.699127197265625)`。**canvas_world 拖拽变体为上述 STOP，不并入此绿结论。**
2. **有效 frame ID 字节不变：PASS**。两个有效帧、local/world 标签、负值与小数坐标，对比原 `requireStoredLayout` → `buildLayoutPayload` 的 JSON 字节。服务级 stale local/world/无标签保存前后屏幕矩形相等，输入不变、回包 hydration/二次保存幂等；这里的 world 绿仅限保存边界，不含松手收集。
3. **frameless 回归：PASS**。原首次落籍、不同世界原点/inset、最大重叠、primary stack 优先、无集合继续抛错均通过；新增 primary/stack 悬空时首帧兜底并断言屏幕矩形不变。
4. **修五两级串联：PASS**。adapter 的旧 frameless 与新 stale 参数均证明：集合 PUT 未完成前没有 placement PUT；完成后才发带有效 ID 的布局。浏览器 `?stale=1&missing=1&hold=1&holdLayout=1` 也实际确认顺序、恰好一次铸集合、回包前后所有块/文字矩形相等。
5. **tray 往返：PASS（既有完整功能文件）**。`useTrayRelocation`、`NoteTrayInteraction.integration`、`useTrayController`、`trayService` 与 adapter 五文件完整跑 **79/79 PASS**；最终 client 全量这些条目仍绿。未把测试替身结果冒称本轮真实鼠标 tray 往返。

### F12 四条冒烟

1. **历史错位集合红 → 绿**。最终 v2 fixture：`frame.x=80, inset.left=72, pageOffsetX=0`，修前 top-ruler 比块列右偏 **152 CSS px**；`x=-320, inset.left=54` 时左偏 **266px**。最终 fixture 临时恢复修前导线逻辑实跑 **6 FAIL / 3 PASS**，恢复修法后同文件 **9/9 PASS**。浏览器 `?healthy=1&misaligned=1` 修后实际左右边差均 **0px**，没有只靠公式复算。
2. **正常集合：PASS**。本来 `frame.x=pageOffsetX-inset.left` 的集合不变；非默认 y/inset 也对齐；持久化 frames JSON 完全不变。
3. **打印/统揽：PASS**。真实 runtime fragments 的只读页投影，页尺寸仍 `904×1278`，fragment 左起点 `72px`、宽 `760px`，内部块 left=0；beforeprint/afterprint 挂载清理通过。未触发真实打印机。
4. **Fit width/缩放：PASS**。DOM 输入断言覆盖 fit_width、fit_page、physical 各 16 档（0.5–2.0）与可用宽 480/904/1366。浏览器实际 scale `1.084070796460177 / 0.8779875966831581 / 0.7901888370148423 / 0.44549955985915496`，ruler 与块左右边差均为 0；第一档同为 x=`543.0531005859375`、width=`823.893798828125`。

### 验证命令与总门

命令经既有 `scripts/run-isolated-coordinate-validation.mjs` 执行，Vite 指向临时空 envDir；两组复现只用前端内存 API/合成数据，没有运行用户后端或接触用户数据库。

- `--cwd client -- npm run test:unit`：修复后首次 **756/756 PASS**；加入停线反例后最终 **756 PASS / 1 FAIL**，上述唯一红保留。
- `--cwd client -- npm run build`、`--cwd server -- npm run build`：**PASS**（包含各自 TypeScript 编译）。fixture 独立 `tsc -p scripts/pageFrameHealingSmoke/tsconfig.json`：最终 **PASS**。
- `--cwd server -- npm run test:v2`：原脚本及 pretest hooks **完整执行，318 条 / 291 PASS / 27 FAIL / 0 skipped**，exit 1。没有删选既有测试。其中 25 条 canvas 失败与[修三工单已记录的退休面旧 fixture](2026-09-09-v13-4-w4-fix3-order.md)同组；本轮未另跑改前 server 基线。另两项为 MineruWiring 文件与 c-1b-2 table geometry，运行依赖仅是候选解释，未确证本轮详细原因；未越界修 server。`server/`、`shared/` 无本轮 diff。该既有套件含临时测试库和临时空 env fixture，不能称它全内存；没有打开真实 .env 或用户库。
- runtime gate 中 registry/manifest/parity 的既有完整测试与检查、server shared import、canvas runtime boundary、gallery/rail/editor shell、source experience、legacy shutdown、relation freshness、canvas model contract、performance 均 **PASS**。
- `docs:check` **FAIL**：`docs/agent-ops/INDEX.md` 已过期；没有改它。单独 `docs-inventory --check` 与 glossary 检查 **PASS**。`git diff --check` **PASS**。
- **没有直接执行 `npm run verify:v2-bn8-runtime` 整条串联，也不报总门通过**：末端 `check:changed-file-secrets` 与本单「凭据扫描留 HQ」冲突。上列为逐项运行的允许部分，凭据扫描未执行；同时存在已列出的功能/既有套件/docs 红。

### numstat

本轮代码、测试、fixture 合计 **+329 / -22**（含两个未跟踪新文件的完整行数；未用 stage 取得统计）。本工单 append-only 回执 **+72 / -0**，合计 **+401 / -22**；其它开工时已有未跟踪文件未触碰。

```text
15   6  client/scripts/pageFrameHealingSmoke/fixture.tsx
12   3  client/scripts/pageFrameHealingSmoke/mockApi.ts
7    3  client/src/pages/Notes/canvasEngine/hooks/useBlockPlacementInteractions.test.tsx
4    3  client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.test.tsx
5    2  client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx
45   2  client/src/pages/Notes/canvasEngine/placementContractService.test.ts
7    3  client/src/pages/Notes/canvasEngine/placementContractService.ts
17   0  client/src/pages/Notes/canvasEngine/pageFramePresentationService.ts (new)
217  0  client/src/pages/Notes/canvasEngine/layers/pageFrameAlignment.test.tsx (new)
```

### 未做与纪律

- **未 stage / commit / push / PR / merge**；未改 agent 权限/操作指令。
- 未打开真实 `.env`、读取或输出 key 值；未接触用户数据库。Henry 真机取证留 HQ，不作为本轮阻塞理由。
- 未设计、新增、派发安全类测试或做凭据扫描；仅按授权完整运行既有套件与几何/布局功能冒烟。
- **未修停线反例、未改 Canvas frame 移动/命中协议、未修 server 历史/依赖问题与 docs 索引**，也未以删断言、过滤套件换绿。停线发生后只继续收取完整验证结果、校验 diff 与写回本回执。

## HQ 复核(2026-09-09):停线收货,裁释放收集规则,续建

1. **F11-B/F12 主体收货**(红转绿证据充分,冒烟齐);停线反例收货——网密恰好兜住第三种存量组合,正是纪律的价值;
2. **补遗二(裁定,入本单射程)**:释放收集器(`collectCrossingBlockOnRelease`)的**帧权威判定必须与保存边界同源**——`frame_id` 在现行集合解析不到帧=**无帧**,走无帧早退(⛔对死帧执行 `clampCrossingPlacementIntoPageFrameContent`),块留在松手处零跳变;随后保存边界剥死 id 重落籍(F11-B 主修)。⛔ 改动有效 frame_id 的收编语义(clamp 照旧);首存后 frame_id 转正,后续释放自动恢复正常收编——不对称一次自愈,可接受;
3. 补遗二落地后:停线红断言应转绿,client 全库应 757/757;追加冒烟:死 id+canvas_world+Layout 拖拽松手→零跳变→保存绿→frame_id 转正→**再拖一次**走正常收编语义;
4. **Canvas 面 guides/outer boundary +56px**:该面已退役(CANVas_MODE_RETIRED,产品不可达)——⛔本单修;记 13.6 休眠清册(死代码带已知错位,删或复活随册裁);
5. **server test:v2 两新红(MineruWiring/c-1b-2 table geometry)**:本轮 server/shared 零 diff,非本单所致;判环境/陈旧候查,记 13.6 总测清单立基线;25 条 canvas 红=修三 STOP-1 已裁同组,维持;
6. docs:check 的 INDEX 过期=HQ 工单文件所致,HQ 收口时 `node scripts/docs-index.mjs` 重生成。

**裁定:按补遗二续建,做完追加 Result 二。**
