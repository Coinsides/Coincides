> **状态 (Status)**: ready
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-11
> **单号**: 13.5 · 波次 D1 · 可调的墙(页边距直接操纵)+把手安家
> **上游**: 09-11 墙案对谈(Henry 拍板,`plans/v13-5-layout-credentials-plan.md` 专场补遗「已拍两条+候认三条」全数生效);三路勘察 workflow `ruler-groundwork-survey`(file:line 证据在案)

# D1 · 可调的墙 + 把手安家

## 零 · 裁定原文(约束一切实施细节)

1. **做墙不做标尺**:代理控件只配给抓不到的东西——控件只给墙(contentInset),⛔任何标尺条/每块标尺 UI;
2. **撞墙约束**:auto 块=长到墙为止,manual 块=自有盒但出不了墙;墙=一切 page_frame_local 块的硬极限;
3. **跟墙走语义,零坐标改写**:page_frame_local 原点=内容区原点,墙动=原点动——全体块存储坐标**不改写**而视觉整体跟随;⛔任何 rebase;唯一例外=墙内收后违反约束的 manual 块,同事务 clamp(只写违例块);
4. **横移语义维持现状**:auto 块横拖压墙变窄(F13 `constrainFrameLocalAutoWidth` 行为),⛔改;
5. **墙 v1=note 级**:一次调整统一写该 note 全部 page frame 的 contentInset(left/right;top/bottom 本单⛔动);
6. 媒体块同受墙 clamp;墨水(freehand)按现契约可越入边距区,⛔动。

## 一 · 交付面

### 1. 墙的直接操纵

- 纸面(page 模式)上,鼠标靠近左/右边距线时线显影(hover 显影,与"安静纸"三态同气质:平时零可见),可独立拖动;拖动中实时预览(auto 块宽度随动重排);
- 松手=提交:写该 note **全部** page frame 的 `contentInset.left/right`(持久化现物:`page_frame_extensions.content_inset_json`;写门走既有 frame 更新路由——若现役路由不收 inset 字段,在**既有人类路由**上最小扩面,⛔新开专用路由);
- 夹取:每侧 inset ∈ [24, 240] 且内容区宽(span)≥ 320;新建页继承 note 现行 inset(勘察显示新帧从 preset 取默认——改为优先取 note 现役帧的 inset,无帧才回落 preset);
- 参考线现物 `pageFrameGuideService` 的 left_margin/right_margin 从 inset 派生,应自动跟随——验证,若有硬编码假设一并修。

### 2. 同事务 clamp(唯一写坐标的场合)

- 提交墙调整时,对 page_frame_local 且 width_mode=manual 的块检查:`x + width > span'` 则 clamp x(`x = max(0, span' - width)`),仍超则 clamp width(`width = span'`);auto 块零写(宽度本就派生);
- clamp 与 inset 写入同一保存批次,撤销时一并恢复(见 §3);
- ⛔动 `isCanvasWorkspaceBlock`/退役闸的判定**语义**——但其消费的内容区宽必须与新 span 一致取值,逐处核对并在 Result 申报核对清单。

### 3. 撤销

- 墙调整作为一条撤销条目进**既有纸面撤销栈**(与块位移同栈;实施时申报接的是哪套机器);undo=恢复旧 inset+被 clamp 块的旧 layout,redo 对称;
- 机械闸法源:撤销覆盖不到的操作将来 Agent 不许有——本条不可裁剪。

### 4. 760 常量解耦(auto 宽长到墙)

- `constrainFrameLocalAutoWidth`(placementService.ts:357-373)及 auto 宽四处复用点(placementService.ts:465/:495、pageFrameService.ts:29、placementContractService.ts:316,勘察在案)中,`DEFAULT_PAGE_CONTENT_WIDTH` 封顶改为**逐帧派生**(`frame.width - inset.left - inset.right`),帧不可解析时才回落常量(workspace/tray/v1 兜底路径维持现状);
- 验收点:墙拓宽后 auto 块能长过 760 直到新墙;墙内收后 auto 块随收。F13 既有测试(placementAutoWidth.test.ts、autoWidthFrameSave.test.tsx)全数保绿并按新语义扩例(拓宽/内收两向)。

### 5. 把手安家(边距车道)

- 文本单元把手(`TextUnitGutterLayer` 按钮)移入左边距车道:块框外、纸内,不与块边框重叠不超块框;尺寸缩小(Henry 09-11 点名);
- 显隐三态维持现役(不点=安静、hover=现、选中=现),⛔加噪;拖拽/菜单行为零变。

## 二 · 禁区

⛔TextFlow-Contract 面(本单全在 Canvas/placement 域,零 amendment);⛔标尺 UI;⛔rebase 存量坐标;⛔动 top/bottom inset;⛔动漂移判定/三态视觉;⛔数据迁移(存量 content_inset_json 行已在);⛔安全类测试(测试面=功能/数据/回归);⛔碰 .git(工作树留存,commit 归 HQ);⛔改 CLAUDE.md/AGENTS.md/权限配置。

## 三 · 验收

- typecheck + build 绿;client 定向套件(placement/autoWidth/interactions/coordinateContractIntegration + 新增墙测试)绿;`canvasEngineModelContractCheck` 绿;
- 冒烟(合成/内存,⛔真库):①拖右墙内收→auto 块变窄;拖宽→auto 块长过 760 到新墙;②manual 块贴右缘,左墙外扩→存储坐标零变(读 placement 断言)而视觉跟随;③右墙内收越过 manual 块→x/width clamp 且 undo 恢复 inset+块 layout,redo 对称;④新建页继承 note 现役 inset;⑤打印/overview 投影回归(预期零改动);⑥把手住边距车道不超块框;
- 冒烟证据(JSON/截图)落 `docs/audits/2026-09-11-d1-builder/`。

## 四 · 申报义务

Result 必含:交付文件清单+diff 规模;§2 内容区宽消费点核对清单;§3 撤销接线申报;冒烟证据路径;测试数字;若发现工单与现物冲突,**停线申报⛔自作主张**(补遗机制在)。

## Result

2026-09-11 · codex builder · **STOPPED / 工单与现物冲突，未完成 D1**。按 §四及 Henry 本轮指令停线，header 保留 `ready`，未翻 `done`。产品代码、原有测试、契约、权限配置均零修改；未写 .git、未 commit / push / PR。现有未跟踪文件保留。下列交付仅为停线证据与回执，不能用于放行。

### 1. 冲突及复现

**§零.3 / §一.2 的 auto 零写，与 §一.2 禁止更改现役归属判定语义，不能在当前加载链同时满足墙内收要求。**

- `placementService.ts:324–346` 的 `isCanvasWorkspaceBlock` 用 `stored.x/stored.width` 判定；它的 v2 内容区右界在 `:344` **已经**是当前 `frame.width - left - right`。所以这里不是漏换常量或漏传新 span。
- `canvasObjectRepository.ts:67–94` 加载行，经 `reconcileHydratedBlockLayoutSurfaceAuthority` → `placementContractService.ts:98–115` 的 `toStoredLayout` 按旧存储宽重新分类。`shared/types/canvasSurfaceAuthority.ts:80–95` 将 crossing 判为 workspace。
- 页面真实 `useNoteCanvasResolvedLayoutModel` 在 `hooks/useNoteCanvasLayoutModel.ts:111–150` 为该行选 affiliated-workspace 路径。`placementService.ts:365` 的 auto 约束跳过 workspace，最终仍用旧宽。
- 现有 `boundaryAccount.test.tsx:68–78` 明确要求旧宽快照越过当前墙时判 workspace（未指定 `width_mode`，即 auto 默认）。把分类输入改成派生 auto 宽会改变这一既有判定结果；本轮未自行将它解释为获准的“内容区宽消费点修正”。

合成实跑：904 宽 A4 帧，left=72，right 从 72 改为 232，span 从 760 变为 600；合法 auto 行 `x=0,width=760` 的存储输入字节完全不变。真实 hydrate 后为 `canvas_workspace/crossing`，真实页面 hook 最终 `width=760`，超出新墙。对照：合法 manual 500→500、旧窄 auto 500→600、拓宽到 span=808 后 auto 仍受现役 760 封顶。四例分类与布局见 [wall-width-conflict.json](../../audits/2026-09-11-d1-builder/wall-width-conflict.json)，合成输入见同目录 `wall-width-conflict.test.ts`。

**需 HQ 补遗后才能继续**：明确 auto 在归属分类/加载链使用何种宽度，以及这项输入语义变更是否属于本单授权。builder 未改判定、未写 auto 存量宽、未绕退役闸、未改测试期待值来消除冲突。

另有一项勘察描述与现物差异：插页 `pageFrameCollectionService.ts:161–175`、追加页 `pageStackCollectionService.ts:218–232` 已通过 `...source` 继承 inset；无帧 seed 才回落 preset（`engineModel.ts:87–100`）。`pageFramePrintScaleService.ts:98–105` 对缺失 pageSize 的历史帧仍会重套 preset。未为已具备的主路径自造修改，也未声称所有历史路径已经动态验证。

### 2. 内容区宽消费点核对清单

以下 client 文件均相对 `client/src/pages/Notes/canvasEngine/`；均为本轮只读核对，未施工。

| 消费点 | 现物 / 核对结论 |
|---|---|
| `placementService.ts:357–373` `constrainFrameLocalAutoWidth` | 已算逐帧剩余宽，但仍 `min(760, contentWidth, remainingWidth)`；workspace/tray 排除仍在。auto 拓宽/内收需施工，受上述冲突阻断。 |
| `placementService.ts:465/:495` resolved/default auto 宽 | 两处仍有 760 封顶；未改。 |
| `pageFrameService.ts:29` draft v1 fallback | 仍有 760 封顶；该分支属于 v1 fallback，不能无条件改掉。 |
| `placementContractService.ts:303–316` v2 draft | 同时取 760、contentWidth、逐帧 span 的最小值；仍需按工单解耦。 |
| `isCanvasWorkspaceBlock` `placementService.ts:324–346` | 右界已用实时逐帧 span；box 仍用 stored.width。冲突直接消费点，判定语义零改。 |
| `modePolicyService.ts:63/:119` | 两个生产调用均向 `isCanvasWorkspaceBlock` 传当前 pageFrames；`:70–76` affiliation 仍用旧存储盒。 |
| `hooks/useNoteCanvasLayoutModel.ts:103–150` | 可见性/affiliation 决定后续归一化模式；已用真实 hook 动态验证冲突路径。 |
| `placementContractService.ts:87–115`、`canvasObjectRepository.ts:67–94` | hydrate 分类边界用当前 frame span；旧存储宽会提前产生 workspace 标签，不能只修渲染末端。 |
| `placementService.ts:540/:657` runtime/payload、`:510/:722/:729` boundary/export/AI | fallback 仍有 760；正常 v2 前序 `toStoredLayout` 提供的当前 frame receipt 优先。源码核对，未扩大 fallback 修改范围。 |
| client 退役闸 `canvasRetirementPolicy.ts:18–37`、`canvasObjectRepository.ts:124–141` | 闸本身只消费 surface/boundary 标签，不独立算宽；标签来自上游分类。未改闸，未新增或执行安全类测试。 |
| server `validators/index.ts:750–752`、`services/canvasObjects.ts:440–481` | 校验/保存消费 surface/boundary，未独立按新 span 派生 auto 宽；`canvasPlacementLayout.ts:8–27` 读投影仍返回存储宽。未改判定。 |
| `pageFrameService.ts:85–91`、`pageFrameGuideService.ts:33–70` | content rect 与 left/right guide 已直接从 inset 派生。源码核对通过，拖墙视觉验证未执行。 |
| `hooks/useCanvasContentWidth.ts:19–47`、`hooks/useRuntimeLayoutModelController.ts:46–69` | contentWidth 来自容器测量，pageFrames 另行传入布局；不能将容器宽冒充逐帧 span。 |
| `placementContractService.ts:47–70`、`pageFramePresentationService.ts:5–16` | world 原点含 inset；纸面 screen x 仍固定为 `layout.x + pageOffsetX`。跟墙走的纸面投影尚待施工，未 rebase。 |
| `pageFramePrintScaleService.ts:56–70/:88–105`、`layers/NoteOverviewLayer.tsx:52` | 打印物理尺度取 frame.width，overview 取 runtime.pageFrames；源码零改，打印/overview 动态回归未执行。 |
| `exportPreviewService.ts:134/:178`、`pageStackBlockFragmentService.ts:68` | preview 内容框/affiliation 及 fragment clipping 使用当前 frame/content rect。 |
| `pagePrintProjectionService.ts:24`、`layers/NoteOverviewLayer.tsx:140`、`layers/NoteReadOnlyPageContent.tsx:43` | print/overview 共用 world fragment 投影；投影本身未消费 760，仍须后续动态回归。 |

### 3. 保存与撤销接线申报

**未接线，墙操作入栈数 = 0。** 已核实应复用的纸面机器为 `usePlacementHistory`：`useRuntimeBlockHistoryController.ts:46–56` 调用，`usePlacementHistory.ts:108–124` 的 `pushHistoryEntry` 与块位移 `pushLayoutHistory` 共用 `scope.undo`。`historyService.ts:6/:71/:99` 已有 async `reversibleEdit`；`:76–88` 的历史串行队列、`:142–166` 成功后才移栈的逻辑位于 `usePlacementHistory.ts`。本轮未扩 TextFlow-Contract，也未另建撤销栈。

既有人类 `PUT /canvas-objects/by-note/:noteId/page-frame-collection`（server `routes/canvasObjects.ts:45–57`）已经收 collection/inset；server `services/canvasObjects.ts:1802/:1935` 已在事务中写 `content_inset_json`。但块 layout 目前走另一请求/保存入口，collection 没有 clamp 批次参数。正常提交、undo、redo 共事务仍需在既有路由最小扩面后实施；**未用顺序两次请求冒充原子保存**。

### 4. 证据、测试数字及未验收范围

证据根目录：`docs/audits/2026-09-11-d1-builder/`。仅合成行、内存加载与 jsdom hook；零应用 API 调用、零真库访问、零数据迁移，截图数 = 0。

| 测试 | 最终结果 |
|---|---:|
| `boundaryAccount.test.tsx` | 14/14 |
| `placementAutoWidth.test.ts` | 12/12 |
| `hooks/autoWidthFrameSave.test.tsx` | 3/3 |
| 审计 `wall-width-conflict.test.ts` | 4/4 |
| 合计 | **4 文件，33/33，失败 0** |

**这些绿灯证明旧行为及冲突可复现，不证明 D1 功能通过。** 首轮 33 例中 32 过、1 失败，并发生审计 JSON URL 写出失败：当时直接调用 `normalizeResolvedBlockLayout(surfaceMode='page')` 实得 600，否定了探针预想的 760。已改用真实页面 hook 验证其实际分路，保留首轮 `width-test-results-attempt-1.json`；最终 JSON 同时记录 direct=600 与 live hook=760，未混淆两种调用口径。

回放命令（工作目录为仓库的 `client/`）：`node node_modules/vitest/vitest.mjs run --config ../docs/audits/2026-09-11-d1-builder/wall-width-conflict.config.mjs`。

工单六项冒烟 **0/6 完整验收**：①仅完成加载链冲突诊断，未实现拖墙；②左墙外扩视觉/零写交互未跑；③clamp/undo/redo 未接；④新页继承仅源码核对；⑤打印/overview 仅源码核对；⑥把手车道尚未施工。typecheck、build、完整 client 定向组合、`canvasEngineModelContractCheck`、`npm run verify:v2-bn8-runtime` 均因按冲突停线而未跑，**不作通过、不作豁免**。

### 5. 交付文件与 diff 规模

产品/原测试 diff：**0 文件，+0/−0**。本轮只追加本工单 Result，以及新增审计证据；原工单 §零–§四未改。最终回读：**7 文件，+568/−0 行**（回执 +83，新增审计 6 文件 +485，含缓存；审计合计 43,334 字节）。新增文件单独枚举计数，未将 `git diff` 不显示的未跟踪文件遗漏。

| 文件（审计文件相对上述证据根目录） | 本轮规模 |
|---|---:|
| 本工单 `2026-09-11-v13-5-d1-walls-order.md` | +83/−0 行 |
| `wall-width-conflict.test.ts` | 新增 116 行 / 6,442 字节 |
| `wall-width-conflict.config.mjs` | 新增 23 行 / 886 字节 |
| `wall-width-conflict.json` | 新增 343 行 / 9,255 字节 |
| `width-test-results.json` | 新增 1 行 / 12,586 字节 |
| `width-test-results-attempt-1.json` | 新增 1 行 / 13,690 字节 |
| `.vite/vitest/da39a3ee5e6b4b0d3255bfef95601890afd80709/results.json` | 新增 1 行 / 475 字节 |

审计缓存清理曾被自动审批策略拒绝（工具仅返回 `blocked by policy`），故原位保留并如上申报；未尝试替代删除通道。补遗前保持停线，工作树交 HQ 复核/代账。

## 补遗一(HQ 裁定,2026-09-11;解除停线,续按本单+本补遗施工)

停线成立,举证采信(四例合成对照 + 真实 hook 分路复现,`wall-width-conflict.json` 在案)。裁定如下:

### A · auto 块的边界/归属分类改消费派生盒(本单授权,核心裁定)

- **法理**:auto 块的存储宽**按设计不是真相**(归一化本就忽略之,勘察 placementService.ts:398-411 在案);分类器消费非真相输入=幻影病。F13 已立"auto 派生不得发明溢出",本补遗把同一条律**补齐到分类器官**:auto 的陈旧快照不得发明 crossing。
- **实施**:page_frame_local 且 width_mode≠manual 的块,hydrate 分类(`toStoredLayout`/`canvasSurfaceAuthority` 边界判)与 `isCanvasWorkspaceBlock`/affiliation(modePolicyService)消费**派生宽**(即 F13 约束后的 auto 宽);**stored.x 仍为两种模式共同的真相输入**——x 真越界照判 workspace。manual 块零变(存储即真相)。
- **⛔拔牙**:同景况 width_mode=manual 的陈旧越界块**必须仍判 workspace/crossing**——`boundaryAccount.test.tsx` 的 auto 期待按新语义改写,并**新增 manual 阳性对照例**(闸保牙证据,Result 申报)。
- 零写不变式维持:本裁定纯读侧,存储字节零触碰。

### B · clamp 批次与 inset 写同一事务(既有路由最小扩面)

- `PUT /canvas-objects/by-note/:noteId/page-frame-collection`(routes/canvasObjects.ts:45-57)最小扩面:可选携带 clamp 批次(违例 manual 块的 layout 更新),服务端与 `content_inset_json` 写**同一事务**落库;块 layout 校验复用既有 placement validators/归一化,零新语义;⛔顺序双请求冒充原子(你们拒得对)。
- 撤销接线采信你们的勘定:`usePlacementHistory`(pushHistoryEntry/reversibleEdit),一条目=旧 insets+被 clamp 块旧 layout,undo/redo 对称。

### C · 打印刻度服务历史帧路径(顺手加固)

- `pageFramePrintScaleService.ts:98-105` 对缺 pageSize 的历史帧**⛔用 preset 覆盖既有 inset**——只补缺失字段,既有 content_inset 保留;若现逻辑会覆盖,本单内修,Result 申报。

### D · 纸面投影原点从活 inset 派生

- 纸面 screen 投影(pageFramePresentationService 一族)内容原点从帧的**现役 inset** 派生,拖墙实时重排即靠此(§一.1 已授权面,此处明确确认)。

### E · 勘察修正收账

- 插页/追加页继承 inset 已是现物(`...source` spread)——采信,⛔自造修改;seed 无帧回落 preset 维持。
- 首轮探针 direct=600 vs live hook=760 双口径分开记录的做法记为正面先例。
- 审计缓存(.vite)由 HQ 代账时清理,builder 免责。

### 续工范围

按原单 §一 全项继续(拖墙 UI/同事务 clamp/撤销/760 解耦/把手安家)+ 本补遗 A-D;验收=原单 §三全项+A 的 manual 阳性对照;完整定向套件+typecheck+build+contractCheck 此轮必跑。
