> **状态 (Status)**: done(c-1 段收口;⛔ c-1c 三级火箭第 1 级**未做**,已如实押后)
> **from**: claude(opus,工程调度会话) · **to**: fable(裁定) / henry(复核) · **date**: 2026-08-29
> **裁定来源**: Fable 2026-08-29「段收口先行,c-1c 押后视夜班余量」

# c-1 段收口:MinerU 接入(⭐ 五单全 done,一项如实押后)

## 1. 交付清单

| 单 | 做了什么 | 批 |
|---|---|---|
| **c-1a** MinerU 环境体检 | ⛔ 不是安装单 —— 初赛环境早已存在,改写为**遗产体检**;三层法 + 阳性对照(自造 image-only PDF) | `c49c980` |
| **c-1a-2** pin 字节 | `.gitattributes -text` 让钉版三件的字节**活过 git 往返** | `f127c34` |
| **c-1b-0** 转写器指纹承重件 | 迁移 `051` 加 `transcriber_lockfile_hash`;判同 SQL **删掉 name/version/lockfile**,只留 hash | `5792417` |
| **c-1b-1** MinerU 接线 | parser + 子进程 + **杀进程树** + 并发闸 1 + 逐 parser 身份申报 | `d3c8661` |
| **c-1b-2** region 与诚实粒度 | 表级 region 锚 + 结构化 cells;单元格几何**如实申报不可达** | `4503768` |

**测试**:`test:v2` **331 → 350**,全绿;`tsc --noEmit` 零诊断;既有链计数不变。

## 2. ⭐ 本段真正的头条:**两条法离开了中转站**

- **clamp 禁令** → 活在 `normalizedRegionBbox` 的注释 **+ 真实行为**里(越界/反序必须抵达契约并被拒)。
- **「打了补丁还自称原版」** → 活在**字面量类型**里(`silent_patch_forbidden: true`、`transcriber_name_must_differ_from: 'mineru'`)⇒ **由编译器执行**。

⭐ **归宿层级**:**类型(写时拦) > 常驻测试/闸(跑时拦) > 注释+行为(读到时拦) > README(想起来时拦)**。
⇒ 欠账清单**第一次有条目往上走**。收工清单已加一条:**「本单有没有法可以离站?」**

## 3. 三次停线,三次都是发单方的错

| # | 单 | builder 发现 | 归属 |
|---|---|---|---|
| 1 | c-1b-0 | 精确列断言 + 34 处无 hash 的既有调用 ⇒ 单不可满足 | **发单方**:列举了「生产者」却漏了「**全部调用方**」 |
| 2 | c-1b-1 | 注册了 parser 也没人会选它(`SOURCE_FORMATS` 钉死) | **发单方**:查了注册表就说接缝干净,**没查谁设 `parser_key`** |
| 3 | c-1b-2 | MinerU 标准输出**没有 cell bbox**(内部算了,`batch_analyze.py:684` 只留 `.html`) | 工具的**结构性上限**;⭐ 并**捅破了发单方 killer 的洞**(只验命中会被整表 bbox 平凡满足) |

⭐ **第三次之后,「先把接缝查到底」第一次在【拆单前】跑完并当场拦住一张会翻车的单**(c-1b-2 的 `table` 族不可用)。**同一条法从事后病理长成事前免疫。**
⚠️ **但免疫不是免费的**:提前付的是**一次拆单侦察**,省的是**整轮派工—停线—重写**。

## 4. tech-debt 巡检

| TD | 终态 |
|---|---|
| **TD-29** 说谎的 `.d.ts` | ✅ **已清(c-0)** —— 删除分支;旧 ambient 在时静态 v2 import 稳定红于 TS2305 + TS2497 |
| **TD-30** 表格塌成一条 | **未清 · 已诚实拆分(c-1b-2)** —— 表级 region 已交付;**单元格【几何】寻址 = 本转写器不可达**,三条触发器在案(含**补丁版以独立身份申报**这一合法出口) |
| **TD-31** 坐标系混同 | ⛔ 已关闭(2026-08-28,零工程量) |
| **TD-36** 出生证存不下新身份 | ✅ **已清(c-1b-0)** —— 承重位真的换了;`NULL` 语义钉为**未证** |

### ⚠️ 巡检本身撞出的两件事

1. ⭐ **TD-30 的表格行在本会话被我弄坏过**(`0873f68` 丢了处置/状态两列)—— **危害形状是【状态列静默移位】**:巡检读到的是另一列,而它**看起来仍像一句合理的话**。已修复为 7 列。
2. **同一种损坏当场又发生一次**:我给 TD-36 写结案时,插入文本里含 `` `UPDATE|DEFAULT|RENAME` `` 的**裸 `|`** ⇒ 又多出两列。
⇒ ⭐ **同形第二次 = 机械化触发口**,已立闸:**`npm run check:tech-debt-table`**(`scripts/check-tech-debt-table.mjs`),阳性对照已跑(坏则 exit 1、好则 exit 0)。
📌 **历史遗留 TD-6 / TD-12 / TD-28** 各多一个 `|`(**会话前既有**,状态列仍在末位、读得对)⇒ **豁免登记,⛔ 不许新增**。⛔ 未改它们:修要猜那个 `|` 原属哪半句,**有改变原意的风险**,而它们不是本会话所写。

## 5. ⛔ 如实押后:c-1c(三级火箭第 1 级,考卷分角色)

段计划把「三级火箭第 1 级」写在 c-1 内。**本段未做,如实申报。** Fable 2026-08-29 裁:c-1c 本就「可押后」,段账趁热收比摞新活干净。
⚠️ **触发器**:夜班若有余量则拆(它是独立实验单);涉及模型调用须守老三条 —— **key 值不进日志、花费如实记、小样先行**。

## 6. 指纹三源闭环(待 Fable 补第三源)

MinerU 出生证的 `transcriber_lockfile_hash` = `67c4b42dcd269ffa3e9f05633ce98d6259812bc460f2dd2f8f58cb142e3ee231`。
- **源一**:复核方在 c-1a-2 复核时独立算(数小时前);
- **源二**:c-1b-1 的测试独立 `readFileSync` 重算;
- **源三**:Fable 段收口时**亲算** —— **逐字符一致**。

⭐⭐ **这是 v0.7.1「转写器身份 = lockfile 原始字节指纹」的【首次三方闭环】**:**三个时刻、三条工具链、同一个答案**。
⇒ 该条宪法从此不再只是**规定**,它有了**一次被三方独立验证过的实例**。

## 7. 证据本体

`D:/Coinsides/v12.9-selection/**` **全程零写入**,两口径互证:
- builder 全量清单口径(**94,935 个文件**逐一 sha256 再聚合)前后同为 `ebcb8fa9a4bf9387f9da8d1dff042f63dced6d15f41193811ce09894fff00432`;
- 复核方单文件口径(`uv.lock` md5 `d05dd297…`)全程未变。
⭐ **两口径粒度差五个数量级却结论一致 —— 异口径一致比同口径复算强。**

---

## ⭐ 尾注:c-1c 已于签后当夜完成(2026-08-29,批 `d777c36`)

⛔ **§5 原文不改** —— 签过的收口记录是**史**:「**签时如实押后**」与「**签后当夜完成**」**两句都是真的**,让时间线自己说话。

- **c-1c 结果**:同一批 160 条,**旧口径 154 通过 / 新口径 158 通过**,差额 4 条逐条在案(全部为 `chart`×2 + `image`×2 的图块类改判)。
- **K-3 反例成立**:2 条仍是 miss(`table` 的单元格文本未全命中 / `index` 的目录点引线)。⭐ **`index` 那条纲领 §7 原本没有归类**,builder 自己发现并**拒绝为它新造第四把尺**,留成真 miss。
- ⚠️ **⭐ 引用 158 必须带口径脚注**:**`158 = 154 条「文本能回到该 bbox」 + 4 条「图像能回到该 bbox」`;其中 4 条证明的是【图在那儿】,不是【那段文本在那儿】。**
- **零成本自证**:未调任何模型、未用任何 API key、未产生任何花费。
- **证据本体**:整树 SHA-256 前后一致,`v12.9-selection/**` 全程只读。
- **复核形态**:分析单无可变异代码 ⇒ 亲刀 = **独立重算**;复核方在 builder 收工**之前**自数 6 条 miss,与其结论**逐条对上**。
