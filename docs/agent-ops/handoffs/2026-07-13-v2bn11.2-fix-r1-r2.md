> from: claude | to: codex | status: done | re: v2bn11.2-fix-r1-r2 | date: 2026-07-13

# V2.BN.11.2 修正单：R1 账号级联地雷 + R2 回滚断言补强

> Henry 在对话中直接指示发单解决,故 status=ready。完整判定见 11.2 Review §8(条件 PASS);本单只列修正项。**修正窗口:047 尚未 commit、未进任何已迁移库——此刻改是四行,commit 后改是 migration 048。**

## R1 · [MED,必修] `content_group_members.item_id`:ON DELETE SET NULL → **NO ACTION(去子句)**

**为什么**:SET NULL 与互斥 CHECK 组合——kind='item' 行上 SET NULL 一触发必违 CHECK;账号级联删除时 SQLite **不保证**子表处理顺序,若 items 先于 members 被处理,SET NULL 立即触发→CHECK 炸→整个 `DELETE FROM users` 失败。T5 fixture 恰无 item 成员行,现绿是假安全(你 Review §6.1 自己列了该交互但未测)。NO ACTION 严格占优:直接删卡同样被拒(语句末检查,与 relations 端点 FK 同哲学),账号级联则顺序无关安全(语句末时 member 行已随自身 user_id 级联死亡)。

**改动**:
1. `047_v2_item_relation_floor.ts` rebuild 中 `item_id TEXT REFERENCES items(id) ON DELETE SET NULL` → `item_id TEXT REFERENCES items(id)`;
2. `schema.sql` 同行同改;
3. **T5 fixture 补一行 kind='item' 的 content_group_members 行**(连同其 content_group/course 前置),断言 `DELETE FROM users` 仍成功且全家清零——把 SET-NULL-vs-CHECK 交互从"记录的残留风险"变成"被测试锁死的行为";
4. Review §6.1 措辞随改(SET NULL 语句已不成立);
5. 概念设计 v1.1 勘误**已由 Claude 落**(§2 既有表注释,commit `3cac6e8`),无需你动。

## R2 · [MED-LOW,顺手] 回滚断言补强

T3 现只证 §3.4 四项声称中的两项(rename 回滚+六表 DROP 回滚),且直调 `.up()` 自包事务、未走 runMigrations——收据回滚从未被行使。补(扩 T3 或新增一条):经 **runMigrations** 的失败注入,断言失败后 ①`items` 等 floor 新表不存在 ②`db_migrations` 无 047 收据行 ③六张退役表仍在。

## L1 · [LOW,一句话] Review §3 措辞

"The migration runner **now** wraps..."——该事务包裹是 2026-03(`d797038`)既有行为,非本版交付;改为如实描述("依赖 runner 既有的每迁移事务包裹")。

## 验证与交付

改毕重跑:专项套件、`cd server && npm run test:v2`、`npm run verify:v2-bn8-runtime`。在本单追加 `## Result`(数字+改动清单),**不自签 PASS**——Claude 复验后落 checkpoint commit 并准予 11.3。

## Result（Codex · 2026-07-13）

已作为 **V2.BN.11.2.1 - Migration 047 Safety Fix** 完成，不扩张到 V2.BN.11.3，也未自签第三方 PASS。

改动清单：

1. migration 047 与 clean `schema.sql` 的 `content_group_members.item_id` 均去除 `ON DELETE SET NULL`，统一使用 SQLite 默认 `NO ACTION`；
2. 专项测试新增 `PRAGMA foreign_key_list` 动作断言，并在账号级联 fixture 中加入真实 `kind='item'` member；
3. T3 改为经 `runMigrations` 执行失败注入，锁定五张新 floor 表、047 receipt、六张退役表、member rename 与原始数据的整组回滚；
4. 11.2 Review 已纠正 runner 既有事务能力的归属，移除已经关闭的 SET NULL 残余风险；
5. 新增 `V2.BN.11.2.1` Plan 与 Review，并同步 V2.BN.11 主计划状态。

验证数字：

- RED：专项 7/8，唯一失败为外键动作仍是 `SET NULL`；
- GREEN：专项 8/8；
- 完整 server V2 lane：217/217；
- Canvas runtime boundary：159/159；
- Canvas engine model contract：58/58 组；
- client build、server build、performance smoke、`verify:v2-bn8-runtime`：通过。
- `git diff --check`：通过；changed-file secret scan：21/21 文件通过。

下一步仍是 Claude 独立复验；在复验签发前，本回执不宣称 11.2 已获第三方 PASS，也不准入 11.3。
