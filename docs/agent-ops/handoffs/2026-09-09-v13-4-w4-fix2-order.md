> **状态 (Status)**: ready(**候修一收货后派**,单 builder ⛔并行)
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④第二批 findings+对谈拍定(claude-log 09-09 §23);修一(F1-F4)同族后续
> **单号**: 13.4 走查修二 · 两 bug + 粉笔幽灵框 + 引卡小签改造

# 走查修二

## F5 · 【bug,置顶】板上 New note 三建一存

- 现象(Henry 实测):开箱仪式建笔记,创建 3 次只保存下来 1 次;疑与 F1 帧契约同族(ceremony 预建 frame 的保存链)或静默失败无提示;
- 先复现后修:真实浏览器多次连建(含就地新建 project 与选既有 project 两路),失败场景与根因写进 Result;修复后**任何失败必须可见报错**⛔ 静默吞;
- 回归:连建 10 次全部落库可重开;失败注入时用户可见错误且无半成品残留。

## F6 · 【bug】删 Project 报 Internal server error

- 现象(截图在案):Delete Project 预览对话框(0 placements/0 projections)选"Delete projections"→红条 Internal server error;
- 先复现后修,根因嫌疑按 0831 字面量教训倒查:**059-062 新表**(board_text_ranges/items.origin/board_members placed/board_layers)对 notes/courses 的 FK 是否阻断删除链;`courseLifecyclePolicies` 表名登记面是否漏新表;
- 修复规则(HQ 裁):①删除链⛔被新表 RESTRICT 阻断;②引用侧一律走既有可见降级——源笔记消失→board_text_ranges 转 **lost 保快照**(⛔ 随删静默蒸发),items.origin SET NULL 降级文案,board_members 引用走既有 missing;③板/魂身份不随 project 死(project=弱标签只清标签,对话框既有承诺);legacy purpose 阻断语义(有板阻断永久删除)保留不动;
- 回归:含板引用/引卡/板生 item 的 project 删除全链走通,各引用面显正确降级,魂与板存续。

## F7 · 粉笔幽灵框(Henry 拍定交互)

- 双击空白=先出**虚线幽灵框**(起草态);**输入了内容才变实心**(可保存);内容全删→退回虚线;虚线态下点击空白/失焦=**消失不落库**;
- 既有:Escape 弃沿用;280 上限沿用;⛔ 空粉笔落库(现状 bug:空白失焦会留下空 sticky);
- 回归:双击→不输入→点别处=零残留零请求;输入→失焦=保存;保存后清空内容→失焦=该粉笔删除。

## F8 · 引卡小签改造(货大签小,Henry 拍定)

四处同族:text_range 卡、item 卡、staging 行、笔记内 item_ref 块。

1. **卡面主体=内容正文**(选段文字/item 正文,按正文字号排版,该多大多大);
2. **出处收进一枚小签**(卡角落):极简——**⛔ 显示笔记名**,就一枚小色签;**状态用签的形态区分**(Live 与 Drifted 两种不同样式——形式即语言,⛔ 文字徽章占地);
3. **点开小签=详情浮层**:来源笔记名、位置、锚健康态(Live/Drifted/Lost 详述)、"Open source note" 跳转;
4. 既有降级语义不变(Drifted/Lost 内容区仍显快照,只是装置收进签里)。

## F9 · 签色=源笔记稳定映射

- 同一源笔记的引用=同色签;不同笔记尽量异色;
- 复用**标注系统既有调色板**;映射=hash(note_id)→调色板索引(跨会话稳定);色数有限循环撞色可接受;
- Result 报一笔:调色板现有色数清点(Henry 问账)。

## 裁量与停线

- 停线举证不改判;F5/F6 根因触更深契约→停线;
- ⛔ 动块边框淡化(另批,候 Henry 看完 F8 效果一起裁视觉语言);⛔ 新 event verb;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。

## 验证(单内纪律)

- typecheck + build 全绿;定向回归(Boards+canvasEngine+notes/courses 删除面)不破;
- 冒烟:F5 连建 10 次+失败可见;F6 含引用 project 删除全链+降级面;F7 三态回归;F8 四处卡面主次翻转+签点开详情+跳转;F9 同笔记同色异笔记异色。

## Result 格式

`## Result`:numstat + F5/F6 复现根因 + 冒烟逐条 + 调色板清点 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触。
