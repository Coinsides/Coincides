> **状态 (Status)**: done(HQ 收口:F5 经补遗一原子端点修复验证,F6 局部修复+F7-F9 收货;凭据扫描 HQ 例行补跑)
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④第二批 findings+对谈拍定(claude-log 09-09 §23);修一(F1-F4)同族后续
> **单号**: 13.4 走查修二 · 两 bug + 粉笔幽灵框 + 引卡小签改造

# 走查修二

## F5 · 【bug,置顶】板上 New note 三建一存

- 现象(Henry 实测):开箱仪式建笔记,创建 3 次只保存下来 1 次;疑与 F1 帧契约同族(ceremony 预建 frame 的保存链)或静默失败无提示;
- 先复现后修:真实浏览器多次连建(含就地新建 project 与选既有 project 两路),失败场景与根因写进 Result;修复后**任何失败必须可见报错**⛔ 静默吞;
- 回归:连建 10 次全部落库可重开;失败注入时用户可见错误且无半成品残留。

## F6 · 【bug】删 Project 报 Internal server error

- 现象(截图在案):Delete Project 预览对话框(0 placements/0 projections)选"Delete projections"→红条 Internal server error;
- 先复现后修,根因嫌疑按 0831 字面量教训倒查:**059-062 新表**(board_text_ranges/items.origin/board_members placed/board_layers)对 notes/courses 的 FK 是否阻断删除链;`courseLifecyclePolicies` 表名登记面是否漏新表;
- 修复规则(HQ 裁):①删除链⛔被新表 RESTRICT 阻断;②引用侧一律走既有可见降级——源笔记消失→board_text_ranges 转 **lost 保快照**(⛔ 随删静默蒸发),items.origin SET NULL 降级文案,board_members 引用走既有 missing;③板/魂身份不随 project 死(project=弱标签只清标签,对话框既有承诺);legacy purpose 阻断语义(有板阻断永久删除)保留不动;
- 回归:含板引用/引卡/板生 item 的 project 删除全链走通,各引用面显正确降级,魂与板存续。

## F7 · 粉笔幽灵框(Henry 拍定交互)

- 双击空白=先出**虚线幽灵框**(起草态);**输入了内容才变实心**(可保存);内容全删→退回虚线;虚线态下点击空白/失焦=**消失不落库**;
- 既有:Escape 弃沿用;280 上限沿用;⛔ 空粉笔落库(现状 bug:空白失焦会留下空 sticky);
- 回归:双击→不输入→点别处=零残留零请求;输入→失焦=保存;保存后清空内容→失焦=该粉笔删除。

## F8 · 引卡小签改造(货大签小,Henry 拍定)

四处同族:text_range 卡、item 卡、staging 行、笔记内 item_ref 块。

1. **卡面主体=内容正文**(选段文字/item 正文,按正文字号排版,该多大多大);
2. **出处收进一枚小签**(卡角落):极简——**⛔ 显示笔记名**,就一枚小色签;**状态用签的形态区分**(Live 与 Drifted 两种不同样式——形式即语言,⛔ 文字徽章占地);
3. **点开小签=详情浮层**:来源笔记名、位置、锚健康态(Live/Drifted/Lost 详述)、"Open source note" 跳转;
4. 既有降级语义不变(Drifted/Lost 内容区仍显快照,只是装置收进签里)。

## F9 · 签色=源笔记稳定映射

- 同一源笔记的引用=同色签;不同笔记尽量异色;
- 复用**标注系统既有调色板**;映射=hash(note_id)→调色板索引(跨会话稳定);色数有限循环撞色可接受;
- Result 报一笔:调色板现有色数清点(Henry 问账)。

## 裁量与停线

- 停线举证不改判;F5/F6 根因触更深契约→停线;
- ⛔ 动块边框淡化(另批,候 Henry 看完 F8 效果一起裁视觉语言);⛔ 新 event verb;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。

## 验证(单内纪律)

- typecheck + build 全绿;定向回归(Boards+canvasEngine+notes/courses 删除面)不破;
- 冒烟:F5 连建 10 次+失败可见;F6 含引用 project 删除全链+降级面;F7 三态回归;F8 四处卡面主次翻转+签点开详情+跳转;F9 同笔记同色异笔记异色。

## Result 格式

`## Result`:numstat + F5/F6 复现根因 + 冒烟逐条 + 调色板清点 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触。

## Result

> **回执日期**：2026-09-09 · **执行**：Codex builder，单 builder 串行。
> **判定**：F6 局部修复、F7–F9 已施工并验证；**F5 停线，整单未完成**。不改上游判定，不翻 done，不 commit；工作树交 HQ。

### 变更账（numstat）

代码及功能测试共 **24 文件，+372 / -60**；本回执另计 **+85 / -0**。新文件按全部新增行计；未计入开工前已有的未跟踪文件及被忽略的 `.codex-tmp/w4-fix2/` 隔离证据。

```text
added deleted path
98    0       client/src/components/ReferenceTag/ReferenceTag.tsx (new)
17    0       client/src/components/ReferenceTag/ReferenceTag.module.css (new)
39    0       client/src/components/ReferenceTag/ReferenceTag.test.tsx (new)
16    0       client/src/pages/Boards/BoardReferenceTag.tsx (new)
5     2       client/src/pages/Boards/BoardChalk.tsx
31    2       client/src/pages/Boards/BoardPage.chalk.test.tsx
6     5       client/src/pages/Boards/BoardPage.item.test.tsx
2     1       client/src/pages/Boards/BoardPage.staging.test.tsx
14    29      client/src/pages/Boards/BoardPage.tsx
7     2       client/src/pages/Boards/BoardPage.unboxing.test.tsx
2     0       client/src/pages/Boards/BoardStaging.module.css
8     2       client/src/pages/Boards/BoardStaging.tsx
6     0       client/src/pages/Boards/Boards.module.css
6     5       client/src/pages/Boards/boardTextRangeClipboard.test.tsx
3     0       client/src/pages/Boards/boardTypes.ts
6     4       client/src/pages/Notes/canvasEngine/blocks/ItemRefBlockProjection.module.css
6     3       client/src/pages/Notes/canvasEngine/blocks/ItemRefBlockProjection.tsx
1     0       client/src/services/itemSummaryReader.ts
4     3       server/src/__tests__/v13ItemFloor.test.ts
72    0       server/src/__tests__/v13ProjectDeletionReferences.test.ts (new)
5     1       server/src/services/boards.ts
15    0       server/src/services/courseLifecycle.ts
1     1       server/src/services/itemSummaries.ts
2     0       shared/types/itemSummary.ts
85    0       docs/agent-ops/handoffs/2026-09-09-v13-4-w4-fix2-order.md
```

### F5 复现、根因证据与停线

- 真实 Chrome 浏览器，独立 `127.0.0.1:5201`，使用实际 BoardPage/New note/NoteDetail/canvas runtime、实际 API routes 与 SQLite `:memory:`。未连接用户前后端或用户数据库。最初三次走“新建 Project／选既有／新建 Project”，写正文后关闭，得到 **3 notes / 3 blocks / 3 frame collections**；**未复现 Henry 原始三建一存的频率现象**。
- 对既有 Project 分支注入一次 `PUT .../page-frame-collection` 503：前面的 `POST /notes` 已 201，结果变成 **4 notes / 3 blocks / 3 frame collections**。界面明确显示 `Your note was created, but could not be opened. Retry opening the same note.`，可 Cancel / Retry；取消不会清理已建笔记。
- 对就地新建 Project 分支再次注入同一失败：`POST /courses` 201 → `POST /notes` 201 → `PUT .../page-frame-collection` 503。错误同样可见；点击 Cancel 后仍有 `F5 injected inline project` 与 `F5 injected inline note`，该笔记无帧集合。
- 可证根因是 `BoardNewNoteDialog.tsx` 的分步保存：Project（可选）、note、frame collection 为独立写请求，`createdNote` 只在本次弹窗里支持重试；关闭后没有整体回滚或持久的失败恢复身份。这能解释已证的半成品，**不能据此宣称已定位 Henry 原例的唯一原因**。
- 后续未修改 F5 链路，补跑十次连续创建：5 次就地新建 Project、5 次选既有 Project，每篇输入 `F5 durable body 01` 至 `10` 后立即关闭；**10 notes / 10 正文 blocks / 10 frame collections**，随后在真实浏览器逐篇重开并命中各自正文。此为正常路径基线通过，**不是修复后验收**。
- **停线**：满足“失败无半成品”需要明确 note + frame（及就地新建 Project）的原子创建/回滚、失败恢复契约。按本单“F5/F6 根因触更深契约→停线”，未擅加事务端点、补偿删除或重写保存协议。F5 的无残留验收明确不通过，待 HQ 裁定后续契约。
- 证据：`.codex-tmp/w4-fix2/f5-before-normal.json`、`f5-frame-failure.json`、`f5-ten-created.json`、`f5-inline-failure-final.json`；浏览器截图与逐篇重开记录在本次 thread。

### F6 复现、根因与局部修复

- 倒查 059–062：059 的来源 note/block 不设删除级联 FK，保留选段快照；060 的 `items.origin_board_id` 为 SET NULL，既有 `origin_note_id/origin_course_id` 亦为 SET NULL；061 的 placed 与 062 的层表没有新增 course 删除阻断。`courseLifecyclePolicies` 已登记 boards/purposes 的弱 Project 标签与 items.origin_course_id；coverage 检查通过，**未发现该登记面漏新表**。
- 当前迁移的隔离库中，空 Project 和普通笔记 Project 删除成功。含板引用、选段、note-born item、staging item、board-born item、层与 library soul 的 Project，在真实删除对话框显示 **0 placements / 0 projections**，选 Delete projections 后删除成功。因此没有把新表嫌疑当成已证根因，也未修改这些 FK 或登记政策。
- 另建 057 前的升级夹具，保留 note-bound legacy purpose，再迁到 062 并挂板：删除 Project 可复现 `FOREIGN KEY constraint failed / SQLITE_CONSTRAINT_TRIGGER`，没有 AppError statusCode，既有错误中间件会包装成 500。链路是 course 删除 → note 删除 → legacy purpose 删除 → `boards.soul_id RESTRICT` 拒绝。该拒绝本身是本单要求保留的语义。
- 局部修复只在**既有删除事务内**预查即将被删除的 note 是否有挂板 legacy purpose，将预期阻断转为 **409 + 明确说明**，不拆板、不解绑魂、不弱化 FK。Move to Home 仅豁免本次确实保留的 projection notes；普通 note 的 legacy 阻断仍在。两种 action 均有无部分删除的功能回归。
- 修后真实 ProjectDeleteDialog 显示 `This Project contains a legacy note-bound purpose with a board. Permanent deletion is blocked; the Project and its contents have been kept.`；返回 409，Project/note/purpose/board 四者保留。legacy 浏览器夹具接实际删除服务及错误中间件；其独立升级数据库不与新库混用。
- 普通含引用 Project 删除后：板与魂只清 Project 标签；层仍在；note member 为 missing；range 行及 excerpt 保留，读取投影 `anchor_status=lost`（不伪写历史 range 状态）；item 正文保留，note/course origin 清空，详情呈 Birthplace unavailable / Lost；board-born item 仍保留板出生地。
- 限定：这是**已证的一个 500 来源**，不是对 Henry 用户库状态的断言。若原例不属 legacy purpose 挂板，需要 HQ 提供不含凭据的最小复现继续定位，不以此改判原例已收货。
- 证据：`.codex-tmp/w4-fix2/f6-legacy-before.json`、`f6-after-final.json`、`f6-legacy-after.json`；自动回归在 `v13ProjectDeletionReferences.test.ts`。

### 冒烟逐条

| 项 | 结果 | 已亲跑内容 |
| --- | --- | --- |
| F5 连建十次 | 正常基线通过；修复验收停线 | 两入口各 5 次，写后立即关闭，10/10 落库并逐篇重开 |
| F5 失败注入 | 报错可见通过；无残留不通过 | 两入口帧保存 503；取消后笔记残留，新建 Project 分支还保留新 Project |
| F6 删除与降级 | 隔离夹具通过 | 实际对话框删除含引用 Project；重载板核对 Lost 快照、missing note、item 正文、弱标签、魂/板/层存续；legacy 409 与零部分删除 |
| F7 幽灵框 | 通过 | 双击无输入：透明虚线；失焦零写请求、零残留。输入：实心；失焦 POST 201。已存稿清空：退虚线；失焦 DELETE 200，零空 sticky、无空文本 PATCH。Escape、280 上限既有整套回归通过 |
| F8 text_range 卡 | 通过 | 正文为主体，无常驻来源名或 Live/Drifted 文字徽章；点签看来源/块位置/范围/健康态，跳来源成功；改来源后虚线斜签，仍显示原快照；删除后点状 Lost 签保快照 |
| F8 item 卡 | 通过 | 完整正文可滚读，未截成 240 字预览；详情来源/位置说明/状态及跳转通过；失去 origin 后正文仍在，详情显示可见降级 |
| F8 staging 行 | 通过 | 完整正文与角落签；来源名收进详情，详情与来源跳转可用；Place/Remove 及拖入笔记既有回归通过 |
| F8 item_ref 块 | 通过 | 只存 item 身份、重读当前正文；正文为主体，来源详情及跳转可用；modal/full-page 重开及退休/缺失功能回归通过 |
| F9 同源色 | 通过 | 源 A 的选段/item/staging/item_ref 全为 annotation-yellow，源 B 为 annotation-rose；页面重新挂载颜色不变。无 origin 的 item 使用中性色，不虚构源笔记 |

### 调色板清点

复用 `annotationColorService.ts` 的 `ANNOTATION_COLOR_OPTIONS`，现有 **6 色**：Yellow `#facc15`、Blue `#60a5fa`、Teal `#2dd4bf`、Violet `#a78bfa`、Rose `#fb7185`、Slate `#94a3b8`。FNV-1a 对 note_id 做确定性哈希，取 unsigned 值 `% 6` 选索引；不依赖会话或遇见顺序，有限色循环碰撞保留。未新增颜色。

### 验证与未做

- 最终前端 **83 个文件 / 726 tests 全通过**；无 test name filter。后端 Boards 全部功能测试文件加 Items/ItemRef/RecordedAction/ProjectDeletionReferences/Purposes/NotesLifecycle/SourceLifecycleClosure/RelationLifecycleClosure 完整文件运行：**79 passed / 0 failed / 0 skipped**。新增的测试均为本单功能回归，未设计安全类测试。
- 使用 `.codex-tmp/w4-fix2/validate.mjs` 在净化子进程环境、`:memory:` 及禁读 dotenv 的现有 preload 下逐项运行 `verify:v2-bn8-runtime` 内容。shared/client typecheck、client/server build、工具注册/清单/覆盖、canvas runtime/model/performance、文档检查均通过。`git diff --check` 在净化 runner 中因 Git 仓库发现失败返回 129，随后在原工作区直接重跑 **通过**。日志在同目录 `validation-gate.json`、`validation--client.json`、`validation--server.json` 及各 stage `.log`。
- **未执行凭据扫描**：本单明确留 HQ。因此不声明原样 `npm run verify:v2-bn8-runtime` 整体绿；授权的其他子门已跑，HQ 仍需完成 scanner 与最终门记账。
- **未做**：F5 深层保存契约修复；Henry 原始库上的复现/主观验收；块边框淡化；新 event verb；schema/FK 改造；安全测试或凭据扫描；任何 `.env` 读取、用户库访问、密钥外传、commit/push/PR/merge。开工前未跟踪文件保持原样。
- 隔离浏览器、失败注入和诊断数据均只作用于内存夹具；脚本与证据留工作树 `.codex-tmp/w4-fix2/` 给 HQ。**停线仅 F5 原子创建/失败恢复契约；整单不得据此回执翻 done。**

## 补遗一(HQ 裁定,2026-09-09:F5 原子仪式契约,续工令)

1. **停线采纳**:分步保存(可选 project→note→frame 三独立请求)在中途失败时留半成品,举证成立;
2. **裁定:服务端原子仪式端点**——板域新 route(如 `POST /boards/:boardId/ceremony-note`):**单事务**内完成(可选)就地建 project+建 note+初始 frame collection,整包返回;失败=整体未发生(**零半成品**);client `BoardNewNoteDialog` 改调该端点,失败错误可见可重试;
3. **⛔ 客户端补偿删除**(补偿链路自身可失败,会撒谎);⛔ 动笔记页自身的创建路径(仪式专用端点);事件面沿既有创建链的既有记账,⛔ 新 verb;
4. **回归**:①帧步注入失败(两入口)→ project/note/frame 零残留+错误可见+重试成功;②正常两入口各 5 连建 10/10;③既有 ceremony 测试面迁移到新端点后不破;
5. **F6 附注**:局部修复(legacy 409)收货;若 Henry 重测原例仍 500,HQ 与 Henry 在场取真实报错继续定位,⛔ 以本修即宣告原例已解;
6. F6-F9 成品⛔ 重做;完成后新 Result 追加于本补遗后,整单同批交 HQ 代账。

## Result（补遗一续工：F5）

> **回执日期**：2026-09-09 · **执行**：Codex builder，沿本单单 builder 串行施工。
> **工程结果**：按补遗一完成 F5 原子仪式端点与三条回归。前一 Result 的“F5 未施工／停线待裁”是裁定前状态，本回执接续更新；原举证和 HQ 判定不回改。F6–F9 成品保留，整单工作树同批交 HQ 复核、代账；未 commit，未自行翻单状态或作主观放行。

### 本次增量账（numstat）

相对续工前工作树，代码、测试及测试入口共 **8 文件，+237 / -73**；本回执另计 **+53 / -0**。与前次成品合并，整单代码、测试及测试入口共 **30 文件，+609 / -133**。不计开工前其他未跟踪文件及被忽略的隔离证据。

```text
added deleted path
13    34      client/src/pages/Boards/BoardNewNoteDialog.tsx
38    35      client/src/pages/Boards/BoardNewNoteDialog.test.tsx
1     1       client/src/pages/Boards/BoardPage.tsx
6     2       client/src/pages/Boards/BoardPage.unboxing.test.tsx
5     0       server/src/routes/boards.ts
55    0       server/src/services/boardCeremonyNote.ts (new)
118   0       server/src/__tests__/v13BoardCeremonyNote.test.ts (new)
1     1       server/package.json
```

`BoardPage.tsx` 只补传 `boardId`；`BoardPage.unboxing.test.tsx` 只迁仪式 HTTP 夹具，原 F8 的详情与出处跳转断言保留。其余 F6–F9 文件本轮没有续改。F9 调色板仍为前次已清点的 **6 色**，本轮没有颜色改动。

### F5 根因与落地

- 沿用前次已证根因：可选 Project、note、初始 frame collection 分三请求写入，中途帧失败留下半成品；不把它升级为 Henry 原始“三建一存”频率现象的唯一原因。本轮按 HQ 采纳的原子契约施工，没有另改判据。
- 新增 **`POST /api/boards/:boardId/ceremony-note`**。输入为 `{ project_id, title, collection }` 或 `{ project: { name }, title, collection }`；返回 **201 + `{ project, note, collection }`**。板与 Project 归属检查、可选就地建 Project、建笔记、既有 operation batch、初始帧写入及回读均在一个外层 SQLite 事务内。调用既有 `savePageFrameCollection` 时，其内部事务成为同连接 savepoint，不形成独立提交；任一步抛错时整体回滚。
- 保留原 name-only Project 默认值，以及笔记 `flow`、空 metadata、`Create note: <title>` 的 manual/applied operation batch 记账。未新造 event verb；未借用 `mounted` 或创建新事件。未修改 `POST /notes`、`POST /courses` 或笔记页自身的创建路径。
- `BoardNewNoteDialog` 复用既有初始帧种子，改为一次仪式 POST；收到完整成功响应才打开 note。失败显示 **“Could not create the note. Your entries are kept. Try again.”**，保留 Project 选择／新 Project 名／标题并显示 **Retry creating note**。取消与防重复提交仍在；没有客户端补偿删除或“保留半成品后补帧”路径。

### 三条回归与浏览器实跑

真实 Chrome，独立 **`127.0.0.1:5202`**，实际 BoardPage／BoardNewNoteDialog／NoteDetail／canvas runtime 与实际 API routes，SQLite **`:memory:`**。禁读 dotenv preload、净化子进程环境、`envFile:false` 沿用已有隔离装置。browser-harness 因 Chrome 调试端口文件读权限失败，改用已连接的 CUA Chrome；未改用户浏览器配置。

| 回归 | 结果与证据 |
| --- | --- |
| 既有 Project：帧步失败→零残留→报错→重试 | 在 `page_frame_extensions` 的 BEFORE INSERT 触发器抛错，发生时 collection/object/placement 已执行插入；仪式返回 500，UI 显示错误和 Retry。失败前后 courses/notes/operation_batches/collection/objects/placements/extensions/events/blocks **完整行相等**；0 note、0 frame、0 batch，新 Project 增量 0。重试返回 201，打开一篇有初始帧的笔记。 |
| 就地新建 Project：同样失败→零残留→报错→重试 | 同一帧步注入；失败前后上述完整行再次相等，保留前一条成功笔记但 **本次 Project/note/frame/batch 增量全部 0**。输入的新 Project 名和标题仍在，Retry 返回 201，仅新增一个 Project、一篇 note、一套初始帧。 |
| 正常两入口各 5 连建 | 既有 Project 5 篇、就地 Project 5 篇；每篇写入唯一 `F5 durable body ...` 正文后立即关闭，**10/10 note、10/10 正文 block、10/10 初始帧**。随后在真实完整笔记页逐篇重开，10 篇均显示各自标题、正文与 `1 / 1` 帧；实际 GET 均 200。 |
| 既有 ceremony 面迁移 | 原 7 个 dialog 用例全部保留并迁到新端点，涵盖输入要求、两入口、错误保留输入、重试、列表失败、取消、双提交、卸载后不打开；原 unboxing/runtime/item_ref 集成用例通过。新增 3 个服务端功能用例验证两入口的晚帧失败全量回滚与重试、十次完整创建及回读。新文件已纳入 `test:v13-boards`。 |

浏览器最终夹具共 **12 篇完整笔记**（2 篇失败后的成功重试 + 10 篇正常连建）、**7 个 Project**（1 个原有 + 1 个重试创建 + 5 个正常创建）、**12 套帧、10 个正文块**。22 条 operation batch = 12 次建笔记 + 10 次正文块创建；events 为 0。请求账为 **14 次仪式 POST = 2 次注入失败 + 12 次成功**，没有 `POST /notes`、`POST /courses`、初始帧独立 PUT 或补偿 DELETE。

证据留在 **`.codex-tmp/f5-atomic/`**：`existing-before.json`／`existing-failed.json`、`inline-before.json`／`inline-failed.json`、`normal-ten.json`、`final-reopened.json`、`verified-summary.json`。`verify-evidence.mjs` 亲跑通过，机械核对两次完整行零差异、十篇正文与帧、逐篇 GET 和写请求账。失败提示与最终正文截图、逐篇可见性断言记录在本 thread；隔离夹具及重导脚本同目录留 HQ。

### 验证、边界与交账

- 前端完整 suite：**83 files / 726 tests passed**。后端沿前次 Boards 全部文件加 Items/ItemRef/RecordedAction/ProjectDeletionReferences/Purposes/NotesLifecycle/SourceLifecycleClosure/RelationLifecycleClosure 完整运行：**82 passed / 0 failed / 0 skipped**。纳入入口后的 `test:v13-boards` 另整跑 **28 passed / 0 failed / 0 skipped**；不与 82 重复相加。没有 test name filter；未设计或新增安全类测试。
- `verify:v2-bn8-runtime` 依既有净化 runner 逐项实跑：shared/client typecheck、client/server build、工具注册／清单／覆盖、canvas runtime/model/performance、文档门均通过。净化 runner 中 `git diff --check` 同前次返回 129，随后在原工作区直接重跑通过。日志与 `validation-gate.json`、`validation--server.json`、`validation--boards-script.json` 已留在本次证据目录；该 JSON 保留 runner 的原始 129，不伪写为绿。
- **凭据扫描仍留 HQ，未执行**；因此不声明原样 `npm run verify:v2-bn8-runtime` 整门绿，也不把 scanner 视作豁免。HQ 完成扫描与最终门记账后再收整单。
- **未做**：F6–F9 重做、F6 原例已完全解决的声明、块边框淡化、schema/FK 改造、笔记页创建路径改造、新 event verb、客户端补偿删除、安全测试设计／新增、凭据扫描、`.env` 读取、用户数据库访问、key 出境、commit/push/PR/merge。
- **停线**：本次 F5 未产生新停线；原停线依补遗一裁定解除后完成施工。F6 仍按 HQ 附注处理：legacy 409 局部修复已收，若 Henry 原例仍 500，由 HQ 与 Henry 在场取真实报错。整单同批交 HQ 代账，不替 HQ 或 Henry 作主观验收。
