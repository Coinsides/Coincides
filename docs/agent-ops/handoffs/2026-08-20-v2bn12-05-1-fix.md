> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: ready | re: v2bn12-05-1-fix | date: 2026-08-20

# V2.BN.12 修正单 05.1:Slash 事务四缺陷(2H/2M)

## 背景

工单 05 复核 FAIL(2H/2M/1L),全文见工单 05 ## Review。已 VERIFIED:D3/D4 双债清偿、owner/range/duplicate 核心 guard(mutation 验杀)、菜单/03/schema 边界零触碰、全门。修理以 Review §1 finding 为准,其建议修法为默认蓝本。

## 修正要求

1. **R1[HIGH-1] rollback 不许吃 revision**:真实 `setDraftText` 独占 ref/TextFlow/revision 更新——rollback 路径**禁止预写共享 `draftTextRef`**;保证 rollback 递增 revision,使在途 create 返回后 latest-save loop 发出纠正 save。落 production integration test(穿过 useRuntimeNaturalWritingController+useDraftBlockController):hold create→输入 `/query`→Escape→resolve 携旧文本的 create receipt→断言纠正 `saveBlock` 发出、durable reconciliation/reload 无 slash。复核官的隔离复现场景为蓝本。
2. **R2[HIGH-2] slash 会话期不发布中间 offsets 为 durable**:方案 A(默认)——slash transient query 的 annotation forward rebase 留本地,session commit/rollback 前不 PUT 中间 offsets,终态一次发布;方案 B(若 A 破坏既有编辑路径)——version/CAS+operation receipt+串行化+read-after-error reconciliation,申报选择理由。**认识论修正**:一切申报不得把 Promise rejection 当"服务端未落盘"的证据——正确表述=「client 观察到失败时本地保持 inverse 一致+toast;durable outcome unknown」。补两条测试:forward 已 durable+inverse reject(断无分叉或有 reconciliation);server commit 后 response loss 同 catch 路径。
3. **R3[MED-1] drain 真实化**:废弃嵌套 no-op setter 的 `flushSync` 猜测;三份 draft authority 在 enqueue 时同步维护可读 snapshot/ref,或把同 event 的权威 snapshot 显式交给 rollback。复核官的 coherent positive(排队 plain/TextFlow/Formula 后 rollback applied=true 且 suffix 保留)落为 shared test;验收=删除 drain 的 mutation 必须命中 applied=true oracle 而红。
4. **R4[MED-2] 三条 killer 落户**:①canonical `textFlowIdForBlock` guard(`useSlashBlockRollbackController.ts:146`)独立 killer(同 block 同 unit 仅 flow 漂移→拒绝且零写);②owner 漂移时 interaction 零覆盖 oracle(1 call 不变 2);③duplicate 正确指向第二处的正控(aggregate+caret 同指第二处→只删第二处,第一处原样)。
5. **R5[LOW-1] 回执纪律**:每轮 RED 写全 command/exit;定向集列全文件与完整命令;所有 fixture 适配在 path-level scope 表点名。

## 保护性约束

已 VERIFIED 面不许回归:D3 四行矩阵/D4 门接线(verify 链 25-28s 量级)/owner/range/duplicate 现有 guard/菜单导航/03 保护面/schema/migration。禁 migration --apply。

## 验证与回执

全门重跑(unit 146+新增/server 262/verify 链/双 tsc/migration 5 项)。## Result 追加进本文件:四 R 落实+收据、R1 集成测试红绿证、R2 方案选择申报。不自评 PASS,不 commit。
