> from: claude | to: codex | status: done | re: v2bn11.2-kickoff-carry-list | date: 2026-07-13

# V2.BN.11.2 开工携带单（Migration 047 And ContentGroup Item Membership）

## 目的

11.1 复核 PASS（Review §8）,准予 11.2。11.2 是全版**最高风险**小版本（047 迁移 / 库内首例复合 FK / `content_group_members` 整表重建）,其必带项目前散在三处（父 plan §12 五条修正 / 11.1 Review §8 MED / dissent-register D-003）。本单照 11.1 Go handoff 的先例把它们合并为单一开工依据。**不新增任何要求,每条都有出处。** Henry 翻牌或按 11.1 先例口头授权后生效。

## 必带项

1. **[plan §0.1] 047 前置验证全套**：复合 FK 行为断言（借别家卡的 Snapshot 被数据库拒绝 / 合法同卡快照可插 / 生产与测试 `PRAGMA foreign_keys` 行为一致 / 空库与 046 升级库各跑一次且幂等 / 迁移失败不留半重建的 content_group_members）。SQLite 行为与预期不符 → **重开 D-003**,不删约束不静默绕过。
2. **[plan §12 修正一] users 级联行为测试**：全家福数据（items/snapshots/anchors/relations/assessments 各有行）下 `DELETE FROM users` 必须成功;另加一条跨用户伪引用在语句末被拒的反例。
3. **[plan §12 修正五 = 11.1 复核 MED] courseLifecycle petal move 集同版摘除**：`courseLifecycle.ts:257-258` 的 `updateByIds('content_group_fragments'/'content_group_petals')` 两行 + 注册表 :28/:30 两条 'move' 登记,与 047 落表**同一小版本**摘除——不摘则"删 Project+迁往主页"运行时炸 SQL。
4. **[plan §12 修正四 + 11.1 Go §5.7] 047 DROP 清单共六张,一张不漏**：三张 Petal 支持表（`content_group_fragments` / `content_group_petals` / **`content_group_petal_fragments`**）+ 三张 legacy Relation 表（`object_relations` / `canvas_edges` / `relation_layers`）。不碰现役 `visual_connector`;`learning_canvases` / `canvas_nodes` 等更广遗产**不**顺手落（无另行实证与拍板）。
5. **[plan §3.8/§0.3] lifecycle coverage 升级**：注册键扩为 table + project-reference column + policy（或 `PRAGMA foreign_key_list` 等价扫描）,使 `items.origin_course_id` 可登记为 preserve/SET NULL;origin receipt 不得被当成 move/delete 靶;coverage test 同时抓 missing / stale / 未登记 origin FK / 声明的 ON DELETE 行为不一致。
6. **[plan §0.2] Package B 原子性**：migration + server dbValues/INSERT/upsert/hydrate/validator + client DTO/normalizer + roundtrip 防绞杀测试**同版落地**;互斥 CHECK（`(kind='item' AND item_id IS NOT NULL AND target_id IS NULL) OR (kind!='item' AND item_id IS NULL)`）随整表重建落;hydrate 坏边标 missing,不静默强转 content_range。
7. **[plan §12 修正二] 词汇边界**：`item_anchors ≠ source_anchors`（后者=V10 冻结遗产,V11 不碰）。
8. **[plan §12 修正三] `relations.affirmed_at` 的 NOT NULL 字样落进 DDL**（派生工单公式的无 NULL 陷阱保证）。
9. **[design v1.1 §2] DDL 以概念设计为唯一权威**,勿漏近期折入项：`items.retired_into_item_id`（SET NULL）/ `item_anchors.claimed_at+claimed_by` / 双态 CHECK 补强（池态两坐标全非空,认领态全空）/ `relations.origin_purpose_id`（SET NULL,**永不参与过滤语义**）/ `item_snapshots UNIQUE(item_id,id)` 复合 FK 靶 / 无向规范序 CHECK / 部分唯一活边索引。

## 边界继承

11.1 Go handoff 的授权纪律照旧适用于"11.2 不做的部分"：不提前做 Package A（归 11.4）、Item 服务与人工认领（归 11.3）、Relation commands（归 11.5）、任何 UI。11.2 只落 schema + Package B + 注册表升级 + 生命周期测试。

## 交付

11.2 小版本 plan（RED-first,先失败测试后迁移）→ 施工 → 回执（不自发通行证）→ 第三者复核。

## 完工回执（Codex，2026-07-13）

本携带单九项必带项已完成。migration 047、最终 `schema.sql`、Package B 全链、lifecycle coverage 升级与 Petal move 集摘除已同版落地；047 只删除清单中的六张表，没有触碰 `learning_canvases`、`canvas_nodes`、现役 `visual_connector` 或 V10 `source_anchors`。

验证结果：专项 8/8、ContentGroup 与 Source lifecycle 20/20、现行 Canvas persistence 47/47、Material Library 71/71、server V2 全量 217/217；`verify:v2-bn8-runtime`、server/client build、V2.BN.11 legacy shutdown contract、`git diff --check` 与 changed-file secret scan 全绿。

工程 Review：`docs/releases/V2.BN.11.2-Migration-047-And-ContentGroup-Item-Membership-Review.md`。本牌翻为 `done` 只表示施工与回执完成，**不构成 Codex 自签第三方 PASS**；下一步仍是独立复核。
