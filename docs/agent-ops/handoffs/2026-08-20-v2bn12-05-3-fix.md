> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: ready | re: v2bn12-05-3-fix | date: 2026-08-20

# V2.BN.12 修正单 05.3:三条状态转移窄缝(架构已定,只收缝)

## 背景

05.2 复核 FAIL(3H),但**设计收缩被完整验证**:05.1 五 finding 全灭、回滚地图归零、正门共门成立、五出口回滚经普通路径核形通过。三个新 HIGH 全是窄缝,修法以 05.2 ## Review §1 的建议为准。**本单禁止架构变更**——只收缝。

## 修正要求

1. **R1[HIGH-1] receipt 创建时铸造**:`saveAnnotationTruths` callback/operation **创建时**绑定不可变 `{noteId, generation}` receipt,旧 callback 永久携旧 generation,禁止调用体内从 ref 现取。range-edit continuation 检查 matching block save receipt。killer:A→B→A + 旧 callback 重放,断 stale 零 PUT、当前 A snapshot 与 durable 双不变。
2. **R2[HIGH-2] 保存尾巴按 note 分桶**:annotation save tail 至少按 note/route receipt 分桶;无本 note 前序的首笔 publish 在 enqueue 时即开始;同 note 真 stale 的 queued 项仍零 PUT。killer:`A held → B edit → leave B → release A`,断 B 恰一 PUT 且旧 response 不污染当前 snapshot。**注意**:B 被静默丢弃的路径若保留任何「拒绝」分支,必须可观察(reject 理由入收据/console 至少一样),不许静默 return——「静默丢有效人类意图」是本缺陷的真正罪名。
3. **R3[HIGH-3] session 随 owner 迁移**:draft→durable reconciliation 时,以 canonical receipt 把活跃 slash session 从 draft target 迁移为 block owner/range(优先),或在切 owner 前经正门完成 rollback——二选一申报理由;不得留旧 draft target。killer 矩阵:快 create/reconcile→Escape(prevented+回滚+durable 无 `/query`);reconcile→disabled/annotation/missing 各出口;断 durable TextFlow 与 annotation inverse rebase 一致。

## 保护性约束

05.2 全部已验绿面零回归(正门/五出口回滚/R1 revision/R3 snapshot/R4 killers/D3/D4/adapter 加固/A→B 既有负控);11 文件回滚面不许重新长出任何 gate;schema/migration/03 面零触碰;禁 migration --apply。

## 验证与回执

全门重跑(unit 157+新增/server 262 隔离 asset/verify 链/双 tsc/migration 5 项)。## Result 追加进本文件:三 R 落实+红绿收据(复核官三 probe 场景为 RED 蓝本)、保留面证明。不自评 PASS,不 commit。
