> **From**: codex（按本会话用户直接指令建单并记录）
> **To**: codex
> **Status**: done（用户直接授权的只读侦察已交付；非施工放行）
> **日期 (Date)**: 2026-09-09
> **性质**: 只读侦察单；只允许写本工单与指定报告

# 13.4 · 单 0 · 投影全谱与 item 化侦察

## 任务与依据

按 `docs/agent-ops/handoffs/plans/v13-4-projection-itemization-plan.md` 的单 0 六题侦察，产出 `docs/agent-ops/analysis/2026-09-09-v13-4-s0-recon.md`。报告须给出文件:行号级证据，区分现物、缺口与方案候选；不代行设计裁定或施工放行。

1. 047 item/relation 地基、ContentGroup item membership 消费链、board item 开闸缺口与 boards item 身份桥接候选。
2. AnnotationRange / source_anchors / TextFlow 与选区收据的现物及编辑后跟变、失效可见能力。
3. 准备区盘内重排、拖入手势挂点和 Tray→Staging/准备区文案面。
4. 板画笔、选择、删除现物，以及橡皮擦、普通撤回/重做候选。
5. 多页铺陈统揽挂点，一行 3～4 页、有限小、支持翻页。
6. 按依赖先后与最小改动面提出后续拆单建议。

依据依次为仓库入口与 active 现状/ADR、13.4 段 plan、其引用的图二与 13.3 裁定甲附账；代码现状以本次工作树静态读取为证。其它 ready 工单不在本单射程。

## 约束与交付判据

- 禁止改产品代码、接触用户库、commit、读取 `.env`、打印任何 key；不动 3001。
- 不运行服务、迁移、模型调用或产品测试；本单只做静态阅读、文档证据校对与工作树差异核对。
- 唯一写入面为本工单和指定报告；不改 agent 指令、现状层、段计划、旧工单或生成索引。
- 六题全部回答；关键结论有文件:行号证据；工单末尾追加 `## Result`，含报告路径、六题各一行摘要、验证与未做清单。

## Result

- 报告：`docs/agent-ops/analysis/2026-09-09-v13-4-s0-recon.md`。
- 第 1 题：047 表/服务与 CG item 身份链已落；item resolver 已有状态分支，仍缺新写、正文/导航/刷新及 Board–Item 桥接，A/B/C 候选与读侧断链已列证。
- 第 2 题：AnnotationRange 最适合复用地址与重映射内核，但 split/merge、删除、owner 和正文/锚双提交未闭合；source_anchors 与 selection receipt 不能直接承担活文本投影。
- 第 3 题：order_index、按钮移入、拖回纸已有；全盘重排需覆盖块/画物/mount 的写入口，拖入可接 pointer onEnd；Tray→Staging/准备区文案点已列齐。
- 第 4 题：板有 select/pan/connect/pen 与分类删除，无普通撤回；建议队列内 command 栈后接整笔擦，补复建 ID/级联边恢复边界，保持搬板回滚独立。
- 第 5 题：纸叠裁片和只读打印渲染可复用，三档旁可挂独立有限缩略网格；旧选页会保存页集合，纯统揽导航需避开该写副作用。
- 第 6 题：按甲部 1→2→3→4→5 列内部先后、最小改动面、前置裁定和定向验证；不代行后续派工/放行。
- 补充：收尾观察段 plan 出现非本会话写入的修订一草案，报告附录只读回答新增⑦弹窗挂载、⑧未摆位态、⑨z 序；新增设计仍候冻结。
- 验证：UTF-8 解码、六题/Result 结构、297 处引用行号（含同文件简写）的文件存在及边界、关键证据语义抽核、空白/冲突标记和工作树范围校对通过；未运行产品测试或 runtime gate，不表示运行/体验验收通过。
- Numstat：报告 +221/-0；本工单 +42/-0；合计新增 263 行、删除 0 行（两份均为新文件）。
- 工作树：本会话只新增报告与本工单，产品目录无差异、HEAD 未变；保留已有未跟踪文件及过程中观察到的 plan/claude-log 非本会话改动。
- 未做：产品代码/配置/迁移修改；用户库任何读写；读取 `.env`、打印 key；触碰 3001 或启停服务；API/模型/外部账号调用；产品测试、浏览器与主观验收；commit/push/PR/merge；后续施工与设计裁定；修改旧工单、段计划、current-state、agent 指令或生成索引。
