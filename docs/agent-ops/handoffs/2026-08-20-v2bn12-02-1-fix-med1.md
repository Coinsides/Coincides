> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: done | re: v2bn12-02-1-fix | date: 2026-08-20

# V2.BN.12 修正单 02.1:MED-1(调度语义)+ LOW-2(收据 cwd)

## 背景

工单 02 复核 **FAIL(仅 MED-1)**:useState×5 → useReducer 的改写没有机械保留 React 调度语义(render-phase 调度与 functional-updater 求值时机),reviewer 有最终 state 反例。其余全部 PASS(工装/lockfile/RED #1/正控/边界)。复核全文见 02 号工单 ## Review。

## 修正要求

1. **R1[MED-1]——抽逻辑,不抽状态基座**:hook 恢复原有的独立 `useState`(调度语义按定义零变化);两个 pure reducer 保留为**转换计算函数**——各 setter 内部委托 pure transition 计算下一字段值,再经**原生 setState** 应用。等价证明责任反转:不再需要证明 useReducer 等价,因为状态基座未换。若某 transition 无法以此形式委托(涉及跨字段原子更新),如实申报并保留原实现+镜像测试。
   - reviewer 反例场景必须转化为**hook-level parity 测试**收入 test:unit(用 02 已装的 RTL/jsdom):至少覆盖 render-phase 调度与 functional-updater 时机各一例,断言与原生行为一致。
2. **R2[LOW-2]**:回执验证命令一律写成自足可复现形式(含 cwd 或等价 --project 路径)。
3. LOW-1(如有,见 Review)一并处理或申报不改理由。

## 边界与验证

不碰 02 边界之外任何东西;RED #1 必须保持红(不许顺手转绿——那是 03 的活)。全套重跑:test:v2 244 / verify 链 / test:unit(唯一红=RED #1)/ tsc(自足命令)。回执 ## Result 追加进本文件,不自评 PASS;复核将按 Review §9 要求重验全部并查 hook-level parity 收据。

## Result

> builder: Codex | date: 2026-08-20 | branch: `fable/v2-bn12-exoskeleton` | resumed at HEAD: `b8d7ea5b4f5c68c3d128ed60cde4ebdee81de435`

### 1. 中断接续盘点

- 第一次中断为机器崩溃；崩溃前已完成 R1 主体：两个 hook 回迁原生 `useState`、`useDraftBlockController.test.tsx` 写入两项 reviewer 反例 parity、`slashCommandReducer` 收尾为 pure transition functions。
- 第二次中断为上一会话沙箱故障；该会话只完成只读盘点，未取得测试、编译或总门收据。
- 本次从现存工作树继续，先读本修正单与工单 02 `## Review`，再用 CodeGraph（CLI 不在 PATH，改用 MCP）核对调用面，并以当前盘面、`HEAD` diff 与直接读盘复核新文件。未发现需再改的 production/test 残余；本次写操作仅为本回执及 header `ready -> done`。

### 2. diff 范围

写回本 Result 前，工作树共有 **15 个代码/工装文件**：工单 02 原 14 个文件，加本修正单新增的 hook-level parity 文件。均在 02/02.1 边界内。

- tracked 修改 6 个：`client/package-lock.json`、`client/package.json`、`client/src/pages/Notes/canvasEngine/hooks/useDraftBlockController.ts`、`client/src/pages/Notes/canvasEngine/hooks/useSlashCommandController.ts`、`client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx`、`package.json`。
- untracked 9 个：`client/src/pages/Notes/canvasEngine/draftBlockLifecycleReducer.ts`、`draftBlockLifecycleReducer.test.ts`、`slashCommandReducer.ts`、`slashCommandReducer.test.ts`、`surfacePersistenceContract.test.ts`、`hooks/useDraftBlockController.test.tsx`、`hooks/useSlashCommandController.test.tsx`、`client/test/setup.ts`、`client/vitest.config.ts`。
- 写回后另有本修正单 1 个收据文件，故最终工作树相对 `HEAD` 为 16 个路径。未见 schema、server 实现、RC-A/B/C/D、工单 03、Playwright、Tailwind 或构建链边界外改动。

### 3. R1 / R2 / R3 落实

**R1[MED-1]**

- `useDraftBlockController` 的 `draftActive / draftText / creatingDraft / draftFocusNonce / draftLayout` 为 5 个独立原生 `useState`；公开 `setDraftText` / `setDraftLayout` 仍保持 `Dispatch<SetStateAction<...>>` 形状，在原生 setter updater 内委托对应 pure transition。
- `useSlashCommandController` 的 `slashTarget / activeSlashCommandIndex` 为 2 个独立原生 `useState`；target/index 各路径委托 `transitionSlashTarget` / `transitionSlashCommandIndex`。`canvasEngine` 当前 wiring 无 `useReducer`。
- 两个 reducer 文件只保留 pure transition/selector 计算；`slashCommandReducer` 已无聚合 reducer 或 production dispatch 残留。未发现需要申报的跨字段原子 transition。
- reviewer 两个反例已进入 `useDraftBlockController.test.tsx`：same-value 的 render/effect 增量与原生 hook 对照；functional updater 的求值时机与原生 hook 对照并锚定最终值 `one`。slash hook 另有 closed target 重复 clear 的 same-value 0-render 回归。
- 针对性收据：cwd=`D:\Coinsides\v2.x\Coincides\client`，命令 `npm.cmd run test:unit -- src/pages/Notes/canvasEngine/draftBlockLifecycleReducer.test.ts src/pages/Notes/canvasEngine/slashCommandReducer.test.ts src/pages/Notes/canvasEngine/hooks/useDraftBlockController.test.tsx src/pages/Notes/canvasEngine/hooks/useSlashCommandController.test.tsx`，exit `0`；`4 files / 25 tests` 全部通过，其中 draft parity `2/2`、slash hook `3/3`。

**R2[LOW-2]**

- 下列每条命令均写明 cwd；独立 tsc 从 `client` 执行，不再依赖读者猜测 `tsconfig.json` 的解析位置。

**R3[LOW-1]——申报不改**

- 未为新增 lock 节点手填或重生可选 `license` metadata。工单 02 Review 已核对这些节点的 `version / resolved / integrity / dev`、`91/91` 依赖闭包及本地 package manifest license 完整；缺失项不参与 npm 解析或可复现安装。
- 当前接受这项 SBOM/license 收据退化，留待统一 npm lock 生成工具链处理。仅为可选字段用可能不同的 npm 版本重生 lock，会引入与 02.1 无关的供应链 diff 噪声，故本修正单不扩大改动。

### 4. 全套验证收据

1. cwd=`D:\Coinsides\v2.x\Coincides\client`
   - 命令：`npm.cmd run test:unit`
   - exit：`1`（本工单保留的 intentional RED）
   - 关键输出：`Test Files 1 failed | 4 passed (5)`；`Tests 1 failed | 26 passed (27)`。唯一失败为 `surfacePersistenceContract.test.ts > keeps default Page-local layout formal and Page-visible across projection, payload, and hydrate`；原始差异为期望 `payloadSurface: formal_page`、`pageVisibleBlockIds: [default-page-block]`，实收 `payloadSurface: canvas_workspace`、`pageVisibleBlockIds: []`。同文件“真正越出 Page”反向正控为绿；两项 draft parity 与 slash hook tests 均为绿。
2. cwd=`D:\Coinsides\v2.x\Coincides`
   - 命令：`npm.cmd --prefix server run test:v2`
   - exit：`0`
   - 关键输出：`tests 244 / pass 244 / fail 0 / skipped 0`。
3. cwd=`D:\Coinsides\v2.x\Coincides`
   - 命令：`npm.cmd run verify:v2-bn8-runtime`
   - exit：`0`
   - 关键输出：Canvas runtime boundary `159 checks`；Relation freshness contract；Canvas engine model contract `60 groups`；client `tsc -b && vite build` 与 server `tsc`；5 个 performance scenarios；`git diff --check`；changed-file secret scan `15 changed file(s)`，整条链执行完成。
4. cwd=`D:\Coinsides\v2.x\Coincides\client`
   - 命令：`npm.cmd exec tsc -- --project tsconfig.json --noEmit`
   - exit：`0`
   - 关键输出：无诊断输出。

### 5. 未做事项

- 未修 RED #1 对应的 surface persistence root cause；未修 RC-A/B/C/D，也未启动工单 03。
- 未补写 lock 新节点的可选 `license` 字段，理由见 R3 申报。
- 未做 browser journey、服务重启或联网 audit；本修正单没有声明这些验收面。
- 未 commit、未 push、未 PR/merge。

## Review

> 复核者：Codex(reviewer，非 builder) | 日期：2026-08-20 | 分支：`fable/v2-bn12-exoskeleton` | 基线：`b8d7ea5b4f5c68c3d128ed60cde4ebdee81de435`
>
> 本节为洁净室复核结论：只依据 02 / 02.1 工单原文、`HEAD` 对照 diff、当前代码与 reviewer 本轮亲跑收据；不采信 builder 自报绿灯，不改产品代码，不接触 `main`，也不代 Fable 放行。

### 1. 判定

**判定：PASS（仅表示 02.1 修正要求复核通过；放行权仍在 Fable）。** Findings：**0 BLOCKER / 0 HIGH / 0 MED / 1 LOW**；其中唯一 LOW 是工单 02 已记录、02.1 明确申报保留的 LOW-1，**本轮无新增 finding**。

- **R1 / MED-1：CLOSED。** 两个生产 hook 的状态基座已回到原生独立 `useState`；pure transition 只在原生 setter updater 内计算。原 reviewer 的 same-value render 与 functional-updater timing 两个反例均被 hook-level parity 忠实复现并转绿。
- **R2 / LOW-2：CLOSED。** Result 中每条验证命令均写明 cwd；独立 tsc 命令按字面可复现并亲跑为绿。
- **R3 / LOW-1：OPEN，申报不改理由成立。** 新 lock 节点的可选 `license` metadata 仍缺失，性质与等级不变；它不阻断本修正单，但不得写成已修复。
- **RED #1：仍按原理由 RED。** 真越界反向正控仍绿；02.1 没有偷修 03 的 surface persistence root cause。

### 2. diff、分支与边界

1. 当前 `HEAD` 为 `b8d7ea5b4f5c68c3d128ed60cde4ebdee81de435`，分支为 `fable/v2-bn12-exoskeleton...origin/fable/v2-bn12-exoskeleton`；无 staged 变更，未切换或触碰 `main`。
2. 机械集合对照实收 **15 个代码/工装路径 + 1 个修正单收据路径**：6 个 tracked 代码/工装修改、9 个 untracked 代码/工装文件，以及本 handoff。与 02 原 14 文件加 02.1 新增 `useDraftBlockController.test.tsx` 完全一致；`unexpected=[]`、`missing=[]`。
3. 未见 schema、server 实现、RC-A/B/C/D 修复、工单 03、Playwright、Tailwind、构建链改写或其他文档路径。`placementService.ts`、`modePolicyService.ts`、`canvasPersistenceNormalizer.ts`、`canvasObjectRepository.ts` 对 `HEAD` 均无 diff。
4. `NoteWritingSurfaceLayer.tsx:3719-3722` 仍只把原布尔式委托给 `shouldShowEmptyPagePrompt`；参数仍为 raw `sortedBlockCount`，渲染结构与 prompt 语义未改。`git diff --check` exit 0。

### 3. R1 / MED-1 对抗性核形

#### 3.1 状态基座与 pure transition 委托

- `useDraftBlockController.ts:76-84` 是 5 个独立原生 `useState`：`draftActive / draftText / creatingDraft / draftFocusNonce / draftLayout`。公开 `setDraftText / setDraftLayout` 在 `:88-93` 内调用原生 setter，并在其 updater 中委托 `transitionDraftText / transitionDraftLayout`。
- discard、reset、activate、empty-create、persist begin/success/finally 的其余 setter 均以同一形状委托 pure transition（`:114-170,184-227`）；setter 次序、ref 写入、async I/O、focus、callback 与 finally 顺序逐项对照 `HEAD` 未变。
- `useSlashCommandController.ts:101-106` 是 2 个独立原生 `useState`：`slashTarget / activeSlashCommandIndex`。clear、reset、clamp、sync、commit、annotation、move、Escape 与 Ctrl+Enter 路径均在原生 setter updater 内委托 `transitionSlashTarget / transitionSlashCommandIndex`（`:121-154,227-285,308-314,350-386`）。
- 对 `client/src/pages/Notes/canvasEngine/**/*.{ts,tsx}` 全量静态扫描，`useReducer` 命中数为 `0`；没有聚合 reducer 的 production 调用残留。两个 reducer 文件现在只导出字段级 transition / selector。
- 不存在需另行申报的跨字段原子 transition：`HEAD` 本来就是 5+2 个独立 setter，当前同步路径仍按原顺序提交；测试中的聚合 helper 只作 pure model 镜像，并未进入 production wiring。

#### 3.2 parity 测试是否忠实、是否假绿

1. `useDraftBlockController.test.tsx:48-85` 同时比较 subject 与原生 `useState` 的 render/effect 增量，并把原生基线额外钉死为 `{renders: 0, effects: 0}`。它不是只断言最终 state，因此旧 `useReducer` 的额外 render 会直接失败。
2. `useDraftBlockController.test.tsx:88-106` 忠实复现原最终-state 反例：调用 functional updater 时外部值为 `one`，在 React render 前改成 `two`；测试既要求 subject 等于 native，又把 native 最终值钉死为 `one`。旧 reducer 延迟求值会得到 `two`，无法因两边一起错而假绿。
3. `useSlashCommandController.test.tsx:83-97` 另测已关闭 target 再次 clear 的 same-value 零 render，覆盖 slash hook 的同类调度风险。
4. reviewer 另用一次性 mutation probe 复刻上一版 `useReducer` wiring，亲跑实收：
   - same-value：旧 wiring `{renders:1,effects:0}`，原生 `useState` `{renders:0,effects:0}`；
   - functional updater：旧 wiring=`two`，原生 `useState`=`one`。
   这正好触发上述两项 parity 的失败条件。probe 运行后已移除，最终 diff 未扩出 15+1 边界。
5. parity 文件本身不是“禁止 `useReducer`”的语法 lint，也没有穷举所有 queued updater；但本单要求的两个原反例已被强断言覆盖，且生产源码静态扫描已确认七个状态基座均为原生 `useState`，因此不存在本次 MED-1 的 false-green 缺口。

**MED-1 结论：真正修复。** 这次不是以“reducer 返回同一 identity”替代 React 语义，而是由原生 `useState` 本身重新拥有调度与 updater 求值语义。

### 4. R2 / LOW-2 收据 cwd

Result §4 的 unit、server、verify、tsc 每条均显式给出 cwd。reviewer 从 `client` cwd 按字面执行：

```text
npm.cmd exec tsc -- --project tsconfig.json --noEmit
```

实收 exit 0、无诊断。原先“从 repo root 字面执行找不到 `tsconfig.json`”的可复现性问题已消失，LOW-2 关闭。

### 5. R3 / LOW-1

#### LOW-1 — 新 lock 节点缺少可选 license metadata（延续）

**性质：收据完整性 / 供应链元数据退化。等级：LOW。状态：未修，已申报。**

reviewer 重新机械比较 `HEAD:client/package-lock.json` 与 worktree：

- `packages` 为 `158 → 249`；旧 non-root `157/157` 全字段等价，删除 `0`、升级 `0`；旧 root entry 只新增三项申报的 devDependency。
- 新增 `91` 节点；沿 `dependencies + optionalDependencies` 从 `vitest / @testing-library/react / jsdom` 三根解析，`91/91` 可达、orphan `0`、unresolved edge `0`。
- `91/91` 均有 `version / registry resolved / integrity / dev:true`；三项 direct pin 的 manifest 与 lock 均精确为 `vitest@3.2.7`、`@testing-library/react@15.0.7`、`jsdom@26.1.0`。
- `91/91` 新 lock entry 仍缺可选 `license` 字段，但本地对应 `91/91` package manifest 均有 `license` 或 `licenses`。`npm.cmd --prefix client ls --depth=0` 与 `npm.cmd --prefix client ls --all --json` 均 exit 0。

因此 builder 的“不为可选字段用另一 npm 工具链重生 lock、避免扩大 02.1 供应链 diff”理由成立：该缺口不影响当前 npm resolution、integrity 或可复现安装图。但它仍是 SBOM/license 收据的一致性退化，故 LOW-1 原样 carry forward，不宣称关闭。

### 6. RED #1 核形

1. `surfacePersistenceContract.test.ts:27-41,85-100` 把 persisted entity fixture 锁为 `frame.x=0 / width=904 / contentInset.left=72 / pageOffsetX=0`，并先断言 projection 为 `{x:72,width:760,surface:'formal_page'}`；没有借用 runtime fallback `x=-72`。
2. reviewer 只读查询当前 `server/coincides.db`：active PageFrame 主样本组 `x=0,width=904,inset={top:0,right:72,bottom:96,left:72}` 为 `9` 条，fixture 仍与实际 persisted 形状相符。
3. 测试链仍为 `Page-local layout → project → build payload → normalize/hydrate → Page visibility`（`:59-81`）。当前 `placementService.ts:259-282` 把 local `x=0` 投影为 `x=72`；`:305-312` 又按 local `0..760` 边界解释该 projected layout，因 `72+760>760` 重分类为 `canvas_workspace`，Page policy 随后过滤该块。
4. 隔离 RED 实收 `payloadSurface=canvas_workspace / pageVisibleBlockIds=[]`，失败仍落在期望 `formal_page / [default-page-block]` 的断言；原因与 02 Review 完全相同。
5. 反向正控 `surfacePersistenceContract.test.ts:110-121` 先确认 projected explicit surface 仍是 stale `formal_page`，再要求 payload 重分类为 `canvas_workspace` 且 Page 不可见；隔离执行为绿，能拦“永远信 explicit surface”的假修。

**RED #1 结论：保持红，理由不变，正控有效。**

### 7. reviewer 亲跑验证收据

Windows 命令均从 `npm.cmd` 入口执行；本轮未遇 `canvas-asset EPERM`，因此没有启用 `CANVAS_ASSET_DIR` 变通。

| cwd / 命令 | exit | reviewer 实收 |
|---|---:|---|
| `client` — 四份 reducer/hook tests | 0 | 4 files，`25/25` pass；draft parity `2/2`、slash hook `3/3` |
| `client` — 仅 `useDraftBlockController.test.tsx` | 0 | 两项 parity 均 pass |
| `client` — 一次性旧 wiring mutation probe | 0 | 2/2 probe pass；实收旧/native 分别为 `1 render vs 0`、`two vs one`；probe 已移除 |
| `client` — 隔离 RED “keeps default Page-local...” | 1（预期） | 1 failed / 1 skipped；payload 被重分类且 Page 列表为空 |
| `client` — 隔离正控 “reclassifies a truly out-of-Page...” | 0 | 1 pass / 1 skipped |
| repo root — `npm.cmd run test:unit` | 1（预期） | 5 files；4 pass / 1 RED；`26/27` tests pass；唯一失败为 RED #1 |
| repo root — `npm.cmd --prefix server run test:v2` | 0 | `244/244` pass；未触发 EPERM |
| repo root — `npm.cmd run verify:v2-bn8-runtime` | 0 | 159 runtime checks、Relation freshness、60 model groups、client/server build、5 performance scenarios、diff check、16-file secret scan 全绿 |
| `client` — `npm.cmd exec tsc -- --project tsconfig.json --noEmit` | 0 | 无诊断 |
| repo root — lock deep compare + closure audit | 0 | 旧 `157/157` 不变；新 `91/91` 可达；pins/integrity/dev 完整；license 缺口如 LOW-1 |
| repo root — `npm.cmd --prefix client ls --depth=0` / `ls --all --json` | 0 / 0 | direct pins 精确；完整安装树无 invalid |
| repo root — `git diff --check` | 0 | whitespace gate 绿 |

### 8. 5-2 跨条合取 / 耦合扫描

1. **native setter × slash 调用链**：`useRuntimeNaturalWritingController.ts:91-106` 取得 draft controller 的 `setDraftText`，并在 `:151-168` 原样传给 slash controller。于是 slash trigger 清理、writing-role projection 与 draft persist 现在都落回同一原生 setter 基座；公开 `Dispatch<SetStateAction<string>>` 契约未断，target/index 自身也已回迁原生 `useState`。
2. **字段级 transition × async finally**：pure model 的各字段结论与生产中的独立 setter 合取后，没有生成新的原子性假设；同步路径沿用 `HEAD` 的 setter 顺序，pending create / persist 的 ref、focus、callback、finally 顺序未变。`create_finished` 仍只清 `creatingDraft`，不会擦掉 pending 期间重新打开的 local draft。
3. **parity × intentional RED**：四份非 RED 文件 `25/25` 全绿，而整套 unit 有且只有 surface RED；parity 不是被 RED 提前中止或被 runner 漏收。根 verify 脚本不包含 `test:unit`，所以 runtime 总门全绿与 intentional RED 并存是工单设计，不是误吞失败。
4. **surface RED × raw prompt gate**：ghost 仍被 Page policy 过滤，而 `shouldShowEmptyPagePrompt` 仍按 raw `sortedBlockCount=1` 隐藏入口；原 RC-A.5 × RC-B.3 legacy dead-end 合取保持存在。它是工单明确要求留给 03/04 的已知问题，不是 02.1 新 finding；后续仍不得只修一半。
5. **runner × lock ×旧构建链**：Vitest/jsdom/RTL 的 unit、既有 Vite alias、client/server build、model contract 与 performance gate 可同时运行；未发现 runner 配置或新依赖闭包与旧构建链的复合冲突。

跨条扫描未发现会推翻上述逐项 VERIFIED 结论的新复合缺陷。

### 9. 5-1 收据完备与显式范围排除

- 已取得：`HEAD` / branch、完整 15+1 路径集合、tracked diff 与 untracked 文件、CodeGraph callers/blast radius、生产 `useReducer` 全量零命中、parity 定向与旧 wiring mutation、RED/正控隔离、root/client unit、244 server tests、runtime 总门、独立 tsc、lock 深比较/闭包/integrity/license、完整 npm 安装树、只读 persisted PageFrame 样本及 diff/secret gates。没有借用 builder 的绿灯。
- **未做 browser journey / 服务重启**：02.1 只修 React hook 调度语义并保留一个 pure/model RED，没有安装 browser runner、没有声称修复任何 live 用户旅程，也没有服务端改动；重启服务不能为 hook scheduler 或 pure payload classifier 提供更相关的观测，故显式排除 browser/live UX 签收。
- **未执行会改写共享安装树的 `npm ci`**：以 lock JSON 深比较、依赖闭包、resolved/integrity、`npm ls --all` 与实际 build/test 取了等价的只读供应链收据；reviewer 不以重装依赖污染共同施工工作树。
- **未做联网 vulnerability audit**：它不属于 02/02.1 对“旧节点零升级、仅新增测试闭包”的 closure 条件，且不能替代本轮机械 lock 对照。
- 未修 RED #1、RC-A/B/C/D 或 LOW-1；未写 production/reviewer 常驻测试。唯一一次性 mutation probe 已删除，最终工作树仍严格为工单的 15+1。

### 10. 最终边界

02 的测试工装、pure transition 基线、RED #1 与正控仍成立；02.1 已把生产状态基座恢复为原生 `useState`，并以能杀死旧接线的 hook-level parity 关闭 MED-1；LOW-2 关闭；LOW-1 以已申报的供应链元数据收据退化继续保留。

本 reviewer 的 **PASS 不等于发布或合并授权**；checkpoint、放行及后续 03/04 排程仍由 Fable 决定。

## Review-2

> 二级复盘：Claude(Opus 5,工程并行会话) | 日期：2026-08-20 | 分支：`fable/v2-bn12-exoskeleton`
>
> 复盘对象 = **复核的质量**，不重做复核。reviewer 的承重论断逐条自验，不代入信任。上下文链：工单 02 `## Review`（FAIL 判定书）→ 修正单 02.1 → 本 PASS。

### 判定：复核质量 **优**，明显强于上一轮。0 误判 / 0 论证缺口 / 1 条跨状态转移的漏合取 / 2 条流程建议。

---

### ① mutation probe 手法：**本轮最高分项**，附一条流程红线

**手法严谨性：优。** reviewer 没有停在「parity 测试跑绿」，而是**复刻旧 `useReducer` wiring 反向验证测试对它有杀伤力** —— 实收旧 wiring `{renders:1}` vs 原生 `{renders:0}`、`two` vs `one`，正好命中两项 parity 的失败条件。

这是**变异测试**，也是「测试有效性」唯一的机械证明方式。区别很实：
- 常规复核证明的是「新代码让测试变绿」；
- 它证明的是「**旧 bug 回来，测试会红**」。

后者才是回归护栏的定义。**建议写进 reviewer 角色卡：凡「修复类」工单，复核须证明测试对旧接线有杀伤力，而非仅对新代码变绿。**

**边界干净性：自验通过。** `git status --short` 实收 **16 条** = 15 代码/工装 + 本 handoff，与申报完全一致；`useDraftBlockController.ts:76-84` / `useSlashCommandController.ts:101-106` 均为原生 `useState`，无 probe 残留；`canvasEngine/**` 全量扫描 `useReducer` 命中 **0**。

**⚠️ 但 probe 跑在共享施工树上 —— 这是流程缺口，不是本轮事故。**

`git worktree list` 实收只有一个工作树。也就是说 probe 那段「临时把生产 hook 改回旧 wiring」是**直接写在 builder 也在用的那棵树上**。本轮没出事（builder 正好在两次会话之间），但：

reviewer 自己在 §9 里明确拒绝跑 `npm ci`，理由是「**不以重装依赖污染共同施工工作树**」。**同一套卫生标准下，临时改写生产源码比重装依赖危险得多** —— 前者会让并发的 builder 观测到一个不存在的代码状态，或被第三方在 probe 窗口期误提交。这是它自己卫生标准里的一处内部不一致。

**建议角色卡加一条红线：mutation probe 必须在隔离工作树（`git worktree add` / 独立 clone）里做，不在共享施工树上改生产代码。** 成本极低，消除的是一类静默污染。

---

### ② MED-1 CLOSED 的论证链：**无缺口**，且最强的一环是责任反转

自验四点，全部成立：

1. **状态基座**：draft hook 5 个 + slash hook 2 个 = 7 个独立原生 `useState`（`:76-84` / `:101-106`），pure transition 只在原生 setter 的 updater 内计算（`:88-93`）。
2. **无残留**：`canvasEngine/**` `useReducer` 零命中。
3. **parity 不会假绿**：same-value 一例除比对 subject-vs-native 外，**额外把 native 基线钉死为 `{renders:0,effects:0}`**（`:81-85`）；functional-updater 一例除比对外，**把 native 最终值钉死为 `'one'`**（`:105`）。两边一起错的假绿路径被堵死 —— 这两处「绝对锚」是关键，只做相对比对是不够的。
4. **probe 已证其杀伤力**（见①）。

**论证链最强的一环是 reviewer 点出的责任反转**：02.1 不是「证明 useReducer 与 useState 等价」，而是**把状态基座换回原生 useState，于是等价性无需证明**。前者是永远证不完的（React 调度语义没有公开契约面）；后者是定义上成立的。**修正单的方向选对了，这才是 MED-1 能真关而不是"论证到看起来关了"的原因。**

---

### ③ LOW-1 carry-forward：**处置恰当**，但它现在是一笔孤儿债

**恰当在三点**：性质定性准（SBOM/license 收据退化，非解析/完整性问题）；**不宣称关闭**（申报保留 ≠ 已修）；且 reviewer 用机械比对独立背书了「不影响 npm resolution / integrity / 可复现安装」—— 旧 `157/157` 全字段等价、新 `91/91` 可达且 integrity 齐全、本地 manifest 均有 license。builder「不为可选字段用另一 npm 工具链重生 lock、避免引入无关供应链 diff」的理由成立。

**但**：LOW-1 被 carry forward 到了**哪里**？它不在 03 单范围内，也没有独立工单承接。它现在是一笔**被正确记录、被正确降级、却没有承接方**的债。

这不构成对本轮 PASS 的异议 —— 记录本身是对的。只是提醒：**「已申报」不等于「有人会做」。** 建议 Fable 把它挂进待决/技术债清单，与「统一 npm lock 生成工具链」那件事绑在一起，否则它会在若干轮之后变成没人记得来源的历史噪声。

---

### ④ 合取扫描：§8 五条质量高（我上轮的建议已生效），补一条**跨状态转移**的漏合取

**先说好的**：上一轮我建议「逐条核对后追加一次跨条耦合扫描」，本轮 §8 已成建制并抓到五条真耦合 —— 尤其 §8.1（native setter × slash 调用链，证明 `setDraftText` 经 `useRuntimeNaturalWritingController:91-106,151-168` 传给 slash controller 后仍是同一原生基座）与 §8.2（字段级 transition × async finally，`create_finished` 只清 `creatingDraft`、不擦 pending 期间重开的 local draft）。这两条都不是逐条表格能看见的。**建议采纳后第一轮就见效，形制可固化。**

**我补的一条 —— §8 扫的是「当前状态内的耦合」，没扫「状态转移时的耦合」：**

#### 漏合取：`test:unit` 是**没有触发器的回归护栏**

自验：`verify:v2-bn8-runtime` 的完整链为
`check:canvas-runtime-boundary && check:v2-bn11-relation-freshness && smoke:canvas-engine-model-contract && build:client && build && smoke:canvas-engine-performance && git diff --check && check:changed-file-secrets`
—— **不含 `test:unit`**。`test:unit` 也不在任何其他门里。

§8.3 判「根 verify 不含 test:unit 是工单设计，不是误吞失败」—— **对当下完全正确**（RED #1 必须保持红，接进总门会把总门弄红）。但它停在了当下。

**⚠️ 精确说明（避免我自己犯 MED-1 那类"从缺席推断"的错）**：工单 03 的验证段**已明文要求 `test:unit` 全绿**，所以 03 会跑它。我说的不是"03 会漏跑"。

我说的是：**03 之后没有任何工单负责把 `test:unit` 接进常驻门**。届时的状态是 ——
- RED #1 已转绿，接进总门的技术障碍消失；
- 02 投了 14 个文件建起来的 runner 与 parity 测试，**仍然没有任何自动触发器**；
- 这些 parity 测试的存在理由，正是防止「将来某次重构又把状态基座换成 useReducer / 又破坏调度语义」—— 而**没有门跑的回归护栏，等于没有护栏**。

这是三者的合取：**02 的工装投资 × 门接线缺失 × 03 收口后的状态**。单看任一条都没问题，合起来是「花了一整单建的测试，从此无人自动运行」。

**建议**：把「**RED #1 转绿后，将 `test:unit` 接入 `verify:v2-bn8-runtime`**」写成 **03 的交付物之一**（而非仅验证项），或立即开一张最小承接单。**否则它和 LOW-1 一样，是第二笔没有承接方的孤儿。**

> 方法论注记：这类缺口逐条核对看不见，**当前状态的合取扫描也看不见** —— 因为它只在状态转移之后才成立。建议角色卡的合取扫描补一句：**不仅扫"现在耦合了什么"，也扫"本单交付物在下一单收口后会变成什么"**。

---

### ⑤ 给 Fable 终检的两条提醒

1. **checkpoint 入库的是 16 个路径**（15 代码/工装 + 本 handoff），我已独立核对与申报一致。其中 `client/package-lock.json` 带 91 个新节点 —— 入库后 LOW-1 就成了**已提交的历史**，此后再想统一重生 lock 成本更高。可接受，但建议在 checkpoint commit message 里点名 LOW-1，让它在 git 史里留下入口。
2. **总门与 `test:unit` 此刻的绿红状态不一致是设计**（总门全绿、`test:unit` 1 红）。checkpoint 说明里建议写明这一点，否则将来回看会误以为入库时有失败测试没人管。
