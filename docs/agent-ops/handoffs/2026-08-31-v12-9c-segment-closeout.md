> **状态 (Status)**: done(12.9c 全段收口;⛔ **c-3 全段未开单**,如实移交)
> **from**: claude(opus,工程调度会话 coincides-8b) · **to**: fable(裁定) / henry(复核) · **date**: 2026-08-31
> **层 (Layer)**: 段收口 / V12.9c「T1 载体批 + 真转写器接入」
> **上游**: `handoffs/plans/v12-9c-segment-plan.md` · `handoffs/2026-08-29-v12-9c-c1-segment-closeout.md`(c-1 子段收口)

# V12.9c 段收口:插头真插进了插座(⛔ 但只插了一半)

## 1. 交付清单(10 单,全 done)

| 单 | 主题 | 批 |
|---|---|---|
| c-0 | T0 存量对齐带锚碎片流 | `6ba6152` |
| c-1a | MinerU 遗产体检 | `c49c980` |
| c-1a-2 | pin 字节活过 git 往返 | `f127c34` |
| c-1b-0 | 转写器指纹承重件(版本号退出承重位) | `5792417` |
| c-1b-1 | MinerU 接线 | `d3c8661` |
| c-1b-2 | region 保真与诚实的表格粒度 | `4503768` |
| c-1c | 考卷分角色(三级火箭第 1 级) | `d777c36` |
| c-1 | 子段收口 | `ddb6065` |
| c-2 | 双转写分歧普查(实验层,两遍制 v3) | `eac1cc8` |
| c-4-a | §4 从 canonical JSON 重渲 | 本批 |

## 2. ⛔ 段目标达成度对账(⭐ 本收口最该被读的一节)

段计划的目标原文是「碎片流的第一批**真生产者**进场」,并明写「**闸与它的对手同批到位**(b-3 移来的引用校验闸在此落)」。逐条对账:

| 段计划承诺 | 实况 | 判 |
|---|---|---|
| T0 存量对齐带锚碎片流 | **只对齐四种**:`pdf` / `docx` / `txt` / `md`;⛔ `xlsx` / `csv` 仍 `stored_only` | ⚠️ **部分达成**(新入 TD-38) |
| MinerU 接入,region 级 fidelity 首次真实产出 | 转写器已注册进解析器表,region 全有或全无判据已落;⛔ **须 `COINCIDES_PDF_PARSER=mineru` 显式点名才被选中**,本仓两个 env 文件均未设该键 ⇒ PDF 默认仍走 `native-pdf` | ⚠️ **形状与开关俱在,默认路径未变** |
| c-2 双拓互证质量档:Docling 接入 + 置信度落出生证 | **只做了实验层普查**;⛔ `server/src` 生产码中 Docling **零文件**,置信度**未落出生证** | ⚠️ **实验层达成,生产层未动** |
| c-3 识别器引用模式 + **引用校验闸落地** | ⛔ **全段未开单** | ⛔ **未做** |
| 三级火箭第 3 级(定向补刀) | ⛔ 随 c-3 一并未做 | ⛔ **未做** |

⭐ **一句话**:12.9c 让碎片**写得进**了,⛔ 但没让任何人**读得出** —— `getImprintFragmentsByAnchor` 在生产码中调用者仍为零。段计划里那把「闸与它的对手同批到位」的刀,**对手(引用者)没到场,闸也就没落**。

## 3. c-4 本体:词典 / 程序表状态刷新

- **词典**(`current-state/glossary.md`,72 → 101 行):
  - 三条现状栏按 12.9c 现物重写(`转写(带锚)` / `碎片(可寻址单元)` / `引用源(拓印件)`),每句均带 commit 或 `文件:行号`;
  - 新立五条 12.9c 词汇:`存在性差异` / `分类学差异` / `三道门` / `两遍制` / `维度覆盖申报`;
  - ⭐ **常驻断言当场拦了我一次**:原拟写「MinerU 已接入」——`已接入` 正是 `glossary-shape-vs-capability.test.mjs` 的 K-1 禁词之一。**这就是「形状 ≠ 能力」那条法长成门之后的样子**:写的人是立法者本人,照样被拦。
  - 闸与阳性对照均已跑:常驻断言 exit 0;`--self-test-k1` / `--self-test-k2` 各自如期报红后自清。
- **程序表 §2**(`handoffs/plans/v12-9-intake-and-selection-program.md:20-21`):T0 行改写为「只对齐了其中四种」并点名 `xlsx`/`csv` 欠账;T1 行补 12.9c 进度(图像族入表但按 `no-imprint` 不产出生证、MinerU 须显式点名、`pptx`/`zip` 仍 `stored_only`)。

## 4. 欠账盘点(⛔ 段关但账不关)

**入表的**(`current-state/tech-debt.md`):

- **TD-38(本次新增)**:`xlsx` / `csv` 仍 `stored_only`,T0 六格式只对齐四种。⛔ 不是 TD-30 的一部分——TD-30 是「PDF 里的表塌成一条」,这条是「表格格式压根没进链」。
- **TD-37**:`DASHSCOPE_API_KEY` 在 `.env`(长度 35)与 `.env.experiment`(长度 116)各定义一次且值不同 ⇒ **c-3 入场前置**,须 Henry 一句话定夺留哪个。⛔ 复核方只验存在与长度,值从未进入任何输出。
- **TD-30**:未清,已诚实拆分(单元格几何本转写器不可达的部分随触发器另走)。
- **TD-29** ✅ 已清(c-0) · **TD-36** ✅ 已清(c-1b-0) · **TD-31** ⛔ 已关闭。
- **TD-28 / 33 / 34 / 35**:未清,非本段引入,状态不变。

**未入表、随段移交的**:

1. **c-3 债**:`imprint_fragments` 生产码引用者为零;b-3 遗训的**引用校验闸**因此没有落地对象。
2. **c-2 持久层债**:互证置信度**未落出生证**,字段形态随消费形态定,故未先造。
3. **151 个 `non_divergence` 无逐事件收据** —— 按普查程序 §2 第 6 条它是内部完成 disposition、不进四类分歧表;其 seq 仅在 §3.8 的 per-family distinct 集里聚合可见 ⇒ 日后要逐事件复盘须重跑。
4. **P-TEXT 的 772 个事件依赖同一 pinned 运行时**,换 pin 即须重跑,数字不可跨 pin 搬运。
5. **「3 路审计」的原始 transcript 在仓外**,仓内只有结论摘录。
6. ⭐ **C3 / P-NULL 的 `m:n` 条款本轮未被检验**:普查已按 K-7 如实申报「本轮未触发 K-3」。⚠️ 沿用本段立的家法——**未触发 ≠ 正确,只是未经检验**。

## 5. 本段的方法学产出(⭐ 比代码更耐用的那部分)

12.9c 三次停线全部来自**发单方**,且三次都不是同一个错:

1. **c-2 v1 停在配对门** —— 骨架预设「两边都有」,撞上存在性差异即失效(`135f782`);
2. **c-2 v2 停在角色门** —— 四分类全在问「长什么样」和「在哪」,没有一类问「是什么」(`c462a2b`);
3. 二者的共同病根被 Fable 一句话钉住:**骨架比宪法少维**。v0.7.4 判词早已分出三个维度,骨架只实现了一个。

由此立的两条通则已进 `handoffs/README.md`:**「分类骨架必须显式申报维度覆盖」**(⛔ 申报的是射程不是赦免)与 **「普查是点名,判定是判刑;点名必须点完,判刑可以停堂 —— 两者⛔不共用一条停线流」**。第二条的实测收益是**存在域覆盖率 6.5% → 100%**。

⭐ 另一条值得单记的:v2 停线时撞见的 `[heading] × [text]` 全程只有 **2 张**,而真正的大族 `[text] × [list]` 有 **194 张**。**你撞到的第一个,几乎从不是最有代表性的那个** —— 这是本段第二次实证该判语。

## 6. 证据本体与已知瑕疵闭环

- `D:/Coinsides/v12.9-selection/**` 全程只读;c-1c 收尾整树复核为 94,935 文件 / 3,149,405,801 字节,SHA-256 前后一致。
- c-2 单据签署时挂的两条已知瑕疵**均已由 c-4-a 闭环**:①§4 编码破损 ⇒ 从 canonical JSON 重渲,ASCII `?` 由 388 行降为 **0**;②K-8 的 5 MB JSON 归宿 ⇒ 已作 gzip+Base64 非易失内嵌于普查件 §6(7,731 行 Base64 → 440,641 字节 gzip → 5,096,178 字节原文,SHA-256 `2bfc5b892a107cd5fb79e7cbf1cf8f005d3b769b621eaceb3b395dd54f1e0dab`,调度方端到端亲验)。

## 7. 收段基线(⭐ 本次实测,⛔ 非引用历史数字)

| 项 | 命令 | 实测 |
|---|---|---|
| server v2 全量 | `npm --prefix server run test:v2` | **351 / 351 pass**,fail 0,exit 0 |
| 类型 | `npm exec --prefix server -- tsc --noEmit -p server` | exit 0,零诊断 |
| 词典常驻断言 | `node docs/agent-ops/current-state/glossary-shape-vs-capability.test.mjs` | exit 0(K-1~K-3) |
| 词典阳性对照 | 同上 `--self-test-k1` / `--self-test-k2` | 各自如期报红并自清 |
| tech-debt 表格闸 | `npm run check:tech-debt-table` | exit 0(三条历史遗留如实播报,⛔ 未新增) |

⚠️ **口径**:351 与 c-1 子段收口所载的 350 相差 1,是 c-1c 签后当夜(`d777c36`)之后的增量;两个数字同一命令、同一入口,可直接比较。

## 8. 移交

- **c-3 是 12.9c 唯一整单未做的工项**,连同引用校验闸与三级火箭第 3 级一并移交;其**入场前置是 TD-37**(须 Henry 定夺 `DASHSCOPE_API_KEY` 两处定义留哪个)。
- **TD-38** 的落点按「债的落点认第一个生产者」通例待定:须先有一个产 `sheet` 锚碎片的生产者。
- 本段所有单据、普查件与证据摘录均在仓内;⛔ 唯一在仓外的是「3 路审计」原始 transcript。
