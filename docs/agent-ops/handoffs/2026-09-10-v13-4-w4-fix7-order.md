> **状态 (Status)**: done
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-10
> **上游**: Henry 真机现场取证(HQ 亲驾内置浏览器,console 栈+by-note 响应+模块级复算三组对照,claude-log 09-09 §43);修一/五/六的"同 toast 异病"终局
> **单号**: 13.4 走查修七 · F13 auto 宽扩张造溢出→退役闸误伤

# 走查修七 · F13

**真实异常(真机 console,⛔猜测)**:`Error: canvas_workspace_retired at assertNoRetiredCanvasWrite`——与修一/五/六追的"A resolved page frame is required"**完全不同的抛点**,toast 文案相同掩盖了病种。

**根因(HQ 真机实证,证据链完整)**:

1. 存量布局行 `x=88, width=672, surface=formal_page, frame_id 有效`(88+672=760=帧内容宽——**原写入者维持"x+width≤页宽"不变量**;x=88 来自弹窗时代窄 contentWidth 语境);
2. `normalizeBlockLayout`/`normalizeResolvedBlockLayout` 对 v2 page_frame_local **auto 宽**块:width 重算为 `min(DEFAULT_PAGE_CONTENT_WIDTH, contentWidth)`=760,而 x 按 v2 契约保留不夹(F1 规矩,本身正确)→ resolved 几何 x88+w760=848 **凭空溢出** 760 页;
3. `toStoredLayout` 分类器(几何压过陈旧标签)→ crossing → **canvas_workspace** 印上运行时布局;
4. 拖动草稿继承退役面 → `assertNoRetiredCanvasWrite` 正当拒收 → toast。**闸无错,上游造假几何**。
5. 旁证全闭合:合成夹具 x=0 故修一/五/六全绿;Henry 真笔记 x≠0 故必红;tray 绕道=写回新鲜 x≈0 几何故逐块治愈。真机复算对照在案:cw=760 现行链产 crossing/canvas_workspace;x=0 输入则 inside。

## 零 · HQ 已裁(⛔复议)

1. **修法=auto 宽以帧内余量为界**:v2 page_frame_local 非 manual 宽块,width = `min(DEFAULT_PAGE_CONTENT_WIDTH, contentWidth, 帧内容宽 − max(x,0))`;x 保留不夹(⛔动 F1 契约坐标规则)。对 Henry 现物即恢复 672,与存量行一致;x=0 块 width 照旧;
2. **⛔拔/绕退役闸**(它判得对);⛔数据迁移(毒在派生层不在库,归一化治好即全好);
3. **两本边界账调查项(申报裁量)**:`isCanvasWorkspaceBlock` 以 contentWidth 当边界 vs `toStoredLayout` 以帧内容宽当边界——v2 下应统一为**帧账**(contentWidth 只是渲染提示);若统一波及面大,申报方案⛔自行扩面;
4. 零跳变口径:修后 auto 宽块渲染宽从 760 收回 672 = **修正到存量真相**(此变化是治病);保存前后逐像素零跳变照常断言。

## 一 · 验证(单内纪律)

- typecheck + build 全绿;定向回归(canvasEngine 布局/归一化/契约面)不破;
- 冒烟五条:①真机同参合成复现(帧 904/inset 72,存量 x=88 w=672 formal_page,cw=760)——修前拖动保存断言红(`canvas_workspace_retired`)→修后同路径绿,保存 payload surface=formal_page、x=88、width=672,零 collection PUT;②x=0 auto 宽块回归(width=760 照旧,保存绿);③manual 宽块零行为变化;④修五自愈/修六死帧重落籍与释放收集全回归;⑤client 全库整跑⛔过滤。

## 二 · Result 格式

`## Result`:numstat + 五冒烟逐条 + 两本账调查申报 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触(复现全走内存合成);⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。

## Result

**From: codex(builder) · 2026-09-10 · 工程完成，待 HQ 复核/真机验收；不代表放行。**

**实现**：仅改两个生产文件。`useNoteCanvasResolvedLayoutModel` 把当前 `pageFrames` 传给两处归一化；`normalizeBlockLayout` / `normalizeResolvedBlockLayout` 对有有效归属帧的 v2 `page_frame_local` auto 宽，在估高前按 §零1 限制为 `min(DEFAULT_PAGE_CONTENT_WIDTH, contentWidth, frame.width − leftInset − rightInset − max(x,0))`。低于 `MIN_BLOCK_WIDTH` 的余量仍为硬上限；耗尽时最低为 0，避免负宽。有效帧按原 `selectPlacementFrame` 解析，缺帧不猜归属，维持原宽路径。manual/v1/world/workspace/tray 原分支保留；F1 坐标函数、退役闸、分类器、保存归属规则均未改。

**numstat（仅本单；新增文件以 `git diff --no-index --numstat -- NUL <file>` 计，未 stage）**：

| 文件 | + | − |
|---|---:|---:|
| `client/src/pages/Notes/canvasEngine/placementService.ts` | 29 | 3 |
| `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasLayoutModel.ts` | 2 | 0 |
| `client/src/pages/Notes/canvasEngine/hooks/autoWidthFrameSave.test.tsx` | 122 | 0 |
| `client/src/pages/Notes/canvasEngine/placementAutoWidth.test.ts` | 43 | 0 |
| 本工单（含状态前进与 Result） | 38 | 1 |
| `docs/agent-ops/INDEX.md`（状态索引同步） | 1 | 1 |

**五冒烟逐条**：

1. **HQ 真机同参内存合成：先红后绿。** 帧宽 904、左右 inset 72、存量 `x=88/w=672/formal_page/page_frame_local`、有效 frame ID、`contentWidth=760`。用真实 hydration → resolved-layout hook → pointer move/up hook → `saveBlockCanvasPlacementForNote`，仅 API transport 为内存 mock。修改生产码前整跑 client：**87 文件中 1 失败、86 通过；767 测试中 1 失败、766 通过**；唯一失败为此用例，实际异常为 `canvas_workspace_retired`，栈为 `canvasRetirementPolicy.ts:19 → canvasObjectRepository.ts:131 → persistChangedBlockLayouts → pointerup`。同用例修后成功，payload 精确为 `x=88/y=140.25/width=672/surface=formal_page/boundary_role=inside`；恰好一次 block placement PUT，**零 collection PUT**。清掉 draft 后重新 hydrate，保存前后 `resolveScreenRect` 全部数值严格相等（保留小数，不以误差容忍掩盖跳变）。证据边界：这是 jsdom 指针事件与生产投影函数的内存集成断言，不冒充 HQ 真机像素截图。
2. **x=0 auto：PASS。** 同一 pointer/save/reload 链，宽仍 760，payload 为 formal_page，保存前后投影几何严格相等；零 collection PUT。
3. **manual：PASS。** 同链 `x=88/width=321.5/width_mode=manual` 保存重载全保持。既有 v1/v2 DOM 坐标集成与契约套件全通过；未改 manual 宽计算与 F1 坐标保留规则。
4. **修五/六与释放收集：PASS。** 既有文件全部整跑：`coordinateContractSession.test.ts` **12/12**（无存量布局自愈、repository 保存/重载等），`placementContractService.test.ts` **24/24**（死 frame ID 重落籍、主栈回退、v1/坐标约束等），`useBlockPlacementInteractions.test.tsx` **21/21**（未落籍/死帧释放保持、后续收集、crossing/resize/native formal inside-overflow 等）。`coordinateContractIntegration.test.tsx` **2/2**、`surfaceAuthorityContract.test.ts` **16/16** 同绿。
5. **client 最终全库：PASS，88 文件 / 779 测试，零过滤、零跳过。** 原命令 `npm.cmd --prefix client run test:unit`（无文件筛选/名称筛选）；最终输出留在 `.codex-tmp/fix7-client-final.log`。本单新增 3 条拖动保存集成 + 12 条两入口几何回归，覆盖窄渲染提示、窄帧、非首帧匹配、负 x/y 坐标保留、低于最小宽/耗尽余量、先定宽再估高与输入不变。

**其余验证**：`npm.cmd run build:client`（`tsc -b && vite build`）、`npm.cmd run build`（server `tsc` 与 manifest check/copy）均退出 0；模型契约 **60 组**、runtime boundary **159 条**、性能 smoke **5 场景**全通过。既有 registry **5/5**、manifest **10/10**、parity **10/10** 整套通过；tool-face parity、server/shared import、Gallery/Rail/Single Editor shell、Source experience、legacy shutdown、Relation freshness、docs 检查与 diff whitespace 检查通过。构建已有 chunk size/dynamic-import 与递归 schema 提示，无构建失败。Vitest/Vite 使用已有 `COINCIDES_VALIDATION_ENV_DIR` 配置指向本单新建空目录 `.codex-tmp/fix7-empty-env`，未读取项目 env 文件。

**两本边界账调查申报（未实施）**：

- 现物需精确区分：`isCanvasWorkspaceBlock`（`placementService.ts:321`）传 `contentWidth`，但 `classifyBlockSurfaceAuthority`（`:140`）优先已有 `surface_authority.pageBoundary`；仅无该 authority 的 local 行走 `contentWidth`（`:168`）。`toStoredLayout`（`placementContractService.ts:91–103`）按当前 resolved frame 内容宽建边界。差异可能随 hydration/渲染提示或过期 authority 显现，不是共享分类器算法分叉。
- 直接生产调用只有 `modePolicyService.ts:63/119` 两处；间接影响 `useNoteCanvasLayoutModel.ts:103/115` 的 page 可见块、workspace 附帧识别，以及其选择的归一化 surfaceMode，故不能因只有两个调用点而当作无影响小修。旧 `useSurfaceModeController` 路径当前提前退役返回，不应为统一重开。
- **提请 HQ 独立限定工单**：v2 读取侧传 contract + frame context，沿既有帧解析使用当前帧账；预计最小生产面为 `placementService.ts` + `modePolicyService.ts`，hook 已有上下文。需先明确缺帧与过期 authority 的处理，不用 contentWidth/任意首帧冒充归属。v1、退役写闸、F1、持久化与迁移不扩面。本单仅调查，不改该链。

**未做**：未 stage/commit/push/PR/merge；未读 `.env`、任何 key 值或用户数据库；未启真实服务、执行迁移或写用户数据；未设计/新增/派发安全类测试，既有套件未过滤；未运行凭据扫描，留 HQ。未做 Henry 真机主观验收。工作区原有其他文档/配置不归本单，未改写。

**停线**：按本单禁令未启动含 `check:changed-file-secrets` 的 `npm run verify:v2-bn8-runtime` 聚合命令，不能宣称总门完整 PASS；上述获准组成项已单独执行，凭据扫描仍归 HQ。本单停在工程交付与调查申报，待 HQ 复核/真机验收与两本账裁量；不自行统一两本账、不动 F1、不绕退役闸、不触发数据变更。
