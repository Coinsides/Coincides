> **From**: fable
> **To**: codex
> **Status**: done(2026-09-12 状态头补翻:交付与 Result 早已在案,头未跟上;派发期原头:ready;两层制;13.1 单 0)
> **日期 (Date)**: 2026-09-07
> **性质**: 只读侦察单(⛔ 改任何产品代码)

# 13.1 · 单 0 · K-0 视口侦察(只读)

## 〇 · 任务

按段 plan(`plans/v13-1-paper-viewport-plan.md` 单 0)完成五问侦察,产出报告文件 `docs/agent-ops/analysis/2026-09-07-v13-1-k0-recon.md`。**只读单:除该报告文件外⛔ 新建/修改任何文件。**

## 一 · 五问(每问要文件:行号级证据,⛔ 印象陈述)

1. `CanvasViewport.zoom` 的全部消费者,按【渲染变换/文本与布局测量/命中判定/其他】四类归档成表;
2. 文本测量族(averageCharWidthPx、行高计算等)与 zoom 的现有耦合点逐一列举;若零耦合,以调用链证据申报「已解耦」;
3. 导出管线的 scale 来源:是否已走 `createPageFramePrintProfile`;导出与屏显的比例通道是否同源;
4. page/canvas 双模式的视口控制器分叉点:13.1 只动 page 侧的最小切口在哪个文件/哪层;
5. 三档档位状态的天然存放点:现有 viewport 持久化通道(存哪、何时写、按 note 还是按会话)。

## 二 · 报告格式

报告文件带标准状态头(active/分析层);按五问分节;末节「拆单建议」:单 1(档位机)与单 2(度量解耦)应先后还是可并行、各自的最小改动面清单。完工在本工单末尾追加 `## Result`(报告路径+五问一行摘要各一条+未做清单);apply_patch 落盘;⛔ commit(工作树交 HQ)。

## 三 · 边界

⛔ 改产品代码 · ⛔ 跑 build 以外的长命令(本单无 build 义务)· ⛔ 读 .env · ⛔ 打印任何 key。现物与段 plan 冲突(如三档设想与现有视口机制根本不容)⇒ 照常写报告并在「拆单建议」节申报冲突,本单性质为侦察,无停线。

## Result

> **完成日期**: 2026-09-07 · **执行者**: Codex 侦察员 · **性质**: 静态只读取证，待 HQ 冻结后续单口径；原 status/正文保留。

报告：[2026-09-07-v13-1-k0-recon.md](../analysis/2026-09-07-v13-1-k0-recon.md)（active/分析层，文件:行号证据及检索射程在报告内）。

1. **zoom 消费者**：已按渲染变换/文本与布局测量/命中判定/其他四类列全表，区分活入口、转递、未接线 helper、默认写入与旧 LearningCanvas 通道。
2. **度量耦合**：纯文字估算、clientWidth/scrollHeight 采集及高度回写主链已解耦；另列 resize 的 profile 漏传和文本命中/caret 浮层坐标风险，后两者为源码推导、未浏览器复现。
3. **导出 scale**：print profile 已进入模板与 runtime 几何，屏显与 Preview 输入同源；A4/Letter 现有内部宽均为 904，未定位到最终 PDF 输出链，不能申报物理100%或最终导出保真。
4. **模式切口**：现物为 page 固定1、canvas 自由缩放；单1需新增 page 状态/呈现通道，并同步 runtime、固定内层布局宽及输入坐标边界，不能仅放开现有 zoom。
5. **状态与持久化**：Notes viewport 仅组件实例 React 内存态，无按 note 恢复通道；旧 SQLite 表按 user+LearningCanvas、未接现役 Notes。三档宜放 page 专用视图状态并显式带 note 作用域。

拆单建议：先由 HQ 冻结物理单位、固定布局盒、Custom 族与 profile 优先级、档位恢复及输出入口；纯函数可有限并行，公共接线顺序集成（建议单2输入接稳→单1呈现/交互→单3）。最小改动面及四项现物冲突见报告末节，未施工。

验证摘要：CodeGraph/rg 均不可用后采用定向 PowerShell 检索；544 个源码文件内 zoom 命中71行（含非消费者）；报告136处路径引用、51个文件、351个行号均存在且未越界，报告尾空白0行；导出结论独立只读复核无必改点。

改动统计：报告新增159行/删除0行；本工单新增20行/删除0行，仅末尾 Result；均以 apply_patch 落盘。

未做：未运行 build/typecheck/测试/浏览器/导出或查询用户数据；未改产品代码、原工单正文/status、权限配置及其他文件；未读 .env 或凭证材料、未输出密钥；未 commit/push/PR/merge。开工既有修改保持原状，工作树交 HQ。
