> **状态 (Status)**: draft(Fable 骨架稿 2026-08-29;⚠️ 依赖 12.9b 全段落地——**逐单派发前须按「候锁重对现物」家法对 HEAD 重核**)
> **层 (Layer)**: 段计划 / V12.9c「T1 载体批 + 真转写器接入」
> **上游**: `v12-9-intake-and-selection-program.md` §2/§5/§7 · `v12-9b-anchored-fragment-stream-spec.md`(v0.3.1)· 12.9a 第 1/2 场档案

# V12.9c 段计划骨架:插头开始上插座

**段目标**:碎片流的第一批**真生产者**进场——T0 存量对齐 + MinerU 接入 + 识别器引用模式落地。**闸与它的对手同批到位**(b-3 移来的引用校验闸在此落)。

## 单序(草案,派发前逐单重对现物)

- **c-0 T0 对齐首单**(TD-29/30/33 钉定落点):现役 pdf/docx/xlsx/csv/txt/md 解析产物对齐带锚碎片流(页级 fidelity 起步,诚实申报);**顺带清 TD-29**(说谎的 .d.ts)与 **TD-30**(表格经 .data 消费,killer=含表 fixture 碎片数>1);TD-33(包导入空指纹)按其条目处置。
- **c-1 MinerU 接入**(uv 工具箱 → 子进程调用 → 碎片流落库):**region 级 fidelity 首次真实产出** ⇒ b 段欠下的「region 语义验证」在此还账(回程票式 killer,验证者与生产者同批);出生证 text_normalization 按实申报;**三级火箭第 1 级(考卷分角色)在此实装**。
- **c-2 双拓互证质量档**(三级火箭第 2 级):Docling 备选接入 + 碎片级互证 + 置信度落出生证;普通单拓/教材级双拓的档位开关。
- **c-3 识别器引用模式**(qwen-vl-max,2b 生产形态):候选产物引用碎片 id + **引用校验闸落地**(b-3 遗训:`id ∈ 该拓印件碎片集`,违者拒收留痕);三级火箭第 3 级(定向补刀)同单或尾随。
- **c-4 段收口**:词典/程序表状态刷新(b-4 同式),欠账盘点。

## 预挂账(骨架期已知,拆单时核)

- Python 子进程调用的进程管理(超时/僵尸/并发上限)——**转写是征用时动作,天然低频,先简单后优化**;
- c-1 的考卷:Henry 私料(扫描/中文卷)到位则**确认赛与接入同批跑**(§7 保留条款);未到位则官方样题先行、确认赛记欠;
- 识别器 API 计费走 `.env.experiment` 直至选品终审落章,终审后迁正式配置。

## 边界(段级)

⛔ 不碰 MCP 工具面(Agent 读碎片的门归 12.9d)· ⛔ 不碰旧链(TD-34 收敛债照挂)· 每单安全词零(纯工程词汇)。
