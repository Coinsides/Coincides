> **状态 (Status)**: active(V12 收官作战图,委派层调度依据)
> **层 (Layer)**: 计划
> **日期 (Updated)**: 2026-08-30
> **维护**: Fable(总部,只改此图);执行归并行会话(调度)+ Codex(施工)

# V12 收官里程碑(M1–M5)

**总则(Henry 2026-08-30 令)**:快速收官 V12 进 V13。总部(Fable 会话)只出设计/规格骨架/判据/抽检;工地活(施工/测试/监视/复核)全在委派层。**里程碑间不空转**:一颗收口即回报总部并自动接下一颗,⛔ 不停车等批(升级三情形除外:设计级 finding / 止损触发 / 步骤收口随报随走)。

## M1 · c-2 实验层(在飞)

工单已在 handoffs(裁定 §9);已移交并行会话派工监视。复核判据以工单为准。

## M2 · 12.9c 收口(c-4)

c-2 落地后:汇总 + 铸段。
- **c-3(识别器引用模式)不挡收口**——其触发器(`imprint_fragments` 出现真引用者)未开,按「没有消费者就是在猜形状」记债随触发器走(Fable 裁定 2026-08-30,判断点在 claude-log 候 Henry 复核);
- c-2 持久层(置信度字段)同随消费形态,不挡。

## M3 · 12.9d 检索地基

BGE-M3 工作假设落地(嵌入工序+存储);嵌入终选收据(真实检索采纳,≥50 组标注集)**延至 V14**。
⚠️ 开工前需总部出规格骨架——M2 收口回报时一并向总部催要。

## M4 · 12.10 打磨节

1. Template Studio 降级切口(Henry 已裁:运行时留、工作室删 ~4600 行 + 6 路由 + 18 表;TD-33 债随码销);
2. i18n 中英统一小单(英文为准,中文进翻译档);
3. 走查 4a(居中不中);
4. OD 一号实验(样式三档①最小闭环;前置:OD 锁版 + `telemetry.content` 关;实验单草案在可行性报告 G 节,**执行前找总部翻牌**);
5. **冻结纪律**:凡触碰画布野地 / 跨界(crossing)语义的修补一律冻结不修——纸与板方向档已判该族语义 V13 退役,不修将死之物。

## M5 · V13 开版

前置 = 纸与板施工蓝图(总部在写,与 M3/M4 施工并行)。M4 尾声若蓝图未就绪,向总部催单。

## 调度纪律

- 单 builder 姑息模式(本机 CPU 退化嫌疑,小步勤落盘);**动代码全归 Codex**,Claude 会话不亲手改产品代码;
- 回报小格式:里程碑 / 判据绿红 / 异常 / 需裁定项,⛔ 不贴全文 diff;
- 红线沿用:不 push、key 值零出境(只验存在性)、不改 agent 权限档、不杀 PID 8292、中性工程措辞。
