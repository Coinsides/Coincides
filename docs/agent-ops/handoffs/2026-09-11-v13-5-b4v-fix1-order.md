> **状态 (Status)**: done（builder 工作树交付；待 HQ 复核放行）
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-11
> **单号**: 13.5 · B4v 修一 · Board 夹具台账补记书签 GET
> **上游**: HQ 总门+隔离复验:B4v 书签栏挂载期 GET(viewport-bookmarks)未被既有 Board 测试夹具登记,chalk 2+tools 1+unboxing 4 共 **7 例隔离复现红**(⛔负载 flaky,deterministic);B4v 自身 bookmarks 测试另有全库负载敏感 1 例(隔离绿,记档不修语义);E5-fix1 同族病二犯

# B4v-fix1 · 台账补记

## 零 · 裁定

1. 挂载期书签 GET=合法设计⛔惰性化凑测试;**全 Board 夹具普查**(BoardPage.* 全族+repository/hook 夹具),逐一显式登记该 GET(返回 `{bookmarks:[]}` 或按测试需要),台账严格性零松动;
2. 逐文件修后:**先隔离跑全 Board 族证绿,再跑 client 全库证绿**(bookmarks 自测若仅全库负载红,独立复验绿即申报归 flaky 名录候 13.6);
3. **常备条款(自本单起写进所有含挂载期新请求的工单)**:凡新增挂载期网络请求,交付前必须对消费该组件的**全部**既有测试夹具做台账普查并补记——定向网⛔以"直接相关"自限。

## 一 · 验收

隔离 Board 族全绿;client 全库绿(或仅存名录内负载超时且独立复验绿);受影响静态门绿;Result=补记位置清单+普查证据+测试数字。⛔动产品代码;⛔碰 .git。

## Result

2026-09-11 · Codex builder。完成 11 处既有 GET handler 补记，返回 `{ bookmarks: [] }`；保留全部未知请求 `throw` / `fail`、请求台账、写入断言与测试超时。未修改产品代码、未回滚 B4v、未执行 git commit 或其它 Git 写操作。未发现合并冲突或施工期间的源文件并发冲突。

### 补记位置

| 文件（仓根相对路径） | 补记行 | 约束 / 消费者 |
|---|---:|---|
| `client/src/pages/Boards/BoardPage.chalk.test.tsx` | 82 | 仅当前 `path` 的书签 GET |
| `client/src/pages/Boards/BoardPage.item.test.tsx` | 99 | 仅 `item-board` |
| `client/src/pages/Boards/BoardPage.modal.test.tsx` | 71 | 仅 `board` |
| `client/src/pages/Boards/BoardPage.smoke.test.tsx` | 192 | 完整 pathname 匹配，先经 `detailFor` 验证 Board 存在，再返回空书签；置于既有 `/boards/` 泛匹配之前，避免错回 BoardDetail |
| `client/src/pages/Boards/BoardPage.staging.test.tsx` | 57 | 仅 `board` |
| `client/src/pages/Boards/BoardPage.unboxing.test.tsx` | 113 | 仅 `board`；`unknownRequests` 及四例空台账断言保留 |
| `client/src/pages/Boards/boardTextRangeClipboard.test.tsx` | 104 | 仅 `board`；真实 BoardPage 消费者 |
| `client/src/pages/Notes/canvasEngine/hooks/useTrayRelocation.test.tsx` | 98 | 仅当前 `board.id`；两处延迟 GET 覆盖仍转发该基础 handler |
| `client/scripts/boardToolsSmoke/mockApi.ts` | 87 | 仅 `BOARD_PATH`；覆盖 layers / selection / snapping / tools 四份测试及 tools / layers 两个浏览器入口 |
| `client/scripts/boardOpenNoteSmoke/mockApi.ts` | 225 | 仅 `BOARD_ID`；open-note 浏览器入口 |
| `client/scripts/boardTextRangeSmoke/mockApi.ts` | 115 | 仅 `state.boards` 中存在的 Board；text-range 浏览器入口 |

伴随修补及生成件同步单独记账：

- `client/scripts/boardOpenNoteSmoke/mockApi.ts:64`：扩展夹具类型检查发现该旧 Board 数据缺 B3 的 `identity_item_id` / `identity_description`，补为 `null`，与其它 Board 夹具一致。
- `docs/generated/object-inventory.md:25`：总门发现上游 B4v 的 `066` / schema 已在工作树，而生成清单尚未同步。运行既有 `node scripts/docs-inventory.mjs`，新增 `board_viewport_bookmarks` 行，活表 92→93、schema 声明 66→67、回填 46→47，后续序号顺延；生成器、schema、migration 均未修改。
- `docs/agent-ops/INDEX.md`：本工单翻为 done 后运行既有 `node scripts/docs-index.mjs`，仅同步本工单状态行。

### 普查证据与分母

先尝试 CodeGraph；本环境 CLI 不可用且无可调用 MCP，`rg` 亦不在 PATH，故用 Node `fs.readdirSync` 递归枚举，再检查导入、挂载和 handler 转发路径；另由只读子代理独立普查并复核补丁。

| 射程 / 谓词 | full | processed | remaining |
|---|---:|---:|---:|
| `client/src` 文件枚举及消费路径扫描 | 524 | 524 | 0 |
| 其中全部测试文件 | 133 | 133 | 0 |
| `client/src/pages/Boards` 测试逐项处置 | 23 | 23 | 0 |
| 其中 `BoardPage.*.test.tsx` | 11 | 11 | 0 |
| 实际挂载 BoardPage 的测试（含目录外 / 非同名） | 13 | 13 | 0 |
| 实际书签栏测试消费者（上行 + 独立书签栏自测） | 14 | 14 | 0 |
| `client/scripts` 文件枚举及消费路径扫描 | 65 | 65 | 0 |
| 其中挂载 BoardPage 的浏览器夹具入口 | 4 | 4 | 0 |
| 测试及浏览器夹具的基础 GET handler | 13 | 13 | 0 |

13 个基础 handler = 本单补齐 11 个 + B4v 两个已提供书签响应的自测 handler。目录枚举与处置清单双向核对：未列出的 Board 测试 0，清单中不存在的测试 0。

`pages/Boards` 23 份测试逐项处置（本表文件名均相对此目录）：

| 文件 | 处置 |
|---|---|
| `BoardPage.bookmarks.test.tsx` | 已登记，55–59 行精确区分 Board / bookmarks，延迟覆盖转发；保持 B4v 自测原样 |
| `BoardPage.chalk.test.tsx` | 补记 |
| `BoardPage.item.test.tsx` | 补记 |
| `BoardPage.layers.test.tsx` | 通过共享 tools handler 补记 |
| `BoardPage.modal.test.tsx` | 补记 |
| `BoardPage.selection.test.tsx` | 通过共享 tools handler 补记 |
| `BoardPage.smoke.test.tsx` | 补记，修正该 GET 原先被宽路径分支回错数据的情形 |
| `BoardPage.snapping.test.tsx` | 通过共享 tools handler 补记 |
| `BoardPage.staging.test.tsx` | 补记 |
| `BoardPage.tools.test.tsx` | 通过共享 tools handler 补记 |
| `BoardPage.unboxing.test.tsx` | 补记 |
| `boardTextRangeClipboard.test.tsx` | 补记，真实挂载 BoardPage |
| `BoardViewportBookmarks.test.tsx` | 36 行已有按需书签数据，65 / 240 行分别断言 board-a / board-b 精确 GET；延迟、失败及切换场景保持原样 |
| `boardRepository.test.ts` | 无挂载；直接方法响应与候选读取 handler 不发书签 GET，无需增加请求 |
| `useBoard.test.tsx` | repository mock + renderHook，无书签栏 |
| `useBoard.history.test.tsx` | repository mock + renderHook，无书签栏 |
| `BoardRelocatedVisual.test.tsx` | 仅 repository.get / useBoard，无书签栏 |
| `BoardDeleteDialog.test.tsx` | 仅删除框，无书签栏 |
| `BoardList.test.tsx` | 仅列表，无书签栏 |
| `BoardNewNoteDialog.test.tsx` | 仅新建笔记对话框，无书签栏 |
| `BoardNoteModal.test.tsx` | 仅笔记 modal，无 BoardPage |
| `BoardNoteModal.rangeSession.test.tsx` | note / range runtime handler，无 BoardPage |
| `boardSnapping.test.ts` | 纯算法，无网络请求 |

跨目录补齐 `useTrayRelocation.test.tsx`，其路由真实挂载 BoardPage。四个浏览器入口为 `boardToolsSmoke/fixture.tsx`、`boardLayersSmoke/fixture.tsx`、`boardOpenNoteSmoke/fixture.tsx`、`boardTextRangeSmoke/fixture.tsx`；layers 的启动器复用 tools transport，故对应三份 transport。

另沿 repository 的间接消费链检查 `useNoteCanvasRuntimeController.test.tsx`、`useNoteRouteSaveBoundary.test.tsx`、`useTrayController.test.tsx`、`NoteRuntimeDocumentLayer.test.tsx`、`NoteTrayInteraction.integration.test.tsx`、`NoteTraySidebar.test.tsx`、`pageFrameAlignment.test.tsx`，以及 `cFix1Smoke`、`pageFrameHealingSmoke`、`pageReadingSmoke`（fixture / overview / print / tray）、`paperInkSmoke` 的浏览器入口。它们挂载 Note/runtime/tray 或仅引用类型，不挂载书签栏，故无需凭空登记书签请求。调用归属证据：`BoardViewportBookmarks.tsx:39,45` 拥有书签列表请求及挂载 effect；`useBoard.ts:104` 仅读取 Board 详情，`useTrayController.ts:65` 仅读取 Board 列表。

### 验证数字与边界

按工单顺序先隔离 Board，后 client 全库；无更改测试超时、断言或产品代码。

| 验证 | 命令 / 范围 | 结果 |
|---|---|---|
| 修前复现 | `npm run test:unit --prefix client -- src/pages/Boards/BoardPage.chalk.test.tsx src/pages/Boards/BoardPage.tools.test.tsx src/pages/Boards/BoardPage.unboxing.test.tsx --reporter=dot` | 复现 7 例失败：chalk 2、tools 1、unboxing 4；错误为额外书签加载 alert / 未登记 GET 台账 |
| 修后 Board 隔离全族 | `npm run test:unit --prefix client -- src/pages/Boards src/pages/Notes/canvasEngine/hooks/useTrayRelocation.test.tsx`（另开 JSON reporter） | **24 文件 / 196 测试全部通过**，22.35s；其中 BoardPage 全族 **11 文件 / 90 测试** |
| 默认全库首轮 | 根 `npm run verify:v2-bn8-runtime` 的 client 首步 | 132 文件通过、1 文件失败；1452 通过、1 超时，32.11s。unboxing 首例超默认 5000ms；原 7 项请求失败已消失，bookmarks 自测本轮通过 |
| 超时项独立复验 | 单跑 `BoardPage.unboxing.test.tsx` | **1 文件 / 4 测试全部通过**；首例 978ms，文件测试 2296ms |
| client 全库复跑 | `npm run test:unit --prefix client -- --maxWorkers=4`（另开 JSON reporter） | **133 文件 / 1453 测试全部通过**，41.03s；只限制 worker 并发，默认 5000ms 超时不变 |
| 受影响扩展类型检查 | `node client/node_modules/typescript/bin/tsc -p .codex-tmp/b4v-fix1/tsconfig.fixtures.json --noEmit` | **通过**；该临时配置继承 client tsconfig，并纳入四个 Board 浏览器夹具、client/src、shared |

首轮 unboxing 超时如实留档，**未直接认定为既有名录内 flaky，也未改自测语义**；本单以随后 133/133、1453/1453 的全库绿交付。

总门其余工程项按根脚本顺序逐项执行：tool-face registry / manifest / parity、server-shared runtime import、canvas / group-gallery / groups-rail / single-editor / source / legacy-shutdown / freshness 边界、canvas model contract、client/server build、canvas performance 均通过。`docs:check` 首次在上述 B4v 生成清单处失败，生成后完整重跑通过；`git diff --check` 与 changed-file secret scan 通过。**这是默认总门首轮失败后逐项复验收齐，未宣称未经修改的总门命令单轮全绿。**

证据保存在本地 `.codex-tmp/b4v-fix1/`：`baseline.json`、`repro.log`、`board.json` / `board.log`、`verify.log`、`unboxing-recheck.json`、`client.json` / `client.log`、`remaining-gates.json` / `remaining-gates.log`、`fixture-types.log`、`docs.log`。开工基线覆盖 `client/src`、`client/scripts`、`server/src`、`shared` 共 **897 文件**；收尾比对的源文件差异仅上表 **11 个夹具**，既有 B4v 产品实现及其两个 client 自测字节未变。
