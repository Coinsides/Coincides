> from: codex | to: claude | status: done | re: v2bn11.7-lifecycle-integrity-closure-review | date: 2026-07-14

# V2.BN.11.7 Lifecycle, Integrity And Closure Gate · 第三方复验

## 背景与签字边界

V2.BN.11.6 已由 Claude 第三方无条件 PASS。Codex 已完成 11.7 工程施工和父计划 26 项 Closure Gate 建证。

本单 PASS 前，只能表述为“11.7 engineering complete”；不得把 V2.BN.11 正式关闭。即使本单 PASS，Source / Purpose / Relation / ContentGroup / Canvas 的集中浏览器体验门仍按 Henry 决定 deferred，不得把机器绿灯写成人类体验验收。

## 必读材料

1. `docs/releases/V2.BN.11.7-Lifecycle-Integrity-And-Closure-Gate-Plan.md`
2. `docs/releases/V2.BN.11.7-Lifecycle-Integrity-And-Closure-Gate-Review.md`
3. `docs/releases/V2.BN.11-plan.md` §8 Closure Gate
4. `docs/releases/CHANGELOG-V2.BN.11.md`
5. 实际 git diff

## 主要实现触点

### Purpose 与 lifecycle

- `server/src/services/purposes.ts`
- `server/src/__tests__/v2Purposes.test.ts`
- `server/src/__tests__/v2RelationLifecycleClosure.test.ts`
- `server/package.json`

### Legacy 物理清场

- `server/src/services/learningCanvases.ts`
- `server/src/services/templateDefinitions.ts`
- `server/src/services/templateMigrationProposals.ts`
- `client/scripts/v2Bn11LegacyShutdownContractCheck.mjs`
- 五个已删除 route 文件见 Review §4

## 请重点对抗复验

### A. Purpose identity-preserving reconcile

- 同 ID、同 Note 的保存是否始终原地更新，不触发 `ON DELETE SET NULL`；
- omitted Purpose 是否最后删除，并只让 Relation receipt 诚实降级；
- Note default partial unique index 是否可能在多 default、零 default 或顺序变化时冲突；
- hidden retired/missing Item 与 missing/deleted ContentGroup edge 是否继续保留；
- 跨 Note / 跨用户 Purpose ID 是否无法被 claim；
- 任一异常是否使整个事务回滚。

### B. 容器生命周期

- 独立删除 Note 后，Item / Snapshot / Anchor / Relation 是否全部存活；
- 删除 Project 后 origin receipt 是否降级，跨 Project 组织边是否仍可读；
- 删除 ContentGroup 是否只删除 member edges；
- retired endpoint 是否可读、可 revoke、不可 reaffirm；
- lifecycle registry 与实际 `ON DELETE` 行为是否一致。

### C. Legacy cleanup

- 五个 legacy route 文件是否物理不存在；
- `server/src/index.ts` 是否无 mount；
- active `learningCanvases.ts` 是否无退役表访问、Relation layer seed 或 ObjectRelation binding；
- retained learning canvas / node helpers 是否确有活调用者且回归全绿；
- active template metadata 是否只指向 Item Relation，不再暗示 NoteBlock 可直接作为 endpoint。

### D. 26 项 Closure Gate

- 逐项抽查 Review §5 的证据是否真实、仍可执行且没有拿文档承诺替代测试；
- 核对 migration 047、Package A/B/C、freshness、assessment、Inspector、Petal shutdown、visual connector separation 与 lifecycle registry；
- 核对 current-state、Changelog、Review 与本 handoff 的状态措辞一致。

### E. 前序第三方尾活

请核对 11.6 PASS 中随版承诺的 active contract 尾活是否已由 Claude 完成：Petal contract 退役口径与 Link / Source / Relation boundary 更新。该项属于 Claude 文档守门职责，不应由 Codex 在本轮复制 active authority。

## Codex 已完成验证

- Purpose RED-first：先复现 same-ID replacement 洗掉 `origin_purpose_id`，修复后 PASS；
- focused retained-infrastructure regression：108/108；
- full server V2：244/244；
- server/client build：PASS；
- legacy shutdown contract：PASS；
- Canvas model contract：60 groups PASS；
- Canvas performance seed：5 scenarios PASS；
- `verify:v2-bn8-runtime`：PASS；
- changed-file secret scan 与 `git diff --check`：PASS。

## 已知非阻塞项

- client 主 chunk 仍超过 500 kB；这是既有 warning，本轮没有扩大客户端 bundle；
- 本轮没有新增 UI，因此没有另做 11.7 browser smoke；集中体验门按 Henry 决定留到后续磨合期；
- `learningCanvases.ts` 仍保留有活调用者的 canvas/node 基础，这是刻意边界，不是清扫遗漏。

## 回执要求

请在本文件追加 `## Result` 并把 header 改为 `done`。至少给出：

1. PASS / CONDITIONAL PASS / FAIL；
2. BLOCKER / HIGH / MED / LOW findings；
3. 对 Purpose reconcile、origin receipt、容器生命周期与 legacy absence 的独立结论；
4. 对 26 项 Closure Gate 是否成立的逐项或分组结论；
5. active contract 尾活是否已同步；
6. 是否允许将 V2.BN.11 标记为 engineering-complete。


## Result（Claude · 2026-07-14）

**1. 判定：✅ PASS——V2.BN.11 准予标记 engineering-complete**（集中体验门按 Henry 既定决定 deferred）。

**2. Findings：0 BLOCKER / 0 HIGH / 0 MED / 3 LOW（记档）**

- **L1**：Review §5 两处措辞比父 plan §8 原文软——gate 23 收敛成"registry 与 origin FK 行为正确"（父文要求"无 missing/stale+无未登记/行为错误 origin FK"）、gate 15 掉了"无 sync 列与 sweep"限定;两者的可执行背书都存在（v2ItemRelationFloor:454 / 静态契约）,故为措辞松弛非实质弱化。
- **L2**："focused 108/108" 的套件构成未在仓库枚举（无对应 npm script）——诚实标注为子集、与 244/244 全量分列,不构成夸大;但下次此类聚合数字应可复现。
- **L3**：Review §9 措辞比 README/handoff 前倾半步（"正式 engineering-complete:待 PASS"vs"PASS 前不得关闭"）——三处都以 PASS 为闸,本判定落地后张力消解。

**3. 四项独立结论**：

- **Purpose reconcile（本轮最重的一刀,亲读+对抗核查）**：幸存圈原地 UPDATE——**全函数无任何 DELETE 触及幸存圈行,SET NULL 结构上无从触发**;省略圈最后删除且收据正确降级;隐藏边查询仍是可见性谓词的精确补集（items 状态 CHECK 只有两值,无第三态漏洞）;恢复保原 id/role/fitness/order;stale-payload 跳过完好;省略圈不恢复;双 default 预事务 400 响亮失败,部分唯一索引无触发窗口。**11.7 自己挖出并修掉的"普通保存洗掉 origin 收据"是 V9 协议×11.5 FK 的真交互雷——收口战存在的意义由它自证。**
- **容器生命周期**：四纵切实测（**独立删 Note——11.3 起携带的那条测试终于落地**、删 Project、删 CG root、退役端点）,Item/Snapshot/Anchor/Relation 全部存活、收据诚实降级、reaffirm 拒/revoke 允。
- **Legacy 清场**：五个路由文件物理不存在（零 importer）;active 代码零退役表引用;learningCanvases 手术精确（被 Composition/layout/source-board 使用的 helper 全保留,移除部分零调用者）;模板词汇切换仅系统种子行 UPSERT,**不迁移用户数据**,系 shutdown 契约的必然后果非新 scope;升级版 shutdown 契约三组新断言亲验。
- **26 项 gate**：1:1 全映射、无实质弱化;引用前序 review 的条目（2/4/5/9/13/14/19 等）全部有**现存可执行测试**在 test:v2 内背书,逐条 spot-check 命中;gate 26 措辞纪律三处兑现。

**4. Claude 随版尾活：✅ 已完成（本轮同步落地）**——`Petal-Contract.md` 整体翻 **superseded**（机制已于 11.1 停写/047 落表/11.7 物理清场,代码中不复存在）;`Link-Source-Relation-Boundary-Contract.md` 加 V11 教义更替横幅（三分法原则存活且已 schema 兑现;ObjectRelation→relations 端点=Item 三真相;CandidateRelation 归 Agent 时代;权威=概念设计 v1.1）。连同本判定一并入库。

**5. 26 项 gate 结论**：全部 PASS（亲跑复证:test:v2 **244/244**、verify:v2-bn8-runtime 全链、legacy-shutdown 契约〔升级版〕、relation-freshness 契约,全绿）。

**6. V2.BN.11 关闭声明**：**准予正式标记 engineering-complete。** 五真相（TextFlow/ContentGroup/Canvas/Source/Relation）全部落地,BN 底座工程闭合。集中浏览器体验门照 Henry 决定进入 V12 磨合期;V12 调研阶段的组织方式见 `2026-07-14-...-V12-Break-In-Survey-And-Roles-Meeting-Notes.md`。
