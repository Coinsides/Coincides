> **状态 (Status)**: done（builder施工回执；HQ复核/完整总门待接）
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-09
> **上游**: Henry 走查④第四批(纸上移块保存失败,绕道 Staging 一趟才能存);F1(修一)射程边界 + F5(修二)半成品遗患的合流
> **单号**: 13.4 走查修五 · F11 半成品笔记的页框自愈(落页先于落籍)

# 走查修五 · F11

**症状(Henry 实测,全页 NoteDetail)**:直接拖动 paragraph 块 → toast "Failed to save block layout",每次必炸;先把块移进 Staging(托盘)再移出来,之后就能正常保存。

**根因(HQ 静态勘定,builder 须先复现红证实)**:渲染与保存用的是**两套页框集合**——

1. 笔记**没有持久化的 page frame collection**(entity 无、legacy metadata 也无)。来源:F5 时代"三建一存"分步创建中断的半成品(修二只治了新建,存量半成品还在);`CourseDetail.handleCreateNote` 同样是两步建(POST note → PUT collection),中断也产半成品。
2. 渲染端:`useNoteCanvasFrameModel` 用 `normalizePageFrameCollection(null, { fallbackPageFrame: createRuntimePageFrame(...) })` 合成内存默认帧 → 白页照常渲染、能打字能拖。
3. 保存端:`persistBlockLayout` → `saveBlockCanvasPlacementForNote` → `normalizeBlockLayoutForSave(layout, pageFrameCollection=null, 'v2')` → frames=[] → 落籍循环空转、primary 兜底也空 → `requireStoredLayout` 抛 "A resolved page frame is required to save this coordinate contract" → toast。修一的落籍没错,它只是**无帧可落**。
4. Staging 绕道为何治好:tray 面写入豁免帧要求(能存),回落纸面的流程沿途把集合持久化了 → 笔记转正 → 此后直接保存全通。(此环 builder 在复现中核实确切触发点并申报。)

## 零 · HQ 已裁(⛔ 复议)

1. **修法=写时自愈(落页先于落籍)**:v2 契约下,canvas placement 写入口发现持久化集合缺失时,**先把当前渲染在用的运行时集合持久化**(`savePageFrameCollectionForNote`),成功后再落籍保存布局。单点治愈:自愈落在统一咽喉(`resolveCanvasPlacementWriteContext` 或 adapter 同级),块布局/generic object/mount 各写入口同受益,⛔ 逐入口复制粘贴。
2. **铸的=渲的,零跳变铁律**(F1 补遗二同款):自愈持久化的集合必须与当前渲染的 `runtimePageFrameCollection` 逐字段一致,保存前后块的屏幕矩形逐像素不变。运行时集合怎么送达保存边界(prop/ref 桥)由 builder 裁量,申报即可。
3. **⛔ 打开即写**:零写断言保持——打开半成品笔记(含 Overview)不产生任何写;自愈只在用户第一次真实写动作(拖块保存等)时触发。
4. **⛔ 绕过只读闸**:source projection 只读笔记的写仍被 `assertSourceProjectionNoteContentWriteAllowed` 拒绝,自愈不得先斩后奏(顺序:确认本次写本身被允许,才允许铸集合)。
5. **⛔ 松动 v2 契约**:frames 仍空(自愈失败/被拒)时照旧抛错,⛔ 存 frameless 布局;collection 持久化失败则整次保存报错,⛔ 只存块不存页。
6. **⛔ 动用户库**:复现全走内存合成;⛔ 扫库迁移存量半成品(写时自愈逐本治愈即可)。

## 一 · 验证(单内纪律)

- typecheck + build 全绿;定向回归(canvasEngine + Notes + tray/Staging)不破;
- 冒烟五条:
  1. **复现红先行**:内存合成半成品笔记(有 note 有块、无 entity collection、无 legacy metadata)→ 全页 NoteDetail 拖块保存 → 修前断言必失败(即 Henry 的 toast 路径);修后同路径成功,`page-frame-collection` PUT 恰一次,块 layout 带 frame_id 且该 frame 在铸的集合内;
  2. **零跳变**:自愈保存前后,被拖块与其余块的屏幕矩形逐像素不变;
  3. **健康笔记回归**:已有集合的笔记拖块保存,⛔ 多余 collection PUT;
  4. **Staging 绕道回归**:tray-in/tray-out 流程不破;半成品笔记 tray-in 写入也走同一自愈(或申报为何豁免);
  5. **零写与只读**:打开半成品笔记(含 Overview)零写断言;source projection 只读笔记自愈不落地、写仍被拒。

## 二 · Result 格式

`## Result`:numstat + 五冒烟逐条 + 未做 + 停线;**⛔ commit**;⛔ 读 .env;⛔ key 出境;⛔ 用户库接触;⛔ 安全类测试(既有套件整跑⛔过滤;凭据扫描留 HQ)。若复现推翻 HQ 根因假设(比如 Henry 的笔记其实有集合),**停线举证⛔自行改判**。

## Result

> **From**: codex(builder) · **日期**: 2026-09-09（Toronto）
> **结论**: **STOP / 未修复**。冒烟①修前红已实证；同一合成样本的普通纸面 Staging 绕道与 HQ 根因第4点不符，依本单停线条款停止施工，等待 HQ 复核。未自行改判 Henry 的真实笔记状态。

### numstat（本轮自己的改动）

产品源码 `client/src` / `server/src`：**0 / 0**。以下新增 fixture 均未跟踪、未 stage；以 `git diff --no-index --numstat -- /dev/null <file>` 计量。本工单开工前已是 untracked、原文39行，因此工单只计本轮追加量，不把原单算作本轮新增。

| + | - | 文件 |
|---:|---:|---|
| 120 | 0 | `client/scripts/pageFrameHealingSmoke/fixture.tsx` |
| 9 | 0 | `client/scripts/pageFrameHealingSmoke/index.html` |
| 110 | 0 | `client/scripts/pageFrameHealingSmoke/mockApi.ts` |
| 36 | 0 | `client/scripts/pageFrameHealingSmoke/start.mjs` |
| 4 | 0 | `client/scripts/pageFrameHealingSmoke/tsconfig.json` |
| 60 | 0 | 本工单，仅追加 Result |
| 339 | 0 | 本轮合计 |

工作期间另见 `docs/agent-ops/claude-log/2026-09-09.md` 出现他方 +9/0；未读取该增量、未修改，未计入本轮。

### 五冒烟逐条

1. **冒烟①：修前红已取得；修后未做。** 内存 note=`f11-incomplete-note`，两段合成 paragraph，`metadata={}`、entity collection=null，契约 v2。使用真实全页 NoteDetail 的 provider/controller/ChromeLayer/DocumentLayer，仅替换 HTTP 为内存 Axios adapter；无后端、无数据库。点击第一段使 Move block 手柄出现，真实鼠标拖动手柄约54屏幕像素后，出现 `Failed to save block layout`。再点只读 `Assert smoke 1`，返回：
   ```text
   FAIL smoke 1: Expected 1 collection PUT, received 0;
   No successful block placement PUT;
   Saved block layout lacks a frame_id in the stored collection;
   Observed Henry toast: Failed to save block layout
   ```
   浏览器错误记录时间 `2026-09-10T01:52:20.040Z`，异常为 `A resolved page frame is required to save this coordinate contract`，栈为 `requireStoredLayout → normalizeBlockLayoutForSave → saveBlockCanvasPlacementForNote → useNoteCanvasDataAdapter`。集合 PUT=0、布局 PUT=0；运行时第一块 y 从60到109.81224489795918，持久化布局未变、collection仍null。之后点击采样按钮导致正文 blur 保存的 `PUT /note-blocks/f11-block-1` 单独记录，未将其误算作布局成功。

2. **冒烟②：未做。** 未实施自愈，因此没有保存前后逐字段集合全等或全块屏幕矩形逐像素相等的结论。fixture 预留真实 DOM 采样与 collection PUT 暂停/放行按钮；未将预留能力计作验证通过，未实施 runtime 集合到 adapter 的桥。

3. **冒烟③：未做。** fixture 含 `?healthy=1` 正常集合样本，但触发停线后未跑健康笔记拖动回归。

4. **冒烟④：修前实走发现停线反证。** 同一页面、同一块，未点页框面板、未切 canvas：展开 Staging，点真实 `Stage`。tray-in 成功，恰1次布局 PUT，payload 为 `surface:'tray', frame_id:null, x:0,y:0,width:0,height:0,order_index:0`；collection PUT仍0、collection仍null，Staging显示1项。随后真实鼠标把该项拖到纸面空白处：
   - document capture 记录到真实 `drop`，类型含 `application/x-coincides-tray-placement`，值为 `f11-placement-1`；目标是纸面 DIV，`data-canvas-surface-mode='page'`、`data-page-frame-id='primary-page-frame'`、`data-source-content-read-only='false'`。
   - drop前后 API calls 都为18，**新增请求0**；collection仍null，块仍为tray，Staging仍显示1项，未回到纸面。
   - 只读源码对应：`NoteWritingSurfaceLayer.tsx:3252/3317` 生成带运行时frame_id的drop布局；`useTrayController.ts:128` 却用输入的持久化collection=null解析authority；`useRuntimeNaturalWritingController.ts:180` 找不到显式frame_id后返回null，tray-out提前结束。tray-in的 `useTrayController.ts:85` 直接走repository，豁免来自 `placementContractService.ts:128`。
   - 本样本未发现“沿途铸集合”的触发点。静态存在的另一路是点击页框面板 → `useRuntimePresentationController.ts:207` → 保存集合，但该额外动作未混入本次复现，也未拿它补齐 HQ 假设。

5. **冒烟⑤：仅取得部分修前观察，未判 PASS。** 初次打开记录集合/布局 PUT=0，所有 PUT/DELETE=0；但已有加载链还调用了1次 `POST /source-anchors/generate`（本样本带合成course_id）。mock仅回空对象，不能据此宣称生产加载链“任何写均为0”。未实走Overview，未验证自愈后的只读拒绝；遵守禁令，没有设计、新增或派发安全类测试，也未过滤运行既有测试。

### 复现入口与证据边界

- 启动：`node client/scripts/pageFrameHealingSmoke/start.mjs`。
- 打开：`http://127.0.0.1:5182/scripts/pageFrameHealingSmoke/index.html`；`Capture evidence`将调用payload、内存存储状态、toast/drop收据写入 `[data-f11-evidence]`，`Assert smoke 1`只读取并断言，不代替真实拖动。
- Vite使用 `configFile:false, envFile:false`，产品API模块映射为内存mock；`/api`返回503，没有配置真实后端。仅合成数据，没有接触Henry的笔记或用户库。
- CodeGraph索引存在，但CLI不可用且本会话无对应MCP；先尝试后才用限定路径源码检索。browser-harness因本地Chrome端口文件读取权限失败，后改用已提供的CUA Chrome连接完成实走。fixture服务已停止。
- fixture实际执行了真实客户端手势和保存边界；未执行生产server路由，不能据此推断Henry笔记是否确实缺集合，也不能宣称服务端数据验证已通过。

### 未做

未修改产品源码，未实现自愈、迁移、扫库或桥接；因停线未跑typecheck、build、Notes/canvasEngine/tray回归、完整既有套件或 `npm run verify:v2-bn8-runtime`，未宣称验证门通过。总门末尾含凭据扫描，本单未执行扫描，留HQ。未读.env或任何key值，未触碰用户数据库，未执行commit/stage/push，未修改agent指令或权限配置。`git diff --check`已跑，无空白错误；未跟踪fixture另以numstat计量，此检查不等于编译或测试。

### 停线

**已停线，不标done、不自行改判。** 直接拖动的缺帧失败支持HQ第1–3点；但普通Staging往返并不治愈且tray-out无请求，直接反证第4点的完整路径解释。HQ需先复核根因解释和实际操作前提，再决定后续施工依据。Henry真实样本未读取，未知仍为未知；本回执不把“合成样本反证”升级成对真实样本的新根因判定。

## HQ 复核(2026-09-09):停线收货,第 4 点改判,施工继续

1. **冒烟①红收货**:根因 1–3 点(半成品笔记/渲染合成帧/保存无帧可落)由真实手势+真实保存边界证实,转正;
2. **第 4 点 HQ 改判(builder 反证成立)**:纯半成品笔记的 tray-out 死于 authority 解析(`useTrayController.ts:128` 用持久化 collection=null;`useRuntimeNaturalWritingController.ts:180` 返 null 提前终止),**零请求**——"绕道沿途铸集合"不存在。纯半成品笔记=保存死胡同,无任何用户可达的自救路径,病情比原判更重,修法(写时自愈)更有必要;
3. **Henry 真实样本另案取证**(HQ 向 Henry 收:console 报错原文 + `GET /canvas-objects/by-note/<id>` 响应 + 确切手势序列);真实样本若是"集合在而块 frame_id 陈旧"等 lesion B,**另开单,⛔ 本单射程**;builder ⛔ 猜真实样本;
4. **补遗一(入本单射程)**:tray-out 的 drop/authority 解析改用**运行时集合**(与渲染同源),⛔ 另造第二套解析——使半成品笔记的 tray-out 能到达保存边界,由同一写时自愈落库;
5. **冒烟④改写**:半成品笔记 tray-in 照旧成功;修后 tray-out 真实回纸、块落籍到铸的帧、collection PUT 恰一次(tray-in 若先触发自愈则 tray-out 不再重复 PUT,申报实际次序);健康笔记 tray 回归不破。其余冒烟按原单(①含修后绿,②零跳变,③健康笔记,⑤零写与只读)继续;
6. 冒烟⑤观察到的加载链 `POST /source-anchors/generate`:记账候查,⛔ 本单处置。

**裁定:按原修法 + 补遗一继续施工,做完追加 Result 二。**

## Result 二

> **From**: codex(builder) · **日期**: 2026-09-09（Toronto）
> **结论**: **F11 原修法 + 补遗一施工完成，交 HQ 复核；非放行判定。** 前轮 STOP 已按 HQ 复核解除。冒烟①–④通过；⑤取得客户端观察和静态守卫证据，保留 HQ 已登记的加载 POST 例外与服务端动态验证未做边界。完整 runtime 总门未宣称通过。

### 实现与范围

- 单点自愈落在 adapter 的 `resolvePlacementWriteContext`：复用现有 repository context；v2 且持久化集合缺失时，先校验当前已加载笔记与 runtime 收据 noteId 一致、source projection 可写，再 await `savePageFrameCollectionForNote`，成功才进入原布局归一与写入。空帧仍走既有拒绝，集合失败不写布局，后续用户写可重试。
- 桥接 = frame model 原始 `runtimePageFrameCollection` → presentation 返回 → runtime controller 的 `useLayoutEffect` → adapter 中带 noteId 的 ref。未拿 chrome 拼装集合冒充渲染源，未新增另一套集合生成或 authority 解析。挂载/Overview 的桥接仅更新内存引用。
- 块/草稿布局、generic object（含 mount）及 Staging 共用该 context；后台 finalize recovery 显式关闭自愈，旧笔记恢复无当前渲染授权时仍保留原有失败/待恢复行为。每个 note 共用一次 in-flight collection PUT；成功缓存也拦住晚到 GET(null) 再铸；显式页框编辑等待这次自愈后再写，其新状态不被旧响应覆盖。
- tray-out 继续使用 `resolvePageDraftSessionAuthority` 与 `reconcileHydratedBlockLayoutSurfaceAuthority`，仅将输入换成同源 runtime 集合。tray-in 本轮也接同一自愈，并无豁免分支。
- 冒烟②首次发现旧 `buildLayoutPayload` 将实拖 y=109.81224489795918 取整为110，造成约0.22屏幕像素移动。已让 repository 显式传递 contract/frames，v2 写保留分数几何；v1 仍保留原取整。未放宽 v2 的 frame_id / page_frame_local 要求。

### numstat（仅本轮增量，未 stage）

产品与测试用 `git diff --numstat -- client/src`；前轮已存在的未跟踪 fixture 及工单，以开工时 `.codex-tmp/f11-baseline/` 副本作 `git diff --no-index --numstat`，不重算前轮新增。工单含本次状态头从 ready 改为 done（施工回执）及追加本节。

| + | - | 文件 |
|---:|---:|---|
| 5 | 1 | `client/src/pages/Notes/canvasEngine/canvasObjectRepository.ts` |
| 65 | 30 | `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.ts` |
| 15 | 1 | `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasRuntimeController.ts` |
| 2 | 0 | `client/src/pages/Notes/canvasEngine/hooks/useRuntimePresentationController.ts` |
| 11 | 4 | `client/src/pages/Notes/canvasEngine/hooks/useTrayController.ts` |
| 3 | 1 | `client/src/pages/Notes/canvasEngine/placementService.ts` |
| 238 | 0 | `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.test.tsx` |
| 1 | 0 | `client/src/pages/Notes/canvasEngine/hooks/useTrayController.test.tsx` |
| 1 | 0 | `client/src/pages/Notes/canvasEngine/hooks/useTrayRelocation.test.tsx` |
| 1 | 0 | `client/src/pages/Notes/canvasEngine/layers/NoteTrayInteraction.integration.test.tsx` |
| 24 | 5 | `client/scripts/pageFrameHealingSmoke/fixture.tsx` |
| 4 | 2 | `client/scripts/pageFrameHealingSmoke/mockApi.ts` |
| 65 | 1 | 本工单，本轮状态头及 Result 二 |
| 435 | 45 | 本轮合计 |

产品源码小计 **101/37**，既有测试增量 **241/0**；server 源码 **0/0**。前轮 fixture 的 start.mjs / index.html / tsconfig.json 未改。工作期间他方的 claude-log、v14 plan、其他未跟踪文件均未改动、未计入。

### 五冒烟逐条

1. **① 红转绿：PASS。** 修前红复用上一 Result 已由 HQ 收货的真实 Move 手柄拖动与 `requireStoredLayout` 异常。修后重新挂载纯内存半成品（note+两 paragraph、entity collection=null、metadata={}、v2），真实拖动约54屏幕像素；holding 状态下 collection PUT 未完成、placement PUT=0；释放后恰1次 collection PUT、1次成功 placement PUT，frame_id=primary-page-frame 且在已铸集合内，无 Failed to save block layout。正文 blur 的独立 PUT 单列，不当作布局收据。
2. **② 零跳变：PASS。** collection 请求暂停时采样，再释放并读取最终 DOM。runtime-before / PUT collection / runtime-after 逐字段严格全等；两块外框（ARTICLE[data-note-block-shell]）和正文 textarea 的 x/y/width/height 均严格全等，无容差、无取整后比较。外框实测：块1=(543.0531005859375, 333.418212890625, 823.893798828125, 47.6990966796875)；块2=(543.0531005859375, 457.22247314453125, 823.893798828125, 47.699127197265625)，前后原值一致。先前精度红见上方实现说明，修后重新实拖采样。
3. **③ 健康笔记：PASS。** `?healthy=1` 使用已有集合，真实同幅拖动成功，保存 y=109.81224489795918、frame_id=primary-page-frame，collection PUT=0。
4. **④ Staging（按 HQ 改写）：PASS。** 新半成品真实 Stage → 托盘条目1 → 真实拖回纸面 → 托盘0。实际次序：正文 flush PUT → **collection PUT一次** → tray placement PUT → tray-out formal_page placement PUT；tray-out 未重复铸集合，回纸 layout 带 primary-page-frame。健康笔记同样实走 tray-in/out，collection PUT全程0。另加 `?staged=1` 合成初态（首块已在tray、集合仍null），打开/展开时集合PUT=0；真实拖出后，按 collection PUT → placement PUT 顺序各一次成功，块回纸、tray=0。此额外样本直接覆盖补遗一原先的零请求死胡同，不依赖 tray-in 先治愈。
5. **⑤ 零写与只读：限定证据，不声称字面全量零写。** 新半成品打开时 collection/layout PUT=0、全部PUT/DELETE=0；进入真实 Page overview 后新增请求0、collection仍null。原加载链 `POST /source-anchors/generate` 仍一次，按 HQ 第6条记账候查，未改、未隐去。合成 `?readonly=1` 的 source_projection 笔记显示 Source locked，正文/标题只读，Layout/New PageStack/Move block/Save block 禁用；选中正文后仍无PUT、集合仍null。新增自愈代码在collection PUT前检查既有source policy；服务端 `canvasObjects.ts` 的 collection/placement/object 三条 PUT仍先调用 `assertSourceProjectionNoteContentWriteAllowed` 再保存。**后者仅静态核对，未执行服务端动态拒写/对抗性测试，未将客户端mock说成server验证。**

### 验证命令与结果

- 完整 client 既有套件：**84 files / 740 tests PASS**（adapter 52条；最终运行开始22:16:51，15.79秒），无过滤。使用既有 Vitest 配置，programmatic `startVitest('test', [], { run: true }, { envFile: false })` 禁用.env加载。
- client `tsc -b`、Vite `build({ envFile: false })`、fixture 独立 `tsc -p scripts/pageFrameHealingSmoke/tsconfig.json`：PASS。首轮两个 nullability 编译错误已修并复验。
- server `tsc --noEmit` 与原 `npm run build`：PASS；未启动server。仅既有 bundle大小 / schema递归 / React Router警告。
- 既有 canvas runtime boundary **159 checks PASS**；model contract **60 groups PASS**；performance **5 scenarios PASS，14.48ms**。未过滤安全条目、未运行凭据扫描。
- 新增6条普通持久化回归：原样先落页+分数几何、未完成PUT的并发共享、晚GET(null)的去重、集合失败后无布局PUT且可重试、generic+mount共享自愈、显式新页框编辑排在自愈后且状态不倒退。未设计安全类新用例。
- 最终日志：`.codex-tmp/f11-client-unit.log`、`f11-client-typecheck.log`、`f11-client-build.log`、`f11-fixture-typecheck.log`、`f11-server-typecheck.log`、`f11-server-build.log`、`f11-canvas-runtime-boundary.log`、`f11-canvas-model-contract.log`、`f11-canvas-performance.log`（均在同目录）。`git diff --check` PASS。
- 浏览器全程内存 Axios adapter，真实 NoteDetail controller/layers + CUA Chrome 鼠标手势；fixture入口仍为 `node client/scripts/pageFrameHealingSmoke/start.mjs`。`?hold=1` + Release collection PUT + Check saved geometry 可复核②，Assert smoke 1按healthy状态断言集合次数；fixture没有数据库，/api保持503，Vite configFile/envFile均false。收尾已停止fixture服务并关闭冒烟标签页。

### 未做

- **未运行完整 `npm run verify:v2-bn8-runtime`**：其尾部含本单明禁的凭据扫描；未改脚本、未把拆项通过冒称总门通过。完整总门/凭据扫描交 HQ 承接。未另行执行server测试套件或source projection动态拒写测试；具体边界见⑤。
- 未读取.env或任何key值，未接触用户数据库，未迁移/扫库，未访问、猜测或修理Henry真实样本；lesion B仍另案。未stage/commit/push，未改agent指令、权限或安全配置。
- 未动HQ已登记的source-anchors加载POST；未作主观验收/放行。

### 停线

前轮根因第4点反证已由 HQ 收货改判，本轮未再触发“推翻根因”的施工停线；修复与允许范围内验证已完成。**交付停在 HQ 复核/完整总门承接处，不跨越放行权。** 冒烟⑤的加载写例外和服务端动态证据缺口如上，均未偷换成PASS；真实样本未知仍为未知。
