> **状态 (Status)**: done
> **From**: fable(HQ) · **To**: codex(builder)
> **日期**: 2026-09-11
> **单号**: 13.5 · E5 修一 · 请求台账测试补记封面元数据 GET
> **上游**: HQ 总门(verify:v2-bn8-runtime 首步 test:unit)咬出:`BoardPage.unboxing.test.tsx` 严格请求台账断言(剩余请求=[])被 E5 笔记挂载新增的 `GET /notes/:id/tags`、`GET /notes/:id/metadata` 击穿(4 例失败,断言型非超时)

# E5-fix1 · 台账补记

## 零 · 裁定

1. E5 的挂载期 tags/metadata GET=**合法设计**(封面 chips/折叠条首屏需要),⛔改成惰性来凑测试;
2. **台账严格性零松动**:⛔把断言改成"忽略未知请求"——在期望清单里**显式补记**这两类 GET(按测试的现有记账形式);
3. **全射程排查**:grep 全 client 测试中同型"剩余请求=空/严格请求清单"断言,凡挂载笔记 runtime 的逐一补记,⛔只修 unboxing 一处;
4. 修完跑 client 全库必须全绿(unboxing 若再超时,独立复验并申报——超时归 flaky 名录,断言归本单)。

## 一 · 验收

client 全库绿(或仅存 flaky 名录内超时且独立复验绿);受影响静态门绿;Result 申报:补记位置清单+全射程 grep 证据+测试数字。⛔动产品代码;⛔碰 .git。

## Result

2026-09-11 · codex(builder)。本单测试修复完成；client 全库 **131/131 文件、1435/1435 测试通过，0 失败、0 超时**。受影响静态门与构建通过。另申报：完整 `verify:v2-bn8-runtime` 已实跑，最终因既有 E5 工作树的生成库存过期返回 1，**不申报总门全绿**，详情见下。

### 补记位置与严格性

- `client/src/pages/Boards/BoardPage.unboxing.test.tsx:125`：显式登记 `GET /notes/new-note/tags`，返回 `{ tags: [] }`。
- 同文件 `:126–129`：显式登记 `GET /notes/new-note/metadata`，返回完整 `upstream: { sources: [], notes: [], count: 0 }` / `downstream: { boards: [], content_groups: [], count: 0 }`。
- 两个精确 URL 分支共用原有 `response()` 克隆方式；未改 `fail()`、未知请求收集、`:221/:236/:249` 三处 `unknownRequests === []` 断言（覆盖 4 条用例）。未扩大 URL 匹配范围、未过滤未知请求、未改挂载期加载行为或任何产品源码。
- 全 client 排查与独立只读复查均确认：**需新增记账的实际 fixture 仅上述一份**。以下逐项核对覆盖了其他严格台账，未把「全库绿」当成无需排查的理由。

### 全射程 grep 证据

先枚举整个 `client`：**131 个 `*.test.*` / `*.spec.*` 测试文件 + 1 个测试支持文件 `client/test/setup.ts`**；排除依赖/构建缓存目录 `node_modules`、`dist`、`.vite`、`coverage`。文件全集入档 [test-file-inventory.txt](../../audits/2026-09-11-e5-fix1/test-file-inventory.txt)。`.codegraph` 存在，但 `codegraph explore` 不可执行且无 callable MCP；`rg` 亦不在 PATH，实际使用 Git 自带 GNU grep。

可重跑的两组同型台账检索（无 head 截断）：

```powershell
$auditGrep = 'C:/Program Files/Git/usr/bin/grep.exe'
$auditArgs = @('-R','-n','-E',
  '--exclude-dir=node_modules','--exclude-dir=dist','--exclude-dir=coverage',
  '--include=*.test.*','--include=*.spec.*')
& $auditGrep @auditArgs '(unknownRequests|unexpectedRequests|remainingRequests|requestLog|readCalls|apiCalls|requestHistory|[.]get[.]mock[.]calls|expect.*[.]get.*toHaveBeenCalledTimes)' client
& $auditGrep @auditArgs '(Unexpected|Unmapped|Unhandled|Unknown|unexpected|unmapped|unhandled|unknown).*(GET|read|request|endpoint)|remaining.*(request|call)|expect.*(requests|Requests|writes|Writes).*(toEqual|toStrictEqual)' client
```

第一组 **40 行 / 13 文件**，第二组 **70 行 / 23 文件**，并集 **28 个候选文件**；原始结果分别入档 [read-ledgers.txt](../../audits/2026-09-11-e5-fix1/read-ledgers.txt)、[unknown-ledgers.txt](../../audits/2026-09-11-e5-fix1/unknown-ledgers.txt)。另对上述 132 个文件逐一传给 `grep -HniE` 作不区分大小写的交叉扫描：台账/拒收模式 **54 行 / 22 文件**、所有精确/次数/数组断言与 `mock.calls` **2500 行 / 126 文件**、runtime/封面入口 **168 行 / 32 文件**。完整模式、文件命中集、计数见 [grep-receipt.json](../../audits/2026-09-11-e5-fix1/grep-receipt.json)，对应原始输出同目录保存。

28 个候选逐一结论（路径相对 `client/src/pages/`；行号为修复后的工作树）：

| 文件 | 位点 | 核对与处置 |
|---|---|---|
| `Boards/BoardPage.unboxing.test.tsx` | 125–129、221、236、249 | 真实 modal/full-page runtime + 封面；已补两类 GET，4 例共用 |
| `Boards/BoardPage.smoke.test.tsx` | 63–102、134、203、458、588、737 | 笔记路由为只组装 data/layout/reading hooks 的 `PaperProbe`；Enter note 到该探针，双击对象为 content group，不挂封面；无需补记 |
| `Boards/BoardNoteModal.rangeSession.test.tsx` | 25、127、184 | runtime 被替换为 adapter 探针，不挂封面 |
| `Boards/BoardPage.modal.test.tsx` | 12、76、197、246 | modal 被替换；次数只计 `/boards/board` |
| `Boards/BoardPage.staging.test.tsx` | 12、63 | modal 被替换 |
| `Boards/BoardPage.chalk.test.tsx` | 82、178、252 | chalk/standalone item 路径，不开笔记 |
| `Boards/BoardPage.item.test.tsx` | 35–38、48、104、214 | 笔记跳转目标为自定义 `NoteDestination` |
| `Boards/boardTextRangeClipboard.test.tsx` | 44–66、104 | 笔记路由为 `SourceEditor` 探针 |
| `Boards/BoardPage.layers.test.tsx` | 172、240 | 仅写入台账 |
| `Boards/BoardPage.selection.test.tsx` | 145、186、244、265 | 仅写入空台账 |
| `Boards/BoardPage.snapping.test.tsx` | 58、164、168 | 仅写入空台账 |
| `Boards/BoardPage.tools.test.tsx` | 209 | 仅 POST members 台账 |
| `Boards/boardRepository.test.ts` | 176、184、203、228 | repository 单测，无封面挂载 |
| `Boards/BoardDeleteDialog.test.tsx` | 96 | board repository 精确读取清单 |
| `Boards/BoardRelocatedVisual.test.tsx` | 77、79、86 | relocated visual 加载次数 |
| `Boards/useBoard.test.tsx` | 97、107、111、180 | board hook/repository 读取次数 |
| `AgentMemories/AgentMemories.test.tsx` | 67、122、159 | memories 请求，不挂笔记 |
| `GroupGallery/groupGalleryItemSummary.test.tsx` | 35、38 | gallery/item summary，不挂笔记 |
| `GroupGallery/groupGalleryPurposeRetirement.test.tsx` | 31、74 | gallery 读取清单，不挂笔记 |
| `Notes/canvasEngine/coordinateContractSession.test.ts` | 39、44、74、82、176、191、207 | coordinate/canvas repository 单测 |
| `Notes/canvasEngine/hooks/pageFrameWallsPersistence.test.tsx` | 83、114 | adapter/wall hook 探针，不挂封面 |
| `Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.test.tsx` | 379、479、2525、2558、3269、3350 等 | adapter hook 单测，无封面；相关计数限定 annotation/blocks |
| `Notes/canvasEngine/hooks/usePaperInkCommands.test.tsx` | 68 | ink command hook，无封面 |
| `Notes/canvasEngine/hooks/useTrayRelocation.test.tsx` | 100 | tray relocation hook，无封面 |
| `Notes/canvasEngine/hooks/useTextFlowHistory.document.test.tsx` | 100、108、109 | editor 写入历史，无 HTTP GET 台账 |
| `Notes/canvasEngine/layers/NoteTrayInteraction.integration.test.tsx` | 63、95–150、188 | 自组 tray/controller/sidebar，不挂封面；空断言仅 writes |
| `Notes/canvasEngine/layers/NoteChromeLayer.test.tsx` | 195–213 | trash hook 读取测试，次数断言针对 trashed blocks |
| `Settings/Settings.test.tsx` | 43 | settings API fixture，不挂笔记 |

封面入口交叉检查：`NoteCoverMetadata.test.tsx:71–72` 已在 repository seam 分别 mock tags/metadata；`NotePaperHeader.test.tsx:5–8` 未传 metadata props；`NoteRuntimeDocumentLayer.test.tsx` 未提供封面 metadata 配置。宽扫描中 `usePageFrameWalls`、`useRuntimeSurfaceStateController.pageReading`、`useTextFlowHistory`、`useTrayController` 的 pending/allowed 命中为非 HTTP 台账。没有发现第二个需补而未补的封面 GET fixture。

### 验证数字与总门申报

- `npm.cmd run verify:v2-bn8-runtime` 首步执行未经筛选、未改超时配置的 `vitest run`：**131 文件、1435 测试全部通过**；用时 **19.31s**。unboxing **4/4**，**2609ms**；无超时，无 flaky 豁免或独立复验需求。
- 同次总门继续通过：tool-face registry **5/5**、manifest **10/10**、parity **10/10**；manifest/parity/server-shared-runtime-import 检查；canvas boundary **167 checks**、gallery **8 checks**、groups rail、single editor、source experience、legacy shutdown、relation freshness；model contract **60 groups**；client/server 构建；performance **5 scenarios**。
- 总门随后在 `docs:check` 的 `docs-inventory.mjs --check` 报 **`docs/generated/object-inventory.md` 过期**并退出 **1**。该生成器读取 server schema/migrations/routes/kinds 和根 package scripts，不读取本次改动的 client 测试；这些产品输入本次均未修改。按本单仅测试补记的范围，未重生成 E5 产品库存。**该项仍需 E5 收口方处理，总门尚未全绿。**日志：[verify-runtime.log](../../audits/2026-09-11-e5-fix1/verify-runtime.log)、[exit](../../audits/2026-09-11-e5-fix1/verify-runtime.exit.txt)。
- 被上述文档失败短路的尾门已独立补跑：glossary、`git diff --check`、changed-file-secrets 均 **exit 0**，见 [tail-gates.json](../../audits/2026-09-11-e5-fix1/tail-gates.json) 与同目录日志。
- 施工前后对 `client/src`、`server/src`、`shared` 及原 E5 工单共 **867 文件**做 SHA-256 比对，仅 unboxing 测试发生变化，**意外改动 0**；证据：[before-sha256.json](../../audits/2026-09-11-e5-fix1/before-sha256.json)、[preservation-check.json](../../audits/2026-09-11-e5-fix1/preservation-check.json)。现有 E5 未提交交付保留；无回滚、无产品源码修改、无 commit/stage/push/分支操作，未发生施工冲突。
