> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: done | re: v2bn12-03-3-fix | date: 2026-08-20

# V2.BN.12 修正单 03.3:恢复收据子系统两 MED(半径=draftBlockPersistence)

## 背景

03.2 复核 FAIL(0B/0H/2M/1L),全文见 03.2 ## Review。已确认:三负例落户忠实、adapter mutation 杀伤力正确、cleanup-conflict 幂等、全门/D4/三轮累计零打架。残余两 MED 全在 R4 恢复收据子系统。

## 修正要求

1. **R1[MED-1] 写失败次态保收据**:storage write 返回可验证结果(捕获异常+read back 比对目标 bytes);**只有确认 v2 已持久化才允许清 v1**;`rememberDraftRecoveryReceipt` 的 same-key replacement 同序。常驻 oracle(复核官 witness 落户):v2 `setItem` 抛错(quota 模拟)时 v1 bytes 不变;随后 replay 失败再 load,原 receipt 仍在(replayable 或 blocked,不许消失)。
2. **R2[MED-2] validator 全 tuple 校验**:`hasValidCanonicalOwner` 按完整 `(surface, boundary_role, coordinate_space, frame_id, surface_authority.coordinateSpace, pageBoundary)` 一致性校验——`formal_page/inside` 与 `canvas_workspace/crossing` 必须有匹配 frame+boundary;仅 `canvas_workspace/outside` 可 frame/boundary 同空;矛盾 tuple 一律 fail closed 留队。常驻负例:raw v1 与 raw v2 各一条 contradictory tuple(如 crossing+frame_id=null),断 finalizer/POST/PUT=0、原 bytes 留队。
3. **R3[LOW-1]**:blocked 提示测试补 `toHaveBeenCalledTimes(1)` 并触发一次 effect rerun,证明 guard 而非单次 mount 偶然。

## 保护性约束(复核官 §5.6 点名,违者即 FAIL)

- 本轮已验绿的正控**不许回归**:合法 multi-frame、append、canonical v1→v2 升级、blocked same-key/corrupt bytes 双向保留;
- **不许用「全部 v1 一律 blocked」规避升级逻辑**——那是砍功能不是修缺陷;
- 不碰 03/03.1/03.2 已定面(placement/classifier/meaningful/cleanup/migration/schema/RED);禁止 migration --apply。

## 验证与回执

全套门重跑(test:unit 87+新增/test:v2 262/verify 链/双 tsc/migration 5 项)。## Result 追加进本文件:两 witness 落户收据、正控保持证明、申报。不自评 PASS,不 commit。

## Result

> builder: codex | date: 2026-08-20 | disposition: submitted-for-review

### 1. 施工

1. **R1 写失败次态保收据**
   - `writeStorageEntries` 现以目标序列化 bytes 为准：尝试 `setItem`/`removeItem` 后独立 read-back，只有读回值与目标 bytes 完全一致才返回 `true`；序列化、写入或读回失败均不能误报持久化成功。
   - canonical v1→v2 migration 仅在 v2 写入获确认后清理 v1；v1 清理失败时允许双份留存，不丢收据。
   - `rememberDraftRecoveryReceipt` 的 same-key replacement 使用同一顺序：先确认新 v2，再清理 replayable v1 duplicate；blocked same-key 仍不参与清理。
2. **R2 validator 全 tuple 一致性**
   - durable receipt 只接受三类 canonical tuple：`formal_page/inside`、`canvas_workspace/crossing` 均须有非空 frame、world authority、有效且 frame 匹配的 boundary；`canvas_workspace/outside` 才允许 frame/boundary 同空，authority 若存在仍须为 world。
   - raw JSON 仅将 `null`/`undefined` 视为空；`frame_id: 0`、`surface_authority: false`、`pageBoundary: 0` 等错误类型亦 fail closed 留队。
   - Page-local receipt producer 在 projection 后补齐 `boundary_role: inside`，使既有合法 Page 正控满足完整 tuple；未修改 placement/classifier 的既定语义。
3. **R3 提示次数与 effect rerun**
   - adapter 测试通过 `pageFrameCollection` 从 `null` hydration 为合法空 collection，使 `finalizeDraftBlock` identity 改变并触发 recovery effect 再运行；同一 blocked key 的 toast 精确断言为一次。

### 2. 常驻 witness 与保护性正控

- **MED-1 quota witness**：canonical Page v1 在 v2 `setItem` 抛 quota 时，首次 load 仍给出 replayable v2 view，v1 原 bytes 不变且 v2 为空；replay 返回失败后再次 load，原 receipt 仍在，且两次 migration write attempt 均有断言。
- 同组补充 silent no-op read-back witness，以及 `remember` same-key quota witness；两者都证明未确认 v2 时不会清 v1。
- **MED-2 contradictory tuple witness**：raw v1/raw v2 的 `canvas_workspace/crossing + frame_id=null + pageBoundary=null` 均断言 replayable 为空、blocked reason 为 `owner_authority_unknown`、finalizer=0、adapter POST/PUT=0、原 storage bytes 不变。
- 矩阵回归同时覆盖合法 crossing、surface/role 互换、outside 携带 frame/boundary、outer/authority coordinate mismatch、boundary frame mismatch 及 malformed falsy raw fields。
- 保护性正控仍绿：合法 multi-frame/append、canonical v1→v2、blocked same-key、corrupt v2 bytes、blocked v2 与 canonical v1 并存。
- RED 落户证据：主 witness 首轮定向为 7 failed / 13 passed；扩展 tuple 首轮为 8 failed / 14 passed；追加 malformed raw-field 负例在收紧前为 3 failed / 22 passed。修复后定向复核 37/37。

### 3. 全门回执

- `npm.cmd run test:unit`：15 files，104/104。
- `CANVAS_ASSET_DIR=<repo>/.codex-tmp/canvas-assets-v2bn12-03-3 npm.cmd --prefix server run test:v2`：262/262。
- `npm.cmd --prefix server run test:v2-bn12-migration`：5/5；仅 dry-run 测试，未执行 migration `--apply`。
- client 与 server `npm.cmd exec tsc -- --noEmit`：均 exit 0。
- `npm.cmd run verify:v2-bn8-runtime`：exit 0；含 unit 104、runtime boundary 159 checks、relation freshness、model contract 60 groups、client/server build、performance 5 scenarios、`git diff --check`、changed-file secret scan 58 files。
- 本回执追加后再次执行 `git diff --check` 与 changed-file secret scan：exit 0，59 files。
- 非阻塞输出仅为既有 React Router future-flag、Vite dynamic-import/chunk-size 及 Git LF/CRLF warning。

### 4. 范围与申报

- 生产改动限定于恢复收据的 verified write / validator，以及为严格 tuple 补齐 Page receipt producer 的 `inside` 字段；测试改动限定于 persistence 与 adapter 常驻回归。
- `server/src/db/schema.sql`、`server/src/db/migrate.ts`、`server/src/db/migrations/**` 无 tracked diff；未改 migration/schema/RED，未执行 migration `--apply`。
- 未 commit。本回执仅陈述施工与门禁证据，提交复核方判定。

## Review

> reviewer: Codex（independent reviewer） | date: 2026-08-20 | disposition: FAIL | 03.2 clean candidate: `df0925308051f6733d82c55d52d83fe659a0c4e7` | shared-tree HEAD: `e9a203b22562f7b6e0594240cf3bc793d6fb33ac`

### 1. 判定与 Findings

本轮 **FAIL（0B/0H/1M/0L）**。03.2 的两枚原始 witness 均已在旧快照复现原红、在当前树按原脚本转绿；R1 写失败保收据、03.2 所抓的 null-frame contradictory tuple、R3 blocked toast 一次、保护性正控、全门和 D4 readonly post-state 都成立。不能通过的原因是 §5-2 跨条/下一状态扫描发现：R2 所称“全 tuple”仍漏验 receipt 几何与申报 owner 三元组的一致性。

#### MED-1 — geometry 与 owner role 矛盾的 raw v1/v2 仍 fail-open，并进入三段写链 `[技术缺陷][R2 owner authority][恢复路径][§5-2]`

1. 工单 03 D2 要求 create/move/save/hydrate 共用 sole boundary classifier，且有权威几何时 geometry 覆盖 stale explicit surface；03.3 R2 又要求矛盾 tuple 一律 fail closed。当前 `hasValidCanonicalOwner`（`draftBlockPersistence.ts:99-147`）只核 surface/role、coordinate、frame、boundary 的类型与 frame equality，完全没有把 `x/width` 相对 `pageBoundary` 交给 classifier。
2. Reviewer 用 `esbuild.buildSync({ write:false })`、内存 `sessionStorage` 直接加载当前 production，做了两个方向 × raw v1/v2 共四例：
   - 申报 `formal_page/inside/frame-a`，但 `x=300,width=10,boundary=[100,200]` 的真实几何是 `canvas_workspace/outside/null`；
   - 申报 `canvas_workspace/crossing/frame-a`，但 `x=120,width=20,boundary=[100,200]` 的真实几何是 `formal_page/inside/frame-a`。
3. 四例实际均为 `replayable=[key]`、`blocked=[]`，并完整执行 `create → placement → latest`；共享 classifier 对两组分别给出 `outside` 与 `inside`。另用真实 `buildLayoutPayload` 的单例得到：

```json
{"replayable":["geometry-contradiction"],"blocked":[],"stages":[["create","crossing"],["placement",{"x":300,"y":40,"width":50,"height":72,"surface":"canvas_workspace","boundary_role":"outside","version":"V2.BN.8","coordinate_space":"canvas_world"}],["latest"]]}
```

4. 因而下一状态不是“安全正规化”：create 已消费 receipt 自称的 crossing，placement 又按权威几何改写成 outside；production recovery 成功后还会 forget 原 queue evidence。该缺口同时穿过 R2 validator、03 已定 classifier 和 finalizer，定级 MED。

修正核形：framed receipt 应直接调用共享 `classifyCanvasSurfaceAuthority`，用 receipt 的 coordinate space、`{x,width}`、`pageBoundary` 与 explicit surface 求唯一 decision，并要求 `(surface,boundaryRole,frameId)` 与申报 tuple exact-equal；不要手写第二套 classifier。常驻回归至少补上述双方向 × raw v1/v2，断 blocked、原 bytes 保留、finalizer/POST/PUT=0。

### 2. 03.2 两枚原始 witness 原样重放

两份脚本均沿用 03.2 Review §1 的 esbuild 内存 witness，未改输入或绕成 builder 测试版；先对 detached 03.2 candidate 重放，再对当前 production 重放。

1. **quota / failed replay / reload**：旧快照精确复现原红：

```json
{"failedMigrationThenFailedReplay":{"firstReplayable":["canonical-key"],"recovered":[],"v1":null,"v2":null,"afterReloadReplayable":[],"afterReloadBlocked":[]}}
```

当前树首次 load 仍给出 `canonical-key`，v2 `setItem` 抛错后 v1 与原始 bytes exact-equal、v2 为 null；replay 失败后再次 load 仍为 `afterReloadReplayable=["canonical-key"]`、blocked 为空。**MED-1（03.2）已转绿。**

2. **crossing + null-frame/null-boundary**：旧快照精确复现原红：

```json
{"ambiguousCrossingWrite":{"replayable":["ambiguous-crossing"],"blocked":[],"writes":[{"stage":"create","frame_id":null,"boundary":null,"boundary_role":"crossing"},{"stage":"placement","frame_id":null,"boundary":null,"boundary_role":"crossing"},{"stage":"latest","frame_id":null,"boundary":null,"boundary_role":"crossing"}]}}
```

当前树精确结果为：

```json
{"ambiguousCrossingWrite":{"replayable":[],"blocked":["ambiguous-crossing"],"writes":[]},"exactSafetyOracles":{"finalizerCalls":0,"recoveredCalls":0,"postCalls":0,"placementCalls":0,"putCalls":0,"v1BytesPreserved":true,"v2":null}}
```

**MED-2（03.2）所点名的原形已转绿。** 本轮 MED-1 是相邻的 geometry↔role 缺轴，不推翻上述定向修复结论。

### 3. 常驻 witness、矩阵与 RED-first 核形

1. `draftBlockPersistence.test.ts:323-354` 忠实覆盖 `load → quota write failure → replay false → second load`，精确断 v1 bytes、v2 null、两次 v2 write attempt；silent no-op readback 与 `remember` same-key quota 也各有独立负例。R1 断言有真实杀伤力。
2. `:192-223` 覆盖 raw v1/v2 crossing+null-frame 的 blocked/bytes/finalizer=0；adapter `useNoteCanvasDataAdapter.test.tsx:304-323` 再断真实 hook 路径 POST/PUT=0。合法 crossing、surface/role 互换、outside 携 frame/boundary、coordinate mismatch、boundary frame mismatch 与 malformed falsy field 均有常驻 oracle。
3. 现有 inconsistency matrix 只改标签/字段形状，未改 `{x,width}` 形成 geometry↔role contradiction；所以它能杀死 03.2 原 bug，却不能支撑 Result 的“全 tuple 已闭合”。这正是本轮 MED-1。
4. Result 所记 `7f/13p → 8f/14p → 3f/22p` 没有独立日志或中间 snapshot，精确历史数字只能记为 builder receipt，不能改写为 reviewer 亲证。其核心 RED 因果仍成立：旧 `df09253` 对两枚原 witness 都稳定为红，当前定向修复为绿。

### 4. Page receipt producer、保护正控与 03.2 LOW-1

1. 03.2 snapshot → 当前 code-root 内容差分精确只有四文件：`draftBlockPersistence.ts/test.ts`、`useNoteCanvasDataAdapter.test.tsx`、`useRuntimeNaturalWritingController.ts`；server/src、server/scripts、shared 均无内容差分。
2. runtime 唯一 production hunk 是 page-frame-local 分支把既有 `projectPageFrameLocalLayoutToCanvasLayout(...)` 结果 spread 后补 `boundary_role:'inside'`；runtime-surface 分支原本已有 inside。`placementService.ts`、共享 sole classifier 与 surface contracts 相对 03.2 内容相同，`buildLayoutPayload` 仍按几何自行重算三元组。因此它虽进入 canonicalized layout（不只是 storage API 的独立对象），行为上确实只补齐 receipt 字段，未改 placement/classifier 既定语义。
3. 正控重验：合法 multi-frame/append、canonical crossing、canonical v1→v2、blocked same-key、corrupt v2 bytes、blocked-v2 + canonical-v1 并存全部绿；未用“全部 v1 blocked”砍升级功能。正确 cwd 的定向集为 5 files / **47/47**。
4. 03.2 LOW-1 已关闭：adapter test 先等 `pageFrameCollection` 从 null hydrate 为合法空 collection；production `finalizeDraftBlock` 依赖该 state，recovery effect 又依赖 callback identity，故 effect 确实 rerun；同 blocked key 在 wait 内与 rerun 后均精确 `toHaveBeenCalledTimes(1)`，POST/PUT=0、bytes 留存。

### 5. Reviewer 亲跑门禁

| 门 | 结果 |
|---|---|
| `npm.cmd run test:unit` | 15 files，**104/104** |
| 隔离 `CANVAS_ASSET_DIR=.codex-tmp/canvas-assets-review-v2bn12-03-3-rerun` 的 `npm.cmd --prefix server run test:v2` | **262/262** |
| `npm.cmd run verify:v2-bn8-runtime` | exit 0；unit 104、runtime boundary 159、relation freshness、model contract 60 groups、双 build、performance 5、diff/secrets 均绿 |
| client `tsc --noEmit` | exit 0 |
| server `tsc --noEmit` | exit 0 |
| `npm.cmd --prefix server run test:v2-bn12-migration` | **5/5**；只用 fixture/`:memory:`，未执行 `--apply` |
| 定向 persistence/adapter/runtime/PageStack 集 | 5 files，**47/47** |

非阻塞输出仍只有既有 React Router future flags、Vite dynamic-import/chunk-size 与 LF/CRLF warning；它们不改变判定。

### 6. D4 readonly post-state

1. Reviewer 没有让 SQLite 打开 live path。先 `readFileSync` 三件并 fingerprint，把主 DB bytes 克隆到 Buffer，只在克隆上把 header `[18,19]` 从 `[2,2]` 改为 `[1,1]`，SQLite 参数为该 Buffer + `{readonly:true}` + `PRAGMA query_only=ON`；关闭后再逐件 fingerprint。
2. WAL pre/post 均为 0，`live.identical=true`。前后 exact：DB `2539520` bytes / `2c47907fcaeedcfe9a47e1bff701437c1eea63978285d10e0b162cb3a6e9c519`；WAL `0` / empty hash；SHM `32768` / `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb`；mtime 亦逐项相同。
3. 四 tuple 仍为：`1b83…`、`38f9…` = `x72/w760/formal_page/inside/primary-page-frame/canvas_world`；`c02b…` = `x414/w760/canvas_workspace/crossing/...`；`f6ce…` = `x14/w760/canvas_workspace/crossing/...`。21 ghosts 六类 residue 均 0，birth operation batches 21，client create receipts `[]`，FK `[]`，quick/integrity 均 `ok`。
4. 结论：D4 live 状态与 03.2 收据一致，字节级零触碰；本轮没有 migration apply、row write/delete 或 sidecar 清理。

### 7. §5-2 跨条、下一状态与四轮累计扫描

| 合取面 | 03→03.1→03.2→03.3 结论 |
|---|---|
| same-key response loss × adapter reused × controller revision | 相关 production/test 与 03.2 相同，VERIFIED，无打架 |
| compensation predicates × cross-note refs × cleanup identity | server lifecycle/service tests 与 03.2 相同，VERIFIED；visible cleanup toast 仍按原裁定后移 |
| crossing hydrate/normalize/save × D4 tuples | placement/classifier/surface contracts 未改，VERIFIED，无旧回归 |
| meaningful gate × Page table × D4 cleanup | helper/render/tests 未改，VERIFIED，4/4 旧修复无打架 |
| visible primary / multi-frame / PageStack append × immutable session | 原回归均保留；本轮只补 receipt inside，VERIFIED |
| storage failure × failed replay × reload | 原 03.2 witness 与常驻 test 均转绿，VERIFIED |
| structural tuple × geometry authority × finalizer | **FAIL：本轮 MED-1**；validator 放行后下一写阶段重分类 owner |
| schema/migration/package/受保护 RED | 相对 03.2 无内容变化，无未申报施工或旧面稀释 |

四轮累计没有发现 03.3 把 03/03.1/03.2 已确认修复互相打架；失败性质是 R2 与原 D2 sole-classifier 契约之间仍有未闭合的跨条状态，不是旧修复 regression。V2.BN.12 handoff 扫描没有 03.3 之后的 `to: codex / status: ready` 实现单；下一修正必须先封住该 geometry axis，再重复本报告原 witness、正控、全门与 D4 readonly。

### 8. 5-1 收据完备、范围与过程披露

1. shared tree 原本即含 builder 的 03 系列累计未提交改动。Reviewer 以既有 detached `df09253` 作 03.2 clean 对照；所有 witness 均 `write:false`/内存执行，未在 shared production/test 上 mutation。
2. 一次定向 Vitest 命令最初从 repo root 误用 `npm --prefix`，导致 Vitest 扫到 `.codex-tmp` 并出现与候选无关的 `sessionStorage` 错误；该轮明确作废，未当证据。随后以 cwd=`client` 的正确命令重跑 47/47。
3. server 262 在独立绝对 `CANVAS_ASSET_DIR` 下运行；没有复用 live asset 目录。D4 查询前后 WAL=0 且三件 fingerprint 完全相同，否则本报告不会采信查询结果。
4. 明确未做真实浏览器 E2E/hit-test、未执行 migration `--apply`、未改 schema/migration/受保护断言、未 commit/push/PR/merge。Reviewer 对共享树唯一交付写入是本 `## Review`。
5. 本 Review 追加后复跑 `git diff --check` 与 changed-file secret scan：均 exit 0，secret scan 覆盖 59 个 changed files。

### 9. 结论

本轮 **FAIL（0B/0H/1M/0L）**。03.2 的 2M/1L 原修正均可确认转绿，生产小改未改写 placement/classifier，保护正控、全门和 D4 也全部成立；但 geometry 与申报 owner role 的矛盾 raw receipt 仍可穿过 validator，并在 finalizer 中发生 owner 漂移。因此复核侧意见是：**工单 03 全链暂不具备 checkpoint 条件**。最终拍板、放行与 checkpoint 权仍在 Fable。
