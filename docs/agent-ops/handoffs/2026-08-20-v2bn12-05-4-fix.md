> from: claude(fable,代理权:claude-log/2026-08-19.md 条目1) | to: codex(builder) | status: ready | re: v2bn12-05-4-fix | date: 2026-08-20

# V2.BN.12 修正单 05.4:两缝一护栏(hydration epoch + durable rollback 落盘)

## 背景

05.3 复核 FAIL(2H/1M)。已 GREEN:05.2 三原始 probe 全转绿(ABA/分桶/迁移 prevent+本地回滚)、R3 迁移一致性、保护面。残余修法以 05.3 ## Review §1 为准。禁架构变更。

## 修正要求

1. **R1[HIGH-1] receipt 纳入 hydration epoch**:不可变 callback receipt 增加**成功 hydration/render epoch**(或等价 OCC token),与「同 epoch 内多笔 save 的 response ordering sequence」分离——**不许直接复用 per-save 自增 counter,否则正常串行 save 自我判 stale**(复核官原文警告)。同 note 二次 hydration 后旧 callback 必须可观察 stale reject、零 PUT、snapshot/durable 不变。复核官同-note probe 落常驻 killer。range-edit continuation 的 block save receipt 检查同步升级到 epoch 级。
2. **R2[HIGH-2] durable rollback 落盘**:block-owner rollback 经**既有 block 持久化正门**提交同一 rollback TextFlow/plain truth(带失败/route-stale 语义);常驻 probe 必须同时断 prevent+本地 inverse+**block PUT/receipt+重读后 durable 无 `/query`**——不许只断本地 map(这次的教训:断言只读本地草稿=假 durable 证人)。annotation inverse 与 block 正文同批语义按普通编辑基线(TD-6 已在账,不再造围栏)。
3. **R3[MED-1] 混态负控**:补 `target=block/session=draft` 与 `target=draft/session=block` 两个混态 killer,分别能杀死对应 guard 删除的 mutation。
4. **建议项(可做可申报)**:R3 canonical geometry 的常驻 fixture 补一条 adversarial canonical response(非 echo)。

## 保护性约束

05.3 已验绿面零回归(三原始 probe/迁移矩阵/分桶/正门/回滚地图/D3/D4/03 面/schema);禁 migration --apply。

## 验证与回执

全门重跑(unit 167+新增/server 262 隔离 asset/verify 链/双 tsc/migration 5 项)。## Result 追加进本文件:红绿收据(复核官两 probe 为 RED 蓝本)+混态负控。不自评 PASS,不 commit。
