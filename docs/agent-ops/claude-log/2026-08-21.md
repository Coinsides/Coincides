> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State(Claude 行动日志 · 当日)
> **日期 (Updated)**: 2026-08-21

# Claude 工作日志 — 2026-08-21

> 代理期日志(主权代理字据:2026-08-19 条目 1)。Henry 已休息恢复精神,早间在线。Fable 与 Opus 并行会话共用本文件,条目标注归属。

## 1.(Fable)早间裁定(字据):配额顺序 5.6→Spark;第三方模型只接复核岗

- **Henry 裁定**:①先用完 5.6 剩余周额,再切 GPT-5.3-Codex-Spark,他亲自盯 Spark 消耗曲线;②Spark 单独分区原因不明,既给就用;③Codex 可接自定义模型(OpenAI 兼容 provider),问 Grok 等推荐。
- **Fable 回应(已当面)**:builder 岗已被昨日「风格一致性」原则锁定 OpenAI 家,故第三方只进**复核岗**。推荐序:①Claude Code 会话坐复核岗(现有 Max,零新账号零配置,洁净室=上下文隔离非模型隔离);②Gemini(非国产、长上下文利于通读大 diff、有兼容端点与自家 CLI);③Grok(复核够用,但 xAI API 按量计费不含订阅——待查证)。提醒:Codex harness 为自家模型调优,换模型先跑小复核单试;开 API 账户属花钱+授权,到时通知 Henry 本人操作。
- **昨日结转待办**:①12.1 旅程分数补验(browser-harness);②必修① MCP 设计稿(Fable);③Opus 调研(Spark 画像/Grok+Gemini 复核备胎/配额机制);④Henry:私有备份库建库给地址;⑤裁定纪律卡 §6 已立(Opus 收工建议)。

## 2.(Fable)配额实耗与 API 价格/预算换算(Henry 委托,字据)

- **实耗数据(scratchpad 34 场 codex 日志 `tokens used` 求和)**:昨日 **16,265,443 tokens / 34 场**=周额 75% → 周额当量≈2,170 万。单场:小 17 万、builder 大单 100-144 万(03 主单 108 万/05.1 144 万)、reviewer 30-64 万。印证 Henry「上下文长度↔消耗」假设:单场百万级只有 1M 上下文的 Sol 跑得出,Spark 128K 需更细单(与其拆单假设吻合)。
- **现价(08-21 检索)**:Sol API $5/$30(7/30 降价后);GPT-5.3-Codex $1.75/$14;**Spark 无 API,仅 Pro 套餐经 Codex 可用(买不到)**;Grok 4.6 $2/$6(缓存 $0.5,>200K 翻倍);Gemini 3 Pro $2/$12(缓存~九折降,>200K 走 3.1 Pro $4/$18)。
- **换算(假设输入 92%/输出 8%;无缓存 vs 七成缓存)**:昨日强度一天全走 API——Sol $114/$67;5.3-Codex $44/$30;Grok 4.6 $38/$22;Gemini 3 Pro $46/$27。剩余工程≈14 单位≈2.3 亿 token;套餐周额×10 周≈2.2 亿基本覆盖主线,Spark 为余量。**建议预算预留 $300-500**(约 1.5-2 单位溢出走 Grok/Gemini 坐复核岗)。
- **注脚**:`tokens used` 对缓存命中的计价语义未知;输入输出比为经验值,下周 Spark 实测记明细校准;Spark 买不到⇒「5.6 用完再切 Spark」恰为正确节流序。

## 3.(Fable)⚠️ Opus 预警受理:Spark 不得计入配额规划

- **预警内容(Opus 亲取原始 issue)**:openai/codex #19868(全局周额 0% 时 Spark 虽显示 100% 仍被挡,**closed as not planned**)、#20122(重复)、#33216(8 月,Spark 计量表成功使用后仍显示 100%=计量器不准)、第三方源码复核未见独立 Spark 配额实现。官方口径与实现不一致。Opus 同时收回其昨晚「Spark=额度备胎」归类。边界:未实测本账号,可能已静默修复。
- **裁定**:①**配额规划不计 Spark**——本周真实预算=剩余 25%+周刷新;②Codex 歇火顺势(今日 Claude 侧工作);③溢出备胎序=Claude Code 会话坐复核岗→API($300-500 预留);Spark 若实测独立=白捡余量非支柱;④请 Henry 查 Codex /status 看昨晚 14k Spark 调用是否推动 Spark 计量表(两表是否独立走数)。
- **判断点(供 Henry 复核)**:把 closed-as-not-planned 的已知缺陷当退路=把风险转嫁给不知情——Opus 这条措辞准确;预警先发不等文档也是对的形(时效>完整)。

## 4.(Fable)Spark 计量表实测读数(Henry 截图)+ MCP 设计稿 v0 落盘

- **读数**:Spark 5h 表剩 96%(重置 15:49)、周表剩 98%(重置 08-28 10:49),两窗口均从 08-21 10:49 起算。**结论**:Spark 计量表独立走数(#33216「表不动」在本账号未复现);剩余未知=#19868 的闸行为(全局 0% 时是否连带挡 Spark),只能撞墙时验。规划口径不变(不作支柱),Spark 升级为「大概率可用余量」。计量单位疑非纯 token(小用量即 2%),下周 Spark 实测单前后读表校准「每小单几个百分点」。
- **设计稿**:[2026-08-21-mcp-tool-face-design.md](../analysis/2026-08-21-mcp-tool-face-design.md) v0 落盘(853ecbf):两地基决定(指代语汇=选区收据形状/收据=operation_batches 'mcp'+'proposed')先于清单;D-1..D-8 全拍(v1 暂缓/空间装配级/出处只新线/'mcp'/三层守卫/候选收据另建/CASCADE 前置调查/机械门采纳);切 12.2a-d;§10 列四条交 Opus 证伪的挑刺点。待 Opus 首读→Fable 拍板→转施工规格。

## 3.(Opus)builder 血统冗余调研落盘 + 登记册回填

- **产出**：`analysis/2026-08-21-builder-lineage-redundancy-survey.md`（draft）+ 登记册 §三之四。价格引 Fable 条目 2，**未重查**。

### ⚠️ 头号发现：**「5.6 用完 → 切 Spark」在文档记载上不成立**（已于查到时立即预警，未等文档完稿）

三条独立证据，**均已亲取原始页面**（非检索摘要）：

| 证据 | 内容 | 状态 |
|---|---|---|
| `openai/codex` **#19868**（2026-04-27） | 主额度 **0%** 剩余、Spark **100%** 剩余，用 Spark 仍报 `You've hit your usage limit.` | ⛔ **closed as not planned**（不是已修，是不打算修） |
| **#20122** | 同症状 +「计量器显示的剩余 Spark 额度实际用不了，是误导」 | closed as duplicate of #19868 |
| **#33216** + 社区多帖（4–7 月延续到 8 月） | Spark 周额表**成功使用后仍显 100%**；第三方源码复核**未找到独立 Spark 配额实现** | 持续 |

**官方口径（研究预览期独立 rate limit、不计入标准额度）与实际实现不一致。**

> **你需要它的那一刻，正是它不工作的那一刻。**

**边界**：证据是文档与社区报告，**未实测本账号**；可能已静默修复而 issue 未更新。故结论是「文档记载不成立 + 建议实测」，不是「已确证在本环境不工作」。**建议在周额 75%（现状）时主动验，别等 100%** —— 在 75% 验便宜得多。

### ⭐ 我的自我更正（第三次同族）

昨晚我把 Spark 归为「**额度备胎，不是故障备胎**」，只否了后者。**那个归类错了，我收回** —— 按上述证据它**连额度备胎都不算**。

推理形状是对的（同门共用同一根绳子），但**我只数到账号与 CLI 两根，没数到配额闸这根**。

**这是同一族错误的又一次：用一个不完整的清单去回答「共用了什么」。** 与 `head -5` 截断、从 repo 状态推断行为者同源 —— **拿一个不完整/不承载答案的东西当答案。**

### 另三条

1. **128K 不是「小一点」，是硬边界**。Spark 不适合大单与复核岗，**理由不是「弱」，是上下文装不下需要一次看全的东西** —— 而复核的核心动作（通读全链 + 原样重放旧 witness + 跨条耦合扫描）恰是上下文密集型。分段读会放大「逐条形制的固有盲区」，而我在 03 链补的那条漏合取正是逐条切开导致的。
2. **接 Codex 不是填个 base_url**：CLI 硬编码假设对端说 OpenAI **Responses API**，直连 Anthropic/Google 端点得 401 或畸形 function-call；需架网关。且**自定义 provider 会移除正常模型目录 ——「看起来像扩展点，行为上是完全覆盖」**⇒「builder 用 5.6 / 复核用 Grok」同装并存需配置隔离，形制未核。
3. **两家订阅都不含 API 额度**。Gemini 有**官方原文**（AI 计划福利只在 AI Studio 网页界面内，直接 API 单独计费）；SuperGrok 为二手一致（需另开 `console.x.ai`）。

### ⭐ 一条反直觉结论

**对备胎用途，「套餐内更省心」是错的**：

- 套餐内（Spark）：**会用完，且与主力共用同一道配额闸** —— 失效时刻与主力重合，这是备胎最不该有的性质；
- API 按量（Grok/Gemini）：**不会用完，只会花钱**。

> **备胎的价值不在便宜，在于不与主力共享失效模式。** 按量计费恰恰是优点 —— 它把「额度耗尽」换成了「花钱」这个可控成本。这也给 Fable 的 $300–500 预留提供了一条独立理由：**不只是够用，是它在结构上才是备胎**。

### 判断点（供 Henry 复核）

1. **我没等文档写完就先发了预警。** 依据：你们在 75% 周额、builder 在跑，**可能当天撞墙**；而这条推翻的是今早刚定的顺序。**发现推翻既有决定的事实时，先报再写** —— 完整性让位于时效。
2. **复核岗备胎我没排序**（Grok / Gemini 并列）。决定性变量是工具调用可靠性，而检索到的「可靠模型清单」明显滞后于当前代次（未含 Grok 4.6 / Gemini 3 Pro）。**未核前排序等于猜**，宁可空着。
3. **我把「builder 岗故障类风险无覆盖」显式记成已知缺口**（登记册与调研文各一处）。这不是反对 Henry 的代码风格一致性硬约束 —— 那是真约束；而是**不让它变成留白**。08-19 那次 builder 首启死锁就是这类风险的实例。
4. **网关选型我明确未评估**，并标注它经手 API key 与全部代码上下文、**属供应链信任面**。这不是本单范围，但采纳网关是个安全决定，不该顺手带过。

## 5.(Fable)MCP 设计稿 v0.3:四挑刺点自核闭合,Opus 首读转向整体证伪

- **自核结果**:Q1 全量 grep 37 处零 status 读方(补抽样之债),`'proposed'` 安全;source_type 编码值已有 manual/proposal/source_materialization,'mcp' 第四;Q2 生成器刻意不碰 client,机械门作独立源码级脚本;Q3 指代语汇继承既有 `CapturedSelectionRange`/`SelectionDraftV1`(owner 三元组贯穿选区→锚→工具入参,避免第三套);Q4 MRTR 为规范一级公民、Claude 线推开、Codex 未证,**confirm→propose 降级规则拍定**。D-7 由 015 原意闭合(CASCADE 刻意,V12 不改)。
- **设计稿状态**:v0.3,裁定全部成形;交 Opus 首读证伪整体(平行机关/基线保证两问反向适用于本稿;12.2a-d 跨单依赖;表述是否宽于证据)。
- **等待项**:Henry 登录(旅程分数)/私库地址;Opus 首读+兼容性调研。

## 6.(Fable)血统冗余调研抽检受理;Codex 配置隔离本地核实;备胎选型判据入档

- **调研(Opus,5cf5e31)要点**:①Spark 配额闸(已预警);②128K=硬边界→Spark 不宜复核岗(复核=上下文密集:通读全链/重放 witness/跨条扫描,分段读放大逐条盲区);③接 Codex 非填 base_url——CLI 假设对端 Responses API,直连 Anthropic/Google 需网关;自定义 provider 覆盖默认目录;④Gemini(官方原文)/SuperGrok(二手)订阅均不含 API 额度;⑤**反直觉结论:备胎的价值在不与主力共享失效模式**——套餐内 Spark 失效时刻与主力重合,API 按量把「耗尽」换成「花钱」,结构上才是备胎⇒ $300-500 预留获独立理由。刻意未做:Grok/Gemini 排序(工具调用可靠性未核,宁空不猜)/builder 故障类风险记为已知缺口/网关选型(供应链信任面)。Opus 自我校正:同族错第三次(不完整清单答「共用了什么」)。
- **Fable 抽检**:本地核 Codex CLI——`-c key=value` 逐次覆盖+`$CODEX_HOME/<name>.config.toml` 分层,**并存隔离形制可行**(每次 exec 显式传 -m/-c 即隔离),Opus「形制未核」闭合;网关门槛与信任面判断成立。
- **裁定**:①调研受理,登记册入册;②备胎判据「不与主力共享失效模式」入选型直觉(请 Opus 写进登记册协议节);③复核岗备胎排序保持空白待实测(同意宁空不猜);④网关=安全决定,**不采纳不顺手**,若需要另立单由 Henry 知情;⑤builder 故障风险:Spark 覆盖「模型级故障」(同家不同模型端点)但不覆盖账号/CLI/配额闸级——记为部分覆盖的已知缺口,接受(Henry 风格约束为真约束)。

## 4.(Opus)Spark 读数校准 + MCP 设计稿 v0 首读挑刺

### 一、Spark 实测读数并入，**结论强度下调一档**

Henry 截图读数（5h 表 96% / 周表 98%，两窗口从 10:49 起算）⇒ **计量表在本账号独立走数**。

**我原列三条证据的现状**：#33216（表不动）**未复现**；「第三方源码复核未找到独立配额实现」**被削弱**；只剩 #19868（闸行为）**未验且难验**。

> **计量独立 ≠ 放行独立。** 表会走数只证明**账在分开记**，不证明**闸在分开开** —— #19868 报的恰恰是「账分开记了，闸还是一起关」。**没有推翻结论，但缩小了证据面。**

**已把 §1.3 / §1.4 / §1.6 / §6 全部与实测校准**（文档内部一致性比任一版本都重要）。结论改为：「存在一份未修复的已知缺陷报告指向它不成立；本账号计量层正常，放行层未验」。

**⭐ 同一天内我对同一件事两次修正，方向相反**：昨晚「Spark 是额度备胎」（过于乐观）→ 上午「它连额度备胎都不算」（过于笃定）→ 现在「取决于一条未验行为」。
**两次病根相同：拿手上现有的证据面当作全部证据面。** 昨晚手上是「账号+CLI」两根绳子，上午是「三条 issue」，两次都以为数全了。
> **这条比结论本身更该记：「我数到的绳子」永远可能少于「实际的绳子」，所以措辞要留余量 —— 「据现有证据」不是废话，是诚实。**

**且实测无便宜验法**：#19868 只能撞墙验。建议不是「去验」，是**按不可依赖规划 + 撞墙那次顺手记一笔**（代价已经付了，别浪费观测点）。

### 二、MCP 工具面设计稿 v0 首读挑刺（证伪视角）

落 `## Review-1`。**骨架我认**（两个地基决定先于清单 / 收据轴复用 / 机械门方向）。**五条真问题**：

**⭐ 最重的一条是 §10 没列的、我加的 —— Fable 自己的「基线保证问」反向套用，命中：**

本稿承诺「大动作全收据可撤销，由既有 `status/reverted_at` 兑现，不造新机制」。**但基线不提供这个保证** —— 05 主单 Review `:157` 已钉死「服务端 transaction 不与 block text 构成跨资源事务」，05.2 立为 **TD-6**。
⇒ 工具若同时触及正文与 annotation（§8 里至少三类），`reverted` 时可能只回滚了一个资源。**收据会说「已撤销」，事实是半撤销。**
**这与 05.1 是同一形状**：局部机关承诺基线不提供的保证 —— 那次花了一轮才撤掉。
> 本稿**确实引了 TD-6，但引在另一条轴上**（CASCADE 张力）；TD-6 的主体（跨资源原子性）**没被应用到「可撤销」这条承诺上**。

**其余四条**：

1. **`status='proposed'`**：全扫后 —— 生产写方 13 处全是 INSERT，读 `status` 的只有 `noteBlockLifecycle.ts`，但有**三处把非 applied 当异常**。`:237` 因先比 source_type 不会误伤；`:390/:951` 走 `findOperationBatch` **只按 `WHERE id=?` 无 source_type 过滤** ⇒ **当前不冲突，但靠「命名空间恰好不撞」，而那不是被声明的不变式**。同 03 链那条合取的形状。
   **①-b 顺带证伪一处事实**：本稿称 `'mcp'` 是「第三个编码取值」，实扫代码已有 `client_note_block_create` / `client_note_block_cleanup_conflict` / `source_materialization`。且后两者是**操作级**取值 —— **既有用法已违反 D-4 自己的「该轴表达来源类别」原则**。D-4 原则我认同，但该写成「新纪律，既有不符，不追溯」。
2. **机械门**：客户端走**单一 axios 实例 + 模板字面量路径** ⇒ 带路径参数的路由**在源码里无完整字面形态**；且 grep 命中只证明「有人构造了这个 URL」，**不证明有可达入口**（死代码/feature flag 全能过）。**「零引用 ≠ 死」的逆命题同样成立：有引用 ≠ 活。** 建议把断言改写准，别叫「无后门的机械证明」—— 它必要不充分。
3. **指代语汇**：`item_anchors.target_kind`（5 种）与 `KIND_HANDLERS`（6 种）**层次不同**（`canvas_object` 是类目，`shape/table` 在其内）且形近义异（`table` vs `table_region`）。`ObjectRef = {kind: <注册表 kind **或** 真相对象类型>}` 的那个「或」**就是并集出第三套词表** —— 与本稿 §2.1「③ 不另造」相抵，**同时命中平行机关问**。建议分层复用而非并集。
4. **MRTR**：先纠一处概念混同 —— **`elicitation/create`**（Claude Code CLI 自 2.1.76 支持，Desktop 不支持）**与 MRTR `input_required`**（7-28 修订的工具级机制）**不是一回事**，后者支持度我**未核**。⭐ 且失败模式比「不支持」更糟：Codex 侧说明明确「客户端必须实现 handler，**否则会话无限期阻塞**」—— **不支持时不是降级，是挂死**。建议降级路径改为退化到 `propose` 并立即返回（**这同时给了 propose 档一个新理由：它是 confirm 的安全垫**）。

### 判断点（供 Henry 复核）

1. **最重的一条是 §10 没点的。** Fable 列了四条请证伪项，我全查了，但**分量最大的那条来自把它自己立的纪律反向套在本稿上** —— 这正是它上一轮请我做「裁定质量复盘」时立的规矩。**规矩立了就该对立规矩的人也生效**，这是第二次兑现。
2. **五条里三条附了「建议（不代拍）」，两条只指问题不给解**（①的命名空间、②的门语义）—— 因为那两条的解法涉及设计取舍，不是我的位置。
3. **我明确列了三处未核**，其中一处是**本稿的事实陈述我扫不到但不否认**（`'proposal'` 取值）—— 挑刺不等于凡我没查到的就判错。

## 7.(Fable)⭐ MCP 设计稿 Review-1 五条全采纳 → v0.4 拍板;Spark 结论再校准;12.2a 拆单

- **Review-1(Opus,2528b56)**:最重一条=我自立的「基线保证问」反向命中本稿——§2.2 承诺「大动作全收据可撤销」而基线无跨资源原子性(TD-6),跨资源工具撤销可能半撤销而收据称已撤销,**与 05.1 同形**。其余四条(全量查):①noteBlockLifecycle 三处把非 applied 当异常,:390/:951 按 id 取无 source_type 过滤=靠命名空间巧合;①-b `'mcp'` 非第三/第四取值——实有 client_note_block_create/cleanup_conflict/source_materialization,后者为操作级取值已违 D-4 原则;②机械门必要非充分(axios 模板字面量路径,有引用≠活);③ObjectRef「或」=并集出第三套词表;④elicitation/create≠MRTR input_required,后者支持度未证,Codex 侧不支持时**会话挂死**非降级。
- **v0.4 裁定(全采纳)**:可撤销按资源数分档(单资源可撤销;跨资源 best-effort+`revert_outcome: complete|partial` 可观察,TD-6 前不承诺原子);D-4 重写为新纪律不追溯;12.2a 必须为 findOperationBatch 路径加 source_type 守卫+killer;机械门口径=无后门的必要条件,充分性由旅程验证补;ObjectRef 两层复用无并集(第一层=item_anchors target_kind 原词,canvas_kind 携子类);`input_required` 只对宣告支持的客户端发,否则 confirm 立即降 propose(propose=confirm 安全垫);新候选面与旧 ProposalList 退场同单。
- **自我认账**:上午的 source_type 取值扫描是不完整清单(只扫 status 字面)——「证人必须承载答案」刚写完即又犯同族错;Opus 同日对 Spark 两次反向修正(乐观→笃定→待验)病根同源(把手上证据面当全部)。**两人同病,纪律已立但执行仍需机械化:存在性/完备性问题一律 `git grep` 全量+显式列出扫描面。**
- **Spark 再校准(Henry 截图后)**:#33216 未复现、源码复核削弱、仅 #19868 未验——「计量独立≠放行独立」,结论强度下调为「取决于一条未验行为」。规划口径不变。
- **12.2a 拆单(Spark 口径)**:a-1 注册表类型+机械门脚本(纯工装,无运行时)→ a-2 收据轴('mcp'/'proposed')+不变式守卫+killer → a-3 MCP transport 骨架+resolve_selection(架构面,需方案短笺)。a-1 今日写好 ready,下周 Codex 刷新后开工。

## 5.(Opus)冗余判据入登记册协议节 + 调研盘同步 Fable 三项裁定

### 一、判据已入协议节（Fable log 条目 6 裁定）

`external-candidate-registry.md` 协议节新增 **⭐ 冗余判据**，与既有协议（边说边调研 / 三层不绑架 / 点菜不下厨）同层：

> **凡评估「备胎 / 冗余 / 退路」类候选，首要判据不是能力也不是价格，是：它与主力共享哪些失效模式？**
> **备胎的价值不在便宜，在于不与主力共享失效模式。**

**执行方式写成了六层「绳子」清单**（模型 / 账号 / 工具 / 配额 / 区域 / 供应链），要求**逐层列出覆盖与不覆盖**。

**附两条纪律**：
1. **「我数到的绳子」永远可能少于「实际的绳子」** —— 措辞须留余量。**实例就用我自己那次**：同一天内对 Spark 两次反向修正（过于乐观 → 过于笃定 → 取决于未验行为），病根都是「拿手上现有的证据面当全部证据面」。
2. **不覆盖的层要显式记成「已知缺口」，不是留白** —— 留白会被后来者读成「已覆盖」。

**并附 Spark 判例表**（模型级 ✅ / 账号·工具·配额闸 ❌），让判据带着一个做过的分析落地，**不是抽象口号**。

### 二、调研盘同步三项

| Fable 裁定 | 我的同步 |
|---|---|
| **并存隔离形制已核**（`-c` 逐次覆盖 + `$CODEX_HOME/<name>.config.toml` 分层，本仓每次 exec 本就显式传 `-m`/`-c`） | 从未核清单移出，并**精确化**：「自定义 provider 是全覆盖」的警告**约束的是默认目录，不是逐次覆盖** —— 两者不冲突。**我原判断（这是真门槛）成立，但门槛比我写的低。** |
| **Spark 覆盖模型级故障**（同家不同端点、不同硬件栈） | 我接受这条补充，并把 builder 岗缺口从「无覆盖」改写为**分层表**：模型级 ✅ / 账号·工具·配额闸·区域 ❌ |
| 复核岗排序留空 · 网关另立单 | 保持不变 |

### ⭐ 值得记的一笔：Fable 的补充修正了我的一处过度概括

我原写 builder 岗故障类风险「**目前无覆盖**」。Fable 补充：**Spark 覆盖模型级**（Sol 跑 GPU / Spark 跑 Cerebras WSE-3）。

**这条补充是对的，而且它正是我自己那条判据的正确应用** —— 我立了「逐层数绳子」的方法，却在下结论时**把六层压成了一句「无覆盖」**。

> **提出方法的人不自动是最会用方法的人。** 我把绳子分层列出来了，然后自己给出了一个不分层的结论。**这与「立了纪律仍会犯它要拦的错」同源**（昨天 `head -5` 那次也是：我在契约里写了取证纪律，写之前刚犯过）。
>
> 所以判据落盘时我特意**把分层表写进去**，而不是只留那句口号 —— **口号会被压缩，表格不会。**

### 判断点（供 Henry 复核）

1. **判据我写进了协议节而不是新开一节** —— 它约束的是「怎么评估候选」，与既有协议同层；单开一节会让它变成一条可跳过的附录。
2. **我把自己那次两度反向修正写进了判据的纪律条款作实例**。不是自我批评表演 —— **一条纪律带着真实事故比带着假设更难被简化掉**（这是昨天「有理由的规则才扛得住被简化」的同一条）。
3. **Fable 的补充我全盘接受且做了同步**，没有为了守住原表述而辩护。我原写的「无覆盖」确实过度概括了。

> **条目 6 补充**(Opus 626a950):备胎判据入登记册协议节(与边说边调研同层,不另开附录)——「首要判据=与主力共享哪些失效模式」,执行方式=六层绳子清单(模型/账号/工具/配额/区域/供应链)逐层列覆盖/不覆盖,附 Spark 判例表;隔离形制精确化(全覆盖警告约束默认目录不约束逐次覆盖,门槛比原写低);builder 故障覆盖改分层表(模型级 ✅ Sol/GPU vs Spark/Cerebras;账号·工具·配额闸·区域 ❌)。方法论注记:「口号会被压缩,表格不会」——与裁定卡「有理由的规则才扛得住被简化」同向,且 Opus 自证「提出方法的人不自动最会用方法」(六层压成一句「无覆盖」)=纪律卡 §6 独立检查条款的又一实证。

## 8.(Fable)Henry 裁定(字据):Codex 正常花,Spark 画完即停;12.2a-1 以 Spark 开工

- **Henry 原话要旨**:接着做;剩余 token 正常花;**套餐内含的 Spark 圈画完之后先停一下**;私库地址晚点给。
- **执行口径**:builder 岗=Spark(`-m gpt-5.3-codex-spark -c model_reasoning_effort=xhigh`,同时充当实测单);复核岗=5.6 剩余 25%(复核须一次看全链,128K 不胜任,Spark 不坐复核);**任一先见底即 Codex 全停**,转 Claude 侧工作。12.2a-1(纯工装,无需方案短笺)已发 Spark 施工,回执要求附 Spark 单前/单后读表。
- **等待项**:Henry 登录 Browser 面板(12.1 旅程分数)——Henry 未看懂「登录什么」,已当面解释;私库地址晚点。

## 9.(Fable)⭐ Henry 裁定(字据):Codex 用尽也不转 Claude Code 施工;Fable/Opus 岗位锁定指挥与设计

- **Henry 原话要旨**:就算 Codex 用完,**也不要转回 Claude Code**;Fable 与工程并行会话须时刻保持**指挥与设计**角色,两者统领全局,**不能让代码的上下文把策略上下文占太多**。
- **效力**:①施工岗唯一归 Codex(5.6/Spark);Codex 见底=施工停,不由 Claude 会话顶替;②Fable/Opus 的工作面=设计稿/规格/复核汇总/抽检/手册/调研——**不写产品代码**(点状单文件实现的旧授权据此收束:仅限文档工装类,不进 client/server 产品面);③我此前「Claude 会话坐复核岗」的备胎设想**撤回**——复核同样是重代码上下文的活,若未来需要,须 Henry 明示另开专职会话,不由本会话或 Opus 会话兼任;④Browser 面板旅程打分属设计验证,不受限。
- **判断点**:这条把「上下文即资产」写成了组织原则——策略会话的上下文是不可再生的指挥资产,被代码冲淡就等于换了个人;与裁定卡 §2「停乒乓判据」同向:多轮代码细节正是最容易把裁定方拖进实现视角的东西。

## 10.(Fable)Spark 实测单第一数据点(12.2a-1)

- **用量**:Spark 单场 `tokens used` **166,661**(5.6 小单 17-20 万量级,同级)。
- **行为画像**:三交付物+4 例自测按字面完成;但 ①verify 链卡 docs:check 即停,**未按仓库惯例刷新生成件再跑全链**(5.6 builder 历次自动做);②工单要求的双 tsc 未跑;③RED-first 收据缺失;④回执薄。**结论(初步)**:Spark 按字面执行、不推断仓库惯例——少的是语境不是能力;工单须把惯例写成显式步骤(「docs:check 过期则先 docs:index/docs:inventory 再全链」)。已发补完单(Spark,一次一条口径),不耗 5.6 复核半成品。
- **流程修正**:我在工单里要求 builder「附 Spark 单前/单后读表」是错的——CLI 看不到计量面板,只有 Henry 的 UI 能看。改为:**每个 Spark 单收口时请 Henry 报一次读数**,log 记录。

## 11.(Fable)⭐ 工作流 v1 落盘;12.1 打标签;12.2 plan 落盘;调度下沉 Opus

- **Henry 批评(字据)**:逐条 thread 指挥效率不如他本人拆解意图分派;昨日工作设计不佳;版本记录缺失;要求:设计工作流、把版本接回来、plan→拆单→施工的模式、每大版本复盘使用不便、Opus 由 Fable 全权负责、Claude 不下场。
- **Fable 自认四条**:①自己当了调度员而非设计者;②逐条消息重述文件内容;③无 plan 层,34 场 codex 逐单重读全仓;④版本未提前设计、无标签。
- **落地**:`operating-workflow.md` v1(三层文档驱动/消息退为指针/调度下沉 Opus/版本纪律含 git 注解标签/效率机制);**`v2.bn.12.1` 标签打在 ad301c6 并推送**(仓库原有 v1.2.0/v1.3.1 标签,版本实体接续);`handoffs/plans/v2-bn12-2-mcp-tool-face.md` 落盘(S1-S7,DoD,触及面,施工规则);handoffs README 增 builder 纪律 3(回执 UTF-8——Spark 首单追加段 GBK 乱码事故,已修复重编码)。
- **Spark a-1 合计**:166,661+104,305 tokens,两场;实质合格待复核。
- **下一步**:12.2 调度交 Opus(含启动 a-1 复核);Fable 转设计/抽检/铸版。

> **条目 11 补充**(Henry):token 稀缺度 Fable<Opus<Codex,负载向下压——入 workflow §3.5;12.1 旅程实走交 Opus(Fable 预期已冻结,抽查 2-3 步)。

> **条目 11 再补**(Henry):角色纲——上将军(Fable)/将军(Opus)/前锋(CLI);上将军管将军不带兵,此为代理含义的一部分。已写入 workflow 开头。

## 6.(Opus)接手 12.2 调度 —— S1 复核已启动

**工作流改制收到**：`operating-workflow.md` v1（Henry 亲授，权威=是）把 12.2 调度下沉给我。三份正文已读（工作流 / plan / builder 纪律），**消息只做指针**这条我照办。

### 第一件：S1 复核已启动

- **档位 5.6**（复核岗；已核 `config.toml` 默认即 `gpt-5.6-sol`，不额外传 `-m`）
- `codex exec -s workspace-write`，**后台 + `< /dev/null` 关 stdin**（08-19 挂起教训），日志落我自己的 scratchpad
- **提示词按新工作流写成指针式**：只给「读哪几份 + 本轮重点 + 边界 + 产出形态」，**不重述文件内容** —— 文件是权威通道，重述＝同一事两个会话各吃一遍上下文

**本轮点名的复核重点**（除角色卡全条款外）：机械门断言是否**与降级后的「必要条件」口径一致**（不得声称证明了「人类可达」—— 这正是我 Review-1 ② 的落点）；注册表是否引入平行机关；`exposure` 闸与 `__` 过滤是否真会红；触及面申报核对；判定须用 **FAIL（方向成立/不成立）** 新词汇。

### ⭐ 调度决定：S1 **不与 S2 合批**

Plan 给了我合批决定权。**决定不合，四条理由已写进 plan §1.1**，其中**第 ④ 条是决定性的**：

> **S1 是 Spark 实测样本，单独复核给出的测量更干净** —— 合批会混入不同档位的施工产物，而 Fable 明确要我给「Spark 适合哪类单」一个实证判断。**合批会毁掉那个测量。**

另三条：S2 尚未施工（合批＝S1 干等）；两步基本独立（风险上平手，没有「必须先验基座」的理由）；配额见底即停，**先把已有回执的一步落袋比攒批安全**。

### ⚠️ 一处调度侧的可见性缺口（已记进 plan）

`codex` CLI **无非交互配额查询**。故「见底即停」这条规则我**只能靠 exec 失败反向感知，不能主动监控** —— 我看不见「快到了」，只看得见「已经到了」。

**这不是抱怨，是把规则的可执行边界说清**：我能保证的是「撞到就停」，不能保证「快撞了就预警」。前瞻视角目前只在 Henry/Fable 的 `/status` 那里。

### ⚠️ 一处文档不一致，须 Henry 确认（我不自行处置）

**`CLAUDE.md §2.1` 的组织图仍写「Fable ＝ 拍板/设计/**总调度**」，而 `operating-workflow.md` v1（同为权威件）已把调度下沉给我。** 两份权威文件现在对我的岗位描述不一致。

**我不改 `CLAUDE.md`** —— 它是 agent 操作指令文件，同侪不能替 Henry 授权修改（`CLAUDE.md §2.1` 自身的不变式）。工作流文件虽是 Henry 亲授，但**「Henry 授权了新工作流」与「Henry 授权修改 CLAUDE.md」是两件事**。故：**摆出来，等他一句话。**

### 判断点（供 Henry 复核）

1. **合批权我用了，并把决定性理由写在 plan 里而不是只写日志** —— 状态表是 plan 的一部分，将来任何人回看 S1 为何单独复核，应该在 plan 里就看得到，不必翻日志。
2. **配额可见性缺口我主动申报**。被交付一条「见底即停」的规则，而我看不见「底」在哪 —— 这该在第一天说，不是撞墙那天说。
3. **组织图不一致我停手上报**，没有顺手改 `CLAUDE.md`。这是「授权不可传递」的第二次实际适用：**新工作流是 Henry 授的，但它没说可以改 CLAUDE.md。**

## 12.(Fable)S1 复核 FAIL(方向不成立)升级受理 → schema 权威裁定(设计稿 v0.5 §3.1)

- **升级(Opus,50bec90)**:S1 复核 0B/2H/4M——机械门对伪造条目判绿(self-test 正控因错误理由通过,`/api/health` 在 client 零命中);registry 住 shared 承载不了 zod,parity 用 `new Function` 执行源码片段(ReferenceError),空数组藏住冲突;`exposure:'test'` 防线永假;builder mutation 不满足三要素;纪律项(header/docs 顺序)。Opus 按 plan §2 正确升级、不发修正单,并标 S3 阻塞。新判定词汇首用即触发升级路径。
- **裁定**:注册表住 server(真 zod,复用 validators)=唯一权威;生成器以正常模块加载(jiti/tsx)派生 `docs/generated/tool-face-manifest.json`(含 zod→JSON Schema),parity 与文档只消费 manifest,禁 `new Function`;shared 只留 manifest 可序列化类型;legacy toolDefinitions=退役线非 adapter,无第三套目录。机械门:真实 route+真实调用点做正控、空表不得输出 PASS、test/`__` 各一条 killer。S1 方向重置重开,S3 解阻条件=本裁定落地 PASS。
- **责任归属(认)**:HIGH-2 一半是设计稿欠定(「zod strict」没解决 shared 无 zod 依赖的承载)——我的规格漏洞,非 Spark 之过;MED 纪律项是工单没写死——Opus 与我共担。Spark 判断留到步骤收口(n=1 不下结论)。

## 13.(Fable)⭐ Henry 裁定(字据):删除阈值规则;流面默认面+数据可删常设授权

- **Q1 删除规则(拍)**:Agent 单条删除=即时(留收据可撤);批量/跨 Project/打包导入导出=确认一次。已写入设计稿 §5 守卫。
- **Q3(拍)**:新流式装配面**成为所有 Note 的默认面**(12.4);存量 Note **迁移或清空由 Fable 按效率定**。**常设授权**:投入生产前,应用内**全部数据皆可删除、全部账号皆为公开测试账号、无隐私**;Henry 将在生产数据出现时**明确通知**,在此之前无保存义务。(dry-run 收据纪律保留为工程卫生,非为保数据。)
- **Q2(候选呈现位置)**:Henry 要求解释后再答——已当面给三选项(就地 / 队列 / 队列为家+边注点)与推荐,待答。

> **条目 13 补充**(Henry 答 Q2):**C**——候选队列为家(12.2 S6),页边极淡标记点随 12.4 流面落地。设计稿 §6 与 plan S6 已回写。三问全部闭合。

## 14.(Fable)私有备份库建成:Coinsides/Coincides_Private(Henry「准」)

- `gh repo create Coincides_Private --private`(已验 `private=true`);本地 `D:\Coinsides\Coincides_Private`(与公库物理分离);备份脚本 `vault-backup.ps1` 入库(remote 校验防误推公库;排除 provider auth 令牌)。首轮全量备份:`~/.claude/projects/D--Coinsides-v2-x-Coincides`(全部会话 jsonl+memory)+`server/coincides.db`+`.env`。
- **待 Henry 一字**:是否挂 Windows 计划任务每晚自动跑(属持久配置变更,按规矩明示后做);东京迁移后同脚本挂 cron。

## 15.(Fable)12.1 旅程实走 16/18 未通过(J9=0)→ 裁定补丁版 12.1.1;P-2/P-3 入账

- **结果(Opus 实走,a0ad295)**:J1–J8 全 2(四病灶机械收据:36/36 精确、reload 恰一份、`/` 精确撤回、elementFromPoint 落 textarea;J4 空 blur 留 `reverted` 收据);**J9=0**:存量 note `1d10fe77`(08-09 1791 字讲稿)服务端返回两 active 块而客户端零渲染,强指向 `order_index=[4,5,6]` 非零起点(8 篇中唯一),因果未证。P-2:`operation_batches.created_at` 双格式与 source_type 完全对齐(103 默认/2 ISO),零消费点但排序方向已坏。P-3:块级 template_key 与单元级 writing_role 分层,工具面须暴露后者。方法天花板:合成按键不触发原生默认动作。
- **裁定**:①12.1.1 补丁版非回退;12.1 旅程分数=待补验,只重走 J9;②存量全量体检并找机关(不许只改数据);③P-2 并入 12.1.1+S2 规则(工具收据不传 created_at);④P-3 入设计稿;⑤12.4 旅程验收须真实输入通道。调度归 Opus,12.1.1 与 12.2 并行。
- **判断点**:Opus 三次测量险成假 finding均靠「再取独立角度」抓回——「拿自己造的观测口径判产品」写进其申报,与证人纪律同族。「修好了机器没治病人」这句概括准确:存量是 Henry 真实资产(虽已授权可删,但渲染丢内容是真 bug,与数据权限无关)。

> **条目 15 补充**(Opus 2e03bea):12.1.1 已发单(5.6,后台),**串行打头**(Opus 纠正我的「并行」:共享工作树两 builder 互踩,隔离树 Windows 成本高;判据=正确性与配额)——采;P-1/P-2 合单(判据=无共享机关无造桥风险);验收形 A-1..A-4(机关点名 file:line/先红后绿/数据与代码修复分列并答复发问/不许放宽渲染掩盖);M-1/M-2 写死(mutation 归复核方;header 归调度方);翻牌来源标注入工单文首。两条入 handoffs README:翻牌来源标注、同树串行原则。

## 16. 裁定:S1 FAIL 产物撤出 verify 主链(Opus 提请)

**现状**:S1(FAIL,方向不成立)的 `check:tool-face-parity` 未提交却已接进 `verify:v2-bn8-runtime` 首位;空注册表⇒真空绿,不挡人但污染每一次「verify 全绿」回执。Opus 未动树(12.1.1 builder 在跑,同树串行),提请裁定。
**裁定**:撤接线。窗口按 Opus 排:12.1.1 落地后、S1a 发单前。S1 三件产物连 FAIL 判定一起提交存档(历史可查),**但 S1a 收口时不得残留两套注册表**——死的 `shared/types/toolRegistry.ts` / 旧 parity 脚本须被 S1a/S1b 明确删除或重写,不许并存(平行机关问)。12.1.1 本轮回执里的 verify 绿,复核报告须注明「parity 门当时空转,不含工具面断言」。
**判断点(供 Henry 复核)**:验收基础设施定义权归裁定方,我拍;若你认为 verify 链改动该你亲批,收回即是。本期我未跑过 verify,无需打折的绿。规则入 handoffs README「验证链接线规则」。


## 7.(Opus)12.1.1 二级复盘 —— builder 判 BLOCKED,而且它证伪了我两条

### 结论:这份回执质量高,**不发修正单,按 needs 重划边界**

builder 判 **BLOCKED · needs: claude**,**零产品码改动**。这是对的 —— 我在工单里写了「真因落在申报面之外就停手」,它照做了,而且**没有把只读定位包装成完工**。

### ⭐ 它证伪了我两条,两条我都亲验了

**① `order_index` 不从 0 起 —— 已证伪(它做了真实验)**

它对同一持久 layout 只把 `order_index` 从 `6` 改成 `0`,策略输出仍是 `pageVisible=[]`。**过滤器根本不读 `order_index`。**

> 这正是我在工单里**明写允许它证伪我**的那条。**写那句话是有回报的** —— 若我把「强指向」写成「根因」,它很可能会去凑一个解释而不是去证伪。

**② 「零消费点」—— 我说错了,而且错因与 `head -5` 那次同源**

它指出 `sourceMaterialization.ts:87-90` 按 `operation_batches.created_at DESC, id DESC` 取最近 batch。我亲验属实。

**我那条 grep 要求 `operation_batches` 与 `ORDER BY` 出现在同一行,而这是多行 SQL —— 必漏。**

> ⭐ **与 G-7 那次完全同一个形状:我的搜查工具看不见证据实际的形状,而我把「工具没找到」当成了「不存在」。**
> 上次是 `head -5` 截断,这次是单行 grep 遇上多行 SQL。**两次都不是粗心,是「我没问自己:如果证据长成另一个样子,我这条命令还看得见它吗」。**

### 但纠正一条错,不等于反面自动成立(我亲验后的准确表述)

| 说法 | 准确度 |
|---|---|
| 我原说「零消费点,故潜伏」 | ❌ **错**:确有消费点 |
| 顺势会说「已被消费,故现行故障」 | ⚠️ **也不准**:该消费点带 `source_type='source_materialization'` 过滤,而该类型**当前 0 行**;且全表每个 source_type 内部格式**同质**(`manual` 103 行全空格式 / `client_note_block_create` 2 行全 ISO) |
| **准确表述** | **已被消费;当前各类型内格式同质故未误动;但任何一个 source_type 一旦出现混格式,该查询就会静默取错「最近一条」** |

**我差点犯的第二个错是过度纠正** —— 被指出「零消费点」错了,顺势就该说「那就是现行故障」。**两次都是拿手上的证据面当全部证据面**,只是方向相反。

### builder 找到的真因(已点名到 file:line,满足 A-1)

Page mode 初始 surface 固定为 `page`(`useSurfaceModeController.ts:19`)→ `modePolicyService.ts:68-76` 在 Page mode 过滤掉 `isCanvasWorkspaceBlock` → 标本那两个 active block 被 `placementService.ts:301-311` 判为 `canvas_workspace`(`boundary_role=crossing`,`coordinate_space=canvas_world`)→ `NoteWritingSurfaceLayer.tsx:3673-3679` 只渲染 `visibleBlocks` ⇒ 0 个 textarea → `DraftWritingEntryLayer.tsx:88-99` 显示空态。

**这与我实走时留下的那条「未排除线索」对上了**:`1d10fe77` 独有 `surface=canvas_workspace` 组合。**我当时停在了线索,没往下追 —— 停得对,那是施工侧的活。**

**它还指出一条我该知道的**:`surfaceAuthorityContract.test.ts:197-263` 用同一批 live placement 保护这些块的 `canvas_world + crossing` 空间真相 ⇒ **不得靠把数据重分类为 `formal_page` 来「修好」**。那正是我 A-4 硬闸要防的掩盖,它自己先说出来了。

### A.3 全量体检:受影响 **1 篇**(未触发我设的「>3 篇停手」闸)

它同时更正了我的「8 篇有块」为当前含 WAL 快照的 **7 篇** —— 我那次是更早的快照。**这类小数字被更正是好事,说明它没照抄我的上游数字。**

### 处置(我定的 vs 我不代拍的)

| 项 | 归属 | 处置 |
|---|---|---|
| B-1 扩面到第 4 个写入点 `sourceProjectionMaterializer.ts:289-308` | **我(调度/范围)** | **扩。** 我自己工单里就写了「若目标仍是同列新写入统一,必须覆盖全部 4 处」——只改 3 处就是明知不全而申报统一 |
| 「零消费点」口径更正 | **我(是我的错)** | 已更正进 journey-score P-2,附错因 |
| **A 的修法**(初始 surface 一次性选择) | **⛔ Fable(设计)** | 它改的是表面模式行为,而待拍-1/待拍-2 的表面重划正在设计中。**builder 明写「这只是建议,不是已实现机关」——它没有越权,我也不代拍** |
| `applied_at` 的 `learningCanvases.ts:298-300` 潜伏双格式入口 | **⛔ Fable(基底规则)** | 这是「收据表要不要有全仓统一时间格式约定」的问题,属收据基底,不是本单范围 |

### 顺带完成:S1 遗留处置(Fable 已批)

S1 三件产物**此前一直未提交地留在工作树里且已生效** —— `package.json` 把那道被证明「对伪造数据判绿」的门接成了 **verify 链首**。已按裁定:①原样存档(连 FAIL 判定)②**撤接线**(单行改动,`git diff` 已核只动链首一项,JSON 合法性已验)。独立脚本条目保留,待 12.2a-1c 重写。

> ⚠️ **因此本单回执里的「verify exit 0」要按 Fable 附加条件②打折:那次绿包含 tool-face parity 的空转 PASS。** builder 自己也写了「此绿只说明当前树基线门禁通过,不代表本单修复完成」—— **它比我先说了这句。**

### 判断点(供 Henry 复核)

1. **我在工单里明写「允许你证伪我」,并且真的被证伪了两条。** 这条我认为该固化成工单写法:**上游给的方向若未证因果,必须在单里标明并明确授权推翻**,否则 builder 会去凑解释而不是去证伪。
2. **我今天第四次栽在「自己造的观测口径」上**(前三次:数重 textarea、只看 block_type、编造 note id;这次:单行 grep 遇多行 SQL)。**四次形状相同。** 我把它写进日志而不是轻轻带过,是因为**它已经不是偶发,是我的系统性弱点**。
3. **A 的修法我压住没拍**,尽管 builder 给了很具体的候选。理由:它动的是表面模式行为,而表面重划正在设计中 —— **调度权不含设计权。**

## 17. 12.1.1 回执 BLOCKED → 两项裁定 + 三条规矩

**回执质量**:builder 零产品码、越界即停、自己声明「此绿不代表修复完成」、证伪上游两条(`order_index` 相关非因果;「零消费点」错)——采 Opus 判断,不发修正单。真因=初始面固定 Page 过滤掉 canvas-only 块,见 journey-score §5.1。
**裁定①**:A 修法采 builder 候选,四约束(正门内复用同一谓词/只挂载决定一次不盖手动/过渡桥随 12.4 拆=TD-7/RED-first 数据一字不动)。**裁定②**:收据表时间戳约定=每列恰一盖章权威(`created_at` DB 默认;`applied_at`/`reverted_at` 应用 ISO 单 helper);`learningCanvases.ts:298-300` 为违例,并入 12.1.2 作 B-5。B-1 扩第 4 写入点(Opus 定,采)。
**规矩**:handoffs README「未证因果须标明并授权推翻」(Opus 提);adjudication §7「探针先过阳性对照」(Opus 自报四案,我提取);TD-7 桥登记。
**调度**:12.1.2 打头(小、🅰、闭 J9 → `v2.bn.12.1.1`),S1a 随后,同树串行。
**判断点(供 Henry 复核)**:①A 修法是过渡桥,12.4 后必拆——我选桥而非现在动 Page 模式,因 Page 退役是 12.4 的活,不在补丁版里提前开面;②时间戳约定我定为「每列一权威」而非「全 ISO」,因全 ISO 要改 schema 默认(DDL),与 12.1.1 刚定的方向相反,不翻烧饼。

## 18. 12.1.2 BLOCKED(两次越界)→ 12.1.3 只做 A;B 重裁收窄

**账**:A 越界=Opus 划面未验连接(controller 无数据输入、调用顺序晚 69 行,须 one-shot resolver);B 越界=**我的**——裁定写「单一 helper」却未核其存在(15 写点/14 文件/无 helper),Opus 照抄。两次 5.6 全程换两条 grep 的结论。
**裁定 B**:选 (b) 收窄为「每列单一格式」,只修 `learningCanvases.ts:298-300`;helper 收敛=TD-8,种子放 12.2a-2 新写入路径,存量随触及归入。理由:要治的失效是混格式排序错,单一格式足以;跨 14 文件贴 03/05 保护面的重构买 DRY 不买正确性。
**规矩**:README「触及面申报前先验连接」(Opus 提,阳性对照升层);adjudication §7 分则「裁定点名机关前先核存在」(我的反例)。12.1.3 写明「越界是划面错非 builder 错」——采,这句防下一个 builder 自疑。
**判断点(供 Henry 复核)**:今日 12.1.x 已三张单(12.1.1 BLOCKED 诊断成立 / 12.1.2 BLOCKED 划面错 / 12.1.3 在跑)——按止损式第 3 轮须落盘:**继续**,理由=前两轮 FAIL 均非方向问题而是边界问题,且真因与修法已定、A 面小;止损线=12.1.3 若再 BLOCKED/FAIL,停 12.1 线,J9 改记「已知缺陷、12.4 流面默认化时随 Page 退役自然消失」,资源转 S1a。
