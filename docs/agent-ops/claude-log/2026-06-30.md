> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State（Claude 行动日志 · 当日）
> **日期 (Updated)**: 2026-06-30

# Claude 行动日志 — 2026-06-30

> 最新在最上。承接 8.11 object-family 逐版审查（登记册 `analysis/canvas-object-review-issue-register.md`）。协议：**不在 8.11 收口前动手修，只登记；收口后一版一版照册烧单。**

## 4 — 补审 8.11 地基三版（8.11.1 / .1.1 Cutover + 8.11.3.1 Hydration）

- **触发**：Henry 让 8.11 收口、进入补丁/填坑阶段，要我"彻底检查所有没检查到的地方"，只论 8.11 内。我对账发现：之前主审的是引入新对象的 .3–.9，**地基层 8.11.1 cutover / .1.1 硬化 / .3.1 PageFrame hydration 从未单独对抗审**——而整个家族坐在它上面。起 6 维度审查。
- **产出**：登记册新增 §8.11.1（**1 HIGH / 4 MED / 6 LOW**）+ §8.11.3.1（**1 HIGH / 1 MED**）；汇总升至 **4 HIGH / 32 MED / 49 LOW = 85**。优先级表新增 tier-0「数据丢失·真 HIGH」并置顶。
- **重大发现（全战役第一颗真·新 HIGH 数据丢失）**：**migration 035 `page_frame_extensions` 跨 note 塌缩**——PK=frame_id 单列 + `INSERT OR REPLACE` + 主框 id 是常量 `'primary-page-frame'`（每篇 note 都用）→ 单事务循环所有 note 时每篇被后一篇覆盖 → 每篇非末位 note 永久丢 margins/page_size/background/template/exportable。**四个承重事实我全部亲读真码坐实**（035:132 PK、035:210 OR REPLACE、engineModel.ts:70 常量 id、039:48-59 只拷幸存行救不回）。这正是 8.11.3.1 那个"Canvas Mode 页框消失"bug 的根；而 8.11.3.1 的 LEFT-JOIN 兜底只救几何、**adapter 还会把兜底默认值再存回去 → 丢失变永久（CR-8.11.3.1-01，LIVE tail）**。
- **好消息（同样要记）**：cutover 持久化层主体扎实——**import-once 销毁顺序安全**（strip 严格 gated 在 durable 实体写之后，写失败则导航离开、绝不 strip → legacy 不会无副本丢失）；**strip 竞态真消除**（adapter 单次 strip PUT，非只是收窄）；`canvas_layout` 确为单一运行时投影；**休眠 seed（writeLayoutOverride 等）复活担忧已澄清**（今不可达）。annotation 层欠约束：range 焊死绝对 offset、parent/child 零服务端完整性、036 backfill 同病根（窄）。
- **判断点（供 Henry 复核）**：
  ① **035 塌缩定 HIGH 的理由 + 诚实血量边界**：它是真·静默不可逆数据丢失（你最在意的"心爱之物凭空消失"），且有 **live 的 re-save tail**（当前任何 extensionless frame 一开+存就烤死默认）→ 我给 HIGH。但**血量受限**：① 只伤 pre-cutover 且**自定义过页样式**的 note（默认样式 note 丢的是默认值、LEFT-JOIN 兜回一致，无可观测损失）；② local-dev 单用户 pre-release（patch note 明说不迁生产数据）；③ **结构性复发已被 039 note-scope PK 堵死**，不会再发生在新数据。**所以紧但不慌**——建议先修 live tail（hydration 标 recovered + save 拒默认覆盖），再 ship repair migration。**这条请你拍板优先级**。
  ② **scope 守住**：只审了 8.11 内的三版地基，没掺 8.12/Agent/export。
  ③ **优先级我重排了**：把数据丢失 HIGH 提到 tier-0（因填坑阶段 + 仍 live + 不可逆），AI 层双源 HIGH 降到 tier-1。你若觉得 Agent 层更急可以调回。
  ④ 至此 **8.11 全版本（地基 + 对象 + inspector）逐一审完**，登记册是完整的烧单底本。

## 3 — 审 8.11.9 Object Inspector + Context Actions（8.11 家族收口）

- **做了什么**：对 8.11.9 起 5 维度对抗审查（duplicate 身份 / availability 单一边界 / export-visibility+open-original / AI-tree flatten 回归 / feature+closure）。第 3 维报 **FAIL·3 HIGH**，我**逐一亲读真码裁定**。
- **产出**：登记册追加 §8.11.9（**0 HIGH / 5 MED / 5 LOW**）；汇总升至 **2 HIGH / 27 MED / 43 LOW = 72** 新坑。**8.11 object-family 审查全部收口**（.1.2 → .9 全审完），登记册末加"已收口"段。
- **8.11.9 关键**：① **duplicate 维 PASS（最高危面）**——亲验 table 复制每个 id（object/row/col/cell）真重生、cells 从重生数组重建 → 无串扰；sticky/block-backed 在 draft factory 真禁、无旁路。② **flatten-then-index 修复 PASS**——实证正确、无 CR-8.11.3-01 双源回归、containment 保留。③ inspector 半闭环 CR-8.11.7-11（image asset-detail 未渲染）；delete 绕过"唯一边界"；image duplicate 共享 asset 放大 CR-8.11.7-01 的无-GC 债（refcount 从此强制）。
- **判断点（供 Henry 复核）**：
  ① **本版又一次"agent 高估、我验完回正"**——export-visibility 维报 **3 HIGH**，我亲验后**无一是 live HIGH**：HIGH-A(open_original→ghost 块)裁 MED（坏导航死路、非数据丢失，与 CR-8.11.4-03 同级）；HIGH-B(hide-from-export 是 no-op 假控件)裁 MED（UX 死路、无数据损伤）；**HIGH-C(枚举 clobber 静默吞 scratch/ai_hidden)裁 LOW**——关键亲验：scratch/ai_hidden 只在 block-projection 路径（placementService.ts:228 `BlockPlacementModel`）产生，inspector toggle 只碰 generic 对象的 CanvasPlacement、后者今只会 normal/export_hidden → **静默状态丢失当前不可达**（只是"未被产生"非"被守住"）。**这三处定级请你过一眼**，尤其 HIGH-C：它是你在意的**相对不焊绝对设计气味**（一个枚举槽扛 4 个正交关切），我按"今不可达"压成了 LOW，但标注了它的结构 hazard。
  ② **跨版本规律再确认**：6 个含 finding 的版本里，**真 live HIGH 只有 2 颗**（CR-8.11.3-01 双源 / CR-8.11.4-01 隐藏块泄 AI），都在 8.11.3/.4（首批接 generic 通路时）；之后 .5–.9 的对抗审查共报出 **5+ 颗 agent-HIGH，我亲验后全部下调**。说明 Codex 的对象家族**越往后越稳**，早期两版的 AI 层坑是真要紧的。
  ③ **"对象不进导出"现在是确证的系统性缺口**（shape/block/image/table 全缺席 + 8.11.9 还加了个会撒谎的 hide-from-export 开关），根因是**无真 export-to-file 管线**。我在优先级表把它们捆成"导出族·待管线建起一次性统一补"，强烈建议**别逐版打补丁**。

## 2 — 续审 8.11.7 Image / 8.11.8 Table

- **做了什么**：对 8.11.7（image asset-backed CanvasObject + migration 041）、8.11.8（table structured-backed + migration 042）各起 5 维度对抗审查 workflow（5 agent），每条争议/HIGH finding 我**亲读真码裁定**再登记。
- **产出**：登记册追加 §8.11.7（**0 HIGH / 4 MED / 7 LOW**）+ §8.11.8（**0 HIGH / 4 MED / 6 LOW**）。优先级表新增"数据丢失·产品红线" tier。
- **8.11.7 关键**：安全面（5-agent 一致 + 我亲验 handler/upload）**整体过关**——blob 读在 authMiddleware 后、IDOR 已闭（`WHERE id=? AND user_id=?`）、mime 白名单(SVG 排除)+10MB、storage_key server 生成无路径穿越、单 kind 无双源。**无 ship-blocking 安全洞**。真坑：asset GC 缺失（删图 → `canvas_assets` 行+磁盘 blob 永久孤儿，handler 无 cleanupOnDelete，测试把"存活"写死）+ 上传零引用孤儿 + 图不进导出 + 上传失败静默死路。
- **8.11.8 关键**：5 维度 **3 PASS + 2 CONCERN**。数据层是本家族最干净的之一——**无双源**（单 objectId 贯穿，根除 8.11.3 坑）、**payload 校验防得最严**（三层：Zod+service+DB CHECK，rows≤50/cols≤20/cells≤1000/cell≤2000 字符 → 最大~2MB、**无 DoS**）、删级联干净、min-1×1 floor 双侧守住、scope 守住。真坑：**删行/删列不可逆毁数据、无 undo、无确认**（亲验：cells 硬 filter 无 trash + RuntimeHistoryEntry 无 structured 类型）+ Esc-cancel 被 blur-save 覆盖 + 客户端 mutation 模块零单测。
- **判断点（供 Henry 复核）**：
  ① **几处定级我下调了 agent 的评分**，理由都写进登记册括注：8.11.7 asset-GC（1 agent 评 HIGH·磁盘泄漏/erasure）→ **裁 MED**（已知 gap、无腐败、local-first 单用户削弱隐私角度）；8.11.7 mime-spoof（1-2 agent 评 MED）→ **裁 LOW**（缓解强：content-type 强制 image + `<img>` blob URL + SVG 排除 + self-only）；8.11.8 删行列毁数据（1 agent 评 HIGH）→ **裁 MED·产品红线**（删列是具名销毁命令、惊讶度低于 demote，但"具名销毁却完全不可恢复 + 画布他处有 undo"是真红线，置数据丢失 tier 顶）。**这三处定级请你过一眼是否同意**——尤其删行列毁数据，我在 MED/HIGH 边界，按与 CR-8.11.4-06 的一致性压成了 MED。
  ② **8.11.9 是 Codex 新落的**（current-state README 2026-06-30 已自报 8.11.8/8.11.9 落地）。我把 8.11.9 也纳入审查范围（它属 8.11 家族，且直接触及我登记的"无 inspector/可发现性"缺口、duplicate 身份语义是最高危新面）。
  ③ **隔离纪律守住**：全程只读 Codex 的码、只写自己工作区（`docs/agent-ops/`），没碰他的源文件/数据库/端口。
  ④ 一个跨版本观察：**"对象不进导出"已是第 4 次撞见**（shape/block-backed/image/table 全缺席 export-preview），但根因是**无真 export-to-file 管线**（无 jsPDF/html2canvas）→ 我标成"尚未建边界"而非已上线路径的数据丢失，建议 export 管线建起时**统一一次补全所有 CanvasObject**，而非逐版打补丁。

## 1 — （承接 06-29）8.11.3/.4/.5/.6 审查已登记

> 见 06-29 日志与登记册 §8.11.3–§8.11.6。两颗 HIGH（CR-8.11.3-01 双真相源 / CR-8.11.4-01 隐藏块泄 AI）+ demote 销毁文本红线均在册。
