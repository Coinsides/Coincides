> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State（Claude 行动日志 · 当日）
> **日期 (Updated)**: 2026-06-28

# Claude 行动日志 — 2026-06-28

> 最新在最上。承接 2026-06-27 那个长会话（昨晚通宵的产品哲学/工程对话在 `2026-06-27.md`）；今天醒来这段新起一天。

## 7 — 验 Codex 的 8.11.1.3 加固（H1-H5）

- **做了什么**：Codex 据我 §11 审查收了 8.11.1.3（migration 038 partial unique index + 写前清旧 mount + save 守卫防 kind-flip + H2-H4 补测试；回执 handoff §12，144 tests）。我跑 3-维度对抗验证 workflow（自写 in-memory SQLite probe **复现原 fan-out 场景**验修复，不只读代码）→ 全 PASS → 写验证结论 handoff §13 + 销 current-state 的 mount-join watch。
- **结论**：**H1-H5 全部实证通过，地基干净，可进 8.11.3。** 关键验到：① 038 去重在前、建索引在后（probe 反证：不去重则索引在旧重复数据上抛错——顺序对）；② 修复真挡住第二个 note_block mount；③ **关键回归**——合法同 kind 重存幂等不撞约束、守卫没误伤更新；④ H2-H4 测试非空（delete 级联用 foreign_keys=OFF 反证 FK 驱动、payload 保真断的是 DB 重读值）。
- **判断点（供 Henry 复核）**：① 3 条残留 low 我都判为"当前不可达、可选、不拦路"（kind-flip 守卫只覆盖 active 行 / 038 索引键不含 note_id 但 object_id 天然 note 内唯一 / 全局 dedup 依赖 object_id 唯一）——若你想要"不变量自证"，索引加 note_id 是最干净的一条，但非必须。② 这是连续第三轮 review/verify（8.11.1.2 审 → 8.11.1.3 收 → 本轮验），handoff 现在 §10-13 是完整往返链。③ 我把 current-state 的 mount-join watch 标了解决，inert-seam watch 仍留（8.11.3 必接）。

## 6 — 审 Codex 的 8.11.1.2（kind-general 管线）+ distill current-state

- **做了什么**：Codex 把 handoff 落成 8.11.1.2 前置补丁（回执在 handoff §10，全绿 140 tests）。我跑 6-维度对抗审查 workflow（扫真代码 + 跑测试 + live zod probe，不信回执）→ 写审查结论进 handoff §11 → 同步 current-state（Pillar 3 + watch list + frontier）。
- **结论**：**通过，质量高**。4 PASS / 2 CONCERN，无高危无阻塞。registry 真通用（核心零 if-kind）、两范围钉子守住、生产路径行为逐字节不变、validator 真严（live probe 证 throw）、回执无夸大、软指针清理对当前表完整。
- **挖出的要收项**：H1〔真坑〕mount 读 join 结构性歧义（无 UNIQUE，对抗 probe 复现 fan-out；今不可达但会被 8.11.x 踩响）→ 建议 `content_mounts(object_id,target_kind)` UNIQUE；H2-H4 便宜补测试（delete 级联只断言 placement / round-trip 没验 payload 保真 / content_group_members 清理已写未测）；H5 防御纵深（generic SAVE 的 kind-flip 旁路）；H6〔我已办〕inert 缝 distill 进 current-state。
- **判断点（供 Henry 复核）**：① 我把 6-agent 的 finding 自己复核过 severity，**只把 H1 定为"建议现在做"**——它是静默腐败缝且修法便宜（一条 UNIQUE）；其余降级为"凑个小补丁一起收"。② current-state README **之前飘到 8.8**（还写持久化 thin/absent，早被 035-037 推翻）——我只surgical 同步了承重的 Pillar 3 / watch / frontier，**没做全量重写**（避免 scope 蔓延）；全量 current-state 校准值得单独排一次。③ handoff §11 是 `draft`，是否翻 ready 把 H1-H5 交 Codex，等你定。④ 审查跑了 ~667k subagent tokens（Ultracode 下放开了）。

## 5 — 落 8.11.2 handoff（to: codex, draft）+ 核实 foreign_keys

- **做了什么**：核实 `PRAGMA foreign_keys=ON`（init.ts:30 单连接 singleton，确认开着，035 级联会触发，隐雷拆除）→ 写 `handoffs/2026-06-28-canvas-object-kind-general-pipeline.md`（status: draft，等 Henry 翻 ready）。存了 [[test-accounts-in-env]] 记忆。
- **产出**：handoff 含 北极星(通用≠抹平) / 现状 5 面 file:line / kind registry 目标形态(server+client 签名) / 逐面任务+验收 / 删除级联(自动 FK + 手动软指针清单) / 行为不变 + dummy-kind 通用性证明 / 边界 / 5 个坑 / 验证命令 / 回执请求(含 3 个决策点)。
- **判断点（供 Henry 复核）**：① handoff 是 voice-neutral spec，含 3 个**留给 Codex 的决策点**（content_mount id 迁走否 / note_block 反向缺口本版补否 / block 旧路由撤留）——我没替他拍，让施工方按成本定，符合"spec 给约束不替他做技术选型"。② 线协议变更(client 发 kind)我按已与 Henry 定的"本版做"写进去了。③ dummy-kind round-trip 当通用性硬证——这是我加的验收门，确保不等到 8.11.3 才发现地基漏。④ status=draft，需 Henry 翻 ready 才进 Codex 视野。

## 4 — 8.11 plan 据实细化（5-面实证）

- **做了什么**：按 Henry 5 点修正（通用≠抹平/route·validator·repo 三处没写清/删除级联/connector 边界/table 验收克制），先跑 5-agent workflow 实证当前代码（route、validator、repository、持久化+级联图、runtime+AI-tree），再据 file:line 把修正钉进 plan。
- **核心实证（反直觉但关键）**：「通用核心 + kind-extension」**数据层(035)其实已存在**（canvas_objects/placements 核心 + 两种扩展风味：page_frame_extensions 自有边车 / content_mounts 投影 mount）——**地基没偏**；要做的全在它上面**四个派发层**。而**唯一真的 JSON 黑盒在 validator**（`z.record(z.unknown())`@validators/index.ts:41/548），不在 schema。8.11.2 因此从"一句通用化"扩成 ①service ②route(补 generic PUT+DELETE) ③validator(discriminatedUnion) ④repo+engineModel(kind→builder 注册表) ⑤runtime/AI-tree(已是"按存在性挂扩展"，对的接缝) + 完整删除级联清单。
- **判断点（供 Henry 复核）**：① **`PRAGMA foreign_keys=ON` 已确认 ON**（init.ts:30，单连接 singleton getDb()）——035 级联会触发，隐雷拆除。〔2026-06-28 本次核实闭环，原写"待验"。〕② validator 上 discriminatedUnion **要 client 在 payload 发 `kind`=线协议变更**，我标了"记一笔"，是否本版做由你/Codex 定。③ table 的 `StructuredObjectExtension`{rows,cols,cells} + `structuredRef` 形状是我据 AI-tree 现状(唯一扁平 text 通道)给的具体提案，方向稳但字段名 Codex 落地可调。④ 把 roadmap"至少一个结构化家族"软化成可顺延（沿用上一条，仍待你确认）。

## 3 — 重写 8.11 plan（地基优先）

- **做了什么**：先核查地基现状（grep 实证：`canvasObjects.ts` 持久化只硬编码 `page_frame`@334 + `paragraph_block_projection`@458 两 kind；shape/connector 仅 seed service 无 UI；image/table/object-style/asset 不存在）→ 据此重写 `V2.BN.8.11-Structured-Object-Family-Plan.md` 为**地基优先**。
- **产出**：① 第一原则=「地基正确 > 任何单个对象；地基偏将来加对象要重写 runtime」。② **共享基建 vs 各对象坑**分清（按 Henry 要求）。③ 小版本重排：8.11.2 kind-通用管线（重中之重）→ 8.11.3 shape（通用性试金石）+建对象交互面 → 8.11.4 block-backed 路径 → 8.11.5 object-style+sticky → 8.11.6 connector → 8.11.7 image(asset) → 8.11.8 table(后端无关) → 8.11.9 inspector → 8.11.10 结构化家族(可顺延) → 8.11.11 收口。④ 接缝（course_id 项目无关、connector 端点别被 relation 复用）写进边界。
- **判断点（供 Henry 复核）**：① 我把 roadmap 的「8.11 至少一个 structured family 可用」**软化成 8.11.10「可顺延 8.12」**——是顺着你"地基+基础对象先行"的 scope 收缩，但属于改 roadmap 范围，请确认。② 自查抓到 8.11.4/5 依赖顺序错（sticky/shape-带字 都需先有 block-backed），已把 block-backed 提到 sticky 前。③ 「8.11.2 把 2-kind 硬编码通用化」是基于 grep 实证的方向，但真正改起来 runtime/AI-tree 里还有多少处隐式假设，要 Codex 动工时才见底——验收硬标准已写死（新 kind 走通且不新增专属分支）。

## 2 — 路线盲点普查 + 写进会议记录的清单

- **做了什么**：6-lens 对抗式"完整性批判"workflow（每个 agent 扫真 schema + PRODUCT.md，专挑 Henry 还没提、但要命的盲点）→ 我筛选组织 → 写进今日会议记录 §九「路线盲点清单」（~36 条，6 组，标待解不待做，少数 🔴 接缝决定）。
- **产出**：改 `会议记录/2026-06-28-...-Meeting-Notes.md`（+§九）。最大几颗：① 整底座被 `course_id NOT NULL` 锁死，跨项目能力非 deferred 而是 unrepresentable；② 系统存"思考的产物"不存"思考"、且无时间维度（telos 是跨年记忆、设计是快照）；③ 信任不随引用链复合 + bbox 锚与"内部引用只到 NoteBlock"撞车；④ proposal 审查界面 + 目的捕获界面双缺失；⑤ "画布即图"的节点≠tile/off-canvas/布局-vs-视图真相；⑥ 无忠实导出 + 奠基输入（AI 聊天记录）传不进来。
- **判断点（供 Henry 复核）**：① 「跨项目被 course_id 锁死」「export 只有分类器无发射器」等是 critic **读真 migration/代码**得出的（031/024、exportPreviewService、DocumentManager accept-list）——有据，但仍建议你按真实 schema 复核一两条再据此排活。② 哪些标 🔴「现在拍形状」是**我的优先级判断**（基于 seam-vs-混凝土）——你可调。③ 清单**非穷尽**（你自己也说还有更多），是"看见即记下"的认知债，不是待办池。

## 1 — OCR 工具研究 + 三层入库设计 + 会议记录

- **做了什么**：web 研究 Mistral OCR 4 与 MinerU（非 Anthropic，全程查证不凭记忆）；据此与 Henry 设计文档上传管线；把今天聊的（含昨晚哲学）蒸成一篇会议记录。
- **产出（结论 + 文件）**：新建 `会议记录/2026-06-28-...-Meeting-Notes.md`（哲学铁律 / ContentGroup-Relation 深化 / telos / 三层源重建 / 两工具）。核心设计：**三层入库**（原始字节 → SourceArtifact[OCR 结构化、bbox 锚、仍是源] → 按目的懒蒸馏 ContentGroup[锚回②]）；**源结构化可急、知识蒸馏必等**；OCR 的 block ≠ NoteBlock；两个 OCR 工具是 ②层可换后端（契约优先），MinerU 本地优先默认、Mistral premium/兜底。
- **判断点（供 Henry 复核）**：① 「别 eager 把原始文档转成 NoteBlock」是我的强建议（基于源-grounding + 蒸馏 + 铁律），与 Henry 一致。② OCR/MinerU 的能力/价格/许可是 web 来源、且工具很新（Mistral OCR 4 才几天）——以官方为准。③ 「入库车道 defer 着、现在只定形状不建」沿用 seam-vs-混凝土。
