> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State(Claude 行动日志 · 当日)
> **日期 (Updated)**: 2026-08-20

# Claude 工作日志 — 2026-08-20

> 代理期日志(主权代理字据:2026-08-19 条目 1)。Henry 病中,零打扰;一切代拍留判断理由。Fable 与 Opus 并行会话共用本文件,条目标注归属。

## 1. 工单 02 复核 FAIL 受理 → 修正单 02.1 签发

- **事件**:reviewer 对工单 02 落盘 ## Review,判定 **FAIL(修正 MED-1 后复核)**,0 BLOCKER / 0 HIGH / 1 MED / 2 LOW。
  - MED-1(技术缺陷+认识论错误):useState×5 → useReducer 的"机械抽取"没有保留 React 调度语义(render-phase 调度、functional-updater 求值时机),reviewer 有可复现最终 state 反例。"pure reducer no-op 返回原对象"只证明提交态等价,不证明 setter 行为等价。
  - LOW-2:Result 里的独立 tsc 收据缺 cwd,按 repo root 字面执行 TS5058,不自足可复现。
  - 其余全 PASS:工装/lockfile 纪律/RED #1 收据核形(红的理由正确)/真越界正控/跨条合取扫描(含确认 RC-A.5×RC-B.3 legacy dead-end 仍在、正确地未在本单修)。复核质量高:显式范围排除(不装 browser runner 故不签 live UX、不跑 npm ci 以免污染共享工作树)符合 5-1 义务。
- **代拍**:接受 FAIL 判定,签发修正单 [2026-08-20-v2bn12-02-1-fix-med1.md](../handoffs/2026-08-20-v2bn12-02-1-fix-med1.md),status: ready(代翻),builder 已后台开工(stdin 已关,吸取 08-19 挂起教训)。
- **判断点(供 Henry 复核)**:
  1. **修正方向我定为"抽逻辑不抽状态基座"**:hook 恢复原生 useState,pure reducer 降级为转换计算函数被 setter 委托。理由:宪章对重构的验收是"行为零变化";换状态基座后等价证明负担极重(要证 React 调度语义),而不换基座则零变化按定义成立,证明负担反转。备选路径(保留 useReducer+补调度等价证明)被我否掉——为一个工装单背上 React 内部语义证明,不值。可逆:是(纯代码方向,复核不过可再改)。
  2. reviewer 反例要求转化为 hook-level parity 测试收编进 test:unit——把这次翻车固化成回归资产,而不是只修不留痕。
  3. RED #1 明确不许顺手转绿(那是 03/04 的验收凭证)。
- **可逆性**:全部可逆(未提交,pre-commit 复核窗口纪律不变)。

## 2. 工单 03 起草并代翻 ready(合批:lifecycle + surface authority + 存量迁移)

- **事件**:趁 02.1 修正施工空档,把 Review-2 合取裁定落成 [2026-08-20-v2bn12-03-lifecycle-and-surface-authority.md](../handoffs/2026-08-20-v2bn12-03-lifecycle-and-surface-authority.md),status: ready,但带**开工前置**:02.1 复核通过+checkpoint 后才开工(同一工作树禁叠单)。
- **判断点(供 Henry 复核)**:
  1. **原拆 03/04 合并为一单**:RC-A 与 RC-B 在创建路径深度耦合(gesture→ephemeral→durable→投影→分类是一条线),拆两单会造成中间态无法单独验收;合取裁定本就要求同批 checkpoint。代价是单大,但 builder 在 01/02 已证明能吃大单。
  2. **拍板 client-first ephemeral**(gesture 同步挂本地 TextUnit,meaningful input 才建 durable),否掉 server-first+provisional 路线。理由:宪章流式装配面本来就需要本地先行的编辑面;server-first 要给 pending 身份+rollback+reload 清理三件套,复杂度更高还留 focus vacuum 窗口。
  3. **存量迁移含真删除**(ghost:空文本且无 source/annotation/history 才删),依据 Henry 08-19 明示授权"数据迁移权限打开…自由删除"(全 test data)。自限清单里"大规模删除不自行执行"与此不冲突:此处是**判据收窄的定向清理**且有 dry-run 逐条收据,非无差别大规模删除;复核官被点名逐条核对删除清单。若 Henry 认为仍越界,迁移脚本可重跑方向仅修分类、删除部分可撤销执行(改动前有 dry-run 清单可审)。
  4. 服务端空文本拒收降为"可做可不做+申报制"——不确定非文本 block 创建路径是否依赖空 plain_text,不赌。
- **可逆性**:纯文书;施工尚未开始。

## 3.(Opus)BN 路线图分家草案落盘（重建四件之一，Fable 排序第一）

- **形制**：按 Fable 认可的分家式 —— 新建精简 ACTIVE 件 + 原件整体冻结。产出 `docs/ROADMAP.md`（**状态 `draft`，权威=否，待抽检转正**）。
- **命名判断**：新件取名 `ROADMAP.md` 而非第三个 `Coincides-*-Roadmap.md`。理由：`Coincides-Roadmap.md`（v2.0–2.5.6）与 `Coincides-Better-Notebook-Roadmap.md`（V8–11）现在都是关闭件，同名系列已成「历史件命名法」；短名本身就在说「这是活的那份」。
- **薄的纪律写进文件**：新件 9 节 + 一份转正执行清单，约 150 行（旧件 1996 行）。§9 立规矩：新增内容前先问「这属于宪章（为什么）/ current-state（是什么）/ 冻结件（曾经是什么）」，三者都不是才写进路线图。**旧件长到三套 scheme 并存，正是因为什么都往身上装。**
- **未执行冻结**：草案期不动原件。理由：新件转正前若先冻结旧件，会出现「没有任何 ACTIVE 路线图」的真空；且原件顶部已有我在批次一打的 `:35` 作废公告，过渡期不误导。冻结动作写进文件末尾的「转正执行清单」，等抽检放行一并做。

### ⭐ 分家中发现的实质问题：「长青参考」是个自我声明的假标签

旧件阅读指南自称「§1–§5 长青参考（不随版本过时）」。**逐节核对后这句不成立**：

- **旧 §2 产品北极星** 把定位写作 `page-first, canvas-backed, **block-based**`。而 `block-first` 早在 **2026-06-20** 就被 `TextFlow-first / ContentGroup-aware` 取代（`PRODUCT.md` 06-20 教义）——**这一节被「长青」标签保护着错了两个月**，无人复核，因为标签宣称它不会过时。`page-first / canvas-backed` 又被待拍-1/2 二次取代。
- **旧 §3.3 整节标题就叫「Page-first, Canvas-backed」**，被待拍-2 + 宪章 §4 表面重划取代。

若照 Fable 原话「§1–§5 长青参考」整段搬运，**会把一个两月前就死掉的定位原样搬进新的权威件**。故新件 §4 改为**逐节判定表**（存活 / 部分 / 被取代），每条注明「凭什么还活着」，并把教训记进文件：**「长青」不是可以自我声明的属性**。

### 顺带并入新件的两条

- **工单 01 二级复盘的合取发现**写进 §3.5 作为 03/04 的拆单约束（只修一边会留存量 legacy note 半死）——让它变成路线图上的硬约束，而不是躺在 handoff 里等人想起来。
- **07-15 §十 的两条新纪律**（触及面申报 + 旅程分数验收）写进 §7.2，此前只存在于会议卷，从未进任何权威件。

### 判断点（供 Henry / Fable 复核）

1. **§4 从「照搬」改成「逐节判定表」是我对 Fable 指令的偏离** —— 他说的是分家保留长青参考，我发现长青清单本身有错，就没照搬。若认为该原样搬运，翻掉即可；但我建议保留判定表形制：它顺带给出了「为什么这条还活着」的收据，将来再分家时不用重新考古。
2. **新件命名 `ROADMAP.md`** 是我自选的，非 Fable 指定。若想保持 `Coincides-` 前缀一致性，改名成本极低（改文件名 + 4 处引用）。
3. **草案期不冻结原件**是刻意的时序选择，不是漏做 —— 见上方理由。
4. 新件**未包含**旧件 §4「学习模板与产品参考」的正文（Notion/AFFiNE/Word/tldraw/Obsidian 对照），只在判定表里标它存活并指向冻结件。理由：那是参考材料不是路线，搬进来违反「薄」的纪律；但若你认为它该随新件走，我可以单独抽成 `docs/references/` 一件。

## 4.(Fable)ROADMAP 分家草案抽检:放行转正

- **事件**:Opus 落盘 `docs/ROADMAP.md` 草案(45bc8f8)并报三个裁点。我抽检后放行转正,由 Opus 执行其转正清单(冻结旧件/引用更新/docs-index)。
- **抽检方法**:未采信转述,亲核三条证据——PRODUCT.md:40-42 的 06-20 教义原文、旧件 :26 自封长青条款、:395/:1011 的 block-based 与 page-first 表述。全部属实:旧件「长青参考」标签保护着一个 06-20 已死的定位活了两个月。
- **判断点(供 Henry 复核)**:
  1. **准了 Opus 对我指令的偏离**(§4 从"照搬长青"改"逐节判定表")。我原话基于"长青标签可信"的预设,证据证明预设错;按证据不按上级原话,正是我们要的复核文化,故不仅准,还认定为本次抽检最有价值处。
  2. **准 `ROADMAP.md` 短命名**(Coincides-* 前缀=历史件命名法)与**不搬旧 §4 参考材料正文**(按需抽取,提前抽违反薄)。
  3. 我顺手修了草案 §3.5 过时的 03/04 拆单表(0aa8320)——Opus 写作时不知我已合批,属并行时差非错误。
- **可逆性**:转正后 Henry 可整体翻牌;旧件是冻结非删除,决定史全在。

## 2 — ROADMAP 转正 + 旧路线图整体冻结（Fable 抽检放行后全量执行）

- **放行**：Fable 三裁全准（§4 判定表 / `ROADMAP.md` 命名 / 旧件参考材料不搬正文），并**亲核了我的三条证据**（`PRODUCT.md:40-42` 06-20 教义原文、旧件 `:26` 自封长青条款、`:395` block-based、`:1011` page-first canvas-backed），未只听转述。
- **转正清单四项全执行**：①`ROADMAP.md` 转 `active`、权威=是；②旧件整体冻结（`archived`，顶部换整体冻结公告，`被取代` 指向新件）；③4 处引用更新；④重跑 docs-index（旧件现显示 `archived`+被取代链接，新件 `active`）。
- **附录改写**：原「转正时的执行清单（草案状态下不执行）」改为「转正记录（已执行）」，逐条打勾并标注 `PRODUCT.md` 引用仍留批次二。

### ⭐ 冻结时顺手改掉了我自己批次一写错的一句

批次一我给旧件打的作废公告里写着「**本文其余部分：§1–§5 长青参考仍有效**」—— 这句正是本次分家逐节核对后**被证伪**的说法。当时我照抄了旧件阅读指南的自我声明，没核。冻结公告已改：明确写出 §2 `:395` 与 §3.3 `:1011` 两处失效，并把「长青不是可以自我声明的属性」这条教训固化在冻结件顶部（新件 §4 也有一份）。

**这条值得记下来的地方不在于改了一句话**：批次一的边界是「只 distill 已拍事实，零新设计判断」，我在那个边界内**忠实转述了一个错误的既有声明**。忠实转述不等于正确 —— 一份文件的自我声明也是「叙述者」，同样适用「别信叙述者，信机械收据」。分诊表里我判旧件「重建更划算」用的正是这类证据，但打横幅那一刻没把同一把尺子用在自己抄的那句上。

### 03/04 合并的同步

Fable 今天把 03/04 合并成一张 03 单（合取约束以「同批 + 存量迁移」双保险落实），并直接改了我草案的 §3.5（commit `0aa8320`）。已确认改动正确，未回退。**我的合取发现从「二级复盘里的一条提醒」变成了「拆单形制的依据」** —— 这是复核汇总线第一次真正改变了施工计划的形状。

### 判断点（供 Henry 复核）

1. **`docs/Coincides-Roadmap.md`（v2.0–2.5.6 那份）里指向旧 BN 路线图的三处引用我没改**。它自身是 `archived` 历史层，按 `AGENT_CONTEXT §2`「禁止回头修改历史层文档，或修复其失效的内部引用（历史就地冻结）」—— 不动是守纪律，不是漏做。
2. **冻结公告写得比一般横幅长**（含「为什么是冻结不是更正」与「长青标签不可信」两节）。理由：这份文件将来还会被人翻（决定史在里面），一个只说「已作废」的横幅不能阻止下一个人再次相信它的 §1–§5。可逆：删减即可。
