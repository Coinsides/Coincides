> **状态 (Status)**: active
> **日期**: 2026-08-31

# Claude 工作日志 2026-08-31

## M1 / c-2(双拓互证·实验层)收官 —— 三轮停线,两条新维,一次两遍制重构

**结果**:v3 **PASS 无停线**。门遍 **856 全量走完**(覆盖率 24 行 `remaining=0`),`856 = 175 存在性差异 + 150 pairing_unresolved + 211 分类学差异 + 320 classification_full`,四集两两不交;分类遍 **320/320**,**本轮未触发 K-3** 并如实申报。终审五路(四取证 + 一对抗)**零 blocking**。

**三轮的形状**:
| 轮 | 停在哪 | 缺的维 | 覆盖率 |
|---|---|---|---|
| v1 | 第 2 个事件 | **存在**(有没有) | —— |
| v2 | 第 18 个事件 | **是什么**(角色) | 存在域 **6.5%**(9/138) |
| **v3** | **不停** | —— | **100%**(175/175) |

⭐ **连停两次是同一个病:骨架比宪法少维。** 四分类全在问「长什么样」与「在哪」,没有一把在问「有没有」或「是什么」;而 v0.7.4 早写着「锚答在哪,角色答是什么」,12.9a:204 一个月前已命名「分类学差异」并判「两者都不算错」。**名字早就有,骨架把它漏了。**

**⭐⭐⭐ 头条:v2 停在了一个占 0.9% 的形态上。** 我从 5 MB canonical JSON 独立重算:9 种有序对,`MinerU[text]×Docling[list]` **194 条(92%)**,而 v2 撞停的 `[heading]×[text]` 只有 **2 条**。⇒ **你撞到的第一个,几乎从不是最有代表性的那个。**

**⭐⭐ 两遍制**(总部裁):**普查是点名,判定是判刑;点名必须点完,判刑可以停堂,⛔ 两者不共用一条停线流。** 成因是 v2 把一个明令「不停线」的域塞进了「一停即全停」的流 ⇒ **「不停线」只意味着它自己不触发停线,不意味着它不被别人的停线打断。**

**⭐⭐⭐ 分母本身被从第一性原理验证**:终审从原始 8 份文件独立重建 —— 自数 1,682 fragment、三谓词划分 0 mismatch、事件重建 772+64+20=856、exactly-once 双向验、**705/705 收据成员集合逐一命中**、残差恰为 151 non_divergence。递归自检最后一环闭合。

**⚠️ 本轮立的新法(全部由现物逼出)**:
1. **分类骨架必须显式申报维度覆盖**(⛔ 申报是射程不是赦免);
2. **口径冻结于【实现】之前**,不止于实跑之前;
3. **普查是点名,判定是判刑** + **分母义务** + **唯一副本禁令**;
4. **开工回执是一句会过期的真话**(中断后重派第一动作 = 与现物对齐);
5. **SHA 只证明冻结段没被改,⛔ 不证明实现忠实于它**;
6. ⭐ **提示 ≈ 文档,不是门;原则靠记,动作靠抄**;
7. **未触发 ≠ 正确,只是未经检验**;
8. **你撞到的第一个,几乎从不是最有代表性的那个**;
9. 便宜量具名单添 `mtime`;元字符同罪名单添 `%`。

**⚠️ 我自己的账(本段共 5 笔)**:①「一家漏掉一个图」= 判对错(措辞失误);②「冻结早于实现 17 分钟」用 mtime 当首字节,余量说大一个数量级;③「洗白链」定性从形状推断意图(已更正);④修订段写在 `## Result` 之后导致闸看不见(**我自己立的法**);⑤`printf` 的 `%` 截断提交信息 + 写那条法的脚本被反引号杀死 + 验证的 grep 也被咬(**同一形状三连**)。**五笔全部入档,无一只修不记。**

**⭐ 对抗复核连续三轮抓到取证路的盲区** —— 总部原话:「**对抗复核这个岗位用数据证明了自己不可裁撤**」。⚠️ 尤其第二、三轮**同一条**(「零命中≠不存在」),而第三轮该法**已逐字内联进提示** ⇒ 这正是第 6 条法的出处。

**判断点(供 Henry 复核)**:
1. §4 编码破损我倾向「记瑕疵不重派」,总部裁为**并入 c-4 顺手修**(理由:破损列不该以永久面貌入史,而档案 c-4 才进版本历史)—— 这个中间道比我的方案好。
2. 三轮 builder 全部由**同一个 codex 线程**完成,五功/六功累计;**它的停线两次都是对的**,且第二次撞出的洞比停线本身值钱。
3. 本段最大的方法论收获我认为是第 6 条:**把法写进提示不等于它会被执行** —— 它同时解释了为什么第 1–5 条都必须做成**判据**而不是**叮嘱**。

## M2 / c-4 段收口 —— 12.9c 关段,⛔ 但只达成了一半,不粉饰

**结果**:12.9c **10 单全 done**,批 `c4b2a4a`。收段基线**本次实测**(⛔ 非引用历史数字):`test:v2` **351/351 pass**、`tsc --noEmit` exit 0、`docs:check` exit 0、tech-debt 表格闸 exit 0。

**⭐⭐⭐ 本条最该被读的一句:段目标只达成一半,我把它写进了收口件的第 2 节而不是脚注。**

| 段计划承诺 | 实况 |
|---|---|
| T0 存量对齐碎片流 | **只对齐四种**(pdf/docx/txt/md);⛔ `xlsx`/`csv` 仍 `stored_only` |
| MinerU 接入 | 转写器已注册,⛔ 须 `COINCIDES_PDF_PARSER` 显式点名,两个 env 文件**都没设** ⇒ 默认路径未变 |
| c-2 双拓互证 + 置信度落出生证 | 只做了**实验层普查**;⛔ Docling 在 `server/src` **零文件**,置信度未落出生证 |
| c-3 识别器引用模式 + **引用校验闸** | ⛔ **全段未开单** |

⇒ **一句话**:12.9c 让碎片**写得进**了,⛔ 没让任何人**读得出** —— `getImprintFragmentsByAnchor` 生产调用者仍为零。段计划要「闸与它的对手同批到位」,**对手没到场,闸也就没落**。

**⭐⭐ 词典写作时被我自己立的门拦了一次。** 原拟在现状栏写「MinerU **已接入**」—— 而「已接入」正是 `glossary-shape-vs-capability.test.mjs` 的 K-1 禁词之一(五词定表:已 live/已接入/已打通/已可用/已上线)。**「形状 ≠ 能力」这条法长成机关之后,第一个被它拦住的是立法者本人。** 这正是上一条日志第 6 条法(**提示 ≈ 文档,不是门**)的正面例证:同一条道理,写在提示里我照写不误,做成门就拦住了。

**⛔ 而且被拦得对**:我去查了现物才发现,不只措辞过头 —— `COINCIDES_PDF_PARSER` 在 `.env` 与 `.env.experiment` 里**都没有定义**(`grep -c` 各为 0),所以 PDF 的默认路径**根本没变**。若照原措辞写下,词典会留下一句需要下一个人去证伪的假话。

**⚠️ 巡检自己挖出一条新债 TD-38**:纲领 §2 的 T0 列了六格式,而 `SOURCE_FORMATS` 里 `xlsx`/`csv` 的 `capability` 仍是 `stored_only` ⇒ **入库收得下,征用时不进转写、不写出生证、不产碎片**。⛔ **不并入 TD-30**:TD-30 是「PDF 里的表塌成一条」(单元格几何不可达),TD-38 是「表格格式压根没进链」——生产者不同、不可达原因不同,合并会把两笔账都记糊。

**c-4-a 复核 PASS**:六判据我**全部自己在磁盘上重算**,未采信回执自述(§4.1=175、§4.2=211、101+74=175、ASCII `?` 由 388 行降为 **0**、`×`/`→` 全角正常、空侧渲染用破折号);§6 证据本体端到端亲验:7,731 行 Base64 → 440,641 字节 gzip → 5,096,178 字节原文,SHA `2bfc5b89…` 逐字符相符。⇒ c-2 签署时挂的两条已知瑕疵至此**闭环**。

**欠账移交**(收口件第 4 节全表):TD-38(新)· TD-37(c-3 入场前置,须 Henry 定夺 `DASHSCOPE_API_KEY` 两处留哪个)· TD-30(已诚实拆分)· TD-28/33/34/35(非本段引入);未入表的:c-3 债、c-2 持久层债、151 个 `non_divergence` 无逐事件收据、P-TEXT 772 依赖同一 pin、3 路审计 transcript 在仓外、`m:n` 条款**本轮未被检验**(照「未触发 ≠ 正确」如实记)。

**判断点(供 Henry 复核)**:
1. **我把「段目标只达成一半」放在收口件第 2 节(正文第一节之后)而不是末尾的欠账里。** 判断依据:收段文件将来最常被引用的是「12.9c 做完了什么」,若达成度藏在末节,引用者会默认全达成。⚠️ 若你认为收口件该以交付清单为主调、达成度对账降级,请纠。
2. **TD-38 我选择单列而非并进 TD-30。** 依据是本段立的「债的落点认第一个生产者」——两笔债的生产者不同(一个要 `sheet` 锚生产者,一个要 PDF 单元格几何)。⚠️ 若你更希望「表格类欠账」归一条便于追踪,可合并,但请保留两者不可达原因的区分。
3. **c-3 未做我没有自行开单补上**,而是如实关段并移交。依据:c-3 的入场前置 TD-37 是**只有你能答**的问题(`.env` 两处 key 留哪个,那是你的账号)。⛔ 我不猜、不删、不试调。
4. 收段基线我坚持**当场实跑**而非引用 c-1 收口的 350。多出的 1 是 `d777c36` 之后的增量,同命令同入口可比。
