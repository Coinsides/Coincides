> **状态 (Status)**: active
> **日期**: 2026-08-31

# Claude 工作日志 2026-08-31

## M1 / c-2(双拓互证·实验层)收官 —— 三轮停线,两条新维,一次两遍制重构

**结果**:v3 **PASS 无停线**。门遍 **856 全量走完**(覆盖率 24 行 `remaining=0`),`856 = 175 存在性差异 + 150 pairing_unresolved + 211 分类学差异 + 320 classification_full`,四集两两不交;分类遍 **320/320**,**本轮未触发 K-3** 并如实申报。终审五路(四取证 + 一对抗)**零 blocking**。

**三轮的形状**:
| 轮 | 停在哪 | 缺的维 | 覆盖率 |
|---|---|---|---|
| v1 | 第 2 个事件 | **存在**(有没有) | —— |
| v2 | 第 18 个事件 | **是什么**(角色) | 存在域 **6.5%**(9/138) |
| **v3** | **不停** | —— | **100%**(175/175) |

⭐ **连停两次是同一个病:骨架比宪法少维。** 四分类全在问「长什么样」与「在哪」,没有一把在问「有没有」或「是什么」;而 v0.7.4 早写着「锚答在哪,角色答是什么」,12.9a:204 一个月前已命名「分类学差异」并判「两者都不算错」。**名字早就有,骨架把它漏了。**

**⭐⭐⭐ 头条:v2 停在了一个占 0.9% 的形态上。** 我从 5 MB canonical JSON 独立重算:9 种有序对,`MinerU[text]×Docling[list]` **194 条(92%)**,而 v2 撞停的 `[heading]×[text]` 只有 **2 条**。⇒ **你撞到的第一个,几乎从不是最有代表性的那个。**

**⭐⭐ 两遍制**(总部裁):**普查是点名,判定是判刑;点名必须点完,判刑可以停堂,⛔ 两者不共用一条停线流。** 成因是 v2 把一个明令「不停线」的域塞进了「一停即全停」的流 ⇒ **「不停线」只意味着它自己不触发停线,不意味着它不被别人的停线打断。**

**⭐⭐⭐ 分母本身被从第一性原理验证**:终审从原始 8 份文件独立重建 —— 自数 1,682 fragment、三谓词划分 0 mismatch、事件重建 772+64+20=856、exactly-once 双向验、**705/705 收据成员集合逐一命中**、残差恰为 151 non_divergence。递归自检最后一环闭合。

**⚠️ 本轮立的新法(全部由现物逼出)**:
1. **分类骨架必须显式申报维度覆盖**(⛔ 申报是射程不是赦免);
2. **口径冻结于【实现】之前**,不止于实跑之前;
3. **普查是点名,判定是判刑** + **分母义务** + **唯一副本禁令**;
4. **开工回执是一句会过期的真话**(中断后重派第一动作 = 与现物对齐);
5. **SHA 只证明冻结段没被改,⛔ 不证明实现忠实于它**;
6. ⭐ **提示 ≈ 文档,不是门;原则靠记,动作靠抄**;
7. **未触发 ≠ 正确,只是未经检验**;
8. **你撞到的第一个,几乎从不是最有代表性的那个**;
9. 便宜量具名单添 `mtime`;元字符同罪名单添 `%`。

**⚠️ 我自己的账(本段共 5 笔)**:①「一家漏掉一个图」= 判对错(措辞失误);②「冻结早于实现 17 分钟」用 mtime 当首字节,余量说大一个数量级;③「洗白链」定性从形状推断意图(已更正);④修订段写在 `## Result` 之后导致闸看不见(**我自己立的法**);⑤`printf` 的 `%` 截断提交信息 + 写那条法的脚本被反引号杀死 + 验证的 grep 也被咬(**同一形状三连**)。**五笔全部入档,无一只修不记。**

**⭐ 对抗复核连续三轮抓到取证路的盲区** —— 总部原话:「**对抗复核这个岗位用数据证明了自己不可裁撤**」。⚠️ 尤其第二、三轮**同一条**(「零命中≠不存在」),而第三轮该法**已逐字内联进提示** ⇒ 这正是第 6 条法的出处。

**判断点(供 Henry 复核)**:
1. §4 编码破损我倾向「记瑕疵不重派」,总部裁为**并入 c-4 顺手修**(理由:破损列不该以永久面貌入史,而档案 c-4 才进版本历史)—— 这个中间道比我的方案好。
2. 三轮 builder 全部由**同一个 codex 线程**完成,五功/六功累计;**它的停线两次都是对的**,且第二次撞出的洞比停线本身值钱。
3. 本段最大的方法论收获我认为是第 6 条:**把法写进提示不等于它会被执行** —— 它同时解释了为什么第 1–5 条都必须做成**判据**而不是**叮嘱**。

## M2 / c-4 段收口 —— 12.9c 关段,⛔ 但只达成了一半,不粉饰

**结果**:12.9c **10 单全 done**,批 `c4b2a4a`。收段基线**本次实测**(⛔ 非引用历史数字):`test:v2` **351/351 pass**、`tsc --noEmit` exit 0、`docs:check` exit 0、tech-debt 表格闸 exit 0。

**⭐⭐⭐ 本条最该被读的一句:段目标只达成一半,我把它写进了收口件的第 2 节而不是脚注。**

| 段计划承诺 | 实况 |
|---|---|
| T0 存量对齐碎片流 | **只对齐四种**(pdf/docx/txt/md);⛔ `xlsx`/`csv` 仍 `stored_only` |
| MinerU 接入 | 转写器已注册,⛔ 须 `COINCIDES_PDF_PARSER` 显式点名,两个 env 文件**都没设** ⇒ 默认路径未变 |
| c-2 双拓互证 + 置信度落出生证 | 只做了**实验层普查**;⛔ Docling 在 `server/src` **零文件**,置信度未落出生证 |
| c-3 识别器引用模式 + **引用校验闸** | ⛔ **全段未开单** |

⇒ **一句话**:12.9c 让碎片**写得进**了,⛔ 没让任何人**读得出** —— `getImprintFragmentsByAnchor` 生产调用者仍为零。段计划要「闸与它的对手同批到位」,**对手没到场,闸也就没落**。

**⭐⭐ 词典写作时被我自己立的门拦了一次。** 原拟在现状栏写「MinerU **已接入**」—— 而「已接入」正是 `glossary-shape-vs-capability.test.mjs` 的 K-1 禁词之一(五词定表:已 live/已接入/已打通/已可用/已上线)。**「形状 ≠ 能力」这条法长成机关之后,第一个被它拦住的是立法者本人。** 这正是上一条日志第 6 条法(**提示 ≈ 文档,不是门**)的正面例证:同一条道理,写在提示里我照写不误,做成门就拦住了。

**⛔ 而且被拦得对**:我去查了现物才发现,不只措辞过头 —— `COINCIDES_PDF_PARSER` 在 `.env` 与 `.env.experiment` 里**都没有定义**(`grep -c` 各为 0),所以 PDF 的默认路径**根本没变**。若照原措辞写下,词典会留下一句需要下一个人去证伪的假话。

**⚠️ 巡检自己挖出一条新债 TD-38**:纲领 §2 的 T0 列了六格式,而 `SOURCE_FORMATS` 里 `xlsx`/`csv` 的 `capability` 仍是 `stored_only` ⇒ **入库收得下,征用时不进转写、不写出生证、不产碎片**。⛔ **不并入 TD-30**:TD-30 是「PDF 里的表塌成一条」(单元格几何不可达),TD-38 是「表格格式压根没进链」——生产者不同、不可达原因不同,合并会把两笔账都记糊。

**c-4-a 复核 PASS**:六判据我**全部自己在磁盘上重算**,未采信回执自述(§4.1=175、§4.2=211、101+74=175、ASCII `?` 由 388 行降为 **0**、`×`/`→` 全角正常、空侧渲染用破折号);§6 证据本体端到端亲验:7,731 行 Base64 → 440,641 字节 gzip → 5,096,178 字节原文,SHA `2bfc5b89…` 逐字符相符。⇒ c-2 签署时挂的两条已知瑕疵至此**闭环**。

**欠账移交**(收口件第 4 节全表):TD-38(新)· TD-37(c-3 入场前置,须 Henry 定夺 `DASHSCOPE_API_KEY` 两处留哪个)· TD-30(已诚实拆分)· TD-28/33/34/35(非本段引入);未入表的:c-3 债、c-2 持久层债、151 个 `non_divergence` 无逐事件收据、P-TEXT 772 依赖同一 pin、3 路审计 transcript 在仓外、`m:n` 条款**本轮未被检验**(照「未触发 ≠ 正确」如实记)。

**判断点(供 Henry 复核)**:
1. **我把「段目标只达成一半」放在收口件第 2 节(正文第一节之后)而不是末尾的欠账里。** 判断依据:收段文件将来最常被引用的是「12.9c 做完了什么」,若达成度藏在末节,引用者会默认全达成。⚠️ 若你认为收口件该以交付清单为主调、达成度对账降级,请纠。
2. **TD-38 我选择单列而非并进 TD-30。** 依据是本段立的「债的落点认第一个生产者」——两笔债的生产者不同(一个要 `sheet` 锚生产者,一个要 PDF 单元格几何)。⚠️ 若你更希望「表格类欠账」归一条便于追踪,可合并,但请保留两者不可达原因的区分。
3. **c-3 未做我没有自行开单补上**,而是如实关段并移交。依据:c-3 的入场前置 TD-37 是**只有你能答**的问题(`.env` 两处 key 留哪个,那是你的账号)。⛔ 我不猜、不删、不试调。
4. 收段基线我坚持**当场实跑**而非引用 c-1 收口的 350。多出的 1 是 `d777c36` 之后的增量,同命令同入口可比。

## M3 / d-0 收工 —— 401 定因为区域端点;⚠️ 同一天我把一个对的记录"更正"成了错的

**结果**:d-0 PASS,批 `e1a4a8b`。锁已释放。**401 不是 key 无效,是区域端点不匹配。**

**⭐ 单变量对照**(同一把 key、同一模型、同一 payload,只换主机名;累计 2 次调用 / 26 字符,上限 3 次 / 200 字符):

| 端点 | HTTP | 维度 | 模型标识 |
|---|---|---|---|
| `dashscope.aliyuncs.com`(北京) | **401** | 不可得 | 不可得 |
| `dashscope-intl.aliyuncs.com`(国际站) | **200** | **1024** | `text-embedding-v4` |

⇒ ①key 有效、模型有权限;②**c-3 会撞同一堵墙** —— `qwen-vl-max` 用的是同一把 key,应在开单前钉死端点,⛔ 别等它再 401 一次;③**K-3 的陷阱从假设变成现实**:实测就是 1024 维,与现役 `doc_chunk_vec` 的 `float[1024]` 完全同形 ⇒ **混表是一个不报错就能做成的动作**,`model_id` 入行是承重件不是装饰。

**⚠️⚠️ 我自己的账(本条最该被复核的一笔)**:今天早些时候我把 TD-37 的 key 长度从 **116 "更正"为 117**,还写了「已排除 CR 伪影」。**错的是我。** 该行是 `DASHSCOPE_API_KEY=<空格><key>` —— **值前有一个前导空格**;我的 `awk` 量的是含空格的原始捕获,而且**我只查了尾部空白和末字符,从没查首字符**。有效长度 **116**,台账原值一直是对的(`dotenv@17.3.1` 实测会 trim,生产不受影响)。

⭐⭐ **两条法**:
1. **更正本身需要证明资格。** 我用**更差的量具**去更正一个由更好量具得出的记录 —— ⛔ 不是谁最后动手谁算数。「更正」这个动作自带权威感,**反而更该被要求出示证据**。
2. **你先想到的那个,也几乎从不是唯一的那个。** 首尾是对称的两处,我查了尾就宣布"已排除伪影"。这是「你撞到的第一个几乎从不是最有代表性的那个」的**孪生形态**。
3. ⚠️ 附带损失:第 9 节的大结论(**债务台账也会过期**)是对的,⛔ 但我在里面塞了一个错的小结论 —— **错的小结论会连带污染大结论的可信度**。

**⭐ builder 三处记功**:①K-3 没被"看起来对"糊弄,明写「请求里的 1024 只是请求参数,**不能冒充实测结果**」;②停线**没越权下结论**,写明「无法仅凭受控标量区分 key 无效 / 权限不足」,把判断权留给下一个人;③**它更正了我的 K-0** —— 现役检索真入口是 **Agent 消息路由**(`routes/agent.ts` → `orchestrator` → `tools/executor.ts:610` → `VectorStore`),`/api/embedding` 只做 status 与 backfill、**不做 KNN**;我的侦察件那张表不算错但不完整。

**另两笔量具自纠**:①派单命令尾接 `| tail -60`,管道缓冲到结束才写文件 ⇒ **我把自己的监视器弄瞎了**,"0 字节"证明不了任何事,改看产物文件是否长出来;②内联引号第 N 次咬我(`node -e` 里嵌单引号),仍是「脚本进文件」解决。

**判断点(供 Henry 复核)**:
1. **我在复核里用掉了工单预算的 1/3**(3 次里的第 2 次)去做端点对照。判断依据:401 若不定因,d-1 会带着一个假前提开工,而这次对照是**单变量、零风险、成本近零**的。⚠️ 若你认为复核方不该动工单预算、该另开一单,请纠。
2. **端点这件事我倾向立即钉死成常量**(而不是等 d-1 自然发现)——因为它跨段影响 c-3。已写进 d-0 报告与本条,⛔ 但没自行改生产码。
3. 上面那笔"更正错误"我是**在 d-0 复核时被 builder 的独立测量撞出来的** —— 若不是它报了 116 与我的 117 打架,我不会回头查。**两个独立量具打架,是我今天最有价值的一次报警。**

## M3 / d-1a 收工 —— 规模落地 110,但撞出「粒度是页不是块」

**结果**:PASS,批 `2ad0034`,锁已释放。**规模从假设变实测:110 碎片 / 114,066 字符 / 4 accepted 0 rejected / 全 page fidelity。**

**复核方式**:⛔ 未采信回执任何数字,**自写查询独立复算**(且未看 builder 贴的 SQL)。七项逐一对上,含 `fragment_count` declared=actual 四件全中(46+5+26+33=110)、`imprint_fragment_vec` 表不存在。证据本体只读**三方一致**:我独立算的四份 PDF SHA 与回执灌前、灌后两组逐件相等。

**⚠️⚠️ 设计级发现:碎片粒度是「页」,不是「块」**

同四卷,MinerU 侧(c-2 普查已入档)320+69+152+267=**808**,本单 native-pdf 侧 46+5+26+33=**110** ⇒ **7.35 倍**,最悬殊一卷 **13.8 倍**。且 `role` 分布是 **`para` 110 / `heading` 0**(100% 单一角色),长度 median 643、max 3,675 字符。

⇒ **d-1b 若在这 110 个上建检索,一次命中返回的是「大半页」,不是「一个可寻址单元」。** page 级是保真阶梯的**地板**(宪法明写低保真合法),⛔ 但它把 12.9d 的检索质量**预先钉在地板上**。

**我给总部的建议**:d-1b 照常做,⛔ 不先切 MinerU —— **管线正确性与碎片粒度是两条独立的轴**;110 个够把全链跑通、成本不到 ¥0.02,而 MinerU 是重 CPU 子进程,姑息模式下不该为"更好看的数"去扛。**但 d-1b 产出必须硬性声明:本轮检索质量只代表 page 级地板上的质量。** 候裁。

**📌 一处校准**:d-0 按上限假设估 48,000 碎片,实测 110 ⇒ **假设高出 436 倍**。存储选型在此规模下**不承重**(110 条向量,暴力余弦与 sqlite-vec 无差别)。⛔ 但**不**构成推翻「另起表 + `model_id` 入行」的理由 —— 那是**正确性**要求(同维不同空间),与规模无关。**规模能推翻性能选型,推翻不了正确性条款。**

**⭐⭐ builder 记功:它拒绝伪造一个 exit code。** 证据树不是 git worktree,`git diff --quiet` 返 **128**。它**没把 128 当 0 写进表,也没省略那一行**,而是明写「不适用,改以前后 SHA 逐件相等作为该禁区的字节自证」。⇒ **一个不适用的量具,它的输出不是证据。**

**另一件:家法文件自己有 118 行重复块。** 追加新法前例行查结构,发现 1371–1488 行是 1011–1128 行的**逐字副本**(SHA 相同,7 个标题成对),已删,现重复标题数 0。⚠️ 危害是**静默**的 —— 读起来完全正常,只是把同一批法说了两遍。`tech-debt` 与 `glossary` 各有常驻闸,**README 没有**;按「同形第二次即机械化」这是第一次,先记不立闸。

**判断点(供 Henry 复核)**:
1. **粒度这条我选择了"报而不停"** —— 上报总部候裁,同时把 d-1b 的形先想好,⛔ 没有自行改单也没有停在那里等。依据:它改的是 d-1b 的**声明义务**,不改 d-1b 该不该做。
2. **「规模能推翻性能选型,推翻不了正确性条款」** 是我这轮自己划的线。实测 110 让存储选型变得无所谓,我**没有**顺势把「另起表 + model_id」也一并放松 —— 那两件事的理由根本不同。⚠️ 若你觉得这条划得太死,请纠。
3. 家法文件的重复块**我按自己的法只记不立闸**(第一次)。⚠️ 但它与 tech-debt 表格闸是同一形状的危害(结构坏了不报错、只静默说谎),你若认为该现在就立,我立。

## ⛔⛔ M3 / d-1b **作废停线** —— 凭证事件:被删掉的旧 key 没死,只是搬了家

**结果**:d-1b **未收工,已作废**。锁已释放。**零 API 调用、零花费、零文件改动**(`git status` 仅剩开工前即存在的 EOL 噪音)。已入 **TD-39**。

**经过**:builder 一条 PowerShell 只读搜索因**续行写法错误逸出仓库范围**,工具输出**完整显示了一把 `DASHSCOPE_API_KEY`**。它**当即按工单 §0.6「出现即本单作废」自行停线** —— 未调 API、未写任何文件、未复述该值、**未写 `## Result`**(明说"避免把作废工单冒充完成"),并主动建议轮换 key。⭐ **这是停线该有的样子:它宁可交白卷,也不交一份看起来完成的东西。**

**⚠️⚠️ 我核查后发现的事,比 builder 报的更要紧**(⛔ 全程未回显任何值,只查变量名/长度/路径):

暴露的那把是 **`DASHSCOPE_API_KEY`,值长度 35** —— **正是 TD-37 记的 `.env` 旧值长度**,也就是 **Henry 08-30 亲令删掉的那一把**。有界排查(限 `D:/Coinsides/` 项目家族目录,⛔ 未搜文档/学习目录)找到存活副本:

**`D:\Coinsides\Coincides_Private\app-data\dot.env`,长度 35**;而本仓 `.env.experiment` 现值长度 **116**,两者不同。

⇒ ⭐⭐⭐ **删掉一处出现,不等于清除了这个秘密。** TD-37 我判为"✅ 已清偿",依据是 `.env` 里 `DASHSCOPE` 出现 **0** 次 —— 那个核查**本身没错**,但**射程只到一个文件**。**清偿一个副本 ≠ 清偿这一类。**

**遏制核查(我独立跑的)**:本仓 tracked 命中 **0** · 暂存区 **0** · 调度会话任务输出命中 **0** · 仓内唯一含现值的文件是 `.env.experiment` 且**已 gitignore** ⇒ **仓库未被污染**。⚠️ 但该值**持久化于 codex 会话记录** `rollout-…T08-21-28-….jsonl`(2 行)。

**⛔ 我没有做、也不会做的三件**(全属 Henry 本人权限):轮换/吊销那把 key · 删除仓外副本 · 清理 codex 会话记录。**⛔ 并已冻结 d-1b 重派** —— 重派会让同一目录再次进入搜索射程。

**判断点(供 Henry 复核)**:
1. **我把 d-1b 冻结而不是"改个搜索范围重派"。** 依据:重派前那份副本还在原地,同一形状会再犯;且是否轮换 key 只有你能定,而**在你定之前重派 = 让一个可能已作废的秘密再暴露一次**。⚠️ 若你认为可以先删副本再重派、不必等轮换,请纠。
2. **有界排查我只搜了 `D:/Coinsides/`**,⛔ 没搜 `Documents` / OneDrive / 学习目录。依据:那是你的私域,越界搜索本身就是二次伤害。⚠️ **⇒ 我不能保证"只有这一处副本"** —— 我只能说"在项目家族目录里只有这一处"。这个射程限制**必须由你知道**,⛔ 不能让它伪装成"已排查干净"。
3. **TD-37 我判"已清"是判早了。** 核查没错,射程太窄。⭐ 与我今天那笔"更正错误"是同一族:**证明的资格取决于射程,不取决于动作做没做。**
