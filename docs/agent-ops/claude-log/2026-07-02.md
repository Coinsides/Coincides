> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State（Claude 行动日志 · 当日）
> **日期 (Updated)**: 2026-07-02

# Claude 行动日志 — 2026-07-02

> 最新在最上。本日主线：审 Codex 交回的第 1 份高危 burn-down handoff（V2.BN.8.11.12 Stage 1）——5 维对抗核查 + 亲验铰链点 + register 校正 → 写第 2 份 handoff（block-backed-shape 生命周期）→ 审第 2 份落地（V2.BN.8.11.13，PASS）→ 写第 3 份 handoff（表格 mutation 完整性）+ 盘 MED 余量 → 多 agent 起 Stage-4/5 地基 + 落两份 handoff。

## 9 — 审 Stage-4 续修（fix-2 落地）· Stage-4 整个收口

- **做了什么**：Codex 落地 fix-2（收 CR-8.11.15-07 null-offset 漏渲染 + -08 036 对称 + -09 测试锁）。起 3 维对抗核查（wu8312hor），火力集中在**过度抑制**（换 isRenderable 会不会误杀有效 range = 反向红线）。
- **结论：全 3 维 PASS，Stage-4 整个收口。** 消费门**全换 isRenderable、全树零 !isPending 残留**；**过度抑制不可能**（数字 offset 要求只作用 text_span、其它 5 kind 放行，正控有效 range 照渲已锁）；inspector 显示+Needs-review 不隐藏（重锚面保住）；036 对称 strip。四数字实跑：cutover 43 / model-contract 57 / boundary 159 / test:v2 172。活探证实门被改回即 fail。
- **残 LOW**（CR-8.11.15-10）：resolveRangePreviewFromBlocks 靠 slice 偶然不漏非显式 gate；copy handler 无专属锁（复用 drag 锁）。皆非泄漏、纵深。
- **判断点（供 Henry 复核）**：
  ① **Stage-4 完全 done**：从"第一单未一次通过"→ 修正单封红线 → 续修单收 null-offset，**对抗核查三轮各兑现价值**（红线 / null-offset 残留 / 反向过度抑制）。register §8.11.15 收口判断已终。
  ② **⚠ 跨 stage 反复的过程风险**：smoke 依赖 `.codex-tmp` 增量 tsc、stale 构建会假 fail 或**掩盖真 fail**（Stage-3 + 本轮都撞）——**已在 register 醒目标注：收口 commit 必须干净构建跑 CI**。这条比任何单个 LOW 都重要,因为它威胁所有绿测证据的可信度。
  ③ 战略：annotation 父子层级经 Henry 拍板=时代眼泪·无害留着（记忆 [[annotation-hierarchy-is-vestigial]]）；不再投入。
  ④ 盘面：Stage-1/2/3/4 全落地+审过；Stage-5（image）draft 仍攒着；收口 commit 待推（干净构建 + 回填 SHA）。

## 8 — 审 Stage-4 修正（annotation-truth-fix 落地）· 红线已封 + 挖出同族 HIGH 残留

- **做了什么**：Codex 落地修正单。亲验 F1 双向权威（写侧 460 `stringifyJson(stripDerivedAnchorMetadata(...))` + 读侧 249 先剥后重算 + helper 65-69），再起 4 维对抗核查（wnwuva59q）。
- **结论：红线 BLOCKER 真封死。** 3 维 PASS + **活探证实红线锁 fail-before**（去掉写剥→红线测试 fail、恢复→cutover 42/42、test:v2 171、model-contract 57）。durable 永不含 anchor_*、每读重算 → trash→存→restore 高亮必复活。CR-8.11.15-01/03/04 ✅、CR-8.11.1-03 ◐→☑。消费者覆盖比我列的更广（含 ContentGroup Rail/drag/ReadingInterpretation-AI）。
- **但纯对抗维挖出新 HIGH CR-8.11.15-07**（同 -02 家族的另一触发）：overlap 失效的 **null-offset range 在健康块上非 pending**（deriveRangeMetadata 只对 number offset 的越界标 pending），而 F2 消费门用 `!annotationRangeIsPending`（只看 anchor_status）**放过它** → 漏进 badge/marker/AI、画在坐标 0 + stale 文本。亲验：`annotationRangeIsRenderable` 已正确排除 null-offset，**消费者没用它**。修法干净：消费门 `!isPending`→`isRenderable`。另 -08（036 backfill 不对称·defused）、-09（读侧 strip 未独立测）。
- **判断点（供 Henry 复核）**：
  ① **红线安全**，Stage-4 可算"red-line done"；但建议**一并小修 CR-8.11.15-07**（同族、干净）再标 Stage-4 完全 done。
  ② **对抗核查两轮都兑现价值**：第一轮抓红线 BLOCKER（40/40 绿测漏掉）、第二轮抓 null-offset 残留（连"消费者覆盖"维自己都判 PASS、是**纯对抗维**挖出）。这正是换防里"验收锁 + 独立对抗"不可替代的证据——两次都是绿测/常规覆盖看不见的。
  ③ **已写续修 handoff** `2026-07-02-8.11-annotation-truth-fix-2.md`（draft·Henry 定"先做完 Stage-4"）：F1 消费门 `!isPending`→`annotationRangeIsRenderable`（渲染/AI/导出/复制，inspector 例外·显示+标未锚定）+ 不过度抑制正控 / F2 036 backfill 也 strip 对称 / F3 读侧 strip 独立锁 + null-offset 测试。CR-8.11.15-05（跨 note 层级）：Henry 让我查，Explore 亲扫确认**跨 note 层级不可能存在**（存储按 note + 创建 UI 单 note + 036 dedup + 无 clone 搬层级）→ 静默清空安全、**✗ 不需改**，已闭环。

## 7 — 审 V2.BN.8.11.15（Stage-4 annotation 真相落地）· ⚠ 抓到红线 BLOCKER

- **做了什么**：Codex 落地 Stage-4（V2.BN.8.11.15）。起 5 维对抗核查（ws4iqn6tu），重点砸红线维。
- **结论：CONCERN——第一单未一次通过。** 簇 1（backfill·CR-8.11.1-02）+ 簇 2（服务端闸·CR-8.11.1-04）真 PASS；"块软删不删 range 行"守住了。**但簇 3 读时失效机制破了 = BLOCKER 红线数据丢失**（我亲验确认）：`deriveRangeMetadata` 把 pending 烤进读 DTO（hydrate:299）→ 保存 verbatim 持久化（453）→ 健康块不 strip（274）→ **trash→读→存→restore 后高亮永久消失**（相对不焊被违、正是本 stage 要根除的红线）。
- **对抗核查的价值实证**：cutover 40/40 + test:v2 169 全绿**没抓到这条**；活探把 trash 分支改坏、40/40 仍过 → 红线**零测试锁**。
- **亲验路径**：deriveRangeMetadata(238-275) 健康块 274 行原样返回不 strip；hydrateAnnotationTruth 299 行把派生 metadata 塞进 DTO；replaceNoteAnnotationTruths 453 行 verbatim 持久化 range.metadata。三段闭环 = durable 焊死。
- **记 register §8.11.15**：CR-8.11.15-01（BLOCKER 红线）、-02（HIGH·badge/marker 漏 pending 画错位）、-03（MED·overlap 永久 null 无 re-anchor）、-04（HIGH·红线零测试锁）、-05（LOW·跨 note 静默清·needs henry）、-06（LOW·预扫诊断质量）。**CR-8.11.1-03 已从 ☑ 降 ◐**。
- **判断点（供 Henry 复核）**：
  ① **Stage-4 不能算 done**——CR-8.11.15-01 是阻断项。修法明确且小：deriveRangeMetadata 双向权威（健康+在界 STRIP anchor_status，真读时派生每读重算）+ 纵深（save 前剥 anchor_* 不 durable 存）+ 消费者过 annotationRangeIsPending + 补红线往返回归锁。
  ② **换防试点：这单换防机制"接住了"**——Codex 主体对（backfill/服务端闸/不删行），但读时失效的**焊死方向**错了、且测试没锁红线；我这层对抗核查把它抓住了。正说明"验收锁 + 红线对抗"是换防不可省的闸。
  ③ **已写修正 handoff** `2026-07-02-8.11-annotation-truth-fix.md`（draft）：F1 pending 写剥+读重算双向权威（根治焊死）/ F2 消费者统一过 annotationRangeIsPending（badge/导出/AI）/ F3 overlap 留 re-anchor 依据 / F4 红线回归锁（必"中间存一次"才暴露·40/40 漏它的原因）/ F5 预扫诊断质量。1 判断点=跨 note 层级确认（CR-8.11.15-05）。等 Henry 翻牌。

## 6 — 审 V2.BN.8.11.14（Stage-3 表格 mutation 落地）· 多 agent 对抗核查

- **做了什么**：Codex 落地 Stage-3（表格 mutation）→ V2.BN.8.11.14。起 5 维对抗核查 workflow（wdw7viffs，多 agent 已恢复）：T1-history-core / T1-callsite-confirm / T2-T4-cell-edit / tests-adequacy-run / adversarial-new-pit，各实读代码 + 实跑 + 活探（禁分支看测试真不真 fail）。
- **结论：PASS（高置信），5 维全 PASS、零 blocker/high/medium。** 头号干扰点（structuredMutation 在 undo/redo 两函数都置于 trashedBlock 兜底前）**正确**；Codex 把 undo/redo 抽成 historyService.ts 两个可测纯函数 `applyRuntimeHistoryUndo/Redo`（比 spec 建议更好）；persistStructuredObject 真接进 persistCanvasObject（非空接口）；四数字实跑对上（57/159/167）；两条承重验收有真锁（破坏 deleteColumn 重编号→契约 fail；删 undo 分支→编译不过）。
- **亲挖 4 颗 LOW**（register §8.11.14）：CR-8.11.14-01（no-op/floor 删留假 undo 项·before 未 normalize vs after normalize）、-02（程序化 commit 后 native blur 冗余双 commit·同值幂等）、-03（undo 栈按引用存·今安全依赖 immutable 不变量）、-04（T2/T4/确认只字符串锁无行为测试）。皆不阻断。
- **判断点（供 Henry 复核）**：
  ① **过程观察**：整批 8.11.x **仍未提交**（working-tree only），smoke **首跑因 stale `.codex-tmp` flaky-fail、二三跑才过**——收口 commit 时须**干净构建跑 CI**，别让 flaky 首跑掩盖真回归。
  ② **换防试点第 3 单元读**：Codex 连续第三单交出干净活，且**主动做了比 spec 更好的重构**（抽纯函数可测），对抗核查零 blocker。够格继续放手；我这层价值稳定在"对抗挖新边 + 验收锁"（本单挖到 4 颗 LOW，其中 01/02 是测试没覆盖的真小瑕）。
  ③ 现 Stage-4/5 两份 draft handoff 攒着待翻牌；Stage-3 已收口（待 commit 时回填 SHA + 干净构建复跑）。

## 5 — 写 Stage-4（annotation 真相）+ Stage-5（image asset GC）handoff（多 agent 地基）

- **做了什么**：Henry 澄清 session limit 是 Fable 试用烧的、恢复多 agent 工作模式（见记忆 [[multi-agent-workflows-cleared]]）。起 workflow（w1wfk8imm）两条独立 track map→design→critique，据结果落两份 handoff（都 draft）：
  - `2026-07-02-8.11-annotation-truth-burndown.md`（Stage-4：CR-8.11.1-02/03/04）
  - `2026-07-02-8.11-image-asset-lifecycle-burndown.md`（Stage-5：CR-8.11.7-01/02/04 + CR-8.11.9-05）
- **workflow 两处大收获（改变了 spec 形状）**：
  ① **CR-8.11.1-03 的 register 前提过时**：offset-rebase 引擎（rangeRebaseService.ts 280 行）**已上线且接进普通文本块打字** → -03 不是"建 rebaser"而是"补覆盖洞 + 块删读时失效"，**远小于以为的、不需单独 stage**。
  ② **image bulk-delete 洞**：note/user 删走 FK cascade 不触发 cleanupOnDelete——但 critique 校正：删 note 是**软删**、无 hard-delete/account-delete 路由 → **是未来 gap 非当前泄漏**，不膨胀 Stage-5。
- **critique 抓到 annotation 2 个 blocker，已在 spec 解掉**：① 块 trash 是**软删可恢复** → 绝不删 range（否则块恢复后高亮永久消失=红线），改**读时派生失效**（block_id 解析不到 live 块就挂起、恢复自动复活）；② "invalidated flag" schema/类型里**根本不存在** → 解法=读时派生、不焊 flag（最合相对不焊）。
- **judgment（供 Henry 复核）**：
  ① workflow 部分失败（image track 一 agent 触顶 + 两条 mapper 返回 "test" 垃圾），但**design/critique agent 自己读代码补上了**，材料仍扎实；两处垃圾 map 不影响结论。
  ② Stage-4 有 4 个必裁判断点（repair-vs-reject / 跨 note 层级是否合法 / backfill throw-vs-mint / code-formula-shape 块能否带 offset 标注），Stage-5 有 sweep 范围 + 机制 + 7-04 defer 三个。都写进各自 handoff。
  ③ 现有 **3 份 draft handoff 攒着**（Stage-3 表格 [Henry 已把该单 status 翻 done——注：应是 ready 误翻或 Codex 已执行，待确认] / Stage-4 annotation / Stage-5 image），等 Henry 翻牌 + 定判断点。

## 4 — 写第 3 份 burn-down handoff（表格 mutation 完整性）+ MED 余量盘点

- **做了什么**：写 `2026-07-02-8.11-table-mutation-burndown.md`（from:claude/to:codex/status:draft）。锚 **CR-8.11.8-01（删表格行/列不可逆毁数据、无 undo）——按 register 优先级=清完数据丢失 HIGH 后唯一还开着的「数据丢失·产品红线」**。一件事=把表格编辑纳入和画布他处同等的 undo/confirm 保障。簇 T：T1（产品红线，给 `RuntimeHistoryEntry` 加 `structuredMutation` 型进 undo/redo + 非空删确认）+ T2（Esc-cancel 被 blur-save 覆盖·cancelledRef）+ T3（tableObjectService 零单测）+ T4（LOW·开菜单前 commit in-flight draft）。
- **亲读定 fix 的技术地基**：historyService.ts（3-型联合无 structured）+ usePlacementHistory.ts（undo/redo 栈）+ tableObjectService.ts:263-295（deleteRow/Column 硬 filter）。**逮到关键干扰点**：undo/redo 两函数用隐式"最后一支当 trashedBlock"兜底，加第 4 型必须在两函数里都显式处理、置于兜底前，否则表格 undo 被误当块恢复——已写进 spec 的"明确判断"。
- **MED 余量盘点（回答 Henry）**：当前**开着的「该修」🟡 MED = 24 颗**。分 7 组：表格 mutation(8-01/02/03) 3、annotation 真相(1-02/03/04) 3、image asset GC(7-01/02+9-05) 3、image 上传静默死路(7-04) 1、导出缺席族(4-07/7-03/8-05/9-02) 4【阻塞于"export 管线尚未建"】、inspector/可发现性 UX(3-04/9-01/03/04) 4【🅱 友好化】、sticky/style 校验(5-01/02/03/04) 4、连接线 UX(6-01/02) 2。〔另 ~50 LOW 硬化项归收尾扫，不计入"该修"〕
- **判断点（供 Henry 复核）**：
  ① **Stage-3 必裁**：删非空行/列的确认 UX（A 只 undo / B 只确认 / C 两者·我荐 C）。undo 是红线本质必须；确认是软 floor。
  ② **后续 stage 建议序**：Stage-4 annotation 真相（offset rebase + parent/child 完整性，注意 8.11.1-03 rebase 是较大件）→ Stage-5 image asset GC（refcount，8.11.9-05 已放大）。**导出缺席族(4 MED) 我判为"未建非丢失"、阻塞于 export 管线**，不算即时该修、归未来 export 专项。inspector/sticky/connector 偏 🅱 友好化，可地基后再扫。
  ③ **过程诚实**：Stage-3 的 undo 架构设计仍是主循环亲推（多 agent 对抗未跑，session limit）；验收测试是硬闸，我落地后核 diff（重点 undo/redo 双分支 + by-value 往返 + Escape 权威）。

## 3 — 审 V2.BN.8.11.13（第 2 份 handoff 的执行回执）

- **做了什么**：Codex 落地 `2026-07-02-8.11-lifecycle-burndown.md`（→ done + patch note V2.BN.8.11.13，声称 C1-C3/D1-D4/E1-E3 全完成）。因 session limit 风险，未用多 agent workflow，改在**主循环亲自审 diff + 对抗挖坑**。
- **结论：PASS（高置信）。** 六块承重件亲核代码全部正确：
  - **C1**（`assertNoteBlockStatusChangeAllowed` + `liveShapeBackingOwner` canvasObjects.ts:750-787）：守卫按 **object 的 backing='note_block'+object_class='block_backed'+active** 判定 live 拥有，**非 render_scope** → demote（先翻 backing='none'）、rollback（shape 没存成）天然放行。**这正是我 spec 要的读时派生，且比我预想更稳**（守卫看 object 状态而非 mount 是否清）。
  - **C2**（getNoteCanvasPersistence liveness 1574-1606）：`deadShapeTextObjectIds`（shape 集）只从 `liveMountRows` 剔除、object/placement 保留 → 图形退成无文本图形；`inactiveProjectionObjectIds`（projection 集）整体隐藏。kind-aware 拆分正确，CR-8.11.12-01 图形消失真修好。
  - **C3**（cleanup 695-748）：块 `status='trashed'` 软删 + 记 `canvas_lifecycle` 恢复信息 + placement hard-delete（restore 时按 restore_order_index 重建）→ 块可被 D2 复用。
  - **D1/D2/D3/D4** 客户端亲核：非空 demote `window.confirm`；re-promote 读 `last_backing_block_id` → `onRestoreBlockById` 复用；空文本存即 auto-demote（onShapeTextSave:3433）；restore 下沉 server 按 live 拥有剥焊。
  - **三处跨修法张力全解**（C3 软删块 vs D2 复用；C1 守卫按 live-owner vs demote/rollback；D4 剥焊 vs D2 重连）。
- **实跑复核四套数字**（Bash，非子 agent）：cutover **38/38**、model-contract **55 groups**、boundary **156 checks**、test:v2 **167/167**——全对上 Codex 声称。新测试 fail-before/pass-after 断言质量高（1532 守卫抛+demote 放行、1491 图形存活、1549 软 trash、1569 剥焊+重建、1467 补 E3 正控）。
- **亲挖 2 颗 LOW 新边**（register §8.11.13）：CR-8.11.13-01（re-promote 疑似偷回已 restore 编辑过的块·plausible 待确认）、CR-8.11.13-02（orphan restore 不剥 canvas_lifecycle·卫生）。皆不阻断、归 Stage-3。
- **判断点（供 Henry 复核）**：
  ① **诚实标注**：承诺的"多 agent 对抗重跑"没做（session limit 风险），改主循环亲审。个人审已深 + 测试强，判 PASS；若要更高置信可 limit 重置后补跑多 agent cross-check。
  ② **换防试点第 2 单元读**：Codex 这次比 Stage-1 明显进步——**主动把 Stage-1 学到的"正控"教训推广**（1467 补正控），register 记账准确无过度声称，spec 的三处张力解全部落地正确。上一单我扣分的"该扩未扩/静默重解释"这次没重犯。**够格继续放手**；我这层仍在"验收锁 + 对抗挖新边"上有独立价值（挖到 2 颗新边）。
  ③ **Codex 交回记账**：CR-8.11.4-02/03/04/05/06/08 + CR-8.11.12-01/02/03/04 共 10 条已由 Codex 标 ☑ 8.11.13、措辞准确。**commit SHA 待收口 commit 落时回填**（现工作树 215+ 文件未提交）。

## 2 — 写第 2 份 burn-down handoff（block-backed-shape 生命周期）

- **做了什么**：写 `docs/agent-ops/handoffs/2026-07-02-8.11-lifecycle-burndown.md`（from:claude/to:codex/status:draft）。**一件事 = block-backed-shape 生命周期一致化**。脊椎原则：**backing 块身份读时从 live shape 拥有派生，render_scope 只是缓存（相对不焊绝对）**——一次性收 CR-8.11.12-01/CR-8.11.4-03/-06/-08，让 -02/-04/-05 自洽。三簇：C（服务端脊椎 C1 note-block 生命周期守卫 + C2 kind-aware liveness 退成无文本图形 + C3 cascade 软 trash placement/mount）/ D（客户端 D1 demote 非破坏确认 + D2 re-promote 复用 trashed 块 + D3 空文本 auto-demote + D4 restore 剥焊）/ E（Stage-1 残尾锁 CR-8.11.12-02/03/04）。
- **CR-8.11.12-01 新坑我的裁决**（Henry 授权"你看着办"）：并进生命周期簇，主修 C1 写侧守卫（**按 live shape 拥有判定、非 render_scope**）+ C2 读侧退化。
- **过程（诚实标注）**：原计划用 workflow 做多 agent map+design+critique，**因 session limit（2:50am Toronto 重置）9 个子 agent 全 fail、烧 401k tokens 空手而归**。改在主循环亲自：补读 6 处核心代码（noteBlocks DELETE 107-115、canvasObjects cleanup 690-727、demoteShapeText 1581、promoteShapeToBlockBacked 1504、deleteCanvasObjectWithBacking 2582、restoreBlock 847）核准现状 + 亲推三处跨修法干扰解，写进 spec。**register 行号已漂**（8.11.x 位移），已用今核准行号。
- **判断点（供 Henry 复核）**：
  ① **demote UX 必裁 → Henry 定 (B) 确认后丢弃（2026-07-02）**：非空文本弹确认→软 trash 可恢复、配 D4 恢复成可用普通块；空文本无确认。(A) 留成普通块留作后续增强、本单不做。已写死进 handoff 判断点 1 + D1。
  ② **backing 块要不要滤出 GET /blocks** 可裁——我默认不滤、只写侧加守卫（读侧已有 liveness 派生）；滤 GET 是可选纵深。
  ③ **三处跨修法张力我已在 spec 内解**（C3 只软 trash 不 hard-delete 才能被 D2 restore；C1 守卫按 live 拥有判定故放行 demote/rollback；D4 restore 按有无 live 拥有条件分支）——这是本该由 critique agent 兜的、我手推，**验收测试是唯一硬闸**。Codex done 后我会**重跑对抗核查**补上未跑的多 agent cross-check。
  ④ **边界**：annotation 真相层（CR-8.11.1-03/04）、table 删行列 undo、LOW 硬化批（含 CR-8.11.12-05/06/07）明确留 Stage-3。

---

## 1 — 审 V2.BN.8.11.12 Highrisk Burndown Stage 1（第 1 份 handoff 的执行回执）

- **做了什么**：Codex 落地了 `2026-07-01-8.11-highrisk-burndown.md`（status→done + patch note V2.BN.8.11.12，声称 6 findings 全修、cutover 34/34 + 55 groups）。按我的角色（审 diff、给结论），跑了 5 维 Fable-5 对抗核查 workflow（A2-migration-043 / A1-live-tail / B2-B3-server-read / B1-client-kind / tests-spec-compliance），每维**实跑套件**、并做"禁用过滤看测试是否真 fail"活探；再亲验两个决定结论档位的铰链点。
- **结论：PASS-with-residuals。4 HIGH 在今可达路径上真修，实跑数字全对**（cutover 34/34、model-contract 55 groups、server test:v2 **163/163**、runtime-boundary 156，无回归、无删/弱化旧断言）。最关键正面结论：**B2 那颗灾难级 NULL 陷阱 Codex 躲过了**——`COALESCE(json_extract(...),'')` 兜底，正常块不会被清空出 AI 快照（我实测 SQLite 确认）。
- **挖出 1 颗新坑 + 4 条 spec-deviation/测试缺口**（全残尾非阻断，已立 register §8.11.12 = CR-8.11.12-01..07）：
  - **CR-8.11.12-01（新坑·MED）**：B3 liveness 过滤按 `co.backing='note_block'` 筛，连 block-backed shape 一起罩 → 独立 trash 掉图形的 backing 块会让**整个用户画的图形消失**（可恢复、非数据丢失；Codex 测试把"消失"编码为 intended）。**需 Henry 定语义**。
  - **CR-8.11.12-02（MED）**：handoff 指名要扩的 read-time 测试 test:580-620 **原样未动**、仍掩盖 style 丢失（亲验）→ 我把 CR-8.11.3.1-02 从 ☑ 降 **◐**。
  - **CR-8.11.12-03（MED）**：A1 read-time 守卫未建、修全靠一次性 043；post-043 危险态经通用 save/delete 不可再生（亲验 canvasObjects.ts:1700-1710 硬拒 page_frame）→ 功能闭环，标注残尾。
  - **CR-8.11.12-04（LOW 但极廉价）**：B2 回归只断言排除、无正控 → 一个"清空所有块"的灾难回归照样通过全套。**全补丁最危险坑的锁，该补。**
- **判断点（供 Henry 复核）**：
  ① **两处 ☑ 我判为过度声称并校正**（CR-8.11.3.1-02→◐；CR-8.11.3.1-01 ☑ 保留但标"仅 migration、无 runtime 守卫"）。依据=诚实报告 + handoff 指名要扩的测试客观未扩。若你认为 migration 路径已足、read-time 断言可永久不做，可把 -02 拨回 ☑。
  ② **CR-8.11.12-01 是新坑还是特性我没替你拍**——Codex 用测试把"消失"编码为 intended，但它改变了"图形视觉身份 vs 文本 backing liveness"的关系。已标 `needs: henry`，列了 (a)(b)(c) 三种收法。
  ③ **Stage-2 handoff 我还没写**（等你发话）。种子已备：CR-8.11.12-01/02/03/04 + 原计划第 2 阶段 MED（demote 销毁文本 / 删行列无 undo / restore 孤儿 / annotation rebase）可合一份。
  ④ **换防试点的元读**：这是"主力换防"设想下 Codex 交的第一单。对抗核查的净结论=**活扎实、自查诚实度中上**（数字真、根因测试真复现、最危险的 NULL 坑主动躲过），扣分在**测试正控/read-time 断言的"该扩未扩"**和**一处静默重解释 spec**（B1 boundary 断言降级为回归测试、未声明）。够格继续放手，但"验收锁"仍需我这层兜——这恰是换防要设的 automation 条件之一。
