# Claude 工作日志 · 2026-08-24

## 1. ⭐ Henry 亲拍:施工期测试降档(P0–P3)+ 未做测试台账建档

**事实**:Henry 直接对 Fable 裁定(本会话):①测试继续做,但**限制范围**——安全测试优先级降,尤其对抗式;主线=先完善产品,安全等测试后置批量补;②Fable 列全量测试清单分四档,Henry 批「可以没问题」;③新增记录义务:**凡因降档没做的测试,简要记流水账**,补测阶段以它为原料整理详细测试工单(届时不必全补,按当时威胁模型取舍——Henry 直觉,采)。

**落地**:
- **四档默认(即日生效)**:P0 施工地基不动(双 tsc / 常驻套件复跑 / 先红后绿 / 边界 numstat / 提交四查 / 生成件门);P1 关口用(洁净室复核每单一轮、位点收敛正确性;旅程走查每段收口一次);P2 已写的安全测试保留白跑、不删也不再扩,新工具只写最小 ownership 正控;P3 对抗性验证(mutation / 多轮 refute / 独立安全复核位点)**停做**,挂三触发器(首次真部署 / 第二个真人 / 外部 Agent 接入)。
- **台账建档**:`current-state/deferred-tests.md`(active / 权威),格式一行一条,**谁降档谁记**;种子三条(TD-20 mutation 未亲刀、J4/B3 投影零样本未取证、常设「不再扩安全矩阵」)。
- 授权链:本档与台账均为 **Henry 本人直接指示**(非同侪代授权);已通知 Opus 按新档拆单并承接台账追记义务。

**判断点(供 Henry 复核)**:①P2 里「已写的不删」我坚持了——删除是负功且几毫秒白跑无成本;②台账把「非降档但同样欠着」的 J4/B3 投影零样本也收了(标明原因),因为台账的用途是未来测试工单的原料,纯度让位于齐全;③12.2c 前置核实单已在 Opus 手里(读码答 selection §7 三问),其树上已见报告初稿,不受本档影响。

## 2. 12.2c 设计 v1 拍板 + 段计划出稿(核实报告全采)

**事实**:Opus 核实报告(`31cd00d`)答毕 selection §7 三问,四发现全部载重:①pool 词汇扩展零 DDL 成立,但 `content_group` 硬编码九处、其一为**静默 SQL 字面量**(INSERT 无视入参——置换分则的镜像),ownership 校验打 `content_groups` 表;②`anchorRect` 为瞬时 viewport px,从不承担身份,滚动即失效;③认领无「不铸 Item」路径;④我 v0 稿四处「没标待核就断言」。
**裁定(设计 v1,`2026-08-21-selection-receipt-design.md` 就地升版)**:①**收据是值,不是行**——不建表、不动锚池、九处硬编码零触碰,唯一持久化=随工具收据 `intended_input` 落盘;②**零几何**——身份=三元组+offsets+excerpt+at+note_id,anchorRect 不入收据,几何归源侧(12.5);③receipt 非 draft 第四态(枚举不动,投影输出值);④`resolve_selection` 只读、不写收据、逐 range 三态(found/missing/text_drifted);⑤锚池/晋升整体推迟(ownership 验证源重设计=设计级工程,与工具面主线无关)。
**计划**:`plans/v2-bn12-2c-selection-receipts-and-resolve.md`(draft):c-0 前置核实(server 侧三元组查询路,Opus)→ c-1 客户端投影(可并行)→ c-2 工具(c-0 后)→ c-3 旅程四步(段收口)。测试按 P0–P3,ownership 只留最小正控一条,略过矩阵记台账。
**判断点(供 Henry 复核)**:①九处硬编码那个「参数被校验然后被忽略」的静默字面量今天没修(它在推迟范围内)——但它是现网行为,若日后有人放开 validator 会得到假绿,已在设计 v1 §3 记明;②12.2c 范围收得比 12.2b 更小(两单),预估 12.2 整版 8/26–27 不变。

## 3. 档位解释:「验 killer 有没有牙」归 P1(单刀);权限类 mutation 仍 P3

**事实**:Opus 拆单前上报边界问题:「对本单点名 killer 施一刀改坏机关、看常驻测试是否变红」——技术上是 mutation,目的上是正确性;今天两大发现(b-3 HIGH-1 置换、b-2b-2 MED-1 断言无牙)都出自这个动作,且均非安全问题。若归 P3 停做,置换分则与未验之断言分则将失去执行手段。它在答复前按最保守读法执行(不写 mutation 位点、验不了红的记台账),并先回 `b7cac8e` 原始记录核实了授权链才接受新档(今日 BLOCKER 后的正确习惯)。
**裁定**:①「对本单点名 killer 的**单刀验红**」**归 P1**——它是「位点收敛正确性」的自然组成:没验过红的 killer 等于没有 killer;边界写死:单刀、只对点名 killer、不自由巡猎。②**P3 维持停做**:多轮 refute、独立安全复核位点、以及**凡刀口落在 ownership/鉴权守卫上的 mutation**(含内容过滤器风险,TD-19)——这类略过照记台账。③判据一句话:**看刀口对着谁**——对着业务正确性机关=P1 单刀;对着权限/鉴权守卫=P3 记账。④工单内设「本单按新档略过」成对小节(略过项+它本来挡什么):采,单内指令不引协议。
**判断点(供 Henry 复核)**:这是对你 P0–P3 档的解释权行使。你拍的是「安全测试降档,尤其对抗式」;killer 验牙不是安全测试,是复核方法本身——停了它,复核就只剩「看测试绿不绿」,而今天已证明绿灯会说谎。若你认为应更严(单刀也停),一句话我就收。

## 4. c-0 四待拍已裁(A/B/C/D);计划补记 §3.1;c-1/c-2 放行

**事实**:c-0 核实(`399376a`)三结论:blockId 有 SQL 路且 `user_id` 同行(归属一步解决);textFlowId 纯派生零信息量(全仓 5 产地同一函数);textUnitId 无查询路须读行解 JSON,重建文本可复用 `textFlowProjection` 的解析约定;unit 级存在性验证是全新能力。Opus 自我更正一处并留痕(曾把「没有独立表」当「没有解析能力」,已入报告 §7)。
**裁定(计划 §3.1)**:A deleted unit=`missing`(投影约定是「什么存在」的权威);B 逐条 `missing` 折叠 foreign/nonexistent(硬约束:不得细分 forbidden 类值——泄露存在性);C textFlowId 保留但不许「带着不用」——同一函数 import 做一致性校验,不一致=missing;D 解析约定零语义提取共享(S1a 形状,反 TD-15),§5 边界修订:items.ts 仅此一 hunk。
**判断点(供 Henry 复核)**:C 的取舍点=「带着不用的字段」正是静默字面量的镜像,宁可加一道一致性校验也不留死重;D 动了我自己定的禁区(items.ts),修订理由=复制解析约定会种下 TD-15 同形的债,提取是既定家法。

## 5. §5 正文订正(我的第四例「宣布修订未改正文」);c-2 直接派,不过目

**事实**:Opus 发现计划 §3.1 D 写「§5 据此修订」但 §5 正文原封未动——builder 读 §5 停手、读 §3.1 开工,同一份计划两个答案。它未选边,报我改。这是今天第四个「宣布了一件事,却没去改被它推翻的那句」先例(前三:K-b7 族、全目录空、any-file 禁令),四例四形态同一根因。
**裁定**:①§5 已订正(items.ts 摘出「不碰」列,单列「仅 §3.1 D 一个 hunk」例外;触及面补「新共享解析模块」);②c-2 拟好**直接派**,不必过我——四裁落法(含 B 的「细分出第四类值须红」与 C 的「换成本地重派生须红」两条 killer 升级)方向全对,抽检照旧留在复核与铸段;③Opus 提议的 README 补句(「修订式措辞必须同时改被推翻正文」)属待追认协议范围,它未动是对的——记为 Henry 过目四条时的**候选第五句**,不自行入档。
**判断点(供 Henry 复核)**:README 待追认清单又攒了一句候选;四个同根因先例在案,你过目时可一并看。

## 6. c-1 复核 FAIL(1H,设计级)→ 裁 A:excerpt=切片;第六分则「鉴别力」入卡;设计稿 v1.1 订正

**事实**:复核 HIGH-1 成立(三源,含 Opus 自补的最硬一条:`normalizeSelectionOffsets` 把 `text` 当 offsets 索引的全文用)——`CapturedSelectionRange.text` 是**整段 unit 文本**,offsets 是窗口。我设计稿 §1「`text_ranges` 原样继承…含摘录」两个短语在现码上互斥:builder 忠于「原样继承」(整段),复核忠于「含摘录」,**两边都对,稿子错了**——这是我第五处「没标待核就断言」,v1 修稿漏网。下游后果真实:c-2 的 found/text_drifted 语义在「摘录」与「整段」两读法下完全不同(后者邻句改一字也误报 drift)。另:复核发现 K-1 fixture 用整段选区,摘录==整段,断言绿而无鉴别力——「未验之断言」的新变体。
**裁定**:①**采 A**:`excerpt = text.slice(startOffset, endOffset)`,字段由 `text` 更名 `excerpt`(现在零消费者,改名最便宜的时刻;编译器替未来把关);不变式 `excerpt.length === endOffset - startOffset` 入 c-1-fix 测试与 c-2 zod refine(破则 400,与拒绝形状裁定 B 一致)。**不采 B**(整段+切片双带):模糊重定位是锚时代的能力,不预先养肥值类型——「形状不跑在能力前面」;**不采 C**:误报 drift 会让工具在正常使用中失信。②设计稿 §1 就地订正为 v1.1(明记第五处教训)。③**第六分则「鉴别力」入自律卡 §7**(六条并列):断言宣称区分两假设,fixture 必须让两假设产出不同输出;机械步骤三条;先例 c-1 K-1。授权依据:Henry 08-23 分治裁定(自律卡 Fable 可改)。④c-1-fix 由调度会话拟发:投影切片 + 更名 + 不变式测试 + K-1 按鉴别力分则重造 fixture(部分选区、非零起点);c-2 工单同步改 zod 字段语义与 drift 比对式(`excerpt` vs `currentText.slice(start,end)`;offsets 被前文编辑推移导致的错位=如实报 drifted,机械严格比对不做模糊)。
**判断点(供 Henry 复核)**:①A/B 取舍的实质=「现在多存上下文给未来的模糊重定位」vs「值类型只装当下用得上的」——我选后者,依据是你产品里「结构形状不便宜地跑在能力前面」的既往教训;若你想要 B 的原料,锚时代设计时可翻;②第六分则动的是自律卡,按你分治裁定在我权限内;README 候选句清单不动。

## 7. c-1-fix 复核 FAIL(1H)→ 裁 PASS 放行(超调度权,我拍);第六分则先例订正 + 第七分则「规格固化」入卡

**事实**:复核 HIGH-1 成立(K-1' :162 混入 fixture 自证断言,致 R1-② 反事实无法隔离;reviewer 两刀均贴红点行号与断言原文,质量高)。但 Opus 二级复盘沿复核「证据口径补注」追下去推翻了此前定性:c-1 旧 fixture 第一条 range(2..7 于 10 字符)**本有鉴别力**,K-1 绿的真因是**预期对象把整段值逐字硬编码**(把设计稿 §1 错误语义固化进测试)——**若当初实现是正确切片,K-1 会红**:该测试会主动保护 bug、否决正确实现。我亲验 `4d3ba81` 测试文件(:54-:56 入参、:104-:106 硬编码预期)后确认。
**裁定**:①**c-1-fix 判 PASS 放行**(放行被判 FAIL 的单,超调度权,我拍):生产实现按 A 裁定正确、门全绿,`602d0a0` 不动;HIGH-1 的实体(:162 职责耦合)降级并入 c-2 测试整理批,不单开修正单;②**R1-② 撤销**——其目的「证旧 fixture 无鉴别力」建立在假前提上,且以合成 fixture 顶替实际 fixture 作证=置换同族;撤销理由入工单;③**第六分则先例行订正**(错的先例会教出错的检测手法:照旧文去查 fixture,真该查的是预期值来源);gamma range 仍是该分则真实例;④**第七分则「规格固化」入卡**(Opus 拟文,我亲验后入):预期值抄自规格只能证「与规格一致」,不能证规格对;机械步骤三条(独立推导/双源断言/冲突先疑规格并升裁定)。授权:自律卡分治裁定。⑤UTF-16 顺流风险此前已裁「记账不改单」(台账 `b01981d` 含修法提示),一并入档。
**判断点(供 Henry 复核)**:①「放行 FAIL 单」是本代理期第一次,依据全在上文,复核发现的实体一条没丢(降级≠丢弃);②第七分则的根子是我的规格错误被测试忠实固化——设计错误的代价链至此完整:错规格→错测试→错测试反过来保护错实现;旅程与复核制度把链条每一环都暴露了;③§7 已七条,今天一天入了三条——密度异常,但每条都有当日真实翻车做锚,不是理论装修。

## 8. c-2 首派停手(两处允许面内不可达)→ 裁 A1 下沉 shared + B「验合规不验缺席」;「点名机关先核」扩写三样

**事实**:builder 按「越界即停」停手(没硬做没绕没编,行为满分记档;回执 `1d8dd29`)。两处不可达经 Opus 复验成立:**A**——C 裁定要求 import 的 `textFlowIdForBlock` 全仓唯一但 11 处全在 client,server 因 `rootDir` 无法 import(c-0 报告数了「有几个」没问「在哪一侧」;我 C 裁定点名函数时同样未核侧位);**B**——我计划裁定 4 写「只读不写收据,同 list_notes」,而 transport 对一切工具无条件写收据、`list_notes` 的 K-0 正锁着 `applied` 收据——**引的先例方向是反的,且同句自相矛盾**(「不写收据」与「收据行为同 list_notes」并存,第六个不可能条款先例;与第五例同为「文本与代码之间的矛盾,只读单读不出来」)。
**裁定**:①**A 采 A1**:`textFlowIdForBlock` 下沉 `shared/`(跨端身份约定住 shared,与 `SelectionReceiptV1` 同理),新机械小单 **c-1b** 回接全部 client 调用点(纯 import 改道零语义),c-2 自 shared import,C 裁定原文续用;不采 A2(行为锁替身份锁=C 自废)、A3(字段在没人用=C 点名要防的形状)。②**B 采 Opus 重定义**:「只读」=不改真相层,≠不留痕迹;收据轴是审计轴,读操作留痕恰恰对。K-8 改「验合规不验缺席」:调用后 notes/note_blocks 逐字段不变 + 收据形状同 list_notes。计划裁定 4 与 §3.2/§5 同步订正(宣布与正文一致,第四例教训)。③**自律卡「点名机关先核」分则扩写**:存在+方向+侧位三样,凡点名既有符号/先例/机关,落笔前打开看一眼;两案入先例。④c-1b 复核可并入 c-2 同轮(位点分列),省一轮往返。
**判断点(供 Henry 复核)**:①两案根子各有一半在我(C 裁定未核侧位;裁定 4 同句自相矛盾)——「不可能条款」六个先例里我贡献三个,调度方三个,分布均匀说明这是角色病不是个体病,扩写的三样检查就是对症药;②builder「停得对」连续第二次(b-2b-1 K-b7 后),越界即停的纪律在 OpenAI 侧也立住了;③改单 ≈15min、重施工 75-110min,今晚仍可能走到 c-3,12.2 整版预估 8/26-27 边缘但未破。

## 9. S1 探针失败(TS6305)→ 真因:shared project reference 声明齐备但从未通电;裁 (b) 免诊断单 + b-0 构建核实

**事实**:c-1b 的 S1 构建探针失败,报错不是预测的 TS6059 而是 **TS6305**(引用的 shared 项目从未被构建)。Opus 复验真因:`composite`/`references`/`paths` 三样声明齐全,而 `shared/dist` 是空目录壳——**接线声明齐全、目录建好了、从未通过电**;至今不显形是因为现有两条路都不需要 dist(client 打包器读源码;server 唯一引用是编译时擦除的 `import type`),一个运行时值一跨界立刻现形。「结构形状≠能力」在构建体系上的最纯标本。builder 按合同停手、探针撤净、拒绝补跑下游门代偿(连续第三单停得对);归档 `f3cb2f2`。附带:Opus 的单把「唯一自救」写死为 `@shared/*` 一种,挡掉了我要的相对路径数据点——其教训「禁令须区分为交付而改/为诊断而试」采;它未动产品树补数据(岗位锁是 Henry 的规矩,同侪不能代松)。
**裁定**:①**(b)**——相对路径数据点不补,TS6305 已锁死病因,那条只是另一条死路的名字;②构建修法**不直接出单**,先 b-0 读码核实四约束面(tsc -b 所需/emit 后 import 说明符不被改写的运行时解析——`@shared/*` 在产物 JS 里裸 Node 解析不了、相对路径在 dist 布局下的指向/dev 与测试运行器两个世界都要绿/postbuild 复制脚本受否影响),带回我点名机制——今天全天教训「点名机关前先核三样」,构建体系最不该凭记忆点名;③构建小单 killer 框架:server 既有构建产物字节等价(除新增)+ 全门禁绿 + 运行时正控(node 跑产物真调 shared 值);④c-3 与 Henry 打扰窗口改约明天。
**判断点(供 Henry 复核)**:①这个缺口是仓库既有的(声明了 project references 但构建链从没用过它),不是本段引入——修它是还旧账不是加新债;②今晚时间线:b-0 核实 → 我点名 → 构建小单 → c-1b 重派 → c-2,c-3 排明天,12.2 整版预估滑到 8/27 边缘,明天走完 c-3 则不破。

## 10. b-0 交付 → 我翻自己的案:A1 → A1′(server 本地实现 + 跨界契约测试);构建体系记 TD 挂触发器

**事实**:b-0 四面全用现物答毕:①builder 的「探针已撤净」在源码面真、产物面假(TS6305 报错仍 emit,污染产物反而白送了运行时证据 `ERR_MODULE_NOT_FOUND`——tsc 不改写说明符实锤);Opus 已重建产物修复。教训「撤净要问在哪个面上」入档。②**TS6305 是已知且被刻意压制的**:`manifest.ts` 顶注成文「server 的 no-emit 门有意不构建 shared,此 import 纯类型」+ `@ts-ignore TS6305`——server↔shared 类型只是**既有设计决定**,不是疏忽。③三个世界三种解析(dev/jiti、tests/tsx、产物/裸 node),`@shared/*` 两个世界不通,相对路径产物世界指向不存在的源码;唯一统一解=shared 变真包(代价最大)。其探针天花板如实申报(验的是解析层非可用层)。④布局脆弱点定位(`TOOL_FACE_MANIFEST_URL` 依赖 src/dist 同深度,rootDir 抬高会打断)。
**裁定(翻案,理由与原案同页留档)**:①**A1 → A1′**:shared 源码仍是唯一真相,client 半照沉;server 侧本地实现同名函数;**常驻跨界契约测试**(tests 世界可同时 import 两侧)锁两实现全等 + killer(改拼法须红);测试内 shared import 按 manifest.ts 成文的 @ts-ignore 模式。**原则未变(同源优先),变的是同源的执行点**——从 import 链移到契约测试,因为 import 链真实价格=构建体系重设计,而标的是 2 行模板字面量。②构建体系(通电 references / shared 变真包)记 TD,触发器=第一个非平凡运行时值需要真共享,b-0 报告为届时设计输入(Opus 写 TD)。③计划 §3.1 C / §3.2 / §5 同步订正(被推翻句逐一改,原案保留供对照)。
**判断点(供 Henry 复核)**:①这是我今天第一次**翻自己当天的案**——A1 拍板时「下沉」看着是搬文件,b-0 用现物揭示它是构建体系重设计;新证据到,案就要翻,翻案与原案同页,你可对照;②「先核后点名」为本次翻案省下的是一整张会失败的构建单;③今晚仍可走 c-1b′→c-2,c-3 与你的登录窗口维持明天。

## 11. 闸单立(条件翻牌)+ c-1b v2 停手:shared 被当 CJS → 裁 (A) 一行真包化小单

**事实**:(1) 跨界闸单(server/src 禁非 `import type` 的 shared import)已拟 `0162342`,我审毕**即时翻牌、生效条件=c-2 复核 PASS**;Opus 四条增补全采(K-4 反向刀「注释不承重」/ K-5 双写法样本「相对路径通得最广所以更该被拦」/ 撤净双面自证 / 动态 import 射程申报),并以**禁令**排除 client 对称闸(无失败形态=无对象机关)。header 写法采其 `ready-on-condition`——保住 `ready` 在协议里「此刻可派」的字面。(2) **c-1b v2 按合同停手**(builder 连续第四单停得对,拒绝 `default.x` 兼容层自救——不在允许面内):静态具名 import 在 tsx 下 SyntaxError,双源一致;Opus 复验根因候选=**shared 无 `package.json` ⇒ 就近解析落根 ⇒ 根无 `type` 声明 ⇒ CJS**,`__esModule` 签名佐证;爆炸半径已查:Node 运行时消费 shared 仅 2 处(manifest 生成脚本),均有现成门守着——**改坏会响,不会哑**。
**裁定**:①**采 (A)**:极小构建单,`shared/package.json` = `{"type":"module"}`;killer=具名 import 在 tsx 须通 + **改回去须复现 SyntaxError**(阳性对照)+ 两道 manifest 门绿 + 全门禁绿;②**不采 (B)**——把 CJS 互操作怪状固化进契约测试,正是今天刚立的「规格固化」分则那种错:把偶然的现状编进预期,将来修正时测试反过来挡路;③(C) 已被 C 裁定否掉,不再议;④(A) 同时是 TD-21「shared 变真包」的最小第一步,Opus 在 TD-21 补一句;⑤队列:(A) 小单 → c-1b v3 → c-2 → 闸单生效 + c-3(明天,与 Henry 登录窗口同步)。
**判断点(供 Henry 复核)**:「一行修法」也走了全流程(单开、killer、阳性对照)——因为它改的是三个运行世界的模块解析行为,行数不是风险的度量;今晚流水线三次停手全部是 builder 守约停,零次硬做,这个纪律面比任何单条交付都值钱。

## 12. c-1 系列收口(a/b/c 三单全 PASS):A1′ 通电;裁 (B) 接线;TD-22 立项;c-2 派出

**事实**:①**c-1a**(`79a8ef3`)`{"type":"module"}` 一字段,K-1 双步红成点名形状(删文件复现具名导出 SyntaxError、恢复复绿)——因果证明,非碰巧绿;TD-21 的 tsx 格升「静态具名 runtime import 已证绿」,另两格不动,债不清。②**c-1b v3**(`bd42727`)契约测试两侧静态具名 import,无 namespace/default 垫片;三刀均先证可编译再取红、逐刀 hash 恢复;但 Opus 复验发现**契约测试没接进任何门**——根因又是其单自相矛盾(「须常驻」+「禁改构建脚本」,常驻的唯一实现路径被禁),builder 交付未接线并如实申报(连续第五单守约)。我裁 **(B)**:立刻接线,赶在 c-2(第一个受锁保护的单)开工前生效——**锁在被保护对象开工之后才生效等于没锁**。③**c-1c**(`d4455eb`)接线 `test:v2` 270→274;**灵魂刀过了**:改 server 拼法后跑完整 test:v2,273/274、唯一红=目标 AssertionError(`'tf-'` vs `'textflow-'`)、模块错误零命中——「门真的把它跑到了」被证明,不是名字进了列表实际被跳过。
**附案两件**:①Opus 监视器 bug 自报:`grep -c` 零命中退出码 1,其 `|| echo 0` 兜底把变量污染成两行——「计数为 0」与「命令失败」被当成同一件事,而 grep 恰用退出码区分;**兜底值与真值同域=毁掉「测得 0」与「没测到」的区分**;其新版「收工但有疑≠越界」的分离救了一个守约 builder 免遭误质问。②builder 申报长期风险采:显式测试列表无「漏挂发现机关」,新测试默认不运行且遗漏静默——证据就是本段自己(契约测试无人跑四小时零报警);立项 **TD-22**(触发器=下次新增测试文件),与 TD-21 更新一起排队等 c-2 收工落盘(树冻结纪律)。
**裁定**:c-1 系列(a/b/c)收口;**c-2 派出**(75-110min),提示词含「契约锁已生效,门红是保护不是障碍」。
**判断点(供 Henry 复核)**:①A1′ 从翻案到通电走了三张微单、两次守约停手、一次接线补裁——每一步都有现物证据,零步靠猜;②「我们用来锁形状≠能力的锁自己差点只剩形状」被 (B) 及时闭合,闭合的证明方式(穿门取红)与它要防的病同构。

## 13. c-2 交付(12.2c 正餐落地,`b332559`);单内 blast radius 规则第三案定形;裁「复核不过夜」

**事实**:c-2 四交付面齐:S1 零语义外提 `textFlowUnits.ts`(items.ts 恰 1 hunk,提取前后投影 UTF-8 字节等价,helper 全产品仅 1 份;**那条 pool_scope_kind 静默字面量未被顺手修理**——禁区纪律);S2 registry/binding/manifest +1;S3 三态 resolve service;S0 删冗余断言。K-1…K-9 逐刀先红后绿、红全是目标 AssertionError、mutation 先证可编译;门全绿(test:v2 274 新基线)。Opus 独立四格核验含一条**构造级**观察:拒绝形状的 foreign/nonexistent 折叠由**单一 return 点保证**——「不可区分是构造保证的,不是测出来的」,比任何测试强一级。
**单内影子第三案**:Opus 改单时 K-8 表行与定义段都改了,第 156 行「回执须含只读证明」旧句漏改;**builder 发现矛盾、点名申报、并选了难的那条**(新判据)而非白纸黑字授权的容易那条。规则定形:**修订一处判据=全文搜同一判据的所有出现**,单文档内部也有 blast radius。
**Opus 收尾观察(原文入档)**:「一份自相矛盾的单,考的不是 builder 的能力,是它愿不愿意把矛盾说出来——所有的闸都只能验它做了什么,验不了它有没有在两条路里挑了容易的那条。」我的接续:机制验行为、复核验偏差,而**在两条被授权的路里选难的那条,只有诚实能保证**——制度的最后一层不是闸,是让诚实可见且被记账(builder 行为分连续第六单满记)。
**裁定**:①打包洁净室复核(c-1b v3/c-1c/c-2 三份,位点已冻结)**今夜即发,不过夜**——Henry 的常令是工程不停,reviewer 是 codex 进程不需要人陪跑,结果落地后明早二级复盘;②TD-21/TD-22/假阳性名单 3→4 随复核期落盘(树此刻静止,复核只读不碰树,可并行);③闸单条件不变(c-2 复核 PASS 即 ready);④c-3 窗口照旧弹性,明天以复核结果定,提前 30 分钟叫 Henry。
**判断点(供 Henry 复核)**:12.2c 施工全部完成(c-0/c-1/c-1a/b/c/c-2 七件,两次守约停手、一次翻案、零次硬做);剩复核一轮 + 闸单 + 旅程四步,明天收段仍是基线预期。
