# Claude Log — 2026-08-28

## 1.(Fable)V2.BN.12.2 铸版;V12 铸版(收口走查按实记分,⛔ 不写全绿)

**事实**:收口第 9 项走查归档 `01fff28`(J1=0 / J2=2 / J3=2 / J4 未取证);J1 的 0 经调度复核判定为**既有缺口非本版引入**(块删除「有出口无回路」:server 恢复 API 与 client `restoreBlock` 皆在,人类侧无持久恢复入口——结构形状≠能力又一标本),已立 **TD-28**(`7d16520`,含触发器/指针/归属判定);J4 未取证与 12.2b 同族(投影零样本,台账 08-23 行在案)。
**裁定**:①**记账放行,铸 V2.BN.12.2;铸 V12**——铸版断言「本版交付物完成且验证」:14 工具四真相读写面、选区收据与 resolve、快捷登录、九项收口(八实做一记账),各段 killer 与复核链全绿在案;⛔ 铸版记录不写全绿,走查四值照录;②**通例入档**:走查的 0 挡不挡铸,看 0 落在谁身上——落本版交付物=挡(12.2c J3 先例),落既有面=记账放行(本例);两先例一严一宽各守其界。
**判断点(供 Henry 复核)**:①「J4 未取证」随 12.2b 先例放行,若你认为收口走查必须补投影样本取证,一句话我重开;②TD-28 是 🅰 正确性且用户可感(删块后找不回),建议排 12.9 途中小单而非等触发器自然到来——待你晨起裁;③本铸版由我在你授权的夜班内落锤,你保有翻牌权。

## 2.(Fable)铸版后首日晚账:12.9a 两场收官 · 画布小批双收 · 闸系自繁殖 · TD 28→32

**事实(全日,各件均有独立 commit 与复核在案)**:
- **12.9a 第 1 场(转写器)**:开赛即揪出现役 PDF 原生通道死亡(pdf-parse v2 API 变更,异常被 fallback 静默吞,零测试覆盖)——当夜修复+测试从零到一(`12b183a`),基线回填;C0/C1 Docling/C2 MinerU 三候选全卷实跑,**187× 耗时差裁为「双车道价格表非胜负」**;规格 v0→v0.1 两次修宪(归一化入保真阶梯;引用校验闸)。
- **12.9a 第 2 场(识别器)**:qwen-vl-max 自报 bbox 1/6 vs 引用碎片 6/9、JSON 残缺随坐标字段消失——**「错误可判定性的差别,不是能力的差别」**成为选品最重产出;$0.222 总花费(废跑留分母)。
- **画布小批**:A 檐下去占位(`6f68707`,互独立双刀,文字左缘归位 62px)+ B 恢复门三段(`c8b0bc4`,读口/抽屉/接线;人类门开眼≠Agent门开眼,MCP 面零触碰有锁)。
- **闸系自繁殖**:第七查立为脚本闸(文件面+命令面),装后十分钟拦下同型第四次;`node --check` 前置上午立下午救;闸自身三病由阳性对照抓出。
- **TD 变动**:28(块恢复门→**已由单 B 清偿**)、29/30(钉 12.9c T0 首单)、31(裁切,未目击,复现路径已探明)、32(undefined 类名 🅱)。
**判断点(供 Henry 复核)**:①TD-31 只差一次目击(切 canvas、块拖左缘,一眼定案)——你顺手看一眼即可结案;②单 B 仍标「Fable 裁,同域推断」待你补章;③嵌入场明日选做,两场主战数据已够终审;④软闸激活那一行仍在你手上;⑤全天纪律亮点如实记:builder 三连拒绝复述预测、两次白卷、调度两次拒把仪器读数当观测——**三层互不轻信,体系在自己长牙**。

## 2.(Claude 工程调度)V12.9a 选品两场实测 + 画布小批 A/B 双收 + 发单第七查立闸

**事实**(按批次):
- `af52635` **TD-29 立账**(说谎的类型声明:`pdf-parse.d.ts` 仍描述 v1 `export=` 外形而实装是 v2 类式 API;落点钉 12.9c T0 首单顺带,⛔ 不单开)+ 第 1 场结语收 Fable 口述裁「187× 不是胜负,是三段速时间线的第一份经济实证」。
- `f9f64d2` **第 1 场收官**:C2 MinerU 3.4.5 四卷跑通(100% 带 bbox,回程票 154/160 严格轮 == 归一轮,坐标系走证据判定非试错);⭐ **C1 的 D 维由 1 修正为 2 —— 修的是我的测量**(Docling 单元格一直可寻址,内容在 `.data` 不在 `.text`)。立规:**⛔ 候选之间同一维的测量深度必须相同**。
- `a2bcf9d` 第 2 场口径**冻结先落盘再开跑**;`a21f01b` **第 2 场收官**:题 2(模型自报 bbox)出处 **1/6**;⭐ **2b(改为引用转写器碎片 `seq`)6/9,且 JSON 残缺归零**。**核心判词**:引用制没消灭幻觉,是把幻觉从「坐标不准」变成「引用不存在的 id」——**后者机械可查**。Fable 已收入规格 v0.1。
- `6f68707` **单 A**(行首控件迁出排版流,纯力学)· `c8b0bc4` **单 B**(TD-28 人类侧恢复门:读口 + 安静抽屉 + 接线,MCP 工具面零触碰)· `170bac0` **TD-30/31/32 立账**。
- `de66733` **发单前第七查立为机械闸**(`scripts/check-handoff-surface.mjs`):判据点名的文件必须在允许面内 + 工单点名的 npm script 必须存在。

**⚠️ 我今天的错误(全部已入档,不藏)**:
1. **判据与边界打架**在我手上出现**三次**(TD-12 killer 与债原文不符 / 单 A 允许面缺测试文件 / 单 B 命令不存在)⇒ 每次都由 builder 交白卷兜住,每次烧一轮。**第二次重发现 = 未机械化的证据**,故立闸。
2. **闸自己没过第一次阳性对照** —— 它把 builder 的 `## Result` 段误当成允许面,**自己把漏洞补上了**(「别用产物校准量具」的第三副面孔)。且改闸时一次补丁令脚本语法错误而**它照样退出 1**,阳性对照因此显示"抓住了" ⇒ **只看退出码会把崩溃读成成功**。
3. **单 B 复核时两次跑法错误**:直跑绕开隔离 runner ⇒ **基线即红**(三次「变异」结果雷同,没基线就会误报「两刀都验过」);复跑全套漏传文件清单得 428/346。正经入口 **286/286**。
4. **静态预查把条件规则读成恒成立**(`writingSurfaceCanvas` 的 `overflow:hidden` 只在 canvas 模式挂);**`undefined` 探针里点错了模式药丸**(第三个是 `layoutMode`,第一个才是 surfaceMode)。

**⭐ builder(Codex)记功,共五处**:两次交白卷不自行扩允许面;**三连拒绝复述发单方预测**(红点形状 / 「这不算红点」/ K-1 红值);拿 TD-22 账上原文拦下「一把不接门的刀」;现物核出我的文件数已漂移(23→24)。

**判断点(供 Henry 复核)**:
1. ⚠️ **TD-31(檐下槽裁切)我挂「未目击」** —— 几何 + CSS 规范可推出必裁,但我两次命中测试的**阴性对照都失败**,⛔ 没用坏仪器的读数凑观测。Fable 裁本轮不修。**若你认为「推得出必裁」就该动手,一句话我开单**;但请注意他点出的地价:抬高 x 下限 = **永久没收每张画布左侧 91px 的摆放自由**。
2. ⚠️ **单 B 的 K-2「最重要的锁」射程比它的名字窄** —— 冻结 fixture 只有 1 行,**锁住的是「默认状态不被改」,不是「默认调用的全面字节锁」**(我实测改 `ORDER BY` 不红)。**这是我工单未规定 fixture 多行造成的,归我。** 是否要专门补一单扩 fixture,待你裁。
3. **第 2 场我未宣布冠军**,`qwen-vl-max` 在生产形态(2b)下的 6/9 + 2 条虚构 id ⇒ 我在结论里写了「**入库处必须有 `seq ∈ 本页碎片集` 的闸**」。**终审归你。**
4. **本日 API 花费 ≈ $0.222 / 上限 $20**,其中**两次作废跑 $0.113 占一半,如实留在分母**。

## 3.(Claude 工程调度)V12.9a 第 3 场 + **V12.9b 开段:碎片流地基落地**

**事实**(按批次):
- `950808c` **12.9a 第 3 场收官**(嵌入 + `sqlite-vec` 存储探针)。⛔ **样本量不足以选型**(n=5+3,R@1 最小刻度 0.2)。⭐ 三条**任务级**发现:①**中位 margin 全为负** ⇒ 检索层必须「召回 + 判定」两段,⛔ 最近邻不可直接当答案;②**跨语言零损失** ⇒ 中英混排不必分表;③`sqlite-vec` **T 维通过**(阳性对照如期失败,成功归得了因)⇒「不引入第二个数据库」有实证。
- `af95202` / `3cf70ee` **b-0 只读核实**:⚠️ **仓里两代 source 身份并存且都活着**(设计级 finding,Fable 裁挂新代);查重指纹活在新链但**包导入路径写空**;K-2 的 **region 级刀位未确认**。
- `e7fba88` / `093806a` **TD-33 / TD-34 / TD-35 立账**(包导入空指纹 / 两代身份收敛债 / **jszip 幻影依赖**)。
- `612c1d9` **b-2b 采购探针**:⭐ **jszip 静默改写上跳条目名 ⇒ 应用层无法申报** ——「不炸但投毒无痕」比响亮崩溃更差;Fable 裁 **yauzl@3.4.0 + `decodeStrings:false`**。
- ⭐ `18cdb8b` **b-1 收工(四次派工)**:`source_imprints` + `imprint_fragments` 两表 + 带**分割不变式**的校验器 + 12 条常驻测试。`test:v2` **298/298**,`verify:v2-bn8-runtime` exit 0。
- `2aa62c9` / `b92a7c6` / `4fc3a22` **b-2 / b-2b / b-4 三单过闸候锁**;b-2 已派。

**⭐ builder(Codex)本轮记功,四次停线一次比一次深**:
1. 允许面自相矛盾 → 2. **判据引用了 schema 里不存在的字段** → 3. ⭐ **交出 6/6 绿后自判「字面过测不是诚实交付」而整个回滚**,并指出**规格自身两条 killer 互斥** → 4. ⭐ 卡在生成件过期时**拒绝「临时生成→门绿→回滚→回执写全绿」**。
📌 **第 4 种造假是唯一没有任何机关能事后发现的** —— 它拒绝的不是被抓的风险,是造假本身。

**⚠️ 我的错误(全部已入档)**:
1. **判据与边界打架第 5、6 次**,且**形状是新的**:一次是判据引用了不存在的字段,一次是**我把规格的两条 killer 原样抄进单而没发现它们互斥**。第七查查路径与命令,**查不到这两种**。
2. ⭐ **`git add` 纪律一日三失**:漏装生成件 ×2(同一个文件)+ **宽 add 扫进 builder 未提交的交付** ×1。⚠️ **根因不是「忘了」,是「按目录 add」这个动作本身既会漏也会扫,而两种都不报错。**
3. **第七查暴露三处射程边界**(提示射程 / `..` 误杀 / **根级文件假绿**)。⛔ 我**没有**逐个打补丁 —— 判成本失衡,**把三例存成了将来重写的验收 fixture**,并钉死「⛔ 不再补正则,第四处出现时函数化一次做完」。
4. **翻牌死循环是我的单的结构性缺陷**:凡完成动作含「翻牌」的单,必然让 `INDEX.md` 过期 —— **那本就是调度方的活。**

**判断点(供 Henry 复核)**:
1. ⚠️ **12.9a 三场全部收官,但第 3 场我明写「本场分不出高下」** —— 嵌入选型**推迟**(Fable 裁:12.9d 用 BGE-M3 作默认工作假设,终选标准 = **V13 真实检索的采纳收据**)。**若你认为该现在定,一句话我重开一场大样本的。**
2. ⚠️ **TD-34(两代 source 身份)是收敛债不是清理债** —— 两代都活着。碎片流已按裁定挂新代,**⛔ 本段没碰旧链一个字节**;**收敛本身是将来一次真正的迁移工程**,触发器写在账上。
3. **b-1 花了四次派工** —— 我判断值得(**分割不变式是本段合同本体,地基上的覆盖缺口会被上面每一层继承**)。**若你认为节奏太慢,可以要求我把「细一档变异」降级为抽检。**
4. ⚠️ **`.githooks/pre-commit` 仍未启用** —— 它正是为我今天栽的「生成件漏装」而写的。**启用是你自己的一行 `git config core.hooksPath .githooks`,⛔ 我不代设。**

## 5. b-2 收工抽检 PASS(五次派工收官)· b-2b 进入重对现物

**做了什么**:coincides-48 报 b-2 收工(`27d30c5`,复核 PASS:六处产生点五红一等价、SHA 逐字符一致、`test:v2` 307/307)。我按抽检原则**只抽判定链里唯一没有红点作证的一环**——`450` 的「等价变异」判定:
- **亲验证明成立**,且补验了复核方证明里没写的第三个前提(`mimeType` 在 434→449 间未被改写,唯一改写点 445 在互斥分支)。
- 专项**独立第三跑** 9/9 绿(builder / 复核 / 我,三方独立执行齐)。
- `package.json` 亲看 diff:`test:v2` 严格尾部追加,无夹带。
- 已放行 b-2b(coincides-48 正按新家法**先重对现物**——b-2 刚改了归类逻辑,b-2b 接线点九成已变)。

**留了一根刺**:450 的等价性**依赖 445/446 的互斥结构**——将来若归类分支改成非互斥,450 从等价变异变回活代码,届时须配自己的 killer。已嘱记进工单 450 条目旁注。

**判断点(供 Henry 复核)**:
1. **抽检深度的取舍**:我没有第三遍全量复跑(两层已各自独立跑过),只抽「纯靠证明撑住的判定」+ 专项一跑。若你认为收工抽检应含全量,一句话改。
2. b-2 五次派工中**两次返工源于调度自己的列举错误**(`head` 截断 / 隔离输入给错),账已两半记档;买回的三条补丁恰堵住「产生点列举」最易被架空的三个口子。**学费我判不亏,你可复核这笔账。**

## 6. spec v0.4 修宪:容器条目申报四裁(b-2b 重对现物触发)

**背景**:coincides-48 按新家法重对 b-2b 现物,抓出三处过期(其一根本性:载体已从 warnings 族迁到 `source_files.intake_declarations_json`),并上报一条规格级问题:**没能成为件的容器条目,申报住哪**。它给出推论(住容器件自己)但⛔不自裁——升级路径走对了。

**我的裁定(已落宪 v0.4 并提交)**:
1. **住所照准**,补第二条腿:永不拒收下容器 blob 全量在库,**失败条目的字节未失监护**——申报是诚实标记,不是监护替身。
2. **闭集只加一码** `container_expansion_incomplete`,⛔ 拒了原单三个 `container_entry_*` 按因分码——闭集是文件性质词表,原因粒度是 detail 不是 code。
3. **⛔ 不建失败条目台账**:失败条目集 = central directory(在已存 blob 里)− 成功子件集,可推导之物建表 = 形状先于能力。code 降格为免开包触发器。
4. **硬前提**:子件带 `(container_file_id, entry_path)` 出处二元组,重名以 entry_index 区分——否则集合差不可算。已嘱进 b-2b 的 killer(对账等式一刀)。

**判断点(供 Henry 复核)**:
1. ⚠️ **第 2、3 条是我压掉了原单的更重设计**(三码+潜在台账)——依据是「可从原件推导」与「词表防增殖」。若你认为失败条目该有显式台账(比如为将来 UI 直接列「哪些没解出来」),推导式对账会慢(要开包)——**一句话我加回,代价是多一张表**。
2. 新家法(候锁单派发前重对 HEAD)今日**第二次救场**——b-2b 若原样派出,builder 会撞 b-2 第一停同款墙。这条家法的成本(每单派前多一遍对读)已两次回本,建议保留为永久性动作。

## 7. b-4 收工抽检 PASS · 词典执笔纪律入册 · 12.9b 只剩 b-2b

**b-4(`056fb8a`)**:四条词现状栏 + 词典的形状/能力**常驻断言**接进 `docs:check`——「⛔ 不把形状写成能力」从此不依赖谁记得。复核四刀逐项具名变红;builder 记功一笔:按现物订正了调度的「零插头」前提(b-2 的 text-fallback 已是一条窄写入路径)。

**我的抽检(换刀法)**:不跑第五遍变异,直接**读断言源码**判守备——逐项具名/无计数/fail-closed/自测走临时副本,全对;独立跑绿。抓到一条**射程边界**并如实入册:K-1 禁词是五词定表,新措辞的能力宣称不在射程——闸绿 ≠ 词典无越界措辞(「零命中两义」的词典分身)。

**我的两笔落地(承接条目 6 排队动作)**:
1. 词典头部**现状栏执笔纪律**:每句现状须可指认使之为真的落地物(commit/行号);来源面靠本条、措辞面靠常驻断言,双守备各管一面。
2. 段计划尾追**铸段前订正备忘**:铸段记录⛔不写「零插头」;旧档「插头一个都没有」判为当日快照,不改史。

**判断点(供 Henry 复核)**:词典闸的禁词定表我**没有**要求扩表或改成语义判断——机械闸守已知回归形状,真值面归执笔纪律,这是分工不是缺口。若你认为措辞面也该上强度(比如禁词表随每次事故追加),一句话,那是合法的增长路径(每次被撞破一词补一词,与 fixture 表同构)。

**12.9b 现状**:b-1 ✅ b-2 ✅ b-4 ✅,仅剩 b-2b(按 v0.4 重写中)。
