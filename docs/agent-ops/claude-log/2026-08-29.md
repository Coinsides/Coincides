# Claude Log — 2026-08-29

## 1. 🏁 V12.9b 铸段:四单全落(b-1/b-2/b-4/b-2b),规格七次修宪 —— 12.9c 已开令

**b-2b 收工(`b0572c5`,三次派工)**:容器逐条目降级 + 如实申报。决定性一验:**曾把假申报锁住的断言,反转后专门抓那个假申报**(K-4 反例刀,复核亲刀 13/1 红且 tsc exit 0——语义变异非编译错)。builder 记功:无效取证自曝(编译错不当红点)、连续两轮先写回执、audit 如实 exit 1 不跑 fix 掩盖。两笔账都在 Review:builder 借语义不符的码未停线(b-2 builder 是对照组);48 自记「谎言是我先写进单里的——抓到它不抵消造出它」。

**我的收工抽检**:容器专项独立三跑 0 fail;K-4 反例刀亲读(:383);K-2 fixture 逐字对裁定(4 条目/1 坏/2 同字节 ⇒ 件恰+2、容器带码、条目层对账);**「瞬时对账报告回调」记功——推导做成可观测而不落库,两条约束的正解非折中**。

**铸段记录**已落段计划(遵订正备忘:如实写窄插头非零插头;在案债 TD-34/35、audit 基线全列)。

**下一段令(已发 48)**:12.9c 开工,**第一动作 = 整张段计划对 HEAD 重对现物**(它写于 12.9b 动土前,七次修宪后接线点几乎必变);MinerU 属真转写器第一次接线,「闸与生产者同单」入场条件生效;.env.experiment 两把 key 是粮草,key 值永不进日志/回执。

**判断点(供 Henry 复核)**:
1. **铸段是我拍的**(四单复核 PASS + 我逐单抽检 PASS + 全绿判据由 b-1 K-6 达成)。段收口九项式的「统一走查」本段没做——我判 12.9b 是纯服务端地基(无 UI 面变更),走查无对象;若你认为地基段也该有走查等价物(比如手造一个坏 zip 从 API 走一遍),说一声,12.9c 开头补。
2. 今晚累计:b-2 五派、b-2b 三派、三次停线、修宪 v0.3.1→v0.5.1 共四次(今夜),全部证据驱动。**节奏由停线驱动而非计划驱动**——我认为这正是把关在工作,但派工次数的成本你可复核。

## 2. c-0 岔路案:v0.6 修宪 —— 归一化从标签升格为可重放函数(采 A)

**案情**:48 重对现物发现 c-0 真形态是「映射不建造」(`sourceArtifact.ts` 已有 parser 身份/writing_role/页锚/seq),但其归一化(空行分段、换行折叠、trim)使 b-1 的 `join('')` 分割不变式必红——两不变式(逐字保真 vs 结构角色)看似首撞。48 给 A/B/C 三路,倾向 A + 修宪(基准改为归一化投影),并如实点名代价:K-1 将抓不到吞段。

**裁定(v0.6)**:
1. **正法源**:v0.2 的 K-1 基准本就是「申报类归一化后的参照」——所谓修宪其实是把 b-1 的 `join('')` 认清为 `none` 类特例。两不变式未撞。
2. **新承重梁**:申报的不是标签是**可重放函数**(canonicalizer 确定性/共享/版本钉死);**白空间保全元不变式** `nonWS(canon(x))===nonWS(x)` 常驻检——由此吞段可抓性成定理不靠运气,直接回应 48 的代价点。防自我印证边界:校验器只重放规范化,⛔ 不重放分段/角色。
3. **采 (A)**;(B) 买没有消费者的保真、(C) 为可推导物造第二链,均否。校验器 whitespace 类与 c-0 同单落地(闸与生产者同生第三次)。

**判断点(供 Henry 复核)**:此裁把「逐字保真」正式让位给「申报保真 + 原件永在」——碎片文本从此可以合法地不等于原件字节(只要归一化类可重放且仅动空白)。引用/检索将引用归一化文本。我判这符合保真阶梯的原意(低保真合法,伪造违法);若你要求「碎片必须能逐字引用原件」,那是 (C) 的方向,代价是双链。

## 3. c-0(T0 存量对齐带锚碎片流)第二次派工落地 + 亲刀复核结案

**做了什么**:c-0 按规格 v0.7 重写后第二次派工,builder exit 0。我按「亲刀复核比施工的刀细一档」自取红点复核,翻 `ready → done`,顺带结案 TD-29。

**亲刀三条(全部实测有咬,非读回执)**:

| 刀 | 变异 | 观测 |
|---|---|---|
| K-5 元不变式 | 给共享 canon 追加 `.replace(/a/gu,'')` | `not ok 7`,9/10 —— 元不变式 `nonWS(canon(x))===nonWS(x)` 真咬 |
| K-1c 第三产生点 | 删 `sourceImprints.ts:269` canonical 投影里的 `block_index` | 10 → **7/3**,同页两锚 canonical 撞车 |
| K-4 结构锁 | grep 两文件 | 双侧均 import `sourceTextCanonical.js`,**本地空白规范化零命中** |

- 恢复后 md5 回 `bf89a1…`,基线回 10/10。**全量真入口 `npm --prefix server run test:v2` 我自跑 331/331**(⛔ 未用裸 runner 空参)。

**边界**:9 条改动全部 ⊆ 允许面 7 项;`sourceFileIntake.ts`(教义级明禁,防工序前移)**零 diff**;`documentParser.ts` / `sourceContainerIntake.ts` / migrations / `INDEX.md` 全零 diff;第七查 exit 0。`projections.ts` 仍是既存 EOL 假脏,`git diff --stat` 空。

**TD-29 结案(⭐ 病名被直接目击)**:探针裁定 `pdf-parse@2.4.5` 自带 v2 类型 ⇒ 走删除分支。**旧 ambient 尚在时静态 v2 import 稳定红于 TS2305 + TS2497,删后零诊断** —— 这正是「说谎的类型声明**遮蔽**真声明」的现场证据,不是推断。K-7 常驻防回潮。

**记 builder 一功**:K-8 我只写了正反两刀,它**自己补了第三次变异**(删 `block_index >= 0 integer` shape 校验 ⇒ 负数反例漏收),并自行守住 `block_index: 0` 不被 truthiness 吞掉。

**记我一错(⚠️ 两义性 4b 现场复发)**:头两次施 K-5 变异都**没打中**(shell 吃掉了 `\s` 的反斜杠),测试照绿 10/10。**那个绿是我变异失败的产物,不是断言无效的证据。** 靠「先证明变异落地」这一步才没误判 —— 第三次改用按行号 + 不含反斜杠的模式,并以 `diff` 打印落地证明后才认红绿。⇒ 归拢:**施变异后必须先出示落地证明(diff / grep 命中),再读红绿**;凡「变异了却没红」,第一嫌疑是变异没落地,不是断言不咬。

**判断点(供 Henry 复核)**:
1. 我把 K-2 的绿只按「非空白字节序列一致」采信(回执措辞也是这个,且它自行补了「不外推到 Markdown heading」的适用面),⛔ 没让它升格成「文本完全一致」。
2. TD-30 落点仍在 c-1,本单未搭车 —— 我没允许 builder 顺手做。
3. `object-inventory.md` 本次零变化,故未 add;只翻了 `INDEX.md`。

## 3. c-0 结案(`6ba6152`)· v0.7 四裁全兑现 · c-1 开拆(三张单)

**c-0 两停两派后落地**:sourceArtifact 映射进 materialization 工序本体(⛔ intake 明禁,工序前移=教义违反);page 锚扩 `{page_index, block_index}`(三处产生点含最隐蔽的 canonical 层);每格式一拓印件(PDF=page,DOCX/TXT/MD=flow);K-1 参照取分段上游 per-page 文本;TD-29 结案(病名「遮蔽」:旧 ambient 声明遮蔽 pdf-parse@2.4.5 自带 v2 类型,删除分支,TS2305+TS2497 目击)。`test:v2` 331/331。

**新家法两条**:①「施变异后先出示落地证明再读红绿」(48 亲历 shell 吃反斜杠、变异未落地而绿——4b 两义的操作者侧机械化);②收工新惯例:复核方把 Fable 预定抽检点先亲刀、红点摆好再交。

**我的裁定/拍板**:c-1 拆三张(c-1a 环境探针/c-1b 映射+region 还账+TD-30/c-1c 火箭 1 级可押后);**Python 环境拍板:uv + Python 3.11 钉版 + 独立 venv 落 _external_tools/mineru(Henry 早前授权我定)**;夜间风控入单(禁睿频诊断窗,MinerU 重推理=削峰修复的真实压力测试,分小批批间落盘,重启如实记为诊断数据);c-3 门未开照准(有生产者无引用者)。

**判断点(供 Henry 复核)**:凌晨开 c-1(重型外部件环境搭建)是我判在夜班授权射程内(「所有工程会话任用」+连续作业令);风险=重推理可能触发 CPU 尖峰重启,已用分批落盘对冲,且重启数据对你的供电诊断有价值。若你醒来觉得夜里不该跑重活,c-1a 之后的批量环节可改日间跑。

## 4. 家法两条入 README + c-1a 拆单撞出四条冲突(⛔ 未自裁,已送裁)

**Fable 令 ①**:「变异落地证明」立为家法,写 README 时带上出生证明 ⇒ 已入 `handoffs/README.md`,挂在两义性专栏 4b 之后,连同**为什么 4b 不够**(4b 管语义层「改对地方没有」,本条管更早一层「编辑发生了没有」)与 Windows/Git-Bash 反斜杠被吃的工具面副产品。另立「抽检点先亲刀,红点摆好再交」为收工惯例。

**Fable 令 ②**:拆 c-1a。⚠️ **对现物后发现本单性质与派单令不同** —— 令写「装环境」,而**环境早已存在**:`D:/Coinsides/v12.9-selection/tools/mineru/`,`.python-version` = **3.12**、`mineru` 钉 **3.4.5**、`uv.lock` **441 KB / 130 包**、`.venv` 仍在,**正是产出初赛全卷证据与 187× 价格表的那一套**。⇒ 单改写为**遗产体检**,已落 `draft`,⛔ 未开工。

**四条冲突(⛔ 我不自裁,已送 Fable)**:

| # | 令 | 现物 | 后果 |
|---|---|---|---|
| 1 | Python 钉 3.11 | 3.12 | 改版 ⇒ 130 包的 lock 整体作废,与初赛证据脱钩 |
| 2 | venv 落 `_external_tools/mineru/` | 在仓外 | venv 不可搬(绝对路径写死),只能由 lock 在新址重建 |
| **3** ⭐ **设计级** | 工具箱 ⛔ 不进仓 | `.gitignore:55` 整目录忽略 | 出生证 `transcriber_lockfile` 会指向**无人可核的路径** —— **与 TD-29「说谎的类型声明」同族**:不可复核的声明比没有更危险。且 gitignore **无法**从被忽略的父目录反选子文件 |
| 4 | 「装环境」 | 已在 | 重装会重解 `>=3.4.5`(**下限不是钉死**),很可能拿到 3.5.x,却继续引用初赛价格表 |

**⭐ 闸自己撞出第四处射程边界**:允许面写了 4 个路径,闸只认 3 个 —— `\.[a-z0-9]+` 这个扩展名类**认不出含连字符的 `.python-version`**。危害形状是**「看不见」而非误杀**:闸对该类文件全程失声,整体仍报绿(两义性 #7 又一实例)。照前三处先例:先记录 + 立 **F-5 重写 fixture**,⛔ 暂不打补丁(单点改正则易误吞句中的 `a-b`)。

**判断点(供 Henry 复核)**:
1. 我把 c-1a 从「安装单」改写成「遗产体检单」是**对现物后的改写**,不是抗令 —— 但**四条冲突一条都没自裁**,全部停在 draft 送 Fable。
2. 冲突 3 我判为**设计级**并升级,理由是它动摇 b-1 立的出生证三元组之可复核性,不是本单局部问题。
3. 探针单没有 killer,我用「阳性对照」补位:**扫描件上 pdf-parse 应空而 MinerU 应非空**;若两者皆空则「跑通了」是假绿。无扫描件则如实记「未做」并停线,⛔ 不许拿有文本层的 PDF 冒充。

## 4. c-1a 四裁:初赛 MinerU 环境完整存活,「安装单」改「遗产体检单」

**48 拆单最大发现**:初赛那套 MinerU(3.12/3.4.5/130 包 lock/venv)完整活在 `v12.9-selection/tools/mineru/`——正是产出 12.9a 全卷证据与 187× 价格表的那套。

**四裁**:①沿现物 3.12/3.4.5(证据连续性 > 版本整齐,**我钉 3.11 是盲拍作废**);②⛔ 不搬不重建——c-1 直接用现址 venv,重建推迟到有搬迁消费者;③钉版三件入库 `_external_tools/mineru/` + killer 验入库副本与原件逐字节一致(「不可复核的声明比没有更危险」与 TD-29 遮蔽并族);④`--frozen` 强制 + dry-run 漂移如实记。另:扫描件阳性对照缺料的解法=机械自造 image-only PDF(渲染页面封图,是真无文本层原件非冒充)。

**记我账上 + 自立规**:「裁定前不查现物」第二次(v0.4④ UNIQUE / 本次 3.11)——按二次即机械化:**凡裁定含可被现物否证的具体参数,裁前必查,查不了明标盲拍**。已授权 48 见违即打回。

**第七查函数化触发器到期**:`.python-version` 连字符盲区是第四处射程边界,按我立的「第四处函数化一次做完」,已令 48 在 c-1a 派工后排重写单(F-1~F-5 fixture 齐)。

**判断点(供 Henry 复核)**:②的「用仓外现址 venv」让生产码依赖一条仓外绝对路径——单机现状下我判可接受(它同时是初赛证据本体),搬迁触发器写明(打包/部署单)。若你要求环境必须仓内自足,一句话改,代价是一次 --frozen 重建 + 同版验证。

## 5. 我的抽检刀被 48 拦下:它会毁掉它要保护的证据 —— 「亲刀不得毁证」立法

**案情**:我给 c-1a K-0 预定的抽检刀(「临时改裸 `uv run` ⇒ K-0 必红」)会**真实改写现址 `uv.lock`**——现址不在版本控制里,不可恢复,而它是 187× 价格表与 12.9a 全卷证据的本体。**为证守卫有效而先毁被守卫物**,48 拦下并给出等价安全刀:副本上两半证明(半一:裸 uv 确实改写 lock=危险是真的;半二:哈希比对分得出=量具会报警),合成原刀全部证明力,本体零代价。

**立法两条**:①「亲刀不得毁证——变异施在副本上,⛔ 不施在被保护物上」(48 拟,照准);②上游设计规则(我的教训):**设计刀时先算红点的代价;证明力可在副本等价重建的,⛔ 不许在本体上取**。另点名:两半刀展示了**证明义务可分解、可移址**——刀法的结构性进步。

**判断点(供 Henry 复核)**:这是今晚我第三笔个人错账(3.11 盲拍/毁证刀设计/加上 v0.4④)。模式:**裁定与刀设计快于查证**。已立的两条机械规则(拍板先查现物/先算红点代价)都授权 48 打回;若你认为该再加约束(如「宪法级承诺的抽检刀须 48 会签后才施」),说一声。

## 5. c-1a 收工复核结案(亲刀四处)+ 一处措辞收窄 + 三件 pin 扣住不提交

**亲刀(⛔ 非读回执)**:K-0 用**派工前**自记的四个 md5 复测,逐一相等(跑完 MinerU 两遍后再测仍等);K-2 三件逐字节全等,且 ⭐ `git add -n _external_tools/` **枚举**证明 208 个文件里只有那三件会进索引;K-3 上半我自己用现役 `pdf-parse` 抽自造 PDF,**去空白字符数 = 0**;K-3 下半我自己写脚本用现址 venv 跑 MinerU **两遍**,均 3 块 / 494 字符 ⇒ **阳性对照真成立,不是两者皆空的假绿**。

**⚠️ 一处措辞收窄(归我账)**:K-6 用的是 `--upgrade-package mineru`(**包级**),结论却写成「今日 registry 解未漂移」。我在副本上跑了环境级的 `uv lock --upgrade`:`mineru` 仍 3.4.5,但 **`uv.lock` md5 变、五个传递依赖漂了**。⇒ 改判为「**mineru 包**未漂移」。⛔ 不是 builder 的错 —— **工单只写了「记它今天会解出哪个版本」,是我把问题问窄了**。

**两处如实记差**:①字符数 494(我)vs 500(回执)—— 已排除 OCR 不确定(**两个独立进程各跑一遍都是 494**),差在**计数口径**。⭐ 入档教训:**一个数字要能被复核,必须连同计数口径一起申报**;c-1b 碎片计数进对账等式时这会变硬伤。②「冷」不是真冷 —— 权重下载 **0 B**,权重早在盘上,**首次真冷启(约 1 GB 下载)本单没测到**,⛔ 别把 13.18s 当新机首次成本。

**⛔ 钉版三件本轮扣住不提交**:实测三件走一次 git 往返 `uv.lock` **441341 → 443957** 字节 ⇒ 入库副本的原始字节指纹**必不等于**证据本体 ⇒ v0.7.2 的同一性判据**每次 checkout 产一个假阴性**。三件与 `.gitattributes` 推 c-1a-2 同批。本轮只提交报告与 `.gitignore`。

**两个 `needs: claude` 处置**:①PyMuPDF —— 核实后 **builder 对、我错**,工单已更正;②`docs:check` 报 INDEX 过期 —— builder 处置正确(INDEX 是它的禁区,且它**拒绝把总门冒充全绿**),INDEX 由我收口时重生成。

**判断点(供 Henry 复核)**:
1. 我把 c-1a 判为**通过**,但**扣住了它三分之一的交付物**(pin 文件)。理由是提交那一刻就会让 K-2 的绿变成假的 —— 宁可交付不全,不要交付一个会自我否定的绿。
2. K-6 的窄问题归我账不归 builder —— 它严格照单做了,单写窄了是发单方的责任。
3. 字符数差我**没有当作缺陷**,因为两次独立复现都稳定,判定不依赖具体数值;但把「口径须随数字申报」记成了 c-1b 的前置。

## 6. c-1a 结案(`c49c980`):MinerU 遗产体检全绿,证据本体零污染

**探针结论**:初赛环境完整可用——自造 image-only 扫描件上 pdf-parse 0 字符、MinerU 双进程稳定 3 块含 roles(阳性对照真成立);四哈希全程未变(K-0);pin 三件逐字节同源且枚举验证(208 文件仅 3 进索引);温启 13.18s/热 6.15s(⚠️ 如实记:权重早在盘上,**首次真冷启未测**,勿当新机成本)。c-1a-2(`.gitattributes` 与 pin 同批)在跑。

**过程中的互纠(全部入档)**:我的毁证刀被拦(条目 5)→ 我的 gitattributes 落点被 +2616 字节实测纠偏(伤害在提交时刻,防线须在上游)→ 48 的 K-6 问窄自认(包级≠环境级)→ builder 被质疑 PyMuPDF 后用现物自证反驳成功(下级证据推翻上级判断且上级认账)。新判语:「数字连同口径一起申报」「冷不是真冷」。

**⭐ 转报 Henry 的机器数据点**:MinerU 双进程推理(1GB 权重、CPU 密集)全程**零重启**——这是削峰修复后第一次真实重负载,撑住了。诊断窗口的最强证据。

**12.9c 进度**:c-0 ✅ · c-1a ✅ · c-1a-2 在跑 · c-1b(映射+region 还账+TD-30/36)待拆。

## 6. c-1a-2 结案 + c-1b 拆三张 + 自报一次「结果对、流程错」的提交

**c-1a-2(pin 字节)结案**:回执给了前后两组(前 443957/6/193 全 `equal=False`;后 441341/184/5 全等)。⭐ **我没只读回执** —— 从 **HEAD** `git archive` 独立签出实测,三件与证据本体 sha256 逐一相等;反例 `git check-attr text` 对 `server/package.json` 与 `.gitignore` 仍 `unspecified`。证据本体四哈希**第三次**复测仍全等。

**⚠️ 自报失手(第四种 `git add` 形态)**:`git add <三个文档名>` 后直接 `git commit`,而**索引里早已躺着 builder 暂存的 `.gitattributes` + 三件 pin` ⇒ 一次「只提交三份文档」的提交实际提交了七个文件。
⭐ 与前三次不同:**前三次错在 `add`,这次 `add` 完全正确,错在 `commit` 的默认语义**。已机械化:提交前必须打印 `git diff --cached --name-only` 逐行看过。
⚠️ **而且结果对得侥幸** —— 扫进去的正好包含 `.gitattributes`,pin 才是带保护入库的;若 builder 那刻还没建出它,我就把 pin 裸提交了,**正是我扣住它们要防的事**。⇒ 归拢:**由侥幸得到的正确结果和由流程得到的正确结果,在 `git log` 里长得一模一样。**

**c-1b 拆三张,排序由法推出**:
- **c-1b-0 指纹承重件**(已落 draft)—— 必须先于任何 MinerU 拓印。v0.7.2 同时规定「身份=指纹」与「既有行⛔不回填」⇒ 承重列后到 = **亲手制造一批永远无法回填的行**。这是「防线必须站在伤害上游」的直接适用。心脏是 K-3 反刀:**name 与 version 相同而 hash 不同 ⇒ 必判「不同」**,⛔ 不许比三元组。
- **c-1b-1 MinerU 接线** —— 现物:materialization 已不在请求路径(`setImmediate`)、`claim` 防同 source 重跑,**但不同 source 无并发上限**;13 秒/页 + 1GB 模型,N 并发会压死机器(此机在诊断窗)⇒ 钉并发 1 + 超时 + 页数上限。
- **c-1b-2 语义** —— region 验证还账 + TD-30 表格 + 计数口径结构化字段。

**判断点(供 Henry 复核)**:
1. 我把自己那次提交失手**主动记档并归为新形态**,而不是因为「结果没坏」就略过 —— 理由写在归拢句里。
2. c-1b 拆三张、且把指纹排在 MinerU 之前,是**用法推的顺序**不是偏好;若顺序反了,造成的损害按 v0.7.2 **不可修复**。
3. MinerU 并发上限我打算钉 1(保守),这会让多文档场景变慢 —— 属产品可感知的取舍,已在报 Fable 的消息里点明。

## 7. c-1b-0 结案(第二次派工)—— 版本号真正退出承重位

**亲刀两处,各取真红**:①**K-3 反刀**(Fable 预定抽检点):两个生产者的判同 SQL 改成 `AND (transcriber_lockfile_hash = ? OR 1=1)`(⚠️ **保参数个数**,否则红在别处)⇒ `not ok 5`,7→6/1;②**K-4 强制**:`requiredSha256` 条件改 `if (false)` 且缺值返回伪 hash ⇒ `not ok 6`,7→6/1。两处恢复后均回 7/7。全量 `test:v2` 我自跑 **338/338**(基线 331),`tsc` 零诊断,迁移 051 里 `UPDATE|DEFAULT|RENAME` **零命中**。

**记 builder 两功**:①它把判同 SQL 里的 `name`/`version`/`lockfile` **三个身份条件删掉了**,只留 hash + 作用域 —— ⛔ 不是「三元组多比一项」,是**版本号真正退出承重位**;②正反两刀走**真实 intake 与 materialization 路径**,不只测谓词 helper —— 防「谓词对了但生产路径没用它」这种假绿,**这一层我单里没写,它自己补的**。

**一处设计判断值得记**:`sameSourceImprintTranscriber` 要求两侧都是合法 64-hex 才判同 ⇒ **旧行的 NULL 永不判同**。这是「既有行不回填」的正确下游语义:**NULL 不是「同一」也不是「不同」,是【未证】**,而未证按不轻信处理。

**⚠️ 一处我的假阳性**:用 `grep -oE "'[a-f0-9]{64}'"` 搜「写死的转写器指纹」,命中三条 —— 查后全是**本轮未动**的 `content_hash` fixture,新增的确实是 `createHash` 现算。⇒ 入档两义 **#6b:grep 命中 ≠ 命中的是你要找的那个语义**;⛔ 命中数不是证据,命中的**是什么**才是。

**判断点(供 Henry 复核)**:
1. 第一次派工我判 builder 停线**正确**并归错于自己,而不是让它「挑一条违反」——两难本身是单的缺陷,不该由施工方消化。
2. K-3 反刀的变异我特意**保住了参数个数**;若删掉那行会因绑定参数数量不符而红,那种红**与判据无关**,会把「刀咬到了」误判成立。
3. 我的假阳性主动记档并归为新两义,而不是查清后默默删掉 —— 理由同上一条:**排除法本身也要留痕**。

## 8. c-1b-1 结案(第二次派工)—— MinerU 接上,且杀得死

**亲刀(Fable 预定两处我都先跑)**:①**K-2 指纹来源**换成 `package-lock.json` ⇒ `not ok 4`,5→4/1;②**K-1 杀得死**:第一刀去掉 `taskkill /T` **照绿** —— 按家法先怀疑变异,读实现后发现是**被测物有冗余**(还有一段 `Win32_Process` BFS 枚举后代逐个杀);补掐第二条后测试**挂死**,且机器上真的留下 `mineru-fixture-….py grandchild`、父进程已消失的**真孤儿**。⇒ **我亲手造出了那把刀所守的孤儿**,随即清除并复查零残留。全量 `test:v2` 自跑 **343/343**,tsc exit 0,开关未设时仍走 `native-pdf`。

**⭐ K-2 的第三方闭环**:落库值 `67c4b42d…` 与**我在本单之前**独立算的 `uv.lock` sha256 完全一致 —— 两次计算相隔数小时、来源互不相干。

**⭐⭐ 挖出 4b 的第三种含义**:变异落空的第三种解释是**被测物在那一点有冗余防护**(不是断言瞎、也不是没扎到点)。排除法:**读实现数清楚有几条路径,全部禁用才算一次完整变异**。⭐ 而且 (C) 是**好消息** —— ⛔ 别把它读成「断言不咬」去削弱一个本来结实的地方。附副则:**亲刀若可能产生外部残留,收刀后必须复查并清理现场**。

**记 builder 三功**:①`ownsTimeout` 边界准(只有 MinerU 绕开那层杀不死人的 `Promise.race`,既有 parser 逐字节不变);②测试缝**同名即抛**不能覆盖既有 parser,不是后门;③K-8 反例走**真 registry dispatch**(`parserCalls=1` 为证),自己堵死了注入绕过这条假绿。⭐ 另外它给 `native-image` **显式申报「不写出生证」**——这条我单里没写,它自己把「没有拓印就没有出生证」补齐了。

**判断点(供 Henry 复核)**:
1. 我把「变异落空」查到底才下结论,没有停在「K-1 不咬」——而查到底的代价是真造了一个孤儿,我认为这个代价值得付,因为它把「杀得死」从断言变成了目击。
2. 造孤儿之后我**主动复查并清理了现场**(进程 + 临时目录),并把这条立为副则。
3. 本单第一版又不可满足,又是我的错,又归发单方账——今天这是第二次(c-1b-0 一次、c-1b-1 一次),两次病根不同但同属「拆单前没把接缝查到底」。

## 7. c-1b-0/c-1b-1 结案:指纹承重件落地 + MinerU 正式接线(真转写器第一次进生产)

**c-1b-0(`5792417`)**:`transcriber_lockfile_hash` 承重列落地,判同 SQL 里 name/version 退出承重位(仅指纹+作用域);NULL=未证语义入宪。**c-1b-1(`d3c8661`)**:MinerU 进 PARSERS 注册表(显式启用⛔非默认)、并发闸三件套、子进程杀进程树(现有 `withTimeout` 被证实结构上杀不死任何东西——「取消的是等待,不是工作」)、每 parser 显式申报身份(v0.7.3:共享默认值=宪法被从内部绕过)。`test:v2` 343/343。指纹双源闭环成立(相隔数小时独立计算一致)。

**过程账**:两单第一版均不可满足(调度自认「拆单前没把接缝查到底」,病根=注册表是形状、选择才是接线);builder 两次停线均停对,另自补「native-image 不写出生证」「测试缝同名即抛」等四功。**4b 三分法定型**:断言瞎/变异没扎到/**被测物有冗余防护**(第三种是好消息,误诊为第一种会去拆冗余防护——唯一越修越坏的误诊)。复核方亲手造出并清掉一个真孤儿进程,立「亲刀有外部残留必清场」副则。

**里程碑意义**:12.9 的核心承诺「真转写器接线」自此兑现第一档——PDF 可走 MinerU 出 region 级拓印,身份可复核、失败落既有降级机制、机器有并发保护。剩 c-1b-2(表格+region 还账+口径)即 c-1 收口。

## 9. c-1b-2 结案 —— 诚实版落地,且两条法离开了中转站

**亲刀(Fable 预定的排他半边)**:把归一化分子换成整页(`[0,0,pageWidth,pageHeight]`)让表级锚膨胀 ⇒ **`not ok 1 - …hit and exclusion, and red on wider or inferred boxes`**,7→4/3,恢复回 7/7。全量 `test:v2` 自跑 **350/350**,tsc exit 0。

**⭐⭐ 本单最值得记的不是交付,是【两条法离开了 README】**:
1. **clamp 禁令**成了 `normalizedRegionBbox` 里的代码注释 + 真实行为(*"Deliberately do not clamp… must reach the contract and be rejected"*)。
2. **K-7 的三条触发器是【类型】**:`silent_patch_forbidden: true`、`transcriber_name_must_differ_from: 'mineru'` 都是字面量类型 ⇒ ⭐ **「打了补丁还自称原版」这条裁定现在由编译器执行**。
⇒ 元条说的「README 是法的中转站,不是归宿」,本单一次搬走两条。

**记 builder 一功(它多加的一刀)**:K-4 除换分母与跨面错配外,它还测了**反序 bbox** `[531,132,56,325]` 被拒 —— 证明没有静默排序。⚠️ **这一刀我单里没写**:静默排序会把坏坐标变成**看起来合法**的坐标,是「看起来对的错东西」家族的又一员。

**证据本体两口径互证**:它按全量清单(94,935 文件逐一 sha256 再聚合)前后同为 `ebcb8fa9…`;我另以 `uv.lock` 单文件 md5 复测亦未变。

**判断点(供 Henry 复核)**:
1. 我把「诚实粒度」判为通过的依据是**它明确没有冒充** —— 表级 fragment 报 `role:'table_row'`,20 个 cells 走 `style.source_table` 且**没有生成任何 cell bbox**。「做得少」被如实申报。
2. 第一次派工的停线我判为**今天最好的一次**:它发现的是**工具的结构性上限**,并拒绝了两条能凑绿的路,还捅破了我 killer 的一个洞(只验命中会被整表 bbox 平凡满足)。
3. 那个洞我认了并升格成通用法域(凡验定位必配排他),⛔ 没有只在本单打补丁。

## 8. c-1b-2 结案(`4503768`)· c-1 全线落地 · 裁段收口先行

**c-1b-2**:表级 region 碎片(真实整表 bbox)+ 结构化 cell 文本落地;单元格几何「本转写器不可达」为闭集状态非散文;`test:v2` 350/350。**排他半边**(v0.7.5-1)有真红作证(膨胀锚 4/3);builder 自加反序 bbox 刀(静默排序=把坏坐标变合法坐标,「看起来对的错东西」家族新成员——它已从躲开进化到主动补员)。

**⭐ 本单头条:两条法离开中转站**——clamp 禁令进代码(注释+行为),「打了补丁还自称原版」进**字面量类型**(编译器执行)。法的归宿层级就此排明:README(中转)< 常驻测试(跑时拦)< 类型(写时拦)。已令收工清单加一问:「本单有没有法可以离站」。

**中途裁定(v0.7.5,c-1b-2 停线案)**:MinerU 内部算出 cell bbox 但落盘前丢弃(精确到行)——裁「做得少不说得假」:①共享整表 bbox=伪高保真⛔;②静默补丁=身份指纹说谎⛔(合法形态=补丁版独立身份申报,「禁令必须说明正门在哪」);③采表级诚实交付+TD-30 诚实拆分(表级已达/cell 几何挂三出口触发器)。**排他通例入宪**:凡验定位必配排他,否则验的是「在不在里面」不是「在哪」。

**顺序裁定**:段收口先行(48 汇总+tech-debt+生成件,我签),c-1c(火箭 1 级)押后视夜班余量(~07:00 前)。

**判断点(供 Henry 复核)**:TD-30 的 cell 几何被我裁为「本转写器不可达」而非「必须达成」——若你认为单元格级引用是硬需求,三出口在案(换转写器/上游修/补丁版独立身份),任选一条我开单。

## 10. c-1 段收口(Fable 裁:收口先行,c-1c 押后)

**五单全 done**:c-1a(遗产体检)/ c-1a-2(pin 字节)/ c-1b-0(指纹承重件)/ c-1b-1(接线+杀得死)/ c-1b-2(region 与诚实粒度)。`test:v2` **331 → 350** 全绿,tsc 零诊断。收口记录:`handoffs/2026-08-29-v12-9c-c1-segment-closeout.md`。

**⭐ 本段头条不是交付,是【两条法离开了中转站】**:clamp 禁令活进注释+行为;「打了补丁还自称原版」活进**字面量类型** ⇒ **由编译器执行**。⇒ 立**归宿层级**:类型(写时拦) > 常驻测试/闸(跑时拦) > 注释+行为(读到时拦) > README(想起来时拦);收工清单加一条「**本单有没有法可以离站?**」。

**三次停线全归发单方**(我):①列举「生产者」漏了「全部调用方」;②查了注册表没查谁设 `parser_key`;③(工具上限,但我的 killer 有洞)。⭐ 第三次之后「先查接缝」**第一次在拆单前**拦住一张会翻车的单。

**⚠️ tech-debt 巡检撞出我自己两次同形损坏**:①`0873f68` 把 TD-30 弄丢两列 —— **危害是【状态列静默移位】,读到的是另一列却看起来像句合理的话**;②给 TD-36 写结案时插入文本含裸 `|`,又多两列。⇒ **同形第二次即机械化**:立 `scripts/check-tech-debt-table.mjs` + `npm run check:tech-debt-table`,**阳性对照已跑**(坏 exit 1 / 好 exit 0)。TD-6/12/28 的既有损坏**豁免登记,⛔ 不许新增**,未改是因为修要猜那个 `|` 原属哪半句、有改变原意的风险。

**另记两条边界**:①第七查是「**派发前**」的闸,对收口记录跑它得到的红是**范畴错误** —— ⚠️ 而这种红最容易被「顺手修好」(给收口记录硬加假的允许面段),那等于**为了让量具闭嘴而伪造被测物**;②异口径一致 > 同口径复算(94,935 文件聚合 vs 单文件 md5,粒度差五个数量级而结论一致)。

**⛔ 如实押后**:c-1c(三级火箭第 1 级)本段**未做**,已在收口记录里明写,触发器与老三条(key 不进日志/花费如实记/小样先行)一并在案。

**判断点(供 Henry 复核)**:
1. TD-30 我判为「**未清 · 已诚实拆分**」而不是「已清」—— 表级 region 交付了,但**单元格几何寻址确实没做到**,那是工具上限,不是我们做完了。
2. TD-6/12/28 的既有表格损坏我**登记豁免而非顺手修** —— 它们不是本会话所写,修=猜原意。
3. 我把自己两次弄坏 tech-debt 表格的事**写进收口记录**,而不是修好就算 —— 因为**危害形状(状态列静默移位)比那两处损坏本身更值得留档**。

## 9. c-1 段收口签讫(`ddb6065`)· 三源指纹闭环 · c-1c 放行

**签**:五单全 done(`test:v2` 331→350 全绿),收口记录抽读干净。**三源指纹闭环成立**:我亲算现址 `uv.lock` sha256 与 48 两次独立计算逐字符一致(三时刻/三工具链)——v0.7.1「身份=指纹」首次三方闭环。

**收口过程产出**:归宿层级四档(类型>常驻测试>注释+行为>README);tech-debt 表格闸(调度自己两次同形损坏后当场立,阳性对照已跑,既有三处裸 `|` 豁免登记不猜修);「伪造被测物让量具闭嘴」与「教量具认识错误答案」并为一罪两向。TD 终态:29/36 清,31 关,**30 诚实拆分未清**(表级已达,cell 几何=工具上限,⛔ 未冒充完工)。

**c-1c(火箭 1 级,考卷分角色)已放行**:定性为语义实验单(⛔ 不产生产码接线),产出按 12.9a 终审档案格式,老三条在案。

**判断点(供 Henry 复核)**:凌晨连开 c-1c 是我判夜班授权内(8 小时窗,~07:00 前)+ 连续作业令;它花 API 钱(量级参照 12.9a:$0.22 总花费级别),你充的 $20×2 射程内。

## 11. c-1c 结案 —— 实验单的亲刀是【独立重算】,不是施变异

**⭐ 复核形态换了**:c-1c 是分析单,没有可变异的代码。⇒ 亲刀 = **在 builder 收工之前**,我自己从 `_out/c2-mineru/_return_ticket.json` 把 6 条 miss 数了一遍。**两条互不相干的路径给出同一份清单**:`_total`=`{160,154,0.963}` 对上;role 分布 `table:1 · index:1 · chart:2 · image:2` **逐条对上** builder 的「改判 4 条(chart×2+image×2)、留红 2 条(table+index)」。

**⭐ 纲领 §7 的背景判断核实为真**(「拿文本尺量图块 4 例」= chart 2 + image 2);⚠️ **但 §7 没给第 6 条(`index` 目录点引线)归类** —— builder **自己发现了,并且没有为它新造第四把尺**(冻结口径明写不许),把它留成真 miss。**这正是 K-3 想要的东西。**

**⭐⭐ 我补的唯一一条:158 不是同一个谓词的合计**。`158 = 154 条「文本能回到该 bbox」+ 4 条「图像能回到该 bbox」`(表格那条仍红,贡献 0)。其中 `chart p3` 那条的 `fragment.text` 是**真实文本**而 crop 为空 —— **新口径用图像相似度让它通过,那段文本是否在那儿,新尺子既没验证也不再声称**。⇒ 凡引用 158 必须带脚注。⛔ **不必重跑**:档案已分别说了两把尺各自的射程,只是没把**合计**这一层说破。
📌 它是 Fable「换尺子后历史数字需口径脚注」的**递归适用**:那条管新旧之间,这条管**新数字内部** —— **一个数字底下可以躺着两个谓词,而它看起来只是一个数**。

**答 builder 的 `needs: claude`**:`projections.ts` 相对 HEAD **零内容 diff**,是 EOL 假脏(两义 #1)⇒「禁区零 diff」前提**成立**;它未猜归属、未触碰、前后哈希一致,**处置完全正确**,并且**没把这条含糊过去**而是请调度方定案 —— 记一功。

**记 builder 三功**:①冻结是真冻结(口径段前后 SHA-256 同一,冻结时刻早于唯一一次实跑);②**尺子自带防松条款**(不换类补救 / 缺 html 不退回正文尺 / 不事后造第四把尺 / 不试多阈值取高分)—— **把「改量具迎合被测物」这一向在口径里就堵死了**;③⭐ §5 第 2 条最见品格:它自己发现 `pypdfium2` 版本被记成 `unknown`,事后独立查得 5.10.1,**并明确拒绝为纯元数据错误重跑**(「避免把一次实跑变成事后新一轮」)—— **知道什么时候【不】重跑,和知道什么时候重跑一样难。**

**判断点(供 Henry 复核)**:
1. 我在 builder 收工**之前**就做完独立重算 —— 目的是让对账时**我手里的数字不来自它的回执**。
2. 我把「158 是两个谓词的合计」判为**需要脚注而非需要重跑**:射程各自已在档案里说清,缺的只是合计层的一句话。
3. `index` 那条 §7 未归类,我判为**纲领的疏漏而非 builder 的**,并且认为它**留红比强行归类更诚实**。

## 10. 夜班收停:c-1c 结案 + c-2 侦察押日间 —— 今夜总账

**c-1c(`d777c36`)**:零成本尺子修正——160 条重打,旧口径 154/新口径 158(**158 = 154 文本回程票 + 4 图像回程票,跨谓词合计必须拆开申报**——新法升总纲);K-3 反例自然成立(index 那条留真 miss,拒造第四把尺);builder 品格刀「拒绝为纯元数据错误重跑」。复核形态进化:分析单亲刀=赶在收工前独立重算。

**c-2 侦察(`684dc59`,⛔ 未派)**:环境遗产体检级,但重量在工序(唯一索引强制一 source 一 materialization)+判据(「一致」四分类:①外形②归一化③切分=尺子域,④真实指错=置信域)+形状(置信度无消费者=在猜形状)。两问押日间,我的倾向留档:c-2 拆实验层(c-1c 形态,先看分歧长什么样)与持久层(等真消费者)。

**⭐ 今夜总账(供 Henry 晨读)**:
- **工程**:12.9b 铸段(b-1/2/4/2b)→ c-0 → c-1 全收(a/a-2/b-0/b-1/b-2 + c-1c)→ c-2 侦察。`test:v2` 307→350。**MinerU 在生产**(region 锚+指纹身份+并发闸+杀进程树)。
- **宪法**:v0.3.1→v0.7.5,共 11 次修宪,全部施工证据驱动;三条法离站进代码/类型;三源指纹闭环。
- **互纠**:我 4 笔错账(v0.4④/3.11 盲拍/毁证刀/gitattributes 落点+零成本假设),调度 5+ 笔自报,builder 三次停线全停对——三层互不轻信全向运转。
- **机器**:削峰后含 MinerU 重推理在内**全程零重启**。
- **待 Henry**:UX 十条+Agent 设计商议(晨提醒);DASHSCOPE key 双定义确认;c-2 两问(会后)。

## 12. 夜班收停 —— c-2 侦察不派,停点核过

**c-2 判轻重:⛔ 不派。** 环境是遗产体检级(Docling 完整存活),**但重量不在环境**:①`046` 的 `UNIQUE INDEX idx_source_mat_one_per_record` 让一个 source 只能有一份 materialization(**数据库层强制**);②`storeSourceImprint` 在工序里只有一处调用;③出生证无置信度字段需迁移。⇒ 它是 **c-1b-0 级的结构单**,不是轻单。✅ 唯一就绪的是 c-1b-0 的意外红利:判同只比 hash ⇒ 两个转写器的拓印**天然共存**。

**⭐⭐ 两问都是设计级,凌晨不裁(Fable 明示)**。我把它的两个**倾向**留档并**明标「⛔ 非裁定」** —— 日间开裁时不得当已裁引用。⭐ 其中一条**我没想到**:实验层的副产品是**为一致判据供料** —— **先看到分歧长什么样,再冻结判它的口径**;与 c-1c 的顺序一致,⛔ 反过来就是凭空造尺子。

**⭐ 总纲又立一条**:**「没有消费者,我们就是在猜它的形状」**,与「闸先建好没有对手,形状很可能是错的」并为一对 —— 一个说**判据**没被真实错误检验过,一个说**字段**没被真实读取检验过。**共同点:两者都能被建出来、能被测试、能全绿,而形状是错的。⛔ 绿不证明形状对,只有【被真实使用】才证明。**

**风险节入单**:「制造假低置信,会把 c-3 的点射预算烧在尺子问题上」——⚠️ 而且**这种错不会报错**:分歧率看起来很高、报告看起来很充实,**只有账单知道出了问题**。

**停点核过**:c-1 全收(五单 + c-1c)· c-1c 尾注另立(⛔ 未改签过的 §5)· 三法离站(clamp 禁令 / 补丁身份类型 / tech-debt 表格闸)· TD 巡检净(TD-29 ✅ / TD-36 ✅ / TD-30 诚实拆分 / TD-37 钉 c-3 前置)· c-2 draft 两问在 §6。

**判断点(供 Henry 复核)**:
1. c-2 我**按住没派**,理由不是「做不完」,是**两问都是设计级、且其中一问(置信度形状)在没有消费者时只能靠猜**。
2. Fable 的两个倾向我**明标为非裁定**才留档 —— 凌晨的倾向若不标,日间会被当成已裁,那是**署名失真**的另一种形态。
3. 今夜从 c-0 复核到 c-2 侦察,**我自己有四次归账**:两次拆单不可满足(c-1b-0/c-1b-1)、一次 killer 有洞(c-1b-2)、一次把 tech-debt 表格弄坏两回。**四次都写进了档,没有一次只修不记。**

## 11. 崩机验尸定案 + 姑息模式复工

**验尸(Windows 事件日志)**:8/20 起 7+ 次崩全是**蓝屏非断电**(0x50/0x1E/0x139 内存损坏族,dump 俱在);今日三连崩含开机一分钟即崩;空载晨间 PCIe 已更正错误 89 条/3 天。**头号嫌疑:i7-14700F 退化**(Raptor Lake 事件核心型号 + BIOS 1661=2024-06 缺修复微码,带病两年;禁睿频缓解=退化芯片让稳定裕度,吻合);次号:DDR5 四槽混装。供电/睿频两旧理论**证伪**;与我们工具的关系仅"负载是油门,病在发动机"(开机即崩+空载报错为证)。

**Henry 裁定**:治本=换 CPU,回国前换不了,**姑息模式继续作业**。已建机器健康档案入 memory(作业对策:小步勤落盘/单 builder/崩机后 ACL 预检——预检当场第 4 次抓到 NUL 截断)。Intel 延保 5 年可 RMA,回国后议。

**UX-4b 现况**:builder 二派(蓝屏亡)→ 三派(ACL 亡,零伪造停线得体)→ 四派(蓝屏亡)→ **五派在跑**(新纪律:每完成一件立即追写 Result 不攒)。遗产在工作树按「无回执=未验证」待体检。

## 12. UX-4b 六派收工 + Fable 亲征复核 PASS(实机验证)

**六个 builder 前赴后继**(二派蓝屏亡/三派 ACL 亡/四派蓝屏亡/五派体检停线/六派完工),调度线下线期间由 Fable 亲任调度+复核(Henry:「上将军上前线」,批「这批做完后续交还」)。

**交付四件全落**:①几何归属→page mode 可见性(hitbox 中心点,outer 界)②虚线框分色(inside/crossing 蓝、workspace 灰)③gutter 撤三补一(右侧工具条新增真实 Insert)④整理模式合并(snapEnabled 并入 layoutMode+松手收编 crossing)。**中途裁定**:五派证死遗产 resize 刀(双重违约:inside 判收编+期望值算错)——规格不动、刀作废重写,hitbox 律钉死(inside 溢出合法)。

**复核**:独立重跑 tsc 0 + 专项 22/22(builder 自报另有 unit 249/249、boundary 159/159、verify:v2-bn8-runtime 全绿);**实机亲验**:快捷登录(零凭证)→ canvas 模式页内建块 → 切 Page 模式**块如实显示且位置不动**;Henry 晨间亲建的 "123" 块(原 bug 的第一现场)现于 page mode 带溢出露头。控制台零错误。批 `9c9f4bb...`(待查实际 hash)已提交。

**判断点(供 Henry 复核)**:①我没有对六派做自己的变异抽检(两层取证在:builder 变异日志 + 我的独立重跑与实机行为验证)——姑息模式下机器随时可能崩,我判「行为级实证 > 第三遍变异」,你可翻;②负例(workspace 块 page 不可见)只有单测作证,实机没验到(UI 建工作区块两次被吞,小事,K-2b 单测在);③本单是我直发直收(发单/裁定/复核/提交同一人)——三层互不轻信在此单降级为两层,系调度阵亡的非常态,后续恢复分权。

## 13. 🏛 组件产房章程 v1 铸版(午后-傍晚设计对谈,Henry 全程亲裁)

**从 Template Studio 验尸到一个完整子系统设计,约六小时对谈**。产出:`analysis/2026-08-29-component-nursery-charter.md`(权威);讨论轨迹在走查档 §7 补一~补十。

**定案清单**:①三段律(库中形先于能/入役晚绑定/服役形能必配)+「场所即申报」;②五站流水线+创造零面板(注意力框原语立项/检修口/异步黑盒);③库结构:形制树×自选集网、家族只增不改、三层持久模型(库精选/实例覆写/毕业通道)、接口指纹分拣、覆写至上、孤本实例;④改骨骼提案流四步(Agent 预审拼法复检,(a) 拼得出直接摆成品);⑤能力对称:两句为律(人能做的 AI 几乎都能/AI 能做的人全能做),中句(AI 无超人类之门)降为方向性原则(Henry 存疑,黑箱最小化非绝对);⑥落位链:样式三档 12.10(苗圃)→ Agent 基建 13 → 产房 13.x。

**过程亮点**:Henry 两次推翻 Fable 方案且均更优(「双库只增不改」溶解掉改版追随问题;「AI 件不自动入库」堵住库污染);Fable 的「形状先于能力」被 Henry 勘定射程(服役律非设计律)——**法理被产品直觉反向修宪,记录在案**。

**判断点(供 Henry 复核)**:章程标 active/权威,但其全部为 V13+ 设计,⛔ 无任何当期施工义务;唯一近期动作是样式三档(12.10,档位勾选仍待 Henry)。若你认为设计章程该走 draft→翻牌流,说一声我降状态。

## 14. UX 双单收官(委派层首航成功)+ 走查 #3/#6/#10 全闭环

**三层结构重建后首个完整循环**:Fable 拆单裁定 → 调度员子会话(监视/复核/接派/提交)→ builder 施工。**UX-6+10 生活痕迹卡**(`545f43f`)与 **UX-3 Gallery 去处化**(`ae3a894`)均复核 PASS,Fable 实机终验对版(新卡三件套+右键菜单;去处左栏+空货架零渲染+出处徽标)。调度员复核质量高:独立重跑全部门禁、逐 K 对照行号、各抽一刀亲验(含变异落地证明与字节级还原核对)。

**账目**:①UX-3 builder 擅自翻牌 ready→done(纪律偏差记行为账,状态与事实相符未回改;UX-6+10 头由 Fable 翻);②**中英文混排**:UX-6+10 卡英文(Continue/Untitled)、UX-3 左栏中文(全部组/最近)——两 builder 各自忠实工单字面,病根 = Fable 两张 mockup 语言不一致,**待 Henry 一句话定 UI 语言方针**(应用现状英文 + i18n 基建在);③server 全量 339/341 的 2 失败 = 本机 Python/MinerU 环境噪声(12.9c 域),非本批引入。

**今日走查战果终版**:#3/#4b/#4衍生/#6/#10 **五条已落地实证**;#7 章程铸版待 12.10 执行;#2 观察期;其余按停车场。
