# 2026-08-22 · Claude(Fable)工作日志

## 1. 12.1.4 复核 FAIL(方向成立)→ 止损线如期触发,12.1 线停

**事实**:builder `49755db` 只写测试、产品码 diff=0(reviewer 核);reviewer 亲测 X1/X2/X3 + layout→passive 四靶精确红绿;FAIL 在 harness 把中间承重点 `useRuntimeSurfaceStateController` 整体 mock 掉(leaf `noteId→undefined` 仍 209/209 绿)、payload 四来源同值、`layerProps` 双 cast。
**裁定**:按 log 21 止损线**停**,不再续打——第二次改线会让机制失效;且停的代价可控:桥本身已被两轮对抗复核证实,剩余是护栏精度,记 TD-9 随 TD-7 桥退役。J9 待真实走查(需登录态)后打 `v2.bn.12.1.1`。资源转 12.2 S1a。
**判断点(供 Henry 复核)**:四单换来一处 ~100 行修复 + 护栏——成本高在前两单划面错(Opus 已自认)与我「单一 helper」的空机关;机制(越界即停、阳性对照、未证因果授权推翻)在第三四单已见效,builder 两轮均零越界。

## 2. 云电脑不断线方案 v2 交付(取代 v1 东京隧道方案)

**交付**:`Coincides_Private/ops/2026-08-22-cloud-pc-plan-v2.md` + 私有 artifact。按 Henry 指示只用两路单 agent 核官方页,未再整轮跑工作流。
**核实**:Business 不能选区(租户地理不可改);Enterprise 可强制 Japan East;**Business 2026-05 起断开 1 h 休眠、无关闭开关**(进程冻结不死);Enterprise 无定时休眠文档但有版本中立条款;Business 美国页 $56 / 加拿大页 CAD $85.90;Enterprise $66 / CAD $101.20 + 前置许可;大陆到微软登录/RDP 网关端点不封(GreatFire),MVP 实测直连「时断时续」。
**拍板**:推荐 **B Enterprise Japan East(≈C$148/月含税)**;A Business 只在 30 天试用实验证明「常连可阻休眠」时可选;C Azure VM 排除(贵一倍)。**先试后买**:出发前用免费试用跑心跳实验①②③定档。备胎 $6 节点只转 RDP 流、建好不开。预算 5 个月 B ≈ C$740,比 v1 省约 1/3。
**判断点(供 Henry 复核)**:①我没有把「云电脑=不断线」当成立——Business 休眠是官方原文,这条把他的前提打了一半,所以方案骨架改成「实验定档」;②推荐 B 多花 C$50/月买区域保证与无定时休眠,是我的价值判断;③心跳监控替代了 v1 的整套封 IP 手册。

## 3. 铸版 V2.BN.12.1.1(tag `v2.bn.12.1.1` @ `31972bc`)

Opus 以 Henry 首轮留下的登录态补验 J9:标本 1791 字讲稿可见、对照 note 保持 Page、真空 note 留 Page,真 reload 成立 → **12.1 旅程分数 18/18**(边界见 journey-score §6.4)。按 §5.2 裁定打标签。S1a(服务端 zod 注册表 + manifest)已由 Opus 发单在跑(`ac1bf04`)。
**判断点(供 Henry 复核)**:标签打在 J9 通过的 docs 提交上(含桥 `70a2aaa`、护栏 `49755db`);TD-9 未闭合仍铸版,理由=它是护栏精度不是产品缺陷,且与桥同寿命。

## 4. 工程停摆排查:S1a 续跑死于 Codex 沙箱状态文件损坏(第二次)

**Henry 报「工程并行会话好像停了」。排查**:S1a 首跑 04:26 发单、04:46 产物停更无 Result;Opus 12:36 发续跑(ps1a2.log),builder 起不了任何进程:`helper_unknown_error: apply deny-read ACLs`,只做只读审计、未写 Result(处理正确)。根因:`~/.codex/.sandbox/deny_read_acl_state.json` 被崩溃截断成 22 个 NUL 字节,setup refresh 每次 parse 失败(`expected value at line 1 column 1`)。与 08-20 同一病。Codex 额度正常(默认模型 gpt-5.6-sol 探针 OK);唯一活着的 codex.exe 是桌面 app 的 app-server,不是 builder。
**修复**:备份并删除该文件 → 自动重生成合法 JSON;workspace-write 探针(spawn git status)DONE;setup_error.json 消失。已通知 Opus 发续跑。
**判断点(供 Henry 复核)**:这是环境修复不是施工,我直接做了;第一次(08-20)与这次都在 builder 长跑中途崩溃后出现 → 若第三次出现,该把「启动 builder 前探针一次沙箱」写进 Opus 的发单流程。

## 5. S1a 续跑口径裁定(Opus 提请)

**①** `docs:check` 不含 manifest,无需撤线(Opus 亲验,采)。**②** R-4 记 N/A 归 12.2a-1c,且「生成器不过滤是刻意的」——采并上提为设计不变量(design §3.1 补注):manifest = 忠实投影 ≠ 暴露清单;过滤只在机械门与 MCP 列表层。**③** adjudication §7 加反面分则「探针照见了自己」(Opus 提,第八次同族、首次拦在报出前)。S1a 续跑在跑(实物审计阶段)。


## (Opus)S1a 二级复盘 —— 五条硬闸独立核实通过,送复核

### 我亲验的(不听回执自述)

| 闸 | 我的独立核验 |
|---|---|
| **R-3 真实条目链路** | ⭐ **最要紧的一条,已证实**:声明的人类调用点 `CourseDetail.tsx#fetchSummary` **真实存在**(`:283`),且其内部**真的构造该 URL** —— `api.get(\`/notes?course_id=${courseId}\`)`。**这正是第一版失败的形状(伪造 call-site),这次是真的。** |
| 生成器纯度 | `new Function`/源码截片/正则解析 **0 处**;阳性对照确认正常 `import` 在 |
| **R-2 legacy 隔离** | 我的探针命中 **1 处**,查证为**注释**(`legacy v1 toolDefinitions are intentionally not…`);真依赖(import/require/调用)**0 处** ⇒ 回执成立。**是我的探针把注释算进来了,不是它报错** |
| **R-5 无第三套目录** | `shared/types/toolRegistry.ts` 物理删除(tracked diff `0/35`),无兼容层 |
| **未上主链** | `package.json` 已变,故复验:`verify:v2-bn8-runtime` **不含** tool-face/manifest/parity;`docs:check` 仍只有 index+inventory |
| **R-1 空表不判绿** | 源码 `PASS` 字样 **0 处**;0 条分支文案为「0 条 public 条目;manifest 未过期,**但未证明任何公开工具链**」 |

### builder 自己做对的两件(值得记)

1. **自纠了一条过紧的断言**:把注册表测试从 `length === 1` 收窄修正为 `length >= 1` + 按 name 查找 —— **避免未来合法增员被误杀**。这是它自己发现的,工单没要求。
2. **正确判定 P-3 不适用**:`list_notes` 只返回 note 元数据、不返回 TextUnit 内容,故「读面须暴露单元级 `writing_role`」在本单**无适用 payload**。**没有为了「显得覆盖全」而硬套一条不适用的要求。**

### R-4 的处置符合新立的设计不变量

Fable 于本日把我的读法上提为**设计不变量**:**manifest ＝ 注册表的忠实投影,不是暴露清单**;过滤只许在 ①12.2a-1c 机械门 killer ②12.2b MCP `tools/list` 两处。

回执写的是「R-4 记 N/A(归属 12.2a-1c);生成器不过滤是刻意为下游 killer 留靶,不是缺陷」—— **与不变量一致。**

> ⭐ 这条的实质:**若生成阶段就滤掉坏条目,下游的门将永远见不到靶子,变成永远不会红的绿灯** —— 那正是 S1 第一版失败的形状。**「不过滤」在这里是正确性要求,不是疏漏。**

### 送复核时点名的三条

1. **R-3 的充分性边界**:我已证实该 call-site 真实且真构造 URL。但**「client 有一处构造该 URL」不等于「人类在 UI 上能触发它」** —— 前者是必要条件。**复核请确认回执没有把必要条件写成充分条件。**
2. **`zod-to-json-schema` 落在 `devDependencies`**:回执理由是「只供 manifest 工装使用」。**若将来 MCP 运行时要在请求路径上派生 schema,dev-only 会在生产缺失。** 请评估该归类是否与 12.2b 的规划冲突。
3. **`object-inventory.md` 的门外脚本计数由 1 变 2**:新增 `check:tool-face-manifest`。**这是生成器如实报告「存在但不在门内」** —— 请确认该增量是预期的,而非悄悄多了一道无人跑的门。

### 判断点(供 Henry 复核)

1. **我把「探针命中注释」这件事记下来了**,尽管结论对 builder 有利。理由:**下次同样的探针会再命中一次,记下来才不必重查。**
2. **R-3 我是逐层验到「符号内部真的构造了那个 URL」才认的** —— 第一版正是栽在「有个同名符号存在」就认了。
3. **三条送复核的点名里,有两条不是找茬而是找边界**(必要/充分之分、dev-only 的未来冲突)。**回执写得越好,越该问「它没说的是什么」。**

## 6. 不断线方案再转向:阿里云无影云电脑(东京)(Henry 定)

Henry 判断 Windows 365 在华无官方服务、慢且不稳,改用**无影云电脑·日本(东京)**——本就面向中国账号、跨境走阿里骨干、持牌厂商正规服务,且无 W365 的不能选区与休眠两硬伤。我按截图给了配置(通用办公型 4 或 8 vCPU/16 GiB、Win11 Pro 中文、按月不限时长 + 自动续费、系统盘 200 GiB、策略改「断连不关机」、开每日快照、看独享 IP 价),并点明两条风险(凭证落在中国公司云上→细粒度 PAT 回国后轮换;阿里东京 IP 段在 AI 厂商的信誉未知→落地第一小时实测)。**未跑任何 agent**(Henry 明令)。v2 文档保留为比较基线,不再改写;无影版配置以本条 + 记忆为准。
