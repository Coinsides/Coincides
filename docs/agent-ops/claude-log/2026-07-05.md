> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State（Claude 行动日志 · 当日）
> **日期 (Updated)**: 2026-07-05

# Claude 行动日志 — 2026-07-05（含 07-09/07-11 追记）

## 8 —（07-11 追记）V2.BN.10.1 复核 PASS + checkpoint commit

- **复核**：迁移 045 与设计 v2 逐字对上（ensureColumn 双查+throw 比 044 更硬;四表 CHECK 全;backfill 幂等）;服务层四行地板单事务+placement ON CONFLICT 幂等;ensureHomeCourse 偏离确定性 id 习语但等价安全（partial unique 兜底,接受）;守卫接线亲验（Home rename/delete、两个前向路径 stamp 'system'）;stale 修恰一行+往返测试;伪造防线有测试直接断言 Zod strip。**亲跑 191/191 + 新套件 7/7,退出门六条全过。** 浏览器 smoke 豁免理由成立（无新 UI）。判定 PASS,准予 10.2。
- **Commit**：`cd3630c`（feat 10.1）+ `ce1b750`（docs:plan v3/设计 v2/10.1 review/Relation 开卷）,工作树净。
- **判断点（供 Henry 复核）**：①checkpoint commit 由我落（沿用你"commit 你们都能做"授权,趁 Codex 停手窗口）;②ensureHomeCourse 习语偏离已记档接受;③Home 英文名"Home"待 10.4 UI 期做文案。

## 7 —（07-11 追记）设计稿升 v2 对齐 Codex plan v3 + 短审查通过

- **背景**：Henry 指派 Codex 重写 plan（v3,吸收五批审视+Henry 六项拍板:四层脊柱/Placement 进 v1/durable run/动态删除默认/撤回可重建/五段纵切）;plan v3 §0 立"开工同步门"点名设计稿升 v2 补清六项。
- **做了什么**：设计稿 v1.1→**v2** —— ①四层脊柱精确 SQL（source_records/files/materializations/placements + 全部 UNIQUE/CHECK/索引 + notes.note_class + note_block_sources 双列 + courses.system_kind partial unique）②上传补偿协议（选定"DB staging→rename→ready",全故障点补偿+启动清扫）③current run 唯一规则（v1=UNIQUE(source_record_id) 每源恰一行,retry 复用行 attempt++;原子发布单事务+claim 单语句抢占+typed errors）④Source 硬删 quarantine 协议 ⑤has_user_work 闭集查询（5 条 SQL 判据+多 placement 独立条件,排除默认 Purpose）⑥生命周期策略注册表（move15/delete/preserve/fk-derived + PRAGMA 扫描 contract test——6→15 教训制度化）。v1.1 的守卫矩阵/扫描机/pdf-parse pin 等核查资产保留。
- **短审查（设计 v2 vs plan v3)**：逐节比对**无冲突**;两处有意变更已同步（note_block_sources 指 source_record_id;image 能力1 去掉 vision 文本地板——Plan v3 口径,与 Henry"简单 API 链不值得背验收债"同判据）;一处补位决定（投影 note course=创建时 placement course,留给 Codex 10.1 异议窗）;一处有意不加（source_block_id,循 Codex 自己的 12.6-7 speculative 字段原则）。
- **另**：会议记录分卷（07-05 封卷至⑲;新建 07-11 卷:五幕图/导出后移/磨合期/用户模拟三层机器/说明书体系双版本活文档）。
- **判断点（供 Henry 复核）**：①run 表 v1 用 UNIQUE(source_record_id) 强约束（比 plan"至多一 active"更强,retry 复用行——我选了更简单更不易错的那半,未来重解析降级为 partial unique 已注释）;②投影 course 归属补位决定;③source_block_id 不加。三处 Codex 若有异议,10.1 提。**开工同步门条件已满足,migration 045 可以发车（待你确认）。**

## 6 —（07-09 追记）V2.BN.10-plan v1→对抗核查→v2 就绪待翻牌

- **前情（07-09 全天讨论,详见会议记录 §2.7.2/§2.9 共十九条）**：存储设计五判断点全拍;交互定型（引用源为主/双开/收转解耦）;删除权限层（非对称删除/主页/逃生口溶解）;溯源宪法三不变量+血统面板;升格三档;网页归档（C 形状,A/B/D 归收集章节,搜索外包 MCP 插座）;音频不收（STT 外包,甜品后置）;视频储存展示定位;性能三板斧归 V12。
- **plan 流程**：写 V2.BN.10-plan v1 → 4 维对抗核查（wk3ht4b5l:迁移与真相层/摄入管线/物化与消费/护栏与忠实度）→ **1 BLOCKER + 5 HIGH + 8 MED 全数回填** → 设计升 v1.1（三处）+ plan 升 v2。
- **核查战果（要点）**：BLOCKER=逃生口"六表"实为 **15 表**（canvas 四件套+图片扩展+CG 碎片/花瓣全漏,漏迁=迁过去的笔记丢图丢页框）;HIGH=①release 顺序（迁移必须先于 releaseCourseCanvasAssets,否则用户选"保留"图片 blob 却被物理删,红线）②canvas_backing 前向路径（只 backfill 不改创建点=漏修复发）③canvas-objects 写面漏守 ④notes PUT 必须字段级（metadata-only 放行,否则杀标注/排版赠品）⑤只读查看器（modePolicyService 无只读概念,须 readOnly 旗+击杀清单,4xx 不是 UX）。另:source_files 状态双轴拆列、precheck 免重复传输、第 5 台扫描机（source-anchors）、pdf-parse 1.x 死 API 陷阱、stale 修的精确指位（normalizeSourceSyncStatus 加值,写方勿动）。
- **判断点（供 Henry 复核）**：plan v2 留了**两个 plan 级决定**待翻牌一并裁:①去重对话 v1 两选项（"新版本"藏,系我的范围推导非你已拍）②课程页上传口易主（旧 Documents Upload 降级,冻结表不再被旧 UI 写）。三道闸第三次兑现:plan 自查看不见 canvas 卫星表,核查抓出。

> 最新在最上。本日主线：目的（purpose）实现设计草稿 → 4 维对抗核查 → v1 定稿 → 正式版本 plan（V2.BN.9）+ roadmap 顺延 → Codex 落地 V2.BN.9 → Claude 收口核查（抓出一颗我设计漏的数据丢失红线）→ 9.1 修复复核 PASS → **封版 commit（Claude 落，Henry 授权）** → Source 定义长谈（会议记录 §2.8 十四条定型）。承接 07-03 构建分解 + 07-04 Source 设计。

## 5 — 引用源存储形态设计 v0→核查→v1（gating 决定就绪待拍）

- **做了什么**：待拍清单清零（第4条=A,5-7 盖章,§2.7.1 落档）→ 写存储形态设计 v0（方案 A:引用源=真·note + note_class;事实源=全局 source_files;artifact 表降级接口形状）→ 4 维对抗核查（wvwfq3gs6:血溅面穷举/子系统交互/物化与锚/少返工+B 钢人）→ 全 CONFIRMED 修正回填升 v1。
- **核查战果**：4 维全 sound-with-fixes,方案 A 站住且论证变硬（B=会议红线"独立编辑副本表"的必然归宿;C 钢人后否:半拆墙更违红线）。**利好**:笔记列表血溅面=恰好 1 个端点(routes/notes.ts:96)+顺手修 canvas_backing 现漏。**3 HIGH 修正**:①双层标记（块级 source_kind——块写路径无笔记上下文+4 台块扫描机器看不见 note_class,两台带写路径）;②版次链必须挂 source_files 不挂 note_id（否则出版机器时代删 course 级联蒸发人工修订=红线）;③删 course 护栏措辞诚实化（标注/CG/目的不可恢复+跨 course 引用退化为仅收据+逃生口迁 course）。另 6 MED/LOW 全回填（服务端写守卫/note_block_sources 加 source_file_id/source_anchors 物理不可 re-point 改判 superseded/闭枚举 block_type/safeAlter 断言加固/拆墙措辞诚实化）。
- **判断点（供 Henry 复核）**：设计 §5 五个判断点(a-e)待拍;拍完写 V2.BN.10-plan。三道闸模式再次兑现:设计自查看不见"块与笔记 M:N"这层,核查抓出。

## 4 — V2.BN.9 封版 commit（Henry 授权 Claude 直接落）+ Source 定义会议记录收口

- **封版**：清 `.codex-tmp` 干净构建复跑三套（test:v2 **183/183** / model-contract **58** / boundary **159**）→ 分层提交：代码 `72712de`（feat: land v2 bn 9 purpose foundation，含 9.1 红线修复）+ 文档 `7531499`（plan/设计/handoff/会议记录/roadmap 顺延）→ 工作树净。SHA 已回填 handoff。**V2.BN.9 正式封版。**
- **Source 定义长谈落档**：`2026-07-05-...-Source-Definition-Meeting-Notes.md` §2.8 **十四条定型**（三种意图/出版模型/三层定名/收据在锚/链接vs引用/身份倒置/三方合并/事实源版本痕迹/一套编辑机器/忠实投影/降级阶梯/介质距离/值为真相式为出处/feed=自动版本化事实源停车）+ §2.7 待拍清单（7 条+3 design-later）+ 契约措辞六条。
- **判断点（供 Henry 复核）**：
  ① 封版由我落 commit 是 Henry 明示授权（"commit 小事,你们都可以做,文档活儿本来你管"）——今后小版本封版 commit 我可直接办,仍守干净构建纪律。
  ② 门口排队：引用源存储形态设计文档（gating,走设计→核查→拍板老流程）→ V2.BN.10-plan（事实层可先行）;§2.7 待 Henry 过。

## 3 — 复核 V2.BN.9.1 修复回执 · ✅ PASS · V2.BN.9 可封版

- **做了什么**：Codex 落地修复单（升级为 V2.BN.9.1）。Claude 亲读主修代码逐行对 spec（快照软删+悬空边 / surviving-purpose 判据 / OR IGNORE 保原值）、亲读 RED 测试（真建模含中间 PUT 的完整往返；fail-before 由上轮 PoC 独立实证）、亲跑回归（test:v2 **183/183**、model-contract **58**（新 PurposeFrame contract group 实证）、boundary **159**）。顺带三件（写放大 / 迁移约束断言 / 客户端契约断言）全核实。
- **结论：红线已封死，V2.BN.9 可封版。** 复核详情回写进 handoff `## Claude 复核` 段。
- **判断点（供 Henry 复核）**：
  ① 残留一条 LOW 记档不阻断：空 payload 分支的隐藏边作废（今日客户端不可达、语义可辩护，未来有空 payload 调用方时把 ensure 重建 id 计入 surviving 即可）。
  ② **流程提醒：V9+9.1 还没 commit**（tip 仍 c641c0d、33 脏路径）——按"每个小版本做完即 commit"新工作流，复核已过、该落 commit 了。
  ③ 本单闭环再证那套模式：设计核查（wrjge82ql）→ 实现核查（wjvxzr6u1 抓出交互红线）→ 修复 → 复核（fail-before/pass-after 两半实证）。三道闸各自兑现了别人漏的东西。

## 2 — 核查 Codex 落地的 V2.BN.9 · 抓出一颗我设计漏的数据丢失红线（BLOCKER）

- **做了什么**：Codex 把 V2.BN.9 目的层做在工作树里（未提交）。Claude 亲验最危险两块（migration 044 + purposes 服务）后起 4 维对抗核查（wjvxzr6u1，实跑 suites + PoC 活探）。
- **结论：3 维 PASS，1 颗 CONFIRMED 数据丢失 BLOCKER。** 迁移(181/181、CHECK/unique 实测拦住、backfill 保 identity_role、schema.sql 没动)、服务端(coalesce/双发/role 冻结可恢复/不清边/validator/路由/纯读)、客户端(17 处 rebind 全对、suites 57/159/build 全绿) 都扎实。
- **⚠ BLOCKER（我盯到、PoC 坐实）**：c' 软删红线**从写路径被绕回来了**。读时过滤了软删 CG 的边（客户端看不见）；但 gallery 每次组编辑都把「过滤后视图」全量 PUT（replaceNotePurposes = DELETE-all+insert）→ 隐藏边不在 payload→被永久删→CG 复活后 role/序没了。对抗核查拿真实服务跑 PoC：往返后边=undefined。触发平常（画布软删一个 CG + gallery 改另一个组保存）。画布表面安全，只 gallery 中招。现有 test #4 假绿（跳过了中间那次 PUT）。
- **责任归属（诚实）**：**这是我设计 v1 的漏，非 Codex 实现错**。c' 只写了"读时过滤"，没规定写路径保护隐藏边；Codex 忠实实现了两半，是交互产生 bug。**而且上一轮 4 维设计核查(wrjge82ql)也没抓到这个交互** —— 我是亲读实现代码时才盯到、再用 workflow PoC 坐实。
- **写了修复单**：`2026-07-05-v2bn9-purpose-softdelete-writepath-fix.md`（draft）：主修=replaceNotePurposes 在 DELETE 前快照软删成员的隐藏边、插完 payload 补回存活 purpose（INSERT OR IGNORE、保原 id/role/order）；RED 测试建模真实往返（修前 fail=undefined、修后 pass）；顺带 MED 写放大 + 两条 LOW 测试缺口。
- **判断点（供 Henry 复核）**：
  ① **V2.BN.9 暂不可封版**：一颗数据丢失红线（心爱之物不可凭空消失）必修不可 defer。修法小（服务端一处 + 一条 RED），blast radius 只在 gallery。
  ② **纵深防御兑现价值的又一例**：设计核查 + 实现核查是两道不同的闸——设计核查看的是"设计对不对"、实现核查亲读代码才暴露"两个各自正确的半拉子的交互"。这个 bug **只有在实现存在、能跑 PoC 时才抓得到**。这也印证了为什么"实现后必核、不信绿测"——现有 test #4 是绿的、却假绿。
  ③ **除这颗外，整版质量很高**：Codex 把设计里 4 个对抗核查抓出的雷全躲过了（CHECK/safeAlter/backfill-COPY/delete-all-insert/竞态安全/读过滤），17 处 rebind 零遗漏，suites 全绿。修掉这颗即封版。

## 1 — 目的实现设计 v0 → 对抗核查 → v1 定稿 → V2.BN.9 plan + roadmap 插入

- **做了什么**：写实现设计 v0（`analysis/purpose-frame-implementation-design.md`：purposes+purpose_members 两表、CG 补 type/冻结 role、懒建默认目的、~15 处客户端 rebind、四阶段）→ Henry 拍定 5 判断点（表名 `purposes`/bootstrap 自动挂/软删清边/role 值迁 type/is_note_default 在 purpose 行）→ 4 维对抗核查（wrjge82ql：迁移数据安全/服务端契合/客户端消费面/少返工语法，全部对照真实代码行号实证）→ 全 CONFIRMED 修正回填升 v1 → 写 `docs/releases/V2.BN.9-plan.md`（按 Phase-Plan-Template）→ roadmap 更新（权威 map + §2026-07-05 dated 段：目的插 V9，Source→10、Relation→11、打磨→12+）。
- **核查战果（4 维全 sound-with-fixes、无需重设计，但抓出）**：
  - **BLOCKER·传输先例误记**：v0 声称"purposes 随 note canvas persistence GET/PUT 走（同 annotation truths 先例）"——**事实错误**：annotation truths 有自己独立的 by-note 路由对，canvas persistence 根本没有整体 PUT，且 GroupGallery 编辑器（v1 的 role 写入口）压根不走 canvas persistence。修：专用 `GET/PUT /api/purposes/by-note/:noteId` + purposeRepository（这才是真 house 先例，diff 反而更小）。
  - **HIGH·判断点 c 翻案（红线陷阱）**：我原推荐"删 CG 同事务清边"、Henry 已照拍——核查证实 **CG 没有硬删路径**（只有可经 upsert 复活的软删 status='deleted'）→ 照原拍实现 = 可撤销的删除不可逆销毁用户写的 role/fitness/序。修：**软删不清边、读时过滤、复活即恢复**（c'，已在设计 §6 标注待 Henry 复核）。
  - **HIGH·P1 独立 ship 清空窗**：旧客户端 full-replace 保存会把 backfill 好的 identity_type 清空 → 过渡期服务端 coalesce(type??role)+hydrate 双发 + P1–P3 定为一个部署单元。
  - **HIGH·replace 习语撞复合 UNIQUE**：镜像 replaceContentGroupMembers 的 ON CONFLICT(id) 会 500 → 改用 annotationTruths 的 delete-all+insert。
  - 另 9 MED + 8 LOW 全部回填（safeAlter 防 schema.sql 启动雷 / CHECK 提前埋防整表重建 / note trash-restore 语义 / bootstrap 滤活 / 三段回退 normalize 保 legacy 导入 / URL ?mode=role legacy 映射 / v1 role 粒度诚实声明 / 成员漂移规则 / 缺失消费点补齐至 17 处）。核查同时实证：迁移机制、血溅面、四个少返工口子、"role 值=type 值"全部为真。
- **判断点（供 Henry 复核）**：
  ① **c' 翻案必须过目**：Henry 拍的 c 是照我的错误推荐拍的；核查用代码证据推翻。新语义（软删不清边+读时过滤）更符合红线与相对不焊，且已写进设计/plan/测试锁。**若 Henry 不同意需在 Codex 动工前说**。
  ② **对抗核查再次兑现价值**：这次抓的是**我自己设计里的两颗雷**（传输先例记错=不可实现、清边=数据丢失陷阱）——设计者自证不可信，核查 vs 真代码是必要闸。已沉淀为 roadmap §2026-07-05 工程纪律。
  ③ **产物三件套**：设计 v1（技术权威）/ V2.BN.9-plan（边界+验收）/ roadmap 顺延（V9 目的、V10 Source、V11 Relation、V12+ 打磨）。**Henry 说 plan 插好就让 Codex 开工** —— plan 已就绪可翻牌。
  ④ 编号红利：目的在 Relation 前 = "role 上边→端点=进目的的向量" 的依赖顺撇。
