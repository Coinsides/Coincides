> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State（Claude 行动日志 · 当日）
> **日期 (Updated)**: 2026-07-03

# Claude 行动日志 — 2026-07-03

> 最新在最上。本日主线：审 Codex 交回的最后一份 burn-down 落地（V2.BN.8.11.16 Stage-5 image asset GC）—— 4 维对抗核查 + 亲验铰链点 + register 校正。

## 1 — 审 Stage-5（V2.BN.8.11.16 image asset 生命周期 GC）· 核心 PASS + 挖出我自己 handoff 错判的当前泄漏

- **做了什么**：Codex 落地 Stage-5（V2.BN.8.11.16）。亲验核心(`releaseAssetReference` + image `cleanupOnDelete`)，再起 4 维对抗核查（wubnons7e）：refcount 核心+删除时序 / duplicate 共享 asset 存活 / 测试充分性+真 blob 正控+实跑 suites / 对抗新坑。
- **结论：核心修复 CONFIRMED PASS（扎实、测试对抗锁牢），但对抗维挖出一条我自己 handoff 错判的当前泄漏。**
  - **核心亲验**：`releaseAssetReference`(canvasAssets.ts:179-221) exclude-self COUNT、**user-scoped**(无 note_id、带注释)、remaining>0 不删、归零删行+best-effort unlink(吞 ENOENT 绝不 throw 挂事务)。image `cleanupOnDelete` 在 canvas_objects DELETE **之前**触发、先删 self-extension 再 release(传 self 作 exclude) → sole-ref 归零 GC、shared duplicate 保活到末删；migration 041:38 RESTRICT 永不触发(canvas_assets 是 SET NULL)。duplicate 建**独立第二 extension 行** → refcount 看得到 2。
  - **测试对抗锁真**：单引用删是**非空跑真 blob 正控**(真写 33 bytes、删前 existsSync=true 删后=false、路径与生产 unlink 对齐)；duplicate 负控真。**活探**：release 变 no-op / 破 exclude-self → cutover 双双转红(42/2)，还原 44/44 零残差 diff。四 suite 实跑绿：cutover 44 / test:v2 173 / model-contract 57 / boundary 159。
- **但对抗维挖出 CR-8.11.16-01（🟡 MED·CONFIRMED 当前泄漏）**：`courses.ts:164` `DELETE FROM courses` 是 live 硬删靠 FK 级联；`image_object_extensions.course_id/note_id` 是 CASCADE(扩展随删)，但 `canvas_assets` 只 SET NULL → **canvas_assets 行+磁盘 blob 永久孤儿**(扩展没了再无引用可 GC)，`cleanupOnDelete` 只在 deleteCanvasObject 跑、FK 级联不触发。**⚠ 这打脸我 2026-07-02 那份 handoff**——我只 grep notes 软删+缺失 user 路由，漏了 courses 级联，错判成"未来 gap 非当前泄漏"。亲验血溅范围：只 courses 一条(page_frame 批删非 asset-backed、主删路径清引用干净)。另 3 条 LOW：-02(原地换 asset 潜伏 under-release·无 live flow)、-03(承重红线测试文件 **untracked**·收口 commit 前必 `git add`)、-04(测试路径不镜像生产 env-aware + 共享盘目录状态敏感)。
- **判断点（供 Henry 复核）**：
  ① **Stage-5 核心可算 done**——修复本身(per-object 删的 refcount GC)正确、测试对抗锁牢，无红线违反。
  ② **⚠ 我的 handoff 有一处事实错误**（CR-8.11.16-01）：我把 bulk-delete 泄漏面判成"未来 gap"，实际 `courses.ts` 硬删是**当前 live 泄漏**。这是**我 distill 失责**——非对称下 Codex 读我的 current-state/handoff 当接口，我的错判会让他漏掉。已在 register 醒目自纠。**是资源泄漏(磁盘/DB 膨胀)非红线**(心爱之物没消失——课程连内容是用户主动删的、只清理不全)，故标 MED 非 HIGH。修法：course/note 删路径接一个复用 `releaseAssetReference` 的资产 teardown。**是否现在写修复 handoff、还是攒进收口一并做——等你定**(register-only until Henry directs)。
  ③ **对抗核查再次兑现价值**：4 维里绿测全过、我亲验核心也过，是**纯对抗新坑维**挖出 courses 级联漏(且反过来证伪了 spec 自身的"无当前泄漏"断言)。这跟 Stage-4 两轮(红线 BLOCKER、null-offset 残留)同一模式——绿测/常规覆盖看不见的，对抗维接住。
  ④ **盘面**：Stage-1~5 全落地+审过。8.11 收口 commit 仍待推（**⚠ 复用风险**：承重测试文件 untracked、`.codex-tmp` stale 构建假绿——收口必须干净构建跑 CI + `git add` 全部承重测试文件 + 回填 SHA）。
- **续**：Henry 拍板"去写吧" → 已写修复 handoff `2026-07-03-8.11-course-asset-teardown.md`（draft·to codex）：簇 A = course 硬删的 refcount teardown（`releaseCourseCanvasAssets` 复用 `releaseAssetReference` 逐对象、先删 self-ext 再 release、跑在 cascade 前、事务包裹、user-scoped 守跨 course 共享）+ RED-first 独占正控/跨 course 负控/课内共享；顺带小修 -03（`git add` untracked 测试文件·必做）/-04（测试 env-aware 路径 + per-test 目录隔离）/-02（upsert asset-swap 注释锁）。单里**明确纠正上一份 handoff line 31/86/95 的错判**。等 Henry 翻牌 ready。
