> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State（Claude 行动日志 · 当日）
> **日期 (Updated)**: 2026-07-04

# Claude 行动日志 — 2026-07-04

> 最新在最上。本日主线：把"Source 该怎么设计"这个总闸聊透 —— 深挖现有 Source 保真度（确认初级）+ 联网调研 OCR/VLM 工具 + 落成 Source 设计决定文档；随后回主线盘 8.11 收口坑面。承接 07-03 的目的/圈模型构建分解。

## 3 — 回主线盘坑 · ⚠ 自我更正（盘点出错→逐行核→改正）· 写收口 handoff

- **做了什么**：Henry 要"看还有什么坑必须填 → 填完收尾清工作区"。我先按 register 的"排查建议优先级（8.11 收口后）"清单报了"§0/§1/§2 一簇 🔴 红线必须填、page_frame 数据丢失是头号未修"。Henry 让我写 handoff 填 page_frame。**我在动笔前逐行核 CR 实际状态，发现报错了。**
- **⚠ 自我更正**：那份优先级清单是 **burndown 之前** 写的、**已过时**。逐行复核 + `🔴.*☐` 零匹配证实：**整簇 🔴 红线已被 8.11.12–14 burndown 全部清掉、0 个 open 🔴**。CR-8.11.1-01 ☑(043 repair) / 3.1-01 ☑(save 硬拒 page_frame) / 3-01 ☑(kind 过滤) / 4-01 ☑(server snapshot 过滤) / 1-05 ☑(liveness) / 4-06·4-03 ☑(8.11.13) / 8.11.8-01 ☑(8.11.14 structured undo)。这些 ☑ 还都是我本 session 亲手对抗核查过的。→ **没有"必须填"的红线坑，不用写 fix handoff，直接进收尾。** 已给 register 那份过时清单打更新戳（§0/1/2 标已修 + 当前实况）。
- **写了收口 handoff**：`2026-07-04-8.11-closeout-commit.md`（to codex, draft）—— 非改代码，是**提交卫生**：干净构建纪律（清 `.codex-tmp` 防 stale 假绿）+ `git add` 未跟踪承重测试（CR-8.11.16-03）+ 四套验证门 + 回填 SHA。
- **判断点（供 Henry 复核）**：
  ① **⚠ 我犯了"信 summary 不逐行核"的错**：register 的过时优先级清单让我误报"§0 必须填"。**教训**：涉及"要不要为某坑动工"的判断，必须逐行核 CR 实际状态标记，不能信可能滞后的汇总段 —— summary 会滞后于 burndown。已当场更正 + 修 register 防复发。**这条请你留意我以后别再犯。**
  ② **8.11 实况 = 可封版**：0 open 🔴；剩 ~19 MED + ~63 LOW 全 🅱 友好性/硬化、foundation 期正确 defer；唯一成群实质待办 = 导出族（待 export 管线）。**burndown 战役实际已完成，不是"还差红线"。**
  ③ **收口是 Codex 的活**（我不碰源/commit）：handoff 写清三条（干净构建 / git add 承重测试 / 验证门）。收口后我核"干净构建证据 + 测试已 tracked + 范围完整"，核过即封版转前方（目的层 / Source 契约接缝）。

## 2 — Source 重建：现状 + 三层契约 + 工具选型 + 纠错 UX（落文档）

- **做了什么**：两轮 map（现状保真度 wtleazfsi + 工具调研 w8z3ebz27 + 手写补查 af5c8f9b）+ 读回 06-28 三层源重建愿景 + PI-048 调研 Outline，落成 `docs/agent-ops/analysis/source-reconstruction-design-and-tooling.md`。Henry 全程拍板。
- **现状实锤（4 agent 实读代码）**：现有 Source = **漂亮关系骨架 + 初级扁平文字内核**。documentParser 把一切压成一个字符串（无 typed block/bbox/LaTeX/表格；OCR prompt 写死"只要文字"）；snapshot 是**从 chunk 派生的投影的投影**（假页码、可变、当不了冻结收据 —— 坐实 Henry"snapshot 做得不好"）；锚点只到页（offset 列是 Potemkin/永远 NULL）；06-28 的 KEY TRAP 落成 bug（层2锚靶=层3 chunk 文字）。**复用有利**：假保真表全派生可重建，不可替代数据（原始字节/note_block_sources/curated 库/anchor-link 模型）都在稳定 KEEP 表。
- **方向（拍板）**：落 06-28 三层 + **契约优先、解析器可换**。定 `source_artifact/block` 契约（typed block+bbox+阅读序+kind+LaTeX+table+confidence+provider，不可变版本化）+ `SourceParser` 接口，**今天的糙解析当 degenerate `native` 默认后端**（零破坏、零数据迁移），锚改指 block。**双闸门**：先立契约、后建管线。
- **工具选型（联网查证 2026-07）**：**PaddleOCR-VL 1.6 主（本地默认）+ MinerU 2.5 备**，都中国实验室出品、开源、中文霸榜（OmniDocBench PaddleOCR-VL 96.33 > MinerU 90.67，且公式/表格均超）；**推翻 06-28"MinerU 默认"旧假设**。Docling 踢掉（中文实验性）；Mistral OCR 4 移出默认路径、只当可选逃生口（闭源+云）。硬件：Henry 的 4060/8GB 够（PaddleOCR-VL 0.9B 很稳）。
- **承重启示**：手写中文是**所有工具的命门（~86%）** → 抽不干净是必然 → **`confidence` 字段承重、UX 要让「改」极顺手**。Source 做得好不好不看"抽多准"，看"抽错改多快" —— 扣上抗幻觉（无锚/低置信送审）+ 红线（Agent 能编人能编）。
- **判断点（供 Henry 复核）**：
  ① **总闸已开**：Source 从"混沌的总闸"变成"有证据、有方向、有工具的车道"。现状初级是坏消息，但**复用有利 + 契约接缝便宜**是好消息。
  ② **修订了先后**：现有 Source 基座太糙、接不了真原文快照 → **member 快照(U3) 的前置 = 先立 Source 契约接缝**；但**目的层(U1/U2) 仍第一、仍不被 Source 挡**。已回写构建分解 §6.5。
  ③ **多 agent 联网调研兑现价值**：4 路查出 PaddleOCR-VL 反超 MinerU（06-28 靠的是当时信息）—— 这种"事实随时间变、要重新查证"的正是不能凭记忆的地方。一路（手写/专项）StructuredOutput 撞重试上限掉了，单独补跑 free-form 补齐。
  ④ **下一步**：建真 PaddleOCR-VL 管线前要跑 PI-048 spike（拿 Henry 真手写中文数学 + 印刷 STEM 验保真 + 校准置信阈值）。契约接缝本身不依赖这个 spike，可先立。
