> **状态 (Status)**: active(**C1 实跑完成 2026-08-28**;C0 基线缺席待补、C2 未跑;评分口径冻结于实跑之前)
> **层 (Layer)**: 分析 / Analysis(V12.9a 选品初赛 · 第 1 场)
> **日期 (Updated)**: 2026-08-28
> **权威 (Authoritative)**: 否(证据归档;终审归 Henry)

# V12.9a 第 1 场:转写器「回程票」赛

## 0. 纪律(先写,免得跑完再对齐)

- ⛔ **不宣布冠军** —— 终审归 Henry。本单只归档:**每候选 × 每卷的原始输出 + 打分表 + 耗时**。
- ⛔ **Exp 类不可钉版模型不入正选**(本场无模型调用,记此以对齐总纪律)。
- ⭐ **评分口径冻结于实跑之前**(§2)。理由同本项目一贯:**口径若写在看见产物之后,评分会不自觉地对齐已发生的事**。
- **本场零 API 花费**(纯本地转写器);花费记账从第 2 场起。

## 1. 考卷与候选

**考卷**(`D:/Coinsides/v12.9-selection/samples/`,官方公开样题,4 份):
1. `ielts-listening-sample-tasks-2023.pdf`
2. `ielts-academic-reading-sample-tasks-2023.pdf`
3. `ielts-academic-writing-sample-tasks-2023.pdf`
4. `ielts-academic-writing-example-responses-to-parts-1-and-2-with-band-scores-and-examiner-comments.pdf`

**候选**:
| # | 候选 | 形态 | 备注 |
|---|---|---|---|
| **C0** | **现役**:`pdf-parse`(+ `pdf-lib` 取页数) | 仓内已接线(`server/src/services/documentParser.ts:4-6`) | **基线** —— 它是「升级目标」的对照物,不是外来候选 |
| **C1** | **Docling** | uv 隔离工具箱 | 版本锁死,装 `D:/Coinsides/v12.9-selection/tools/` |
| **C2** | **MinerU** | uv 隔离工具箱 | 同上 |

⚠️ **本场是「数字原生 PDF」战场**(§3 总表序 2 的前半);扫描/图片族(序 1)归第 2 场识别器赛。

## 2. ⭐ 评分口径(冻结于实跑之前)

每候选 × 每卷,五维各打 **2 / 1 / 0**:

| 维 | 2 分 | 1 分 | 0 分 |
|---|---|---|---|
| **A · 锚粒度**(回程票精度)⭐ **按碎片流规格 v0 §3 的 fidelity 阶梯 `region > block > page` 计** | 产物自带 **region**(bbox,可归一化)或 **block** 级定位 | 只到 **page** 级 | 无任何定位信息 |
| **B · 结构角色** | 标题/列表/段落等**角色显式标注**(可直接填「结构角色」字段) | 靠版式启发式能猜(如 markdown `#`) | 纯文本流,角色全丢 |
| **C · 阅读顺序** | 多栏/图文混排下顺序正确 | 单栏正确、多栏可疑 | 顺序明显错乱 |
| **D · 表格** | 输出**单元格结构**(行列可寻址) | 表格被转成文本但**边界可辨** | 表格塌成散文 |
| **E · 产物可用性** | 产物是**结构化数据**(JSON 类),可直接喂碎片流 | 只有 markdown/文本,需二次解析 | 需要人工整理 |

**耗时**:每卷 wall-clock 秒数,单独记(**不计分**,只作运营参考)。

⚠️ **两条防自欺**:
1. **⛔ 空对空不算数** —— 若某卷根本没有表格,该卷 **D 维记「N/A」不记 0**(0 是「有表格但塌了」)。
2. ⭐ **A 维必须验回程票是否真能用**:不是「产物里有数字」就算 2,而是**拿那个定位回到原 PDF 能指到同一处**;做不到就降级并写明。

### ⚠️ 口径修订一处(2026-08-28,**实跑之前**,如实记)

**A 维原写「块/行/bbox/字符区间」是我自造的词**;Fable 的碎片流规格 v0(`plans/v12-9b-anchored-fragment-stream-spec.md` §3)已定**封闭的五族锚与 fidelity 阶梯** ⇒ **A 维改用规格原词 `region > block > page`**,使本场证据可直接充当该规格翻 active 的修订原料(Fable 2026-08-28 点名要求)。
⭐ **修订发生在任何候选跑出产物之前**,⛔ 不是看见结果后调口径;记此以备核。

📌 **另加一列**:每候选**能诚实申报的最高 fidelity**(规格 §3「**低保真合法,伪造高保真违法**——没有 bbox 就不写 bbox」)。

## 3. 实测记录

**跑期**:2026-08-28 01:1x–01:4x(本机 -0400)。**本场零 API 花费**(纯本地)。
**产物落**:`D:/Coinsides/v12.9-selection/tools/_out/`(仓外,不入库);每卷一份 `*.fragments.json`(按碎片流形状记 `seq/role/text/anchor`)+ `*.md` + `_summary.json`。

### 3.0 ⚠️ C0 基线缺席 —— **基线自己没过阳性对照**

**要拿「现役 pdf 解析」当基线,结果基线跑不起来**:`pdf-parse@2.4.5` 是**类式 API、无 `default` 导出**,而 `documentParser.ts:5` 用 `(mod as any).default || mod` 取值后当函数调 ⇒ **必抛 `pdfParse is not a function`**;`:329` 的 catch 把它吞掉、落视觉兜底并记 `parseChannel='ocr'`。**该文件零测试覆盖。**

⇒ **本场 C0 记「不可用,基线缺席」**。⛔ **不拿它的数字充基线** —— 那会是**一个没在工作的通道的成绩**。
📌 已升 Fable,裁「今夜修」,修单 `handoffs/2026-08-28-pdf-parse-v2-adapter.md`;**修完补跑 C0 并回填本节**(序 2 的比试届时才有基线)。
⭐ **本场的第一笔真实回报不是砍到候选,是砍出了自家地基里的死通道** —— 「基线自己要先过阳性对照」这条家法在开赛前救了整场比试的数据有效性。

### 3.1 C1 · Docling `2.123.0`

| 卷 | 耗时(s) | 碎片数 | 带 bbox | bbox 覆盖率 | table 项 | 页数 |
|---|---:|---:|---:|---:|---:|---:|
| academic-reading | **112.37** | 351 | 351 | **1.000** | 3 | 46 |
| writing-example-responses | **12.45** | 63 | 63 | **1.000** | 1 | 5 |
| academic-writing | **73.29** | 154 | 154 | **1.000** | 0 | 26 |
| listening | **40.71** | 306 | 306 | **1.000** | — | — |

**结构角色**(它自己吐的标签,非我归纳):`section_header` · `text` · `list_item` · `table` · `picture` · `document_index` · `footnote`。

#### ⭐ 回程票实证(A 维的硬要求:不是「有数字」,是「回得去」)

**方法**:取每卷前 40 条「文本 ≥25 字且带 bbox」的碎片,用**独立提取器** `pypdfium2` 按该 bbox 裁剪原 PDF 取文本,检查碎片文本前 40 字是否命中。
⚠️ **坐标系已核**:bbox 为 `[l, t, r, b]` 且 `t > b` ⇒ **BOTTOMLEFT 原点**(PDF 惯例),裁剪时取 `bottom=min(t,b)` / `top=max(t,b)`,各留 1pt 余量。

| 轮次 | 结果 |
|---|---|
| **第一轮(严格字面比对)** | **122/159(77%)** |
| ⚠️ **抽查 4 条 miss 后判定:全部是标点归一差异,不是定位错误** | `-` vs `–`(en dash)· `'` vs `’`(弯引号)—— **bbox 指的位置完全正确** |
| **第二轮(标点/NFKC 归一后)** | ⭐ **155/159(97%)** |

⇒ **A 维的证据是 97%,不是 77%**。**第一轮的 23% 差额是我的比对方法过严造成的,不是 Docling 的锚不准** —— 若我不抽查就下结论,会把方法的限制说成产品的结论。

#### ⭐⭐ 给碎片流规格 v0 的一条修订原料(Fable 点名要的)

**Docling 会归一化标点**(`–`→`-`、`’`→`'` 等)⇒ 它产出的 `text` **不是原件逐字**。
⚠️ 而规格 v0 §2 不变式 ③ 写的是「**`text` 与原件逐字**」,§5 的 **K-1 逐字保真**(碎片拼接 vs 独立抽取全文逐字节比对)⇒ **按现行规格,Docling 会直接违反不变式 ③、K-1 必红**。

**这不是 Docling 的缺陷**(归一化对下游语义任务通常有利),**是规格与现实转写器之间的一处未预料到的冲突**。三条可能的出路(⛔ 我不选,交 Fable):
1. 不变式 ③ 放宽为「**逐字或申报的归一化**」,并在**出生证**里加一个 `text_normalization` 字段(与 `anchor_fidelity` 同族:**低保真合法,不申报违法**);
2. 要求转写器**同时**给出原始 glyph 文本(Docling 是否能给,**未核**);
3. 维持逐字要求 ⇒ **Docling 出局**。
📌 我倾向 ①(它与规格既有的「fidelity 阶梯 + 出生证申报」是同一形状),**但这是设计裁定,不归我**。

### 3.2 C2 · MinerU —— **本轮未跑**

按夜班令「时间不够按此序砍尾」,且 C0 修单与本场同树串行占用了时段。**如实记「未跑」,⛔ 不以 Docling 单方数据代替比试结论。**
**承接**:MinerU 与 C0 补跑同批做完,才构成序 2 的完整比试。

---

## 4. 打分表

⚠️ **只填已实跑的候选**;⛔ 不给未跑的候选打分,⛔ 不宣布冠军(终审归 Henry)。

| 维 | C0 现役 | **C1 Docling** | C2 MinerU |
|---|---|---|---|
| **A · 锚粒度**(`region>block>page`) | **N/A(不可用)** | **2** —— 100% 碎片带 bbox;回程票 **155/159(97%)** 经独立提取器实证 | 未跑 |
| **B · 结构角色** | N/A | **2** —— 显式标签(`section_header`/`list_item`/`table`/`picture`/`footnote`/`document_index`) | 未跑 |
| **C · 阅读顺序** | N/A | **1** —— ⚠️ **未做多栏专项验证**;四卷 `seq` 顺序读上去合理,但**没有针对多栏/图文混排的对照实验** ⇒ **按「未取证不给 2」记 1** | 未跑 |
| **D · 表格** | N/A | **1** —— 表格被识别为 `table` 项(reading 3 个 / writing-responses 1 个),**但本轮未验单元格是否可寻址**;academic-writing 卷 **0 个表格 ⇒ 该卷记 N/A 不记 0** | 未跑 |
| **E · 产物可用性** | N/A | **2** —— `iterate_items()` 直出结构化对象(role + text + prov{page,bbox,charspan}),**可直接喂碎片流,无需二次解析散文** | 未跑 |
| **诚实可申报的最高 fidelity** | — | **`region`**(有真 bbox,且回程票验过)⚠️ 但 `text` 非逐字,见 §3.1 的规格冲突 | — |

**耗时**(不计分):Docling 12.45s–112.37s/卷,**与页数正相关**(5 页 12s / 46 页 112s);首跑另有模型下载开销(未计入)。

---

## 5. 我自己的测量错误

1. ⭐ **差点把方法的限制报成产品的结论**:回程票第一轮 **77%**,若就此记入,Docling 的 A 维会被低估、甚至可能因此出局。**抽查 4 条 miss 后发现全是标点归一**(`–`/`’`),归一后 **97%**。
   **教训**:**「命中率不到 100%」有两种含义 —— 定位错了,或比对器太严**;和「刀不红有两种含义」「零命中有两种含义」是同一族。⛔ 不抽查就下结论 = 用自己的比对器给候选定罪。
2. ⚠️ **C 维与 D 维我给的是 1,不是 2 —— 因为我没做那两项专项验证**。产物看上去顺序合理、表格有识别,但**「看上去合理」不是证据**。按本单 §2 冻结的口径,**未取证就不给满分**,而不是「大概没问题给 2」。
3. **C0 基线缺席这件事,是我按「基线先过阳性对照」的习惯撞出来的,不是设计好的检查** —— 我原本只是想跑一下基线拿数字。**这次是运气与习惯的合力**;若基线是个「跑得起来但结果错」的通道,我这套流程**未必能发现**。记此为本场方法的已知盲区。
