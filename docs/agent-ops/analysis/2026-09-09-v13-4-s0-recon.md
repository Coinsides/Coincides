> **状态 (Status)**: active（13.4 单 0 只读侦察完成）
> **层 (Layer)**: 分析 / 侦察
> **日期 (Updated)**: 2026-09-09
> **权威 (Authoritative)**: 否；现物证据供拆单，方案候选不代替 HQ 裁定
> **任务来源**: 本会话用户直令；`docs/agent-ops/handoffs/2026-09-09-v13-4-s0-recon-order.md`
> **基线**: `fable/v2-bn12-exoskeleton`，HEAD `33604d2bfa5e7f3c282ba674b216d10d4ed0b8e8`；行号取自本次工作树

# V13.4 单 0 · 投影全谱与 item 化侦察

## 取证口径

按开工时段 plan 单 0 六题执行。收尾时 `docs/agent-ops/handoffs/plans/v13-4-projection-itemization-plan.md:1` 已被并行更新为“修订一商量中草案”，原六题保留在 `:14`，甲部单 1～5 现位于 `:25`、`:29`、`:33`、`:37`、`:41`，原 text_range 闭环仍在 `:31`。新增补问 `:23` 仅作末尾现物附录，乙部设计不视为冻结、也不据此派新单。上游总计划 `docs/agent-ops/handoffs/plans/v13-foundation-rebuild-plan.md:49` 及图二 `docs/agent-ops/analysis/2026-09-07-board-data-model-design.md:56` 均区分引用投影与内容真相。停车场 H 区准备区、板工具和铺陈债见 `docs/agent-ops/handoffs/plans/parking-lot.md:85`。

已核入口、方向宪章、current-state 与 active ADR 的状态头及相关正文。current-state 主入口仍记 V12 前沿（`docs/agent-ops/current-state/README.md:35`），本次 V13 射程取用户指定的段 plan 六题，现物结论以代码为准；不把旧概述或旧工单的 ready 字样当成尚未实现的证据。除本报告及新建工单外不写文件；不处理其它 ready 工单。收尾观察到段 plan 与当日 claude-log 出现非本会话写入的 tracked 变动，未回改。

证据为静态源码/DDL/调用链，**没有查询或打开任何用户数据库，也没有实跑迁移、API、浏览器或产品测试**。表存在指建表代码现物，不是用户库普查结论。仓根有 `.codegraph/`，已优先尝试 CodeGraph，但无可调用 MCP，`codegraph` CLI 也不在 PATH；`rg` 同样不可用，改用限定目录的 PowerShell 检索与 UTF-8 行号读取。开工已有未跟踪文件，未读取或改动其内容。

## 一 · 047 item 域现物、board item 开闸与身份桥接

**结论：047 地基和组内 item 身份链已有；板的 item resolver 不是全缺，而是只有状态解析。新写、卡面正文、导航、刷新及板自身的 item 身份桥接仍缺。**

### 1. 表与服务

| 现物 | 证据与含义 |
|---|---|
| Item | `server/src/db/migrations/047_v2_item_relation_floor.ts:10`：正文 JSON/plain text、自由文本 type/topic、active/retired、后继和 origin note/course。 |
| Snapshot | `server/src/db/migrations/047_v2_item_relation_floor.ts:37`：按 item/content hash 去重，保存判断时内容。 |
| Anchor 与待认领 pool | `server/src/db/migrations/047_v2_item_relation_floor.ts:51`、`:70`：item 与 CG pool 二选一，保存 target/range/excerpt/source record。 |
| Item 语义关系 | `server/src/db/migrations/047_v2_item_relation_floor.ts:86`、`:104`、`:122`：两端 Item、端点快照、方向、出处目的、撤销态和 assessment。 |
| CG 的 item membership | `server/src/db/migrations/047_v2_item_relation_floor.ts:145`、`:154`、`:168`：真实 `item_id` FK；item kind 要求 item_id 非空、target_id 空，其余 kind 的 item_id 必空。 |
| 板成员与板连线 | `server/src/db/migrations/057_v13_boards.ts:26`、`:46`：members 留四 kind；edges 指成员实例，属于视觉连接，不能自动等同 Item relations。 |

`server/src/services/items.ts:345`、`:350`、`:354`、`:402`、`:466` 已有 create/get/list/update/retire；hydrate 带当前正文、快照和 anchors（`:275`），正文编辑确保快照（`:437`）。`castItem` 在事务中认领同 CG pool anchors（`:648`、`:673`、`:681`、`:687`），**不顺带写 CG membership**。HTTP 门已在 `server/src/routes/items.ts:60`、`:70`、`:90`、`:94`、`:104`。

`server/src/services/relations.ts:388`、`:495`、`:512`、`:541` 已有创建、撤销、重新确认与 assessment 读取；`:241` 读时比较当前正文与判断快照哈希派生 freshness。按 item 的查询在 `:464`；按 purpose 的 `:475` 调用 `server/src/services/purposes.ts:182`，后者明确 410 deferred。**047 已落地不等于魂材料集编译已经完成**，13.4 不应顺带复活维护式 purpose_members。

### 2. ContentGroup → Item 的真实消费链

| 环节 | 证据 | 当前结果 |
|---|---|---|
| 铸卡后显式挂组 | `client/src/pages/Notes/canvasEngine/panels/ContentGroupPanel.tsx:496`、`:509`、`:520` | attach 后保存 CG，失败保留 unlinkedItem 供重试。 |
| 只挂身份 | `client/src/pages/Notes/canvasEngine/contentGroupService.ts:587` | item_id 有值；target_id/current_content/preview_text/label 均 null，不复制正文。 |
| 保存/读回成员 | `server/src/services/contentGroups.ts:109`、`:155`、`:182`、`:284`、`:302` | 接受 item kind 并存读 item_id；list 成员只读 membership，没有 join Item 正文。 |
| 笔记 Item workbench | `client/src/pages/Notes/canvasEngine/panels/ContentGroupPanel.tsx:356`、`:1216`、`:1225` | 逐 id loadItem，卡面使用当前 plain_text；请求门 `client/src/pages/Notes/canvasEngine/itemRepository.ts:47`，活挂点 `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:4086`。 |
| Gallery | `client/src/pages/GroupGallery/groupGalleryData.ts:63`、`:71`、`:110` | 预览只读 membership 旧文本字段；加载组、笔记、文件夹，没有 item 解引用。 |
| 板候选的 CG 摘要 | `client/src/pages/Boards/boardRepository.ts:110` | 同样拼旧文本字段，没有 item_id 解引用。 |

因此标准 item 成员在 workbench 有正文，在 Gallery/板候选可变成空预览。CG identity.summary 即使能兜底显示，也不是跟随 item 正文变化的证据。

**最小接线候选**：保留混合 membership，抽共享 Item 摘要/导航读侧 adapter，按 item id 去重解引用供 Gallery 和板使用；避免重复逐卡请求。不要把正文写回 membership，不把旧 block/annotation/content_range 成员全部重铸。若新增批量读取端点，其返回 DTO 应服务这两个实际消费者。

### 3. 板 item 开闸的具体缺口

| 位点 | 现物与缺口 |
|---|---|
| resolver | `server/src/services/boards.ts:149` 已检查同用户 Item 的 active/retired/missing；但只 SELECT status，`:134` 的 title/note_id 恒空，没有当前正文、摘要或导航。 |
| 新写闸 | `server/src/validators/boards.ts:45` 只允许 note/content_group；client `client/src/pages/Boards/boardTypes.ts:32` 同样两项。mount 要先过 schema，再 resolver（`server/src/services/boards.ts:231`、`:248`）。数据库 CHECK 已留位，不是再造 member kind。 |
| 候选/卡面 | `client/src/pages/Boards/boardRepository.ts:90` 只生成 note/CG；`client/src/pages/Boards/BoardPage.tsx:402`、`:413` 依 candidate summary 显示。补 server 开闸不等于用户能找到/读到 item。 |
| 导航 | `client/src/pages/Boards/BoardPage.tsx:129` 双击只认 reference.note_id。合法无 origin note 的 item 需要真实 Item 目的地；origin note 是出处，不能冒充 item 的唯一编辑归属。 |
| 刷新 | `server/src/services/boards.ts:175`、`:211` 在 GET 时重解析；`client/src/pages/Boards/useBoard.ts:106` 只订阅 board change，`client/src/pages/Notes/canvasEngine/itemRepository.ts:87`、`:92` 的 update/retire 没有此通知。需明确返回板、重新聚焦或源变化时何时重读，并同步刷新 candidate 摘要。 |
| 事件 | `server/src/routes/boards.ts:119`、`:128`、`:154` 已通用处理 kind/id 与 mounted/unmounted 事务事件，可续用；无须另造 Item 上板事件或修改名单。 |

### 4. boards 的 item 族身份候选

图二 `docs/agent-ops/analysis/2026-09-07-board-data-model-design.md:15` 裁板为一等实体、身份挂 item 族；裁定甲附账 `docs/agent-ops/handoffs/2026-09-08-v13-3-s1-data-layer-order.md:25` 明确留到 13.4。现物 `server/src/db/migrations/057_v13_boards.ts:9` 没有 item FK；`server/src/services/boards.ts:185`、`:222` 仅写 boards。

| 候选（非裁定） | 收益 | 代价/边界 |
|---|---|---|
| A，优先建议：boards.item_id UNIQUE FK，或等价一对一桥表 | 保留 board id，渐进接 CG/Gallery/Item resolver，改动集中。 | 需新迁移、存量身份建立、开板同事务建身份；须定标题/正文来源、普通 Item 编辑及 retire 边界、导航。 |
| B：boards 与 items 共主键，boards 作结构扩展 | 身份直接，没有双 id 映射。 | 现有 `server/src/services/items.ts:317` 内部生成 UUID，需受控 identity 创建；既有 FK/迁移核对面较大；仍须解决内容与生命周期。 |
| C：读侧虚拟 Item，把 boards 合并进列表 | 最少存储改动，可先验证列表呈现。 | 无真实 items 行，不能承接 CG item_id / relations FK；不能单独宣告完成“身份挂 item 族”。 |

**前置裁定不能省**：`server/src/services/items.ts:169`、`:182`、`:190` 要求非空正文，并可生成 TextFlow；普通 Item 可 edit/retire（`:402`、`:466`）。简单 `createItem(board.title)` 会引入另一份可编辑标题/正文及板身份被普通退役的问题。应先冻结“Item 身份如何表示 Board、哪处拥有正文/标题、生命周期怎么映射”，再选择 A/B。不得借桥接把弱 project 标签变成容器所有权，现有保留策略见 `server/src/services/courseLifecyclePolicies.ts:25`、`:46`。

## 二 · text_range 锚体系现物与契约缺口

**结论：优先复用 AnnotationRange 的地址与重映射内核；目前没有一套可直接完整承担段终点。source_anchors 是来源快照锚，selection receipt 是临时选区快照；二者都不能冒充笔记活文本锚。**

### 1. 三套载体的能力对照

| 载体 | 现物与证据 | 可复用能力 | 不能直接承担的部分 |
|---|---|---|---|
| AnnotationRange（036） | `server/src/db/migrations/036_v2_annotation_truths.ts:62`、`:65`、`:67`：annotation_id FK CASCADE，note/block/flow/unit/offset/cache/metadata。 | 持久地址、选中文字快照、同 unit 编辑重映射、人工复核展示。 | 归 annotation 所有，不是 board 独立锚；结构编辑、删除与双资源保存未闭合。 |
| source_anchors（020） | `server/src/db/migrations/020_v2_source_anchors.ts:12`：必须 source_snapshot_id，可带 page/document/chunk/fragment/segment/offset；没有用户笔记 flow/unit 地址。 | 导入来源追溯与回跳。 | `server/src/services/sourceAnchors.ts:180` 新建 offsets 为 NULL，`:419` refresh 仅重找 snapshot page；不消费笔记编辑。 |
| TextFlow 身份 + selection receipt | `server/src/services/textFlowIdentity.ts:1` 的 textflow-blockId；`client/src/pages/Notes/canvasEngine/runtimeDataTypes.ts:70` 的 unit 身份；`shared/types/selectionReceipt.ts:13` 的 frame-free 选区快照。 | 捕获与传递身份三元组、局部偏移、excerpt、时刻；部分漂移诊断。 | 不是 durable range 实体，也没有编辑后 offset 更新。 |

source_anchors 的实际生产者从 note_block_sources/evidence_items 找 snapshot/page（`server/src/services/sourceAnchors.ts:326`），回跳返回 snapshot/page（`:396`），不应为用户笔记锚另加一套来源所有权。

`server/src/services/selectionResolve.ts:69` 核验 flow、active block/unit，按当前 offset 切片对旧 excerpt，输出 found/text_drifted/missing；`:90` 只返回诊断，不回传当前正文、不更新锚，也不使用 receipt 的 note_id/refs/at 做版本判定。**能报漂移，不等于能跟变**。投影生产见 `client/src/pages/Notes/canvasEngine/selectionReceiptProjection.ts:18`。

### 2. AnnotationRange 的活链与已有降级

选区不是只有模型定义：`client/src/pages/Notes/canvasEngine/blocks/TextBlockProjection.tsx:636` 捕获 textarea 的 unit 偏移/全文/三元组 → `client/src/pages/Notes/canvasEngine/selectionRangeService.ts:58` 裁选中文字 → `client/src/pages/Notes/canvasEngine/annotationTruthService.ts:69` 生成 range ID 与 cache；多段组装在 `client/src/pages/Notes/canvasEngine/selectionDraftService.ts:164`。

普通编辑链：`client/src/pages/Notes/canvasEngine/blocks/TextBlockProjection.tsx:605`、`:625` 发布 TextFlow/plain text，普通输入保留 unit ID → `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:3818`、`:2241` → `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasRuntimeController.ts:239` → `client/src/pages/Notes/canvasEngine/hooks/useBlockTextFlowEditController.ts:62` 比较同 ID unit，重映射后在 `:77` 保存 annotation。

`client/src/pages/Notes/canvasEngine/rangeRebaseService.ts:38` 以共同前后缀推导单次替换区；`:139` 至 `:179` 更新精确替换、范围内替换及前后位移。无法安全重映射的交叠在 `:182` **保留 range/cache、清 offsets、存 pre_edit_offsets**，不是删除锚。`client/src/pages/Notes/canvasEngine/annotationDisplayService.ts:51` 把 pending/invalid/缺偏移视为不可渲染；`client/src/pages/Notes/canvasEngine/panels/AnnotationInspectorPanel.tsx:59`、`:293` 留快照并显示 Needs review。该显示仅属于 annotation 面，板卡尚未消费。

草稿 owner 转持久身份的桥也已有：`client/src/pages/Notes/canvasEngine/annotationTruthService.ts:28`，活接线 `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:934`。这只解决 owner reconciliation，不能算通用 split/merge 历史。

### 3. 不能漏掉的五个缺口

1. **结构编辑**：`client/src/pages/Notes/canvasEngine/textUnitEditorService.ts:155` split 后半生成新 unit ID，`:184` merge 保留前 unit、移除当前 unit；活快捷键在 `client/src/pages/Notes/canvasEngine/blocks/TextBlockProjection.tsx:799`。但 `client/src/pages/Notes/canvasEngine/hooks/useBlockTextFlowEditController.ts:62` 找不到 nextUnit 就跳过，没有转移后半锚、合并偏移或统一失效回执。
2. **服务端复放精度**：`server/src/services/annotationTruths.ts:245` 只核 block active 和 block.plain_text 总长度，未核 flow/unit 身份、unit 局部边界或 excerpt；缺偏移也不在这里自动派 pending。不能可靠诊断同长度替换或 unit 删除。board resolver 不能直接把这些 metadata 当完整有效性结论。
3. **正文/锚双提交**：`client/src/pages/Notes/canvasEngine/annotationTruthRepository.ts:107` 独立 PUT annotation；`server/src/routes/noteBlocks.ts:81`、`:114` 独立更新正文。范围编辑在 `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:2232` 也是正文后另存 annotation。`docs/agent-ops/current-state/tech-debt.md:17` 的 TD-6 已登记 durable 耦合/OCC 缺口；本次不宣告清偿。
4. **锚所有权/删除**：`server/src/services/annotationTruths.ts:387` 全 note replacement 先删 annotation_truths 再插入，036 的 ranges 随父 CASCADE。board 若直接 CASCADE FK 到 range，会被此替换协议影响；逻辑引用也须处理 annotation/range/note 删除后留快照。不得把 annotation 删除偷偷等同下板，也不能为上板暗中制造可见批注。独立 range 表或扩现有 owner 都是需裁定的持久化方案；复用算法不等于可以无条件复用表。
5. **空范围与状态语义**：`client/src/pages/Notes/canvasEngine/rangeRebaseService.ts:139` 精确删除可变零宽，`client/src/pages/Notes/canvasEngine/annotationDisplayService.ts:58` 只核偏移是数字；应明确零宽按失效还是其它状态。`server/src/services/annotationTruths.ts:444` 保留 range/cache、缺偏移存 NULL，`:65`、`:460` 去除派生状态，不可把读时状态误记成已持久化状态。

### 4. text_range 上板最小链候选

先冻结 durable range 的 owner/ID、删除语义及**不可被失效覆盖的最后有效快照**。复用选区捕获和 AnnotationRange 地址/算法，保存锚+快照；为所有获准写正文的路径补同步重映射或显式失效，不能只补 textarea。board resolver 读取当前 active unit，返回当前投影文本、明确状态及原因；板面独立显示“原文已变动/丢失”和旧快照。已能确定重映射成功时才显示跟变；仅发现 text_drifted 不能当同步成功。

目前 `server/src/services/boards.ts:135` 对 text_range 一律 reserved/unavailable，写门见 `server/src/validators/boards.ts:45`。**待锚语义、服务读取与卡面降级齐备后再开放该 kind**，不要拿 block ID 冒充 range ID。沿现有 `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:3969` 选区工具栏接上板动作，复用板选择/挂载入口；同步补返回板/重开时的刷新，否则后端能复放、板面仍会旧。

## 三 · 准备区重排、拖入与正名

**结论：已有排序字段、按钮移入和拖回纸；缺全盘重排写入口与拖入命中链。准备区混有块、画物与挂载，不能只按块估量。**

| 现物/挂点 | 文件:行号证据 |
|---|---|
| order_index 已有，无须为字段另开迁移 | `server/src/db/migrations/055_v13_tray_order.ts:4`。 |
| 读侧排序与混合 entries | `client/src/pages/Notes/canvasEngine/trayService.ts:14` 按 orderIndex/placementId 排，空 order 在后；`:22` 区分 block/object/mount 和 boardKind。 |
| 当前侧栏手势 | `client/src/pages/Notes/canvasEngine/layers/NoteTraySidebar.tsx:20`、`:31` 只有 block draggable 与 placementId payload；aside/list/item 无重排 drop。 |
| 按钮移入 | `client/src/pages/Notes/canvasEngine/hooks/useTrayController.ts:82` 取 selectedBlock，max(order)+1，零几何写 tray；`:73`、`:101` 保存/refresh/清选择并提供 before/after undo，动作当前只认 block。 |
| 块 order 写通路 | `client/src/pages/Notes/canvasEngine/canvasObjectRepository.ts:124` → `server/src/routes/canvasObjects.ts:63` → `server/src/validators/index.ts:728`、`:1088` → `server/src/services/canvasObjects.ts:1996`、`:1911`、`:552`。order_index 确可持久，不是只在类型上存在。 |
| 全盘写缺口 | `server/src/validators/index.ts:1093` generic union 不含 CG projection；`server/src/services/canvasObjects.ts:1911` 会写 object/extension，并非通用 placement-only order patch。 |
| 拖回纸 | `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:3252` 接 TRAY_DRAG_TYPE、换算布局 → `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasRuntimeController.ts:362` 的 dropOnPaper。 |
| 纸内块拖柄 | `client/src/pages/Notes/canvasEngine/layers/BlockControlBarLayer.tsx:97` → `client/src/pages/Notes/canvasEngine/hooks/useBlockPlacementInteractions.ts:128`，走 pointer 而非 HTML DnD。 |
| 拖入候选缝 | `client/src/pages/Notes/canvasEngine/interactionController.ts:99` 会把最终事件交 onEnd；`client/src/pages/Notes/canvasEngine/hooks/useBlockPlacementInteractions.ts:173` 当前忽略该事件并直接记几何/persist。 |
| 既有内容边界 | `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasRuntimeController.ts:349` 仅 page 且非 contentReadOnly，先 flush 草稿。 |

**盘内重排候选**：Sidebar 插入位置 → useTrayController.reorder → note-scoped 原子 order 更新 → refresh。若要求全部 entries，优先补只写本 note tray placements 排序的小端点，沿用现有归属与只读策略；不要为改 order 全量重写 object extensions。若先复用块 PUT，须明确仅块、逐条部分成功以及遗漏画物/mount 的范围，不能算全盘二件完成。

**拖入候选**：沿 beginMoveBlock 的 onEnd(event) 判准备区目标，命中后清临时布局/吸附并调用接收明确 blockId、拖前 layout 的移入动作，跳过普通纸面几何保存，复用 flush 和撤回。`client/src/pages/Notes/canvasEngine/hooks/useBlockPlacementInteractions.ts:149` 拖中可推挤其它块；必须处理这些临时碰撞，撤回应回拖动前布局。另做独立 HTML 拖柄可行，但要额外处理文本选择、pointer 冲突。取消路径也须清理；现有 `client/src/pages/Notes/canvasEngine/interactionController.ts:110` 只有 pointerup 清理，不能默认已覆盖 pointercancel。

**正名清单（只改可见文案）**：

- `client/src/pages/Notes/canvasEngine/layers/NoteRuntimeDocumentLayer.tsx:91`：Tray (n) 入口。
- `client/src/pages/Notes/canvasEngine/layers/NoteTraySidebar.tsx:20`：标题、aria 名称、关闭、移入按钮、空态；`:28` 的手势说明随新能力更新。
- `client/src/pages/Notes/canvasEngine/hooks/useTrayController.ts:64`、`:80`、`:139`：错误/刷新提示。
- `client/src/pages/Boards/boardRepository.ts:135`、`:143`、`:148`：准备区搬板错误提示。

目标英文 Staging、中文准备区；保留 tray 标识符、surface 值、MIME、data 属性、路由及 operation_batch 标识。`server/src/services/trayNotes.ts:73` 的历史 label 不是重写历史数据的理由。

## 四 · 板工具、橡皮擦与普通撤回栈

**结论：板已有选择/平移/连线/固定画笔与分类删除；没有 eraser 或普通 undo/redo。串行写队列可作栈落点，不能把队列或准备区搬迁回滚当成普通撤回。**

| 现物 | 文件:行号证据 |
|---|---|
| 工具/选择 | `client/src/pages/Boards/BoardPage.tsx:11` 为 select/pan/connect/pen，选择是单个 member/edge/visual；工具 UI 在 `:321`。 |
| 成员几何/连线 | `client/src/pages/Boards/BoardPage.tsx:141` 两卡连接、拖移/resize、pinned 保护；`:181` 按 zoom 换算；`:257` 键盘移动，`:432` pin/scale/z。 |
| 画笔 | `client/src/pages/Boards/BoardPage.tsx:214` 手势末尾一次生成局部 points 的 freehand，tap 补点、取消不落笔；`:227` 保存固定 ink/2.5。 |
| 删除/命中 | `client/src/pages/Boards/BoardPage.tsx:240` 分 member unmount/edge/visual；`:380` 仅 select 时放透明 stroke hit path。`client/src/pages/Boards/Boards.module.css:74` 命中宽度在 world 内，随 zoom 变化；`:77` 颜色/粗细写死，保存的 data.style 尚未由绘制消费。 |
| API/队列 | `client/src/pages/Boards/boardRepository.ts:38` 至 `:65` 有 member patch、edge/visual CRUD；`client/src/pages/Boards/useBoard.ts:43` 读写串行，成功后更新 detail、失败留错可继续，`:97` 隔离换板，`:170` flush；`:181` 返回面没有 history，尚未暴露 updateEdge/updateVisual。 |

**橡皮擦优先候选：整笔擦除。** 为 freehand 复用命中路径、调用 removeVisual；一手势擦多笔可合成一条 command，不先造路径切割引擎。需解决 pointer capture 后扫过多个 stroke 的命中及屏幕命中宽度。默认只覆盖笔迹；搬来的 shape/image/table/connector 与成员引用是否可擦，须单 4 明定，不能把擦笔偷换成下板。

**普通栈候选：在 useBoard 队列内记录成功写入的 before/after 或创建/删除快照。** 一次拖拽只在 end 记一条，键盘几何、scale/z/pin、pen、edge 经同一入口；失败不推进栈，新编辑清 redo，换板隔离，reload 明确清栈或重新核对。已有纸内模式可借：`client/src/pages/Notes/canvasEngine/historyService.ts:5` 的 async boolean undo/redo、`:35` 的编辑控件快捷键避让；`client/src/pages/Notes/canvasEngine/hooks/usePlacementHistory.ts:37` 的 80 条界限及 redo 清空、`:74` 的失败处理。不能直接挂纸内 block 专用栈。

恢复有两个实物缺口：

1. `server/src/validators/boards.ts:57`、`:70` 的 edge/visual create 不接 id，`server/src/services/boards.ts:303`、`:348` 每次新 UUID；`client/src/pages/Boards/useBoard.ts:53`、`:72` 对外仅返回 boolean。undo 删除再 create 会换 ID，需在 queue 内登记返回实体/活 ID，或另定有界 restore/可选 ID 契约；否则 redo 后再擦/删会瞄准旧 ID。
2. `server/src/services/boards.ts:278` 下板会级联相关 edges，client `client/src/pages/Boards/useBoard.ts:136` 同步过滤；若单 4 纳入下板恢复，必须整包保存成员与边并按依赖恢复。member create 可传 id（`server/src/validators/boards.ts:46`），但重挂仍产生 mounted/unmounted（`server/src/routes/boards.ts:151`）。段 plan 普通栈目前点名成员几何/画笔/连线，建议先按此交付，下板恢复单独声明是否包含。

准备区搬板回滚由 server batch 所有（`client/src/pages/Notes/canvasEngine/trayRelocationHistory.ts:4`），`server/src/services/boardTrayRelocation.ts:217` 会比对目标快照。普通编辑会使其冲突，即使改回几何，updated_at/新 ID 仍可能不同；不得为了普通 history 绕过该规则。图二 `docs/agent-ops/analysis/2026-09-07-board-data-model-design.md:93` 明确 client 操作栈不入 events，单 4 不造史记新表或新 verb。

## 五 · 铺陈统揽视图挂点

**结论：页/纸叠/跨页裁片及只读打印渲染均有，可在三档旁加独立统揽展示；当前阅读档位是整张增长纸面的缩放，不是多页网格。不要靠继续缩小 fit-page 实现铺陈。**

| 层 | 现物/挂点 |
|---|---|
| 入口与局部状态 | `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:3888` 三档及步进；`client/src/pages/Notes/canvasEngine/hooks/usePageReadingViewportController.ts:18` 状态按 noteId 隔离。可并排加独立 overview 开关，不必把 grid 塞进既有三档 enum。 |
| 阅读缩放边界 | `client/src/pages/Notes/canvasEngine/pageReadingViewportService.ts:1` 三档；`:8` 步进 0.5～2；`:62` 长页 fit_page 降为 fit_width。`client/src/pages/Notes/canvasEngine/hooks/usePageReadingPresentation.ts:22` 以 primary frame 和增长 contentHeight 求 paperHeight，layoutWidth 独立。 |
| 当前 DOM | `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:3309` 单层 space/paper transform，`:3368` 保持 layoutWidth；`:3383` 多 pageFrame shell 还在 canvas 分支，不能当退役后的 page 统揽入口。 |
| 页/裁片数据 | `client/src/pages/Notes/canvasEngine/engineModel.ts:353` 排除 tray，`:362` 归一 pageFrames/pageStacks，`:375` 派生 blockFragmentProjections。`client/src/pages/Notes/canvasEngine/pageStackBlockFragmentService.ts:64` 与页内容矩形求交，`:72` 带 frame/stack/pageIndex/visibleRect/blockRect；`:92` 横跨多个 stack 时返回空，不能声称任意布局都已有完美缩略图。 |
| 只读页渲染参照 | `client/src/pages/Notes/canvasEngine/layers/NotePrintLayer.tsx:25` 的局部 PrintPages 按 frame、fragment 复用 BlockEditorLayer；`:60` 传草稿，`:66` contentReadOnly，`:82` 起回调 no-op，`:102` 不回写测量。可提炼共享只读页内容组件。 |
| 坐标复用 | `client/src/pages/Notes/canvasEngine/pagePrintProjectionService.ts:25` 将 world visibleRect 减 frame 原点、blockRect 减 visibleRect，已有双向 x/y 裁片定位。统揽网格只缩放结果，不写回 frame、placement、TextFlow 或排版宽度。 |
| 生命周期不能直接复用 | `client/src/pages/Notes/canvasEngine/layers/NotePrintLayer.tsx:127` 只在 print 事件冻结快照，`:157` portal 到 body；`client/src/pages/Notes/canvasEngine/layers/NotePrintLayer.css:2` 屏幕隐藏且不可点击。需复用渲染内核，不能直接显示整个打印根。 |
| 容器挂点 | `client/src/pages/Notes/canvasEngine/layers/NoteRuntimeDocumentLayer.tsx:83` 并列 writing/print；其 `:88` 起包准备区。可在 document 层以独立只读统揽替换/覆盖书写内容，保持准备区/打印边界。 |

**建议单 5 的最小面**：读 runtime 的 frame/stack 次序和裁片，以 CSS grid 容纳 3～4 页，缩略尺寸设有限下限，超出容量翻页；窄视口减少列或分页，不能为塞完全部页不断缩小。列数、最小缩略尺寸、每屏行数、点击目标（选页还是页内对象）由单 5 明定；这些数字尚不是代码现物或本报告裁定。

先做点击某页返回正常阅读位置，必要时保留页选择态；不在小缩略图里再建文字编辑器。**旧选页处理有写副作用**：`client/src/pages/Notes/canvasEngine/hooks/useRuntimePresentationController.ts:207` 会 `onSavePageFrameCollection` 再 focus；统揽如定位为纯展示导航，应拆用局部选页/滚动动作，不直接沿用此持久化处理。打印物理比例 `client/src/pages/Notes/canvasEngine/pagePrintProjectionService.ts:11` 属打印，不应强制所有 web/长页统揽成 A4。

复用范围应如实限定：现有 PrintPages 遍历的是 block fragments，annotations 传空（`client/src/pages/Notes/canvasEngine/layers/NotePrintLayer.tsx:71`），并非所有 generic objects/批注/页槽的完整缩略投影。后续以有这些内容的合成页核对；需要补的呈现列入单 5，不把“共用 BlockEditorLayer”写成“全对象保真已证”。

## 六 · 拆单建议、验证与未做清单

以开工版甲部单 1→2→3→4→5 为拆单基线。以下是各单内部切片和最小改动面建议，不另行发施工单、不代改裁定；与修订一乙部如何穿插，仍候商量冻结。单 3/4/5 数据依赖较弱，但 shared runtime 与 BoardPage 接线仍应由单 builder 顺序整合。

| 单 | 建议内部先后 | 最小改动面 | 后续定向验证（本次均未运行） |
|---|---|---|---|
| 1 item 地基 | 先定 Board–Item 身份/正文/生命周期/导航 → 桥接迁移与创建 → item resolver/写门 → 共享 CG 摘要 → 候选、卡面、刷新。 | `047` 现物保留；新增迁移及 base schema、boards/items service 接缝、boards validator/types/repository、Gallery 读 adapter；必要的 Item 打开入口。 | 隔离新库/升级夹具；同一 item 在 CG/Gallery/板读到当前文本；retired/missing 可见；成员不复制正文；板身份与标题/退役一致。 |
| 2 text_range 主菜 | 先冻结 durable owner/快照/失效 → 普通和结构编辑重映射/诊断 → resolver → 选区上板/卡面/刷新。可分“锚内核”和“用户闭环”两个内部里程碑，后者齐备才开写门。 | 036/AnnotationRange 相关服务与 TextFlow 编辑链、所选 durable owner 的 schema、selection toolbar、boards resolver/DTO/card；避免改 source_anchors 所有权。 | 选区前插入、范围内替换、交叠/完整删除、split/merge、unit/block 删除；重开仍复放；正文成功而锚失败时不会冒报同步；最后一条框选→上板→编辑跟变→失效留快照。 |
| 3 准备区 | 明确混合 entries 范围 → 原子 reorder 写入口 → Sidebar 重排 → pointer 拖入/取消/撤回 → 正名。 | trayService/useTrayController/NoteTraySidebar、placement order 小写门、块拖动 onEnd 与 runtime props、上列文案。 | 混合 entries 重载同序；失败/取消不假排序；未存内容先 flush；拖入撤回回到拖前位置；旧拖回纸、split、搬板保持可用。 |
| 4 板工具 | 先 queue 内 command/活 ID 契约 → 几何/画笔/连线 undo-redo → 整笔擦 → 按实际需要补笔色/粗细及 renderer。 | useBoard、BoardPage、boardRepository 与必要的恢复契约、Board CSS；不接纸专用栈或搬板 batch 栈。 | 几何往返；画→undo→redo→擦→undo 使用正确 ID；连线恢复；失败留栈、换板隔离、输入框快捷键；搬板冲突规则仍有效。 |
| 5 铺陈 | 明确有限缩略尺寸/分页/点击语义 → 共享只读裁片渲染 → 独立 overview 状态/网格 → 返回阅读定位。 | NoteRuntimeDocumentLayer、三档旁入口、只读页投影组件/CSS；复用 engineModel/fragment 几何，不改纸内布局。 | 1/4/多页、末屏、窄屏及长页；跨页裁片/准备区排除；缩放、切换和选择不写 placement/排版；打印不变；标注/画物不足明确。 |

现有后续测试挂点包括：`client/src/pages/Notes/canvasEngine/hooks/useTrayController.test.tsx:22`、`client/src/pages/Notes/canvasEngine/trayService.test.ts:28`；`client/src/pages/Boards/useBoard.test.tsx:55`、`:95`、`:116`、`:166`；`client/src/pages/Boards/BoardPage.smoke.test.tsx:320`；`client/src/pages/Notes/canvasEngine/layers/NotePrintLayer.test.tsx:217`。这里只核有此代码，**未把既有测试当作本次通过的结果**。

交施工单前应明确的四组设计选择：①Board–Item 一对一桥与标题/生命周期；②text_range owner、快照、结构编辑/零宽/双提交失效；③准备区重排是否覆盖全部混合 entries；④板擦除/下板恢复范围和统揽选择粒度。它们在报告中均为候选或待冻结项，不是本次阻塞侦察的审批问题。

**本次验证**：仅新文档 UTF-8、六题覆盖、引用文件存在/行号有效、关键引文语义抽核、工作树范围与空白差异检查。没有使用浏览器或网络；没有运行 `npm run verify:v2-bn8-runtime` 或其它产品门，因为本单是只读侦察且不启动/导入产品运行链。此处不是替后续施工豁免验证门，也不表示运行正确性已证。

**未做**：产品代码/配置/迁移修改；任何用户库读取或写入；读取 `.env` 或打印 key；触碰 3001、启动/停止服务；模型/外部账号调用；产品测试、浏览器体验和主观验收；commit/push/PR/merge；桥接/锚方案裁定与单 1～5 施工；修改旧工单、段计划、current-state、agent 指令或生成索引。

## 附录 · 收尾期间修订一的三项现物补问

本附录响应段 plan `docs/agent-ops/handoffs/plans/v13-4-projection-itemization-plan.md:23` 的补问，仍是只读盘点。修订一标明商量中，以下不冻结弹窗手势、未摆位字段或分层 schema，也不实施/派发乙部单。

**⑦ 笔记运行时可独立挂载，但现成弹窗与多实例隔离未证。** `client/src/pages/Notes/NoteDetail.tsx:6` 从路由取 noteId；`client/src/pages/Notes/canvasEngine/NoteCanvasRuntimeProvider.tsx:9` 可直接传 noteId，提供的 context 只有该字段。`client/scripts/pageReadingSmoke/fixture.tsx:44` 调生产 controller，`:176` 使用生产层，`:186` 以 HashRouter 和显式 noteId Provider 装配；`:170` 自带滚动壳。`client/scripts/pageReadingSmoke/mockApi.ts:96`、`:112` 用 adapter 返回合成数据并拦未映射请求/非允许写入，这是测试装配面，不是生产弹窗。本次 Notes 范围检索未见 location.state 消费。

弹窗改造不能只包 Provider：`client/src/pages/Notes/canvasEngine/layers/FloatingOverlayLayer.tsx:22` portal 到 body；`client/src/pages/Notes/canvasEngine/hooks/usePlacementHistory.ts:30`、`:124` 默认监听 window，`client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx:1142` 也有全局键盘；`client/src/pages/Notes/canvasEngine/NoteCanvasRuntime.tsx:11` 修改共享 body lock。需明确活跃编辑实例、overlay 宿主、滚动/测量边界及卸载；fixture 不能证明多个可编辑笔记弹窗已经隔离。

**⑧ 未摆位态需要显式契约，零几何不能替代状态。** `server/src/db/migrations/057_v13_boards.ts:26`、`server/src/db/schema.sql:1284` 的 members 没有 placed/state，x/y/w/h 可默认 0，scale 默认 1。`server/src/validators/boards.ts:35`、`:54` strict schema 只接既定字段；`server/src/services/boards.ts:254`、`:272` 为 INSERT/PATCH，`:175` hydrate 只转换 pinned/metadata，`:213` 读全部成员。`client/src/pages/Boards/BoardPage.tsx:138`、`:401` 未过滤未摆位成员，`:372` 用成员中心画边。候选改动面是新迁移/base schema（旧成员默认已摆位）、DTO/hydrate、摆位 PATCH、画布/边/空态过滤；不是新建另一份名单。`server/src/routes/boards.ts:123` 仅首次创建记 mounted，`:141` 几何 PATCH 无此事件，可承接“装卸区 mount 一次、摆位只改状态几何”的候选，但本附录不裁定字段形状。

**⑨ z_index 已有，统一层序尚无。** members/visuals 各有 z_index（`server/src/db/migrations/057_v13_boards.ts:36`、`:70`），分别排序（`server/src/services/boards.ts:213`、`:217`）。UI mount max+1 与前后移只比较 members（`client/src/pages/Boards/BoardPage.tsx:354`、`:436`）；搬板保留源 z（`server/src/services/boardTrayRelocation.ts:105`）。实际渲染是 edges/freehand 共一张 SVG、先边后笔（`client/src/pages/Boards/BoardPage.tsx:370`），之后搬入 visual/member DOM 各设 zIndex（`client/src/pages/Boards/BoardRelocatedVisual.tsx:75`、`client/src/pages/Boards/BoardPage.tsx:398`），freehand 没有逐笔 CSS z。因此有 z 字段不代表板上已有跨类型统一排序。限定板 schema/runtime 未见 board_layers/layer_id；分层候选须同时处理默认层、SVG/DOM 层容器、显隐/锁定命中，以及无 z/layer 的 board_edges 跨层规则。

乙部依赖建议仍只到事实：先把甲部单 1/2 的 identity/range/projection 契约定好；弹窗和板装卸区共同消费该契约，不能复用笔记 tray 的“搬家”语义冒充引用提货；图层需统一渲染次序，不能仅补一张表。实际拆单先后由修订一冻结后决定。本附录未运行上述 fixture 或任何服务。
