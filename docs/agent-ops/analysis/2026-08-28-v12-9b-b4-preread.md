> **状态 (Status)**: active(b-4 预读,只读,2026-08-28;与 b-1 并行,不占 builder 锁)
> **层 (Layer)**: 分析 / Analysis(V12.9b 段 · b-4 前置)
> **权威 (Authoritative)**: 否(现物快照)

# b-4 预读:文档面要改哪几行,以及**最该防的那个错**

## ⚠️ 0. 先说风险:b-4 最容易犯的错**不是漏改,是写过头**

b-1 造的是 **`source_imprints` + `imprint_fragments` 两张表和一个校验器** —— **插座**。
⛔ **本段零转写器接入**(明文段目标)⇒ **插头一个都没有**。

⇒ ⚠️ **⛔ 词典的「现状」栏绝不能写成「带锚碎片流已 live」。**
📌 **理由**:词典是**权威文档**(表头写着「词义以本文为准…施工依据只看现状栏」)。**在代码里把形状当能力,是一个 bug;在词典里把形状当能力,是给所有后来者发一份错的地图。**
⇒ **b-4 的正确写法是:「**存储与校验的插座已落库(规格 v0.1 形状),⛔ 尚无真转写器写入(12.9c)**」——** 一句话里同时给出**有什么**和**还没有什么**。

## 1. 词典三条,逐条定位(`current-state/glossary.md`)

| 词条 | 行 | 现状栏原文 | b-1 后要动什么 |
|---|---|---|---|
| **转写(带锚)** | 21–24 | 「live 但原始(平铺文本+页级锚);方向:三层设计 + 带版面坐标」 | **加一句插座已在**,⛔ **不动「现役转写仍是平铺文本+页级锚」这半句** —— 那半句**依然为真**(12.9c 才改) |
| **碎片(可寻址单元)** | 26–28 | ⚠️ **根本没有「现状」栏** —— 只有定义 + 地位 | **新增一条现状栏**(这是 b-4 的真正工作量所在,不是改字是补栏) |
| **引用源(拓印件)** | 30–35 | 「挂在笔记上的源文件+页锚(`sourceFileIntake`/`SourceScope`/`SourceAnchor` 一族),平铺文本,原始但活」 | **加一句出生证表已在**;⛔ **不得改「原始但活」** —— 拓印工序本身仍未实现 |

## 2. 程序表 §2 状态刷新(`plans/v12-9-intake-and-selection-program.md:20`)

现文:**T0 已有** … 「**存量升级目标:对齐带锚碎片流**」。
⇒ b-1 后可改为:该对齐目标的**落库形状已就位**,**⛔ 但 T0 存量的对齐动作本身归 12.9c**(TD-29/TD-30 落点即在彼)。

## 3. 顺带:b-4 该带的两条账

- **TD-34**(两代 source 身份)—— 词典里「引用源」条目提到的 `sourceFileIntake` 一族**正是新代**;⇒ 现状栏可点一句**碎片流挂新代**,让读词典的人不必去翻台账才知道锚在哪。
- **b-3 已移出本段**(Fable 裁,闸与对手同生)⇒ **⛔ 词典里不得出现任何「引用校验闸已有」的措辞。**

## 4. 给 b-4 拆单的三条输入

1. ⚠️ **首要纪律:⛔ 不把「表建好了」写成「能力有了」** —— 每处新增措辞必须同时说清**有什么 / 还没有什么**;
2. **「碎片」词条是补栏不是改字**(它现在没有现状栏);
3. **两处措辞禁止改动**(转写条的「平铺文本+页级锚」、拓印条的「原始但活」)—— **它们在 12.9c 之前依然为真**。
