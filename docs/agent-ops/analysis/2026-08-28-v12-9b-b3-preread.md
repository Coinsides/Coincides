> **状态 (Status)**: active(b-3 预读,只读,2026-08-28)
> **层 (Layer)**: 分析 / Analysis(V12.9b 段 · b-3 前置)
> **权威 (Authoritative)**: 否(现物快照;⚠️ 含一条需裁的定位问题)

# b-3 预读:引用校验闸的载体与定位

## ⚠️ 1. 需裁:**这道闸目前没有生产者**

段计划的 b-3 = 「理解轨产物**引用碎片 id** 的入库校验(`id ∈ 该拓印件碎片集`),违者拒收留痕 —— 2b 实证的那道门」。

**现物**:
- **b-1 造的 `imprint_fragments` 是新表** ⇒ **引用它的理解轨产物,目前一个都不存在**;
- 现役理解轨产物(`organizedNoteProposals` 等)的 `source_references` 引的是**旧链**的 `document_chunk_id` / `fragment_type`(`organizedNoteProposals.ts:179`)—— **与新碎片流无关**;
- 真正的生产者要到 **12.9c(接真转写器)/ 12.9d(检索地基)** 才出现。

⇒ ⚠️ **b-3 现在建,是给一扇还没有人走的门装锁。**
📌 **这与 12.2c 那次「`get_relation` 的 `human_entry` 零上游」是同一个形状** —— 当时的判据是:**没有活的上游消费链 = 形状在能力不在**,那次的处置是**把工具退列**。

**⇒ 三条路,⛔ 我不选**:
| 路 | 说明 |
|---|---|
| **(A) 照建** | 地基先于插头是本段的明文段目标;闸先在,生产者一到就受管。⚠️ **代价**:killer 只能用**手造产物**,与 b-1 fixture 同源 ⇒ **闸与被测者出自同一只手,鉴别力打折** |
| **(B) 推迟到 12.9c/d** | 与生产者同批到位 —— **与 K-2 region 那条你刚做的裁定同一逻辑**(「验证者与生产者同批到位,刀才有对手」) |
| **(C) 只落契约不落闸** | b-1 的校验器里**预留引用校验的接口与错误码**,⛔ 不实现执行体;12.9c 接生产者时填上 |

📌 **我倾向 (B) 或 (C)** —— 理由就是你两小时前给 K-2 的那条裁定。**若 b-3 照建,请注意它与 K-2 的处置会不一致。**

## 2. 「拒收留痕」的载体:现成机制**属于禁区**

- 仓内唯一成熟的收据机制是 `services/toolFaceReceipts.ts`;
- ⚠️ **它没有自己的表** —— 写的是 **`operation_batches`**;
- ⚠️ **它的消费者是 `server/src/mcp/transport.ts`** ⇒ **在 b-1 单钉的 MCP 工具面禁区内**。

⇒ **b-3 若复用它,等于伸手进禁区。**
**⇒ 建议(不是裁定)**:留痕走**碎片流自己的载体** —— 与 b-1 的 `source_imprints.warnings_json` 同族,**⛔ 不碰 `operation_batches` / 不碰工具面收据**。
📌 **理由**:入库校验拒收**不是 Agent 写操作**,把它塞进 Agent 写操作收据里会**污染那条收据的语义**。

## 3. 给拆单的两条输入

1. ⚠️ **先裁 b-3 的定位**(A/B/C)—— **不裁就开工,会造出一把没有对手的刀**;
2. **留痕载体建议自建,⛔ 不复用工具面收据**(禁区 + 语义污染两重理由)。
