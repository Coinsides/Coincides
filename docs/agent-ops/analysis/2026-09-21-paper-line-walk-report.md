> **状态 (Status)**: active(HQ 亲跑走查实录;数字为 2026-09-21 夜实测)
> **层 (Layer)**: 分析 / 走查报告
> **方法**: 实机(fresh server @3001+Vite @5173,测试账号 token 注入)+ DOM 计算样式取数(⛔眯眼截图定罪);对象=实测·纸面线冒烟·0919 / 立国:陈桥兵变与祖宗之法(31 段落+1 表格+1 note_ref,走查中 +1 toc)

# 纸面线专项走查报告(Q1/Q2/Q3/G7/G9 + T1 冒烟)

> ⚠️ **更正(2026-09-21,T3 builder 诊断拦错)**:本报告 Q3/G9 三个归一数字**混尺度作废**——HQ 用「未缩放 computed 字号 ÷ 已缩放框宽」计算,虚增 1.36×。同尺度复测(Chrome 合成 DOM,四档步进恒定):正文 **16.63**(非 22.6,距样张 16 仅 +4%)、表格 cell **14.63**(非 19.9,档位 0.88)、装订件 **11.95**(非 16.2,样张带宽内)。「字偏大」知觉的真实来源改判:**行高过密(1.467 vs 样张 ~1.7)+ 表格 `min-width:100%` 强制满幅**;阅读步进缩放不改变字/纸比例(builder 实证)。修正后目标见 T3 工单补遗一。测量纪律沉淀:标尺类取数先核公式尺度一致性。

## Q1 页眉页脚「没看到」——不是虫,是呈现弱+默认空文案 ✅结案

- 服务端真相:装订 enabled、页码开(脚中槽,arabic),六槽文案全空;
- DOM 实证:`_pageFramePageNumberSlot` 每页真渲染(1/2/3…),**12px、ink-muted 色(rgb 131,122,108)、字形宽仅 7px**——阅读缩放下近不可见;页眉因空文案零可见物;
- 判:呈现问题(乙猜想),非渲染缺失;封面页 dropFolioOnCover 正常免页码;
- 遗留一点:装订件字号是否随 Q3 排印微调同批调大一档,归 T3。

## G7 样张风格装订——零代码达成 ✅结案

两次 API 配置即得:页眉中=章题(serif 角色字体正确解析为 Georgia)、脚中=「第 1 纸」(prefix/suffix)、脚右=「宋史手册」;重载后全渲染。**候顺手件**:存为「手册式」装订预设一款(gap-list G7 原案),归收尾批装订预设小单或 G1 同批。

## Q3 整体字偏大——实锤并量化 🅰 修单(T3)

- **正文等效字号(归一 900px 纸宽)= 22.6px,样张 16px 的 1.41 倍**(实测:16.7px 字渲染于 665px 宽页框);行高比 1.47;
- 病根方向:阅读步进缩放抬字号而纸宽同步缩放不足 → 字/纸比例错位(样张恒定 16/900);
- 另录:正文字族当前 Aptos/Calibri(sans)——样张正文衬线;是否切默认属皮肤/排印 profile 决策,T3 单里只调标尺⛔动字族(字族归契约/皮肤线);
- 修法照 gap-list 预判:默认 Typography 参数微调(零机制),T3 单附本节数字为验收标尺。

## G9 表格比例——实锤并量化 🅰 修单(T3)

- 3 列 6 行小表占页宽 **98.6%**(满幅贴边;样张同类=内容自适应宽+居中);
- cell 字号归一 **19.9px**(样张表格 cell 约 13.6px,0.85em 档);cell padding 8×10px 显示(归一 ~11×14);
- 修法:表格块默认宽=内容自适应(上限 100%)+居中;cell 字号取正文 0.85 档;padding 收敛——纯显示层,T3。

## Q2 Overview「排版很不对」——未复现 ⏸ 候 Henry 指认

800px 与 1440px 两档宽度下:三列真内容缩略网格、封面标签、页码标签、缩略内连装订件皆正常。今晚证据不足以定罪。**候 Henry 回来实机指认具体不对处**(可能已被 A 波后续砖顺手治愈,也可能在特定视口/状态才现形)。

## T1 目录块实机冒烟 ✅(边界如实)

- 「+插入」菜单现「目录」项,点击落块,`toc` 入库(34 块);
- 冒烟笔记零 heading 块(夹具建于 A4 前)→ 目录渲染空树占位「添加章节标题后,目录会自动显示」——**正确的派生行为**;
- 活树渲染+跳章:单内测试 9/9+RuntimeDocumentLayer 用例背书;实机活树候「冒烟笔记补真 heading」小任务(T3 随手:把两三个段落转 heading,顺带验 Q3 修后的标题标尺)。

## 产出去向

1. **T3 排印对齐批**(Q3+G9+装订件字号一档+冒烟笔记补 heading):小单,数字验收标尺=本报告;
2. G7 装订预设一款:候与 G1 封面预设同批;
3. Q2:候 Henry 指认,⛔盲修。
