> **状态 (Status)**: frozen
> **层 (Layer)**: 分析 / 只读侦察
> **日期 (Updated)**: 2026-09-07
> **权威 (Authoritative)**: 否；源码与合成实验收据，拆单及未决口径由 HQ 冻结
> **取代 (Supersedes)**: —
> **被取代 (Superseded by)**: —
> **工单**: `docs/agent-ops/handoffs/2026-09-07-v13-2-s0-recon-order.md`

# V13.2 单 0：野地退役与坐标 census

## 〇 · 射程与读证顺序

按工单 §〇 完整顺读 `handoffs/plans/v13-2-wilderness-retirement-plan.md`（含修订一）→ `analysis/2026-09-07-wilderness-migration-mapping.md` → `analysis/2026-09-07-v13-1-s4-coordinate-contract-recon.md` → `analysis/2026-09-07-board-data-model-design.md:75–94`。以上相对 `docs/agent-ops/`。仓库入口、方向宪章、现状与 active ADR 亦已查阅；本单是侦察，不接 builder 施工单。遵守段 plan 单线约束，未并行派施工单。

单 4 的坐标读写链、两轮漂移实验和 A 表直接沿用其 §一、§三、§四 A（尤其 `:271–286`），**没有重跑或重画**。本单新增的是五题缺面与可复跑的内存 SQLite census。源码按当前工作树收证（HEAD `f011cef`）；开工已有 `server/src/routes/projections.ts` 修改及其他未跟踪材料，均不属于本单。

以下路径缩写：`E/` = `client/src/pages/Notes/canvasEngine/`，`S/` = `server/src/`，`H/` = `docs/agent-ops/handoffs/`，`A/` = `docs/agent-ops/analysis/`；行号均从仓根文件起计。SQL 中的字符串 ID 全是合成标识，不是用户资料。

**四项应带进拆单的前提**：①加 `tray` 枚举不足以接通托盘，数处二选一归一化会把它吞回野地或正式页；②三张现役 Canvas 表没有通用 `order_index`，纯画物/mount 的排序落点尚待定；③编号已到 053，054 只是当前下一个空位，不能预占；④census 的三种解释是并列候选，主帧、几何规则与 50% 分母的工作口径不构成存量修正授权。

## 一 · 字面量登记面与新写禁令

### 1. 搜索范围与分类方法

`.codegraph/` 存在；已先尝试 `codegraph explore`，CLI 不可用，当前工具表也没有 CodeGraph MCP。`rg` 不可用，回退 `git grep` 和 Node 对 `git ls-files --cached --others --exclude-standard -z` 给出的仓内文本路径扫描。初次全仓扫描 1,565 个文本文件，四目标词及 `exclude_workspace` 命中文件 453 个：347 Markdown、4 HTML 参考件、102 代码/测试/CSS 文件。此数是**文件命中数，不是写入口数**；启发式 test 分组会误收 Inspector，已在下表按源码重新归类。

排除 `.env*`、`dot.env`、私有配置目录、依赖/产物/数据库/上传/备份目录、锁文件和凭证/会话/收据 JSON；不扫描仓外。历史文档只作词汇索引，不作施工依据。`surface` 的 CSS token、DOM `data-*-surface`、文字注释和 ContentGroup surface role 是同名异义，不能全仓替换。下表覆盖现役字面量注册、产生、透传和策略消费点；附带无直接旧字面量、但绕过 API validator 的 writer。

### 2. 注册、写口、兼容读口

| 类别 | 文件:行号 | 现物与退役时须封住的口 |
|---|---|---|
| Server 输入总门 | `S/validators/index.ts:717–738,745,761,784,898,932,1061,1081–1095` | surface 二值、boundary 三值；共享 placement core 被 paragraph/shape/image/table/connector 等 kind schema 和 block-placement schema复用。字段 optional 且 passthrough：只禁显式旧值，挡不住缺省值经 service 再生野地。 |
| 路由入口 | `S/routes/canvasObjects.ts:33–44,51–63,70–82,89–98` | collection、块 placement、generic object、delete 四口；前两类写入与 generic 写入分离，不能只改一种 PUT。 |
| Server 类型与默认值 | `S/services/canvasObjects.ts:22–23,184–192,452–505` | row 类型是 string，输入是 Record 扩展；`:466` 非 formal 一律 workspace，`:467–490` 接 crossing，缺省 boundary 随 surface；`:492` snap 状态也二分。新写政策要落在 service 归一化之后/之内，独立调用者才能同受约束。 |
| Server 列级 writer | 同文件 `:551–592,1919–1930,2021–2035` | INSERT 与 conflict UPDATE 同时写 surface/boundary；block writer 外包事务且清 legacy override。不能靠 client 类型推断数据库已停写。 |
| Server 兼容读 | 同文件 `:296–316,332–350` | block layout 的 `:303` 将非 workspace 都读成 formal；raw placement mapper 原样返回。tray 将出现“一条原始 tray、一条 blockLayout formal”的分叉。 |
| 帧专用 writer | 同文件 `:1741,1804–1815` | collection 事务直接写 formal/inside；page_frame 本体不是待迁的画物，保持单列身份，不应被普通托盘 writer 接纳。 |
| Source 独立 writer | `S/services/sourceProjectionMaterializer.ts:150–206,370–378,529–551` | 直接写帧/块 placement，绕开普通 HTTP validator；当前写 formal/inside，不产生野地，但坐标 A 向转换仍按 S4 A 表核销。 |
| 共享权威分类器 | `shared/types/canvasSurfaceAuthority.ts:3–5,47–52,60–89` | 两值 surface、三值 boundary；只看横轴，几何优先于 explicitSurface；fallback 与 crossing 都能产生 workspace。tray 列表不能再进这个几何分类器后冒充 formal/workspace。 |
| Client 类型 | `E/types.ts:3–10,328–340,546`；`E/runtimeLayout.ts:13,31–35` | CanvasSurface / CanvasBoundaryKind / BlockBoxLayout / export policy 登记。历史读取兼容与新写 DTO 不可不加区分地删旧 union 成员，否则会破坏旧值读入。 |
| Client 持久化读写 | `E/placementService.ts:76–107,111–189,205–298,328–368,386–412,545–572` | 两值 surface guard、三值 boundary guard、authority 重算、workspace 模式分支、payload 中再生 surface/boundary。载入旧值、读到 tray、生成新写值是三个不同检查点。 |
| Client raw normalizer | `E/canvasPersistenceNormalizer.ts:124–156` | 默认 workspace、surface 强制断言为 CanvasSurface，boundary 默认依 surface；不会因为 TypeScript union 收窄就自动运行时拒绝/保留 tray。 |
| 草稿收据 / 恢复 | `E/draftBlockPersistence.ts:220–256` | canonical outside=workspace/outside；canonical framed=正式页/inside 或 workspace/crossing；恢复时再次用共享分类器验证。tray 需要独立列表分支或显式拒绝，不能套原 receipt 形状。 |
| 自然书写 | `E/hooks/useRuntimeNaturalWritingController.ts:67–75,120–145,184–199` | page 草稿投影排除 workspace；无帧/画布默认草稿会直接生成 workspace/outside。 |
| 画物创建与 payload | `E/shapeProjectionService.ts:82,145–159,316`；`imageObjectService.ts:33,49–63,143–144`；`tableObjectService.ts:41,310–324,403–404`；`visualConnectorService.ts:113–114` | shape/image/table 各有“非 formal→workspace”的副本；connector 透传 boundary。类型改动必须贯穿各 kind 的生成/更新/保存。 |
| UI 创建入口 | `E/layers/NoteWritingSurfaceLayer.tsx:1334,1479,1516,1565,1619,3232` | 多种对象有帧 formal / 无帧 workspace，另有固定 workspace 草稿；`:441–442` 将已有 placement 的 surface/boundary 回传。 |
| Runtime 合成 | `E/engineModel.ts:226,259` | 默认 placement/overlay 的 workspace，不一定每条都会持久化，但会进入可见性与保存候选，不能漏掉。 |
| 可执行旧修复脚本 | `server/scripts/v2Bn12LifecycleMigration.ts:150–169,714,743,768–805,931–951` | 仍可计算/写回 workspace/crossing；有 compare-before-write 也仍是一个旧值生产者。将来停用其旧写模式或加版本闸，不能回改历史 035 假装封口。 |
| 历史 035 | `S/db/migrations/035_v2_canvas_objects.ts:39–43,80–102,425–450` | 旧值必须能在历史迁移/读兼容中存在；不删列、不删枚举历史、不重写迁移。用新增迁移/现役写门表达新纪律。 |

### 3. 导出、生命周期与同名异义

| 类别 | 文件:行号 | 处理边界 |
|---|---|---|
| 导出策略全集 | `E/types.ts:7–12`；`E/geometry.ts:12,79–163`；`E/exportPreviewService.ts:197–239,257–265`；`E/layers/ExportPreviewLayer.tsx:248–272` | 五值 `exclude_workspace / include_if_center_inside / include_if_intersects / clip_to_page_frame / manual` 是策略词，不是 surface 值；默认 center-inside。tray 必须明确进入/排除导出名单，不能因“不等于 workspace”误混入正式导出。现有策略历史词不要求删除。 |
| 几何 boundary | `E/geometry.ts:166–183`；`E/pageFrameAffiliationService.ts:14,88–160`；`E/layoutAffiliationService.ts:5,66–133` | crossing 同时是读时几何关系；“禁新写 crossing”不等于几何永远不相交。原边界分类保留作兼容/诊断，不将其输出无条件持久化。 |
| 展示/索引 | `E/layers/BlockStatusBadgeLayer.tsx:63`；`canvasAiTreeService.ts:139,256,273`；`objectInspectorService.ts:42,216`；`layers/ObjectInspectorLayer.tsx:73` | badge/AI tree/Inspector 读 surface/边界。需认托盘身份与列表顺序，不能保留无语义 bbox 冒称画布位置。 |
| 模式与 flow | `E/hooks/useSurfaceModeController.ts:93`；`pageStackContentFlowService.ts:78–94`；`hooks/usePageReadingPresentation.ts:33–50` | page/canvas 模式、flow formal/inside、DOM surface 变量分属不同概念；mode 退役在单 5，坐标链在单 4，勿在本单扩大为重实现。 |
| 生命周期登记 | `S/services/courseLifecyclePolicies.ts:22–37,55–59`；`courseLifecycle.ts:231–233`；`noteBlockLifecycle.ts:788,817–830,990` | 三表按 course move、对象/块删除关系走显式 registry / 查询。tray 仍在同三表时沿用；若加排序旁表或带 course FK 的备份表，需登记。events 的 append-only 与父 FK 级联/SET NULL 也须在单 1 对表，不能意外让删除父项触发改删 events。 |
| 非本题 surface | `E/interactionController.ts:26,38,71`（点击背景目标）；`contentGroupSurfaceRoleService.ts:6–48`；`writingEntryVisibility.ts:59`；`S/services/learningCanvases.ts:360`；CSS `--*-surface` 与 group shell data 属性 | 不是 canvas_placements.surface 的注册/写入点；原样保留，不纳入停写替换。 |

### 4. 测试登记（只定位，未重跑旧覆盖）

| 族 | 文件:代表性行号 |
|---|---|
| 权威/持久化/草稿恢复 | `E/surfaceAuthorityContract.test.ts:42–83,220–277,308–373`；`surfacePersistenceContract.test.ts:84–120`；`draftBlockPersistence.test.ts:145–205,287–383` |
| adapter / layout / runtime / mode | `E/hooks/useNoteCanvasDataAdapter.test.tsx:70,160–203`；`useNoteCanvasLayoutModel.affiliationVisibility.test.tsx:61–112`；`useNoteCanvasRuntimeController.test.tsx:169–170`；`useSurfaceModeController.test.tsx:22–23`；`useBlockPlacementInteractions.test.tsx:129–176,305–309` |
| 几何 / 屏显 / 打印 | `E/blockAffiliationOutlineService.test.ts:29–82`；`layers/BlockEditorLayer.test.tsx:133`；`layers/NotePrintLayer.test.tsx:282–300`；`meaningfulRenderableContent.test.ts:69`；`pageStackContentFlowService.test.ts:65–67`；`hooks/usePageReadingPresentation.test.tsx:42–43,123,226`；`client/scripts/pageReadingSmoke/printSpecimen.ts:59`、`mockApi.ts:58–59` |
| 大模型契约与边界扫描 | `client/scripts/canvasEngineModelContractCheck.ts:648,1216,1419,1492,2223–2226,2368–2412,2614,2798,2909,2976–2977,3178,3301–3334,3474,3548–3567,3613,3756`；`client/scripts/canvasRuntimeBoundaryCheck.mjs:546` |
| Server cutover / Source / 旧脚本 | `S/__tests__/v2CanvasPersistenceCutover.test.ts:202–238,423–444,629,666–679,1465–2341`；`v2SourceMaterialization.test.ts:335–344`；`server/scripts/v2Bn12LifecycleMigration.test.ts:153–201` |

旧值兼容样本与新写拒绝样本应分层留存；不能把旧测试全改成 formal 来获取绿灯。背景 `surface`、Draft DOM、阅读容器测试中的同名命中不构成另一个数据库 writer。

## 二 · 托盘的最小落地面与 UI 拼接选项

**最小贯通面是 schema/排序约定 → 输入与 service → 读回 → client 列表分流 → 入/出托盘动作。** 三表中只有 `canvas_placements.surface` 有该列；`canvas_objects`、`content_mounts` 的去处必须经同作用域 object→placement 关联派生（035 `:59–124`）。一个 object 可有多 placement，一个 mount 指向 object，不指 placement；混合 surface 时不可任选首 placement 给 mount 定去处。

| 层 | 最小需要处理的现物 |
|---|---|
| 数据与排序 | 035 `:80–124` 没有 order_index；块排序在 `S/routes/notes.ts:251–280`、`S/services/canvasObjects.ts:734,863–872` 的 note_block_placements。必须由单 2 冻结：通用 placement 列或托盘成员旁表等承载哪一种“行”，以及 object/mount 多 placement 如何入列表。本单只报缺口，不新增设计。不能把 z_index 偷当已批准的 order_index。 |
| Server | `canvasPlacementCoreSchema` 目前即使 tray 也要求四个 finite 几何字段（`:724–727`）；`normalizePlacementForWrite` 固定列白名单不会保存新的排序字段。新增值、排序字段、tray 无几何语义、停写 workspace/crossing，须在同一写路径闭合；同時保留旧行读取。 |
| Client | `CanvasSurface`、`BlockBoxLayout`、raw normalizer、block layout reader/payload、各 kind mapper、草稿恢复须明确 tray 分支。`E/engineModel.ts:393–415` 排除 paragraph 后另行生成块 runtime：不能只过滤 raw placements，而让同块又经 blockLayouts 回到纸上。此处引用 S4 的既有链，不重画。 |
| 动作 | 上纸要明确 frame 与新坐标；留置按 order；分蘖要接新 note 身份与所有权，不等同删除旧对象。现有 block 新建与挂载事务 `S/routes/notes.ts:255–280` 和 generic writer `S/services/canvasObjects.ts:1919–1930` 是可复用边界，不代表已存在分蘖 API。 |

仅列两个 UI 选项，不选边：

| 选项 | 拼接点 | 涉及层与可观测影响 |
|---|---|---|
| 视口侧栏 | `E/layers/NoteRuntimeDocumentLayer.tsx:35–39,80–82` 的 documentShell 给 writing surface 加同级列表 | 新列表组件与 props 经 runtime/layer props 传递，`NoteDetail.module.css` 给侧栏布局；托盘不进纸的缩放/打印 DOM。侧栏压缩宽度会触发 `hooks/usePageReadingPresentation.ts:32–54` 的 clientWidth/ResizeObserver 重算 fit，须验证开合后的居中和 gear，不改变存储几何。 |
| 独立面板/浮层 | 同文件 `:80` 的 floating panel 层；`E/layers/NoteFloatingPanelLayer.tsx:8–22`、`FloatingOverlayLayer` 现有入口 | 面板可新增同级组件或扩展浮层 props；需自己的开合、选择、焦点、滚动、入出托盘动作。当前浮层只承 SourceJump，不能声称已有通用 tray 插槽；不挤压纸宽，但会有遮挡和焦点恢复问题。独立路由若被选中还需路由和 note 上下文，非本轮预定。 |

两选项均需 block/画物/mount 的列表表达与空态、同一保存/重载来源；不把托盘作为第三级画布或新的 board。打印同级 `NotePrintLayer`（`:82`）需明确排除 tray。

## 三 · 编号、备份命名与 schema 双轨

| 现状 | 文件:行号证据 | 对后续单的含义 |
|---|---|---|
| 047 之后已用 048–053 | `S/db/migrations/048_v2_operation_batches_created_at_timestamp_fix.ts:4`；`049_v2_source_imprints.ts:13`；`050_v2_source_intake_declarations.ts:9`；`051_v2_transcriber_lockfile_hash.ts:9`；`052_v2_imprint_fragment_vectors.ts:53`；`053_v2_template_studio_decommission.ts:20` | 分别 timestamp 修正、拓印、摄入申报、转写器 hash、向量、Template Studio 退役；**下一候选 054**，开单时再查目录，不沿用 048。 |
| 自动迁移机制 | `S/db/migrate.ts:56–84,93–116` | `.ts/.js` 按文件名排序，按 migration.id 去重，每个 up 与登记同行事务；新 migration 只需符合接口/命名，不存在中央静态 import 表需手加。真实数据搬家执行器不能塞进自动 startup up 冒充 Henry 扳机。 |
| schema 先、migrations 后 | `S/db/init.ts:33–40,155–160` | 初始化先 exec schema.sql，再 runMigrations。**schema.sql 不是所有现役表的完整终态镜像**：Canvas 三表只在 035 建，039 重建 frame PK；schema.sql 里仍有 canvas_nodes/frames/viewport（`:878,911,928`）。 |
| 双轨的真实纪律 | `S/db/schema.sql:342–347`；035 `:57–170`；039 `:25–67` | 有的家族明确 migration-only，不能武断要求每个新表在 schema.sql 重复建；单 1 必须让 fresh（schema→全部迁移）与 legacy（已有记录→新迁移）都得到相同 events 表/触发器。若两处都建，要保证相同定义与幂等；这是验收要求，不是本次修改。 |
| 备份表不是旧惯例 | 图三 `A/2026-09-07-wilderness-migration-mapping.md:29–34`；全仓受限 SQL/TS 搜索 `_backup/_bak/backup_pre` | 产品 migrations 未发现三表快照备份后缀的现成实现；图三明确指定 `canvas_placements_backup_pre13_2`、`canvas_objects_backup_pre13_2`、`content_mounts_backup_pre13_2`，不是从旧实现推导的命名习惯。039 的 `_new` 是重建中间表，随后 rename，不能当备份。 |
| 已有备份形态 | `server/scripts/v2Bn12LifecycleMigration.ts:1067–1074,1146,1165–1203` | `db.backup(path)` 写入仓内 `.codex-tmp/v2bn12-03-pre-apply-<timestamp>.db`，记录 bytes/sha256，再 BEGIN IMMEDIATE 重验 plan hash；是文件快照，不是表内 `_backup_pre13_2`。只读查代码，未启动脚本/打开其默认 DB。保留期/清理扳机仍由图三停车场条款待裁。 |

## 四 · 全对象 census SQL 与合成闸

### 1. 计数单位与工作口径

1. **三个原始分母绝不相加冒充内容件数**：placement / object / mount 各按其主键计数，保留 inactive、缺对象、无 placement、多 placement、非 paragraph 画物；逐 note 输出空笔记零数。`page_frame` 结构行在 raw 三表守恒中保留，内容去处矩阵另排除它。mount 与所挂块/画物可能是同一逻辑件的两张行，三表迁移须分别守恒。
2. legacy 以 note_block_placements 行计数，解析 `better_notebook_layout`；记录 invalid JSON / 非 object layout / 缺布局 / legacy-only / dual-any / dual-active / 同 ID 目标不符 / 同块其他实体候选。**legacy-only 严格指没有 exact entity、没有同 ID 行、也没有同块其他 placement 候选；余下未配对者归 legacy_ambiguous，不擅自合并或算作新增内容。** dual 是两份表示共存，不等于两份内容；legacy 永不自动算进三表迁移，图三要求原地封存。note_block_placements 自身无 user_id：仅按 notes.user_id 归属；无 note 的孤行无法归属某用户，查询另列全库无主 note 行**数量**，不回传它们的内容/ID。
3. 帧必须 user_id+note_id+frame_id 配对；不以首帧兜底。主帧只认 canvas_page_collections.primary_frame_id。缺 extension、重复 frame placement、无/坏 inset、非数值、非正内容区、旋转都明确 unknown；不把 unknown 归成 outside。旋转图形只保全行数，本 SQL 不假装实现旋转几何。
4. 并列解释 W=(x,y)、L=(x+O.x,y+O.y)、M=(x+O.x,y)。L/M 仅在**原绑定帧唯一有效**时计算；W 可在无绑定时给原数值的世界候选。所有候选页都返回，不任取第一页；同 stack 多页相交是跨页候选，多 stack 相交另标，最大 overlap 并列也不破同分。表中 local 标签只作分桶，不选择解释。
5. 几何比对同时给完整矩形 inside/crossing/outside、现役 affiliation 的 center-inside、shared authority 的横轴分类，避免把三者混叫 boundary。stored boundary 对**绑定帧**比较，另给主帧比较；没有绑定帧就保留 unavailable。
6. 主帧 overlap 的工作分母是对象未旋转矩形面积 `width*height`，分桶 0 / (0,0.5) / =0.5 / (0.5,1) / =1；oversize 按宽/高超过内容区分别列。50% 只用于报告候选分布；SQL 不自动把 crossing 收编。HQ 可换判据；当前输出足以重新算，不把 center-inside 等同 50%。
7. `note_matrix` 给逐 note 的 inside/crossing/outside 块、画物、mount 与三表分母，`stored_matrix` 再按 status/kind/surface/boundary 展开；两者均是**迁移前 census**。未过滤 inactive；要切 active 可用展开表，不能丢弃全分母。`route_candidate`/`route_distribution` 仅给主帧工作候选，不是迁移后实测。单 3/4 的逐行 plan 与哈希、after 守恒、回滚不在本单伪造为已通过。

以下 SQL 是本报告的完整 census：绑定 `:scope_user_id`，只有 WITH/SELECT；正式使用仍需 HQ/Henry 获准的只读连接。本轮只在 `new Database(':memory:')` 执行。SQL 输出结构化数量与行身份/几何，不输出正文、source_json、metadata 原文。

<!-- S0_CENSUS_SQL -->
```sql
WITH
o AS (SELECT * FROM canvas_objects WHERE user_id=:scope_user_id),
p AS (SELECT * FROM canvas_placements WHERE user_id=:scope_user_id),
m AS (SELECT * FROM content_mounts WHERE user_id=:scope_user_id),
n AS (
  SELECT id AS note_id FROM notes WHERE user_id=:scope_user_id
  UNION SELECT note_id FROM o UNION SELECT note_id FROM p UNION SELECT note_id FROM m
),
pl AS (
  SELECT p.*, o.kind, o.status AS object_status,
    CASE WHEN json_valid(o.source_json) THEN o.source_json ELSE '{}' END AS safe_source,
    (SELECT COUNT(*) FROM m WHERE m.object_id=p.object_id AND m.note_id=p.note_id) AS mounts,
    CASE WHEN json_valid(p.metadata) THEN p.metadata ELSE '{}' END AS safe_meta
  FROM p LEFT JOIN o ON o.id=p.object_id AND o.note_id=p.note_id
),
ol AS (
  SELECT o.*, (SELECT COUNT(*) FROM p WHERE p.object_id=o.id AND p.note_id=o.note_id) AS placements,
    (SELECT COUNT(*) FROM m WHERE m.object_id=o.id AND m.note_id=o.note_id) AS mounts,
    (SELECT COUNT(DISTINCT surface) FROM p WHERE p.object_id=o.id AND p.note_id=o.note_id) AS surfaces
  FROM o
),
ml AS (
  SELECT m.*, o.kind, o.status AS object_status,
    (SELECT COUNT(*) FROM p WHERE p.object_id=m.object_id AND p.note_id=m.note_id) AS placements,
    (SELECT COUNT(DISTINCT surface) FROM p WHERE p.object_id=m.object_id AND p.note_id=m.note_id) AS surfaces,
    (SELECT MIN(surface) FROM p WHERE p.object_id=m.object_id AND p.note_id=m.note_id) AS one_surface
  FROM m LEFT JOIN o ON o.id=m.object_id AND o.note_id=m.note_id
),
l0 AS (
  SELECT l.*, b.status AS block_status,
    CASE WHEN json_valid(l.display_overrides_json) THEN l.display_overrides_json ELSE '{}' END AS j,
    json_valid(l.display_overrides_json) AS valid_json
  FROM note_block_placements l JOIN notes nt ON nt.id=l.note_id AND nt.user_id=:scope_user_id
  LEFT JOIN note_blocks b ON b.id=l.block_id AND b.user_id=nt.user_id
),
l1 AS (
  SELECT l0.*, json_type(j,'$.better_notebook_layout') AS layout_type,
    CASE WHEN json_type(j,'$.better_notebook_layout')='object'
      THEN json_extract(j,'$.better_notebook_layout') ELSE '{}' END AS layout,
    (SELECT COUNT(*) FROM pl WHERE pl.id=l0.id AND pl.note_id=l0.note_id) AS same_id,
    (SELECT COUNT(*) FROM pl WHERE pl.id=l0.id AND pl.note_id=l0.note_id
      AND pl.kind='paragraph_block_projection'
      AND EXISTS (SELECT 1 FROM m WHERE m.object_id=pl.object_id AND m.note_id=pl.note_id
        AND m.target_kind='note_block' AND m.target_id=l0.block_id)) AS entity_any,
    (SELECT COUNT(*) FROM pl WHERE pl.id=l0.id AND pl.note_id=l0.note_id
      AND pl.kind='paragraph_block_projection' AND pl.object_status='active'
      AND l0.block_status='active'
      AND EXISTS (SELECT 1 FROM m WHERE m.object_id=pl.object_id AND m.note_id=pl.note_id
        AND m.target_kind='note_block' AND m.target_id=l0.block_id)) AS entity_active,
    (SELECT COUNT(*) FROM pl WHERE pl.note_id=l0.note_id AND pl.id<>l0.id
      AND EXISTS (SELECT 1 FROM m WHERE m.object_id=pl.object_id AND m.note_id=pl.note_id
        AND m.target_kind='note_block' AND m.target_id=l0.block_id)) AS other_entity_candidates
  FROM l0
),
fr0 AS (
  SELECT e.note_id,e.frame_id,e.page_stack_id,
    COUNT(p.id) AS matches,MIN(p.x) AS fx,MIN(p.y) AS fy,MIN(p.width) AS fw,MIN(p.height) AS fh,
    MIN(p.rotation) AS rotation,
    CASE WHEN json_valid(e.content_inset_json) THEN e.content_inset_json ELSE '{}' END AS j
  FROM page_frame_extensions e JOIN o ON o.id=e.object_id AND o.note_id=e.note_id
    AND o.kind='page_frame' AND o.status='active'
  LEFT JOIN p ON p.object_id=o.id AND p.note_id=o.note_id
  WHERE e.user_id=:scope_user_id
  GROUP BY e.note_id,e.frame_id,e.page_stack_id,e.content_inset_json
),
fr1 AS (
  SELECT *,json_extract(j,'$.left') AS il,json_extract(j,'$.right') AS ir,
    json_extract(j,'$.top') AS it,json_extract(j,'$.bottom') AS ib FROM fr0
),
fr AS (
  SELECT *,fx+il AS ox,fy+it AS oy,fw-il-ir AS cw,fh-it-ib AS ch,
    CASE WHEN matches<>1 THEN 'frame_placement_count'
      WHEN typeof(fx) NOT IN ('integer','real') OR typeof(fy) NOT IN ('integer','real')
        OR typeof(fw) NOT IN ('integer','real') OR typeof(fh) NOT IN ('integer','real')
        THEN 'frame_geometry_unknown'
      WHEN abs(fx)+abs(fw)>1.7976931348623157e308 OR abs(fy)+abs(fh)>1.7976931348623157e308
        THEN 'frame_geometry_nonfinite'
      WHEN typeof(il) NOT IN ('integer','real') OR typeof(ir) NOT IN ('integer','real')
        OR typeof(it) NOT IN ('integer','real') OR typeof(ib) NOT IN ('integer','real')
        THEN 'inset_unknown'
      WHEN il<0 OR ir<0 OR it<0 OR ib<0 OR fw-il-ir<=0 OR fh-it-ib<=0 THEN 'invalid_content_rect'
      WHEN rotation IS NULL OR rotation<>0 THEN 'rotated_frame'
      ELSE 'resolved' END AS frame_state
  FROM fr1
),
b AS (
  SELECT 'placement' AS unit,id,note_id,kind,object_status AS status,x,y,width,height,rotation,
    frame_id,surface,boundary_role,json_extract(safe_meta,'$.layout_policy.coordinate_space') AS tag,
    json_valid(metadata) AS metadata_valid,
    CASE WHEN json_extract(safe_source,'$.imported_from')='display_overrides_json.better_notebook_layout'
      THEN '035_receipt_survives' ELSE 'writer_history_unknown' END AS history_hint
  FROM pl WHERE kind IS NULL OR kind<>'page_frame'
  UNION ALL
  SELECT 'legacy',id,note_id,'legacy_block',block_status,
    json_extract(layout,'$.x'),json_extract(layout,'$.y'),json_extract(layout,'$.width'),
    json_extract(layout,'$.height'),COALESCE(json_extract(layout,'$.rotation'),0),
    json_extract(layout,'$.frame_id'),json_extract(layout,'$.surface'),
    json_extract(layout,'$.boundary_role'),json_extract(layout,'$.coordinate_space'),valid_json,'legacy_layout_present'
  FROM l1 WHERE layout_type='object'
),
bi AS (
  SELECT b.*,f.ox,f.oy,COALESCE(f.frame_state,'missing_or_unbound') AS binding_state,
    CASE WHEN f.frame_state='resolved' THEN b.y>f.fh END AS y_gt_outer_height,
    CASE WHEN f.frame_state='resolved' THEN b.y<0 OR b.y+b.height>f.ch END AS local_y_outside_content,
    CASE WHEN f.frame_state='resolved' THEN b.y>=f.oy AND b.y<f.oy+f.ch END AS raw_world_y_start_in_content,
    CASE WHEN f.frame_state<>'resolved' OR f.frame_state IS NULL THEN 'unknown'
      WHEN f.oy=0 THEN 'origin_y_zero' ELSE 'origin_y_nonzero' END AS origin_bucket,
    c.primary_frame_id,
    (SELECT COUNT(*) FROM fr WHERE fr.note_id=b.note_id AND frame_state='resolved') AS valid_frames,
    (SELECT COUNT(*) FROM fr WHERE fr.note_id=b.note_id AND frame_state<>'resolved')
      + (SELECT COUNT(*) FROM o fo WHERE fo.note_id=b.note_id AND fo.kind='page_frame'
        AND fo.status='active' AND NOT EXISTS (SELECT 1 FROM page_frame_extensions e
          WHERE e.user_id=:scope_user_id AND e.note_id=fo.note_id AND e.object_id=fo.id)) AS unknown_frames,
    CASE WHEN surface='tray' THEN 'tray_no_geometry'
      WHEN typeof(x) NOT IN ('integer','real') OR typeof(y) NOT IN ('integer','real')
        OR typeof(width) NOT IN ('integer','real') OR typeof(height) NOT IN ('integer','real')
        OR width<=0 OR height<=0 THEN 'invalid_geometry'
      WHEN abs(x)+width>1.7976931348623157e308 OR abs(y)+height>1.7976931348623157e308
        OR width*height>1.7976931348623157e308 THEN 'nonfinite_geometry'
      WHEN b.rotation IS NULL OR b.rotation<>0 THEN 'rotated_object'
      ELSE 'resolved' END AS geometry_state
  FROM b LEFT JOIN fr f ON f.note_id=b.note_id AND f.frame_id=b.frame_id
  LEFT JOIN canvas_page_collections c ON c.user_id=:scope_user_id AND c.note_id=b.note_id
),
ex AS (SELECT 'world' AS interpretation UNION ALL SELECT 'local' UNION ALL SELECT 'mixed'),
w0 AS (
  SELECT bi.*,ex.interpretation,
    CASE WHEN geometry_state<>'resolved' THEN geometry_state
      WHEN interpretation<>'world' AND binding_state<>'resolved' THEN 'origin_unknown'
      WHEN interpretation<>'world' AND abs(x)+abs(ox)>1.7976931348623157e308 THEN 'projection_nonfinite'
      WHEN interpretation='local' AND abs(y)+abs(oy)>1.7976931348623157e308 THEN 'projection_nonfinite'
      ELSE 'resolved' END AS interpretation_state,
    CASE WHEN interpretation='world' THEN x ELSE x+ox END AS wx,
    CASE WHEN interpretation='local' THEN y+oy ELSE y END AS wy
  FROM bi CROSS JOIN ex
),
w AS (
  SELECT *,CASE WHEN interpretation_state='resolved' THEN wx END AS world_x,
    CASE WHEN interpretation_state='resolved' THEN wy END AS world_y FROM w0
),
h0 AS (
  SELECT w.unit,w.id,w.note_id,w.interpretation,f.frame_id,f.page_stack_id,
    f.ox,f.oy,f.cw,f.ch,w.world_x AS x,w.world_y AS y,w.width,w.height,
    MAX(0,MIN(w.world_x+w.width,f.ox+f.cw)-MAX(w.world_x,f.ox))
      * MAX(0,MIN(w.world_y+w.height,f.oy+f.ch)-MAX(w.world_y,f.oy)) AS overlap
  FROM w JOIN fr f ON f.note_id=w.note_id AND f.frame_state='resolved'
  WHERE w.interpretation_state='resolved'
),
h AS (
  SELECT *,overlap/(width*height) AS ratio,
    CASE WHEN x>=ox AND y>=oy AND x+width<=ox+cw AND y+height<=oy+ch THEN 'inside'
      WHEN overlap>0 THEN 'crossing' ELSE 'outside' END AS rect_role,
    CASE WHEN x+width/2>=ox AND x+width/2<=ox+cw AND y+height/2>=oy AND y+height/2<=oy+ch THEN 'inside'
      WHEN overlap>0 THEN 'crossing' ELSE 'outside' END AS center_role,
    CASE WHEN x+width<=ox OR x>=ox+cw THEN 'outside'
      WHEN x>=ox AND x+width<=ox+cw THEN 'inside' ELSE 'crossing' END AS horizontal_role,
    CASE WHEN width>cw AND height>ch THEN 'both' WHEN width>cw THEN 'width'
      WHEN height>ch THEN 'height' ELSE 'no' END AS oversize
  FROM h0
),
g0 AS (
  SELECT w.*,
    (SELECT COUNT(*) FROM h WHERE h.unit=w.unit AND h.id=w.id AND h.interpretation=w.interpretation AND overlap>0) AS intersect_count,
    (SELECT COUNT(DISTINCT COALESCE(page_stack_id,'<unstacked>')) FROM h WHERE h.unit=w.unit AND h.id=w.id AND h.interpretation=w.interpretation AND overlap>0) AS intersect_stacks,
    (SELECT MAX(ratio) FROM h WHERE h.unit=w.unit AND h.id=w.id AND h.interpretation=w.interpretation) AS max_ratio,
    (SELECT group_concat(frame_id || '@' || COALESCE(page_stack_id,'<unstacked>') || ':' || rect_role,'|')
      FROM (SELECT * FROM h ORDER BY frame_id) hh WHERE hh.unit=w.unit AND hh.id=w.id AND hh.interpretation=w.interpretation AND overlap>0) AS page_signature,
    (SELECT json_group_array(json_object('frame',frame_id,'stack',page_stack_id,'ratio',ratio,'rect',rect_role,'center',center_role,'horizontal',horizontal_role))
      FROM (SELECT * FROM h ORDER BY frame_id) hh WHERE hh.unit=w.unit AND hh.id=w.id AND hh.interpretation=w.interpretation) AS page_scores,
    ph.ratio AS primary_ratio,ph.oversize,ph.rect_role AS primary_rect_role,
    ph.center_role AS primary_center_role,ph.horizontal_role AS primary_horizontal_role,
    bh.rect_role AS bound_rect_role,bh.center_role AS bound_center_role,bh.horizontal_role AS bound_horizontal_role
  FROM w LEFT JOIN h ph ON ph.unit=w.unit AND ph.id=w.id AND ph.interpretation=w.interpretation AND ph.frame_id=w.primary_frame_id
  LEFT JOIN h bh ON bh.unit=w.unit AND bh.id=w.id AND bh.interpretation=w.interpretation AND bh.frame_id=w.frame_id
),
g AS (
  SELECT *,CASE WHEN primary_ratio IS NULL THEN 'unknown' WHEN primary_ratio=0 THEN '0'
    WHEN primary_ratio<0.5 THEN '(0,.5)' WHEN primary_ratio=0.5 THEN '.5'
    WHEN primary_ratio<1 THEN '(.5,1)' ELSE '1' END AS overlap_bucket,
    (SELECT COUNT(*) FROM h WHERE h.unit=g0.unit AND h.id=g0.id AND h.interpretation=g0.interpretation
      AND h.ratio=g0.max_ratio AND h.ratio>0) AS best_ties,
    CASE WHEN interpretation_state<>'resolved' THEN interpretation_state
      WHEN unknown_frames>0 THEN 'incomplete_frame_inventory'
      WHEN valid_frames=0 THEN 'no_valid_frames'
      WHEN intersect_count=0 THEN 'no_intersection' WHEN intersect_count=1 THEN 'one_frame'
      WHEN intersect_stacks>1 THEN 'multiple_stacks' ELSE 'multiple_pages_one_stack' END AS page_state,
    CASE WHEN unit='legacy' THEN 'legacy_frozen'
      WHEN kind IS NULL OR status<>'active' THEN 'review_identity_or_status'
      WHEN surface<>'canvas_workspace' THEN 'retain_other_surface'
      WHEN kind<>'paragraph_block_projection' THEN 'tray_object'
      WHEN boundary_role='outside' THEN 'tray_outside_block'
      WHEN boundary_role='crossing' AND primary_ratio>=0.5 THEN 'adopt_working_candidate'
      WHEN boundary_role='crossing' AND primary_ratio<0.5 THEN 'tray_crossing_candidate'
      ELSE 'review_unknown' END AS route_candidate
  FROM g0
),
diff AS (
  SELECT unit,id,note_id,SUM(interpretation_state='resolved') AS resolved_interpretations,
    COUNT(DISTINCT CASE WHEN interpretation_state='resolved' THEN COALESCE(page_signature,'<none>') END) AS page_variants
  FROM g GROUP BY unit,id,note_id
),
denom AS (
  SELECT n.note_id,
    (SELECT COUNT(*) FROM p WHERE p.note_id=n.note_id) AS placements,
    (SELECT COUNT(*) FROM o WHERE o.note_id=n.note_id) AS objects,
    (SELECT COUNT(*) FROM m WHERE m.note_id=n.note_id) AS mounts,
    (SELECT COUNT(*) FROM l1 WHERE l1.note_id=n.note_id) AS legacy_rows,
    (SELECT COUNT(*) FROM pl WHERE pl.note_id=n.note_id AND kind='page_frame') AS frame_placements,
    (SELECT COUNT(*) FROM pl WHERE pl.note_id=n.note_id AND kind IS NULL) AS missing_object_placements,
    (SELECT COUNT(*) FROM ol WHERE ol.note_id=n.note_id AND placements=0) AS unplaced_objects,
    (SELECT COUNT(*) FROM ml WHERE ml.note_id=n.note_id AND placements=0) AS unplaced_mounts,
    (SELECT COUNT(*) FROM ol WHERE ol.note_id=n.note_id AND placements>1) AS multi_placement_objects,
    (SELECT COUNT(*) FROM l1 WHERE l1.note_id=n.note_id AND layout_type='object' AND entity_any=0 AND same_id=0 AND other_entity_candidates=0) AS legacy_only,
    (SELECT COUNT(*) FROM l1 WHERE l1.note_id=n.note_id AND layout_type='object' AND entity_any=0 AND (same_id>0 OR other_entity_candidates>0)) AS legacy_ambiguous,
    (SELECT COUNT(*) FROM l1 WHERE l1.note_id=n.note_id AND layout_type='object' AND entity_any>0) AS dual_any,
    (SELECT COUNT(*) FROM l1 WHERE l1.note_id=n.note_id AND layout_type='object' AND entity_active>0) AS dual_active
  FROM n
)
SELECT json_object(
 'denominators',json((SELECT json_group_array(json_object('note',note_id,'placement',placements,'object',objects,'mount',mounts,
   'legacy_rows',legacy_rows,'frame_placements',frame_placements,'missing_object_placements',missing_object_placements,
   'unplaced_objects',unplaced_objects,'unplaced_mounts',unplaced_mounts,'multi_placement_objects',multi_placement_objects,
   'legacy_only',legacy_only,'legacy_ambiguous',legacy_ambiguous,'dual_any',dual_any,'dual_active',dual_active)) FROM denom)),
 'unscopable_legacy_note_rows', (SELECT COUNT(*) FROM note_block_placements l WHERE NOT EXISTS (SELECT 1 FROM notes nt WHERE nt.id=l.note_id)),
 'note_matrix',json((SELECT json_group_array(json_object('note',d.note_id,
   'inside_blocks',(SELECT COUNT(*) FROM pl WHERE pl.note_id=d.note_id AND kind='paragraph_block_projection' AND boundary_role='inside'),
   'crossing_blocks',(SELECT COUNT(*) FROM pl WHERE pl.note_id=d.note_id AND kind='paragraph_block_projection' AND boundary_role='crossing'),
   'outside_blocks',(SELECT COUNT(*) FROM pl WHERE pl.note_id=d.note_id AND kind='paragraph_block_projection' AND boundary_role='outside'),
   'other_boundary_blocks',(SELECT COUNT(*) FROM pl WHERE pl.note_id=d.note_id AND kind='paragraph_block_projection' AND boundary_role NOT IN ('inside','crossing','outside')),
   'drawing_objects',(SELECT COUNT(*) FROM ol WHERE ol.note_id=d.note_id AND kind NOT IN ('paragraph_block_projection','page_frame')),
   'wilderness_drawing_objects',(SELECT COUNT(*) FROM ol WHERE ol.note_id=d.note_id AND kind NOT IN ('paragraph_block_projection','page_frame')
     AND EXISTS (SELECT 1 FROM p WHERE p.object_id=ol.id AND p.note_id=ol.note_id AND p.surface='canvas_workspace')),
   'mounts',d.mounts,'wilderness_only_mounts',(SELECT COUNT(*) FROM ml WHERE ml.note_id=d.note_id AND surfaces=1 AND one_surface='canvas_workspace'),
   'mixed_surface_mounts',(SELECT COUNT(*) FROM ml WHERE ml.note_id=d.note_id AND surfaces>1),
   'before',json_object('placement',d.placements,'object',d.objects,'mount',d.mounts),
   'after',NULL,'conservation','not_executed')) FROM denom d)),
 'stored_matrix',json((SELECT json_group_array(json_object('note',note_id,'kind',kind,'status',object_status,'surface',surface,'boundary',boundary_role,'n',n))
   FROM (SELECT note_id,kind,object_status,surface,boundary_role,COUNT(*) AS n FROM pl GROUP BY note_id,kind,object_status,surface,boundary_role))),
 'object_inventory',json((SELECT json_group_array(json_object('note',note_id,'id',id,'kind',kind,'status',status,'placements',placements,'mounts',mounts,'surfaces',surfaces)) FROM ol)),
 'mount_inventory',json((SELECT json_group_array(json_object('note',note_id,'id',id,'object',object_id,'target_kind',target_kind,'object_kind',kind,'status',object_status,
   'placements',placements,'surfaces',surfaces,'surface_state',CASE WHEN placements=0 THEN 'unplaced' WHEN surfaces>1 THEN 'mixed' ELSE one_surface END)) FROM ml)),
 'legacy_inventory',json((SELECT json_group_array(json_object('note',note_id,'id',id,'valid_json',valid_json,'layout_type',layout_type,
   'block_status',block_status,'same_id',same_id,'entity_any',entity_any,'entity_active',entity_active,'other_entity_candidates',other_entity_candidates)) FROM l1)),
 'frame_inventory',json((SELECT json_group_array(json_object('note',note_id,'id',frame_id,'stack',page_stack_id,'matches',matches,'state',frame_state)) FROM fr)),
 'geometry',json((SELECT json_group_array(json_object('unit',unit,'note',note_id,'id',id,'kind',kind,'status',status,'tag',tag,'metadata_valid',metadata_valid,
   'history_hint',history_hint,'origin_bucket',origin_bucket,'y_gt_outer_height',y_gt_outer_height,
   'local_y_outside_content',local_y_outside_content,'raw_world_y_start_in_content',raw_world_y_start_in_content,
   'stored_surface',surface,'stored_boundary',boundary_role,'frame',frame_id,'binding',binding_state,'primary',primary_frame_id,'unknown_frames',unknown_frames,
   'interpretation',interpretation,'state',interpretation_state,'x',world_x,'y',world_y,'page_state',page_state,'page_signature',page_signature,
   'page_scores',json(page_scores),'best_ties',best_ties,'primary_ratio',primary_ratio,'overlap_bucket',overlap_bucket,'oversize',oversize,
   'primary_rect',primary_rect_role,'primary_center',primary_center_role,'primary_horizontal',primary_horizontal_role,
   'bound_rect',bound_rect_role,'bound_center',bound_center_role,'bound_horizontal',bound_horizontal_role,
   'stored_vs_rect',CASE WHEN bound_rect_role IS NOT NULL AND boundary_role IS NOT NULL THEN boundary_role<>bound_rect_role END,
   'stored_vs_center',CASE WHEN bound_center_role IS NOT NULL AND boundary_role IS NOT NULL THEN boundary_role<>bound_center_role END,
   'stored_vs_horizontal',CASE WHEN bound_horizontal_role IS NOT NULL AND boundary_role IS NOT NULL THEN boundary_role<>bound_horizontal_role END,
   'route_candidate',route_candidate)) FROM g)),
 'interpretation_differences',json((SELECT json_group_array(json_object('unit',unit,'note',note_id,'id',id,'resolved',resolved_interpretations,'page_variants',page_variants)) FROM diff)),
 'route_distribution',json((SELECT json_group_array(json_object('note',note_id,'unit',unit,'interpretation',interpretation,'candidate',route_candidate,'n',n))
   FROM (SELECT note_id,unit,interpretation,route_candidate,COUNT(*) AS n FROM g GROUP BY note_id,unit,interpretation,route_candidate))),
 'distribution',json((SELECT json_group_array(json_object('note',note_id,'unit',unit,'interpretation',interpretation,'kind',kind,'tag',tag,
   'state',interpretation_state,'overlap',overlap_bucket,'oversize',oversize,'boundary_diff',boundary_diff,'n',n))
   FROM (SELECT note_id,unit,interpretation,kind,tag,interpretation_state,overlap_bucket,oversize,
     CASE WHEN bound_rect_role IS NOT NULL AND boundary_role IS NOT NULL THEN boundary_role<>bound_rect_role END AS boundary_diff,COUNT(*) AS n
     FROM g GROUP BY note_id,unit,interpretation,kind,tag,interpretation_state,overlap_bucket,oversize,boundary_diff)))
) AS census_json;
```

### 2. 合成库装置、断言与收据

PowerShell 从仓根将本节 JavaScript 单引号 here-string 送 `node` stdin；不用 Vite、dotenv、db/init、HTTP server 或任何 DB 文件。三个父表与旧 placement 表为最小合成 schema；Canvas DDL 来自真实 035，再用真实 039 改为 note-scoped frame identity。035 只在空合成旧表上建立结构，不重做 S4 回填实验。

<!-- S0_SYNTHETIC_JS -->
```javascript
const fs = require('node:fs');
const assert = require('node:assert/strict');
const ts = require('./client/node_modules/typescript');
const Database = require('./server/node_modules/better-sqlite3');
const reportPath = 'docs/agent-ops/analysis/2026-09-07-v13-2-s0-recon.md';
const report = fs.readFileSync(reportPath, 'utf8');
const sql = report.split('<!-- S0_CENSUS_SQL -->')[1].split('```sql\n')[1].split('\n```')[0];
function migration(name) {
  assert.ok(['035_v2_canvas_objects','039_v2_page_frame_extension_note_scope'].includes(name));
  const code = ts.transpileModule(fs.readFileSync('server/src/db/migrations/'+name+'.ts','utf8'), {
    compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}
  }).outputText;
  const module={exports:{}};
  new Function('module','exports','require',code)(module,module.exports,()=>{throw Error('Unexpected import');});
  return module.exports.default;
}
const db = new Database(':memory:');
db.pragma('foreign_keys = ON');
db.exec(`
 CREATE TABLE users(id TEXT PRIMARY KEY);
 CREATE TABLE courses(id TEXT PRIMARY KEY);
 CREATE TABLE notes(id TEXT PRIMARY KEY,user_id TEXT,course_id TEXT,metadata TEXT,updated_at TEXT);
 CREATE TABLE note_blocks(id TEXT PRIMARY KEY,user_id TEXT,course_id TEXT,status TEXT);
 CREATE TABLE note_block_placements(id TEXT PRIMARY KEY,note_id TEXT,block_id TEXT,order_index REAL,
   display_overrides_json TEXT,updated_at TEXT);
`);
migration('035_v2_canvas_objects').up(db);
migration('039_v2_page_frame_extension_note_scope').up(db);
db.exec("INSERT INTO users VALUES('s0-user'); INSERT INTO users VALUES('s0-other'); INSERT INTO courses VALUES('s0-course');");
function note(id,user='s0-user') {
  db.prepare('INSERT INTO notes VALUES(?,?,?, ?,NULL)').run(id,user,'s0-course','{}');
}
for(const id of ['n','other-note','empty','broken','multi','missing-primary','no-frames']) note(id);
note('foreign-note','s0-other');
function obj(id,kind='shape',nt='n',status='active',user='s0-user') {
  db.prepare('INSERT INTO canvas_objects(id,user_id,course_id,note_id,canvas_id,kind,status) VALUES(?,?,?,?,?,?,?)')
    .run(id,user,'s0-course',nt,nt,kind,status);
}
function place(id,object,{nt='n',x=120,y=230,width=20,height=20,frame='f1',surface='formal_page',role='inside',rotation=0,tag='canvas_world',metadata,user='s0-user'}={}) {
  db.prepare(`INSERT INTO canvas_placements(id,user_id,course_id,note_id,canvas_id,object_id,x,y,width,height,frame_id,surface,boundary_role,rotation,metadata)
    VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,user,'s0-course',nt,nt,object,x,y,width,height,frame,surface,role,rotation,
      metadata===undefined?JSON.stringify({layout_policy:{coordinate_space:tag}}):metadata);
}
function mount(id,object,target='synthetic-target',kind='note_block',nt='n') {
  db.prepare('INSERT INTO content_mounts(id,user_id,course_id,note_id,object_id,target_kind,target_id) VALUES(?,?,?,?,?,?,?)')
    .run(id,'s0-user','s0-course',nt,object,kind,target);
}
function block(id,options={}) {
  const nt=options.nt||'n';
  db.prepare('INSERT INTO note_blocks VALUES(?,?,?,?)').run('b-'+id,'s0-user','s0-course','active');
  obj('o-'+id,'paragraph_block_projection',nt,options.status||'active');
  place(id,'o-'+id,options); mount('m-'+id,'o-'+id,'b-'+id,'note_block',nt);
}
function frame(id,{nt='n',x=100,y=200,stack='stack1',inset={left:10,right:10,top:20,bottom:20},object='of-'+nt+'-'+id,rotation=0}={}) {
  obj(object,'page_frame',nt);
  place('pf-'+nt+'-'+id,object,{nt,x,y,width:120,height:140,frame:id,rotation});
  db.prepare(`INSERT INTO page_frame_extensions(frame_id,user_id,course_id,note_id,object_id,canvas_id,page_stack_id,content_inset_json)
    VALUES(?,?,?,?,?,?,?,?)`).run(id,'s0-user','s0-course',nt,object,nt,stack,typeof inset==='string'?inset:JSON.stringify(inset));
}
function primary(nt,id) {
  db.prepare('INSERT INTO canvas_page_collections(note_id,user_id,course_id,canvas_id,primary_frame_id) VALUES(?,?,?,?,?)')
    .run(nt,'s0-user','s0-course',nt,id);
}
frame('f1'); frame('f2',{y:500}); primary('n','f1');
frame('f1',{nt:'other-note',x:1000,y:2000}); primary('other-note','f1');
frame('bad-inset',{nt:'broken',inset:'{broken'});
frame('negative-inset',{nt:'broken',inset:{left:-1,right:10,top:20,bottom:20}});
frame('zero-content',{nt:'broken',inset:{left:60,right:60,top:20,bottom:20}});
frame('rotated-frame',{nt:'broken',rotation:5});
frame('duplicate',{nt:'broken'});
place('pf-duplicate-extra','of-broken-duplicate',{nt:'broken',frame:'duplicate'});
obj('frame-no-extension','page_frame','broken');
place('pf-no-extension','frame-no-extension',{nt:'broken',frame:'no-extension'});
primary('broken','absent');
frame('fa',{nt:'multi',stack:'stack-a'}); frame('fb',{nt:'multi',stack:'stack-b'}); primary('multi','fa');
frame('f1',{nt:'missing-primary'}); primary('missing-primary','absent');
block('world');
db.prepare('UPDATE canvas_objects SET source_json=? WHERE id=?').run(JSON.stringify({imported_from:'display_overrides_json.better_notebook_layout'}),'o-world');
block('local',{x:10,y:10,tag:'page_frame_local'});
block('mixed',{x:10,y:230,tag:'page_frame_local'});
block('second',{x:10,y:530,frame:'f2',tag:'page_frame_local'});
block('half',{x:60,y:230,width:100,surface:'canvas_workspace',role:'crossing'});
block('quarter',{x:35,y:230,width:100,surface:'canvas_workspace',role:'crossing'});
block('three-quarter',{x:85,y:230,width:100,surface:'canvas_workspace',role:'crossing'});
block('outside',{x:400,y:700,frame:null,surface:'canvas_workspace',role:'outside'});
block('stored-inside-wrong',{x:60,y:230,width:100,role:'inside'});
block('oversize',{x:60,y:170,width:200,height:200,surface:'canvas_workspace',role:'crossing'});
block('tall-crosspage',{x:120,y:230,width:20,height:320});
block('zero-width',{width:0});
block('rotated',{rotation:45});
block('invalid-meta',{metadata:'{bad'});
block('missing-frame',{frame:'no-such-frame'});
block('invalid-inset',{nt:'broken',frame:'bad-inset'});
block('duplicate-frame',{nt:'broken',frame:'duplicate'});
block('cross-note',{nt:'other-note',x:10,y:10,tag:'page_frame_local'});
block('multi-stack',{nt:'multi',frame:'fa'});
block('missing-primary-block',{nt:'missing-primary'});
block('inactive',{status:'retired'});
block('tray',{surface:'tray',role:'outside',frame:null});
block('no-frame-inventory',{nt:'no-frames',frame:null});
block('text-x',{x:'not-a-number'});
block('infinite-x',{x:Infinity});
for(const kind of ['shape','image','table','visual_connector','future_kind']) {
  obj('object-'+kind,kind); place('p-'+kind,'object-'+kind,{surface:'canvas_workspace',role:'outside',x:500});
}
mount('mount-shape','object-shape','b-world');
mount('mount-table','object-table','structured-target','structured_object');
obj('unplaced-object'); mount('unplaced-mount','unplaced-object');
obj('multi-object'); place('multi-p1','multi-object',{surface:'canvas_workspace',role:'outside'});
place('multi-p2','multi-object',{surface:'formal_page'}); mount('multi-mount','multi-object');
// 同用户但 object 在别 note：FK 可存在，作用域联接必须识别为 missing，而非漏掉分母。
obj('wrong-note-object','shape','other-note'); place('wrong-note-placement','wrong-note-object');
mount('wrong-note-mount','wrong-note-object');
obj('foreign-object','shape','foreign-note','active','s0-other');
place('foreign-placement','foreign-object',{nt:'foreign-note',user:'s0-other'});
function legacy(id,{layout={x:10,y:10,width:20,height:20,frame_id:'f1',surface:'formal_page',boundary_role:'inside',coordinate_space:'page_frame_local'},raw,blockId='b-'+id,nt='n'}={}) {
  if(!db.prepare('SELECT 1 FROM note_blocks WHERE id=?').get(blockId))
    db.prepare('INSERT INTO note_blocks VALUES(?,?,?,?)').run(blockId,'s0-user','s0-course','active');
  db.prepare('INSERT INTO note_block_placements VALUES(?,?,?,?,?,NULL)').run(id,nt,blockId,0,
    raw===undefined?JSON.stringify({better_notebook_layout:layout}):raw);
}
legacy('world'); legacy('inactive'); legacy('legacy-only'); legacy('legacy-invalid',{raw:'{bad'});
legacy('legacy-nonobject',{layout:'not-a-layout'}); legacy('legacy-none',{raw:'{}'});
legacy('legacy-other-id',{blockId:'b-local'}); legacy('half',{blockId:'different-target'});
legacy('unscopable',{nt:'nonexistent-note'});
const before=db.prepare('SELECT total_changes() AS n').get().n;
db.pragma('query_only = ON');
const statement=db.prepare(sql); assert.equal(statement.readonly,true);
const result=JSON.parse(statement.get({scope_user_id:'s0-user'}).census_json);
assert.equal(db.prepare('SELECT total_changes() AS n').get().n,before);
const sum=(a,k)=>a.reduce((n,r)=>n+r[k],0);
const total=table=>db.prepare('SELECT COUNT(*) AS n FROM '+table+' WHERE user_id=?').get('s0-user').n;
assert.equal(sum(result.denominators,'placement'),total('canvas_placements'));
assert.equal(sum(result.denominators,'object'),total('canvas_objects'));
assert.equal(sum(result.denominators,'mount'),total('content_mounts'));
assert.equal(sum(result.stored_matrix,'n'),total('canvas_placements'));
assert.equal(result.note_matrix.length,result.denominators.length);
assert.ok(result.note_matrix.every(r=>r.after===null && r.conservation==='not_executed'));
assert.equal(result.object_inventory.length,total('canvas_objects'));
assert.equal(result.mount_inventory.length,total('content_mounts'));
assert.equal(result.unscopable_legacy_note_rows,1);
assert.equal(result.denominators.find(r=>r.note==='empty').placement,0);
assert.ok(!result.denominators.some(r=>r.note==='foreign-note'));
assert.equal(sum(result.denominators,'dual_any'),2);
assert.equal(sum(result.denominators,'dual_active'),1);
assert.equal(sum(result.denominators,'legacy_only'),1);
assert.equal(sum(result.denominators,'legacy_ambiguous'),2);
assert.equal(result.legacy_inventory.find(r=>r.id==='legacy-invalid').valid_json,0);
assert.equal(result.legacy_inventory.find(r=>r.id==='half').same_id,1);
assert.equal(result.legacy_inventory.find(r=>r.id==='half').entity_any,0);
assert.equal(result.legacy_inventory.find(r=>r.id==='legacy-other-id').other_entity_candidates,1);
const geom=(id,interpretation='world',unit='placement')=>result.geometry.find(r=>r.id===id&&r.unit===unit&&r.interpretation===interpretation);
assert.equal(geom('half').primary_ratio,0.5);
assert.equal(geom('quarter').overlap_bucket,'(0,.5)');
assert.equal(geom('three-quarter').overlap_bucket,'(.5,1)');
assert.equal(geom('world').primary_ratio,1);
assert.equal(geom('world').history_hint,'035_receipt_survives');
assert.equal(geom('mixed').origin_bucket,'origin_y_nonzero');
assert.equal(geom('mixed').raw_world_y_start_in_content,1);
assert.equal(geom('mixed').local_y_outside_content,1);
assert.equal(geom('outside').primary_ratio,0);
assert.equal(geom('oversize').oversize,'both');
assert.equal(geom('half').primary_center,'inside');
assert.equal(geom('half').primary_rect,'crossing');
assert.equal(geom('stored-inside-wrong').stored_vs_rect,1);
assert.equal(geom('mixed','mixed').primary_ratio,1);
assert.equal(geom('mixed','world').primary_ratio,0);
assert.equal(geom('local','local').primary_ratio,1);
assert.equal(geom('second','mixed').page_signature,'f2@stack1:inside');
assert.equal(geom('cross-note','local').x,1020);
assert.equal(geom('cross-note','local').y,2030);
assert.equal(geom('cross-note','local').primary_ratio,1);
assert.equal(geom('multi-stack').page_state,'multiple_stacks');
assert.equal(geom('multi-stack').best_ties,2);
assert.equal(geom('tall-crosspage').page_state,'multiple_pages_one_stack');
assert.equal(geom('duplicate-frame','local').state,'origin_unknown');
assert.equal(geom('invalid-inset','local').state,'origin_unknown');
assert.equal(geom('missing-frame','local').state,'origin_unknown');
assert.equal(geom('missing-primary-block').primary_ratio,null);
assert.equal(geom('zero-width').state,'invalid_geometry');
assert.equal(geom('rotated').state,'rotated_object');
assert.equal(geom('tray').state,'tray_no_geometry');
assert.equal(geom('invalid-meta').metadata_valid,0);
assert.equal(geom('no-frame-inventory').page_state,'no_valid_frames');
assert.equal(geom('text-x').state,'invalid_geometry');
assert.equal(geom('infinite-x').state,'nonfinite_geometry');
for(const [id,state] of [['bad-inset','inset_unknown'],['negative-inset','invalid_content_rect'],['zero-content','invalid_content_rect'],['rotated-frame','rotated_frame'],['duplicate','frame_placement_count']])
  assert.equal(result.frame_inventory.find(r=>r.id===id).state,state);
assert.ok(geom('invalid-inset').unknown_frames>=6);
assert.equal(result.mount_inventory.find(r=>r.id==='multi-mount').surface_state,'mixed');
assert.equal(result.mount_inventory.find(r=>r.id==='unplaced-mount').placements,0);
assert.equal(result.mount_inventory.find(r=>r.id==='wrong-note-mount').object_kind,null);
for(const id of ['p-shape','p-image','p-table','p-visual_connector','p-future_kind']) assert.ok(geom(id));
assert.equal(geom('legacy-only','local','legacy').primary_ratio,1);
assert.ok(result.interpretation_differences.find(r=>r.id==='mixed').page_variants>1);
assert.equal(result.geometry.length,3*(total('canvas_placements')-sum(result.denominators,'frame_placements')+5));
assert.equal(sum(result.distribution,'n'),result.geometry.length);
assert.equal(sum(result.route_distribution,'n'),result.geometry.length);
const receipt={node:process.version,sqlite:db.prepare('SELECT sqlite_version() AS v').get().v,
  betterSqlite3:require('./server/node_modules/better-sqlite3/package.json').version,
  db:':memory:',queryReadonly:statement.readonly,placements:total('canvas_placements'),objects:total('canvas_objects'),mounts:total('content_mounts'),
  legacyRows:result.legacy_inventory.length,legacyOnly:sum(result.denominators,'legacy_only'),legacyAmbiguous:sum(result.denominators,'legacy_ambiguous'),dualAny:sum(result.denominators,'dual_any'),
  dualActive:sum(result.denominators,'dual_active'),geometryRows:result.geometry.length,distributionRows:result.distribution.length};
console.log('S0_CENSUS_PASS '+JSON.stringify(receipt));
db.close();

// 独立的第二个内存库只核对图二指定的触发器与事务前提，不是产品实现。
const edb=new Database(':memory:');
edb.exec(`CREATE TABLE actions(id INTEGER PRIMARY KEY);
 CREATE TABLE events(seq INTEGER PRIMARY KEY AUTOINCREMENT,ts TEXT,user_id TEXT,actor_kind TEXT,channel TEXT,
   verb TEXT,objects TEXT,summary TEXT,meta TEXT);
 CREATE TRIGGER events_no_update BEFORE UPDATE ON events
 BEGIN SELECT RAISE(ABORT,'events_append_only'); END;
 CREATE TRIGGER events_no_delete BEFORE DELETE ON events
 BEGIN SELECT RAISE(ABORT,'events_append_only'); END;`);
const add=()=>edb.prepare("INSERT INTO events(ts,user_id,actor_kind,channel,verb,objects,summary,meta) VALUES('synthetic','s0-user','system','synthetic-migration','migrated','[]','synthetic','{}')").run();
add();
for(const mutation of ["UPDATE events SET summary='changed' WHERE seq=1",'DELETE FROM events WHERE seq=1']) {
  assert.throws(()=>edb.exec(mutation),e=>e.code==='SQLITE_CONSTRAINT_TRIGGER'&&e.message==='events_append_only');
}
assert.equal(edb.prepare('SELECT summary FROM events WHERE seq=1').get().summary,'synthetic');
assert.throws(()=>edb.transaction(()=>{edb.exec('INSERT INTO actions VALUES(1)');add();throw Error('action-failed');})(),/action-failed/);
assert.equal(edb.prepare('SELECT COUNT(*) AS n FROM actions').get().n,0);
assert.equal(edb.prepare('SELECT COUNT(*) AS n FROM events').get().n,1);
edb.transaction(()=>{edb.exec('INSERT INTO actions VALUES(2)');add();})();
assert.equal(edb.prepare('SELECT COUNT(*) AS n FROM actions').get().n,1);
assert.equal(edb.prepare('SELECT COUNT(*) AS n FROM events').get().n,2);
// ABORT 撤销当前语句；异常被事务回调向外抛出，better-sqlite3 才回滚整笔动作。
assert.throws(()=>edb.transaction(()=>{edb.exec('INSERT INTO actions VALUES(3)');edb.exec('DELETE FROM events');})(),/events_append_only/);
assert.equal(edb.prepare('SELECT COUNT(*) AS n FROM actions').get().n,1);
assert.equal(edb.prepare('SELECT COUNT(*) AS n FROM events').get().n,2);
console.log('S0_EVENTS_PASS update_rejected=1 delete_rejected=1 rollback_atomic=1 commit_atomic=1 trigger_failure_rolls_back_action=1');
edb.close();
```

复跑入口（仅读本报告代码块，然后执行其两个内存库探针）：

```powershell
@'
const fs=require('node:fs');
const report=fs.readFileSync('docs/agent-ops/analysis/2026-09-07-v13-2-s0-recon.md','utf8');
const code=report.split('<!-- S0_SYNTHETIC_JS -->')[1].split('```javascript\n')[1].split('\n```')[0];
new Function('require',code)(require);
'@ | node
```

### 3. 最终合成收据与覆盖声明

最后一版直接抽取本报告 SQL/JS 代码块执行，exit **0**，约 0.6 秒；没有另一个“测试专用 SQL”副本。

```text
S0_CENSUS_PASS node=v22.22.1 sqlite=3.49.2 better-sqlite3=11.10.0 db=:memory:
queryReadonly=true; query_only=ON; total_changes() 查询前后不变
placements=46 objects=45 mounts=30 legacyRows=8
legacyOnly=1 legacyAmbiguous=2 dualAny=2 dualActive=1
geometryRows=114 distributionRows=81
S0_EVENTS_PASS update_rejected=1 delete_rejected=1 rollback_atomic=1
commit_atomic=1 trigger_failure_rolls_back_action=1
exit=0
```

| 覆盖维度 | 合成断言与限制 |
|---|---|
| 全分母 | P=46 / O=45 / M=30 分别等于原始三表按 scope 直接 COUNT；stored_matrix 总数=P，object/mount 明细长度分别=O/M。空 note 为零，另一用户数据不计入。page_frame placement=13 作为结构行保留，因此 geometry=(46−13+5 个有效 legacy layout)×3=114；分布与 route 分桶各自合计 114。 |
| 身份与双真相 | 有效 legacy object layout 共 5：明确 legacy-only 1、ambiguity 2、dual-any 2（其中 active 1）；其余 3 行是坏 JSON、非 object layout、无 layout。另 1 条无 note 的旧行仅以全局无主计数列出。精确 ID 与 target 不符、同块另一 entity 候选均没有误算为确定 dual 或确定 legacy-only。 |
| 非 paragraph / mount | shape/image/table/visual_connector/未来未知 kind 均有几何记录；unplaced object/mount、多 placement 同 object、mixed surface mount、同用户错 note 关联都保留原始行并标状态。它们不是对生产 kind 合法性的认定，而是 census 对现存异常的保全能力。 |
| 三解释 / 归页 | world/local/mixed 分别命中；次帧 mixed 命中 f2；跨 note 同 frame_id 得到独立原点 `(1020,2030)`；同 stack 合法跨页与两个 stack 同分分别列出；多 stack 最优同分数=2，不自动选边。 |
| 未知类 | bad/negative/zero-area inset、重复 frame placement、缺 extension、旋转 frame/object、缺绑定/主帧、完全无帧、零宽、text x、infinite x、畸形 metadata；不将未知解释变成 outside。legacy 无法确定的旧行仍没有污染判刑结论。 |
| boundary / overlap | 0、0.25、0.5、0.75、1 五桶与双轴 oversize 都命中；半重叠样本 center=inside 但完整矩形=crossing；stored inside 错标被 rect 比较识别；S4 的 035 正向痕迹、非零 y 原点和独立 world-y 候选字段保留。 |
| 史记前提 | 两个 BEFORE 触发器分别拒绝 UPDATE 和 DELETE，错误为 `SQLITE_CONSTRAINT_TRIGGER` / `events_append_only`；动作失败时新事件和动作一起撤销，成功时一起提交；触发器失败向事务回调外抛出时，先前动作也回滚。 |

上述 PASS 的射程是 SQL 可执行性、所列样本的分类和事务前提，不是用户库普查或迁移验收。首次 prepare 曾暴露 `rotation` 列歧义，已限定为 `b.rotation`；最终代码块抽取重跑通过。未测大库耗时/索引计划、旋转几何、真实存量分布、UI/打印、迁移后守恒与哈希；这些没有写成“已过闸”。

## 五 · 史记 v0 前提对表

图二 `:75–94` 定义 seq/ts/user_id/actor_kind/channel/verb/objects/summary/meta、十三个 verb、同事务及通道印章。当前还没有本段 events 实装；现有 operation_batches / toolFaceReceipt 不能当成已经有 append-only 史记。

**语法实证成立**：实际语句是两个独立触发器，`BEFORE UPDATE ON events` 与 `BEFORE DELETE ON events`，body 都为 `BEGIN SELECT RAISE(ABORT,'events_append_only'); END;`。图二 `RAISE(ABORT)` 是设计简记，施工用带消息参数的完整 SQL。`ABORT` 撤销当前语句；本探针整笔动作回滚是异常继续抛出后由 better-sqlite3 的事务包装完成的，不能吞掉异常后仍宣称同生共死。

探针 events 表只用于这两个语法/事务前提：没有冒充正式 migration，也没有实现十三 verb CHECK、认证态派生、索引或外键方案。单 1 仍按图二建正式表并完成这些前提的对表。

| 候选挂点 | 文件:行号 | 适用边界 |
|---|---|---|
| 本段迁移/回滚执行器的受控事务 | `server/scripts/v2Bn12LifecycleMigration.ts:1165–1203`（既有同类脚本的锁定/重验/提交结构）；`S/db/migrate.ts:111–116`（自动 schema 事务） | 单 1 的 `recordEvent(tx,…)` 可供单 4 显式调用；真实数据迁移仍在 Henry 扳机后，不自动塞进 runMigrations。工具函数需接已有事务连接，不能独立 getDb 后另落一笔。 |
| Canvas REST → service | `S/routes/canvasObjects.ts:51–82`；`S/services/canvasObjects.ts:1919–1930,2021–2035` | req.userId 与路由动作可提供受信任上下文；真正动作事务在 service 内。response 后的普通 Express middleware 已晚于提交，不能实现同生共死。 |
| 直接路由事务 | `S/routes/notes.ts:255–280,418` | route 持有 db.transaction 的内部是可接入点；目前仅候选，不为本段新接所有 note verb。 |
| 已有收据动作事务 | `S/routes/toolReceipts.ts:69–94` | read→guard→executor→mark 同事务是结构参考；现有 receipts 可变状态不是 events，且图二禁止 client undo 栈入钢。Agent 工具面接线留 V14，不在本段接入。 |
| 生命周期 | `S/services/courseLifecyclePolicies.ts:3–12,22–78` | 若新表加 user/course/object FK，需核对删除/SET NULL 是否会命中禁止 UPDATE/DELETE 触发器；仅登记问题，不替 HQ 选 FK 策略。 |

本单不实现 `recordEvent`、不设计认证态/通道传播接口、不把 caller 自报 actor/channel 当印章。

## 六 · 拆单建议

按既定单线 **单 1 → 单 2 → 单 3**：

- **单 1**：先交 events 的迁移、触发器、同事务 helper；冻结 fresh/legacy 等价和事件 FK 生命周期。最小改动在 `S/db/migrations/` 与一个 server helper、定向合成验证。十三 verb 按图二闭集，迁移/回滚先消费。
- **单 2**：先定通用排序与 object/mount 多 placement 的身份口径，再一次贯通 validator/service/read/client/列表。HQ 在侧栏与独立面板之间裁定；最小工程面见 §一/§二。不能只加 union 值。分蘖沿用真实身份事务，不能由两个独立请求冒充原子迁移。
- **单 3**：以上数据形态冻结后做双轨 dry-run：野地去处 + W/L/M 坐标解释；使用本节 census 作为前后同口径清单基座，明确每张原始表主键集合、字段哈希及 legacy 封存哈希。unknown/multi-frame/multi-stack/多 placement 单列人裁；50% 待实测校准，「允许溢出」未批时收编候选降托盘，不以等待为阻塞。

单 4 才实现三表备份、真实执行器/回滚及 A 向消费链切换；S4 A 表逐文件核销仍适用。单 5 才摘双模入口与 crossing 新写最后收尾。数据保全以三表各自主键集合和逐行核对为准，不能拿聚合总数相等代替内容/坐标/引用无损。

未做：产品代码/配置/依赖修改；用户库查询或真实 census；迁移执行/回滚；托盘 UI 或史记实现；S4 读写链重侦察；commit/push/PR/merge；`.env` 读取、任何 key 输出；浏览器与主观验收。此为只读侦察交付，不宣称通过产品版本的 `npm run verify:v2-bn8-runtime` 门或放行后续实施。
