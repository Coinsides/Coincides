> **状态 (Status)**: active(**实跑完成 2026-08-28**;口径冻结于实跑之前;⛔ 未宣布冠军,终审归 Henry)
> **层 (Layer)**: 分析 / Analysis(V12.9a 选品初赛 · 第 2 场 · 识别器)
> **日期 (Updated)**: 2026-08-28
> **权威 (Authoritative)**: 否(证据归档;终审归 Henry)

# V12.9a 第 2 场:识别器「出处携带」赛

## 0. 纪律(先写,免得跑完再对齐)

- ⛔ **不宣布冠军** —— 终审归 Henry。本单只归档:**每选手 × 每页的原始输出 + 打分表 + 花费**。
- ⭐ **口径冻结于实跑之前**(§2、§3)。**本节与打分口径写完并落盘之后,才造考卷、才发第一个 API 请求。**
- ⛔ **key 值不进任何落盘物**(产物 / 日志 / 本文件)。只从 `.env.experiment` 读,只记变量名。
- **花费上限 $20**(Henry 原话「上限内随便用」);**每次调用按 token 记账**,§5 汇总。
- ⛔ **Exp 类不可钉版模型不入正选**(总纪律)。
- ⭐ **第 1 场立的规照旧生效**:**候选之间同一维的测量深度必须相同**;**⛔ 不许看完结果再改口径**;**⛔ 不许两种参数都试取高分**。

## 1. 考卷(⭐ 本场的战场是「退化」,所以必须造对照)

**页源**:第 1 场同四卷官方样题(`D:/Coinsides/v12.9-selection/samples/`)。

**每页出两版,构成受控对照**:
| 版本 | 造法 | 作用 |
|---|---|---|
| **N · 原生页** | 200 dpi 直接渲染,⛔ 无退化 | **上界**:选手在干净图上的能力 |
| **S · 伪扫描页** | 150 dpi → 灰度 → **旋转 ±0.6°** → 高斯噪声 → JPEG q=72 | 战场:**退化对能力的削减量** |

⚠️ **「伪扫描」的已知局限,先写下来免得事后当成结论**:合成退化**不等于**真实拍摄/扫描件(无镜头畸变、无阴影、无折痕、无非均匀光照)。
⇒ **本场量的是「退化敏感度」,⛔ 不是「真实扫描件上的绝对表现」**。真件抽检是**后续独立事项**,本场不冒充。
📌 **合成的唯一好处也要说清**:N 版与 S 版**同一页**,且第 1 场已有该页的转写产物 ⇒ **有 ground truth**,准确率**可被量出**而不是目测。

**选页**(8 页,覆盖多题型;⛔ 不挑「好认的」):见 §4 实测表。

## 2. ⭐ 考题(两道,冻结)

### 题 1 · 题型识别(结构化产出)
给一页图,要求输出 **严格 JSON**:
```json
{"page_kind":"...", "tasks":[{"task_type":"...","question_numbers":"...","instruction":"..."}]}
```
`task_type` 从**闭集**选(⛔ 不许自由发挥):`matching_headings` / `true_false_notgiven` / `yes_no_notgiven` / `multiple_choice` / `gap_fill` / `table_completion` / `short_answer` / `essay_task` / `sample_answer` / `examiner_comment` / `toc` / `other`。

### 题 2 · ⭐ 出处携带(本场的真考题)
**每个 task 必须带 `bbox`**,归一化到 `[0,1000]` 的 `[x0,y0,x1,y1]`(左上原点)。
**判定方式与第 1 场同法 —— 独立裁剪回验**:把 bbox 换算回**原 PDF 坐标**,用 `pypdfium2`(**独立提取器**)裁原件取文本,检查模型给的 `instruction` 前 30 字是否命中(严格 / 归一两轮)。
⭐ **答对题型但指不到位置 = 题 2 不得分。** 理由:**碎片流要的是「可回到原件的答案」,不是「听起来对的答案」。**

## 3. ⭐ 打分口径(冻结于实跑之前)

| 维 | 2 分 | 1 分 | 0 分 |
|---|---|---|---|
| **F · 题型识别准确** | 闭集内选对且题号范围对 | 题型对、题号范围错/缺 | 题型错 |
| **G · 出处可回验**⭐ | 裁剪回验**命中** | bbox 存在但**指错位置** | **不给 bbox** 或格式不合 |
| **H · 格式服从** | 严格 JSON 一次过 | 需容错解析(带 markdown 围栏等) | 解析不出 |
| **I · 退化韧性** | S 版得分 **≥** N 版的 90% | 50%–90% | < 50% |
| **J · 文本侧结构化**(DeepSeek 档) | 碎片→结构化**且带回原碎片 `seq`** | 结构化但**丢来源** | 结构化失败 |

⚠️ **两条防自欺**:
1. ⛔ **空对空不算数** —— 某页若本就无可识别题型(如纯封面),该页 **F/G 记 N/A 不记 0**。
2. ⭐ **N 版与 S 版必须同 prompt、同参数、同解析器**;**⛔ 不许给 S 版加提示词补偿** —— 那量的就不是退化韧性了。

## 4. 实测记录

**选手**:`qwen-vl-max`(视觉档,DashScope **国际站** —— cn 端 401、intl 端通,如实记)· `deepseek-chat`(文本档)。
**产物**:`_out/c-trial2/`(仓外)—— `corpus/` 考卷 · `vision/` 题 2 · `vision2b/` 2b · `text/` 文本档 · `_grade.json`。

### ⚠️ 4.0 两次作废跑,存证不删

1. **corpus 缺陷**:选页用 `stem.startswith(前缀)`,而两卷同以 `ielts-academic-writing` 开头 ⇒ **三页取自错卷,其中两页是同一页的重复**。修法:**整名精确匹配 + 撞车/落空一律停不猜**。
   📌 那次重复是**意外的阳性对照**(同页两名喂入,答案近乎逐字一致 ⇒ 模型自洽非乱答),存 `_raw.PRE-CORPUS-FIX.json`。⛔ **意外不当方法用。**
2. **2b 首跑作废**:我的补丁脚本 `ValueError` **抛在写文件之前**,`run_trial2b.py` 实际仍是题 2 的老 prompt ⇒ 那 16 次调用是重复跑,**不是 2b**。存 `_VOID-duplicate-of-trial2.json`。
   ⭐ **判活方式**:`grep -c source_seq` = 6、`grep -c bbox` = 1(仅注释) —— **改没改由树上的证据说了算,不由脚本"跑完了"说了算。**

### 4.1 题 2(原考法:模型自报 bbox)—— **不及格**

| 项 | N 原生 | S 伪扫描 |
|---|---|---|
| task 数 | 6 | 6 |
| **G=2(裁剪回验命中)** | **1** | **1** |
| G=1(有框但指错) | 5 | 5 |
| 闭集外 | 1 | 1 |
| 平均最佳词覆盖 | 0.300 | 0.373 |

⚠️ **定罪前我先查了自己的尺**:同一套 0–1000 归一 / TOPLEFT 换算下,`academic-reading-p4` **命中且词覆盖 0.944** ⇒ **换算无罪,其余的偏是模型的**。
- 1 条**近失**(`reading-p18` 词覆盖 0.857,差在破折号与 2pt 裁切余量)、**4 条真指错**(词覆盖 **0.0**,框落在相邻块:指令写「Complete the table below.」而框圈住了表本身)。
- **格式纪律差**:`writing-responses-p4` **两版 JSON 都残缺**(`[148, 533, 848, 765}}]}` 缺一个 `]`)⇒ **H=0**;三处 task 用**重复 `bbox` 键**塞多个框(`json.loads` 只留最后一个)—— **我把被吞的全捞回来重测,仍不命中**。

### ⭐⭐ 4.2 2b(生产形态:图 + 转写器碎片清单,答案**引用碎片 seq**)

**改动只有一处**:prompt 里**删掉 bbox 字段**,换成 `source_seq`。⛔ 其余同图、同页、同参数、同判分器。

| 项 | 题 2(自报 bbox) | **2b(引用碎片)** |
|---|---|---|
| 出处正确 | **1 / 6** | ⭐ **6 / 9** |
| 出处指错 | 5 | 1 |
| **引用不存在的 id** | — | 2 |
| **JSON 一次过(obey=2)** | 14/16(2 次残缺) | ⭐ **16/16** |

⭐⭐ **本场最该带走的一条,不是分数变好了**:
> **引用制并没有消灭幻觉 —— 它把幻觉从「坐标不准」变成「引用了不存在的 id」。而后者是机械可查的。**

自报 bbox 时,一个错答案要**裁 PDF + 人眼看**才能判;引用碎片时,`seq in seqmap` **一行代码就判掉**。
⇒ **这不是模型能力的差别,是错误可判定性的差别** —— 与 Fable 裁的「理解轨引用真相层对象,⛔ 不自造事实」是同一条教义的实测面。
📌 **顺带的证据**:删掉 bbox 字段后,那两次 **JSON 残缺也消失了** —— 残缺发生在坐标数组里。**让模型少造一样东西,它连格式都稳了。**

**3 条未满分逐条查过**:
| 页 | 情形 | 归谁 |
|---|---|---|
| `listening-p30` | 引用 `seq=276`,**不在本页碎片内** | ⚠️ **该页 Docling 只供了 1 条碎片**(整张表塌成一条)⇒ **料不够,正确答案根本不在清单里**;⛔ 但模型**该弃权而非虚构 id** —— **两边都有账** |
| `writing-responses-p4` | 引用 `seq=53`,不在本页 | 同上(该页仅 4 条) |
| `writing-responses-p4` | 引用 `seq=54` **真实存在**,但 `instruction` 把后一段也接了进去 | **锚对、逐字违规** ⇒ G2b=1 |

⚠️ **碎片供给不足这件事,查到底是我的账**:`listening-p30` / `academic-writing-p2` 只有 1 条碎片,是因为 **Docling 的表格内容在 `.data` 而我的导出器只取了 `.text`** —— **与第 1 场 D 维那个同一个缺陷**(见第 1 场 §4.1)。
⭐ **同一个错误在两场里以两副面孔出现**:第 1 场是「表格得 1 分」,第 2 场是「识别器没料可引」。**这就是「同一条知识被独立重新发现两次 = 未机械化的证据」。**

### 4.3 文本档 `deepseek-chat`(J 维:碎片 → 结构化,须带回 `seq`)

**8 页全部 `obey=2`,J=2 者 13/13**(引用的 seq 逐条回验含所引文本)。
⚠️ **但其中 3 页页内只有 1 条碎片 ⇒ 引对是平凡正确,不算证据**;真正有意义的是 11 / 12 / 7 条碎片那三页 —— **那三页也全对**。
**耗时 0.85–1.91 s/页,花费 $0.0012 / 8 页。**

### 4.4 打分表

| 维 | `qwen-vl-max`(题 2 自报 bbox) | ⭐ `qwen-vl-max`(2b 引用制) | `deepseek-chat`(文本档) |
|---|---|---|---|
| **F · 题型识别** | **1.83**(6 页 2 分 · 1 页 1 分 · 1 页 N/A) | **同左**(题型判定两轮一致) | — |
| **G · 出处可回验** | ⛔ **1/6 命中** | ⭐ **6/9 命中,2 条可机械查出的虚构 id** | — |
| **H · 格式服从** | **1**(16 次里 2 次 JSON 残缺 + 3 处重复键) | ⭐ **2**(16/16 一次过) | **2**(8/8) |
| **I · 退化韧性** | ⭐ **2** —— **N 与 S 得分完全相同** | ⭐ **2** —— 同 | — |
| **J · 文本侧结构化** | — | — | ⭐ **2**(13/13 带回 seq 且回验通过) |

⚠️ **F 的 1 分那页是我的题面缺陷,不是它的错**:`academic-reading-p4` 真题型是**「条目→组别」配对**,模型答 `matching` —— **语义完全正确,但我的闭集里只有 `matching_headings`,没有通用 `matching`**。⛔ **我不事后改闭集抬分**(那是看完结果改口径);**如实记「扣分归我」**。

⚠️ **I 维那个满分,有两种读法,我不替它选**:①模型对退化确实稳;②**我的合成退化太轻**(±0.6° / q72 / 高斯 σ=6),根本没构成挑战。
📌 **可分辨的证据**:S 版**确实改变了输出**(题 2 里 S 把原件的拼写错 `statment` 自动纠成 `statement`,N 版则保留)⇒ **图确实变了、模型确实在看图**,但退化量不足以撼动结论。**⇒ 我倾向读法 ②,⛔ 不把 I=2 当作「抗真实扫描件」的证据。**

## 5. 花费记账

| 轮次 | 模型 | 调用 | input tok | output tok | 估算 USD |
|---|---|---:|---:|---:|---:|
| 首轮(**作废**,corpus 缺陷) | qwen-vl-max | 16 | 24,472 | 3,244 | 0.0599 |
| 题 2 | qwen-vl-max | 16 | 24,552 | 2,138 | 0.0530 |
| 2b 首跑(**作废**,我的补丁失败) | qwen-vl-max | 16 | 24,552 | 2,181 | 0.0532 |
| 2b | qwen-vl-max | 16 | 26,492 | 1,875 | 0.0544 |
| 文本档 | deepseek-chat | 8 | 2,371 | 475 | 0.0012 |
| **合计** | | **72** | | | ⭐ **≈ $0.222 / 上限 $20** |

⚠️ **两次作废跑共 $0.113 —— 占总花费的一半,如实计入,⛔ 不从分母里摘出去。**
(单价按 `qwen-vl-max` $1.60/M in · $6.40/M out、`deepseek-chat` $0.27/M in · $1.10/M out **估算**;⚠️ 非账单实数,**账单为准**。)

## 6. 结论与承接

⛔ **不宣布冠军**(终审归 Henry)。本场归档三条可行动的结果:

1. ⭐⭐ **管线形态的裁定,已有实测支撑**:识别器**不应自报坐标**。同一模型、同一页、只把 `bbox` 换成 `source_seq`,**出处正确率 1/6 → 6/9,JSON 残缺归零**。价值的核心是**错误从不可判变成机械可判**。
2. ⚠️ **引用制不是免死金牌**:仍会**虚构不存在的 id**(2/9)⇒ **入库处必须有一道 `seq ∈ 本页碎片集` 的闸**,⛔ 不靠模型自觉。
3. ⚠️ **本场未取证的**:真实扫描/拍摄件(合成退化不算)· 纯图场景(图表/手写,转写器无能为力时才轮到 VLM 直出坐标)· `qwen-vl-max` 之外的视觉选手。

**承接**:碎片导出器的 `.data` 缺陷(§4.2)**两场各咬一次,应机械化** —— 建议随 12.9c T0 首单一并处置(与 TD-29 同落点)。
