> **状态 (Status)**: active
> **层 (Layer)**: 分析 / Analysis（K-0 只读侦察）
> **日期 (Updated)**: 2026-09-07
> **权威 (Authoritative)**: 否；记录现物与拆单建议，单 1/2 的冻结及放行归 HQ
> **取代 (Supersedes)**: —
> **被取代 (Superseded by)**: —

# V13.1 · K-0 纸的视口侦察

依据：完整阅读 `docs/agent-ops/handoffs/2026-09-07-v13-1-k0-recon-order.md` 与 `docs/agent-ops/handoffs/plans/v13-1-paper-viewport-plan.md`，并核对 Agent 入口、方向宪章、current-state、active ADR-0001 及上游纸视口设计。侦察基线为工作分支 `fable/v2-bn12-exoskeleton`、HEAD `01f6b86` 的当前工作树；不是洁净提交快照。

**结论先行：page 目前没有应用级自由缩放；文字度量主链已与 zoom 解耦；print profile 已进入几何模型，但没有找到最终 PDF 导出链，也不能把当前数值 1 称为已成立的“100% 物理”。** 单 1 是新增 page 阅读缩放通道；单 2 的实质是双族 profile 选择与传递补齐。五问证据如下。

证据路径约定：下文 **`E/` 完整展开为 `client/src/pages/Notes/canvasEngine/`，`N/` 为 `client/src/pages/Notes/`**；例如 `E/geometry.ts:20–21` 即仓库文件 `client/src/pages/Notes/canvasEngine/geometry.ts` 第 20–21 行。行号均取本次工作树原文件，范围包含端点。

检索射程与限制：

- 根目录有 `.codegraph/`；当前可调用工具中未发现 CodeGraph，执行 `codegraph explore 'CanvasViewport zoom consumers and rendering measurement hit testing'` 返回命令不存在。未安装、未索引、未修改其目录。`rg` 亦不可用，退用 PowerShell 的定向枚举、`Select-String` 与带行号读取。
- 对 `client/src`、`server/src`、`shared` 内 `.ts/.tsx/.js/.jsx/.mjs/.cjs/.css/.sql` 共 **544 个文件**执行不区分大小写的 `\bzoom\b` 搜索，得 **71 个命中行**（含类型、写入、测试、旧服务，不能当作 71 个现役消费者）；再追 `CanvasViewport`、`viewportTransform`、坐标转换、runtime viewport 及测量/导出调用链。直接读取 `.zoom` 的现役引擎实现集中在下表五个文件：geometry、viewportService、两个交互 hook、NoteWritingSurfaceLayer。
- 最终导出入口另定向检查 `client/src`、`server/src`、`shared`、`scripts` 的 TS/TSX/JS/MJS/CSS。未扫描依赖、构建产物、历史研究或私人数据来补造产品结论；“未找到”仅适用于该射程。
- 全程静态只读取证；没有浏览器复现、build、typecheck、测试或运行时验证。以下明确区分代码事实与推导风险，不申报功能验收通过。

## 一 · CanvasViewport.zoom 消费者全表

四类按消费目的归档；同一转换函数的独立用途分别列出。仅传递整个 viewport 的中间层及未接活入口的 helper 在表后说明。

| 类别 | 消费者 / 调用链 | 文件:行号证据 | 实际作用与适用面 |
|---|---|---|---|
| 渲染变换 | `transformedWorldStyle` → blockList style | `E/layers/NoteWritingSurfaceLayer.tsx:1130–1136,3311–3320` | 仅 canvas 执行 `translate(-x*zoom,-y*zoom) scale(zoom)`；page 返回空对象。 |
| 渲染变换 | `worldToScreen` → `worldRectToViewportRect` → 块控制条 | `E/geometry.ts:18–22`；`E/overlayService.ts:94–110`；`E/layers/NoteWritingSurfaceLayer.tsx:2471–2487` | 世界点减 viewport 原点后乘 zoom；canvas 的世界布局盒转屏幕浮层锚点。page 另走未缩放加法（同层 2490–2497）。 |
| 渲染变换 | `viewportToWorldRect` → `getVisibleBlockIds` → runtime 诊断 | `E/geometry.ts:167–173,186–194`；`E/engineModel.ts:469`；`E/layers/NoteWritingSurfaceLayer.tsx:3287` | 可视宽高除 zoom 后加 overscan，生成可见 block ID。现有引用终点是 runtime 字段及 DOM 可见数量属性；未发现其直接裁掉实际 block 渲染，不应称为已工作的虚拟化裁剪器。 |
| 文本与布局测量 | **纯文字基线/换行/行高/DOM 高度主链：零直接消费者** | `E/typographyMeasurementService.ts:12–19,33–89`；`E/measurementService.ts:47–73,90–111`；`E/hooks/useCanvasContentWidth.ts:21–45` | 字宽、行高由 profile，宽高由布局尺寸产生；不输入 viewport/zoom。完整正证见第二问。改宽操作的估高是下一类的间接下游。 |
| 命中判定 | `viewportPointToWorldPoint` → canvas 插入位置/所属纸框 | `E/viewportService.ts:221–228`；`E/layers/NoteWritingSurfaceLayer.tsx:1300–1314,1456–1458,1493–1495,1540–1541,1596–1598` | 屏幕点除 zoom 再加 viewport 原点；用于 shape/table/image/sticky-note 落位及所属 frame 判断。 |
| 命中判定 | `viewportPointToWorldPoint` → frame 右键命中、空白拖放 | `E/layers/NoteWritingSurfaceLayer.tsx:2632–2649,3210–3225`；`E/viewportService.ts:221–228` | 两条调用都有 canvas 分支；frame 做矩形命中，drop 生成世界坐标布局。 |
| 命中判定 | `hitTestCanvasScene` → 上述转换 | `E/canvasRuntimeKernelService.ts:108–122` | runtime viewport 转世界点后按反向 z-order 命中 placement；当前产品源码只定位到函数定义，未找到实际调用入口。保留能力，不冒充 UI 已接线。 |
| 命中判定 | `screenToWorld` | `E/geometry.ts:25–29` | 与 viewportPointToWorldPoint 同类的直接除 zoom helper；本次 client/shared 引用搜索仅见定义，无活调用。 |
| 命中判定 | `beginMoveBlock` 的 pointer delta | `E/hooks/useBlockPlacementInteractions.ts:127–164`，直接读点 145–147 | canvas 位移除 zoom；page 固定按 1；还原后的布局位移进入拖动、吸附、碰撞策略。 |
| 命中判定 | `beginResizeBlock` → `calculateResizedBlockLayouts` → `estimateHeight` | `E/hooks/useBlockPlacementInteractions.ts:213–243`，直接读点 229–230；`E/interactionController.ts:209–230` | canvas 屏幕 delta 除 zoom 得世界宽度增量，主动改宽后估高。是交互坐标补偿，不能为“测量零 zoom”删除；不是将字宽/行高随 zoom 改写。 |
| 命中判定 | `handlePageSpaceDoubleClick` | `E/hooks/useCanvasSurfacePointerController.ts:44–59`，直接读点 47–49 | 从已变换 blockList 的屏幕原点求局部坐标；canvas 除 zoom，page 固定 1；随后创建空白草稿。 |
| 命中判定 | 纸框拖动/resize 的 `getPageFrameOperationDelta` | `E/layers/NoteWritingSurfaceLayer.tsx:1179–1185,1191–1192,1228–1229,1258` | 两轴屏幕位移除 zoom；操作入口限定 canvas。 |
| 命中判定 | shape 拖动/resize 的 `getShapeOperationDelta` | `E/layers/NoteWritingSurfaceLayer.tsx:1832–1837,1845,1912` | 两轴屏幕位移除 zoom，进入 shape 世界几何操作。 |
| 其他 | `focusViewportOnWorldRect` | `E/viewportService.ts:111–141`，直接读点 123 | 保持缩放、可视宽高除 zoom 后算居中/留白的平移。hook 入口 `E/hooks/useViewportTransformController.ts:74–82`。 |
| 其他 | `clampViewportToWorld` | `E/viewportService.ts:144–167`，直接读点 150 | 限定 zoom；由可视世界宽高决定 pan 边界，非正文排版度量。 |
| 其他 | `panViewportByViewportDelta` / `scrollViewportByViewportDelta` | `E/viewportService.ts:170–191`，直接读点 177–178、189–190 | 屏幕平移量除 zoom，分别减/加到 viewport 世界原点，再 clamp。 |
| 其他 | `zoomViewportAtViewportPoint` | `E/viewportService.ts:194–218`，直接读点 209–210 | 旧 zoom 求指针锚定世界点，新 zoom 反算原点，写新 zoom；用于保持缩放锚点。 |
| 其他 | 快捷键、wheel、± nudge 的下一个 zoom | `E/layers/NoteWritingSurfaceLayer.tsx:1005–1017,1028–1064,1953–1985` | canvas Ctrl/Meta ± 为 ×1.12/0.88；wheel 为 ×1.08/0.92；nudge 乘因子。1064 为依赖数组再读，不是另一种算法。 |
| 其他 | 百分比、滑块、Reset、诊断属性 | `E/layers/NoteWritingSurfaceLayer.tsx:1973–1985,3298,3826–3868` | 百分比由 zoom×100；滑块输入转比例，Reset 走 controller。控件仅 canvas；3298 读的是派生 runtime viewport，page 会显示 1。 |

转递/写入闭合：`E/types.ts:125–129` 定义 zoom/min/max；`E/engineModel.ts:103–113`、`E/viewportService.ts:42–68` 创建默认值并拷贝输入；`E/hooks/useViewportTransformController.ts:32–99` 持有并更新 viewport，`E/hooks/useRuntimeSurfaceStateController.ts:117–125,185` → `E/hooks/useNoteCanvasRuntimeController.ts:77,298,403` → `E/hooks/useNoteCanvasLayerProps.ts:187` / `E/hooks/useRuntimeFrameModelController.ts:52–71` → 布局模型及显示层转递它。`E/hooks/useNoteCanvasLayoutModel.ts:234–255,281` 将其注入 runtime，page 另经 `E/pageFrameService.ts:123–138` 覆盖为 zoom=1。转递本身没有额外缩放算法。

另外两个有名出口：`E/viewportService.ts:231–235` 的 `worldPointToViewportPoint`、`E/overlayService.ts:113–118` 的 `createWorldOverlayAnchor` 转递到渲染转换，本次 client/shared 引用搜索均仅见定义。上述未接入口 helper 仍纳入消费者盘点，但与活 UI 区分。

排除同名：`server/src/services/learningCanvases.ts:790–833` 的 zoom 属旧 LearningCanvas 表通道，见第五问；`E/hooks/useBlockPlacementInteractions.test.tsx:100`、`E/layers/NoteRuntimeDocumentLayer.test.tsx:60`、`server/src/__tests__/v2MaterialLibrary.test.ts:1868–1875` 是测试输入/断言；schema 与 migration 是存储声明，均非当前 Notes 渲染消费者。类型、默认写入及 JSX 属性中的 zoom 字面也已包含在上述 71 行的分类说明中。

## 二 · 文本测量与 zoom 的现有耦合

**申报“已解耦”的范围：现役纯文字估算、布局宽度采集、DOM 高度读回及高度回写主链。** 这不是“所有文字交互都已正确”的申报，也不以函数签名零命中独自代替调用链证据。

| 测量族 | 调用链与文件:行号正证 | 结果 |
|---|---|---|
| 平均字宽与行高基线 | `E/typographyProfileService.ts:112–151` 由字号×0.48 派生字宽并归一 profile → `E/typographyMeasurementService.ts:33–34,45–65` | 输入来自 profile；换行数由文字/布局宽度/字宽，height 由行数×行高+段距等；无 zoom。 |
| block 高度与初始布局 | `E/hooks/useNoteCanvasLayoutModel.ts:92–95,121–141` 用当前 profile 建估高闭包 → `E/measurementService.ts:90–111,58–73` → `E/typographyMeasurementService.ts:37–78` | 当前 profile 进入默认布局和归一布局；viewport 不参与估高。 |
| 布局宽度 | `E/hooks/useRuntimeLayoutModelController.ts:43–69` → `E/hooks/useCanvasContentWidth.ts:21–45` → 上述布局入口 | 读 blockList `clientWidth`，由 ResizeObserver/window resize 重读，未读缩放后的屏幕 bbox。保持这一边界是单 1 的义务。 |
| textarea/内容 DOM 高度 | `E/layers/BlockEditorLayer.tsx:250–257` → `E/hooks/useBlockMeasurement.ts:31–59`（初测、RAF、ResizeObserver）→ `E/measurementService.ts:47–55` | textarea 与 block 都读 `scrollHeight`，后者加 chrome；未乘/除 zoom，未以 getBoundingClientRect 高度作布局高度。 |
| DOM 高度回写 | `E/layers/NoteWritingSurfaceLayer.tsx:3782` → `E/hooks/useMeasuredBlockReflowController.ts:38–59` → `E/hooks/useLayoutDraftController.ts:40–58` → `E/measurementService.ts:114–146` | measuredHeight 原样进入布局高度及 reflow/collision；此链也无 zoom。不能新增“除 zoom”来修一个不存在的乘数。 |
| 页栈续页估高 | `E/hooks/useRuntimeNaturalWritingController.ts:379–391` → `E/pageStackContentFlowService.ts:106–120` | 输入 draftLayout.width 与当前 profile，不是可视宽度。 |
| 预览页行容量 | `E/exportPreviewService.ts:132–137` → `E/typographyMeasurementService.ts:81–89` | `floor(contentHeight / lineHeightPx)`；无 viewport。 |
| 改宽后估高的间接依赖 | `E/hooks/useBlockPlacementInteractions.ts:229–242` → `E/interactionController.ts:209–230` | zoom 只用于把用户实际拖动换算成新世界宽度。该有意改宽不等于切档改变字宽/行高，归第一问命中/交互类。 |

另有三处相关问题，不能误记为上述测量耦合：

1. **profile 漏传（现物）**：resize 的 `E/hooks/useBlockPlacementInteractions.ts:242` 调 `estimateBlockHeightForText(block,text,width)` 未传当前 profile，沿 `E/measurementService.ts:90–103,58–65` 回落默认值。双族接入时应补齐；与 zoom 无关。
2. **文本右键命中坐标风险（源码推导，未复现）**：`E/blocks/TextBlockProjection.tsx:379–443` 中 mirror 用 `clientWidth`（390），`localPoint` 却只做屏幕点减 `getBoundingClientRect` 原点再加 scroll（422–425），随后与未缩放 mirror 字符坐标比较（430–436）。入口在 675–685，结果用于 draft/annotation 命中（700–708、723–728）。canvas 已有 transform，page 新增 transform 后也会受此假设影响。
3. **caret 浮层坐标风险（源码推导，未复现）**：`E/overlayService.ts:196–247` 的 mirror 按 offsetWidth/Height 测局部偏移（219–220），直接加到屏幕 elementRect（234–238）；`getSlashMenuAnchor`（250–269）消费该结果。局部偏移没有按实际呈现比例投影。反之 `E/blocks/TextBlockProjection.tsx:343–376,591–595` 的局部 badge 锚点仍处在局部坐标，不应一律加 zoom。

双族 profile 尚未实现的证据：

- `E/types.ts:73–80` 仅有 px 字段；`E/typographyProfileService.ts:19–26` 默认字号 **15px**、行高 **22px**、段距 **0**、平均字宽 **7.2px**，不是 plan 的纸族 11pt / 网页族 16px。
- `E/types.ts:55–61` 只有 A4/Letter/Custom 与四个 template ID；`E/pageFrameTemplateService.ts:56–76,86–118` 的模板全部使用同一默认 typography token。screen_note 与 custom 均为 Custom，前者不可导出、后者可导出；没有现役 custom-web 身份，不能仅凭 pageSize=Custom 决定网页族。
- `E/engineModel.ts:149–187,376–381` 将同一个 note profile 放进全部 frame extension；token 只是记录，不驱动选择。`E/pageFramePrintScaleService.ts:55–65` 仅归一传入 typography，也没有随纸型选族。
- 真正字体 CSS 出口是 `E/typographyProfileService.ts:209–218` 的 `documentTypographyToCssVars`；`E/pageFrameTemplateService.ts:176–187` 的 `pageFrameTemplateToCssVars` **只输出背景/边框/阴影/网格**。`E/layers/NoteWritingSurfaceLayer.tsx:700–706,3311–3315,3345–3347` 分别消费二者，后一个调用甚至只传 background，尚无模板与字体汇合机制。
- profile 现按 note metadata 保存一个 `activeProfile`：`E/typographyProfileService.ts:169–190`，hydration 在 `E/hooks/useNoteCanvasDataAdapter.ts:598`，乐观写入/PUT metadata/失败回滚在 935–966。双族选择需明确与现有用户 profile 的优先级，不能默认覆盖。

## 三 · 导出 scale 来源及屏显同源性

**已走 print profile 的是页面模板/几何基线；屏显与 Export Preview 的几何、排版输入同源。最终 PDF 导出 scale 本次未定位到，无法申报屏显与最终导出同源。**

| 环节 | 文件:行号证据 | 能证明什么 |
|---|---|---|
| print profile 定义 | `E/types.ts:82–89`；`E/pageFramePrintScaleService.ts:20–65` | profile 有宽高/inset/内容盒/typography，无输出 scale、DPI、单位字段。A4、Letter **宽都为 760+72×2=904**，高分别按 √2、11/8.5 比例计算；这是内部布局基线。 |
| 已存在生产调用 | `E/engineModel.ts:63–67`；`E/pageFrameTemplateService.ts:56–76,112–114` | 默认引擎尺寸、A4/Letter 模板、custom 默认几何确实调用 `createPageFramePrintProfile`，不是只定义未用。 |
| runtime 归一化 | `E/engineModel.ts:354–355`；`E/pageFrameCollectionService.ts:60–66` → `E/pageFramePrintScaleService.ts:68–85` | 非 Custom 路径调用 print profile；仅无 pageSize 时应用 preset 宽/inset，已有宽度可以保留，高度至少为 profile 高；Custom 提前返回。不能推断所有存量 frame 都是统一 preset 尺寸。 |
| Preview 接线 | `E/hooks/useNoteCanvasLayoutModel.ts:284–292` → `E/exportPreviewService.ts:105–145,192–200,241–246` | 输入 runtime frame/stack/placement 与 documentTypography；输出边界分类、内容盒和估计行容量，没有 viewport/zoom/输出 scale。 |
| Preview 的真实终点 | `E/layers/ExportPreviewLayer.tsx:21–31,101–109,303` | 组件只接预览数据/开关/关闭并展示统计；303 明示它不是最终 PDF export engine。 |
| 屏显排版与尺寸 | `E/hooks/useNoteCanvasLayoutModel.ts:243–255` → `E/engineModel.ts:149–187,376–381` → `E/layers/NoteWritingSurfaceLayer.tsx:695–706,3311–3319`；`N/NoteDetail.module.css:1773–1776,3893–3896` | 同一 profile 进入 frame extension，再由 CSS vars 输出；屏幕与 Preview 的输入同源，不等于光栅输出一致。 |
| 屏显缩放 | `E/layers/NoteWritingSurfaceLayer.tsx:1130–1136`；`N/NoteDetail.module.css:1021–1030,1131–1138` | canvas 有视口 transform；page 是响应式 760 内容宽与外层 padding 的 DOM 表面，还不是按物理纸框整体呈现的等比视口。 |

静态链条：`createPageFramePrintProfile → createPrintTemplate / normalizePageFramePrintBaseline → runtime pageFrames → 屏显几何 / buildExportPreviewModel`；`documentTypographyProfile → runtime frame extension → documentTypographyToCssVars`，同一个 profile 又进入 `buildExportPreviewModel → estimatePageFrameLineCapacity`。**两条链的可证终点止于屏显与预览。**

在声明射程内搜索 `html2canvas/jsPDF/window.print/page.pdf/printToPDF/@media print/exportNote/export_note/pdfExport` 等未找到最终输出实现；未调用外部软件或浏览器打印。图一 `docs/agent-ops/analysis/2026-08-31-paper-viewport-design.md` §二“createPageFramePrintProfile 已按 96dpi A4 约794×1123 建模”的前提与上述 904 基线不符。HQ 须冻结内部几何到物理呈现/输出的映射；不能将 zoom=1 换一个标签就视为比例病已解决，也不能把单 3 当作已有 PDF 引擎上的纯回归单。

## 四 · page/canvas 控制器分叉及单 1 最小切口

**状态层的最小切口在 `E/hooks/useRuntimeSurfaceStateController.ts:104–125` / `E/hooks/useViewportTransformController.ts:23–41`；但最小可用改动跨状态、page 派生 runtime、DOM 呈现及输入坐标边界。只改 hook 的 zoom 不会让 page 缩放。**

| 分叉/入口 | 文件:行号证据 | page / canvas 现状 |
|---|---|---|
| 初始化、模式切换、Reset | `E/hooks/useViewportTransformController.ts:32–41,84–89`；`E/viewportService.ts:42–68` | 共用数值 state；模式变化重新生成种子，两模式初始均 1。 |
| page runtime | `E/hooks/useNoteCanvasLayoutModel.ts:234–253` → `E/pageFrameService.ts:123–138` | page 以主框内容盒定位并强制 zoom=1；并非屏幕真实缩放状态。 |
| 尺寸观察 | `E/layers/NoteWritingSurfaceLayer.tsx:977–985` → `E/hooks/useViewportTransformController.ts:43–49` | 仅 canvas 更新 viewport 像素尺寸；page 另有 987–1003 的居中 observer，使用 appMain 与 window.innerWidth。 |
| 快捷键与手势 | `E/layers/NoteWritingSurfaceLayer.tsx:1005–1064,1138–1176,1953–1970` | Ctrl/Meta±/0、space/middle pan、应用 wheel zoom 全是 canvas 分支；page 未启用这些应用入口。浏览器自身缩放不等于 CanvasViewport.zoom，本单未实测浏览器行为。 |
| 控件 | `E/layers/NoteWritingSurfaceLayer.tsx:1973–1985,3826–3868`；`E/engineModel.ts:84–85` | canvas slider 40–180、step=5；引擎 clamp 0.45–2.4。不能为 page 50–200 步进修改这些 canvas 公共值。 |
| 程序性 focus | `E/hooks/useRuntimePresentationController.ts:92–172,185–211` → `E/hooks/useNoteCanvasRuntimeController.ts:426`；`E/hooks/useRuntimeNaturalWritingController.ts:373–414` → 同 controller 306 | 纸框操作/跨页续写也会调用 focus，无 page guard；最终经 viewport hook 74–82 保持 zoom 改平移。page 的派生层与 DOM 目前又覆盖/忽略它，新增档位时须明确其语义。 |
| 页面滚动容器 | `E/modePolicyService.ts:83–94`；`E/NoteCanvasRuntime.tsx:11–21`；`client/src/styles/global.css:29–36`；`client/src/components/Layout/AppLayout.tsx:214–224` | page 使用 app 主滚动容器；canvas 才锁 app 滚动并用视口平移。 |

单 1 必须守住两条边界：

1. **外层适配显示、内层保持布局宽度。** `N/NoteDetail.module.css:1131–1138` 的 page 内容当前为 `min(100%,760px)`；`E/layers/NoteWritingSurfaceLayer.tsx:3281–3283` 的 blockListRef → `E/hooks/useRuntimeLayoutModelController.ts:43–69` → `E/hooks/useCanvasContentWidth.ts:23–40` 把 clientWidth 送入默认/归一布局与估高（`E/hooks/useNoteCanvasLayoutModel.ts:122,128–140,153–155`）。因此不能用切档结果重设这个内层的布局宽度。建议固定未缩放纸内盒，外层从真实可用区求 fit 比例并管理显示占位/滚动范围；具体纸内盒来源待物理基线冻结。
2. **page 显示比例必须在输入/叠层边界反映。** 第一问的拖动/resize、双击目前固定 page=1；空白 drop `E/layers/NoteWritingSurfaceLayer.tsx:3228–3234`、块控制条 2490–2497 也直接加减未缩放坐标；第二问的文本命中与 caret 浮层存在同类风险。新增 page transform 时需处理这些 page 路径，保持纯测量签名无 zoom。不能为了省事放开 canvas 分支、继承其全部手势，或顺带改 canvas 既有行为。

## 五 · 三档状态天然落点与持久化通道

| 通道 | 存哪、何时写 | 作用域与证据 |
|---|---|---|
| **现役 Notes viewport** | `useViewportTransformController` 的 React `useState`；初始化、mode effect、resize/pan/scroll/zoom/focus/reset 时 setState，无 API/storage 写入 | `E/hooks/useViewportTransformController.ts:23–99`；调用只传 surfaceMode（`E/hooks/useRuntimeSurfaceStateController.ts:117–125`）。**组件实例内存态，不是按 note 持久化，也不是 sessionStorage 会话保存。** |
| 模式与 note 生命周期 | 模式 state 有 noteId，note 变化重置 page，hydration 后可按内容转 canvas；viewport effect 却只依赖 surfaceMode | `E/hooks/useSurfaceModeController.ts:40–62,64–106`；`E/hooks/useViewportTransformController.ts:36–41`。`N/NoteDetail.tsx:5–11` 未用 noteId 强制 remount，`E/NoteCanvasRuntimeProvider.tsx:9–20` 只给 context；`E/NoteCanvasRuntime.tsx:8,23–29` loading 隐藏显示层时 controller 仍调用。**源码支持“可能跨同模式 note 导航留存”，不支持“每 note 已保存/必定重置”**；卸载/刷新会重新初始化。 |
| 现役 NoteCanvas 对象持久化 | GET 按 note 加载对象/placement/frame 等；纸框另有保存 | `E/canvasPersistenceNormalizer.ts:36–45` payload 无 viewport；`E/canvasObjectRepository.ts:52–83`；`E/hooks/useNoteCanvasDataAdapter.ts:911–933`。存纸框几何不等于存视口。 |
| **旧 LearningCanvas 持久化底座** | SQLite `canvas_viewport_states` 存 x/y/zoom/metadata；调用 `updateCanvasViewport` 才立即 UPSERT；无记录读为 0/0/1 | `server/src/db/schema.sql:928–941`，外键 learning_canvases、唯一 `(user_id,canvas_id)`；`server/src/services/learningCanvases.ts:84–89,790–833`。按用户+LearningCanvas，不按 note/session。 |
| 旧底座的现役接线 | 在 client/src、server/src/routes、shared 未找到 get/updateCanvasViewport 或该表的调用；update 除定义外定位到测试 | `server/src/__tests__/v2MaterialLibrary.test.ts:1868–1875`；服务定义见上一行。**不能称为 Notes 已有 viewport 恢复通道。** |

**建议而非已实现：** 阅读档位应放在 runtime surface state 下、与 viewport controller 同层的 **page 专用视图状态**。保存语义档位（适合宽/适合整页/物理100%）及手动步进值，实际显示比例由纸框与外层可用区派生。`E/types.ts:125–129` 当前只有数值 zoom/min/max，单存 zoom 不能恢复 fit 语义。将 noteId 显式纳入作用域可避免当前同模式导航残留；若 HQ 要求跨刷新按 note 恢复，还需另冻结持久化位置与写入时机，现物没有这条现成通道。最小单 1 可先采用组件内、按 note 作用域的 page 状态，不必接旧 LearningCanvas 表或改块/纸框真相。

## 拆单建议

### 需 HQ 冻结的现物冲突

1. **“替换 page 自由缩放”应改为“新增 page 阅读档位呈现”。** 现物三重分叉为 page 无 transform、应用手势仅 canvas、page runtime 强制 1（第四问）。这是范围描述修正，不是本单擅自放行施工。
2. **物理基线尚未成立。** 图纸假定的 96dpi A4 基线与 904 内部宽不符，且存量 frame 可以保留自定义几何（第三问）。须先定内部布局单位与物理100%/输出比例的映射、适合整页的目标 frame 与内容高度；不能在单 1 中悄悄改写已存 page_frame_local 几何。适合档是否受手动步进 50–200% 限制、长页退化规则亦应保持一处定义。
3. **双族选择与既有 profile 的优先级待定。** custom-web 没有现役身份，Custom 同时覆盖 screen_note/custom；现有 note 单一 activeProfile、各 frame 共用，pageFrameTemplateToCssVars 尚未投影字体（第二问）。须明确模板默认、用户覆盖、混合纸框的处理及 canvas 行为不变边界。
4. **单 3 有缺失的前置工程。** 本次未找到最终 PDF 输出链；“100%屏显光栅 vs 导出PDF光栅”还不能作为已有导出器上的纯回归。HQ 须指定实际输出入口或补立前置工作，不能以 Preview 同源冒充最终保真。

### 单 1 / 单 2 顺序

**建议先冻结共同的纸框单位/布局宽/profile 接口，再顺序集成；纯函数部分可以有限并行，不建议两单同时改主显示层。** 度量主链已经解耦，因此不存在“必须先删除测量 zoom 才能做档位机”的依赖；真正共享的是固定内层布局盒、有效 profile 和主层接线。冻结后可并行做单 1 的档位/fit 计算与单 2 的 profile 选择；集成建议 **单 2 的有效 profile 与基线输入先接稳，再接单 1 的 page 显示/交互，最后单 3**。`E/hooks/useNoteCanvasLayoutModel.ts`、`E/layers/NoteWritingSurfaceLayer.tsx` 等公共接线由一个施工者顺序整合。若 HQ 另定物理基线归属，须先调整这个序列，不应让施工者猜测。

### 各单最小改动面（建议清单，未实施）

| 单 | 最小涉及文件/层 | 应交付的边界 |
|---|---|---|
| 单 1：状态与纯派生 | `E/hooks/useRuntimeSurfaceStateController.ts`、`E/hooks/useViewportTransformController.ts` 同层的 page 状态；`E/types.ts`；`E/viewportService.ts` 的 page 计算入口（或同层独立 helper） | 语义三档/步进、note 作用域、外层可用尺寸→显示比例；保留 canvas 公共 clamp/手势。持久化若无额外要求不扩 server。 |
| 单 1：runtime/显示 | `E/hooks/useNoteCanvasLayoutModel.ts`、`E/pageFrameService.ts`、`E/hooks/useNoteCanvasLayerProps.ts` 及 runtime 转递；`E/layers/NoteWritingSurfaceLayer.tsx`、`N/NoteDetail.module.css` | page 派生 viewport 与实际呈现一致；固定内层盒，外层缩放和滚动占位；fit 不反灌 clientWidth→正文布局。程序性 focus 与居中也应适配。 |
| 单 1：坐标边界 | `E/hooks/useBlockPlacementInteractions.ts`、`E/hooks/useCanvasSurfacePointerController.ts`；显示层 drop/control anchor；`E/blocks/TextBlockProjection.tsx`、`E/overlayService.ts` 的相关 page 显示坐标边界 | page 显示缩放后的拖动、命中与浮层映射闭合，保持 canvas 行为；不向纯文字测量签名塞 zoom。 |
| 单 2：profile 选择/归一 | `E/typographyProfileService.ts`、`E/types.ts`、`E/pageFrameTemplateService.ts`、`E/pageFramePrintScaleService.ts` | 纸族/网页族与用户覆盖优先级；沿用 DocumentTypographyProfile。11pt 到内部度量的换算与物理基线统一，不能只换 CSS 单位字符串。 |
| 单 2：传递与既有存储 | `E/engineModel.ts`、`E/hooks/useNoteCanvasLayoutModel.ts`、`E/hooks/useRuntimeNaturalWritingController.ts`、`E/hooks/useBlockPlacementInteractions.ts`；需要保存选择时复用 `E/hooks/useNoteCanvasDataAdapter.ts` 既有 profile metadata 接线 | 有效 profile 一致进入屏显、估高、续页、Preview；补 resize 估高漏传，不能继续所有模板同默认 token 却宣称双族绑定。 |
| 单 2：CSS 汇合口 | `E/layers/NoteWritingSurfaceLayer.tsx`、`E/pageFrameTemplateService.ts`、`E/typographyProfileService.ts` | 若按 plan 让模板 CSS helper 汇合字体，扩展其身份/profile 入参及只传 background 的调用；继续复用 documentTypographyToCssVars，不造第二套排版真相。 |

`E/typographyMeasurementService.ts` 与 `E/measurementService.ts` 的现役无 zoom 算法无须为“解耦”重写；单 2 应审计有效 profile 传递，而单 1 的 K-档位应证明切档不改内层宽度与任意块 page_frame_local 几何。后续每单按已冻结工单执行 typecheck/build+一条功能冒烟；本 K-0 不执行、不替后续申报通过。

未做清单：未改产品代码、状态/权限配置或其他文档；未改工单历史正文或 status；未执行 build/typecheck/测试/浏览器/导出/数据查询；未读取 `.env` 或凭证材料、未输出密钥；未 commit/push/PR/merge。开工已有 `server/src/routes/projections.ts` 修改及三项未跟踪材料，均未触碰。本单写入范围仅本报告与指定工单末尾 `## Result`。
