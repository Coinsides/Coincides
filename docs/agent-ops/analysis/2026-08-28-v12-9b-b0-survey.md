> **状态 (Status)**: active(b-0 只读核实,2026-08-28)
> **层 (Layer)**: 分析 / Analysis(V12.9b 段 · b-0 产出)
> **权威 (Authoritative)**: 否(现物快照;裁定归 Fable/Henry)

# b-0 现物核实:碎片流落库前的地基清点

**方法**:只读 grep + 迁移文件原文 + 写入/读取点清点。⛔ 零改动。
⚠️ **按家法「有表 ≠ 有能力」**,每张表都查了**写入点与读取点**,不以「表存在」当能力。

## ⭐ 结论摘要(给 b-1 的裁定材料)

| # | 问题 | 现物答案 |
|---|---|---|
| ① | 碎片流落表还是文件 | **表已存在(`source_fragments`)且是活的,但形状与身份链都不对** —— 见 §1 |
| ② | `source_id` 身份链现状 | ⚠️⚠️ **仓里有两代 source 身份并存** —— 见 §2,**这是本单最重的发现** |
| ③ | 查重指纹 | **sha256 已落库且是活的**,但**导入路径上是空的** —— 见 §3 |
| ④ | 出参形状被谁消费 | `parseDocument` 返回 `Promise<void>`,**单一消费者**;产物落 `document_chunks`(页级、无锚)—— 见 §4 |

## 1. `source_fragments`:名字被占,形状不对

**存在且活跃**(定义 `016_v2_material_proposal.ts`):
- **写**:`services/courseMaterials.ts`
- **读**:`courseMaterials.ts` · `organizedNoteProposals.ts` · `sourceScopes.ts`(+ 测试)

**它的形状**(逐字):`page_start` / `page_end` / `order_index` / `confidence` / `warnings_json` / `metadata`。

⚠️ **与规格 v0.1 的碎片流差在三处**:
1. **锚只到页级** —— **无 bbox、无 region 级锚**(第 1 场实证 Docling/MinerU 都能给 region);
2. **无出生证字段** —— 无 `transcriber` / `version` / `anchor_fidelity` / `text_normalization`;
3. **外键挂在旧链上** —— `source_material_id` + `document_id` + `document_chunk_id`。

⇒ **裁定材料**:**不能直接复用**,但**表名已被占用**。b-1 面临二选一 ——(a)迁移扩展现表;(b)新表另起名。
⛔ **这是设计级选择,我不裁**;但请注意 (a) 会把**两代身份链的债一起继承**(见 §2)。

## 2. ⚠️⚠️ 设计级 finding:仓里有**两代 source 身份**并存

| 代 | 表 | 定义 | 状态 |
|---|---|---|---|
| **旧** | `source_materials` + `documents` + `document_chunks` | `016_v2_material_proposal.ts` | **活**(`courseMaterials` / `materialMapProposals` / `organizedNoteProposals` 均写) |
| **新** | `source_records` + `source_files` + `source_materializations` | `045_v2_source_identity_floor.ts` | **活**(`sourceRecords` / `sourceFileIntake` / `sourceMaterialization` / `sourceProjectionMaterializer` 均写) |

**而 `source_fragments` 挂的是【旧】那一代。**

⇒ ⭐ **b-1 开工前必须先裁一件事:碎片流的身份锚点挂哪一代?**
- 挂**新**(`source_records`)= 与 045 身份地板一致,但**与现役 `source_fragments` 的消费者三处断开**;
- 挂**旧**= 立刻可用,但**把碎片流建在一条正在被替代的链上**。

⛔ **我不选。这是设计裁定,已升 Fable。**
📌 **⚠️ 并且这不是「谁对谁错」** —— 两代都在活跃使用,**没有一代是死的**;所以这不是清理债,是**选锚点**。

## 3. 查重指纹:活的,但导入路径上是空的

- ✅ **`source_files.content_hash TEXT NOT NULL`**(045),`services/sourceFileIntake.ts:328` 用 **`createHash('sha256')`** 真算 —— **上传路径的指纹是活的**。
- ⚠️ **但 `services/packagePortability.ts:361` 写 `content_hash: null`、`:441` 写 `content_hash: ''`** ⇒ **包导入路径上指纹是空的**。
  📌 **形状在、能力不在的又一例。** ⛔ **本单不修**(只读),记为 b-2「入库永不拒收」的已知输入条件:**从包导入的件没有可用指纹,查重会漏**。

## 4. 现役解析链路:产物落 `document_chunks`,页级无锚

`services/documentParser.ts:300` `parseDocument(documentId, userId): Promise<void>` —— **副作用型,不返产物**。
- **唯一消费者**:`routes/documents.ts`(⭐ 单一入口,改动面窄);
- **写**:`document_chunks(id, document_id, chunk_index, content, heading, page_start, page_end)` + 更新 `documents.parse_status`。

⇒ **现役产物是页级、无锚、无出生证。** **b-1 的碎片流是新链路,不是改它** —— 与段计划「⛔ 不动现役 parser 行为」一致。
📌 **TD-29(说谎的类型声明)/ TD-30(`.data` 未消费)的落点 12.9c T0 就在这条链上**,b-1 不触及。

## 5. 给 b-1 的三条输入

1. ⭐ **先裁身份锚点**(§2)—— **这是 b-1 的前置,不裁就开工会把地基打在会动的地皮上**;
2. **表名冲突需处置**(§1)—— 迁移现表 vs 新表另起名;
3. **b-2 的已知输入条件**(§3)—— 包导入路径无指纹。

---

## 6. 追加:K-1~K-5 刀位核实(b-1 拆单前置)

⚠️ **b-1 造的是新表新码,所以「刀位」问的不是「现码在哪能被捅红」,而是「必红判据所需的脚手架是否已在树上」。**

| 刀 | 所需脚手架 | 现物 |
|---|---|---|
| **K-1 逐字保真** | 参照全文 + 申报类归一化器 | ✅ 参照全文可由 `getText()` 取;归一化器由 b-1 造 |
| **K-2 锚真实性** | **把真文件放进 source 存储 + 按锚回原件取文本** | ⚠️ **分级见下** |
| **K-3 伪高保真必红** | 纯校验器逻辑 | ✅ b-1 自造,无外部依赖 |
| **K-4 顺序保真** | 纯校验器逻辑 | ✅ 同上 |
| **K-5 残缺申报** | 坏页 fixture | ✅ `pdf-lib@^1.17.1` 在 server 依赖里,可现造坏件(pdf-parse v2 修单已用过该手法) |

**✅ 真文件 fixture 模式已成熟**:`v2SourceFileIntake.test.ts` 用 `mkdtempSync` + `writeFileSync`,经 intake 落 `source_files.storage_key`;**隔离 runner 已注入 `SOURCE_BLOB_DIR`**(`run-server-test-suite.mjs:47`)⇒ **新测试的真文件有隔离目录,零额外工作。**

### ⚠️ 6.1 K-2 的刀位是分级的 —— **region 级未确认**

| fidelity | 刀位 |
|---|---|
| **page / block** | ✅ **有刀位** —— `getText()` 返回 `{pages:[{text,num}], text, total}`,页级文本可取,回程票可验 |
| **region(bbox)** | ⚠️ **未确认** —— **服务端没有已确认的文本项级 bbox 提取器** |

**服务端 PDF 依赖只有两个**:`pdf-lib@^1.17.1`(造件,不解析)· `pdf-parse@^2.4.5`。
📌 前两场的 region 级回程票用的是 **Python 的 `pypdfium2`** —— **它不在 server 依赖树里**。

### ⭐ 6.2 一处与第 1 场记录不符,如实更正

第 1 场 §3.0 记「`getPageTables()` 存在但**声明为 private** ⇒ 不使用、不计分」。
**本次运行时探针实测**:`PDFParse` 原型上**这些方法都在、都可达** ——
`getPageText` · `getPageLinks` · `getTable` · `getPageTables` · `getPathGeometry` · `getImage` · `getScreenshot`;
**顶层还导出了定位原语** `Rectangle` · `Point` · `Line` · `Shape` · `Table`。

⇒ **「类型声明为 private」≠「运行时不可达」** —— 第 1 场那句是**按类型声明下的结论**,**运行时它们是可调的**。
⚠️ **但⛔ 不得据此说「region 刀位有了」**:**可达 ≠ 能给文本项级 bbox**。`getText()` 的页对象只有 `{text, num}`;`getTable`/`getPathGeometry` 给的是表格与路径几何,**是否覆盖正文文本项的 bbox,本次未验**。

### 6.3 ⇒ 给 b-1 的第四条输入(⛔ 我不选)

**K-2 二选一**:
- **(a)** K-2 只在 **page 级**施刀,**region 级如实记「未取证」** —— 与规格「低保真合法、不申报违法」自洽,本段不引新依赖;
- **(b)** 先花**一轮探针**核 `pdf-parse` v2 能否给文本项级 bbox;能则 region 刀位就地解决,不能则回落 (a) 或**引新依赖(采购决策)**。

📌 **我倾向 (a)** —— b-1 的段目标是「接住任何转写器产物」的地基,**地基不必自己会拓印**;region 级保真的验证属于 12.9c 接真转写器时的事。**但这是设计裁定,归你。**
