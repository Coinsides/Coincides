> **状态 (Status)**: active(12.9c 段计划**对 HEAD 重对现物**,只读,2026-08-29)
> **层 (Layer)**: 分析 / Analysis(V12.9c 段 · 派单前置)
> **权威 (Authoritative)**: 否(现物快照;⚠️ 含一条段计划的落点更正)

# 12.9c 段计划:对 HEAD 重对现物

**为什么先做这个**:段计划骨架写于 12.9b **动土之前**,而本段落了**七次修宪 + 两张新表 + intake 六码闭集 + 容器逐条目**。
📌 **「⛔ 过闸 ≠ 对现物」这条家法在 12.9b 三次救场** —— 12.9c 每单派前照走。

## ⭐ 1. c-0 的前提**变好了**:不再是「发明接线」,是「照先例扩展」

**b-2 已经建成一条完整的「从 intake 写出生证 + 碎片」路径**,并且**给了它一个转写器身份**:

```
name: 'source-intake-text-fallback'   version: '1'   lockfile: 'server/package-lock.json'
```
(`sourceFileIntake.ts:569-572`,写入点 `:583` / `:603`)

⇒ ⭐ **c-0 的 T0 对齐不必从零设计「解析器 → 碎片流」的接线,它有一个活的先例可照抄** —— **每个 T0 解析器给自己一个转写器身份 + 走同一条 `storeSourceImprint`。**
📌 **这是 12.9b 顺手给 12.9c 留下的红利,骨架期不知道。**

## ⚠️⚠️ 2. **落点更正:TD-30 应从 c-0 改挂 c-1**

**TD-30 的机械化形状**:「生产碎片导出**必须经 `.data` 消费表格**;killer = **含表 fixture ⇒ 该页碎片数 >1 且单元格可寻址**」。

**⚠️ 但对 HEAD 核实**:
| 问 | 答 |
|---|---|
| 仓内的「生产碎片导出」是哪段码? | **只有 `source-intake-text-fallback` 一条**(`sourceFileIntake.ts:583/603`) |
| 它处理表格吗? | ⛔ **不处理** —— 它产的是**纯文本 + `flow` 族行锚**,**没有 table 角色** |
| c-0 的 T0 解析器会产表格碎片吗? | ⛔ **也不会** —— pdf-parse v2 的 `getText()` 只到页级(12.9a 第 1 场 C0 实测:`pages[i]` 仅 `{text,num}`) |
| 那 TD-30 的 killer 在 c-0 能立起来吗? | ⚠️ **立不起来** —— **含表 fixture 喂进去,产不出「碎片数 >1 且单元格可寻址」,因为整条链根本没有表格概念** |

⇒ ⭐ **TD-30 在 c-0 【无处可清】。它的真正落点是 c-1(MinerU 接入)—— 那才是第一个会产出表格碎片的生产者。**
📌 **⚠️ 若不更正,c-0 会被迫在一条没有表格的链上「清」一笔表格债** —— **要么清不掉,要么造一把没有对手的刀。**(⛔ 与 b-3 移出本段是同一形状:**闸与它的对手同生**。)

**⇒ 建议(⛔ 不自裁)**:TD-30 落点由 **c-0 → c-1**;**TD-29 留在 c-0**(`server/src/types/pdf-parse.d.ts` 仍在,仍写着 `numpages`,而 c-0 必然触及 pdf 解析家族)。

## ⚠️ 3. TD-33(包导入空指纹)在 c-0 也**很可能无处可清**

**现物**:`packagePortability.ts` 的空指纹**三处**都还在(`:361` `null` · `:441` `''` · ⭐ **`:659` `''` —— 这一处是 b-0 当时没数到的**)。
⚠️ **而 c-0 是「T0 解析器对齐碎片流」,不触及包导入路径。**
⇒ **建议**:TD-33 **保持其原触发器**(「b-2 顺带 / 包导入路径下次被触碰时」),**⛔ 不硬挂 c-0**。
📌 **另**:b-0 当时记的是「两处」,**现物是三处** —— **又一次「`head`/目测数量」的教训**,已在条目更新时补正。

## ✅ 4. c-3 的入场条件**仍未到**(与骨架一致)

`imprint_fragments` 在生产码里**零消费者**(除 `sourceImprints.ts` 自身与迁移)。
⇒ **理解轨产物引用碎片 id 的第一个生产者仍不存在** ⇒ **引用校验闸继续「与生产者同单」**,骨架无需改。
📌 ⚠️ **注意别误判**:b-2 的 text fallback 是**转写器**(碎片的**生产者**),⛔ **不是**理解轨产物(碎片的**引用者**)。**两者都叫「生产者」,守的门不同。**

## ✅ 5. c-1 的欠据行在案

`deferred-tests.md:36` —— region 语义验证,落点「12.9c 接真转写器之日」。**c-1 即是。** 骨架无需改。

## ⇒ 给拆单的三条输入

1. ⭐ **c-0 有先例可照抄**(转写器身份 + `storeSourceImprint`),比骨架设想的轻;
2. ⚠️ **TD-30 落点建议改挂 c-1**;**TD-33 建议保持原触发器,⛔ 不硬挂 c-0** —— 二者都是「**债的落点要挂在它真能被清的那一单**」;
3. **c-3 / c-1 的骨架无需改。**
