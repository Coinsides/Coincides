> **状态 (Status)**: active
> **层 (Layer)**: 现状 / Current-State(技术债登记)
> **日期 (Updated)**: 2026-08-22
> **权威 (Authoritative)**: 是(债未清则条目不删;清偿须留收据链接)

# 技术债登记

**设立理由(2026-08-20,02.1 二级复盘)**:「已申报」≠「有人会做」。复核体系里被正当申报保留的缺口,若无登记与承接,就是孤儿债。规则:每条债必须有**承接方**(工单/专项/待拍);清偿时写清偿收据并标 `已清`,不删行。

| # | 债 | 来源 | 承接 | 状态 |
|---|---|---|---|---|
| TD-1 | client lockfile 91 个新节点缺可选 `license` 字段(SBOM/license 收据退化;不影响 resolution/integrity/可复现安装) | 工单 02 Review LOW-1,02.1 申报保留 | 绑「统一 npm lock 生成工具链」专项(未立项;立项时一并清) | 未清 |
| TD-2 | `test:unit` 无常驻门触发器(parity/契约护栏没人自动跑) | 02.1 二级复盘漏合取发现 | [工单 03 ## Result](../handoffs/2026-08-20-v2bn12-03-lifecycle-and-surface-authority.md#result):已接入 `verify:v2-bn8-runtime` 首段 | 已清 |
| TD-3 | 5 个 check/smoke 脚本 + `docs:check` 不在任何验证门内(`check:group-gallery-shell` / `check:groups-rail-shell` / `check:single-editor-shell` / `check:source-experience` / `check:v2-bn11-legacy-shutdown` / `docs:check`) | docs-inventory 生成器机器发现(08-20);把 TD-2 的人眼盲区从 1 条扩到 6 条 | [工单 05 ## Result](../handoffs/2026-08-20-v2bn12-05-slash-session.md#result):逐项计时均归快项并直接接入 `verify:v2-bn8-runtime`;慢项为 0,不设空门 | 已清 |
| TD-5 | **crossing 覆盖左右对称必备项**:恢复收据正控矩阵补齐 right-crossing 精确行(raw v1/v2,原 `[96,636]` 形)并确立「crossing 测左必测右」为成对规则——二级复盘发现覆盖不对称与 03 主单病因(有右无左)形状同构、方向镜像复发;按对称项补而非补一行 fixture,防换轴重演 | 工单 03.4 复核 LOW-1 + 全链 Review-2 增量 | [工单 05 ## Result](../handoffs/2026-08-20-v2bn12-05-slash-session.md#result):left/right × raw v1/v2 四行正控矩阵,右界保留 `[96,636]` 原形 | 已清 |
| TD-4 | `__test_probe` 生产 kind 无环境守卫,API 端到端可达 | docs-inventory 生成器机器发现;**已确认刻意**(8.11.1.2 补丁说明:仅用于测试通用派发) | 非缺陷不清;但 **MCP 工具面工单(必修①)必须把 `__` 命名空间 kind 从工具枚举里过滤**——记此处防遗忘 | 设计注记 |
| TD-6 | 正文(TextFlow)+annotation 双 PUT 缺少跨资源 durable 耦合,任一失败可半提交(全应用级,非 Slash 专属;客户端围栏不是解) | 工单 05.1 Review HIGH-1 + 修正单 05.2 设计裁决 | V12 必修后另立专项设计单(服务端 operation receipt / 原子端点方向);server revision / `If-Match` 条件写与 durable OCC 一并纳入该专项。05.5 只做客户端诚实 outcome / recovery / reconciliation,不充当 OCC。05.6 不申报同-note 并发再 hydration 的完整 OCC 支持;hydration epoch fence 仅作 `stale_epoch` + recovery receipt 安全网(生产当前无该触发器)。12.2a-2 工具面跨资源撤销仅以 `revert_outcome: complete|partial` 保持结果可观察,不承诺原子回滚;原子性仍待本专项。 | 未清 |
| TD-7 | **过渡桥:存量 canvas-only 笔记初始面选 Canvas**(`useSurfaceModeController` 初始选择按 hydration 结果一次性改选;12.1.2 引入) | 12.1 旅程 J9=0:Page 模式过滤掉全为 `canvas_world`/`crossing` 的块 ⇒ 空态;真因在初始面而非数据 | **退役触发器**:12.4 流面成为所有 Note 默认面 / Page 模式退役时**一并拆除**,不得残留 | 未清(桥) |
| TD-8 | 收据表时间戳写入无共享 helper:`applied_at` ISO 写点 15 处散在 14 个生产文件(`reverted_at` 2 处),约定靠「每列单一格式」维持而非机关保证 | 12.1.2 BLOCKED:裁定措辞「单一 helper」点名了不存在的机关 | **已清(2026-08-23)**:TD-8 清债单 + TD-8-fix 两轮闭环,复核 **PASS 0/0/0/0**。**方向**:18 个生产 `operation_batches` INSERT 全部不再显式传 `created_at`(走 DB 默认);`learningCanvases:299` 的 `applied_at` 改应用侧 ISO;migration `048_` 幂等回填 2 行 ISO→空格式,已知排序反例消失。**护栏**:两条 route 正控(`POST /api/notes/:id/blocks`、`POST /api/projections`,删任一 `.run()` 实参即红)· learning-canvas 双时钟断言(**冻结 JS 时钟到 2031 哨兵、SQLite 保真实时钟,把两列权威来源物理分离**)· 独立 migration 测试(生产 runner + `:memory:` + 103/2 夹具 + TEMP trigger 计数 + `hex(CAST(... AS BLOB))` 验原字节)。server `test:v2` 270/270。⚠️ **未做且不声称**:helper 收敛(「单一 helper」措辞已于 12.1.2 撤回);`applied_at`/`reverted_at` 靠「每列单一格式」约定维持 | **已清** |
| TD-9 | 过渡桥(TD-7)的 root 接线契约测试 mock 掉了中间承重点 `useRuntimeSurfaceStateController`(把 leaf `noteId` 改 `undefined` 仍 209/209 绿);payload 断言四个输入来源同值无法辨别来源偷换;`layerProps` 观察通道双 cast 与生产类型脱钩 | 12.1.4 复核 FAIL(方向成立)HIGH-1/MED-1/MED-2;止损线触发停 12.1 线 | 桥与现有护栏保留(X1/X2/X3 已红绿);补法见该单 Review §5(保留真实 wrapper 改 mock leaf 重依赖 / 互异哨兵 / 类型化 phase receipt);**随 TD-7 一并退役**——12.4 Page 退役时桥与此测试同拆,若 12.4 前再碰该 hook 则顺手补 | 未清(随桥) |
| TD-10 | **manifest 生成器的「单一 mapper」是约定而非机关**:`renderManifest` 的默认 initializer 与「全仓只有一份九字段 mapper」两者都未被结构锁住。复核 G3 实测——新增同签名 `copyToolFaceManifest` 并把默认值换成它,专项门 6/6 与生产 `check:tool-face-manifest` **均仍绿** | 12.2a-1b-fix2 复核 LOW-1(PASS 0B/0H/0M/1L,Fable 放行 log 08-22 #9);工单把结构 killer 明列为**可选**,故不阻塞放行 | 若要封:用 AST/源码结构契约**同时**锁默认 initializer 与单 mapper,**不得再用「生产字节自洽」当证明**(那正是 G3 绕过的东西)。承接建议:12.2a-1c 触及该文件时评估顺带;或随 12.2b `tools/list` 落地时统一做结构契约 | 未清 |
| TD-11 | **parity 专项门的嵌套 npm 开销是运营风险(候选)**:`test:tool-face-parity` 内 11 次串行嵌套 npm、每次 60s timeout;复核明写「不能承诺零 infrastructure flake」,拥塞 CI 可能超时。**本机复验稳定**(连跑三次 10/10,5450/5511/5525ms,离散度 1.4%),故接门放行 | 12.2a-1c-fix 复核「接门前置与主链状态」;Fable 记为候选(log 08-22 #11) | **触发条件**:`verify:v2-bn8-runtime` 总时长 > 60s 时再议(当前 42s)。届时可选:把 spawn 测试拆出主链只留 `check:tool-face-parity`、或改用直接 node 调用并显式注入 `npm_execpath` | 候选(未达触发线) |
| TD-13 | ⭐ **复核临时树用 junction 链到共享 `node_modules`,递归删除会穿链清空共享依赖**。2026-08-23 亲验:`tmp/` 下两个残留树共 **6 个 ReparsePoint,6/6 指向 `Coincides\{,client\,server\}node_modules`**。因果闭合:建临时树→junction 省空间→递归删除穿过 junction 删进目标→共享依赖被清空。解释了「上次只中 client、这次三处全中」(第二个树把范围扩到全部三处) | 08-22 首次(复核报告自述「Windows junction 令 git 一并清空共享 ignored `client/node_modules`」);08-23 三处全空,Opus 用 `npm ci --offline` 恢复(client 202/server 205 包) | **治本已入 reviewer 角色卡 5-9「隔离树卫生」**(不建仓内、不 junction 共享 node_modules、用完自清)。**删残留须 junction 安全**:先枚举 ReparsePoint,对 junction 用 `cmd /c rmdir <path>`(只解链接不进目标),再删普通文件,删前后各核三处计数 | **已清(2026-08-23)**:6 个 junction 逐个用 `[IO.Directory]::Delete(path,$false)` 解链(非递归,对 junction 只解链接不进目标),复核剩余 0 后再递归删两树,回收 **78.5 MB**。**删前/解链后/删后三次核对三处 `node_modules` 计数均为 `153/183/1` 逐位相同**;删后 server 267/267、双 tsc 0、verify 41s exit 0。⚠️ 注:`cmd /c rmdir` 被本环境安全护栏拦截(把 `/c` 当路径),故改用 .NET 非递归删除,效果等价 |
| TD-12 | **测试资产目录非隔离,fixture 清理遇 EPERM**(候选):复核在严格基线与补充树上均须「隔离资产目录」才能跑 server 全套,否则 fixture 清理触 EPERM。属既有环境债,非 12.2a-2 引入 | 12.2a-2 复核判定「EPERM 为既有环境债」;Fable 记为候选(log 08-22 #14) | 两条备选:①测试默认使用隔离资产目录(`mkdtempSync`)②fixture 清理容错(EPERM 时重试/忽略并记录)。**触发条件**:下次有人在非隔离环境跑 server 全套受阻时立即处置。⚠️ **2026-08-23 TD-8-fix 复核本轮未复现** —— 默认资产目录下 `test:v2` 亦为 270/270,无 EPERM。**故判为偶发而非稳定故障**;不降级不清除,等下次复现再定 | **正式(2026-08-23 升级)**:EPERM **第三次出现**(12.2a-3 续跑回执 265/270,5 条 canvas-assets `mkdir` EPERM)。⚠️ **但调度方同环境复跑为 270/270、零 EPERM** —— 三次出现、两次不复现,**故不是稳定故障也不再是可忽略的偶发**:它会让「270/270」这个数字在不同会话间不可比。**承接**:12.2 收口后清。两条备选仍为 ①测试默认用隔离资产目录(`mkdtempSync`)②fixture 清理容错 | **未清 · 已缓解(2026-08-27)**:交付 `scripts/run-server-test-suite.mjs`(`mkdtempSync` 临时根,**env 在 `spawn` 前注入** —— 因 `canvasAssets.ts:14` 是模块级 const、进程内再设无效)+ `test:v2` 改走该 runner(23 个测试文件参数逐字逐序保留在 `package.json`,守 TD-22)。**四轮 builder 各自手工重做的隔离已固化进仓库。**<br>⚠️ **⛔ 不得写「已清」**。两条硬事实:①**有隔离 279/279 且 `server/uploads/**` 快照零变动**(隔离有效的正控;复核方独立复跑 + `verify:v2-bn8-runtime` exit 0)②无隔离经 npm 出现 5 条失败 —— **记录为观察,⛔ 不作可复现判据**(复核方绕过 runner 直跑为 278/1,唯一失败是 `npm_execpath` 那条已知非缺陷,**未能复现那 5 红**)。<br>📌 **本条的病是「不隔离就跑不完」,不是「跑完会弄脏仓库」** —— 后者经三种情形实测**均为零变动**,发单方最初照记忆把因果写反,killer 因此不成立(builder 正确触发「不许凑刀」分支停手)。<br>**裁定(Fable)**:给不稳定的病造一把要求确定性的刀,唯一产出是凑刀;**假绿的刀比未清的账危险**。<br>**触发器 = 下次仍出现 EPERM 或「跑不完」时,再查真因。** |
| TD-14 | **`scopes` 未强制**:JWT 只含 `userId`,注册表/manifest 的 `scopes` 仅描述性,无任何机关强制授权 | 12.2a-3 短笺 Q1 鉴权行(builder 自报);Fable 裁定 design §12 D-f | 12.2b 不得把 `scopes` 当权限模型;强制归鉴权基线专项(与 TD-6 同级的服务端专项) | 未清(声明边界) |
| TD-15 | **`getOwnedCourse` 全仓五份同名实现,同一条 SQL、同一个 404,签名各异**:`routes/notes.ts:47`(返 `{id}`)· `routes/projections.ts:34`(返 `void`)· `services/materialMapProposals.ts:52`(收 `db` 参数,返 `void`)· `services/materialReconciliationProposals.ts:230`(同前)· `services/organizedNoteProposals.ts:80`(收 `db`,返 `{id,name}`)。**ownership 检查是安全边界** —— 五份各自演化意味着**加固时会漏改**,而漏改不会立刻出错 | 12.2a-3 S1a 起草时亲验(Fable 报「一份重复」,实为五份);**非本单范围,已明令不碰** | 归「server 侧 ownership 正门收敛」专项(未立项)。⚠️ **收敛前提是先定权威签名**(是否收 `db`、返 `void` 还是实体)——那是设计判断,不是机械替换。**在此之前,任何单触及其中一份都不得顺手统一其他份** | 未清 |
| TD-16 | **`routes/notes.ts` ↔ `services/notes.ts` 的 ESM 循环依赖**(LOW 候选):S1a 为「不搬走共用 helper」而让 service 复用 route 导出的 `getOwnedCourse`/`hydrateNote`,形成静态环。**仅在调用期读取已声明导出,`tsc` 与真实 HTTP 正控均通过**,本轮可接受 | 12.2a-3 S1a;Fable 裁定「本轮可接受,复核点名查初始化顺序」(log 08-23 #9) | **正解是把两个 helper 下沉为叶子模块**(如 `services/courseOwnership.ts` / `services/noteHydration.ts`),route 与 service 同时依赖叶子,环自然消失。⚠️ **不在本单改** —— 下沉会触及同文件其他 route 的调用点,属独立改动面 <br>⭐ **2026-08-27 部分执行,剩余面已收窄**:`services/noteHydration.ts` 叶子已落地(`hydrateNote` / `hydrateBlock` / `parseJson` 零语义搬入,route 与 `services/notes.ts` 同时依赖叶子),**hydrate 那条边已拆**。⛔ **但环仍在** —— `services/notes.ts` 仍从 `../routes/notes.js` import `getOwnedCourse` 与 `getOwnedNote`(用于 `:49` / `:116`)。<br>⚠️ **那两个不能顺手搬**:`getOwnedCourse` **全仓五份同名实现签名各异**(TD-15 点名),`getOwnedNote` **同病**(`services/annotationTruths.ts:238` 另有一份 `getOwnedNote(db, userId, noteId)`,与 route 版 `getOwnedNote(noteId, userId)` **参数顺序与签名都不同**)⇒ 搬 = 赌两份行为一致。<br>**⇒ 剩余面 = `getOwnedCourse` / `getOwnedNote` 两个 ownership helper,随 TD-15 的「先定权威签名」裁定一并处理。**| **未清 · 部分执行(2026-08-27)** —— ⛔ **不得标已清**:三条边只拆了一条(hydrate),ownership 两边仍在。<br>📌 拆边收据:`handoffs/2026-08-26-v2bn12-note-hydration-leaf.md`(K-1 六端点原始字节等价、K-2 同源锁复核方亲手复现)。builder 在回执首句即写明「**只拆掉 hydrate 这一条边;TD-16 保持未清,ESM 环仍存在**」,⛔ 未声称清债 |
| TD-17 | **malformed JSON → REST 500**:全局 `express.json()` 解析失败经 `errorHandler` 映为 500(应 400);MCP `/api/mcp` 同受影响(得不到 `-32700`/400,坏体 + 非法 Host 得不到先 403) | 12.2a-3 续跑 2 回执第三项冲突;Fable 裁定 design §12 补裁 | `errorHandler` 对 body-parser `entity.parse.failed` 统一映 400(全路由);12.2 收口后小单 | **部分清偿(2026-08-27)**:✅ **(甲)坏 body → 400 已清** —— `errorHandler.ts` 新增窄分支(判别用结构化字段 `type === 'entity.parse.failed' && status === 400`,⛔ 无 `message` 子串匹配),响应 `{ error: 'Malformed JSON body' }` 与既有 400 同形且**不回显原始坏体**(⚠️ 探针实测该错误对象**自带原始 `body` 字段**,展开即信息回显);两条常驻测试接门(T-1 在 `test:v2`、T-2 在 `test:mcp-transport`),K-1 双红(`500 !== 400` @ `v2McpTransport.test.ts:799` / `v2NoteBlockLifecycle.test.ts:112`,复核方亲手复现)双绿;K-2 用一次性内存路由证普通异常**仍 500**,既有兜底未被改宽。<br>📌 **附带行为变更(已申报,接受)**:malformed JSON 不再打 `Unhandled error` 日志 —— 它已是已处理的 4xx;⛔ 收口记录里不得把本单描述为「只改状态码」。<br>⛔ **残留面,不得标全清**:**(乙)「坏体 + 非法 Host」拿不到先 403 未做** —— 需移动 `express.json()` 的**全局**挂载位置(现 `index.ts:105`,早于 `/api/mcp` 的 Host 守卫 `:113-117`),爆炸半径覆盖每条依赖全局 JSON 解析的路由,**属独立改动面,收版期不碰**;**`-32700` 未产出**是该架构下的必然结果(解析早于 transport),与 (乙) 是同一件事。<br>**触发器 = 有人要求 MCP 坏体产出 JSON-RPC 信封、或要求非法 Host 优先于解析** |
| TD-18 | **note patch 执行器未提取**:`PUT /api/notes/:id` 的 status 分支(含 `archived`、各取一次时间)与 b-1 的 `trashNote`/`restoreNote` 不同源 | 12.2b-1 builder 停手证明固定签名执行器无法与混合单条 UPDATE 零语义共用;Fable 补裁 | note patch 整体提取时统一(不在 12.2b);人类恢复仍走 PUT | 未清(声明边界) |
| TD-19 | ⭐ **环境事实:Codex 复核在「权限守卫类 mutation」内容上会被上游内容过滤器掐断**。2026-08-23 b-5-fix 增量复核亲历:reviewer 在建隔离环境准备施 ownership 绕过类 mutation(移除 `listToolFaceReceipts` 的 `user_id` 过滤、放宽 revert ownership 守卫)时,连续两条 `ERROR: This content was flagged for possible cybersecurity risk`,进程在 **880 秒 / 26 万 token** 处终止,**`## Review` 一字未写**。产品码与协议文件均未受损(mutation 已还原、`user_id` 过滤完好、numstat 全空) | 12.2b-5-fix 增量复核;Opus 三源确认(文件未改 / 无 `## Review` 标题 / 进程日志两条过滤器错误);Fable 裁路线③(log 08-23 #39) | **派此类位点前须知**:凡复核位点包含「移除/放宽 ownership、鉴权、权限守卫」的 mutation 描述,**有被掐断的现实风险**;⛔ **不得为绕过过滤器而改写措辞** —— 那是给安全机制找漏洞。可行做法:该位点改为**结构复核**(测试方向 / 断言落点 / 与代码结构一致性)并**显式标注「结构复核,非亲刀」**;缺口另记(见 TD-20)。**⚠️ 另记遗留物形状**:被掐断的 reviewer 不会自清,遗留 = **junction + 一个 vitest config**;接手者按 TD-13 的「先查目标再动」处置(本次 junction 指向 OS temp 而非仓库,只解链即可) ⚠️ **本条正文的具体描述亦曾命中过滤器一次(进程存活)**;后续引用本条**保持抽象措辞**。 | **未清(环境事实,登记备查)** |
| TD-20 | **ownership-guard 的 mutation 验证未亲刀**:b-5-fix 的 MED-1(A→B 方向跨用户隔离)其「移除 `user_id` 条件 ⇒ 列表侧红」「放宽 revert ownership ⇒ B 的 notes 全行上红」两刀,**未由对抗方独立施加**(原因见 TD-19) | 12.2b-5-fix 增量复核路线③(Fable 裁,log 08-23 #39) | ⚠️ **当前不是「无证据」而是「证据源少一个」**:守卫成立已有三源 —— ①b-5 轮复核用 **API↔service** 与 **DB 全行↔B 自身 API** 双来源确认跨用户不可列/403/B note 全行不变;②b-5-fix builder 自查该两刀**先红后绿**;③本轮结构复核(测试确为 A→B 方向、断言确落 notes 全行)。**mutation 是增量证据,不是唯一来源。** **承接**:留给有能力做此类测试的环境;**若 Henry 认为必须亲刀,由他本人指定方式跑,我们不自行安排**(Fable #39 留口) | **未清(声明边界)** |
| TD-21 | ⭐ **构建体系:`shared` 的 project reference 声明齐备,却从未通电**。`composite: true` / server `references: [../shared]` / `paths: {"@shared/*"}` 三样都在,而 **`shared/dist/types/` 是空目录壳**(零文件),**构建链里没有任何一步构建 `shared`**(server `build` 是裸 `tsc` 不是 `tsc -b`),`shared` 亦无 `package.json`。至今不显形,是因为现有两条路都不需要 dist:**client 走相对路径由打包器读源码**,**server 唯一那条是 `import type` 编译时被擦除**。⚠️ **`manifest.ts` 顶部已成文承认此状**(「server 的 no-emit 门有意不构建被引用产物」+ `@ts-ignore TS6305`)⇒ **这是已知且被刻意压制的状态,不是疏忽**。<br>⭐ **三世界矩阵(b-0 现物实测;⭐ tsx 格已由 c-1a 升级)**:dev(**jiti**)🔴 解析不了 `@shared/*` · 测试(**tsx**)✅ **静态具名 runtime import 已证绿** · 产物(**裸 node**)🔴 `ERR_MODULE_NOT_FOUND`(**tsc 不改写 import 说明符**,产物 JS 里原样是 `@shared/...`)。<br>⭐ **tsx 格升级的根因与证据(c-1a,2026-08-24)**:`shared/` 原**无 `package.json`** ⇒ 就近落到根(**未声明 `type`**)⇒ 其 `.ts` **按 CommonJS 解析**(模块 namespace 只有 `default`,函数套在 `default.x`,`__esModule` 是 CJS 转译签名)。补 `{"type":"module"}` 后 tsx 世界转 ESM。**K-1 双步阳性对照**:有该文件 5/5 绿 → **删该文件重跑复现 `SyntaxError: does not provide an export named ...`** → 恢复后 5/5 绿 ⇒ **证明是这个字段让它能跑,不是碰巧绿**。<br>⚠️ **另两格不变,债不清**;**这只是「把 shared 变成真包」的第一步**(⛔ 未加 `name`/`exports`)。<br>⭐ **布局脆弱点**:`TOOL_FACE_MANIFEST_URL = new URL('../../dist/tool-face-manifest.json', import.meta.url)` **依赖 `src/mcp` 与 `dist/mcp` 同深度** ⇒ **任何抬高 `rootDir` 的改法会同时打断它与 copy 脚本的对齐**。 | 12.2c c-1b 第 1 版 S1 探针(TS6305)+ b-0 读码报告(2026-08-24);Fable 据此**翻案 A1→A1′**(log #10 `3a66ef3`) | **本段不修**:A1′ 用**跨端契约测试**锁全等,把同源的执行点从 import 链移到测试世界,绕开整个构建体系问题。<br>**触发器 = 第一个非平凡运行时值需要真共享**(2 行模板字面量不算)。届时**设计输入 = `analysis/` 的 b-0 报告 + 本条**;⚠️ **已知三条路都不免费**:`@shared/*` 要给 dev 与产物各补解析器 · 相对路径要动 `rootDir`(而动 `rootDir` 打断布局)· **把 `shared` 变成真包(`package.json` + `exports`)是唯一让三世界行为一致的做法,代价也最大**。<br>⚠️ **A1′ 的已知缺口**:契约测试只在 **tests 世界**成立,**dev 与产物世界没有任何东西守这个全等**。 | **未清(设计注记 + 触发器)** |
| TD-22 | ⭐ **显式测试文件列表没有「漏挂发现机关」**:`test:v2` / `test:mcp-transport` / `test:trash-notes-tool` 等均为**显式文件列表** ⇒ **新增测试文件默认不被跑**,且**没有任何机关会告诉你漏挂了**。<br>⭐ **本条用交付自身当证据,不是推测**:12.2c c-1b v3 交付了一个 **4/4 全绿、含 AST 断言的常驻跨端契约测试** —— 它是 A1′ 跨端全等的**唯一执行点** —— 而**四小时内没有任何东西提示它没接门**;由 c-1c 补接(`test:v2` **270→274**)。⚠️ **一个绿的、写得很好的、没人跑的契约测试,和一个不存在的契约测试,在 CI 里是同一回事。** | 12.2c c-1c(2026-08-24):builder 独立申报「长期风险」;打包复核 LOW-1 点名本条未落台账 | **候选做法**:①目录扫描 + 与列表比对的检查脚本(新增 `*.test.ts` 未出现在任何 script 中即红)②改用 glob 而非显式列表(⚠️ 会改变「curated gate」语义,**需裁定**)。<br>**触发器 = 下一次有人新增测试文件。** ⛔ 本段不修(是新机关,要单独设计)。<br>📌 ⚠️ **与 12.2c-gate 闸单不是同一件事**:那道闸拦「server 产品码运行时 import shared」,**本条拦「新测试没接门」** —— 两者都是「形状在、能力不在」的机械化,**保护对象不同**。 | **未清(候选 + 触发器)** |
| TD-23 | ⭐ **`check:server-shared-runtime-import` 的已知射程边界:只看静态 import**。**动态 `import()` 与直接 `require()` 的 CallExpression 可以绕过它**(builder 主动申报,打包复核 B7 亲验与实现口径一致)。已覆盖:`ImportDeclaration` · `ImportEqualsDeclaration`(`import x = require(...)` 拒 / `import type x = require(...)` 放行)· 相对说明符按 importing file 词法 resolve;**不在射程**:动态 `import()` · 运行时 `require()` · `export ... from`。 | 12.2c 闸单(2026-08-24)builder 申报 + 打包复核 2 B7 VERIFIED(2026-08-25) | **本段不补**:现有 server 产品码零处动态 shared 引用,补齐要处理「表达式实参不可静态判定」这类根本限制(⇒ 只能做启发式,**而启发式闸会产生假阳性,与本闸「宁误报不放过」的从严定位冲突,需裁定**)。<br>**触发器 = 第一次有人在 server 产品码里用动态 `import()`/`require()` 引 shared**。<br>⭐ **记账而非假装闭合** —— 闸的价值在于它**知道自己看不见什么**;⛔ 不得因本条把闸描述为「已全面拦住跨界」。 | **未清(设计注记 + 触发器)** |
| TD-24 | ⭐ **快捷登录的 `NODE_ENV` 半门当前不承重**:全仓**无任何地方设置 `NODE_ENV`** —— `server/package.json` 的 `start` 是裸 `node dist/index.js`,产品码里该变量只出现在本单新增的两行(`server/src/index.ts:111` 与 `server/src/dev/quickLogin.ts:7`),无 Dockerfile / compose / Procfile / ecosystem 注入者。⇒ 运行中它为 `undefined`,第一个条件**恒真**,**实际承重的只有显式开关 `COINCIDES_DEV_QUICK_LOGIN`(默认关、fail-closed,已由复核方 K-1 亲刀验红)**。连带:**T-2 测的是本仓现实中不会发生的状态**,是真测试但守着一扇当前没人走的门。<br>⚠️ **归属:工单缺陷,非施工缺陷** —— 发单方 grep 到「零命中」后读成「尚未引入的新约定」,未读成「无人置位 ⇒ 条件不承重」。 | 2026-08-26 快捷登录单复核(发单方自曝);Fable 裁定 | **本轮不修**(Fable 裁):①本仓此刻**不存在「生产模式」这个现实**(无部署、无第二台机器),为尚不存在的状态造承重门=形状跑在能力前面(同日缓存头同款已杀);②⭐ **`node_modules` 里读 `NODE_ENV` 的不是零**(Express 自身即读,错误页/缓存行为会变)⇒「零命中」只对产品码成立,置位 `production` 的爆炸半径非零,需真部署语境下检验而该语境现无。<br>⇒ 处置=**如实改口 + 降格前哨**:一切措辞用「**默认不可达 —— 路由仅在显式开关开启时存在**」,⛔ 不得再引 `NODE_ENV` 作保证;半门留码不删(免费绊线,将来置位即生效),**零代码改动(连注释也不改)**。<br>**触发器 = 首次真实部署**:届时须建立 production 置位约定,让 T-2 **从绊线转正为承重门**,并验 `start` 脚本置位 + Express 侧副作用。 | **未清(设计注记 + 触发器)** |
| TD-25 | ⭐ **「默认目的」的不变式选在读时维护 —— `listNotePurposes` 是会写库的「读」**。`services/purposes.ts:324` 的 `listNotePurposes` 首行即调 `ensureNoteDefaultPurpose`(`:315` → `:266`),后者在事务里 `INSERT OR IGNORE INTO purposes (…, is_note_default=1, created_by='system', …)` ⇒ **首次对某 note「列出目的」会在库里创建一行**。<br>⚠️ **病名要记准(Fable 2026-08-26)**:**病不在工具面,在 service** —— 「每个 note 都有一个默认目的」这个保证本身没错,错的是它由读路径顺手 INSERT 来兑现。<br>📌 **与 12.2c 的 🅰 是镜像**:那次「读」把已删的当存在,这次「读」把不存在的变存在 —— **两次都是读与「什么存在」没对齐**。 | 2026-08-26 S4 拆单前置扫描(调度方按第六查逐个扫读函数的写操作);Fable 裁定 | **裁定:purposes 退出读面本批**。⛔ 不采「绑纯读变体」—— 其真代价不是「行为不同」,是**两扇门报告两个不同的世界**(人类门看到有默认目的、Agent 门看到没有,同一时刻同一 note 两个真相,比「读会写」更糟);⛔ 亦不采「原样挂+声明」—— 工具面第一次出现「读会写」即成先例,收据轴 `truth`/`tier` 语义从此永久带脚注。<br>**将来重构写死四步**:①**物化时机从「读时」挪到「创建时」**(note 创建那一刻建默认目的,它属于写路径)②一次性回填迁移(给现存无默认目的的 note 补行)③`listNotePurposes` 删掉 ensure 调用变纯读 ④**那之后** purposes 读面进场,且**两门看到同一个世界**。<br>**触发器 = purposes 读面需要进工具面时**。<br>⭐ **登记册条目**:「**过 killer ≠ 没有副作用**」—— 它会以读工具身份过全部门禁(`INSERT OR IGNORE` 令每次返回都对),直到有人问「这行是谁建的」。⛔ **本条为第一次发现,不建机械闸**(家法:第二次重发现才机械化);现作为调度方拆单流程的检查项(第六查执行细则:**读面的单,发单前逐个扫读函数里的写操作**)。 | **未清(设计注记 + 触发器)** |
| TD-26 | ⭐ **`test:v2` 现在依赖一个 gitignored 的手工生成物,干净克隆下会直接炸**。`server/src/mcp/manifest.ts:18` 的 `TOOL_FACE_MANIFEST_URL` 指向 `server/dist/tool-face-manifest.json`,而该文件被 `.gitignore:5` 的 `dist/` 排除;**没有任何 test script 跑 `copy:tool-face-manifest`**(只有 `predev` 与 `build` 跑)。<br>⭐ **两条现物证据,均为亲验非推断**:①**staleness**:S4-1 builder 改完注册表后首跑 `test:v2` **exit 1**,三条红全在 `v2DevQuickLogin`,报 `Tool binding parity mismatch (missing: none; extra: list_items, get_item, list_content_groups)` —— 子进程读到的是**旧 dist 副本**;跑 `copy:tool-face-manifest` 后 277/277。②**clean-clone 阳性对照(复核方亲做)**:把 `server/dist/tool-face-manifest.json` **临时移走**后单跑 `v2DevQuickLogin.test.ts` ⇒ `Error: ENOENT … open '…\server\dist\tool-face-manifest.json' at loadToolFaceManifest (manifest.ts:26:54)`,exit 1;移回后 3/3 绿。<br>⚠️ **病因是本轮引入的**:`v2DevQuickLogin.test.ts`(快捷登录单)是 `test:v2` 里**第一个启动真实 app 的测试** —— 在它之前没有测试加载该 manifest,所以这个「未追踪工件 + 无人刷新」的缺口**一直存在但不显形**。**形状早就在,是新消费者把它照出来了。** | 2026-08-26 S4-1 round 2 builder 如实申报(未把首跑的红冒充绿)+ 复核方 clean-clone 阳性对照 | **未修**(本轮不夹带)。**危害面**:干净克隆跑 `test:v2` 必红 —— 而**洁净室复核用的正是干净克隆**,⇒ 下一个 reviewer 会撞上,且症状(ENOENT / parity mismatch)看起来像产品缺陷而不像环境缺口。<br>**两条候选修法**:①给 `test:v2`(及其他启动真实 app 的测试脚本)加 `pre*` 钩子跑 `copy:tool-face-manifest`;②让 `loadToolFaceManifest` 在 dist 副本缺失时回退到真相源 `docs/generated/tool-face-manifest.json`(⚠️ 需裁定:会削弱「产物世界读产物」的现有设计意图,与 TD-21 的三世界矩阵相关)。<br>**触发器 = 下一次洁净室复核开干净克隆**(近在眼前);⛔ 在此之前任何人跑 `test:v2` 遇红,先查这条再怀疑产品码。 | **已清(2026-08-26)**:`pretest:v2` = `check:tool-face-manifest && copy:tool-face-manifest`(与 `predev` 逐字同形)+ 常驻字节等价测试 `v2TestV2ManifestHook.test.ts`(已接 `test:v2` 列表)。**两个症状各有实证**:①缺失 —— 移走 dist 后 `test:v2` **exit 0 / 278 全绿**且输出可见钩子 `Copied … bytes`(复核方亲跑);绕过钩子直跑同一测试则 `ENOENT at loadToolFaceManifest`(成对阴性);②陈旧 —— builder 造 sentinel 实证「只 copy 会照搬陈旧」,补回 `check` 半边后**在进入测试前**被拦,原文 `过期: docs/generated/tool-face-manifest.json`。副本保真:dist 与真相源 sha256 `27fca021…d939360` 逐位相同。⚠️ **⛔ 不得称「已不可能复发」**:**没有任何常驻机关守着「钩子还在」** —— 删掉 `pretest:v2` 不会让任何测试变红(与 TD-22 同族)。候选补法(一行断言:读 `server/package.json` 断言 `scripts["pretest:v2"]` 含那两个命令)随下一张触及 server 测试面的单顺带,**第一次发现不建闸**。 <br>⭐ **LOW-1 已闭(2026-08-27,随 S4-3 §4b 落地)**:常驻断言已加 —— 读 `server/package.json` 断言 `scripts["pretest:v2"]` **同时包含** `check:tool-face-manifest` 与 `copy:tool-face-manifest`(落 `server/src/__tests__/v2TestV2ManifestHook.test.ts` 的 T-2,经 `test:v2` 接门)。**复核方亲施刀**:把钩子缩成只剩 `copy`(⭐ 先证 mutation 自身 `JSON.parse` 通过)⇒ **T-2 红**于 `pretest:v2 must retain both check… and copy…`(`actual: check: false`);还原后 `server/package.json` 对 HEAD **零 diff**、`test:v2` **279/279**。⇒ 「**机关在,但没有守机关的机关**」这条口子已堵上,且**堵得住** |
| TD-27 | ⭐ **近名物防误杀:`proposalStore`(死码)与 `/api/proposals`(活系统)是两套东西,名字近而已**。<br>**待退役(死码,零风险)**:`client/src/stores/proposalStore.ts` —— `grep -rn "proposalStore"` 在 **client + server 全仓(含测试)零 importer**,确认为死代码。<br>⚠️ **⛔ 活着的、有真实客户端消费者的另一套系统,严禁一起清**:`server/src/routes/proposals.ts`(`index.ts:138` 已挂载)承载**领域提案** —— `CourseDetail.tsx` 在用 material-map(`:612`)/ organized-note(`:629`)/ material-reconciliation(`:650`)+ `/proposals/:id/apply`(`:676`)+ `/proposals/:id/discard`(`:698`);`TemplateStudio.tsx:526` 还有 template-migration。<br>📌 **它与「工具面收据队列」(`ToolReceipts.tsx`)不是同一件事**,后者消费的是 `operation_batches` 的工具面收据。 | 2026-08-26 12.2d 拆单前置核实(调度方按第六查核「ProposalList / 待处理队列 / 跳到现场」三词现物);Fable 裁定并指示记档 | **执行约束(常设,非债)**:①退役 `proposalStore.ts` 并入**版本收口批**,两笔动作 = `rm` + **全仓零引用断言**;⛔ **断言措辞必须用 `proposalStore` 精确词,不得用 `proposal` 前缀**(前缀会误伤上述活系统)。②**凡未来出现「proposal 退场 / 清理 / 收敛」字样的工单,发单前必须先引用本条的分辨**,并在单里写明动的是哪一套。<br>📌 **附带裁定(记此备查)**:S6「候选审阅入口」判**已由 12.2b 满足** —— `ToolReceipts.tsx`(按状态分组列表 + apply/dismiss/revert + `navigate('/notes/:id')` 跳现场,`App.tsx:106` 已上路由)就是那个「人类可见、可确认、可拒绝的面」;**12.2d 不再作为独立段存在**,实体在它出生时即已交付。 | **退役动作已完成(2026-08-27)** · **常设约束继续有效**。<br>✅ 已删 `client/src/stores/proposalStore.ts`(整文件,−60);✅ 已删 `relationRepository.ts` 的 singular `loadRelation`(仅 −5,零新增);⛔ `loadRelations` / `loadRelationTypes` **一字未动**(实测仍在 `relationRepository.ts` 2 处、`ContentGroupPanel.tsx` 5 处)。<br>**两条彼此独立的常驻断言**落 `client/src/deadCodeRetirement.test.ts`(vitest 默认 glob 自动接门,`test:unit` 22/222 → **23/224**);断言 B 用**词边界** `...`。⭐ **目标名以 `['proposal','Store'].join('')` 分段构造** ⇒ 断言文件**自身在扫描面内却不自命中**,⛔ 未用「排除自己」制造盲区。<br>**复核方亲施 K-1**:建临时探针引用 `proposalStore` ⇒ **A 红并报出 file:line:column,同轮 B 仍绿** ⇒ 两条断言的独立性经实证。<br>⚠️ **常设部分不随退役而失效**:凡未来出现「proposal 退场 / 清理 / 收敛」字样的工单,**仍须先引用本条对 `/api/proposals`(活系统)与 `proposalStore`(已退役)的分辨**;断言词一律用精确词/词边界,⛔ 不用前缀 |
| TD-28 | ⭐ **块删除「有出口无回路」:trashed 的块在人类侧无持久恢复入口**(🅰 正确性)。<br>**能力在**:server 有 `PUT /api/note-blocks/:id {status:'active'}`(`routes/noteBlocks.ts`),client 有 `restoreBlock`(`hooks/useNoteCanvasDataAdapter.ts:1923`,发的正是该 PUT)。<br>⛔ **入口不在**:`restoreBlock` 的**唯一消费者**是 `restoreBlockForHistory`(`hooks/useNoteCanvasRuntimeController.ts:279`),它只服务**撤销栈**(`historyService.ts` / `hooks/usePlacementHistory.ts`)⇒ **只有「删完立刻 Ctrl+Z」一条路**;**跨会话之后,已 trashed 的块在人类界面里没有任何地方能被看到或恢复**。<br>⭐ **同产品内的刺眼对照**:**笔记有持久回收站**(`pages/Courses/CourseDetail.tsx:122` 的 `active\|trashed` 过滤 + `handleRestoreNote`),**块没有**。 | 2026-08-28 V12 收口统一走查 J1 得 **0 分**(走查单:`analysis/2026-08-27-v12-closeout-journey-sheet.md` §4.1 与 §5 🅰-1;归档 commit `01fff28`) | **未清**。⚠️ **归属已判**:**既有缺口,非 V12 引入** —— `restoreBlock` 与撤销栈的接线早于本版;本轮只是**第一次有人从人类视角去找那扇门**。<br>**Fable 2026-08-28 裁定「记账放行,不阻塞铸版」**,并立为通例:**走查的 0 挡不挡铸,看 0 落在谁身上** —— 落在**本版交付物**上则挡(12.2c J3=0 不铸段的先例),落在**既有面**上则记账放行(**铸版断言的是「本版交付物完成且验证」,不是「全产品无缺口」**)。⛔ 铸版记录**不得写「全绿」**,须如实写「3/4 步满分、1 步 0 分(既有缺口,TD-28 在案)」。<br>**判 🅰 而非 🅱 的理由**:红线是「Agent 能编辑的,人类必须 100% 都能编辑」;此处更进一步 —— **人类自己删的东西自己回不去,而能力已经写好,缺的只是入口**。<br>**承接建议**(⛔ 未立项):给块补持久回收视图(**可复用 note 回收站的现成形状**),或在 note 层暴露「本 note 的已删块」。<br>**触发器 = 下一次有人需要恢复一个跨会话的块**(⚠️ 本次走查自己就撞上了:c-3 靶块只能经客户端写路径清理,**未能走人类门**)。 | **未清(🅰,既有面,记账放行)** |
| TD-29 | ⭐ **说谎的类型声明:`server/src/types/pdf-parse.d.ts` 仍描述 `pdf-parse` v1 的 `export =` 外形**(`numpages` 等),而实装是 **v2 类式 API**(`PDFParse` / `getText()` / `total`)。<br>⚠️ **病名(调度方,Fable 采)**:**「说谎的类型声明比没有声明更危险」** —— 照它写静态 named import,会得到**能过类型检查、运行时失败**的代码;**与 2026-08-28 刚修的那条死通道是同病同形**(该次正是「编译得过、运行必炸」被 catch 成常态)。<br>📌 现状不炸的唯一原因:修单改用了**动态 import + 局部形状约束**绕开它(该 `.d.ts` 不在那张单的允许面内,builder 处置正确,⛔ 未顺手改)。 | 2026-08-28 pdf-parse v2 修单 builder 主动申报 + 复核方判为 LOW-1(单:`handoffs/2026-08-28-pdf-parse-v2-adapter.md`,批 `12b183a`) | **未清**。**Fable 2026-08-28 裁:落点钉死「12.9c T0 存量对齐首单顺带」**(那单必然触及 `documentParser` 家族,搭车零成本),⛔ **不单开单**。<br>**修法(⚠️ 需该单探针先核,⛔ 不照本条硬做)**:先核 **`pdf-parse` v2 是否自带类型声明** —— **自带则删除本仓这份**;**不自带则照 v2 外形重写**。<br>**触发器 = 12.9c T0 对齐首单开工**(或更早:任何人要在 `documentParser` 家族里写 `pdf-parse` 的**静态** import 时)。 | **未清(LOW,已钉落点)** |
| TD-30 | ⭐ **生产碎片导出只取 `.text` 不取 `.data`,表格塌成一条** —— Docling 的 `TableItem` 内容在 `.data`(`num_rows`/`num_cols`/`table_cells`),导出器只读 `.text` 得 `null`。<br>⚠️ **病名(复核方,Fable 采)**:**「同一个错误在两场里以两副面孔出现」** —— 第 1 场表现为「Docling 表格维只得 1 分」(实为我测量不足,补测后是 2),第 2 场表现为「识别器没料可引」(`listening-p30` / `academic-writing-p2` 该页只供 1 条碎片,模型只好虚构 id)。**第二次重发现 = 未机械化的证据。** | 12.9a 第 1 场 §4.1 补测 + 第 2 场 §4.2 逐条查 miss(批 `a21f01b`);Fable 2026-08-28 裁立 | **机械化形状(Fable 钉)**:生产碎片导出**必须经 `.data` 消费表格**;**killer = 含表 fixture ⇒ 该页碎片数 >1 且单元格可寻址**。<br>**落点钉死「12.9c T0 首单」**(与 TD-29 同批),⛔ 不单开。 | **未清(已钉落点)** |
| TD-31 | ⚠️ **canvas 模式下行首檐下槽可能被 surface 裁切(派生风险,⛔ 未目击)** —— 单 A 把行首控件迁出排版流后,槽绝对定位于 `right: calc(100% + 4px)`。**实测常数**:槽宽 **77px**,槽左缘→行左缘 **81px**,块内边距 10px ⇒ **槽需要块左侧 91px 净空**。<br>**已实测(页面模式)**:桌面余量 121px、平板/移动 322px,**三种视口均未被裁**;Layout 模式下**只剩 24px**。<br>**已现物确认**:`writingSurfaceCanvas`(`overflow: hidden`)**只在 `surfaceMode === 'canvas'` 时挂**(`NoteWritingSurfaceLayer.tsx:3239`);块的 x 在 `interactionController.ts:161` 与 `placementService.ts:346/400` **钳到最小 0**。<br>⛔ **但裁切本身无人目击** —— 复核方在 canvas 模式下尝试两次命中测试,**阴性对照均失败**(槽 `opacity:0`;强制可见后又被 `blockBox` 盖住),截图不可用(pane 未显示)。**⇒ 几何 + CSS 规范可推出必裁,但⛔ 不作观测入档。** | 2026-08-28 单 A 复核 §5 提出;调度会话浏览器实测(页面模式三视口 + canvas 模式常数);Fable 2026-08-28 裁 | **⛔ 本轮不修**(Fable 裁):**此刻改产品语义 = 拿推理当观测**。<br>⚠️ **⛔ 并且不要默认选「抬高 x 下限」** —— Fable 点出它的隐藏代价:**把下限从 0 抬到 91 = 永久没收每张画布左侧 91px 的摆放自由,为一个悬停才出现的控件付常驻地价**,还牵动存量块与三处 clamp 语义。**那不是「最小改动」。**<br>**触发器 = 有人真正目击一次**(需可见的浏览器窗口截图,或在 canvas 模式把块拖到左缘)。届时凭观测在 **①贴左翻转到行内侧(呈现层解法,不动产品语义 —— Fable 预倾向)** 与 **②画布安全边距** 之间裁;拿不准上 Henry(画布地皮属他的工作台哲学)。<br>📌 **可复现路径已探明**:三个模式药丸里,**第一个**(`title="Switch to open canvas mode"`)才是 surfaceMode 开关。 | **未清(派生风险 · 未目击 · 有触发器)** |
| TD-32 | 🅱 **`writingSurface` 的类名里渲染出字面字符串 `undefined`** —— `NoteWritingSurfaceLayer.tsx:3239` 的三元 `surfaceMode === 'canvas' ? styles.writingSurfaceCanvas : styles.writingSurfacePage`,而 **`.writingSurfacePage` 在 CSS 里从来不存在**(`git log -S` 查无历史,非「被删」)⇒ 页面模式下 `styles.writingSurfacePage` 为 `undefined`,模板字面量把它拼成字面 `undefined` 进 DOM 的 class 列表。<br>**评级 🅱**:`undefined` 只是一个匹配不到任何规则的类名,**无行为影响、无样式丢失**(本就没有该规则可丢)。<br>⚠️ **⛔ 它不是「模式切换的类绑定断了」** —— 调度会话最初报的现象是「Layout 药丸 `aria-pressed=true` 却没挂模式类」,**那是观测姿势错**:三个药丸里**第一个**才是 surfaceMode(`aria-pressed={surfaceMode === 'canvas'}`),**第三个是 `layoutMode`**;点错了药丸,`surfaceMode` 全程是 `'page'`。**切进 canvas 模式后类绑定完全正常**(`_writingSurfaceCanvas_` 已挂、`overflow: hidden` 生效)。 | 2026-08-28 Fable 立案的 undefined 探针(只查不修);调度会话现物核实 | **候选修法**(⛔ 本轮不修,🅱 按 grow-by-use 排队):①三元的 else 分支改 `''`;②或在 CSS 里补一条 `.writingSurfacePage`(若确有页面模式专属样式要写)。**①更诚实** —— 不存在的样式就别假装有。<br>**触发器 = 下次触及该组件,或页面模式真需要专属样式时。** | **未清(🅱)** |
| TD-33 | ⚠️ **包导入路径上查重指纹为空**:`source_files.content_hash` 是 `NOT NULL` 且**上传路径真算 sha256**(`services/sourceFileIntake.ts:328` 用 `createHash('sha256')`),**但 `services/packagePortability.ts:361` 写 `content_hash: null`、`:441` 写 `content_hash: ''`** ⇒ **从包导入的件没有可用指纹,查重会漏。**<br>⚠️ **病形**:**形状在、能力不在**(字段在、约束在、一条路径填了、另一条路径填空)。⛔ **不是「没做查重」,是「查重有一半是瞎的」** —— 后者更危险,因为它看起来是做了的。 | 2026-08-28 V12.9b b-0 只读核实(`af95202` §3) | **⛔ 本轮不修**(b-0 是只读单)。**落点**:**b-2「入库永不拒收」顺带**,或**包导入路径下次被触碰时顺带**;⚠️ 修前须先裁**空指纹的既有行存量怎么办**(回填 vs 标记 vs 忽略)—— 那是数据裁定不是机械替换。<br>**触发器 = b-2 开工,或任何人再碰 `packagePortability` 的 `content_hash` 写入点。** | **未清(已钉落点)** |
| TD-34 | ⭐⭐ **两代 source 身份并存,且两代都活着**:**旧代**`source_materials` + `documents` + `document_chunks`(`016_v2_material_proposal.ts`,写入方 `courseMaterials` / `materialMapProposals` / `organizedNoteProposals`)· **新代**`source_records` + `source_files` + `source_materializations`(`045_v2_source_identity_floor.ts`,写入方 `sourceRecords` / `sourceFileIntake` / `sourceMaterialization` / `sourceProjectionMaterializer`)。现役 `source_fragments` 挂的是**旧代**。<br>⚠️ **⛔ 这不是「清理债」** —— **没有一代是死的**,两代都在被正常使用。**它是一笔收敛债**:两条身份链最终要合一,而合一是一次真正的迁移工程,不是顺手改几个外键。 | 2026-08-28 V12.9b b-0 只读核实(`af95202` §2);Fable 2026-08-28 裁 | **裁定(Fable)**:**12.9b 的碎片流锚点挂【新代】**(`source_files`),新表取词典名 **`source_imprints`(拓印件出生证)+ `imprint_fragments`(碎片)**;⛔ **本段不碰旧链一个字节**、⛔ 不改名不迁移现役 `source_fragments`。<br>**理由三层**:①方向权威在新代,新地基不打在正被替代的链上;②与旧链三处消费者断开**是正确的隔离不是代价**(那三处是旧链功能,碎片流不欠它们);③查重指纹活在新链(`source_files.content_hash`)⇒ b-2 复用零成本。<br>**触发器 = 旧链某个消费者需要碎片流能力之日** —— 届时才谈收敛。 | **未清(收敛债 · 有触发器)** |
