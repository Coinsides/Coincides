# V2.BN.8.10.3 Line Height And Paragraph Spacing Controls Patch Note

## Completed

- Added line-height and paragraph-spacing checks to the TextFlow typography profile contract.
- Added line-height and paragraph-spacing controls to the existing More -> Typography panel.
- Reused the same note-level profile save path introduced in V2.BN.8.10.2.
- Saved both controls through `patchDocumentTypographyProfile(...)`.
- Kept line-height and paragraph-spacing normalization inside `typographyProfileService.ts`.
- Applied `--document-paragraph-spacing` to document text presentation selectors.
- Added runtime boundary coverage for the new control markers and paragraph-spacing CSS variable.

## Not Included

- No typography measurement service.
- No auto-pagination.
- No PageStack reflow based on typography changes.
- No export/PDF implementation.
- No per-block typography.
- No rich text span styling.

## Manual Experience Check

Passed with Browser Harness on `http://localhost:5173/#/notes/7454b392-85b5-4c28-9b60-289ce88a38f7`.

Checked:

- More panel exposes the Typography section.
- `Line` control appears and reads default `22`.
- `Spacing` control appears and reads default `0`.
- Changing line height to `28` updates `data-document-line-height="28"` and `--document-line-height: 28px`.
- Changing paragraph spacing to `12` updates `--document-paragraph-spacing: 12px`.
- Reloading the note preserves `28 / 12`.
- Reset restores default `22 / 0`.

## Validation

Passed:

```bash
npm run smoke:canvas-engine-model-contract
npm run check:canvas-runtime-boundary
npm run build:client
```

Notes:

- `npm run build:client` still reports the existing Vite dynamic import / chunk-size warnings. They are not introduced by V2.BN.8.10.3.

Passed:

```bash
git diff --check
npm run check:changed-file-secrets
```

## Follow-ups

- V2.BN.8.10.4 should make typography measurement and PageFrame sync read the active typography profile.
- Auto-pagination should remain deferred until typography-aware measurement is stable.
