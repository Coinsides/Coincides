# V2.BN.8.7.9.0 Commit 1 Staging Report

日期：2026-06-23

## 提交边界

Commit 1 暂定为：

> Backend ContentGroup Entity Cutover

这个提交只收纳 ContentGroup System 后端实体化的第一组稳定结果，包括 ContentGroup、ContentGroupMember、ContentGroupPetal / Fragment、GroupFolder / Placement 的数据库结构、迁移、服务、路由、校验器和后端测试。

它不收纳 Gallery / Groups Rail / Single Editor 视觉壳，也不收纳 Canvas runtime、Annotation、TextFlow、模板/材料体系清理和正式文档同步。

## 已暂存文件

- `server/package.json`
- `server/src/db/schema.sql`
- `server/src/db/migrations/031_v2_content_groups.ts`
- `server/src/db/migrations/032_v2_group_folders.ts`
- `server/src/db/migrations/033_v2_content_group_members.ts`
- `server/src/db/migrations/034_v2_content_group_petals.ts`
- `server/src/index.ts`
- `server/src/routes/contentGroups.ts`
- `server/src/routes/groupFolders.ts`
- `server/src/services/contentGroups.ts`
- `server/src/services/groupFolders.ts`
- `server/src/validators/index.ts`
- `server/src/__tests__/v2ContentGroups.test.ts`
- `server/src/__tests__/v2GroupFolders.test.ts`

## 明确排除

- `shared/types/index.ts`：当前 diff 主要是旧 NoteBlock template definitions 的移除，属于 template / material domain 边界整理，不属于后端 ContentGroup 实体切分。
- `client/**`：属于后续 Gallery / Rail / Single Editor / Canvas runtime 的界面与前端运行时提交。
- `docs/**`、`PRODUCT.md`：属于文档提交，单独收口。
- `server/src/lib/noteBlockTemplates.ts`、`server/src/services/templateDefinitions.ts` 等模板相关 server 文件：属于 template / material 提交，不混进 Commit 1。

## 暂存区检查

暂存区文件数量：14

暂存区体量：

- 14 files changed
- 4030 insertions
- 1 deletion

检查结果：

- `git diff --cached --name-only`：只包含 server 侧实体化相关文件。
- `git diff --cached --stat`：体量集中在新服务、新测试、新迁移和 schema。
- `git diff --cached --check`：通过，无空白错误。
- 对 `shared/types`、`client`、`docs`、`PRODUCT` 的暂存区混入检查：无结果。

## 已有主体验证

本次 staging 前已经完成主组验证：

- `cd server && npm run test:v2`：通过，129 tests passed。
- `cd server && npm run build`：通过。
- `npm run check:group-gallery-shell`：通过。
- `npm run check:groups-rail-shell`：通过。
- `npm run check:single-editor-shell`：通过。
- `npm run check:canvas-runtime-boundary`：通过，35 checks。
- `npm run smoke:canvas-engine-model-contract`：通过，21 groups。
- `npm run build:client`：通过，仍有既有 Vite dynamic import / bundle size warning。
- `git diff --check`：通过，仅有 LF/CRLF warning。
- `npm run check:changed-file-secrets`：通过，264 changed files scanned。

说明：因为工作区仍有大量未暂存变更，本轮没有做 staged-only 隔离构建。当前结论是：Commit 1 暂存边界清晰，且在完整工作树状态下已经通过主组验证。

## 后续提交建议

Commit 1 之后，建议按以下顺序继续建立提交边界：

1. Client ContentGroup Runtime Bridge
2. OpenDesign Shell Parity
3. TextFlow / Annotation / Canvas runtime supporting changes
4. Template / Material cleanup
5. Docs and reports
