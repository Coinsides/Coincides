# V2.BN.8.10.4 Typography Measurement And PageFrame Sync Patch Note

## Completed

- Added `typographyMeasurementService.ts` as a pure estimate boundary for typography-aware layout measurement.
- Made text block height estimates read the active note-level `DocumentTypographyProfile`.
- Kept typography as layout estimate truth, not TextFlow content truth.
- Synced PageStack draft continuation with typography-aware draft fit.
- Synced cross-page Block fragment projection with the shared PageFrame content rect semantics.
- Added PageFrame typography and estimated line capacity metadata to Export Preview.
- Added compact typography explanation inside Export Preview PageFrame groups.
- Confirmed AI-readable PageFrame refs expose active font size, line height, and paragraph spacing.
- Removed the runtime-layout dependency on `engineModel.ts` to avoid a canvas runtime circular import.

## Not Included

- No Word-like automatic pagination.
- No line-level or paragraph-level Block splitting.
- No PageSlice extraction UI.
- No PDF/export execution.
- No per-block typography.
- No CanvasObject / CanvasPlacement database migration.

## Validation

Passed:

```bash
npm run smoke:canvas-engine-model-contract
npm run check:canvas-runtime-boundary
npm run smoke:canvas-engine-performance
npm run build:client
git diff --check
npm run check:changed-file-secrets
```

Notes:

- `npm run smoke:canvas-engine-model-contract` passed 51 groups.
- `npm run check:canvas-runtime-boundary` passed 122 checks.
- `npm run smoke:canvas-engine-performance` passed in 11.39ms.
- `npm run build:client` passed with the existing Vite dynamic import / chunk-size warnings.
- `git diff --check` passed with LF/CRLF normalization warnings only.
- `npm run check:changed-file-secrets` passed, 181 changed files scanned.

## Manual Smoke Target

Passed with Browser Harness on `http://localhost:5173/#/notes/7454b392-85b5-4c28-9b60-289ce88a38f7`.

Checked:

- Opened `test note 1`.
- Confirmed the document surface exposes `data-document-font-size="15"` and `data-document-line-height="22"`.
- Opened Export Preview.
- Confirmed the PageFrame group exposes `data-export-preview-typography="default-document"`.
- Confirmed the PageFrame group exposes `data-export-preview-line-capacity="56"`.
- Confirmed visible preview text explains `15px`, `22px line`, `0px gap`, and `56 lines`.

Not exercised in Browser Harness:

- Changing typography controls from More -> Typography, because this smoke stayed read-only to avoid mutating the current test note.
- Draft creation near the PageFrame bottom, because that would write test content into the note.

## Follow-ups

- Word-like measured auto-pagination should come only after this measurement baseline is accepted.
- User-confirmed cross-page Block split materialization remains later.
- Real PDF export and Coincides project package export remain dedicated future lanes.
