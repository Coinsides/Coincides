# V2.BN.8.7.9.0 Commit 4 Staging Report

日期：2026-06-23

## 提交结果

Commit 4 已落地：

- `658789d refactor: simplify v2 note templates`

前置提交：

- `7bb7b18 feat: add content group entity backend`
- `0489129 feat: add content group client surfaces`
- `bae7891 feat: advance notebook runtime foundation`

## 提交边界

Commit 4 的边界是：

> Server material / template / domain foundation.

这个提交处理的是旧结构化 NoteBlock template 的退场与 server material/template/domain lane 的对应调整，不混入 ContentGroup 实体化、不混入前端 runtime、不混入 docs。

## 已提交内容

- `shared/types/index.ts` 中旧模板定义退场：
  - heading
  - concept
  - definition
  - theorem
  - proof
  - example
  - exercise
  - answer
  - source quote
  - warning callout
- legacy block type fallback 统一回落到 `text.paragraph`。
- server note block template runtime 与 material/template/domain services 配套调整。
- material library 与 template migration 测试同步更新。

## 明确排除

以下内容没有进入 Commit 4：

- ContentGroup backend/entity 服务。
- ContentGroup client surfaces。
- TextFlow / Annotation / Canvas runtime。
- docs/contracts/release notes/brainstorm evidence。
- ignored generated output，例如 `server/dist`、`client/dist`。

## 验证结果

- `cd server && npm run test:v2`：通过，129 tests passed。
- `cd server && npm run build`：通过。
- `git diff --check -- server shared`：通过。
- `git diff --cached --check`：通过。
- `npm run check:changed-file-secrets`：通过，162 changed files scanned。

已知非阻塞 warning：

- Git 仍提示本地仓库有较多 unreachable loose objects；本轮没有执行 `git prune`。

## 下一步建议

Commit 5 应收口：

> Docs, contracts, release records, and design evidence.

这组会比较大，建议必要时拆成：

1. formal docs and contracts
2. release notes / brainstorm / OpenDesign evidence
