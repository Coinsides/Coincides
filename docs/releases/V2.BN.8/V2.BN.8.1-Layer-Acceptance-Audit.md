# V2.BN.8.1 Layer Acceptance Audit

> 本文是 `V2.BN.8.1-Runtime-Replacement-Plan.md` 的逐层验收审计。
> 它不新增产品范围，也不替代 `Review.md` / `Experience-Review.md`。
> 目的只是把 L0-L12 当前已经被证明的事实、仍需浏览器/人工验证的事实、以及后续 polish backlog 分开。

## Audit Snapshot

```text
date: 2026-06-13
branch: codex/v2-bn-canvas-engine
current commit at audit creation: 44221e3 docs: refresh v2 bn runtime verification
browser harness: not run in this audit, per Henry instruction to wait until all work is ready
manual acceptance: pending Henry
```

## Completion Rule

`V2.BN.8.1` 只有在以下条件都满足后才可以关闭：

- 代码层证明旧 `NoteDetail.tsx` runtime 已经退场；
- 非浏览器工程验收通过；
- Browser Harness 最终 smoke 通过；
- Henry 手动确认体验 passed。

当前状态：

```text
code replacement: achieved
non-browser gates: passed
browser smoke: pending
Henry manual pass: pending
goal status: not complete
```

## Evidence Index

当前可直接引用的工程证据：

- `client/src/pages/Notes/NoteDetail.tsx`
  - 只读取 route `noteId`；
  - 只挂载 `NoteCanvasRuntimeProvider`；
  - 只渲染 `NoteCanvasRuntime`。
- `client/src/pages/Notes/canvasEngine/NoteCanvasRuntime.tsx`
  - 是 Note 页面 runtime host。
- `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasRuntimeController.ts`
  - 只组合 5 个一级 controller：
    - `useRuntimeSurfaceStateController()`；
    - `useRuntimeDocumentDataController()`；
    - `useRuntimeLayoutModelController()`；
    - `useRuntimeBlockOperationsController()`；
    - `useRuntimePresentationController()`。
- `client/src/pages/Notes/canvasEngine/`
  - 已包含 runtime、service、hook、layer、block projection 文件群。
- `Review.md`
  - 已记录最新非浏览器验收。
- `Open-Issue-And-Brainstorm-Checklist.md`
  - 已区分 V2.BN.8.1 硬门槛和后续 V2.BN.8.x polish backlog。

当前通过的非浏览器命令：

- `npm run build:client`；
- `npm run smoke:canvas-engine-performance`；
- `server/npm run build`；
- `git diff --check`；
- changed-file secret scan。

## L0 - Startup Gate And Baseline

Status: achieved.

Evidence:

- 当前分支为 `codex/v2-bn-canvas-engine`；
- 工作区在审计前为 clean；
- `Plan.md` / `Workflow.md` / `Engineering-Spec.md` / architecture / interaction / state contracts 已存在；
- open issue checklist 已存在，并被纳入当前 release 证据链；
- 当前代码可以 build。

Remaining:

- 无工程阻塞。

Notes:

- Browser Harness 不属于 L0 的当前执行项，本轮按 Henry 指示暂不运行。

## L1 - Local Test Data Reset

Status: not executed in this audit; not currently blocking code replacement.

Evidence:

- 当前代码替换、build、performance seed 不依赖清空本地数据库；
- Henry 没有在本轮要求立即 hard reset；
- 本计划允许 local dev data reset，但这是一项破坏性操作，应在最终浏览器/人工测试前由 Henry 决定是否执行。

Decision:

- 不为了审计而偷偷删除 `server/coincides.db*`；
- 若最终 Browser Harness / manual smoke 被旧测试数据污染，再执行有备份的 local test data reset。

Remaining:

- Henry 需要在最终测试前决定是否清空本地测试数据。

## L2 - Runtime Root

Status: achieved.

Evidence:

- `NoteDetail.tsx` 已退化为 route/provider shell；
- `NoteCanvasRuntimeProvider` 承接 `noteId` runtime context；
- `NoteCanvasRuntime` 是 Note 页面 runtime host；
- `useNoteCanvasRuntimeController()` 是当前 runtime composition root。

Acceptance:

- 旧 runtime 不再由 `NoteDetail.tsx` 直接持有。

Remaining:

- 无工程阻塞。

## L3 - Viewport And World

Status: achieved as V2.BN.8.1 seed; full pan/zoom remains future work.

Evidence:

- `viewportService.ts` 已存在；
- `geometry.ts` 已存在；
- `overlayService.ts` 已有 viewport anchor / world-to-viewport seed；
- `useRuntimeLayoutModelController()` 接管 content width / layout model boundary；
- Browser-facing pan/zoom 完整体验未作为 V2.BN.8.1 硬门槛。

Acceptance:

- 当前层的目标是建立 world / viewport seed 和不再把 viewport 逻辑塞回 `NoteDetail.tsx`；
- 这一点已达到。

Remaining:

- 最终 Browser Harness 仍需验证真实 viewport / overlay 行为；
- 完整 pan / zoom 是后续 V2.BN.8.x 或更后版本任务。

## L4 - PageFrame And Workspace

Status: achieved as V2.BN.8.1 seed.

Evidence:

- `pageFrameService.ts` 已存在；
- `engineModel.ts` 构造 runtime model；
- `useRuntimeFrameModelController()` 接管 frame model boundary；
- PageFrame / workspace 相关规则不再属于 `NoteDetail.tsx`。

Acceptance:

- PageFrame 作为 engine model 的 formal frame seed 已建立；
- workspace 与 PageFrame 的概念边界已经进入 engine。

Remaining:

- 多 frame 产品化不在本版本；
- ruler / margin UI 是后续 polish；
- Browser Harness 仍需验证 Page/Canvas 视觉边界。

## L5 - Placement Service

Status: achieved as runtime boundary.

Evidence:

- `placementService.ts` 已存在；
- `runtimeLayout.ts` 已存在；
- `useRuntimeLayoutModelController()` 接管 layout draft / resolved layout boundary；
- `useRuntimePlacementInteractionController()` 接管 move / resize interaction boundary；
- `NoteDetail.tsx` 中不存在 `blockLayouts` / `layoutDrafts` / `normalizeBlockLayout` 等旧 runtime 符号。

Acceptance:

- placement 不再由 `NoteDetail.tsx` 持有；
- move / resize 相关 logic 已进入 canvasEngine hook/service 体系。

Remaining:

- 真正数据库级 placement table / migration 不在本版本；
- rotation 字段与 relation endpoint reserve 是模型预留，不是 UI 完成项。

## L6 - Block Projection Layer

Status: achieved as projection boundary; polish backlog remains.

Evidence:

- `blocks/TextBlockProjection.tsx`；
- `blocks/DefinitionBlockProjection.tsx`；
- `blocks/FormulaBlockProjection.tsx`；
- `blocks/CodeBlockProjection.tsx`；
- `layers/BlockEditorLayer.tsx`；
- `layers/BlockStatusBadgeLayer.tsx`；
- `layers/BlockControlBarLayer.tsx`。

Acceptance:

- `BlockEditor` 不再在 `NoteDetail.tsx` 内联；
- block projection 已归入 canvasEngine；
- structured block field truth 已有 engine-side boundary。

Remaining:

- CodeBlock 更成熟的视觉语言和行级复制 gutter 是后续 polish；
- inline math conversion 是后续 rich text / agent skill 工作；
- FormulaBlock / DefinitionBlock 的细节体验仍需 Browser Harness 与 Henry 手测。

## L7 - Measurement And Reflow Service

Status: achieved as V2.BN.8.1 seed; browser validation pending.

Evidence:

- `measurementService.ts` 已存在；
- `hooks/useBlockMeasurement.ts` 已存在；
- `hooks/useMeasuredBlockReflowController.ts` 已存在；
- `useRuntimeBlockEditingController()` 接管 measured block height reflow composition；
- `useRuntimeBlockOperationsController()` 进一步组合 block editing / reflow / placement interaction。

Acceptance:

- measurement / reflow 不再由 `NoteDetail.tsx` 内联承担；
- active structured block expansion 已有 engine boundary。

Remaining:

- 真实 DOM 下的 Formula input 展开、Definition active、高度避让仍需最终浏览器和人工验收；
- 未通过 Browser Harness 前不能声明体验完成。

## L8 - Interaction Controller

Status: achieved as runtime boundary; some interaction polish remains.

Evidence:

- `interactionController.ts` 已存在；
- `hooks/useRuntimeInteractionController.ts` 已存在；
- `hooks/useRuntimeNaturalWritingController.ts` 已存在；
- `hooks/useRuntimePlacementInteractionController.ts` 已存在；
- `hooks/useRuntimeBlockOperationsController.ts` 将 natural writing、block editing、placement interaction、history 组合起来；
- `NoteDetail.tsx` 中不存在 `beginMoveBlock` / `beginResizeBlock` 等旧符号。

Acceptance:

- selection / drag / resize / natural writing 的一线逻辑已经迁入 canvasEngine controller 体系；
- `useNoteCanvasRuntimeController()` 只做 controller composition。

Remaining:

- Ctrl+Z / Ctrl+Y 真实手感、elastic avoidance、snap on/off 下新 block 落点仍需 Browser Harness / Henry manual pass；
- 多选与更复杂 layout edit mode polish 是后续工作。

## L9 - Floating Overlay Layer

Status: achieved as overlay portal seed; complete overlay engine remains future work.

Evidence:

- `overlayService.ts`；
- `layers/FloatingOverlayLayer.tsx`；
- `layers/SlashMenuLayer.tsx`；
- `layers/ExportPreviewLayer.tsx`；
- `layers/NoteFloatingPanelLayer.tsx`；
- `layers/BlockControlBarLayer.tsx`；
- normalized anchor record 已进入 overlay path。

Acceptance:

- slash menu、block control bar、preview、formula help、insert floating action 已不再作为普通 block flow 的一部分；
- overlay layer 已有 viewport placement helper seed。

Remaining:

- 完整 collision / flip / clamp service 仍是 future；
- 最终 Browser Harness 需验证 overlay z-index / anchor / focus 行为。

## L10 - Page / Canvas Mode Policy

Status: achieved as policy boundary.

Evidence:

- `modePolicyService.ts` 已存在；
- `hooks/useSurfaceModeController.ts` 已存在；
- `hooks/useRuntimeSurfaceStateController.ts` 接管 surface mode / floating overlay / layout refs / selection state；
- `visibleBlocks` 和 blank double-click placement policy 已进入 engine policy path。

Acceptance:

- Page / Canvas mode 的第一批规则不再散落在 `NoteDetail.tsx`；
- workspace visibility、mode label、transition cleanup、blank draft placement 都有 policy seed。

Remaining:

- 完整 pan/zoom transition policy 不在 V2.BN.8.1；
- Canvas mode 最终视觉仍需 Browser Harness 与 Henry manual pass。

## L11 - State Persistence And Undo Boundary

Status: achieved for placement move/resize and create/trash seed; broader undo remains future.

Evidence:

- `historyService.ts` 已存在；
- `hooks/usePlacementHistory.ts` 已存在；
- `hooks/useRuntimeBlockHistoryController.ts` 已存在；
- `useRuntimeBlockOperationsController()` 组合 history 与 block operations。

Acceptance:

- placement move / resize undo-redo 已进入 runtime history boundary；
- create block / toolbar trash 进入 soft-delete / restore history seed；
- `NoteDetail.tsx` 不再持有 placement undo/redo refs。

Remaining:

- inline formula conversion undo、source/relation mutation undo、跨 note history 不在本版本；
- Browser Harness 仍需验证真实键盘快捷键体验。

## L12 - NoteDetail Decommission And Acceptance

Status: code decommission achieved; final acceptance incomplete.

Evidence:

- `NoteDetail.tsx` 当前只包含：
  - `useParams`；
  - `NoteCanvasRuntimeProvider`；
  - `NoteCanvasRuntime`。
- 以下旧 runtime 符号不在 `NoteDetail.tsx` 中出现：
  - `blockLayouts`；
  - `layoutDrafts`；
  - `normalizeBlockLayout`；
  - `beginMoveBlock`；
  - `beginResizeBlock`；
  - `getSlashMenuAnchor`；
  - `SlashMenu`；
  - `BlockEditor`。
- 非浏览器工程验收已通过。

Acceptance:

- 代码层面的 decommission 已达到 plan 要求；
- `V2.BN.8.1` 仍不能关闭，因为 Browser Harness 和 Henry manual pass 是明确硬门槛。

Remaining:

- 最终 Browser Harness smoke；
- Henry 手动确认 passed。

## Backlog Classification

以下项目保留在后续 polish / interaction backlog，不阻塞当前 runtime replacement 代码清退：

- Formula block preview input contract 的完整产品化；
- 正文选区 `Convert to formula`；
- DefinitionBlock Tab focus 细节；
- structured block active measurement 的体验修补；
- snap alignment 下的 draft placement polish；
- CodeBlock visual language 和 line-copy gutter；
- PageFrame ruler UI；
- 完整 pan/zoom；
- multi-frame productization；
- presentation mode；
- drawing tool；
- relation runtime；
- source picker；
- Template Studio；
- GraphRAG adapter。

## Final Gate Checklist

当前不能勾选的项目：

- [ ] Browser Harness smoke passed；
- [ ] Henry manual visual/interaction pass confirmed；
- [ ] `V2.BN.8.1` goal marked complete。

当前已经有证据支持的项目：

- [x] NoteDetail shell achieved；
- [x] Canvas Engine runtime root achieved；
- [x] runtime controller composition boundary achieved；
- [x] non-browser build/check/performance gates passed；
- [x] code replacement evidence recorded in release docs；
- [x] later polish backlog separated from V2.BN.8.1 hard gate。

## Deferred Browser Smoke Recheck - 2026-06-13

Henry 已明确要求 Browser Harness 不再用于中途测试，等全部做完后再统一执行最终 Browser smoke。

本轮重新验证了非浏览器门槛：

- `npm run build:client` passed；
- `npm run build` passed；
- `npm run smoke:canvas-engine-performance` passed；
- `git diff --check` passed；
- `NoteDetail.tsx` 仍然只挂载 `NoteCanvasRuntimeProvider` 和 `NoteCanvasRuntime`；
- 旧 runtime 符号没有回流到 `NoteDetail.tsx`。

结论不变：代码替换证据足够支持 L12 decommission achieved，但 `V2.BN.8.1` 仍必须等待最终 Browser Harness smoke 和 Henry manual pass。
