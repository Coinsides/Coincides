# V2.BN.8.7.9.0 Commit 3 Staging Report

日期：2026-06-23

## 提交结果

Commit 3 已落地：

- `bae7891 feat: advance notebook runtime foundation`

前置提交：

- `7bb7b18 feat: add content group entity backend`
- `0489129 feat: add content group client surfaces`

## 提交边界

Commit 3 的边界是：

> Editor foundation and TextFlow / Annotation / Canvas runtime work.

这个提交收口的是 Better Notebook 写作面基础，而不是 ContentGroup 独立实体本身，也不是 server material/template/domain lane。

## 已提交内容

- Canvas runtime boundary check 与综合 model contract check 更新。
- App layout shell 对 notebook runtime 的配套调整。
- Note writing surface 的 TextFlow-first 编辑能力。
- AnnotationTruth、Annotation display、Annotation hierarchy、Annotation inspector、Annotation overlay。
- SelectionDraft 与 selection range capture。
- Range rebase 与 source-backed annotation range editing。
- TextUnit editor service 与 TextFlow service。
- Context menu、selection toolbar、inline name prompt、text unit gutter。
- Viewport transform、runtime layout、placement/overlay service 调整。
- `DefinitionBlockProjection.tsx` 删除，旧 definition block projection 退场。
- `TextBlockProjection.tsx` 承担更完整的 TextFlow / TextUnit 渲染与编辑入口。

## 明确排除

以下内容没有进入 Commit 3：

- server material/template/domain foundation。
- `shared/types/index.ts` 的模板/材料旧类型清理。
- docs/contracts/release notes/brainstorm evidence。
- ignored generated output，例如 `client/dist`、`server/dist`。

## 验证结果

- `git diff --cached --check`：通过。
- `git diff --check -- client`：通过。
- `npm run check:canvas-runtime-boundary`：通过，35 checks。
- `npm run smoke:canvas-engine-model-contract`：通过，21 groups。
- `npm run build:client`：通过。
- `npm run check:changed-file-secrets`：通过，224 changed files scanned。

已知非阻塞 warning：

- Vite 仍提示 `taskStore` dynamic/static import 混用。
- Vite 仍提示 client bundle chunk 大于 500 kB。
- Git 仍提示本地仓库有较多 unreachable loose objects；本轮没有执行 `git prune`。

## 下一步建议

Commit 4 应收口：

> Server material / template / domain foundation.

建议只收：

- `server/src/__tests__/v2MaterialLibrary.test.ts`
- `server/src/__tests__/v2TemplateMigration.test.ts`
- `server/src/lib/noteBlockTemplates.ts`
- `server/src/services/compositionTemplates.ts`
- `server/src/services/domainPackages.ts`
- `server/src/services/materialReconciliationProposals.ts`
- `server/src/services/organizedNoteProposals.ts`
- `server/src/services/templateDefinitions.ts`
- `shared/types/index.ts` 如果其 diff 主要服务于 template/material 退场。

Commit 5 再收文档、合同、release note、brainstorm 和设计证据。
