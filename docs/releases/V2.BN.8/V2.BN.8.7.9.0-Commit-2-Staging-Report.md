# V2.BN.8.7.9.0 Commit 2 Staging Report

日期：2026-06-23

## 提交结果

Commit 1 已先落地：

- `7bb7b18 feat: add content group entity backend`

Commit 2 已落地：

- `0489129 feat: add content group client surfaces`

## 提交边界

Commit 2 的实际边界被收窄为：

> ContentGroup client services, standalone Gallery / Single Editor surfaces, and Rail shell model.

也就是说，这个提交负责让客户端拥有 ContentGroup System 的前端实体服务、Gallery / Single Editor 页面、三界面 shell contract，以及 Rail 的逻辑组件基础。

它没有把写作面的大型 runtime 集成一起吞进去。

## 已提交内容

- 根级与 client 级 shell check script wiring。
- Group Gallery 页面、数据加载层、shell model、OpenDesign 风格 CSS。
- Single ContentGroup Editor 页面、编辑 draft service、shell model。
- ContentGroup client domain service：
  - entity cutover
  - index
  - metadata bridge
  - relation projection candidate
  - repository
  - reuse semantics
  - surface role service
  - drag payload
- GroupFolder client domain service：
  - entity cutover
  - repository
  - folder graph operations
- Rail 侧逻辑壳：
  - `ContentGroupPanel.tsx`
  - `contentGroupRailShellModel.ts`
- `runtimeDataTypes.ts` 中 ContentGroup / GroupFolder / AnnotationRange 所需类型。

## 明确排除

以下文件没有进入 Commit 2：

- `client/src/pages/Notes/NoteDetail.module.css`
- `client/src/pages/Notes/canvasEngine/layers/NoteWritingSurfaceLayer.tsx`
- `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasDataAdapter.ts`
- `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasRuntimeController.ts`
- `client/src/pages/Notes/canvasEngine/hooks/useNoteCanvasLayerProps.ts`
- `client/src/pages/Notes/canvasEngine/NoteCanvasRuntime.tsx`
- `client/src/pages/Notes/canvasEngine/layers/NoteChromeLayer.tsx`
- `client/scripts/canvasEngineModelContractCheck.ts`
- `client/scripts/canvasRuntimeBoundaryCheck.mjs`
- Annotation / TextFlow / SelectionDraft / Viewport / RangeRebase 相关新文件。

排除原因：

- 这些文件主要承担写作面、Annotation/TextFlow、layout/viewport 或综合 runtime 契约职责。
- Rail 真正挂入写作面需要这些文件，但它们应该进入 Commit 3，避免 Commit 2 被 Canvas/TextFlow 基础工作稀释。
- `canvasEngineModelContractCheck.ts` 已经是综合模型契约检查，不再是单纯 ContentGroup check。

## 验证结果

- `git diff --cached --check`：通过。
- 暂存边界检查：没有混入 `NoteDetail.module.css`、`NoteWritingSurfaceLayer`、`AnnotationInspector`、`canvasEngineModelContractCheck`、`canvasRuntimeBoundaryCheck` 等文件。
- `npm run check:group-gallery-shell`：通过，5 checks。
- `npm run check:groups-rail-shell`：通过。
- `npm run check:single-editor-shell`：通过。
- `npm run build:client`：通过。
- `npm run check:changed-file-secrets`：通过，251 changed files scanned。

已知非阻塞 warning：

- Vite 仍提示 `taskStore` dynamic/static import 混用。
- Vite 仍提示 client bundle chunk 大于 500 kB。
- Git 仍提示本地仓库有较多 unreachable loose objects；本轮没有执行 `git prune`。

## 下一步建议

Commit 3 应收口：

> Editor foundation and TextFlow / Annotation / Canvas runtime work.

这会包括 Rail 真正接入写作面所需的 mixed runtime 文件，以及 Annotation / TextFlow / SelectionDraft / viewport / layout / context menu 等基础工作。

Commit 3 风险比 Commit 2 高，建议继续显式路径 staging，且不要把 server material/template/domain lane 混进去。
