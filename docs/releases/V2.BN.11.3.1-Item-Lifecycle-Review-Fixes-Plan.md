# V2.BN.11.3.1 Plan - Item Lifecycle Review Fixes

> **状态 (Status)**: engineering complete; awaiting third-party re-review
> **日期 (Updated)**: 2026-07-13
> **层 (Layer)**: 补丁计划 / Patch Plan
> **上游 (Input)**: `docs/agent-ops/handoffs/2026-07-13-v2bn11.3-fix-r1-r4.md`

## 1. 目标

关闭 V2.BN.11.3 第三方条件 PASS 中的 R1-R4，不扩张 Item UI、Purpose membership 或 Relation runtime：

1. 空库与 046 升级库都拥有 `idx_content_group_members_item`；
2. Item 只能退役进同用户、当前仍为 active 的直接后继，同时保留独立 cycle 防线；
3. 父 Plan 明确 11.3 起 Item 写路径生成 / 复用 Snapshot，且 Snapshot 仍无独立编辑 API；
4. 相同正文重存必须复用当前 Snapshot，不增加行数。

## 2. RED / GREEN

- RED：fresh `initDb` 后直接检查 Item member 索引；无环 retired successor 必须以 active 约束被拒；
- COVERAGE：相同 `plain_text` 再写一次，锁定 Snapshot id 与总行数均不变；
- GREEN：047 在 member rebuild guard 外执行幂等索引 ensure；successor chain 检查先验证直接后继 active，再独立检查环。

## 3. 退出门

- Item 专项测试通过，且错误消息能区分 inactive successor 与 successor cycle；
- `cd server && npm run test:v2`；
- `npm run verify:v2-bn8-runtime`；
- `npm run check:v2-bn11-legacy-shutdown`；
- `git diff --check` 与 changed-file secret scan；
- 原 handoff 追加 `## Result`，不自签第三方 PASS。
