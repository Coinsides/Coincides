# v2.0 Quality Review - NoteBlock Foundation

**Created**: 2026-05-19
**Branch**: `feat/v2.0-noteblock`
**Review Status**: `PASS`

This file is the engineering quality state machine for v2.0. It records code, test, migration, security, scope, documentation, and engineering follow-up state. Product experience findings belong in `docs/releases/v2.0-experience-review.md`.

---

## Current Review Status

`PASS`

v2.0 Step 1 is accepted as an implemented and verified NoteBlock Foundation. The auth persistence follow-up discovered during browser testing has been committed and pushed.

---

## Code Review

Status: `PASS`

- NoteBlock Foundation was implemented in commit `860b443` (`v2.0 Step 1: NoteBlock foundation`).
- Backend additions are additive: notes, note blocks, placements, sources, projections, and operation batches.
- Existing Card/Deck routes were left in place and not migrated into NoteBlocks.
- A Windows ESM migration import issue was fixed by loading migration files through file URLs.
- `pdf-parse` import behavior was adjusted so the server TypeScript build passes.
- Auth token persistence fix was committed in `db3a0c8` (`fix: persist auth token across reloads`).

---

## Test / Build Review

Status: `PASS`

- `server` v2 behavior tests passed:
  - additive v2 foundation migration tables;
  - ordered note placements;
  - stable projection snapshots after source block edits.
- `server` TypeScript build passed.
- `client` production build passed.
- Browser smoke passed for the core NoteBlock flow:
  - Course Notes entry visible;
  - note editor reachable;
  - paragraph block creation works;
  - inline KaTeX formula rendering works;
  - block trash flow works;
  - note appears from the Course page.
- Known build warnings:
  - existing Vite dynamic-import/static-import warning around `taskStore.ts`;
  - existing bundle-size warning for the main client chunk.

---

## Migration / Data Review

Status: `PASS`

- Migration is additive.
- Existing v1 Card/Deck tables are not rewritten or deleted.
- v2 entities are course-rooted where required.
- Projection records store snapshots and do not silently mutate when source blocks change.

---

## Scope Review

Status: `PASS`

v2.0 stayed inside the NoteBlock Foundation boundary.

Explicitly deferred:

- AI Note Proposal;
- Course Material Library UI;
- Source Snapshot Viewer;
- Source reconciliation and deduplication;
- full Card/Deck migration;
- custom NoteBlock type system;
- drag-and-drop or spatial NoteBlock layout polish.

---

## Security / Secret Review

Status: `PASS`

- No new provider keys or credentials were intentionally introduced by v2.0 work.
- Historical docs should still be treated carefully during future secret scans.
- Auth token persistence now stores the token in browser storage with an in-memory fallback.

---

## Documentation Review

Status: `PASS`

- `docs/releases/v2.0-plan.md` now includes an Implementation State Checklist.
- `docs/releases/v2.0-engineering-spec.md` remains the implementation contract for Step 1.
- `docs/releases/CHANGELOG-v2.0.md` records the Step 1 implementation and verification result.
- `docs/ARCHITECTURE.md` and `docs/DATA_MODEL.md` were aligned during the Step 1 rebase.
- Workflow/review/experience-review system was committed in `43ec09d` (`docs: add bidirectional release feedback system`).

---

## Development Log

- 2026-05-19: Local branch was behind `origin/feat/v2.0-noteblock` by 24 commits. Rebased onto GitHub source of truth.
- 2026-05-19: Rebase conflicts occurred in `docs/ARCHITECTURE.md` and `docs/DATA_MODEL.md`. Resolved by preserving remote document structure and integrating Step 1 implementation notes as scoped sections.
- 2026-05-19: npm cache and Windows filesystem permission issues were worked around by using a repo-local npm cache and Henry-side commands.
- 2026-05-19: Codex shell could not write `.git/index.lock`; Henry performed staging, commit, rebase continuation, and push actions from PowerShell.
- 2026-05-19: Browser smoke found auth/session fragility. Root cause: auth token was memory-only, so refresh or hot reload could lose session and redirect protected pages to login.
- 2026-05-19: Product feedback accepted v2.0 Step 1 as a foundation while deferring NoteBlock UX polish.
- 2026-05-19: Bidirectional feedback system was committed in `43ec09d`.
- 2026-05-19: Auth persistence fix was committed in `db3a0c8`.

---

## Open Engineering Follow-ups

- None blocking v2.0 release.
- Deferred engineering/product follow-ups should be promoted through v2.1+ plans rather than added silently to v2.0.

---

## Henry Authorization Needed

- `RELEASE_APPROVED`: Henry approved v2.0 as the completed NoteBlock Foundation baseline.
