# v2.3.2 Review - Source Scope Selection

**Created**: 2026-05-22
**Updated**: 2026-05-22
**Current Review Status**: `PASS_WITH_FOLLOWUP`

---

## Code Review

Implemented SourceScope as an additive selection layer over SourceSnapshot, SourceAnchor, SourceMaterial, and MaterialSegment.

Key checks:

- SourceScope creation is user/course scoped.
- Archive/restore only changes SourceScope status.
- Jump target resolution reuses SourceSnapshot / SourceAnchor data.
- Proposal creation can receive selected scopes without changing existing course-level fallback behavior.

## Test / Build Review

Passed:

```text
server npm.cmd run test:v2
server npm.cmd run build
client npm.cmd run build
git diff --check
```

`test:v2` result: 40 tests passed.

Client build note: existing Vite warnings remain for chunk size and one mixed static/dynamic import. They are non-blocking for this version.

## Migration / Data Review

Added migration `021_v2_source_scopes.ts` and mirrored it into `server/src/db/schema.sql`.

The migration is additive and does not mutate existing source snapshots, anchors, materials, notes, evidence, or proposals.

## Scope Review

PASS. v2.3.2 stayed limited to Source Scope Selection.

Still out of scope:

- Source Board;
- annotation editing;
- PDF visual rendering;
- Canvas / 2.5D / 3D;
- external source snapshot tools.

## Security / Secret Review

Changed-file scan found no literal secrets.

One expected environment variable name reference was detected in existing AI-provider code:

```text
ANTHROPIC_API_KEY
```

No actual key value was present.

## Documentation Review

Plan, engineering spec, review, experience review, changelog, and v2.x continuity were updated.

## Development Log

- 2026-05-22: v2.3.2 started. Henry requested continuity to keep batch testing obligations visible until later Source Board / Canvas maturity.
- 2026-05-22: Added SourceScope schema, service, API routes, proposal integration, Course Detail scope selection UI, and automated coverage.
- 2026-05-22: Verification passed; Henry subjective acceptance remains deferred to v2.3.x / v2.4-v2.5 batch acceptance.

## Open Engineering Follow-ups

- Browser smoke is still useful when Henry starts v2.3.x batch testing: generate/open snapshot, select page/range scope, create proposal from selected scopes, archive/restore, and jump back.
- `client/tsconfig.tsbuildinfo` cleanup is separate cache hygiene, not part of v2.3.2 product behavior.

## Henry Authorization Needed

- Henry v2.3.x batch acceptance remains pending and deferred.
