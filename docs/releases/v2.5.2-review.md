# v2.5.2 Quality Review

**Current Review Status**: `PASS_WITH_FOLLOWUP`
**Version**: v2.5.2 Composition / Section Template Seed

## Code Review

- Added persistent CompositionTemplate runtime tables and schema mirror.
- Added idempotent system composition seed with six starter compositions.
- Added composition template service functions for list/get/seed/compatibility report.
- Added `composition_template` proposal creation and apply behavior.
- Apply creates new content/projection/history records only.
- Relation blueprints remain suggested metadata and do not create confirmed ObjectRelations.
- Course Detail Canvas Document can select a composition, create a proposal, preview it, and apply/discard through the existing proposal panel.
- Canvas node hide/restore semantics were clarified after smoke: hiding a node is reversible projection state, and re-adding the same Source Board node restores the existing projection instead of creating a duplicate.

## Test / Build Review

- Added v2 tests for migration, seed idempotency, slot-template validation, non-mutating proposal preview, apply output, skipped slots, and relation blueprint safety.
- `server npm.cmd run test:v2` passed: 81 tests, 81 pass.
- `server npm.cmd run build` passed.
- `client npm.cmd run build` passed. Vite reported existing chunk-size/dynamic-import warnings, not build failures.
- `git diff --check` passed with Windows CRLF conversion warnings only.
- Changed-file secret scan passed after excluding validator field-name false positives.

## Migration / Data Review

- Added additive migration `026_v2_composition_templates.ts`.
- Added:
  - `composition_templates`
  - `composition_instances`
  - `composition_instance_slots`
- Existing notes, blocks, source records, templates, canvas nodes, and relations are not destructively migrated.

## Scope Review

In scope:

- CompositionTemplate runtime;
- system seed;
- validator/resolver;
- proposal-first preview/apply/discard;
- Canvas-facing seed UI;
- manual/continuity/graph-native evidence.

Out of scope:

- Composition Studio;
- DomainBlockSet;
- Package Studio;
- rich editor adapter;
- confirmed ObjectRelation creation;
- graph database migration;
- old NoteBlock migration.

## Security / Secret Review

- No real provider calls.
- No new external dependencies.
- No secrets added.

## Documentation Review

- Plan/spec/review/experience/changelog created.
- Agent operating manual updated with CompositionTemplate rules.
- v2.x/general continuity updated with graph-native and manual evidence.

## Graph-Native Migration Evidence

- `CompositionTemplate` is a future graph node candidate.
- `CompositionInstance` is a graph node candidate if applied section usage becomes meaningful to query.
- `CompositionTemplate -> TemplateDefinition` should be evaluated as a future `USES_TEMPLATE` edge.
- `CompositionInstance -> NoteBlock` slot usage is future edge or edge-like property evidence.
- `relation_blueprint` stays design intent and is not graph truth.
- Confirmed semantic relation truth remains `ObjectRelation`.

## Development Log

- 2026-05-23: v2.5.2 opened from R14/R8 guidance after TemplateDefinition runtime and Template Studio seed.
- 2026-05-23: Added CompositionTemplate schema, seeds, service/API, proposal apply, Canvas proposal UI, tests, and docs.
- 2026-05-23: In-app browser smoke confirmed the Canvas `Use composition` entry renders. The currently running local backend was stale and returned old-route 404s, so the composition dropdown stayed empty until the local server is restarted with this build.
- 2026-05-23: After local server restart, browser smoke passed for Course Detail -> Canvas Document -> Use composition -> preview -> apply -> reload. The canvas persisted the applied composition and node count increased from 3 to 6.
- 2026-05-24: Henry identified that `Archive` behaved like hidden state, not deletion, and that re-adding the same Source Board node could duplicate hidden projections. Renamed node action to `Hide`, exposed hidden nodes for restore, and made backend re-add restore existing archived projections.

## Open Engineering Follow-ups

- v2.5.3 should decide how DomainBlockSet and package/domain scoping interact with composition templates.
- v2.5.4 should handle migration proposals for dangerous template/composition changes.
- Future Composition Studio should stay separate from daily Course/Canvas work.

## Henry Authorization Needed

- Henry release/hold decision after desired local smoke.
