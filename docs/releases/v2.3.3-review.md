# v2.3.3 Review - Source Board Seed

**Created**: 2026-05-22
**Updated**: 2026-05-22
**Current Review Status**: `PASS_WITH_FOLLOWUP`

---

## Code Review

Implemented persistent SourceBoard and SourceBoardNode support.

Key checks:

- Multiple active boards are allowed per course.
- Board/node archive and restore are reversible status changes.
- Seeding from SourceScopes is idempotent and does not mutate source rows.
- SourceBoard nodes reference existing source objects rather than copying source truth.
- Proposal creation can resolve `source_board_id` into active SourceScope nodes.

## Test / Build Review

Passed:

```text
server npm.cmd run test:v2
server npm.cmd run build
client npm.cmd run build
git diff --check
```

`test:v2` result: 45 tests passed.

Client build note: existing Vite warnings remain for chunk size and one mixed static/dynamic import. They are non-blocking for this version.

## Migration / Data Review

Added migration `022_v2_source_boards.ts` and mirrored it into `server/src/db/schema.sql`.

The migration is additive and does not mutate existing source snapshots, anchors, scopes, materials, notes, evidence, or proposals.

## Scope Review

PASS. v2.3.3 stayed limited to persistent structured Source Board Seed.

Still out of scope:

- Canvas / 2.5D / 3D;
- visual edges and relationship graph;
- free layout / drag-drop;
- PDF visual rendering;
- annotation editing;
- real-provider default tests.

## Security / Secret Review

Changed-file scan found no literal secrets.

Real local API keys exist in ignored `.env`, but v2.3.3 default verification did not consume or print them.

## Documentation Review

Plan, engineering spec, review, experience review, changelog, and v2.x continuity were updated.

## Development Log

- 2026-05-22: v2.3.3 started. Henry selected persistent multiple Source Boards per Course and deferred real-provider API test design until after v2.3.3.
- 2026-05-22: Added SourceBoard schema, service, API routes, proposal integration, Course Detail Source Board UI, and automated coverage.
- 2026-05-22: Verification passed. Henry subjective acceptance remains deferred to v2.3.x / v2.4-v2.5 batch acceptance.

## Open Engineering Follow-ups

- Design post-v2.3.3 real-provider smoke/eval flow using Anthropic and Voyage first, with Qwen/DashScope after provider integration.
- Browser smoke is still useful during Henry's v2.3.x batch testing: create scopes, create board, seed scopes, open board node, create proposal from board, archive node.

## Henry Authorization Needed

- Henry v2.3.x batch acceptance remains pending and deferred.
