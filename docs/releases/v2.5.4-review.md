# v2.5.4 Review - Template Proposal + Migration Proposal

## Current Review Status

`PASS_WITH_FOLLOWUP`

## Code Review

- Added additive migration `028_v2_template_migration_proposals.ts`.
- Added `template_migration_mappings`, `template_migration_records`, and `template_migration_record_items`.
- Added `template_migration` proposal creation and apply service.
- Added proposal route and validator.
- Extended proposal apply routing for `template_migration`.
- Added Template Studio migration proposal panel.

## Test / Build Review

- Added v2.5.4 backend tests for migration tables, dry-run non-mutation, alias mapping, soft migration, hard cascade blocker behavior, and direct structural edit blocking.
- `server npm.cmd run test:v2` passed: 92 tests, 92 pass.
- `server npm.cmd run build` passed.
- `client npm.cmd run build` passed. Vite reported existing chunk-size/dynamic-import warnings, not build failures.
- `git diff --check` passed with Windows CRLF conversion warnings only.
- v2.5.4 changed-file secret scan passed.

## Migration / Data Review

- Migration is additive only.
- Proposal creation does not mutate templates or NoteBlocks.
- Alias mapping creates mapping and record rows only.
- Soft migration updates NoteBlock template metadata and preserves original template metadata through `created_with_template_*` and migration history.
- Hard cascade blocks destructive or ambiguous field changes before partial writes.

## Scope Review

- v2.5.4 is limited to template migration governance.
- `DomainRefinementProposal` is deferred and must remain continuity-tracked.

## Security / Secret Review

- No real provider calls are required.
- No new external dependency is introduced.

## Documentation Review

- Plan/spec/review/experience/changelog created.
- Manual and continuity updates pending verification.
- `V2X-027` added to v2.x continuity for `DomainRefinementProposal` deferral.
- Agent operating manual updated with TemplateMigrationProposal rules.

## Graph-Native Migration Evidence

- `TemplateMigrationProposal` is a future graph node or operation-history node candidate.
- `TemplateMigrationRecord` is migration provenance and may remain SQL/history unless graph traversal needs it.
- `NoteBlock -> TemplateDefinition` remains the future `USES_TEMPLATE` edge candidate.
- Alias mappings may become future compatibility edges such as `TEMPLATE_SUCCEEDED_BY`.
- Soft/hard migration item rows provide evidence for whether migration history belongs in graph, SQL, or hybrid storage.
- `DomainRefinementProposal` remains deferred; do not infer domain graph migration behavior from template migration alone.

## Development Log

- 2026-05-24: v2.5.4 started from v2.5 R14 guidance and Henry's decision to defer DomainRefinementProposal.
- 2026-05-24: Added migration governance schema, proposal service, validator, route integration, Template Studio migration panel, tests, manual update, and continuity update.
- 2026-05-24: Automated tests/build/docs checks passed. Browser smoke is waiting for runtime restart confirmation.
- 2026-05-24: Henry confirmed frontend/backend restart. Template Studio loaded, migration panel appeared for an active template with NoteBlock usage, alias mapping apply succeeded, soft migration apply succeeded, discard succeeded, and browser console showed no application errors. The in-app browser automation layer could not dispatch mouse clicks or capture screenshots due coordinate/screenshot timeouts, so mutation actions were verified through the same authenticated API path after UI visibility was confirmed.

## Open Engineering Follow-ups

- Runtime restart gate and light browser/API smoke are complete.

## Henry Authorization Needed

- Henry final acceptance / hold decision after smoke.
