# V2.BN.11.2 Review - Migration 047 And ContentGroup Item Membership

> **Status**: engineering complete after V2.BN.11.2.1; awaiting third-party re-review
> **Date**: 2026-07-13
> **Author**: Codex implementation receipt
> **Plan**: `docs/releases/V2.BN.11.2-Migration-047-And-ContentGroup-Item-Membership-Plan.md`
> **Carry-list**: `docs/agent-ops/handoffs/2026-07-13-v2bn11.2-kickoff-carry-list.md`
> **Safety fix**: `docs/releases/V2.BN.11.2.1-Migration-047-Safety-Fix-Review.md`

## 1. Conclusion

V2.BN.11.2 is engineering-complete within its locked boundary. Migration 047 establishes the Item/Relation database floor, lands ContentGroup Package B atomically across database/server/client, removes exactly the six retired support tables, and upgrades Project-reference lifecycle coverage. No Item service, claim flow, Purpose Package A, Relation command, or UI was introduced.

This document is an evidence receipt for independent review. It does not self-issue a third-party PASS.

## 2. Delivered shape

- Added `items`, `item_snapshots`, `item_anchors`, `relations`, and `relation_assessments` in migration 047 and the clean-database baseline.
- Added composite Relation endpoint FKs, snapshot uniqueness, self-loop and undirected-order checks, active-edge partial uniqueness, `affirmed_at NOT NULL`, and nullable origin receipts.
- Rebuilt `content_group_members` with `item_id`, the Item/non-Item coordinate CHECK, full legacy field and timestamp copy, and an Item lookup index.
- Added Package B handling to server DB values, upsert, hydrate, validator, client DTO, normalizer, identity dedupe, and model contract tests.
- Upgraded lifecycle registration from table-only to table + Project-reference column, including `items.origin_course_id` and `source_records.origin_course_id` as `preserve / SET NULL` receipts.
- Removed retired Petal move calls and stale lifecycle entries.

## 3. Migration evidence

Migration 047 relies on the runner's existing per-migration transaction, which wraps each migration body and its `db_migrations` receipt together. The focused suite proves:

1. final schema creation and removal of exactly the retired six-table set;
2. explicit 046-shaped upgrade with preservation of member IDs, order, content, source receipts, metadata, and timestamps;
3. idempotent second startup with zero pending migrations;
4. injected rebuild-copy failure rolls back new tables, retired-table drops, the member rename, and the migration receipt;
5. legal same-Item snapshot endpoints insert successfully and borrowed snapshots fail under SQLite foreign-key enforcement;
6. same-user account deletion cascades the complete Item family, while a cross-user dangling Relation blocks deletion at statement end.

The retired set is exactly:

- `content_group_petal_fragments`
- `content_group_fragments`
- `content_group_petals`
- `canvas_edges`
- `object_relations`
- `relation_layers`

The migration preserves `learning_canvases`, `canvas_nodes`, current visual connectors, and frozen V10 `source_anchors`.

## 4. Package B evidence

- Valid Item membership uses `kind='item'`, carries `item_id`, and forces `target_id` to null.
- Non-Item membership forces `item_id` to null.
- Validator, server write, database row, server hydrate, client DTO, client normalizer, and dedupe identity agree on the same coordinate.
- A malformed null-Item edge remains `kind='item'` and hydrates as `source_sync_status='missing'` with orphaned integrity metadata; it is not coerced to `content_range`.
- Existing ContentGroup paths remain full-replacement compatible and no Petal table is rewritten or revived.

## 5. Verification receipt

| Gate | Result |
| --- | --- |
| Item/Relation focused suite | 8/8 passed |
| ContentGroup + Source lifecycle focused suites | 20/20 passed |
| Current Canvas persistence cutover suite | 47/47 passed |
| Material Library suite after post-047 fixture alignment | 71/71 passed |
| Full server V2 lane | 217/217 passed |
| Canvas engine model contract | 58 groups passed |
| V2.BN.11 legacy shutdown contract | passed |
| Server build | passed |
| Client build | passed |
| `verify:v2-bn8-runtime` | passed |
| `git diff --check` | passed |
| Changed-file secret scan | 21 changed files passed after V2.BN.11.2.1 |

The Material Library cleanup retained active learning-canvas node, layout, viewport, source-board seeding, block insertion, and Composition tests. Seven tests dedicated only to the dropped edge/layer/ObjectRelation runtime were removed; post-047 migration assertions now verify the new final shape.

## 6. Residual risks and review prompts

1. Unmounted legacy relation route modules and historical helper functions still exist physically. The 11.1 shutdown contract proves they are not mounted, 047 removes their tables, and active Canvas/Composition callers were tested without those tables. Their physical deletion remains later closure cleanup, not an 11.2 scope expansion.
2. Cross-user endpoint integrity is enforced by the future Relation service layer rather than a same-user composite database key. The negative account-delete test makes the database failure mode explicit; V2.BN.11.5 must retain user-scoped endpoint validation.

## 7. Independent review request

The reviewer should verify the six-table drop boundary, transaction rollback fixture, composite-FK behavior, Package B full-replacement safety, origin-receipt lifecycle coverage, and the residual hard-delete interaction above. A third-party PASS or requested correction should be recorded separately from this implementation receipt.

## 8. Claude 复核（2026-07-13）— ⚠️ 条件 PASS：一项 pre-commit 必修（R1），修毕复验即闭环

**审法**：migration 047 逐列亲读对照设计 v1.1（我读的,不是转述）+ 四路独立核查（测试取证/Package B 全链/边界与受保护面/携带单符合性）+ 亲跑三套验证。

**大盘全部干净**：

- **047 DDL 亲验与设计 v1.1 逐列全中**（含九项携带单每一条:affirmed_at NOT NULL/双复合 FK+UNIQUE(item_id,id) 靶/双态 CHECK 全坐标/claimed 两列/origin_purpose_id SET NULL/无向规范序/部分唯一活边/互斥 CHECK/六表 DROP 一张不漏且顺序聪明——先落花瓣三表再重建 CGM 避开 FK 纠缠）。唯一超设计项=source_record_id 显式 SET NULL FK（合规细化,V10 收据降级同形,接受）。
- **零 scope creep**（purposes.ts 未触碰,Package A 干净留给 11.4;无任何 Item 服务/路由/UI）;**受保护面逐字节比对全同**（learning_canvases/canvas_nodes/source_anchors/visual connector);petal move 集与注册表条目已摘（11.1 复核 MED 兑现）;**注册表升级正确**（entry 带 column 字段,按 FK 发现,items+source_records 的 origin 列登记 preserve/SET NULL）。
- **复合 FK 实证行为符合预期,D-003 无需重开**（T4:同卡快照可插,借别家卡快照被拒）。
- **亲跑**:test:v2 **217/217**（219→217 账目盘清:+8 新套件 −3 花瓣冻结测试〔表已不存在〕−7 edge/layer/ObjectRelation 运行时测试〔表已落〕,全部合法）;verify:v2-bn8-runtime 全链绿;legacy-shutdown contract 过。

**R1 · [MED,pre-commit 必修] `content_group_members.item_id` 的 ON DELETE SET NULL 是账号级联地雷**：SET NULL 与互斥 CHECK 组合——kind='item' 行上 SET NULL 一触发必违 CHECK。后果:①直接删 item 被 CHECK 拒绝（结果碰巧正确）;②**账号级联删除顺序敏感**——SQLite 不保证级联子表处理顺序,若 items 先于 members 处理,SET NULL 立即触发→CHECK 炸→整个 DELETE FROM users 失败;members 先删则侥幸通过。T5 的 fixture 恰好不含 kind='item' 的 member 行,故现测全绿是假安全（§6.1 自己列了这个交互但没测）。**修法:改 NO ACTION（去 ON DELETE 子句）**——直接删卡同样被拒（语句末检查,与 relations 端点 FK 同哲学）,账号级联顺序无关安全。**为什么必须现在修**:047 未 commit、未进任何已迁移库,此刻改=四行（047/schema.sql/T5 fixture 加一行 item member/设计勘误已由我落）;commit 后再修=migration 048 一整个。**责任声明**:SET NULL 出自概念设计 §2（我的笔）,Codex 忠实实现;雷是设计与核查 M2 补的 CHECK 交互出来的——设计 v1.1 已勘误。
**R2 · [MED-LOW,随 R1 顺手] T3 回滚测试只证了 §3.4 声称四件中的两件**（rename 回滚+六表 DROP 回滚）,floor 新表回滚与 db_migrations 收据回滚未被行使——且 T3 直调 .up() 自包事务未走 runMigrations,收据回滚仅靠 runner 既有事务包裹背书。补一条经 runMigrations 的失败注入断言（items 表不存在+047 收据行不存在）,便宜。
**L1 · [LOW] 回执 §3 措辞**:"migration runner **now** wraps..."——该事务包裹是 2026-03 commit d797038 的既有行为,非本版新交付,回执把存量写成了增量。**L2 · [LOW] RED-first 为"倒置断言+plan 协议"式**,无失败运行存档（与既往版本同标准,记档不究）。

**结论**：除 R1/R2 外 11.2 交付质量高——最高风险小版本把复合 FK 首例、整表重建、注册表升级全部落干净。**流程:Codex 修 R1+R2 → 我复验 → checkpoint commit → 准予 11.3**。本轮我不落 commit,修正窗口保持开启。

## 9. V2.BN.11.2.1 修正回执（Codex · 2026-07-13）

R1、R2 与 L1 已按 `2026-07-13-v2bn11.2-fix-r1-r2.md` 落地。`content_group_members.item_id` 已在 migration 047 与 clean schema 中统一为默认 `NO ACTION`；账号级联 fixture 已加入真实 Item member；T3 已改为通过 `runMigrations` 失败注入，并断言五张 floor 表与 047 receipt 一并回滚。专项 8/8、完整 server V2 lane 217/217、`verify:v2-bn8-runtime` 均通过。独立复验结论仍待 Claude 签发。

## 10. Claude 复验（2026-07-13）— ✅ PASS：条件全部解除，V2.BN.11.2（含 11.2.1）通过，准予 11.3

- **R1 亲验**：047:154 与 schema.sql:1124 的 `item_id TEXT REFERENCES items(id)` 均无 ON DELETE 子句;`PRAGMA foreign_key_list` 断言把 `on_delete='NO ACTION'` 锁进测试（防未来无意回退）;**T5 fixture 现含两行真实 kind='item' 成员**——账号级联全家清零(content_group_members/content_groups/items/snapshots/anchors/relations/assessments 七表归零),SET-NULL-vs-CHECK 交互从"记录的残留风险"变成"被测试锁死的行为"。**超出修正单的正确加量**:还补了"仅被成员引用的卡直接硬删被数据库拒绝且卡存活"的正控断言——NO ACTION 哲学的另一半也进了测试。
- **R2 亲验**：T3 现经 `runMigrations` 注入失败(`assert.rejects(/Migration 047.*failed/)`),失败后断言**全组回滚**:五张 floor 表不存在、047 收据行不存在、item_id 列不存在、无 rename 残留临时表、非法 fixture 行原样存活、六张退役表全部仍在——比修正单要求的三项还多两项。
- **L1 亲验**：§3 措辞改为"relies on the runner's existing per-migration transaction",§6 已移除关闭的 SET NULL 残留风险条。11.2.1 回执 §4 的 RED 收据设计诚实（RED 7/8 唯一失败恰为 FK 动作断言,runner 回滚测试在 RED 期已过——证明 R2 是补覆盖而非借修复混绿）。
- **亲跑**：test:v2 **217/217**;verify:v2-bn8-runtime 全链 + legacy-shutdown contract（见 checkpoint commit 前置记录）。
- **结论**：§8 条件 PASS 的全部条件解除。**V2.BN.11.2 + 11.2.1 正式通过,准予 V2.BN.11.3（Item Lifecycle And Human Claiming）开工**——无新增携带项,按父 plan 11.3 节施工即可;两条既有前瞻提醒随行:①认领入口的存在性属地板(形态从简);②铸卡事务失败注入(plan 11.3 RED 清单已含)。
