# V2.BN.11.3.1 Review - Item Lifecycle Review Fixes

> **状态 (Status)**: engineering complete; awaiting third-party re-review
> **日期 (Updated)**: 2026-07-13
> **层 (Layer)**: 工程回执 / Patch Review
> **Plan**: `docs/releases/V2.BN.11.3.1-Item-Lifecycle-Review-Fixes-Plan.md`
> **Handoff**: `docs/agent-ops/handoffs/2026-07-13-v2bn11.3-fix-r1-r4.md`

## 1. 结论

V2.BN.11.3.1 已关闭第三方条件 PASS 中的 R1-R4，不扩张 Item UI、Purpose membership 或 Relation runtime。本回执只声明工程修正与验证完成，不自签第三方 PASS。

## 2. 修正结果

- **R1**：migration 047 在 `rebuildContentGroupMembers` guard 外幂等 ensure `idx_content_group_members_item`；fresh DB 与 legacy rebuild 两条启动路径分别有索引断言。
- **R2**：退役命令要求直接 successor 属于同用户且仍为 active；inactive successor 与 successor cycle 使用不同错误信息，失败事务保持原 Item active。
- **R3**：父 Plan §3.2 已写入 2026-07-13 更替注。Item create / update / cast 生成或复用 Snapshot，Snapshot 仍无独立编辑 API，Relation create / reaffirm 后续只复用端点快照。
- **R4**：相同 canonical `plain_text` 重存后，`current_snapshot.id` 与 Snapshot 行数均保持不变。

## 3. RED / GREEN

- RED：Item 专项 7 条中 5 条通过、2 条失败；失败分别是 fresh DB 缺 Item member 索引，以及无环 inactive successor 未被拒绝。
- GREEN：Item 专项 7/7 通过；相同内容 no-op 覆盖在 RED 阶段即通过，证明它是既有能力的缺失断言而非隐藏实现缺口。

## 4. 验证

| Gate | Result |
| --- | --- |
| Item focused suite | 7/7 passed |
| Full server V2 lane | 224/224 passed |
| Canvas runtime boundary | 159 checks passed |
| Canvas engine model contract | 58 groups passed |
| Client build | passed |
| Server build | passed |
| Canvas performance smoke | passed (12.04 ms total seed) |
| V2.BN.11 legacy shutdown contract | passed |
| `git diff --check` | passed |
| Changed-file secret scan | 24 files passed after final documentation |

本补丁没有 UI 改动，因此没有重复 V2.BN.11.3 已完成的浏览器旅程。Client build 仍只有既有 dynamic-import 与 chunk-size warning。

## 5. 留档但未扩张

原修正单列出的 fire-and-forget `replaceGroup` rejection、query schema `.strict()` 一致性、独立 Note delete receipt 测试均未借本补丁扩张；它们继续作为后续审查项，不影响 R1-R4 的闭合。

## 6. 第三方复验请求

请复验 fresh / legacy 两种数据库形状、inactive successor 与 cycle 的错误分离、相同正文 Snapshot no-op，以及父 Plan §3.2 的更替口径。复验结论由第三者签发。
