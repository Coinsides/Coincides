# v2.5.6 Review - DomainRefinementProposal Seed

## Current Review Status

`PASS_WITH_FOLLOWUP`

## Code Review

- Added additive migration `030_v2_domain_refinement_proposals.ts`.
- Mirrored v2.5.6 tables in `schema.sql`.
- Added `domainRefinementProposals` service for dry-run, apply, mappings, records, and classifications.
- Added `POST /api/proposals/domain-refinement`.
- Extended proposal apply for `domain_refinement`.
- Added lightweight `GET /api/domain-refinements/records` and `/classifications`.
- Added Template Studio Domain Refinement panel.
- Added v2.5.6 domain refinement tests.

## Test / Build Review

- `server node --import tsx --test src/__tests__/v2DomainRefinement.test.ts` passed: 5 tests, 5 pass.
- `server npm.cmd run test:v2` passed: 104 tests, 104 pass.
- `server npm.cmd run build` passed.
- `client npm.cmd run build` passed. Vite reported existing chunk-size/dynamic-import warnings, not build failures.
- `git diff --check` passed with Windows CRLF conversion warnings only.
- v2.5.6 changed-file secret scan passed; no provider-key or token-shaped values were found.
- Browser smoke passed after Henry restarted frontend/backend.

## Migration / Data Review

- Migration is additive only.
- Proposal creation does not mutate domains, packages, templates, compositions, NoteBlocks, or memberships.
- Alias apply creates mapping and records only.
- Soft migration writes explicit classification records and NoteBlock domain metadata history only for selected NoteBlocks.
- Hard cascade blocks ambiguous split/merge/reclassify requests without explicit mappings.

## Scope Review

- v2.5.6 implements DomainRefinementProposal with full migration depth.
- It does not implement rich editor adapter, GraphDB migration, AI-driven domain research, or marketplace behavior.

## Graph-Native Migration Evidence

- `DomainRefinementProposal` is a future graph operation node candidate.
- `DomainBlockSet` remains a graph node candidate; `domain_key` is not permanent identity.
- `domain_refinement_mappings` are future edges such as `DOMAIN_SUCCEEDED_BY`, `DOMAIN_ALIAS_OF`, `DOMAIN_SPLIT_TO`, `DOMAIN_MERGED_INTO`, or `DOMAIN_FORKED_FROM`.
- `domain_object_classifications` are future classification edges between content/capability objects and domain nodes.
- `domain_refinement_record_items` are provenance/recovery evidence, not knowledge truth by default.
- Package manifest changes are portability boundary evidence for future `.coincides` graph export/import.

## Development Log

- 2026-05-24: v2.5.6 started after v2.5.5 package portability established the recovery boundary needed for domain migration.
- 2026-05-24: Added failing domain refinement migration test first, then implemented migration/schema.
- 2026-05-24: Added service tests for dry-run, alias mapping, soft migration classification history, and hard-cascade blockers.
- 2026-05-24: Added service, route, validator, Template Studio UI, docs, manual, and continuity updates.
- 2026-05-24: Completed restart-gated browser smoke in Template Studio: created/applied alias mapping, created/applied soft migration with one classification sample, and discarded a separate preview without mutation.

## Open Engineering Follow-ups

- Henry final acceptance / hold decision remains pending.

## Henry Authorization Needed

- Henry final acceptance / hold decision.
