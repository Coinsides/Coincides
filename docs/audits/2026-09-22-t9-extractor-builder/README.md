> **状态 (Status)**: frozen（builder 收据；候 HQ 复核与 Henry 翻牌）
> **层 (Layer)**: 审计 / Audit
> **日期 (Updated)**: 2026-09-22
> **权威 (Authoritative)**: 否；仅记录本次代码、测量与验证结果

# T9 规格提取器 · 宋史手册首航

工具位于 `client/scripts/specExtractor/`，入口 `npm run spec:extract -- <specimen.html>`（在 client 目录）。只有工具、合成测试、npm script 与本审计产物；未改产品源码、产品值、依赖声明/锁文件或标本。产品工厂默认值动态读取，17 个源码文件的 SHA-256 随 JSON 保留。

- [机器产物](2026-09-19-song-handbook.spec.json)
- [完整规格报告与 76 行逐项 delta 候拍表](2026-09-19-song-handbook.spec.md)
- [工单 Result](../../agent-ops/handoffs/2026-09-22-v14-t9-spec-extractor-order.md#result)
- 原始 stdout/stderr、退出码与验证摘要：仓库 `.codex-tmp/t9-extractor/`。

## 测量与口径

本次从 851 条 CSS/inline/SVG 声明，枚举 2435 个静态元素，取得 16 个角色组的 1136 个样本、39 种颜色、28 个间距声明聚类、2 个表格/正文比例。H3–H6 无标本样本，保留空组；未归入指定角色的 827 个文字元素不被冒充为正文。色板声明频次与当前元素显式应用频次分列，所有值均有原始值 / 最近产品格点 / 距离。归格只是候选，不自动采用。

标本 CSS 实读 `.book { max-width:920px }`；工单中的 **900 是等效比较目标**，不作为源宽度。标本 `px900 = px × 900/920`；产品 A4 内部纸宽 904，产品 `px900 = logicalPx × 900/904`。行高比、字重、em 字距与表格/正文比不缩放。纸宽是声明基准，未伪称实际盒宽。产品 profile 实际来自当前 A4 物理映射及 0.1px 量化函数，未把 pt 直接当内部 px。

| 项目 | 当前产品（900 等效） | 标本（900 等效） | delta（标本 − 产品） |
| --- | ---: | ---: | ---: |
| 正文字号 | 16.227876px | 15.652174px | -0.575702px |
| 正文行高比 | 1.650307 | 2.06 | +0.409693 |
| H1 字号 | 22.719027px | 32.282609px | +9.563582px |
| H1 行高比 | 1.519032 | 1.35 | -0.169032 |
| H2 字号 | 19.473451px | 19.565217px | +0.091766px |
| H2 行高比 | 1.581544 | 1.35 | -0.231544 |
| 表格 cell 字号 | 13.793695px | 12.717391px | -1.076304px |
| 表格 cell 行高比 | 1.650307 | 1.7 | +0.049693 |
| 表格 cell / 正文 | 0.85 | 0.8125 | -0.0375 |
| 页眉 / 页脚字号 | 11.946903px | 11.25px | -0.696903px |
| 封面 serif 题名字号 | 35.840708px | 86.086957px | +50.246249px |
| 封面题名行高比 | 1.5 | 1.1 | -0.4 |

这张摘要不构成采值建议；完整表逐项保留字号、行高比、字距、字重与字族栈，含 quote/callout/table-caption/sans/serif。封面题名和无封面首页标题带分开；后者在标本无对等角色，明确 null。图注也不借用表格 caption 的产品值。提示框使用正文众数 14.8px/1.95，容器继承的默认 16px 不充当正文。标本 `.figcap` 的 11.5px 被更高优先级 `.page p` 覆盖成 16px，原标本不改。

**级联量**：角色字体声明及继承/相对单位派生、色值与声明/属性槽频次、间距声明聚类、字级比例。

**布局量（未测）**：实际纸盒与内容盒、真实盒距与 margin 折叠、换行/溢出/分页、字体命中和字形尺寸、像素面积。`line-height:normal` 不补数值。无脚本执行、外部字体/样式下载；动态目录、图形和页码内容未测。桌面 light 为统计基线，媒体/交互/伪元素排除项另列。未支持的字号保留 null；声明级间距 var()/相对单位保留原值及未解说明。

## 验证收据

| 验证 | 结果 |
| --- | --- |
| 最小合成自检（自动发现；标本不进测试热路径） | 1 文件 / 1 测试 PASS |
| client 全库 | 239 文件 / 2464 测试 PASS |
| client / server build | PASS |
| server 接线静态检查 | 111/111 wired，0 exempt / 0 unwired |
| 非 git/secrets 验证门 | N=25，全部尝试：24 PASS / 1 FAIL |
| docs:check 的 inventory / glossary 子项 | 首项失败后独立补跑，均 PASS，不重复计 N |
| 首航完整性 | 标本 SHA-256 不变；17 份产品源指纹吻合 |

唯一红项为 `docs:check`：`docs/agent-ops/INDEX.md` 已过期，首批验证在本单 Result/审计文档落笔前即重现。未擅改现状/索引，不把该红项归因于沙箱或报成全绿，留 HQ 处理派生索引并复验。

工具环境红已绕过且未改环境权限：CodeGraph/rg 命令缺失后用本地只读检索；PowerShell 禁止 `npm.ps1` 后使用 `npm.cmd` / 已安装 npm CLI。验证未遇 Python/MinerU 或 npm_execpath 环境阻断。构建既有 chunk 警告留原日志。

按单未执行 git/secrets 两组件、server 全量、浏览器布局测量、真实模型调用、产品落值或主观验收。没有 git 写操作。未新增数据库、工具面、token 或依赖。采纳哪几项继续留 HQ/Henry；builder 完工不代表放行。
