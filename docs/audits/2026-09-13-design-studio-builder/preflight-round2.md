> **状态 (Status)**: active
> **层 (Layer)**: 审计判据 / 二轮停线
> **日期 (Updated)**: 2026-09-13
> **权威 (Authoritative)**: 否；只记录本轮静态核查事实

# 单3二轮：全库验收与合成测试值长度冲突

补遗一已解除设计室预览／应用的适用域问题。本轮接受裁定：设计室是库存管理入口，缩略即预览，应用动作零出现。下述是另一项验收冲突。

## 证据链

| 来源 | 行号 | 现物 |
| --- | ---: | --- |
| `docs/agent-ops/handoffs/2026-09-13-v14-design-studio-shell-order.md` | 43 | client 全库必跑 |
| 同工单 | 47 | 合成测试值（名／邮箱／口令／key）一律 ≤20 字符 |
| 同工单 | 52 | client 全库绿 |
| `client/src/pages/Boards/BoardViewportBookmarks.test.tsx` | 193 | 用例明确测试接受 32 字符书签名 |
| 同测试 | 197 | 断言名字输入框 `maxLength` 为 32 |
| 同测试 | 201 | `enterName('章'.repeat(32))` 实际输入合成书签名 |
| 同测试 | 202–204 | 点击保存、查找该名字对应按钮、断言 POST 一次 |
| `client/package.json` | 10 | `test:unit` 为 `vitest run` |
| `client/vitest.config.ts` | 全文 | 未排除该测试文件或该用例 |

上述源码由根 agent 亲读确认；没有运行该用例，也没有生成 32 字符测试名。它测试的是明确的长度边界，不是可不改变判据而直接缩短的普通样例。若照跑则违反短值铁律；若跳过或改成 ≤20 则改变强制全库验收范围或原有边界覆盖。

## 待裁定

请 HQ 明确本单验收如何处理既有超长合成值用例，例如是否允许将超长边界用例排除并单独申报未验。builder 未自行添加豁免、跳过测试、削弱断言或改写书签契约。

## 停线时的仓库状态

- 本轮已开始写入导航／路由与壳页面，发现冲突后立即停止子任务和测试。两份既有文件 `App.tsx`、`AppLayout.tsx` 已恢复至二轮编辑前镜像，并逐字节验证相等。
- 两份新壳文件未完成，已从业务目录撤回，只保留在系统临时镜像目录中；调色板、套装抽屉和新增测试未落盘。业务净变更为 0，未留下引用缺失组件的路由。
- 镜像目录：`C:/Users/70208/AppData/Local/Temp/ds2-before-a994aff3/`。含编辑前 `App.tsx`、`AppLayout.tsx`、`order.md`，以及撤回的两份未完成壳文件；不作为交付或验收通过证据。
- 测试执行 0，浏览器冒烟 0，隔离数据库创建 0。未运行含禁项的总验证命令。
- git 命令 0、`.git` 访问 0、commit 0、安全类测试 0；未读取 `.env` 或 key 值，未接触用户库。
- 本目录只增加本判据文件，不含构建产物或原始日志。上轮判据保持原样。
