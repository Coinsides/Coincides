/** Current A4 canvas geometry, shared by paper presets and Source publication. */
export const A4_PAGE_GEOMETRY = {
  width: 904,
  height: 1278,
  contentWidth: 760,
  contentInset: { top: 0, right: 72, bottom: 96, left: 72 },
} as const;
